var __defProp = Object.defineProperty;
var __export = (target2, all) => {
  for (var name in all)
    __defProp(target2, name, { get: all[name], enumerable: true });
};

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/accessor.js
function accessor(fn, fields, name) {
  return Object.assign(fn, {
    fields: fields || [],
    fname: name
  });
}
function accessorName(fn) {
  return fn == null ? null : fn.fname;
}
function accessorFields(fn) {
  return fn == null ? null : fn.fields;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/getter.js
function getter(path4) {
  return path4.length === 1 ? get1(path4[0]) : getN(path4);
}
var get1 = (field3) => function(obj) {
  return obj[field3];
};
var getN = (path4) => {
  const len = path4.length;
  return function(obj) {
    for (let i = 0; i < len; ++i) {
      obj = obj[path4[i]];
    }
    return obj;
  };
};

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/error.js
function error(message) {
  throw Error(message);
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/splitAccessPath.js
function splitAccessPath(p) {
  const path4 = [], n = p.length;
  let q = null, b10 = 0, s = "";
  let i;
  let j;
  let c3;
  p = p + "";
  function push() {
    path4.push(s + p.substring(i, j));
    s = "";
    i = j + 1;
  }
  for (i = j = 0; j < n; ++j) {
    c3 = p[j];
    if (c3 === "\\") {
      s += p.substring(i, j++);
      i = j;
    } else if (c3 === q) {
      push();
      q = null;
      b10 = -1;
    } else if (q) {
      continue;
    } else if (i === b10 && c3 === '"') {
      i = j + 1;
      q = c3;
    } else if (i === b10 && c3 === "'") {
      i = j + 1;
      q = c3;
    } else if (c3 === "." && !b10) {
      if (j > i) {
        push();
      } else {
        i = j + 1;
      }
    } else if (c3 === "[") {
      if (j > i)
        push();
      b10 = i = j + 1;
    } else if (c3 === "]") {
      if (!b10)
        error("Access path missing open bracket: " + p);
      if (b10 > 0)
        push();
      b10 = 0;
      i = j + 1;
    }
  }
  if (b10)
    error("Access path missing closing bracket: " + p);
  if (q)
    error("Access path missing closing quote: " + p);
  if (j > i) {
    j++;
    push();
  }
  return path4;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/field.js
function field(field3, name, opt) {
  const path4 = splitAccessPath(field3);
  const fieldName = path4.length === 1 ? path4[0] : field3;
  return accessor((opt && opt.get || getter)(path4), [fieldName], name || fieldName);
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/accessors.js
var id = field("id");
var identity = accessor((_) => _, [], "identity");
var zero = accessor(() => 0, [], "zero");
var one = accessor(() => 1, [], "one");
var truthy = accessor(() => true, [], "true");
var falsy = accessor(() => false, [], "false");

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/interpreter.js
var DisallowedObjectProperties = /* @__PURE__ */ new Set([
  ...Object.getOwnPropertyNames(Object.prototype).filter((name) => typeof Object.prototype[name] === "function"),
  "__proto__",
  "then"
]);

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/logger.js
function log(method2, level, input) {
  const args = [level, ...input];
  console[method2](...args);
}
var None = 0;
var Error2 = 1;
var Warn = 2;
var Info = 3;
var Debug = 4;
function logger_default(_, method2, handler = log) {
  let level = _ || None;
  return {
    level(_2) {
      if (arguments.length) {
        level = +_2;
        return this;
      } else {
        return level;
      }
    },
    error(...args) {
      if (level >= Error2)
        handler(method2 || "error", "ERROR", args);
      return this;
    },
    warn(...args) {
      if (level >= Warn)
        handler(method2 || "warn", "WARN", args);
      return this;
    },
    info(...args) {
      if (level >= Info)
        handler(method2 || "log", "INFO", args);
      return this;
    },
    debug(...args) {
      if (level >= Debug)
        handler(method2 || "log", "DEBUG", args);
      return this;
    }
  };
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/isArray.js
var isArray = Array.isArray;

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/isObject.js
function isObject(_) {
  return _ === Object(_);
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/mergeConfig.js
var isLegalKey = (key2) => key2 !== "__proto__" && key2 !== "constructor" && key2 !== "prototype";
function mergeConfig(...configs) {
  return configs.reduce((out, source4) => {
    for (const key2 in source4) {
      if (key2 === "signals") {
        out.signals = mergeNamed(out.signals, source4.signals);
      } else {
        const r = key2 === "legend" ? { layout: 1 } : key2 === "style" ? true : null;
        writeConfig(out, key2, source4[key2], r);
      }
    }
    return out;
  }, {});
}
function writeConfig(output3, key2, value4, recurse3) {
  if (!isLegalKey(key2))
    return;
  const out = output3;
  let k, o;
  if (isObject(value4) && !isArray(value4)) {
    const valueObj = value4;
    o = isObject(out[key2]) ? out[key2] : out[key2] = {};
    for (k in valueObj) {
      if (recurse3 && (recurse3 === true || recurse3[k])) {
        writeConfig(o, k, valueObj[k]);
      } else if (isLegalKey(k)) {
        o[k] = valueObj[k];
      }
    }
  } else {
    out[key2] = value4;
  }
}
function mergeNamed(a3, b10) {
  if (a3 == null)
    return b10;
  if (b10 == null)
    return a3;
  const map3 = {};
  const out = [];
  function add7(item) {
    if (!map3[item.name]) {
      map3[item.name] = 1;
      out.push(item);
    }
  }
  b10.forEach(add7);
  a3.forEach(add7);
  return out;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/peek.js
function peek(array4) {
  return array4[array4.length - 1];
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/toNumber.js
function toNumber(_) {
  return _ == null || _ === "" ? null : +_;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/transform.js
function isNonEmpty(arr) {
  return arr.length > 0;
}
var exp = (sign3) => (x6) => sign3 * Math.exp(x6);
var log2 = (sign3) => (x6) => Math.log(sign3 * x6);
var symlog = (c3) => (x6) => Math.sign(x6) * Math.log1p(Math.abs(x6 / c3));
var symexp = (c3) => (x6) => Math.sign(x6) * Math.expm1(Math.abs(x6)) * c3;
var pow = (exponent2) => (x6) => x6 < 0 ? -Math.pow(-x6, exponent2) : Math.pow(x6, exponent2);
var coerceNumber = (x6) => toNumber(x6) ?? 0;
var identity2 = (x6) => x6;
function pan(domain4, delta, lift, ground) {
  if (!isNonEmpty(domain4))
    error("Domain array must not be empty");
  const d0 = lift(domain4[0]), d1 = lift(peek(domain4)), dd = (d1 - d0) * delta;
  return [
    ground(d0 - dd),
    ground(d1 - dd)
  ];
}
function panLinear(domain4, delta) {
  return pan(domain4, delta, coerceNumber, identity2);
}
function panLog(domain4, delta) {
  const sign3 = Math.sign(domain4[0]);
  return pan(domain4, delta, log2(sign3), exp(sign3));
}
function panPow(domain4, delta, exponent2) {
  return pan(domain4, delta, pow(exponent2), pow(1 / exponent2));
}
function panSymlog(domain4, delta, constant2) {
  return pan(domain4, delta, symlog(constant2), symexp(constant2));
}
function zoom(domain4, anchor, scale9, lift, ground) {
  if (!isNonEmpty(domain4))
    error("Domain array must not be empty");
  const d0 = lift(domain4[0]), d1 = lift(peek(domain4)), da2 = anchor != null ? lift(anchor) : (d0 + d1) / 2;
  return [
    ground(da2 + (d0 - da2) * scale9),
    ground(da2 + (d1 - da2) * scale9)
  ];
}
function zoomLinear(domain4, anchor, scale9) {
  return zoom(domain4, anchor, scale9, coerceNumber, identity2);
}
function zoomLog(domain4, anchor, scale9) {
  const sign3 = Math.sign(domain4[0]);
  return zoom(domain4, anchor, scale9, log2(sign3), exp(sign3));
}
function zoomPow(domain4, anchor, scale9, exponent2) {
  return zoom(domain4, anchor, scale9, pow(exponent2), pow(1 / exponent2));
}
function zoomSymlog(domain4, anchor, scale9, constant2) {
  return zoom(domain4, anchor, scale9, symlog(constant2), symexp(constant2));
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/quarter.js
function quarter(date2) {
  return 1 + ~~(new Date(date2).getMonth() / 3);
}
function utcquarter(date2) {
  return 1 + ~~(new Date(date2).getUTCMonth() / 3);
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/array.js
function array_default(_) {
  return _ != null ? isArray(_) ? _ : [_] : [];
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/clampRange.js
function clampRange(range7, min4, max4) {
  let lo = range7[0], hi = range7[1], span;
  if (hi < lo) {
    span = hi;
    hi = lo;
    lo = span;
  }
  span = hi - lo;
  return span >= max4 - min4 ? [min4, max4] : [
    lo = Math.min(Math.max(lo, min4), max4 - span),
    lo + span
  ];
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/isFunction.js
function isFunction(value4) {
  return typeof value4 === "function";
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/compare.js
var DESCENDING = "descending";
function compare(inputFields, inputOrders, inputOpt) {
  const opt = inputOpt || {};
  const orders = array_default(inputOrders) || [];
  const ord = [], get7 = [], fmap = {}, gen = opt.comparator || comparator;
  array_default(inputFields).forEach((f, i) => {
    if (f == null)
      return;
    ord.push(orders[i] === DESCENDING ? -1 : 1);
    const accessor2 = isFunction(f) ? f : field(f, void 0, opt);
    get7.push(accessor2);
    (accessorFields(accessor2) || []).forEach((_) => fmap[_] = 1);
  });
  return get7.length === 0 ? null : accessor(gen(get7, ord), Object.keys(fmap));
}
var ascending = (_u2, _v) => {
  let u4 = _u2;
  let v2 = _v;
  return (u4 < v2 || u4 == null) && v2 != null ? -1 : (u4 > v2 || v2 == null) && u4 != null ? 1 : (v2 = v2 instanceof Date ? +v2 : v2, u4 = u4 instanceof Date ? +u4 : u4) !== u4 && v2 === v2 ? -1 : v2 !== v2 && u4 === u4 ? 1 : 0;
};
var comparator = (fields, orders) => fields.length === 1 ? compare1(fields[0], orders[0]) : compareN(fields, orders, fields.length);
var compare1 = (field3, order) => function(a3, b10) {
  return ascending(field3(a3), field3(b10)) * order;
};
var compareN = (fields, orders, n) => {
  orders.push(0);
  return function(a3, b10) {
    let f, c3 = 0, i = -1;
    while (c3 === 0 && ++i < n) {
      f = fields[i];
      c3 = ascending(f(a3), f(b10));
    }
    return c3 * orders[i];
  };
};

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/constant.js
function constant_default(_) {
  return isFunction(_) ? _ : () => _;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/debounce.js
function debounce(delay, handler) {
  let tid = null;
  return (e) => {
    if (tid)
      clearTimeout(tid);
    tid = setTimeout(() => (handler(e), tid = null), delay);
  };
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/extend.js
function extend(_, ...args) {
  for (const arg of args) {
    for (const k in arg) {
      _[k] = arg[k];
    }
  }
  return _;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/extent.js
function extent(array4, f) {
  let i = 0, n;
  let v2;
  let min4;
  let max4;
  if (array4 && (n = array4.length)) {
    if (f == null) {
      for (v2 = array4[i]; i < n && (v2 == null || v2 !== v2); v2 = array4[++i])
        ;
      min4 = max4 = v2;
      for (; i < n; ++i) {
        v2 = array4[i];
        if (v2 != null) {
          if (v2 < min4)
            min4 = v2;
          if (v2 > max4)
            max4 = v2;
        }
      }
    } else {
      for (v2 = f(array4[i]); i < n && (v2 == null || v2 !== v2); v2 = f(array4[++i]))
        ;
      min4 = max4 = v2;
      for (; i < n; ++i) {
        v2 = f(array4[i]);
        if (v2 != null) {
          if (v2 < min4)
            min4 = v2;
          if (v2 > max4)
            max4 = v2;
        }
      }
    }
  }
  return [min4, max4];
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/extentIndex.js
function extentIndex(array4, f) {
  const n = array4.length;
  let i = -1, u4, v2;
  let a3;
  let b10;
  let c3;
  if (f == null) {
    while (++i < n) {
      b10 = array4[i];
      if (b10 != null && b10 >= b10) {
        a3 = c3 = b10;
        break;
      }
    }
    if (i === n)
      return [-1, -1];
    u4 = v2 = i;
    while (++i < n) {
      b10 = array4[i];
      if (b10 != null) {
        if (a3 > b10) {
          a3 = b10;
          u4 = i;
        }
        if (c3 < b10) {
          c3 = b10;
          v2 = i;
        }
      }
    }
  } else {
    while (++i < n) {
      b10 = f(array4[i], i, array4);
      if (b10 != null && b10 >= b10) {
        a3 = c3 = b10;
        break;
      }
    }
    if (i === n)
      return [-1, -1];
    u4 = v2 = i;
    while (++i < n) {
      b10 = f(array4[i], i, array4);
      if (b10 != null) {
        if (a3 > b10) {
          a3 = b10;
          u4 = i;
        }
        if (c3 < b10) {
          c3 = b10;
          v2 = i;
        }
      }
    }
  }
  return [u4, v2];
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/hasOwnProperty.js
function hasOwnProperty(object2, property2) {
  return Object.hasOwn(object2, property2);
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/fastmap.js
var NULL = {};
function fastmap(input) {
  let obj = {}, test2;
  function has2(key2) {
    return hasOwnProperty(obj, key2) && obj[key2] !== NULL;
  }
  const map3 = {
    size: 0,
    empty: 0,
    object: obj,
    has: has2,
    get(key2) {
      return has2(key2) ? obj[key2] : void 0;
    },
    set(key2, value4) {
      if (!has2(key2)) {
        ++map3.size;
        if (obj[key2] === NULL)
          --map3.empty;
      }
      obj[key2] = value4;
      return this;
    },
    delete(key2) {
      if (has2(key2)) {
        --map3.size;
        ++map3.empty;
        obj[key2] = NULL;
      }
      return this;
    },
    clear() {
      map3.size = map3.empty = 0;
      map3.object = obj = {};
    },
    /** See the FastMap interface for the expected return type, which depends on whether a function is provided */
    test(fn) {
      if (arguments.length) {
        test2 = fn;
        return map3;
      } else {
        return test2;
      }
    },
    clean() {
      const next = {};
      let size = 0;
      for (const key2 in obj) {
        const value4 = obj[key2];
        if (value4 !== NULL && (!test2 || !test2(value4))) {
          next[key2] = value4;
          ++size;
        }
      }
      map3.size = size;
      map3.empty = 0;
      map3.object = obj = next;
    }
  };
  if (input)
    Object.keys(input).forEach((key2) => {
      map3.set(key2, input[key2]);
    });
  return map3;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/flush.js
function flush_default(range7, value4, threshold2, left, right, center) {
  if (!threshold2 && threshold2 !== 0)
    return center;
  const t = +threshold2;
  let a3 = range7[0], b10 = peek(range7), l;
  if (b10 === void 0)
    return center;
  if (b10 < a3) {
    l = a3;
    a3 = b10;
    b10 = l;
  }
  l = Math.abs(value4 - a3);
  const r = Math.abs(b10 - value4);
  return l < r && l <= t ? left : r <= t ? right : center;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/inherits.js
function inherits_default(child, parent, members) {
  const proto = child.prototype = Object.create(parent.prototype);
  Object.defineProperty(proto, "constructor", {
    value: child,
    writable: true,
    enumerable: true,
    configurable: true
  });
  return extend(proto, members);
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/inrange.js
function inrange(value4, range7, left, right) {
  let r0 = range7[0], r1 = range7[range7.length - 1], t;
  if (r0 > r1) {
    t = r0;
    r0 = r1;
    r1 = t;
  }
  left = left === void 0 || left;
  right = right === void 0 || right;
  return (left ? r0 <= value4 : r0 < value4) && (right ? value4 <= r1 : value4 < r1);
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/isBoolean.js
function isBoolean(value4) {
  return typeof value4 === "boolean";
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/isDate.js
function isDate(value4) {
  return Object.prototype.toString.call(value4) === "[object Date]";
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/isIterable.js
function isIterable(value4) {
  return value4 != null && isFunction(value4[Symbol.iterator]);
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/isNumber.js
function isNumber(value4) {
  return typeof value4 === "number";
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/isRegExp.js
function isRegExp(value4) {
  return Object.prototype.toString.call(value4) === "[object RegExp]";
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/isString.js
function isString(_) {
  return typeof _ === "string";
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/key.js
function key(fields, flat, opt) {
  const processedFields = fields ? flat ? array_default(fields).map((f) => f.replace(/\\(.)/g, "$1")) : array_default(fields) : void 0;
  const len = processedFields?.length, gen = opt && opt.get || getter, map3 = (f) => gen(flat ? [f] : splitAccessPath(f));
  let fn;
  if (!len || !processedFields) {
    fn = function() {
      return "";
    };
  } else if (len === 1) {
    const get7 = map3(processedFields[0]);
    fn = function(_) {
      return "" + get7(_);
    };
  } else {
    const get7 = processedFields.map(map3);
    fn = function(_) {
      let s = "" + get7[0](_), i = 0;
      while (++i < len)
        s += "|" + get7[i](_);
      return s;
    };
  }
  return accessor(fn, processedFields, "key");
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/lerp.js
function lerp_default(array4, frac) {
  const lo = array4[0], hi = peek(array4), f = +frac;
  if (hi === void 0)
    return lo;
  return !f ? lo : f === 1 ? hi : lo + f * (hi - lo);
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/lruCache.js
var DEFAULT_MAX_SIZE = 1e4;
function lruCache_default(maxsize) {
  maxsize = +maxsize || DEFAULT_MAX_SIZE;
  let curr, prev, size;
  const clear2 = () => {
    curr = {};
    prev = {};
    size = 0;
  };
  const update3 = (key2, value4) => {
    if (++size > maxsize) {
      prev = curr;
      curr = {};
      size = 1;
    }
    return curr[key2] = value4;
  };
  clear2();
  return {
    clear: clear2,
    has: (key2) => hasOwnProperty(curr, key2) || hasOwnProperty(prev, key2),
    get: (key2) => hasOwnProperty(curr, key2) ? curr[key2] : hasOwnProperty(prev, key2) ? update3(key2, prev[key2]) : void 0,
    set: (key2, value4) => hasOwnProperty(curr, key2) ? curr[key2] = value4 : update3(key2, value4)
  };
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/merge.js
function isArrayConstructor(ctor) {
  return ctor === Array || typeof ctor === "function" && ctor.prototype && ArrayBuffer.isView(ctor.prototype);
}
function merge(compare4, array0, array1, output3) {
  const n0 = array0.length, n1 = array1.length;
  if (!n1)
    return array0;
  if (!n0)
    return array1;
  const ctor = array0.constructor;
  const merged = output3 || (isArrayConstructor(ctor) ? new ctor(n0 + n1) : new Array(n0 + n1));
  let i0 = 0, i1 = 0, i = 0;
  for (; i0 < n0 && i1 < n1; ++i) {
    merged[i] = compare4(array0[i0], array1[i1]) > 0 ? array1[i1++] : array0[i0++];
  }
  for (; i0 < n0; ++i0, ++i) {
    merged[i] = array0[i0];
  }
  for (; i1 < n1; ++i1, ++i) {
    merged[i] = array1[i1];
  }
  return merged;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/repeat.js
function repeat(str, reps) {
  let s = "";
  while (--reps >= 0)
    s += str;
  return s;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/pad.js
function pad(str, length3, padchar, align2) {
  const c3 = padchar || " ", s = str + "", n = length3 - s.length;
  return n <= 0 ? s : align2 === "left" ? repeat(c3, n) + s : align2 === "center" ? repeat(c3, ~~(n / 2)) + s + repeat(c3, Math.ceil(n / 2)) : s + repeat(c3, n);
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/span.js
function span_default(array4) {
  return array4 && peek(array4) - array4[0] || 0;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/stringValue.js
function stringValue(x6) {
  return isArray(x6) ? `[${x6.map((v2) => v2 === null ? "null" : stringValue(v2))}]` : isObject(x6) || isString(x6) ? (
    // Output valid JSON and JS source strings.
    // See https://github.com/judofyr/timeless/blob/master/posts/json-isnt-a-javascript-subset.md
    JSON.stringify(x6).replaceAll("\u2028", "\\u2028").replaceAll("\u2029", "\\u2029")
  ) : x6;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/toBoolean.js
function toBoolean(_) {
  return _ == null || _ === "" ? null : !_ || _ === "false" || _ === "0" ? false : !!_;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/toDate.js
var defaultParser = (_) => (
  // Date.parse accepts string but will implicitly coerce the input value.
  // Type assertion allows Date.parse to handle its own coercion behavior.
  isNumber(_) ? _ : isDate(_) ? _ : Date.parse(_)
);
function toDate(_, parser4) {
  parser4 = parser4 || defaultParser;
  return _ == null || _ === "" ? null : parser4(_);
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/toString.js
function toString(_) {
  return _ == null || _ === "" ? null : _ + "";
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/toSet.js
function toSet(_) {
  const s = {}, n = _.length;
  for (let i = 0; i < n; ++i)
    s[_[i] + ""] = true;
  return s;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/truncate.js
function truncate(str, length3, align2, ellipsis) {
  const e = ellipsis != null ? ellipsis : "\u2026", s = str + "", n = s.length, l = Math.max(0, length3 - e.length);
  return n <= length3 ? s : align2 === "left" ? e + s.slice(n - l) : align2 === "center" ? s.slice(0, Math.ceil(l / 2)) + e + s.slice(n - ~~(l / 2)) : s.slice(0, l) + e;
}

// ../deepseek-harness/node_modules/.pnpm/vega-util@2.1.3/node_modules/vega-util/build/visitArray.js
function visitArray(array4, filter4, visitor) {
  if (array4) {
    if (filter4) {
      const n = array4.length;
      for (let i = 0; i < n; ++i) {
        const t = filter4(array4[i]);
        if (t)
          visitor(t, i, array4);
      }
    } else {
      array4.forEach(visitor);
    }
  }
}

// ../deepseek-harness/node_modules/.pnpm/d3-dsv@3.0.1/node_modules/d3-dsv/src/dsv.js
var EOL = {};
var EOF = {};
var QUOTE = 34;
var NEWLINE = 10;
var RETURN = 13;
function objectConverter(columns) {
  return new Function("d", "return {" + columns.map(function(name, i) {
    return JSON.stringify(name) + ": d[" + i + '] || ""';
  }).join(",") + "}");
}
function customConverter(columns, f) {
  var object2 = objectConverter(columns);
  return function(row, i) {
    return f(object2(row), i, columns);
  };
}
function inferColumns(rows) {
  var columnSet = /* @__PURE__ */ Object.create(null), columns = [];
  rows.forEach(function(row) {
    for (var column in row) {
      if (!(column in columnSet)) {
        columns.push(columnSet[column] = column);
      }
    }
  });
  return columns;
}
function pad2(value4, width2) {
  var s = value4 + "", length3 = s.length;
  return length3 < width2 ? new Array(width2 - length3 + 1).join(0) + s : s;
}
function formatYear(year) {
  return year < 0 ? "-" + pad2(-year, 6) : year > 9999 ? "+" + pad2(year, 6) : pad2(year, 4);
}
function formatDate(date2) {
  var hours = date2.getUTCHours(), minutes = date2.getUTCMinutes(), seconds2 = date2.getUTCSeconds(), milliseconds2 = date2.getUTCMilliseconds();
  return isNaN(date2) ? "Invalid Date" : formatYear(date2.getUTCFullYear(), 4) + "-" + pad2(date2.getUTCMonth() + 1, 2) + "-" + pad2(date2.getUTCDate(), 2) + (milliseconds2 ? "T" + pad2(hours, 2) + ":" + pad2(minutes, 2) + ":" + pad2(seconds2, 2) + "." + pad2(milliseconds2, 3) + "Z" : seconds2 ? "T" + pad2(hours, 2) + ":" + pad2(minutes, 2) + ":" + pad2(seconds2, 2) + "Z" : minutes || hours ? "T" + pad2(hours, 2) + ":" + pad2(minutes, 2) + "Z" : "");
}
function dsv_default(delimiter) {
  var reFormat = new RegExp('["' + delimiter + "\n\r]"), DELIMITER = delimiter.charCodeAt(0);
  function parse8(text4, f) {
    var convert, columns, rows = parseRows(text4, function(row, i) {
      if (convert) return convert(row, i - 1);
      columns = row, convert = f ? customConverter(row, f) : objectConverter(row);
    });
    rows.columns = columns || [];
    return rows;
  }
  function parseRows(text4, f) {
    var rows = [], N = text4.length, I = 0, n = 0, t, eof = N <= 0, eol = false;
    if (text4.charCodeAt(N - 1) === NEWLINE) --N;
    if (text4.charCodeAt(N - 1) === RETURN) --N;
    function token() {
      if (eof) return EOF;
      if (eol) return eol = false, EOL;
      var i, j = I, c3;
      if (text4.charCodeAt(j) === QUOTE) {
        while (I++ < N && text4.charCodeAt(I) !== QUOTE || text4.charCodeAt(++I) === QUOTE) ;
        if ((i = I) >= N) eof = true;
        else if ((c3 = text4.charCodeAt(I++)) === NEWLINE) eol = true;
        else if (c3 === RETURN) {
          eol = true;
          if (text4.charCodeAt(I) === NEWLINE) ++I;
        }
        return text4.slice(j + 1, i - 1).replace(/""/g, '"');
      }
      while (I < N) {
        if ((c3 = text4.charCodeAt(i = I++)) === NEWLINE) eol = true;
        else if (c3 === RETURN) {
          eol = true;
          if (text4.charCodeAt(I) === NEWLINE) ++I;
        } else if (c3 !== DELIMITER) continue;
        return text4.slice(j, i);
      }
      return eof = true, text4.slice(j, N);
    }
    while ((t = token()) !== EOF) {
      var row = [];
      while (t !== EOL && t !== EOF) row.push(t), t = token();
      if (f && (row = f(row, n++)) == null) continue;
      rows.push(row);
    }
    return rows;
  }
  function preformatBody(rows, columns) {
    return rows.map(function(row) {
      return columns.map(function(column) {
        return formatValue3(row[column]);
      }).join(delimiter);
    });
  }
  function format6(rows, columns) {
    if (columns == null) columns = inferColumns(rows);
    return [columns.map(formatValue3).join(delimiter)].concat(preformatBody(rows, columns)).join("\n");
  }
  function formatBody(rows, columns) {
    if (columns == null) columns = inferColumns(rows);
    return preformatBody(rows, columns).join("\n");
  }
  function formatRows(rows) {
    return rows.map(formatRow).join("\n");
  }
  function formatRow(row) {
    return row.map(formatValue3).join(delimiter);
  }
  function formatValue3(value4) {
    return value4 == null ? "" : value4 instanceof Date ? formatDate(value4) : reFormat.test(value4 += "") ? '"' + value4.replace(/"/g, '""') + '"' : value4;
  }
  return {
    parse: parse8,
    parseRows,
    format: format6,
    formatBody,
    formatRows,
    formatRow,
    formatValue: formatValue3
  };
}

// ../deepseek-harness/node_modules/.pnpm/topojson-client@3.1.0/node_modules/topojson-client/src/identity.js
function identity_default(x6) {
  return x6;
}

// ../deepseek-harness/node_modules/.pnpm/topojson-client@3.1.0/node_modules/topojson-client/src/transform.js
function transform_default(transform4) {
  if (transform4 == null) return identity_default;
  var x06, y06, kx = transform4.scale[0], ky = transform4.scale[1], dx = transform4.translate[0], dy = transform4.translate[1];
  return function(input, i) {
    if (!i) x06 = y06 = 0;
    var j = 2, n = input.length, output3 = new Array(n);
    output3[0] = (x06 += input[0]) * kx + dx;
    output3[1] = (y06 += input[1]) * ky + dy;
    while (j < n) output3[j] = input[j], ++j;
    return output3;
  };
}

// ../deepseek-harness/node_modules/.pnpm/topojson-client@3.1.0/node_modules/topojson-client/src/reverse.js
function reverse_default(array4, n) {
  var t, j = array4.length, i = j - n;
  while (i < --j) t = array4[i], array4[i++] = array4[j], array4[j] = t;
}

// ../deepseek-harness/node_modules/.pnpm/topojson-client@3.1.0/node_modules/topojson-client/src/feature.js
function feature_default(topology, o) {
  if (typeof o === "string") o = topology.objects[o];
  return o.type === "GeometryCollection" ? { type: "FeatureCollection", features: o.geometries.map(function(o2) {
    return feature(topology, o2);
  }) } : feature(topology, o);
}
function feature(topology, o) {
  var id2 = o.id, bbox = o.bbox, properties = o.properties == null ? {} : o.properties, geometry = object(topology, o);
  return id2 == null && bbox == null ? { type: "Feature", properties, geometry } : bbox == null ? { type: "Feature", id: id2, properties, geometry } : { type: "Feature", id: id2, bbox, properties, geometry };
}
function object(topology, o) {
  var transformPoint = transform_default(topology.transform), arcs = topology.arcs;
  function arc5(i, points2) {
    if (points2.length) points2.pop();
    for (var a3 = arcs[i < 0 ? ~i : i], k = 0, n = a3.length; k < n; ++k) {
      points2.push(transformPoint(a3[k], k));
    }
    if (i < 0) reverse_default(points2, n);
  }
  function point9(p) {
    return transformPoint(p);
  }
  function line5(arcs2) {
    var points2 = [];
    for (var i = 0, n = arcs2.length; i < n; ++i) arc5(arcs2[i], points2);
    if (points2.length < 2) points2.push(points2[0]);
    return points2;
  }
  function ring(arcs2) {
    var points2 = line5(arcs2);
    while (points2.length < 4) points2.push(points2[0]);
    return points2;
  }
  function polygon(arcs2) {
    return arcs2.map(ring);
  }
  function geometry(o2) {
    var type4 = o2.type, coordinates;
    switch (type4) {
      case "GeometryCollection":
        return { type: type4, geometries: o2.geometries.map(geometry) };
      case "Point":
        coordinates = point9(o2.coordinates);
        break;
      case "MultiPoint":
        coordinates = o2.coordinates.map(point9);
        break;
      case "LineString":
        coordinates = line5(o2.arcs);
        break;
      case "MultiLineString":
        coordinates = o2.arcs.map(line5);
        break;
      case "Polygon":
        coordinates = polygon(o2.arcs);
        break;
      case "MultiPolygon":
        coordinates = o2.arcs.map(polygon);
        break;
      default:
        return null;
    }
    return { type: type4, coordinates };
  }
  return geometry(o);
}

// ../deepseek-harness/node_modules/.pnpm/topojson-client@3.1.0/node_modules/topojson-client/src/stitch.js
function stitch_default(topology, arcs) {
  var stitchedArcs = {}, fragmentByStart = {}, fragmentByEnd = {}, fragments = [], emptyIndex = -1;
  arcs.forEach(function(i, j) {
    var arc5 = topology.arcs[i < 0 ? ~i : i], t;
    if (arc5.length < 3 && !arc5[1][0] && !arc5[1][1]) {
      t = arcs[++emptyIndex], arcs[emptyIndex] = i, arcs[j] = t;
    }
  });
  arcs.forEach(function(i) {
    var e = ends(i), start = e[0], end = e[1], f, g;
    if (f = fragmentByEnd[start]) {
      delete fragmentByEnd[f.end];
      f.push(i);
      f.end = end;
      if (g = fragmentByStart[end]) {
        delete fragmentByStart[g.start];
        var fg = g === f ? f : f.concat(g);
        fragmentByStart[fg.start = f.start] = fragmentByEnd[fg.end = g.end] = fg;
      } else {
        fragmentByStart[f.start] = fragmentByEnd[f.end] = f;
      }
    } else if (f = fragmentByStart[end]) {
      delete fragmentByStart[f.start];
      f.unshift(i);
      f.start = start;
      if (g = fragmentByEnd[start]) {
        delete fragmentByEnd[g.end];
        var gf = g === f ? f : g.concat(f);
        fragmentByStart[gf.start = g.start] = fragmentByEnd[gf.end = f.end] = gf;
      } else {
        fragmentByStart[f.start] = fragmentByEnd[f.end] = f;
      }
    } else {
      f = [i];
      fragmentByStart[f.start = start] = fragmentByEnd[f.end = end] = f;
    }
  });
  function ends(i) {
    var arc5 = topology.arcs[i < 0 ? ~i : i], p02 = arc5[0], p1;
    if (topology.transform) p1 = [0, 0], arc5.forEach(function(dp) {
      p1[0] += dp[0], p1[1] += dp[1];
    });
    else p1 = arc5[arc5.length - 1];
    return i < 0 ? [p1, p02] : [p02, p1];
  }
  function flush(fragmentByEnd2, fragmentByStart2) {
    for (var k in fragmentByEnd2) {
      var f = fragmentByEnd2[k];
      delete fragmentByStart2[f.start];
      delete f.start;
      delete f.end;
      f.forEach(function(i) {
        stitchedArcs[i < 0 ? ~i : i] = 1;
      });
      fragments.push(f);
    }
  }
  flush(fragmentByEnd, fragmentByStart);
  flush(fragmentByStart, fragmentByEnd);
  arcs.forEach(function(i) {
    if (!stitchedArcs[i < 0 ? ~i : i]) fragments.push([i]);
  });
  return fragments;
}

// ../deepseek-harness/node_modules/.pnpm/topojson-client@3.1.0/node_modules/topojson-client/src/mesh.js
function mesh_default(topology) {
  return object(topology, meshArcs.apply(this, arguments));
}
function meshArcs(topology, object2, filter4) {
  var arcs, i, n;
  if (arguments.length > 1) arcs = extractArcs(topology, object2, filter4);
  else for (i = 0, arcs = new Array(n = topology.arcs.length); i < n; ++i) arcs[i] = i;
  return { type: "MultiLineString", arcs: stitch_default(topology, arcs) };
}
function extractArcs(topology, object2, filter4) {
  var arcs = [], geomsByArc = [], geom;
  function extract0(i) {
    var j = i < 0 ? ~i : i;
    (geomsByArc[j] || (geomsByArc[j] = [])).push({ i, g: geom });
  }
  function extract1(arcs2) {
    arcs2.forEach(extract0);
  }
  function extract2(arcs2) {
    arcs2.forEach(extract1);
  }
  function extract3(arcs2) {
    arcs2.forEach(extract2);
  }
  function geometry(o) {
    switch (geom = o, o.type) {
      case "GeometryCollection":
        o.geometries.forEach(geometry);
        break;
      case "LineString":
        extract1(o.arcs);
        break;
      case "MultiLineString":
      case "Polygon":
        extract2(o.arcs);
        break;
      case "MultiPolygon":
        extract3(o.arcs);
        break;
    }
  }
  geometry(object2);
  geomsByArc.forEach(filter4 == null ? function(geoms) {
    arcs.push(geoms[0].i);
  } : function(geoms) {
    if (filter4(geoms[0].g, geoms[geoms.length - 1].g)) arcs.push(geoms[0].i);
  });
  return arcs;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/ascending.js
function ascending2(a3, b10) {
  return a3 == null || b10 == null ? NaN : a3 < b10 ? -1 : a3 > b10 ? 1 : a3 >= b10 ? 0 : NaN;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/descending.js
function descending(a3, b10) {
  return a3 == null || b10 == null ? NaN : b10 < a3 ? -1 : b10 > a3 ? 1 : b10 >= a3 ? 0 : NaN;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/bisector.js
function bisector(f) {
  let compare12, compare22, delta;
  if (f.length !== 2) {
    compare12 = ascending2;
    compare22 = (d, x6) => ascending2(f(d), x6);
    delta = (d, x6) => f(d) - x6;
  } else {
    compare12 = f === ascending2 || f === descending ? f : zero2;
    compare22 = f;
    delta = f;
  }
  function left(a3, x6, lo = 0, hi = a3.length) {
    if (lo < hi) {
      if (compare12(x6, x6) !== 0) return hi;
      do {
        const mid = lo + hi >>> 1;
        if (compare22(a3[mid], x6) < 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }
  function right(a3, x6, lo = 0, hi = a3.length) {
    if (lo < hi) {
      if (compare12(x6, x6) !== 0) return hi;
      do {
        const mid = lo + hi >>> 1;
        if (compare22(a3[mid], x6) <= 0) lo = mid + 1;
        else hi = mid;
      } while (lo < hi);
    }
    return lo;
  }
  function center(a3, x6, lo = 0, hi = a3.length) {
    const i = left(a3, x6, lo, hi - 1);
    return i > lo && delta(a3[i - 1], x6) > -delta(a3[i], x6) ? i - 1 : i;
  }
  return { left, center, right };
}
function zero2() {
  return 0;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/number.js
function number(x6) {
  return x6 === null ? NaN : +x6;
}
function* numbers(values4, valueof) {
  if (valueof === void 0) {
    for (let value4 of values4) {
      if (value4 != null && (value4 = +value4) >= value4) {
        yield value4;
      }
    }
  } else {
    let index4 = -1;
    for (let value4 of values4) {
      if ((value4 = valueof(value4, ++index4, values4)) != null && (value4 = +value4) >= value4) {
        yield value4;
      }
    }
  }
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/bisect.js
var ascendingBisect = bisector(ascending2);
var bisectRight = ascendingBisect.right;
var bisectLeft = ascendingBisect.left;
var bisectCenter = bisector(number).center;
var bisect_default2 = bisectRight;

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/variance.js
function variance(values4, valueof) {
  let count2 = 0;
  let delta;
  let mean2 = 0;
  let sum3 = 0;
  if (valueof === void 0) {
    for (let value4 of values4) {
      if (value4 != null && (value4 = +value4) >= value4) {
        delta = value4 - mean2;
        mean2 += delta / ++count2;
        sum3 += delta * (value4 - mean2);
      }
    }
  } else {
    let index4 = -1;
    for (let value4 of values4) {
      if ((value4 = valueof(value4, ++index4, values4)) != null && (value4 = +value4) >= value4) {
        delta = value4 - mean2;
        mean2 += delta / ++count2;
        sum3 += delta * (value4 - mean2);
      }
    }
  }
  if (count2 > 1) return sum3 / (count2 - 1);
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/deviation.js
function deviation(values4, valueof) {
  const v2 = variance(values4, valueof);
  return v2 ? Math.sqrt(v2) : v2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/fsum.js
var Adder = class {
  constructor() {
    this._partials = new Float64Array(32);
    this._n = 0;
  }
  add(x6) {
    const p = this._partials;
    let i = 0;
    for (let j = 0; j < this._n && j < 32; j++) {
      const y6 = p[j], hi = x6 + y6, lo = Math.abs(x6) < Math.abs(y6) ? x6 - (hi - y6) : y6 - (hi - x6);
      if (lo) p[i++] = lo;
      x6 = hi;
    }
    p[i] = x6;
    this._n = i + 1;
    return this;
  }
  valueOf() {
    const p = this._partials;
    let n = this._n, x6, y6, lo, hi = 0;
    if (n > 0) {
      hi = p[--n];
      while (n > 0) {
        x6 = hi;
        y6 = p[--n];
        hi = x6 + y6;
        lo = y6 - (hi - x6);
        if (lo) break;
      }
      if (n > 0 && (lo < 0 && p[n - 1] < 0 || lo > 0 && p[n - 1] > 0)) {
        y6 = lo * 2;
        x6 = hi + y6;
        if (y6 == x6 - hi) hi = x6;
      }
    }
    return hi;
  }
};

// ../deepseek-harness/node_modules/.pnpm/internmap@2.0.3/node_modules/internmap/src/index.js
var InternMap = class extends Map {
  constructor(entries2, key2 = keyof) {
    super();
    Object.defineProperties(this, { _intern: { value: /* @__PURE__ */ new Map() }, _key: { value: key2 } });
    if (entries2 != null) for (const [key3, value4] of entries2) this.set(key3, value4);
  }
  get(key2) {
    return super.get(intern_get(this, key2));
  }
  has(key2) {
    return super.has(intern_get(this, key2));
  }
  set(key2, value4) {
    return super.set(intern_set(this, key2), value4);
  }
  delete(key2) {
    return super.delete(intern_delete(this, key2));
  }
};
var InternSet = class extends Set {
  constructor(values4, key2 = keyof) {
    super();
    Object.defineProperties(this, { _intern: { value: /* @__PURE__ */ new Map() }, _key: { value: key2 } });
    if (values4 != null) for (const value4 of values4) this.add(value4);
  }
  has(value4) {
    return super.has(intern_get(this, value4));
  }
  add(value4) {
    return super.add(intern_set(this, value4));
  }
  delete(value4) {
    return super.delete(intern_delete(this, value4));
  }
};
function intern_get({ _intern, _key }, value4) {
  const key2 = _key(value4);
  return _intern.has(key2) ? _intern.get(key2) : value4;
}
function intern_set({ _intern, _key }, value4) {
  const key2 = _key(value4);
  if (_intern.has(key2)) return _intern.get(key2);
  _intern.set(key2, value4);
  return value4;
}
function intern_delete({ _intern, _key }, value4) {
  const key2 = _key(value4);
  if (_intern.has(key2)) {
    value4 = _intern.get(key2);
    _intern.delete(key2);
  }
  return value4;
}
function keyof(value4) {
  return value4 !== null && typeof value4 === "object" ? value4.valueOf() : value4;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/permute.js
function permute(source4, keys4) {
  return Array.from(keys4, (key2) => source4[key2]);
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/sort.js
function compareDefined(compare4 = ascending2) {
  if (compare4 === ascending2) return ascendingDefined;
  if (typeof compare4 !== "function") throw new TypeError("compare is not a function");
  return (a3, b10) => {
    const x6 = compare4(a3, b10);
    if (x6 || x6 === 0) return x6;
    return (compare4(b10, b10) === 0) - (compare4(a3, a3) === 0);
  };
}
function ascendingDefined(a3, b10) {
  return (a3 == null || !(a3 >= a3)) - (b10 == null || !(b10 >= b10)) || (a3 < b10 ? -1 : a3 > b10 ? 1 : 0);
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/ticks.js
var e10 = Math.sqrt(50);
var e5 = Math.sqrt(10);
var e2 = Math.sqrt(2);
function tickSpec(start, stop2, count2) {
  const step = (stop2 - start) / Math.max(0, count2), power = Math.floor(Math.log10(step)), error3 = step / Math.pow(10, power), factor = error3 >= e10 ? 10 : error3 >= e5 ? 5 : error3 >= e2 ? 2 : 1;
  let i1, i2, inc;
  if (power < 0) {
    inc = Math.pow(10, -power) / factor;
    i1 = Math.round(start * inc);
    i2 = Math.round(stop2 * inc);
    if (i1 / inc < start) ++i1;
    if (i2 / inc > stop2) --i2;
    inc = -inc;
  } else {
    inc = Math.pow(10, power) * factor;
    i1 = Math.round(start / inc);
    i2 = Math.round(stop2 / inc);
    if (i1 * inc < start) ++i1;
    if (i2 * inc > stop2) --i2;
  }
  if (i2 < i1 && 0.5 <= count2 && count2 < 2) return tickSpec(start, stop2, count2 * 2);
  return [i1, i2, inc];
}
function ticks(start, stop2, count2) {
  stop2 = +stop2, start = +start, count2 = +count2;
  if (!(count2 > 0)) return [];
  if (start === stop2) return [start];
  const reverse4 = stop2 < start, [i1, i2, inc] = reverse4 ? tickSpec(stop2, start, count2) : tickSpec(start, stop2, count2);
  if (!(i2 >= i1)) return [];
  const n = i2 - i1 + 1, ticks2 = new Array(n);
  if (reverse4) {
    if (inc < 0) for (let i = 0; i < n; ++i) ticks2[i] = (i2 - i) / -inc;
    else for (let i = 0; i < n; ++i) ticks2[i] = (i2 - i) * inc;
  } else {
    if (inc < 0) for (let i = 0; i < n; ++i) ticks2[i] = (i1 + i) / -inc;
    else for (let i = 0; i < n; ++i) ticks2[i] = (i1 + i) * inc;
  }
  return ticks2;
}
function tickIncrement(start, stop2, count2) {
  stop2 = +stop2, start = +start, count2 = +count2;
  return tickSpec(start, stop2, count2)[2];
}
function tickStep(start, stop2, count2) {
  stop2 = +stop2, start = +start, count2 = +count2;
  const reverse4 = stop2 < start, inc = reverse4 ? tickIncrement(stop2, start, count2) : tickIncrement(start, stop2, count2);
  return (reverse4 ? -1 : 1) * (inc < 0 ? 1 / -inc : inc);
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/max.js
function max(values4, valueof) {
  let max4;
  if (valueof === void 0) {
    for (const value4 of values4) {
      if (value4 != null && (max4 < value4 || max4 === void 0 && value4 >= value4)) {
        max4 = value4;
      }
    }
  } else {
    let index4 = -1;
    for (let value4 of values4) {
      if ((value4 = valueof(value4, ++index4, values4)) != null && (max4 < value4 || max4 === void 0 && value4 >= value4)) {
        max4 = value4;
      }
    }
  }
  return max4;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/min.js
function min(values4, valueof) {
  let min4;
  if (valueof === void 0) {
    for (const value4 of values4) {
      if (value4 != null && (min4 > value4 || min4 === void 0 && value4 >= value4)) {
        min4 = value4;
      }
    }
  } else {
    let index4 = -1;
    for (let value4 of values4) {
      if ((value4 = valueof(value4, ++index4, values4)) != null && (min4 > value4 || min4 === void 0 && value4 >= value4)) {
        min4 = value4;
      }
    }
  }
  return min4;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/quickselect.js
function quickselect(array4, k, left = 0, right = Infinity, compare4) {
  k = Math.floor(k);
  left = Math.floor(Math.max(0, left));
  right = Math.floor(Math.min(array4.length - 1, right));
  if (!(left <= k && k <= right)) return array4;
  compare4 = compare4 === void 0 ? ascendingDefined : compareDefined(compare4);
  while (right > left) {
    if (right - left > 600) {
      const n = right - left + 1;
      const m3 = k - left + 1;
      const z = Math.log(n);
      const s = 0.5 * Math.exp(2 * z / 3);
      const sd = 0.5 * Math.sqrt(z * s * (n - s) / n) * (m3 - n / 2 < 0 ? -1 : 1);
      const newLeft = Math.max(left, Math.floor(k - m3 * s / n + sd));
      const newRight = Math.min(right, Math.floor(k + (n - m3) * s / n + sd));
      quickselect(array4, k, newLeft, newRight, compare4);
    }
    const t = array4[k];
    let i = left;
    let j = right;
    swap(array4, left, k);
    if (compare4(array4[right], t) > 0) swap(array4, left, right);
    while (i < j) {
      swap(array4, i, j), ++i, --j;
      while (compare4(array4[i], t) < 0) ++i;
      while (compare4(array4[j], t) > 0) --j;
    }
    if (compare4(array4[left], t) === 0) swap(array4, left, j);
    else ++j, swap(array4, j, right);
    if (j <= k) left = j + 1;
    if (k <= j) right = j - 1;
  }
  return array4;
}
function swap(array4, i, j) {
  const t = array4[i];
  array4[i] = array4[j];
  array4[j] = t;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/quantile.js
function quantile(values4, p, valueof) {
  values4 = Float64Array.from(numbers(values4, valueof));
  if (!(n = values4.length) || isNaN(p = +p)) return;
  if (p <= 0 || n < 2) return min(values4);
  if (p >= 1) return max(values4);
  var n, i = (n - 1) * p, i0 = Math.floor(i), value0 = max(quickselect(values4, i0).subarray(0, i0 + 1)), value1 = min(values4.subarray(i0 + 1));
  return value0 + (value1 - value0) * (i - i0);
}
function quantileSorted(values4, p, valueof = number) {
  if (!(n = values4.length) || isNaN(p = +p)) return;
  if (p <= 0 || n < 2) return +valueof(values4[0], 0, values4);
  if (p >= 1) return +valueof(values4[n - 1], n - 1, values4);
  var n, i = (n - 1) * p, i0 = Math.floor(i), value0 = +valueof(values4[i0], i0, values4), value1 = +valueof(values4[i0 + 1], i0 + 1, values4);
  return value0 + (value1 - value0) * (i - i0);
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/mean.js
function mean(values4, valueof) {
  let count2 = 0;
  let sum3 = 0;
  if (valueof === void 0) {
    for (let value4 of values4) {
      if (value4 != null && (value4 = +value4) >= value4) {
        ++count2, sum3 += value4;
      }
    }
  } else {
    let index4 = -1;
    for (let value4 of values4) {
      if ((value4 = valueof(value4, ++index4, values4)) != null && (value4 = +value4) >= value4) {
        ++count2, sum3 += value4;
      }
    }
  }
  if (count2) return sum3 / count2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/median.js
function median(values4, valueof) {
  return quantile(values4, 0.5, valueof);
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/merge.js
function* flatten(arrays) {
  for (const array4 of arrays) {
    yield* array4;
  }
}
function merge2(arrays) {
  return Array.from(flatten(arrays));
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/range.js
function range(start, stop2, step) {
  start = +start, stop2 = +stop2, step = (n = arguments.length) < 2 ? (stop2 = start, start = 0, 1) : n < 3 ? 1 : +step;
  var i = -1, n = Math.max(0, Math.ceil((stop2 - start) / step)) | 0, range7 = new Array(n);
  while (++i < n) {
    range7[i] = start + i * step;
  }
  return range7;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/sum.js
function sum(values4, valueof) {
  let sum3 = 0;
  if (valueof === void 0) {
    for (let value4 of values4) {
      if (value4 = +value4) {
        sum3 += value4;
      }
    }
  } else {
    let index4 = -1;
    for (let value4 of values4) {
      if (value4 = +valueof(value4, ++index4, values4)) {
        sum3 += value4;
      }
    }
  }
  return sum3;
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/intersection.js
function intersection(values4, ...others) {
  values4 = new InternSet(values4);
  others = others.map(set);
  out: for (const value4 of values4) {
    for (const other of others) {
      if (!other.has(value4)) {
        values4.delete(value4);
        continue out;
      }
    }
  }
  return values4;
}
function set(values4) {
  return values4 instanceof InternSet ? values4 : new InternSet(values4);
}

// ../deepseek-harness/node_modules/.pnpm/d3-array@3.2.4/node_modules/d3-array/src/union.js
function union(...others) {
  const set7 = new InternSet();
  for (const other of others) {
    for (const o of other) {
      set7.add(o);
    }
  }
  return set7;
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/formatDecimal.js
function formatDecimal_default(x6) {
  return Math.abs(x6 = Math.round(x6)) >= 1e21 ? x6.toLocaleString("en").replace(/,/g, "") : x6.toString(10);
}
function formatDecimalParts(x6, p) {
  if (!isFinite(x6) || x6 === 0) return null;
  var i = (x6 = p ? x6.toExponential(p - 1) : x6.toExponential()).indexOf("e"), coefficient = x6.slice(0, i);
  return [
    coefficient.length > 1 ? coefficient[0] + coefficient.slice(2) : coefficient,
    +x6.slice(i + 1)
  ];
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/exponent.js
function exponent_default(x6) {
  return x6 = formatDecimalParts(Math.abs(x6)), x6 ? x6[1] : NaN;
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/formatGroup.js
function formatGroup_default(grouping, thousands) {
  return function(value4, width2) {
    var i = value4.length, t = [], j = 0, g = grouping[0], length3 = 0;
    while (i > 0 && g > 0) {
      if (length3 + g + 1 > width2) g = Math.max(1, width2 - length3);
      t.push(value4.substring(i -= g, i + g));
      if ((length3 += g + 1) > width2) break;
      g = grouping[j = (j + 1) % grouping.length];
    }
    return t.reverse().join(thousands);
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/formatNumerals.js
function formatNumerals_default(numerals) {
  return function(value4) {
    return value4.replace(/[0-9]/g, function(i) {
      return numerals[+i];
    });
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/formatSpecifier.js
var re = /^(?:(.)?([<>=^]))?([+\-( ])?([$#])?(0)?(\d+)?(,)?(\.\d+)?(~)?([a-z%])?$/i;
function formatSpecifier(specifier) {
  if (!(match3 = re.exec(specifier))) throw new Error("invalid format: " + specifier);
  var match3;
  return new FormatSpecifier({
    fill: match3[1],
    align: match3[2],
    sign: match3[3],
    symbol: match3[4],
    zero: match3[5],
    width: match3[6],
    comma: match3[7],
    precision: match3[8] && match3[8].slice(1),
    trim: match3[9],
    type: match3[10]
  });
}
formatSpecifier.prototype = FormatSpecifier.prototype;
function FormatSpecifier(specifier) {
  this.fill = specifier.fill === void 0 ? " " : specifier.fill + "";
  this.align = specifier.align === void 0 ? ">" : specifier.align + "";
  this.sign = specifier.sign === void 0 ? "-" : specifier.sign + "";
  this.symbol = specifier.symbol === void 0 ? "" : specifier.symbol + "";
  this.zero = !!specifier.zero;
  this.width = specifier.width === void 0 ? void 0 : +specifier.width;
  this.comma = !!specifier.comma;
  this.precision = specifier.precision === void 0 ? void 0 : +specifier.precision;
  this.trim = !!specifier.trim;
  this.type = specifier.type === void 0 ? "" : specifier.type + "";
}
FormatSpecifier.prototype.toString = function() {
  return this.fill + this.align + this.sign + this.symbol + (this.zero ? "0" : "") + (this.width === void 0 ? "" : Math.max(1, this.width | 0)) + (this.comma ? "," : "") + (this.precision === void 0 ? "" : "." + Math.max(0, this.precision | 0)) + (this.trim ? "~" : "") + this.type;
};

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/formatTrim.js
function formatTrim_default(s) {
  out: for (var n = s.length, i = 1, i0 = -1, i1; i < n; ++i) {
    switch (s[i]) {
      case ".":
        i0 = i1 = i;
        break;
      case "0":
        if (i0 === 0) i0 = i;
        i1 = i;
        break;
      default:
        if (!+s[i]) break out;
        if (i0 > 0) i0 = 0;
        break;
    }
  }
  return i0 > 0 ? s.slice(0, i0) + s.slice(i1 + 1) : s;
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/formatPrefixAuto.js
var prefixExponent;
function formatPrefixAuto_default(x6, p) {
  var d = formatDecimalParts(x6, p);
  if (!d) return prefixExponent = void 0, x6.toPrecision(p);
  var coefficient = d[0], exponent2 = d[1], i = exponent2 - (prefixExponent = Math.max(-8, Math.min(8, Math.floor(exponent2 / 3))) * 3) + 1, n = coefficient.length;
  return i === n ? coefficient : i > n ? coefficient + new Array(i - n + 1).join("0") : i > 0 ? coefficient.slice(0, i) + "." + coefficient.slice(i) : "0." + new Array(1 - i).join("0") + formatDecimalParts(x6, Math.max(0, p + i - 1))[0];
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/formatRounded.js
function formatRounded_default(x6, p) {
  var d = formatDecimalParts(x6, p);
  if (!d) return x6 + "";
  var coefficient = d[0], exponent2 = d[1];
  return exponent2 < 0 ? "0." + new Array(-exponent2).join("0") + coefficient : coefficient.length > exponent2 + 1 ? coefficient.slice(0, exponent2 + 1) + "." + coefficient.slice(exponent2 + 1) : coefficient + new Array(exponent2 - coefficient.length + 2).join("0");
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/formatTypes.js
var formatTypes_default = {
  "%": (x6, p) => (x6 * 100).toFixed(p),
  "b": (x6) => Math.round(x6).toString(2),
  "c": (x6) => x6 + "",
  "d": formatDecimal_default,
  "e": (x6, p) => x6.toExponential(p),
  "f": (x6, p) => x6.toFixed(p),
  "g": (x6, p) => x6.toPrecision(p),
  "o": (x6) => Math.round(x6).toString(8),
  "p": (x6, p) => formatRounded_default(x6 * 100, p),
  "r": formatRounded_default,
  "s": formatPrefixAuto_default,
  "X": (x6) => Math.round(x6).toString(16).toUpperCase(),
  "x": (x6) => Math.round(x6).toString(16)
};

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/identity.js
function identity_default2(x6) {
  return x6;
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/locale.js
var map = Array.prototype.map;
var prefixes = ["y", "z", "a", "f", "p", "n", "\xB5", "m", "", "k", "M", "G", "T", "P", "E", "Z", "Y"];
function locale_default(locale4) {
  var group3 = locale4.grouping === void 0 || locale4.thousands === void 0 ? identity_default2 : formatGroup_default(map.call(locale4.grouping, Number), locale4.thousands + ""), currencyPrefix = locale4.currency === void 0 ? "" : locale4.currency[0] + "", currencySuffix = locale4.currency === void 0 ? "" : locale4.currency[1] + "", decimal = locale4.decimal === void 0 ? "." : locale4.decimal + "", numerals = locale4.numerals === void 0 ? identity_default2 : formatNumerals_default(map.call(locale4.numerals, String)), percent = locale4.percent === void 0 ? "%" : locale4.percent + "", minus = locale4.minus === void 0 ? "\u2212" : locale4.minus + "", nan = locale4.nan === void 0 ? "NaN" : locale4.nan + "";
  function newFormat(specifier, options) {
    specifier = formatSpecifier(specifier);
    var fill3 = specifier.fill, align2 = specifier.align, sign3 = specifier.sign, symbol3 = specifier.symbol, zero6 = specifier.zero, width2 = specifier.width, comma = specifier.comma, precision = specifier.precision, trim = specifier.trim, type4 = specifier.type;
    if (type4 === "n") comma = true, type4 = "g";
    else if (!formatTypes_default[type4]) precision === void 0 && (precision = 12), trim = true, type4 = "g";
    if (zero6 || fill3 === "0" && align2 === "=") zero6 = true, fill3 = "0", align2 = "=";
    var prefix = (options && options.prefix !== void 0 ? options.prefix : "") + (symbol3 === "$" ? currencyPrefix : symbol3 === "#" && /[boxX]/.test(type4) ? "0" + type4.toLowerCase() : ""), suffix = (symbol3 === "$" ? currencySuffix : /[%p]/.test(type4) ? percent : "") + (options && options.suffix !== void 0 ? options.suffix : "");
    var formatType = formatTypes_default[type4], maybeSuffix = /[defgprs%]/.test(type4);
    precision = precision === void 0 ? 6 : /[gprs]/.test(type4) ? Math.max(1, Math.min(21, precision)) : Math.max(0, Math.min(20, precision));
    function format6(value4) {
      var valuePrefix = prefix, valueSuffix = suffix, i, n, c3;
      if (type4 === "c") {
        valueSuffix = formatType(value4) + valueSuffix;
        value4 = "";
      } else {
        value4 = +value4;
        var valueNegative = value4 < 0 || 1 / value4 < 0;
        value4 = isNaN(value4) ? nan : formatType(Math.abs(value4), precision);
        if (trim) value4 = formatTrim_default(value4);
        if (valueNegative && +value4 === 0 && sign3 !== "+") valueNegative = false;
        valuePrefix = (valueNegative ? sign3 === "(" ? sign3 : minus : sign3 === "-" || sign3 === "(" ? "" : sign3) + valuePrefix;
        valueSuffix = (type4 === "s" && !isNaN(value4) && prefixExponent !== void 0 ? prefixes[8 + prefixExponent / 3] : "") + valueSuffix + (valueNegative && sign3 === "(" ? ")" : "");
        if (maybeSuffix) {
          i = -1, n = value4.length;
          while (++i < n) {
            if (c3 = value4.charCodeAt(i), 48 > c3 || c3 > 57) {
              valueSuffix = (c3 === 46 ? decimal + value4.slice(i + 1) : value4.slice(i)) + valueSuffix;
              value4 = value4.slice(0, i);
              break;
            }
          }
        }
      }
      if (comma && !zero6) value4 = group3(value4, Infinity);
      var length3 = valuePrefix.length + value4.length + valueSuffix.length, padding3 = length3 < width2 ? new Array(width2 - length3 + 1).join(fill3) : "";
      if (comma && zero6) value4 = group3(padding3 + value4, padding3.length ? width2 - valueSuffix.length : Infinity), padding3 = "";
      switch (align2) {
        case "<":
          value4 = valuePrefix + value4 + valueSuffix + padding3;
          break;
        case "=":
          value4 = valuePrefix + padding3 + value4 + valueSuffix;
          break;
        case "^":
          value4 = padding3.slice(0, length3 = padding3.length >> 1) + valuePrefix + value4 + valueSuffix + padding3.slice(length3);
          break;
        default:
          value4 = padding3 + valuePrefix + value4 + valueSuffix;
          break;
      }
      return numerals(value4);
    }
    format6.toString = function() {
      return specifier + "";
    };
    return format6;
  }
  function formatPrefix2(specifier, value4) {
    var e = Math.max(-8, Math.min(8, Math.floor(exponent_default(value4) / 3))) * 3, k = Math.pow(10, -e), f = newFormat((specifier = formatSpecifier(specifier), specifier.type = "f", specifier), { suffix: prefixes[8 + e / 3] });
    return function(value5) {
      return f(k * value5);
    };
  }
  return {
    format: newFormat,
    formatPrefix: formatPrefix2
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/defaultLocale.js
var locale;
var format;
var formatPrefix;
defaultLocale({
  thousands: ",",
  grouping: [3],
  currency: ["$", ""]
});
function defaultLocale(definition3) {
  locale = locale_default(definition3);
  format = locale.format;
  formatPrefix = locale.formatPrefix;
  return locale;
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/precisionFixed.js
function precisionFixed_default(step) {
  return Math.max(0, -exponent_default(Math.abs(step)));
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/precisionPrefix.js
function precisionPrefix_default(step, value4) {
  return Math.max(0, Math.max(-8, Math.min(8, Math.floor(exponent_default(value4) / 3))) * 3 - exponent_default(Math.abs(step)));
}

// ../deepseek-harness/node_modules/.pnpm/d3-format@3.1.2/node_modules/d3-format/src/precisionRound.js
function precisionRound_default(step, max4) {
  step = Math.abs(step), max4 = Math.abs(max4) - step;
  return Math.max(0, exponent_default(max4) - exponent_default(step)) + 1;
}

// ../deepseek-harness/node_modules/.pnpm/d3-time@3.1.0/node_modules/d3-time/src/interval.js
var t0 = /* @__PURE__ */ new Date();
var t1 = /* @__PURE__ */ new Date();
function timeInterval(floori, offseti, count2, field3) {
  function interval3(date2) {
    return floori(date2 = arguments.length === 0 ? /* @__PURE__ */ new Date() : /* @__PURE__ */ new Date(+date2)), date2;
  }
  interval3.floor = (date2) => {
    return floori(date2 = /* @__PURE__ */ new Date(+date2)), date2;
  };
  interval3.ceil = (date2) => {
    return floori(date2 = new Date(date2 - 1)), offseti(date2, 1), floori(date2), date2;
  };
  interval3.round = (date2) => {
    const d0 = interval3(date2), d1 = interval3.ceil(date2);
    return date2 - d0 < d1 - date2 ? d0 : d1;
  };
  interval3.offset = (date2, step) => {
    return offseti(date2 = /* @__PURE__ */ new Date(+date2), step == null ? 1 : Math.floor(step)), date2;
  };
  interval3.range = (start, stop2, step) => {
    const range7 = [];
    start = interval3.ceil(start);
    step = step == null ? 1 : Math.floor(step);
    if (!(start < stop2) || !(step > 0)) return range7;
    let previous;
    do
      range7.push(previous = /* @__PURE__ */ new Date(+start)), offseti(start, step), floori(start);
    while (previous < start && start < stop2);
    return range7;
  };
  interval3.filter = (test2) => {
    return timeInterval((date2) => {
      if (date2 >= date2) while (floori(date2), !test2(date2)) date2.setTime(date2 - 1);
    }, (date2, step) => {
      if (date2 >= date2) {
        if (step < 0) while (++step <= 0) {
          while (offseti(date2, -1), !test2(date2)) {
          }
        }
        else while (--step >= 0) {
          while (offseti(date2, 1), !test2(date2)) {
          }
        }
      }
    });
  };
  if (count2) {
    interval3.count = (start, end) => {
      t0.setTime(+start), t1.setTime(+end);
      floori(t0), floori(t1);
      return Math.floor(count2(t0, t1));
    };
    interval3.every = (step) => {
      step = Math.floor(step);
      return !isFinite(step) || !(step > 0) ? null : !(step > 1) ? interval3 : interval3.filter(field3 ? (d) => field3(d) % step === 0 : (d) => interval3.count(0, d) % step === 0);
    };
  }
  return interval3;
}

// ../deepseek-harness/node_modules/.pnpm/d3-time@3.1.0/node_modules/d3-time/src/millisecond.js
var millisecond = timeInterval(() => {
}, (date2, step) => {
  date2.setTime(+date2 + step);
}, (start, end) => {
  return end - start;
});
millisecond.every = (k) => {
  k = Math.floor(k);
  if (!isFinite(k) || !(k > 0)) return null;
  if (!(k > 1)) return millisecond;
  return timeInterval((date2) => {
    date2.setTime(Math.floor(date2 / k) * k);
  }, (date2, step) => {
    date2.setTime(+date2 + step * k);
  }, (start, end) => {
    return (end - start) / k;
  });
};
var milliseconds = millisecond.range;

// ../deepseek-harness/node_modules/.pnpm/d3-time@3.1.0/node_modules/d3-time/src/duration.js
var durationSecond = 1e3;
var durationMinute = durationSecond * 60;
var durationHour = durationMinute * 60;
var durationDay = durationHour * 24;
var durationWeek = durationDay * 7;
var durationMonth = durationDay * 30;
var durationYear = durationDay * 365;

// ../deepseek-harness/node_modules/.pnpm/d3-time@3.1.0/node_modules/d3-time/src/second.js
var second = timeInterval((date2) => {
  date2.setTime(date2 - date2.getMilliseconds());
}, (date2, step) => {
  date2.setTime(+date2 + step * durationSecond);
}, (start, end) => {
  return (end - start) / durationSecond;
}, (date2) => {
  return date2.getUTCSeconds();
});
var seconds = second.range;

// ../deepseek-harness/node_modules/.pnpm/d3-time@3.1.0/node_modules/d3-time/src/minute.js
var timeMinute = timeInterval((date2) => {
  date2.setTime(date2 - date2.getMilliseconds() - date2.getSeconds() * durationSecond);
}, (date2, step) => {
  date2.setTime(+date2 + step * durationMinute);
}, (start, end) => {
  return (end - start) / durationMinute;
}, (date2) => {
  return date2.getMinutes();
});
var timeMinutes = timeMinute.range;
var utcMinute = timeInterval((date2) => {
  date2.setUTCSeconds(0, 0);
}, (date2, step) => {
  date2.setTime(+date2 + step * durationMinute);
}, (start, end) => {
  return (end - start) / durationMinute;
}, (date2) => {
  return date2.getUTCMinutes();
});
var utcMinutes = utcMinute.range;

// ../deepseek-harness/node_modules/.pnpm/d3-time@3.1.0/node_modules/d3-time/src/hour.js
var timeHour = timeInterval((date2) => {
  date2.setTime(date2 - date2.getMilliseconds() - date2.getSeconds() * durationSecond - date2.getMinutes() * durationMinute);
}, (date2, step) => {
  date2.setTime(+date2 + step * durationHour);
}, (start, end) => {
  return (end - start) / durationHour;
}, (date2) => {
  return date2.getHours();
});
var timeHours = timeHour.range;
var utcHour = timeInterval((date2) => {
  date2.setUTCMinutes(0, 0, 0);
}, (date2, step) => {
  date2.setTime(+date2 + step * durationHour);
}, (start, end) => {
  return (end - start) / durationHour;
}, (date2) => {
  return date2.getUTCHours();
});
var utcHours = utcHour.range;

// ../deepseek-harness/node_modules/.pnpm/d3-time@3.1.0/node_modules/d3-time/src/day.js
var timeDay = timeInterval(
  (date2) => date2.setHours(0, 0, 0, 0),
  (date2, step) => date2.setDate(date2.getDate() + step),
  (start, end) => (end - start - (end.getTimezoneOffset() - start.getTimezoneOffset()) * durationMinute) / durationDay,
  (date2) => date2.getDate() - 1
);
var timeDays = timeDay.range;
var utcDay = timeInterval((date2) => {
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setUTCDate(date2.getUTCDate() + step);
}, (start, end) => {
  return (end - start) / durationDay;
}, (date2) => {
  return date2.getUTCDate() - 1;
});
var utcDays = utcDay.range;
var unixDay = timeInterval((date2) => {
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setUTCDate(date2.getUTCDate() + step);
}, (start, end) => {
  return (end - start) / durationDay;
}, (date2) => {
  return Math.floor(date2 / durationDay);
});
var unixDays = unixDay.range;

// ../deepseek-harness/node_modules/.pnpm/d3-time@3.1.0/node_modules/d3-time/src/week.js
function timeWeekday(i) {
  return timeInterval((date2) => {
    date2.setDate(date2.getDate() - (date2.getDay() + 7 - i) % 7);
    date2.setHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setDate(date2.getDate() + step * 7);
  }, (start, end) => {
    return (end - start - (end.getTimezoneOffset() - start.getTimezoneOffset()) * durationMinute) / durationWeek;
  });
}
var timeSunday = timeWeekday(0);
var timeMonday = timeWeekday(1);
var timeTuesday = timeWeekday(2);
var timeWednesday = timeWeekday(3);
var timeThursday = timeWeekday(4);
var timeFriday = timeWeekday(5);
var timeSaturday = timeWeekday(6);
var timeSundays = timeSunday.range;
var timeMondays = timeMonday.range;
var timeTuesdays = timeTuesday.range;
var timeWednesdays = timeWednesday.range;
var timeThursdays = timeThursday.range;
var timeFridays = timeFriday.range;
var timeSaturdays = timeSaturday.range;
function utcWeekday(i) {
  return timeInterval((date2) => {
    date2.setUTCDate(date2.getUTCDate() - (date2.getUTCDay() + 7 - i) % 7);
    date2.setUTCHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setUTCDate(date2.getUTCDate() + step * 7);
  }, (start, end) => {
    return (end - start) / durationWeek;
  });
}
var utcSunday = utcWeekday(0);
var utcMonday = utcWeekday(1);
var utcTuesday = utcWeekday(2);
var utcWednesday = utcWeekday(3);
var utcThursday = utcWeekday(4);
var utcFriday = utcWeekday(5);
var utcSaturday = utcWeekday(6);
var utcSundays = utcSunday.range;
var utcMondays = utcMonday.range;
var utcTuesdays = utcTuesday.range;
var utcWednesdays = utcWednesday.range;
var utcThursdays = utcThursday.range;
var utcFridays = utcFriday.range;
var utcSaturdays = utcSaturday.range;

// ../deepseek-harness/node_modules/.pnpm/d3-time@3.1.0/node_modules/d3-time/src/month.js
var timeMonth = timeInterval((date2) => {
  date2.setDate(1);
  date2.setHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setMonth(date2.getMonth() + step);
}, (start, end) => {
  return end.getMonth() - start.getMonth() + (end.getFullYear() - start.getFullYear()) * 12;
}, (date2) => {
  return date2.getMonth();
});
var timeMonths = timeMonth.range;
var utcMonth = timeInterval((date2) => {
  date2.setUTCDate(1);
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setUTCMonth(date2.getUTCMonth() + step);
}, (start, end) => {
  return end.getUTCMonth() - start.getUTCMonth() + (end.getUTCFullYear() - start.getUTCFullYear()) * 12;
}, (date2) => {
  return date2.getUTCMonth();
});
var utcMonths = utcMonth.range;

// ../deepseek-harness/node_modules/.pnpm/d3-time@3.1.0/node_modules/d3-time/src/year.js
var timeYear = timeInterval((date2) => {
  date2.setMonth(0, 1);
  date2.setHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setFullYear(date2.getFullYear() + step);
}, (start, end) => {
  return end.getFullYear() - start.getFullYear();
}, (date2) => {
  return date2.getFullYear();
});
timeYear.every = (k) => {
  return !isFinite(k = Math.floor(k)) || !(k > 0) ? null : timeInterval((date2) => {
    date2.setFullYear(Math.floor(date2.getFullYear() / k) * k);
    date2.setMonth(0, 1);
    date2.setHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setFullYear(date2.getFullYear() + step * k);
  });
};
var timeYears = timeYear.range;
var utcYear = timeInterval((date2) => {
  date2.setUTCMonth(0, 1);
  date2.setUTCHours(0, 0, 0, 0);
}, (date2, step) => {
  date2.setUTCFullYear(date2.getUTCFullYear() + step);
}, (start, end) => {
  return end.getUTCFullYear() - start.getUTCFullYear();
}, (date2) => {
  return date2.getUTCFullYear();
});
utcYear.every = (k) => {
  return !isFinite(k = Math.floor(k)) || !(k > 0) ? null : timeInterval((date2) => {
    date2.setUTCFullYear(Math.floor(date2.getUTCFullYear() / k) * k);
    date2.setUTCMonth(0, 1);
    date2.setUTCHours(0, 0, 0, 0);
  }, (date2, step) => {
    date2.setUTCFullYear(date2.getUTCFullYear() + step * k);
  });
};
var utcYears = utcYear.range;

// ../deepseek-harness/node_modules/.pnpm/d3-time@3.1.0/node_modules/d3-time/src/ticks.js
function ticker(year, month, week3, day, hour, minute) {
  const tickIntervals = [
    [second, 1, durationSecond],
    [second, 5, 5 * durationSecond],
    [second, 15, 15 * durationSecond],
    [second, 30, 30 * durationSecond],
    [minute, 1, durationMinute],
    [minute, 5, 5 * durationMinute],
    [minute, 15, 15 * durationMinute],
    [minute, 30, 30 * durationMinute],
    [hour, 1, durationHour],
    [hour, 3, 3 * durationHour],
    [hour, 6, 6 * durationHour],
    [hour, 12, 12 * durationHour],
    [day, 1, durationDay],
    [day, 2, 2 * durationDay],
    [week3, 1, durationWeek],
    [month, 1, durationMonth],
    [month, 3, 3 * durationMonth],
    [year, 1, durationYear]
  ];
  function ticks2(start, stop2, count2) {
    const reverse4 = stop2 < start;
    if (reverse4) [start, stop2] = [stop2, start];
    const interval3 = count2 && typeof count2.range === "function" ? count2 : tickInterval(start, stop2, count2);
    const ticks3 = interval3 ? interval3.range(start, +stop2 + 1) : [];
    return reverse4 ? ticks3.reverse() : ticks3;
  }
  function tickInterval(start, stop2, count2) {
    const target2 = Math.abs(stop2 - start) / count2;
    const i = bisector(([, , step2]) => step2).right(tickIntervals, target2);
    if (i === tickIntervals.length) return year.every(tickStep(start / durationYear, stop2 / durationYear, count2));
    if (i === 0) return millisecond.every(Math.max(tickStep(start, stop2, count2), 1));
    const [t, step] = tickIntervals[target2 / tickIntervals[i - 1][2] < tickIntervals[i][2] / target2 ? i - 1 : i];
    return t.every(step);
  }
  return [ticks2, tickInterval];
}
var [utcTicks, utcTickInterval] = ticker(utcYear, utcMonth, utcSunday, unixDay, utcHour, utcMinute);
var [timeTicks, timeTickInterval] = ticker(timeYear, timeMonth, timeSunday, timeDay, timeHour, timeMinute);

// ../deepseek-harness/node_modules/.pnpm/vega-time@3.3.0/node_modules/vega-time/build/vega-time.js
var YEAR = "year";
var QUARTER = "quarter";
var MONTH = "month";
var WEEK = "week";
var ISOWEEK = "isoweek";
var DATE = "date";
var DAY = "day";
var DAYOFYEAR = "dayofyear";
var HOURS = "hours";
var MINUTES = "minutes";
var SECONDS = "seconds";
var MILLISECONDS = "milliseconds";
var TIME_UNITS = [YEAR, QUARTER, MONTH, WEEK, ISOWEEK, DATE, DAY, DAYOFYEAR, HOURS, MINUTES, SECONDS, MILLISECONDS];
var UNITS = TIME_UNITS.reduce((o, u4, i) => (o[u4] = 1 + i, o), {});
function timeUnits(units) {
  const u4 = array_default(units).slice(), m3 = {};
  if (!u4.length) error("Missing time unit.");
  u4.forEach((unit2) => {
    if (hasOwnProperty(UNITS, unit2)) {
      m3[unit2] = 1;
    } else {
      error(`Invalid time unit: ${unit2}.`);
    }
  });
  const numTypes = (m3[WEEK] || m3[ISOWEEK] || m3[DAY] ? 1 : 0) + (m3[QUARTER] || m3[MONTH] || m3[DATE] ? 1 : 0) + (m3[DAYOFYEAR] ? 1 : 0);
  if (numTypes > 1 || m3[WEEK] && m3[ISOWEEK]) {
    error(`Incompatible time units: ${units}`);
  }
  u4.sort((a3, b10) => UNITS[a3] - UNITS[b10]);
  return u4;
}
var defaultSpecifiers = {
  [YEAR]: "%Y ",
  [QUARTER]: "Q%q ",
  [MONTH]: "%b ",
  [DATE]: "%d ",
  [WEEK]: "W%U ",
  [ISOWEEK]: "W%V ",
  [DAY]: "%a ",
  [DAYOFYEAR]: "%j ",
  [HOURS]: "%H:00",
  [MINUTES]: "00:%M",
  [SECONDS]: ":%S",
  [MILLISECONDS]: ".%L",
  [`${YEAR}-${ISOWEEK}`]: "%G W%V ",
  [`${YEAR}-${MONTH}`]: "%Y-%m ",
  [`${YEAR}-${MONTH}-${DATE}`]: "%Y-%m-%d ",
  [`${HOURS}-${MINUTES}`]: "%H:%M"
};
function timeUnitSpecifier(units, specifiers) {
  const s = extend({}, defaultSpecifiers, specifiers), u4 = timeUnits(units), n = u4.length;
  let fmt = "", start = 0, end, key2;
  for (start = 0; start < n; ) {
    for (end = u4.length; end > start; --end) {
      key2 = u4.slice(start, end).join("-");
      if (s[key2] != null) {
        fmt += s[key2];
        start = end;
        break;
      }
    }
  }
  return fmt.trim();
}
var t02 = /* @__PURE__ */ new Date();
function localYear(y6) {
  t02.setFullYear(y6);
  t02.setMonth(0);
  t02.setDate(1);
  t02.setHours(0, 0, 0, 0);
  return t02;
}
function dayofyear(d) {
  return localDayOfYear(new Date(d));
}
function week(d) {
  return localWeekNum(new Date(d));
}
function isoweek(d) {
  return localISOWeekNum(new Date(d));
}
function localDayOfYear(d) {
  return timeDay.count(localYear(d.getFullYear()) - 1, d);
}
function localWeekNum(d) {
  return timeSunday.count(localYear(d.getFullYear()) - 1, d);
}
function localISOWeekYear(d) {
  return timeDay.offset(timeMonday.floor(d), 3).getFullYear();
}
function localISOWeekNum(d) {
  return 1 + timeMonday.count(localISOWeekOne(localISOWeekYear(d)), d);
}
function localISOWeekOne(y6) {
  return timeMonday.floor(timeDay.offset(localYear(y6), 3));
}
function localISOWeekOneDate(y6) {
  const d = localISOWeekOne(y6);
  return d.getMonth() ? d.getDate() - 31 : d.getDate();
}
function localFirst(y6) {
  return localYear(y6).getDay();
}
function localDate(y6, m3, d, H, M2, S, L) {
  if (0 <= y6 && y6 < 100) {
    const date2 = new Date(-1, m3, d, H, M2, S, L);
    date2.setFullYear(y6);
    return date2;
  }
  return new Date(y6, m3, d, H, M2, S, L);
}
function utcdayofyear(d) {
  return utcDayOfYear(new Date(d));
}
function utcweek(d) {
  return utcWeekNum(new Date(d));
}
function utcisoweek(d) {
  return utcISOWeekNum(new Date(d));
}
function utcDayOfYear(d) {
  const y6 = Date.UTC(d.getUTCFullYear(), 0, 1);
  return utcDay.count(y6 - 1, d);
}
function utcWeekNum(d) {
  const y6 = Date.UTC(d.getUTCFullYear(), 0, 1);
  return utcSunday.count(y6 - 1, d);
}
function utcISOWeekYear(d) {
  return utcDay.offset(utcMonday.floor(d), 3).getUTCFullYear();
}
function utcISOWeekNum(d) {
  return 1 + utcMonday.count(utcISOWeekOne(utcISOWeekYear(d)), d);
}
function utcISOWeekOne(y6) {
  return utcMonday.floor(utcDay.offset(Date.UTC(y6, 0, 1), 3));
}
function utcISOWeekOneDate(y6) {
  const d = utcISOWeekOne(y6);
  return d.getUTCMonth() ? d.getUTCDate() - 31 : d.getUTCDate();
}
function utcFirst(y6) {
  t02.setTime(Date.UTC(y6, 0, 1));
  return t02.getUTCDay();
}
function utcDate(y6, m3, d, H, M2, S, L) {
  if (0 <= y6 && y6 < 100) {
    const date2 = new Date(Date.UTC(-1, m3, d, H, M2, S, L));
    date2.setUTCFullYear(d.y);
    return date2;
  }
  return new Date(Date.UTC(y6, m3, d, H, M2, S, L));
}
var REFERENCE_YEAR = 2012;
var ISOWEEK_REFERENCE_YEAR = 2015;
function floor(units, step, get7, inv, newDate2) {
  const s = step || 1, b10 = peek(units), _ = (unit2, p, key2) => {
    key2 = key2 || unit2;
    return getUnit(get7[key2], inv[key2], unit2 === b10 && s, p);
  };
  const t = /* @__PURE__ */ new Date(), u4 = toSet(units), y6 = u4[YEAR] ? _(YEAR, null, u4[ISOWEEK] ? YEAR + ISOWEEK : YEAR) : constant_default(u4[ISOWEEK] ? ISOWEEK_REFERENCE_YEAR : REFERENCE_YEAR), m3 = u4[MONTH] ? _(MONTH) : u4[QUARTER] ? _(QUARTER) : zero, d = u4[WEEK] && u4[DAY] ? _(DAY, 1, WEEK + DAY) : u4[ISOWEEK] && u4[DAY] ? _(DAY, 1, ISOWEEK + DAY) : u4[WEEK] ? _(WEEK, 1) : u4[ISOWEEK] ? _(ISOWEEK, 1) : u4[DAY] ? _(DAY, 1) : u4[DATE] ? _(DATE, 1) : u4[DAYOFYEAR] ? _(DAYOFYEAR, 1) : one, H = u4[HOURS] ? _(HOURS) : zero, M2 = u4[MINUTES] ? _(MINUTES) : zero, S = u4[SECONDS] ? _(SECONDS) : zero, L = u4[MILLISECONDS] ? _(MILLISECONDS) : zero;
  return function(v2) {
    t.setTime(+v2);
    const year = y6(t);
    return newDate2(year, m3(t), d(t, year), H(t), M2(t), S(t), L(t));
  };
}
function getUnit(f, inv, step, phase) {
  const u4 = step <= 1 ? f : phase ? (d, y6) => phase + step * Math.floor((f(d, y6) - phase) / step) : (d, y6) => step * Math.floor(f(d, y6) / step);
  return inv ? (d, y6) => inv(u4(d, y6), y6) : u4;
}
function weekday(week3, day, firstDay) {
  return day + week3 * 7 - (firstDay + 6) % 7;
}
function isoDay(day) {
  return (day + 6) % 7;
}
function isoWeekday(weekOneDate, week3, day) {
  return weekOneDate + (week3 - 1) * 7 + day;
}
var localGet = {
  [YEAR]: (d) => d.getFullYear(),
  [QUARTER]: (d) => Math.floor(d.getMonth() / 3),
  [MONTH]: (d) => d.getMonth(),
  [DATE]: (d) => d.getDate(),
  [HOURS]: (d) => d.getHours(),
  [MINUTES]: (d) => d.getMinutes(),
  [SECONDS]: (d) => d.getSeconds(),
  [MILLISECONDS]: (d) => d.getMilliseconds(),
  [DAYOFYEAR]: (d) => localDayOfYear(d),
  [WEEK]: (d) => localWeekNum(d),
  [WEEK + DAY]: (d, y6) => weekday(localWeekNum(d), d.getDay(), localFirst(y6)),
  [DAY]: (d, y6) => weekday(1, d.getDay(), localFirst(y6)),
  [ISOWEEK]: (d) => localISOWeekNum(d),
  [YEAR + ISOWEEK]: (d) => localISOWeekYear(d),
  [ISOWEEK + DAY]: (d, y6) => isoWeekday(localISOWeekOneDate(y6), localISOWeekNum(d), isoDay(d.getDay()))
};
var localInv = {
  [QUARTER]: (q) => 3 * q,
  [WEEK]: (w3, y6) => weekday(w3, 0, localFirst(y6)),
  [ISOWEEK]: (w3, y6) => isoWeekday(localISOWeekOneDate(y6), w3, 0)
};
function timeFloor(units, step) {
  return floor(units, step || 1, localGet, localInv, localDate);
}
var utcGet = {
  [YEAR]: (d) => d.getUTCFullYear(),
  [QUARTER]: (d) => Math.floor(d.getUTCMonth() / 3),
  [MONTH]: (d) => d.getUTCMonth(),
  [DATE]: (d) => d.getUTCDate(),
  [HOURS]: (d) => d.getUTCHours(),
  [MINUTES]: (d) => d.getUTCMinutes(),
  [SECONDS]: (d) => d.getUTCSeconds(),
  [MILLISECONDS]: (d) => d.getUTCMilliseconds(),
  [DAYOFYEAR]: (d) => utcDayOfYear(d),
  [WEEK]: (d) => utcWeekNum(d),
  [DAY]: (d, y6) => weekday(1, d.getUTCDay(), utcFirst(y6)),
  [WEEK + DAY]: (d, y6) => weekday(utcWeekNum(d), d.getUTCDay(), utcFirst(y6)),
  [ISOWEEK]: (d) => utcISOWeekNum(d),
  [YEAR + ISOWEEK]: (d) => utcISOWeekYear(d),
  [ISOWEEK + DAY]: (d, y6) => isoWeekday(utcISOWeekOneDate(y6), utcISOWeekNum(d), isoDay(d.getUTCDay()))
};
var utcInv = {
  [QUARTER]: (q) => 3 * q,
  [WEEK]: (w3, y6) => weekday(w3, 0, utcFirst(y6)),
  [ISOWEEK]: (w3, y6) => isoWeekday(utcISOWeekOneDate(y6), w3, 0)
};
function utcFloor(units, step) {
  return floor(units, step || 1, utcGet, utcInv, utcDate);
}
var timeIntervals = {
  [YEAR]: timeYear,
  [QUARTER]: timeMonth.every(3),
  [MONTH]: timeMonth,
  [WEEK]: timeSunday,
  [ISOWEEK]: timeMonday,
  [DATE]: timeDay,
  [DAY]: timeDay,
  [DAYOFYEAR]: timeDay,
  [HOURS]: timeHour,
  [MINUTES]: timeMinute,
  [SECONDS]: second,
  [MILLISECONDS]: millisecond
};
var utcIntervals = {
  [YEAR]: utcYear,
  [QUARTER]: utcMonth.every(3),
  [MONTH]: utcMonth,
  [WEEK]: utcSunday,
  [ISOWEEK]: utcMonday,
  [DATE]: utcDay,
  [DAY]: utcDay,
  [DAYOFYEAR]: utcDay,
  [HOURS]: utcHour,
  [MINUTES]: utcMinute,
  [SECONDS]: second,
  [MILLISECONDS]: millisecond
};
function timeInterval2(unit2) {
  return timeIntervals[unit2];
}
function utcInterval(unit2) {
  return utcIntervals[unit2];
}
function offset(ival, date2, step) {
  return ival ? ival.offset(date2, step) : void 0;
}
function timeOffset(unit2, date2, step) {
  return offset(timeInterval2(unit2), date2, step);
}
function utcOffset(unit2, date2, step) {
  return offset(utcInterval(unit2), date2, step);
}
function sequence(ival, start, stop2, step) {
  return ival ? ival.range(start, stop2, step) : void 0;
}
function timeSequence(unit2, start, stop2, step) {
  return sequence(timeInterval2(unit2), start, stop2, step);
}
function utcSequence(unit2, start, stop2, step) {
  return sequence(utcInterval(unit2), start, stop2, step);
}
var durationSecond2 = 1e3;
var durationMinute2 = durationSecond2 * 60;
var durationHour2 = durationMinute2 * 60;
var durationDay2 = durationHour2 * 24;
var durationWeek2 = durationDay2 * 7;
var durationMonth2 = durationDay2 * 30;
var durationYear2 = durationDay2 * 365;
var Milli = [YEAR, MONTH, DATE, HOURS, MINUTES, SECONDS, MILLISECONDS];
var Seconds = Milli.slice(0, -1);
var Minutes = Seconds.slice(0, -1);
var Hours = Minutes.slice(0, -1);
var Day = Hours.slice(0, -1);
var Week = [YEAR, WEEK];
var Month = [YEAR, MONTH];
var Year = [YEAR];
var intervals = [[Seconds, 1, durationSecond2], [Seconds, 5, 5 * durationSecond2], [Seconds, 15, 15 * durationSecond2], [Seconds, 30, 30 * durationSecond2], [Minutes, 1, durationMinute2], [Minutes, 5, 5 * durationMinute2], [Minutes, 15, 15 * durationMinute2], [Minutes, 30, 30 * durationMinute2], [Hours, 1, durationHour2], [Hours, 3, 3 * durationHour2], [Hours, 6, 6 * durationHour2], [Hours, 12, 12 * durationHour2], [Day, 1, durationDay2], [Week, 1, durationWeek2], [Month, 1, durationMonth2], [Month, 3, 3 * durationMonth2], [Year, 1, durationYear2]];
function bin(opt) {
  const ext = opt.extent, max4 = opt.maxbins || 40, target2 = Math.abs(span_default(ext)) / max4;
  let i = bisector((i2) => i2[2]).right(intervals, target2), units, step;
  if (i === intervals.length) {
    units = Year, step = tickStep(ext[0] / durationYear2, ext[1] / durationYear2, max4);
  } else if (i) {
    i = intervals[target2 / intervals[i - 1][2] < intervals[i][2] / target2 ? i - 1 : i];
    units = i[0];
    step = i[1];
  } else {
    units = Milli;
    step = Math.max(tickStep(ext[0], ext[1], max4), 1);
  }
  return {
    units,
    step
  };
}
var localGetters = {
  [YEAR]: (d) => d.getFullYear(),
  [MONTH]: (d) => d.getMonth(),
  [DATE]: (d) => d.getDate(),
  [DAY]: (d) => d.getDay(),
  [HOURS]: (d) => d.getHours(),
  [MINUTES]: (d) => d.getMinutes(),
  [SECONDS]: (d) => d.getSeconds(),
  [MILLISECONDS]: (d) => d.getMilliseconds()
};
var utcGetters = {
  [YEAR]: (d) => d.getUTCFullYear(),
  [MONTH]: (d) => d.getUTCMonth(),
  [DATE]: (d) => d.getUTCDate(),
  [DAY]: (d) => d.getUTCDay(),
  [HOURS]: (d) => d.getUTCHours(),
  [MINUTES]: (d) => d.getUTCMinutes(),
  [SECONDS]: (d) => d.getUTCSeconds(),
  [MILLISECONDS]: (d) => d.getUTCMilliseconds()
};
var grains = [{
  units: [YEAR, MONTH, DATE, HOURS, MINUTES, SECONDS, MILLISECONDS],
  step: 1,
  aligned: () => true
}, {
  units: [YEAR, MONTH, DATE, HOURS, MINUTES, SECONDS],
  step: 1,
  aligned: (dates, g) => dates.every((d) => g[MILLISECONDS](d) === 0)
}, {
  units: [YEAR, MONTH, DATE, HOURS, MINUTES],
  step: 1,
  aligned: (dates, g) => dates.every((d) => g[SECONDS](d) === 0)
}, {
  units: [YEAR, MONTH, DATE, HOURS, MINUTES],
  step: 5,
  aligned: (dates, g) => dates.every((d) => g[MINUTES](d) % 5 === 0)
}, {
  units: [YEAR, MONTH, DATE, HOURS, MINUTES],
  step: 10,
  aligned: (dates, g) => dates.every((d) => g[MINUTES](d) % 10 === 0)
}, {
  units: [YEAR, MONTH, DATE, HOURS],
  step: 1,
  aligned: (dates, g) => dates.every((d) => g[MINUTES](d) === 0)
}, {
  units: [YEAR, MONTH, DATE],
  step: 1,
  aligned: (dates, g) => dates.every((d) => g[HOURS](d) === 0)
}, {
  units: [YEAR, WEEK],
  step: 1,
  skippable: true,
  aligned: (dates, g) => new Set(dates.map((d) => g[DAY](d))).size === 1
}, {
  units: [YEAR, MONTH],
  step: 1,
  aligned: (dates, g) => dates.every((d) => g[DATE](d) === 1)
}, {
  units: [YEAR, MONTH],
  step: 3,
  aligned: (dates, g) => dates.every((d) => g[MONTH](d) % 3 === 0)
}, {
  units: [YEAR],
  step: 1,
  aligned: (dates, g) => dates.every((d) => g[MONTH](d) === 0)
}, {
  units: [YEAR],
  step: 10,
  aligned: (dates, g) => dates.every((d) => g[YEAR](d) % 10 === 0)
}, {
  aligned: () => false
}];
function detectTimeUnits(data4, field3, utc) {
  const getters = utc ? utcGetters : localGetters;
  const dates = data4.map((t) => {
    const v2 = field3(t), d = new Date(v2);
    if (Number.isNaN(+d)) error(`Invalid date: ${v2}`);
    return d;
  });
  const mismatch = grains.findIndex((g) => !g.aligned(dates, getters)), required2 = grains.findIndex((g) => !g.skippable && !g.aligned(dates, getters)), index4 = required2 > mismatch + 1 ? required2 : mismatch;
  const {
    units,
    step
  } = grains[index4 - 1];
  return {
    units,
    step
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-time-format@4.1.0/node_modules/d3-time-format/src/locale.js
function localDate2(d) {
  if (0 <= d.y && d.y < 100) {
    var date2 = new Date(-1, d.m, d.d, d.H, d.M, d.S, d.L);
    date2.setFullYear(d.y);
    return date2;
  }
  return new Date(d.y, d.m, d.d, d.H, d.M, d.S, d.L);
}
function utcDate2(d) {
  if (0 <= d.y && d.y < 100) {
    var date2 = new Date(Date.UTC(-1, d.m, d.d, d.H, d.M, d.S, d.L));
    date2.setUTCFullYear(d.y);
    return date2;
  }
  return new Date(Date.UTC(d.y, d.m, d.d, d.H, d.M, d.S, d.L));
}
function newDate(y6, m3, d) {
  return { y: y6, m: m3, d, H: 0, M: 0, S: 0, L: 0 };
}
function formatLocale(locale4) {
  var locale_dateTime = locale4.dateTime, locale_date = locale4.date, locale_time = locale4.time, locale_periods = locale4.periods, locale_weekdays = locale4.days, locale_shortWeekdays = locale4.shortDays, locale_months = locale4.months, locale_shortMonths = locale4.shortMonths;
  var periodRe = formatRe(locale_periods), periodLookup = formatLookup(locale_periods), weekdayRe = formatRe(locale_weekdays), weekdayLookup = formatLookup(locale_weekdays), shortWeekdayRe = formatRe(locale_shortWeekdays), shortWeekdayLookup = formatLookup(locale_shortWeekdays), monthRe = formatRe(locale_months), monthLookup = formatLookup(locale_months), shortMonthRe = formatRe(locale_shortMonths), shortMonthLookup = formatLookup(locale_shortMonths);
  var formats3 = {
    "a": formatShortWeekday,
    "A": formatWeekday,
    "b": formatShortMonth,
    "B": formatMonth,
    "c": null,
    "d": formatDayOfMonth,
    "e": formatDayOfMonth,
    "f": formatMicroseconds,
    "g": formatYearISO,
    "G": formatFullYearISO,
    "H": formatHour24,
    "I": formatHour12,
    "j": formatDayOfYear,
    "L": formatMilliseconds,
    "m": formatMonthNumber,
    "M": formatMinutes,
    "p": formatPeriod,
    "q": formatQuarter,
    "Q": formatUnixTimestamp,
    "s": formatUnixTimestampSeconds,
    "S": formatSeconds,
    "u": formatWeekdayNumberMonday,
    "U": formatWeekNumberSunday,
    "V": formatWeekNumberISO,
    "w": formatWeekdayNumberSunday,
    "W": formatWeekNumberMonday,
    "x": null,
    "X": null,
    "y": formatYear2,
    "Y": formatFullYear,
    "Z": formatZone,
    "%": formatLiteralPercent
  };
  var utcFormats = {
    "a": formatUTCShortWeekday,
    "A": formatUTCWeekday,
    "b": formatUTCShortMonth,
    "B": formatUTCMonth,
    "c": null,
    "d": formatUTCDayOfMonth,
    "e": formatUTCDayOfMonth,
    "f": formatUTCMicroseconds,
    "g": formatUTCYearISO,
    "G": formatUTCFullYearISO,
    "H": formatUTCHour24,
    "I": formatUTCHour12,
    "j": formatUTCDayOfYear,
    "L": formatUTCMilliseconds,
    "m": formatUTCMonthNumber,
    "M": formatUTCMinutes,
    "p": formatUTCPeriod,
    "q": formatUTCQuarter,
    "Q": formatUnixTimestamp,
    "s": formatUnixTimestampSeconds,
    "S": formatUTCSeconds,
    "u": formatUTCWeekdayNumberMonday,
    "U": formatUTCWeekNumberSunday,
    "V": formatUTCWeekNumberISO,
    "w": formatUTCWeekdayNumberSunday,
    "W": formatUTCWeekNumberMonday,
    "x": null,
    "X": null,
    "y": formatUTCYear,
    "Y": formatUTCFullYear,
    "Z": formatUTCZone,
    "%": formatLiteralPercent
  };
  var parses = {
    "a": parseShortWeekday,
    "A": parseWeekday,
    "b": parseShortMonth,
    "B": parseMonth,
    "c": parseLocaleDateTime,
    "d": parseDayOfMonth,
    "e": parseDayOfMonth,
    "f": parseMicroseconds,
    "g": parseYear,
    "G": parseFullYear,
    "H": parseHour24,
    "I": parseHour24,
    "j": parseDayOfYear,
    "L": parseMilliseconds,
    "m": parseMonthNumber,
    "M": parseMinutes,
    "p": parsePeriod,
    "q": parseQuarter,
    "Q": parseUnixTimestamp,
    "s": parseUnixTimestampSeconds,
    "S": parseSeconds,
    "u": parseWeekdayNumberMonday,
    "U": parseWeekNumberSunday,
    "V": parseWeekNumberISO,
    "w": parseWeekdayNumberSunday,
    "W": parseWeekNumberMonday,
    "x": parseLocaleDate,
    "X": parseLocaleTime,
    "y": parseYear,
    "Y": parseFullYear,
    "Z": parseZone,
    "%": parseLiteralPercent
  };
  formats3.x = newFormat(locale_date, formats3);
  formats3.X = newFormat(locale_time, formats3);
  formats3.c = newFormat(locale_dateTime, formats3);
  utcFormats.x = newFormat(locale_date, utcFormats);
  utcFormats.X = newFormat(locale_time, utcFormats);
  utcFormats.c = newFormat(locale_dateTime, utcFormats);
  function newFormat(specifier, formats4) {
    return function(date2) {
      var string = [], i = -1, j = 0, n = specifier.length, c3, pad4, format6;
      if (!(date2 instanceof Date)) date2 = /* @__PURE__ */ new Date(+date2);
      while (++i < n) {
        if (specifier.charCodeAt(i) === 37) {
          string.push(specifier.slice(j, i));
          if ((pad4 = pads[c3 = specifier.charAt(++i)]) != null) c3 = specifier.charAt(++i);
          else pad4 = c3 === "e" ? " " : "0";
          if (format6 = formats4[c3]) c3 = format6(date2, pad4);
          string.push(c3);
          j = i + 1;
        }
      }
      string.push(specifier.slice(j, i));
      return string.join("");
    };
  }
  function newParse(specifier, Z) {
    return function(string) {
      var d = newDate(1900, void 0, 1), i = parseSpecifier(d, specifier, string += "", 0), week3, day;
      if (i != string.length) return null;
      if ("Q" in d) return new Date(d.Q);
      if ("s" in d) return new Date(d.s * 1e3 + ("L" in d ? d.L : 0));
      if (Z && !("Z" in d)) d.Z = 0;
      if ("p" in d) d.H = d.H % 12 + d.p * 12;
      if (d.m === void 0) d.m = "q" in d ? d.q : 0;
      if ("V" in d) {
        if (d.V < 1 || d.V > 53) return null;
        if (!("w" in d)) d.w = 1;
        if ("Z" in d) {
          week3 = utcDate2(newDate(d.y, 0, 1)), day = week3.getUTCDay();
          week3 = day > 4 || day === 0 ? utcMonday.ceil(week3) : utcMonday(week3);
          week3 = utcDay.offset(week3, (d.V - 1) * 7);
          d.y = week3.getUTCFullYear();
          d.m = week3.getUTCMonth();
          d.d = week3.getUTCDate() + (d.w + 6) % 7;
        } else {
          week3 = localDate2(newDate(d.y, 0, 1)), day = week3.getDay();
          week3 = day > 4 || day === 0 ? timeMonday.ceil(week3) : timeMonday(week3);
          week3 = timeDay.offset(week3, (d.V - 1) * 7);
          d.y = week3.getFullYear();
          d.m = week3.getMonth();
          d.d = week3.getDate() + (d.w + 6) % 7;
        }
      } else if ("W" in d || "U" in d) {
        if (!("w" in d)) d.w = "u" in d ? d.u % 7 : "W" in d ? 1 : 0;
        day = "Z" in d ? utcDate2(newDate(d.y, 0, 1)).getUTCDay() : localDate2(newDate(d.y, 0, 1)).getDay();
        d.m = 0;
        d.d = "W" in d ? (d.w + 6) % 7 + d.W * 7 - (day + 5) % 7 : d.w + d.U * 7 - (day + 6) % 7;
      }
      if ("Z" in d) {
        d.H += d.Z / 100 | 0;
        d.M += d.Z % 100;
        return utcDate2(d);
      }
      return localDate2(d);
    };
  }
  function parseSpecifier(d, specifier, string, j) {
    var i = 0, n = specifier.length, m3 = string.length, c3, parse8;
    while (i < n) {
      if (j >= m3) return -1;
      c3 = specifier.charCodeAt(i++);
      if (c3 === 37) {
        c3 = specifier.charAt(i++);
        parse8 = parses[c3 in pads ? specifier.charAt(i++) : c3];
        if (!parse8 || (j = parse8(d, string, j)) < 0) return -1;
      } else if (c3 != string.charCodeAt(j++)) {
        return -1;
      }
    }
    return j;
  }
  function parsePeriod(d, string, i) {
    var n = periodRe.exec(string.slice(i));
    return n ? (d.p = periodLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }
  function parseShortWeekday(d, string, i) {
    var n = shortWeekdayRe.exec(string.slice(i));
    return n ? (d.w = shortWeekdayLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }
  function parseWeekday(d, string, i) {
    var n = weekdayRe.exec(string.slice(i));
    return n ? (d.w = weekdayLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }
  function parseShortMonth(d, string, i) {
    var n = shortMonthRe.exec(string.slice(i));
    return n ? (d.m = shortMonthLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }
  function parseMonth(d, string, i) {
    var n = monthRe.exec(string.slice(i));
    return n ? (d.m = monthLookup.get(n[0].toLowerCase()), i + n[0].length) : -1;
  }
  function parseLocaleDateTime(d, string, i) {
    return parseSpecifier(d, locale_dateTime, string, i);
  }
  function parseLocaleDate(d, string, i) {
    return parseSpecifier(d, locale_date, string, i);
  }
  function parseLocaleTime(d, string, i) {
    return parseSpecifier(d, locale_time, string, i);
  }
  function formatShortWeekday(d) {
    return locale_shortWeekdays[d.getDay()];
  }
  function formatWeekday(d) {
    return locale_weekdays[d.getDay()];
  }
  function formatShortMonth(d) {
    return locale_shortMonths[d.getMonth()];
  }
  function formatMonth(d) {
    return locale_months[d.getMonth()];
  }
  function formatPeriod(d) {
    return locale_periods[+(d.getHours() >= 12)];
  }
  function formatQuarter(d) {
    return 1 + ~~(d.getMonth() / 3);
  }
  function formatUTCShortWeekday(d) {
    return locale_shortWeekdays[d.getUTCDay()];
  }
  function formatUTCWeekday(d) {
    return locale_weekdays[d.getUTCDay()];
  }
  function formatUTCShortMonth(d) {
    return locale_shortMonths[d.getUTCMonth()];
  }
  function formatUTCMonth(d) {
    return locale_months[d.getUTCMonth()];
  }
  function formatUTCPeriod(d) {
    return locale_periods[+(d.getUTCHours() >= 12)];
  }
  function formatUTCQuarter(d) {
    return 1 + ~~(d.getUTCMonth() / 3);
  }
  return {
    format: function(specifier) {
      var f = newFormat(specifier += "", formats3);
      f.toString = function() {
        return specifier;
      };
      return f;
    },
    parse: function(specifier) {
      var p = newParse(specifier += "", false);
      p.toString = function() {
        return specifier;
      };
      return p;
    },
    utcFormat: function(specifier) {
      var f = newFormat(specifier += "", utcFormats);
      f.toString = function() {
        return specifier;
      };
      return f;
    },
    utcParse: function(specifier) {
      var p = newParse(specifier += "", true);
      p.toString = function() {
        return specifier;
      };
      return p;
    }
  };
}
var pads = { "-": "", "_": " ", "0": "0" };
var numberRe = /^\s*\d+/;
var percentRe = /^%/;
var requoteRe = /[\\^$*+?|[\]().{}]/g;
function pad3(value4, fill3, width2) {
  var sign3 = value4 < 0 ? "-" : "", string = (sign3 ? -value4 : value4) + "", length3 = string.length;
  return sign3 + (length3 < width2 ? new Array(width2 - length3 + 1).join(fill3) + string : string);
}
function requote(s) {
  return s.replace(requoteRe, "\\$&");
}
function formatRe(names) {
  return new RegExp("^(?:" + names.map(requote).join("|") + ")", "i");
}
function formatLookup(names) {
  return new Map(names.map((name, i) => [name.toLowerCase(), i]));
}
function parseWeekdayNumberSunday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 1));
  return n ? (d.w = +n[0], i + n[0].length) : -1;
}
function parseWeekdayNumberMonday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 1));
  return n ? (d.u = +n[0], i + n[0].length) : -1;
}
function parseWeekNumberSunday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.U = +n[0], i + n[0].length) : -1;
}
function parseWeekNumberISO(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.V = +n[0], i + n[0].length) : -1;
}
function parseWeekNumberMonday(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.W = +n[0], i + n[0].length) : -1;
}
function parseFullYear(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 4));
  return n ? (d.y = +n[0], i + n[0].length) : -1;
}
function parseYear(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.y = +n[0] + (+n[0] > 68 ? 1900 : 2e3), i + n[0].length) : -1;
}
function parseZone(d, string, i) {
  var n = /^(Z)|([+-]\d\d)(?::?(\d\d))?/.exec(string.slice(i, i + 6));
  return n ? (d.Z = n[1] ? 0 : -(n[2] + (n[3] || "00")), i + n[0].length) : -1;
}
function parseQuarter(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 1));
  return n ? (d.q = n[0] * 3 - 3, i + n[0].length) : -1;
}
function parseMonthNumber(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.m = n[0] - 1, i + n[0].length) : -1;
}
function parseDayOfMonth(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.d = +n[0], i + n[0].length) : -1;
}
function parseDayOfYear(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 3));
  return n ? (d.m = 0, d.d = +n[0], i + n[0].length) : -1;
}
function parseHour24(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.H = +n[0], i + n[0].length) : -1;
}
function parseMinutes(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.M = +n[0], i + n[0].length) : -1;
}
function parseSeconds(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 2));
  return n ? (d.S = +n[0], i + n[0].length) : -1;
}
function parseMilliseconds(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 3));
  return n ? (d.L = +n[0], i + n[0].length) : -1;
}
function parseMicroseconds(d, string, i) {
  var n = numberRe.exec(string.slice(i, i + 6));
  return n ? (d.L = Math.floor(n[0] / 1e3), i + n[0].length) : -1;
}
function parseLiteralPercent(d, string, i) {
  var n = percentRe.exec(string.slice(i, i + 1));
  return n ? i + n[0].length : -1;
}
function parseUnixTimestamp(d, string, i) {
  var n = numberRe.exec(string.slice(i));
  return n ? (d.Q = +n[0], i + n[0].length) : -1;
}
function parseUnixTimestampSeconds(d, string, i) {
  var n = numberRe.exec(string.slice(i));
  return n ? (d.s = +n[0], i + n[0].length) : -1;
}
function formatDayOfMonth(d, p) {
  return pad3(d.getDate(), p, 2);
}
function formatHour24(d, p) {
  return pad3(d.getHours(), p, 2);
}
function formatHour12(d, p) {
  return pad3(d.getHours() % 12 || 12, p, 2);
}
function formatDayOfYear(d, p) {
  return pad3(1 + timeDay.count(timeYear(d), d), p, 3);
}
function formatMilliseconds(d, p) {
  return pad3(d.getMilliseconds(), p, 3);
}
function formatMicroseconds(d, p) {
  return formatMilliseconds(d, p) + "000";
}
function formatMonthNumber(d, p) {
  return pad3(d.getMonth() + 1, p, 2);
}
function formatMinutes(d, p) {
  return pad3(d.getMinutes(), p, 2);
}
function formatSeconds(d, p) {
  return pad3(d.getSeconds(), p, 2);
}
function formatWeekdayNumberMonday(d) {
  var day = d.getDay();
  return day === 0 ? 7 : day;
}
function formatWeekNumberSunday(d, p) {
  return pad3(timeSunday.count(timeYear(d) - 1, d), p, 2);
}
function dISO(d) {
  var day = d.getDay();
  return day >= 4 || day === 0 ? timeThursday(d) : timeThursday.ceil(d);
}
function formatWeekNumberISO(d, p) {
  d = dISO(d);
  return pad3(timeThursday.count(timeYear(d), d) + (timeYear(d).getDay() === 4), p, 2);
}
function formatWeekdayNumberSunday(d) {
  return d.getDay();
}
function formatWeekNumberMonday(d, p) {
  return pad3(timeMonday.count(timeYear(d) - 1, d), p, 2);
}
function formatYear2(d, p) {
  return pad3(d.getFullYear() % 100, p, 2);
}
function formatYearISO(d, p) {
  d = dISO(d);
  return pad3(d.getFullYear() % 100, p, 2);
}
function formatFullYear(d, p) {
  return pad3(d.getFullYear() % 1e4, p, 4);
}
function formatFullYearISO(d, p) {
  var day = d.getDay();
  d = day >= 4 || day === 0 ? timeThursday(d) : timeThursday.ceil(d);
  return pad3(d.getFullYear() % 1e4, p, 4);
}
function formatZone(d) {
  var z = d.getTimezoneOffset();
  return (z > 0 ? "-" : (z *= -1, "+")) + pad3(z / 60 | 0, "0", 2) + pad3(z % 60, "0", 2);
}
function formatUTCDayOfMonth(d, p) {
  return pad3(d.getUTCDate(), p, 2);
}
function formatUTCHour24(d, p) {
  return pad3(d.getUTCHours(), p, 2);
}
function formatUTCHour12(d, p) {
  return pad3(d.getUTCHours() % 12 || 12, p, 2);
}
function formatUTCDayOfYear(d, p) {
  return pad3(1 + utcDay.count(utcYear(d), d), p, 3);
}
function formatUTCMilliseconds(d, p) {
  return pad3(d.getUTCMilliseconds(), p, 3);
}
function formatUTCMicroseconds(d, p) {
  return formatUTCMilliseconds(d, p) + "000";
}
function formatUTCMonthNumber(d, p) {
  return pad3(d.getUTCMonth() + 1, p, 2);
}
function formatUTCMinutes(d, p) {
  return pad3(d.getUTCMinutes(), p, 2);
}
function formatUTCSeconds(d, p) {
  return pad3(d.getUTCSeconds(), p, 2);
}
function formatUTCWeekdayNumberMonday(d) {
  var dow = d.getUTCDay();
  return dow === 0 ? 7 : dow;
}
function formatUTCWeekNumberSunday(d, p) {
  return pad3(utcSunday.count(utcYear(d) - 1, d), p, 2);
}
function UTCdISO(d) {
  var day = d.getUTCDay();
  return day >= 4 || day === 0 ? utcThursday(d) : utcThursday.ceil(d);
}
function formatUTCWeekNumberISO(d, p) {
  d = UTCdISO(d);
  return pad3(utcThursday.count(utcYear(d), d) + (utcYear(d).getUTCDay() === 4), p, 2);
}
function formatUTCWeekdayNumberSunday(d) {
  return d.getUTCDay();
}
function formatUTCWeekNumberMonday(d, p) {
  return pad3(utcMonday.count(utcYear(d) - 1, d), p, 2);
}
function formatUTCYear(d, p) {
  return pad3(d.getUTCFullYear() % 100, p, 2);
}
function formatUTCYearISO(d, p) {
  d = UTCdISO(d);
  return pad3(d.getUTCFullYear() % 100, p, 2);
}
function formatUTCFullYear(d, p) {
  return pad3(d.getUTCFullYear() % 1e4, p, 4);
}
function formatUTCFullYearISO(d, p) {
  var day = d.getUTCDay();
  d = day >= 4 || day === 0 ? utcThursday(d) : utcThursday.ceil(d);
  return pad3(d.getUTCFullYear() % 1e4, p, 4);
}
function formatUTCZone() {
  return "+0000";
}
function formatLiteralPercent() {
  return "%";
}
function formatUnixTimestamp(d) {
  return +d;
}
function formatUnixTimestampSeconds(d) {
  return Math.floor(+d / 1e3);
}

// ../deepseek-harness/node_modules/.pnpm/d3-time-format@4.1.0/node_modules/d3-time-format/src/defaultLocale.js
var locale2;
var timeFormat;
var timeParse;
var utcFormat;
var utcParse;
defaultLocale2({
  dateTime: "%x, %X",
  date: "%-m/%-d/%Y",
  time: "%-I:%M:%S %p",
  periods: ["AM", "PM"],
  days: ["Sunday", "Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday"],
  shortDays: ["Sun", "Mon", "Tue", "Wed", "Thu", "Fri", "Sat"],
  months: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"],
  shortMonths: ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"]
});
function defaultLocale2(definition3) {
  locale2 = formatLocale(definition3);
  timeFormat = locale2.format;
  timeParse = locale2.parse;
  utcFormat = locale2.utcFormat;
  utcParse = locale2.utcParse;
  return locale2;
}

// ../deepseek-harness/node_modules/.pnpm/vega-format@2.1.3/node_modules/vega-format/build/vega-format.js
function memoize(method2) {
  const cache2 = {};
  return (spec) => cache2[spec] || (cache2[spec] = method2(spec));
}
function trimZeroes(numberFormat2, decimalChar) {
  return (x6) => {
    const str = numberFormat2(x6), dec = str.indexOf(decimalChar);
    if (dec < 0) return str;
    let idx = rightmostDigit(str, dec);
    const end = idx < str.length ? str.slice(idx) : "";
    while (--idx > dec) if (str[idx] !== "0") {
      ++idx;
      break;
    }
    return str.slice(0, idx) + end;
  };
}
function rightmostDigit(str, dec) {
  let i = str.lastIndexOf("e"), c3;
  if (i > 0) return i;
  for (i = str.length; --i > dec; ) {
    c3 = str.charCodeAt(i);
    if (c3 >= 48 && c3 <= 57) return i + 1;
  }
}
function numberLocale(locale4) {
  const format6 = memoize(locale4.format), formatPrefix2 = locale4.formatPrefix;
  return {
    format: format6,
    formatPrefix: formatPrefix2,
    formatFloat(spec) {
      const s = formatSpecifier(spec || ",");
      if (s.precision == null) {
        s.precision = 12;
        switch (s.type) {
          case "%":
            s.precision -= 2;
            break;
          case "e":
            s.precision -= 1;
            break;
        }
        return trimZeroes(
          format6(s),
          // number format
          format6(".1f")(1)[1]
          // decimal point character
        );
      } else {
        return format6(s);
      }
    },
    formatSpan(start, stop2, count2, specifier) {
      specifier = formatSpecifier(specifier == null ? ",f" : specifier);
      const step = tickStep(start, stop2, count2), value4 = Math.max(Math.abs(start), Math.abs(stop2));
      let precision;
      if (specifier.precision == null) {
        switch (specifier.type) {
          case "s": {
            if (!isNaN(precision = precisionPrefix_default(step, value4))) {
              specifier.precision = precision;
            }
            return formatPrefix2(specifier, value4);
          }
          case "":
          case "e":
          case "g":
          case "p":
          case "r": {
            if (!isNaN(precision = precisionRound_default(step, value4))) {
              specifier.precision = precision - (specifier.type === "e");
            }
            break;
          }
          case "f":
          case "%": {
            if (!isNaN(precision = precisionFixed_default(step))) {
              specifier.precision = precision - (specifier.type === "%") * 2;
            }
            break;
          }
        }
      }
      return format6(specifier);
    }
  };
}
var defaultNumberLocale;
resetNumberFormatDefaultLocale();
function resetNumberFormatDefaultLocale() {
  return defaultNumberLocale = numberLocale({
    format,
    formatPrefix
  });
}
function numberFormatLocale(definition3) {
  return numberLocale(locale_default(definition3));
}
function numberFormatDefaultLocale(definition3) {
  return arguments.length ? defaultNumberLocale = numberFormatLocale(definition3) : defaultNumberLocale;
}
function timeMultiFormat(format6, interval3, spec) {
  spec = spec || {};
  if (!isObject(spec)) {
    error(`Invalid time multi-format specifier: ${spec}`);
  }
  const second2 = interval3(SECONDS), minute = interval3(MINUTES), hour = interval3(HOURS), day = interval3(DATE), week3 = interval3(WEEK), month = interval3(MONTH), quarter2 = interval3(QUARTER), year = interval3(YEAR), L = format6(spec[MILLISECONDS] || ".%L"), S = format6(spec[SECONDS] || ":%S"), M2 = format6(spec[MINUTES] || "%I:%M"), H = format6(spec[HOURS] || "%I %p"), d = format6(spec[DATE] || spec[DAY] || "%a %d"), w3 = format6(spec[WEEK] || "%b %d"), m3 = format6(spec[MONTH] || "%B"), q = format6(spec[QUARTER] || "%B"), y6 = format6(spec[YEAR] || "%Y");
  return (date2) => (second2(date2) < date2 ? L : minute(date2) < date2 ? S : hour(date2) < date2 ? M2 : day(date2) < date2 ? H : month(date2) < date2 ? week3(date2) < date2 ? d : w3 : year(date2) < date2 ? quarter2(date2) < date2 ? m3 : q : y6)(date2);
}
function timeLocale(locale4) {
  const timeFormat5 = memoize(locale4.format), utcFormat4 = memoize(locale4.utcFormat);
  return {
    timeFormat: (spec) => isString(spec) ? timeFormat5(spec) : timeMultiFormat(timeFormat5, timeInterval2, spec),
    utcFormat: (spec) => isString(spec) ? utcFormat4(spec) : timeMultiFormat(utcFormat4, utcInterval, spec),
    timeParse: memoize(locale4.parse),
    utcParse: memoize(locale4.utcParse)
  };
}
var defaultTimeLocale;
resetTimeFormatDefaultLocale();
function resetTimeFormatDefaultLocale() {
  return defaultTimeLocale = timeLocale({
    format: timeFormat,
    parse: timeParse,
    utcFormat,
    utcParse
  });
}
function timeFormatLocale(definition3) {
  return timeLocale(formatLocale(definition3));
}
function timeFormatDefaultLocale(definition3) {
  return arguments.length ? defaultTimeLocale = timeFormatLocale(definition3) : defaultTimeLocale;
}
var createLocale = (number9, time4) => extend({}, number9, time4);
function locale3(numberSpec, timeSpec) {
  const number9 = numberSpec ? numberFormatLocale(numberSpec) : numberFormatDefaultLocale();
  const time4 = timeSpec ? timeFormatLocale(timeSpec) : timeFormatDefaultLocale();
  return createLocale(number9, time4);
}
function defaultLocale3(numberSpec, timeSpec) {
  const args = arguments.length;
  if (args && args !== 2) {
    error("defaultLocale expects either zero or two arguments.");
  }
  return args ? createLocale(numberFormatDefaultLocale(numberSpec), timeFormatDefaultLocale(timeSpec)) : createLocale(numberFormatDefaultLocale(), timeFormatDefaultLocale());
}

// ../deepseek-harness/node_modules/.pnpm/vega-loader@5.1.3/node_modules/vega-loader/build/vega-loader.browser.js
var protocol_re = /^(data:|([A-Za-z]+:)?\/\/)/;
var allowed_re = /^(?:(?:(?:f|ht)tps?|mailto|tel|callto|cid|xmpp|file|data):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i;
var whitespace_re = /[\u0000-\u0020\u00A0\u1680\u180E\u2000-\u2029\u205f\u3000]/g;
var fileProtocol = "file://";
function loaderFactory(fs) {
  return (options) => ({
    options: options || {},
    sanitize,
    load,
    fileAccess: false,
    file: fileLoader(fs),
    http: httpLoader
  });
}
async function load(uri, options) {
  const opt = await this.sanitize(uri, options), url = opt.href;
  return opt.localFile ? this.file(url) : this.http(url, options?.http);
}
async function sanitize(uri, options) {
  options = extend({}, this.options, options);
  const fileAccess = this.fileAccess, result = {
    href: null
  };
  let isFile, loadFile, base;
  const isAllowed = allowed_re.test(uri.replace(whitespace_re, ""));
  if (uri == null || typeof uri !== "string" || !isAllowed) {
    error("Sanitize failure, invalid URI: " + stringValue(uri));
  }
  const hasProtocol = protocol_re.test(uri);
  if ((base = options.baseURL) && !hasProtocol) {
    if (!uri.startsWith("/") && !base.endsWith("/")) {
      uri = "/" + uri;
    }
    uri = base + uri;
  }
  loadFile = (isFile = uri.startsWith(fileProtocol)) || options.mode === "file" || options.mode !== "http" && !hasProtocol && fileAccess;
  if (isFile) {
    uri = uri.slice(fileProtocol.length);
  } else if (uri.startsWith("//")) {
    if (options.defaultProtocol === "file") {
      uri = uri.slice(2);
      loadFile = true;
    } else {
      uri = (options.defaultProtocol || "http") + ":" + uri;
    }
  }
  Object.defineProperty(result, "localFile", {
    value: !!loadFile
  });
  result.href = uri;
  if (options.target) {
    result.target = options.target + "";
  }
  if (options.rel) {
    result.rel = options.rel + "";
  }
  if (options.context === "image" && options.crossOrigin) {
    result.crossOrigin = options.crossOrigin + "";
  }
  return result;
}
function fileLoader(fs) {
  return fs ? (filename) => new Promise((accept, reject) => {
    fs.readFile(filename, (error3, data4) => {
      if (error3) reject(error3);
      else accept(data4);
    });
  }) : fileReject;
}
async function fileReject() {
  error("No file system access.");
}
async function httpLoader(url, options) {
  const opt = extend({}, this.options.http, options), type4 = options && options.response, response = await fetch(url, opt);
  return !response.ok ? error(response.status + "" + response.statusText) : isFunction(response[type4]) ? response[type4]() : response.text();
}
var isValid = (_) => _ != null && _ === _;
var isBoolean2 = (_) => _ === "true" || _ === "false" || _ === true || _ === false;
var isDate2 = (_) => !Number.isNaN(Date.parse(_));
var isNumber2 = (_) => !Number.isNaN(+_) && !(_ instanceof Date);
var isInteger = (_) => isNumber2(_) && Number.isInteger(+_);
var typeParsers = {
  boolean: toBoolean,
  integer: toNumber,
  number: toNumber,
  date: toDate,
  string: toString,
  unknown: identity
};
var typeTests = [isBoolean2, isInteger, isNumber2, isDate2];
var typeList = ["boolean", "integer", "number", "date"];
function inferType(values4, field3) {
  if (!values4 || !values4.length) return "unknown";
  const n = values4.length, m3 = typeTests.length, a3 = typeTests.map((_, i) => i + 1);
  for (let i = 0, t = 0, j, value4; i < n; ++i) {
    value4 = field3 ? values4[i][field3] : values4[i];
    for (j = 0; j < m3; ++j) {
      if (a3[j] && isValid(value4) && !typeTests[j](value4)) {
        a3[j] = 0;
        ++t;
        if (t === typeTests.length) return "string";
      }
    }
  }
  return typeList[a3.reduce((u4, v2) => u4 === 0 ? v2 : u4, 0) - 1];
}
function inferTypes(data4, fields) {
  return fields.reduce((types, field3) => {
    types[field3] = inferType(data4, field3);
    return types;
  }, {});
}
function delimitedFormat(delimiter) {
  const parse8 = function(data4, format6) {
    const delim = {
      delimiter
    };
    return dsv(data4, format6 ? extend(format6, delim) : delim);
  };
  parse8.responseType = "text";
  return parse8;
}
function dsv(data4, format6) {
  if (format6.header) {
    data4 = format6.header.map(stringValue).join(format6.delimiter) + "\n" + data4;
  }
  return dsv_default(format6.delimiter).parse(data4 + "");
}
dsv.responseType = "text";
function isBuffer(_) {
  return typeof Buffer === "function" && isFunction(Buffer.isBuffer) ? Buffer.isBuffer(_) : false;
}
function json(data4, format6) {
  const prop = format6 && format6.property ? field(format6.property) : identity;
  return isObject(data4) && !isBuffer(data4) ? parseJSON(prop(data4), format6) : prop(JSON.parse(data4));
}
json.responseType = "json";
function parseJSON(data4, format6) {
  if (!isArray(data4) && isIterable(data4)) {
    data4 = [...data4];
  }
  return format6 && format6.copy ? JSON.parse(JSON.stringify(data4)) : data4;
}
var filters = {
  interior: (a3, b10) => a3 !== b10,
  exterior: (a3, b10) => a3 === b10
};
function topojson(data4, format6) {
  let method2, object2, property2, filter4;
  data4 = json(data4, format6);
  if (format6 && format6.feature) {
    method2 = feature_default;
    property2 = format6.feature;
  } else if (format6 && format6.mesh) {
    method2 = mesh_default;
    property2 = format6.mesh;
    filter4 = filters[format6.filter];
  } else {
    error("Missing TopoJSON feature or mesh parameter.");
  }
  object2 = (object2 = data4.objects[property2]) ? method2(data4, object2, filter4) : error("Invalid TopoJSON object: " + property2);
  return object2 && object2.features || [object2];
}
topojson.responseType = "json";
var format2 = {
  dsv,
  csv: delimitedFormat(","),
  tsv: delimitedFormat("	"),
  json,
  topojson
};
function formats(name, reader) {
  if (arguments.length > 1) {
    format2[name] = reader;
    return this;
  } else {
    return hasOwnProperty(format2, name) ? format2[name] : null;
  }
}
function responseType(type4) {
  const f = formats(type4);
  return f && f.responseType || "text";
}
function read(data4, schema, timeParser, utcParser) {
  schema = schema || {};
  const reader = formats(schema.type || "json");
  if (!reader) error("Unknown data format type: " + schema.type);
  data4 = reader(data4, schema);
  if (schema.parse) parse(data4, schema.parse, timeParser, utcParser);
  if (hasOwnProperty(data4, "columns")) delete data4.columns;
  return data4;
}
function parse(data4, types, timeParser, utcParser) {
  if (!data4.length) return;
  const locale4 = timeFormatDefaultLocale();
  timeParser = timeParser || locale4.timeParse;
  utcParser = utcParser || locale4.utcParse;
  let fields = data4.columns || Object.keys(data4[0]), datum3, field3, i, j, n, m3;
  if (types === "auto") types = inferTypes(data4, fields);
  fields = Object.keys(types);
  const parsers = fields.map((field4) => {
    const type4 = types[field4];
    let parts, pattern;
    if (type4 && (type4.startsWith("date:") || type4.startsWith("utc:"))) {
      parts = type4.split(/:(.+)?/, 2);
      pattern = parts[1];
      if (pattern[0] === "'" && pattern[pattern.length - 1] === "'" || pattern[0] === '"' && pattern[pattern.length - 1] === '"') {
        pattern = pattern.slice(1, -1);
      }
      const parse8 = parts[0] === "utc" ? utcParser : timeParser;
      return parse8(pattern);
    }
    if (!typeParsers[type4]) {
      throw Error("Illegal format pattern: " + field4 + ":" + type4);
    }
    return typeParsers[type4];
  });
  for (i = 0, n = data4.length, m3 = fields.length; i < n; ++i) {
    datum3 = data4[i];
    for (j = 0; j < m3; ++j) {
      field3 = fields[j];
      datum3[field3] = parsers[j](datum3[field3]);
    }
  }
}
var loader = loaderFactory(
  null
  // no file system access
);

// ../deepseek-harness/node_modules/.pnpm/vega-dataflow@6.1.3/node_modules/vega-dataflow/build/vega-dataflow.js
function UniqueList(idFunc) {
  const $ = idFunc || identity, list = [], ids = {};
  list.add = (_) => {
    const id2 = $(_);
    if (!ids[id2]) {
      ids[id2] = 1;
      list.push(_);
    }
    return list;
  };
  list.remove = (_) => {
    const id2 = $(_);
    if (ids[id2]) {
      ids[id2] = 0;
      const idx = list.indexOf(_);
      if (idx >= 0) list.splice(idx, 1);
    }
    return list;
  };
  return list;
}
async function asyncCallback(df, callback) {
  try {
    await callback(df);
  } catch (err) {
    df.error(err);
  }
}
var TUPLE_ID_KEY = Symbol("vega_id");
var TUPLE_ID = 1;
function isTuple(t) {
  return !!(t && tupleid(t));
}
function tupleid(t) {
  return t[TUPLE_ID_KEY];
}
function setid(t, id2) {
  t[TUPLE_ID_KEY] = id2;
  return t;
}
function ingest$1(datum3) {
  const t = datum3 === Object(datum3) ? datum3 : {
    data: datum3
  };
  return tupleid(t) ? t : setid(t, TUPLE_ID++);
}
function derive(t) {
  return rederive(t, ingest$1({}));
}
function rederive(t, d) {
  for (const k in t) d[k] = t[k];
  return d;
}
function replace(t, d) {
  return setid(d, tupleid(t));
}
function stableCompare(cmp, f) {
  return !cmp ? null : f ? (a3, b10) => cmp(a3, b10) || tupleid(f(a3)) - tupleid(f(b10)) : (a3, b10) => cmp(a3, b10) || tupleid(a3) - tupleid(b10);
}
function isChangeSet(v2) {
  return v2 && v2.constructor === changeset;
}
function changeset() {
  const add7 = [], rem2 = [], mod = [], remp = [], modp = [];
  let clean = null, reflow2 = false;
  return {
    constructor: changeset,
    insert(t) {
      const d = array_default(t), n = d.length;
      for (let i = 0; i < n; ++i) add7.push(d[i]);
      return this;
    },
    remove(t) {
      const a3 = isFunction(t) ? remp : rem2, d = array_default(t), n = d.length;
      for (let i = 0; i < n; ++i) a3.push(d[i]);
      return this;
    },
    modify(t, field3, value4) {
      const m3 = {
        field: field3,
        value: constant_default(value4)
      };
      if (isFunction(t)) {
        m3.filter = t;
        modp.push(m3);
      } else {
        m3.tuple = t;
        mod.push(m3);
      }
      return this;
    },
    encode(t, set7) {
      if (isFunction(t)) modp.push({
        filter: t,
        field: set7
      });
      else mod.push({
        tuple: t,
        field: set7
      });
      return this;
    },
    clean(value4) {
      clean = value4;
      return this;
    },
    reflow() {
      reflow2 = true;
      return this;
    },
    pulse(pulse2, tuples) {
      const cur = {}, out = {};
      let i, n, m3, f, t, id2;
      for (i = 0, n = tuples.length; i < n; ++i) {
        cur[tupleid(tuples[i])] = 1;
      }
      for (i = 0, n = rem2.length; i < n; ++i) {
        t = rem2[i];
        cur[tupleid(t)] = -1;
      }
      for (i = 0, n = remp.length; i < n; ++i) {
        f = remp[i];
        tuples.forEach((t4) => {
          if (f(t4)) cur[tupleid(t4)] = -1;
        });
      }
      for (i = 0, n = add7.length; i < n; ++i) {
        t = add7[i];
        id2 = tupleid(t);
        if (cur[id2]) {
          cur[id2] = 1;
        } else {
          pulse2.add.push(ingest$1(add7[i]));
        }
      }
      for (i = 0, n = tuples.length; i < n; ++i) {
        t = tuples[i];
        if (cur[tupleid(t)] < 0) pulse2.rem.push(t);
      }
      function modify3(t4, f2, v2) {
        if (v2) {
          t4[f2] = v2(t4);
        } else {
          pulse2.encode = f2;
        }
        if (!reflow2) out[tupleid(t4)] = t4;
      }
      for (i = 0, n = mod.length; i < n; ++i) {
        m3 = mod[i];
        t = m3.tuple;
        f = m3.field;
        id2 = cur[tupleid(t)];
        if (id2 > 0) {
          modify3(t, f, m3.value);
          pulse2.modifies(f);
        }
      }
      for (i = 0, n = modp.length; i < n; ++i) {
        m3 = modp[i];
        f = m3.filter;
        tuples.forEach((t4) => {
          if (f(t4) && cur[tupleid(t4)] > 0) {
            modify3(t4, m3.field, m3.value);
          }
        });
        pulse2.modifies(m3.field);
      }
      if (reflow2) {
        pulse2.mod = rem2.length || remp.length ? tuples.filter((t4) => cur[tupleid(t4)] > 0) : tuples.slice();
      } else {
        for (id2 in out) pulse2.mod.push(out[id2]);
      }
      if (clean || clean == null && (rem2.length || remp.length)) {
        pulse2.clean(true);
      }
      return pulse2;
    }
  };
}
var CACHE = "_:mod:_";
function Parameters() {
  Object.defineProperty(this, CACHE, {
    writable: true,
    value: {}
  });
}
Parameters.prototype = {
  /**
   * Set a parameter value. If the parameter value changes, the parameter
   * will be recorded as modified.
   * @param {string} name - The parameter name.
   * @param {number} index - The index into an array-value parameter. Ignored if
   *   the argument is undefined, null or less than zero.
   * @param {*} value - The parameter value to set.
   * @param {boolean} [force=false] - If true, records the parameter as modified
   *   even if the value is unchanged.
   * @return {Parameters} - This parameter object.
   */
  set(name, index4, value4, force) {
    const o = this, v2 = o[name], mod = o[CACHE];
    if (index4 != null && index4 >= 0) {
      if (v2[index4] !== value4 || force) {
        v2[index4] = value4;
        mod[index4 + ":" + name] = -1;
        mod[name] = -1;
      }
    } else if (v2 !== value4 || force) {
      o[name] = value4;
      mod[name] = isArray(value4) ? 1 + value4.length : -1;
    }
    return o;
  },
  /**
   * Tests if one or more parameters has been modified. If invoked with no
   * arguments, returns true if any parameter value has changed. If the first
   * argument is array, returns trues if any parameter name in the array has
   * changed. Otherwise, tests if the given name and optional array index has
   * changed.
   * @param {string} name - The parameter name to test.
   * @param {number} [index=undefined] - The parameter array index to test.
   * @return {boolean} - Returns true if a queried parameter was modified.
   */
  modified(name, index4) {
    const mod = this[CACHE];
    if (!arguments.length) {
      for (const k in mod) {
        if (mod[k]) return true;
      }
      return false;
    } else if (isArray(name)) {
      for (let k = 0; k < name.length; ++k) {
        if (mod[name[k]]) return true;
      }
      return false;
    }
    return index4 != null && index4 >= 0 ? index4 + 1 < mod[name] || !!mod[index4 + ":" + name] : !!mod[name];
  },
  /**
   * Clears the modification records. After calling this method,
   * all parameters are considered unmodified.
   */
  clear() {
    this[CACHE] = {};
    return this;
  }
};
var OP_ID = 0;
var PULSE = "pulse";
var NO_PARAMS = new Parameters();
var SKIP$1 = 1;
var MODIFIED = 2;
function Operator(init2, update3, params2, react) {
  this.id = ++OP_ID;
  this.value = init2;
  this.stamp = -1;
  this.rank = -1;
  this.qrank = -1;
  this.flags = 0;
  if (update3) {
    this._update = update3;
  }
  if (params2) this.parameters(params2, react);
}
function flag(bit) {
  return function(state) {
    const f = this.flags;
    if (arguments.length === 0) return !!(f & bit);
    this.flags = state ? f | bit : f & ~bit;
    return this;
  };
}
Operator.prototype = {
  /**
   * Returns a list of target operators dependent on this operator.
   * If this list does not exist, it is created and then returned.
   * @return {UniqueList}
   */
  targets() {
    return this._targets || (this._targets = UniqueList(id));
  },
  /**
   * Sets the value of this operator.
   * @param {*} value - the value to set.
   * @return {Number} Returns 1 if the operator value has changed
   *   according to strict equality, returns 0 otherwise.
   */
  set(value4) {
    if (this.value !== value4) {
      this.value = value4;
      return 1;
    } else {
      return 0;
    }
  },
  /**
   * Indicates that operator evaluation should be skipped on the next pulse.
   * This operator will still propagate incoming pulses, but its update function
   * will not be invoked. The skip flag is reset after every pulse, so calling
   * this method will affect processing of the next pulse only.
   */
  skip: flag(SKIP$1),
  /**
   * Indicates that this operator's value has been modified on its most recent
   * pulse. Normally modification is checked via strict equality; however, in
   * some cases it is more efficient to update the internal state of an object.
   * In those cases, the modified flag can be used to trigger propagation. Once
   * set, the modification flag persists across pulses until unset. The flag can
   * be used with the last timestamp to test if a modification is recent.
   */
  modified: flag(MODIFIED),
  /**
   * Sets the parameters for this operator. The parameter values are analyzed for
   * operator instances. If found, this operator will be added as a dependency
   * of the parameterizing operator. Operator values are dynamically marshalled
   * from each operator parameter prior to evaluation. If a parameter value is
   * an array, the array will also be searched for Operator instances. However,
   * the search does not recurse into sub-arrays or object properties.
   * @param {object} params - A hash of operator parameters.
   * @param {boolean} [react=true] - A flag indicating if this operator should
   *   automatically update (react) when parameter values change. In other words,
   *   this flag determines if the operator registers itself as a listener on
   *   any upstream operators included in the parameters.
   * @param {boolean} [initonly=false] - A flag indicating if this operator
   *   should calculate an update only upon its initial evaluation, then
   *   deregister dependencies and suppress all future update invocations.
   * @return {Operator[]} - An array of upstream dependencies.
   */
  parameters(params2, react, initonly) {
    react = react !== false;
    const argval = this._argval = this._argval || new Parameters(), argops = this._argops = this._argops || [], deps = [];
    let name, value4, n, i;
    const add7 = (name2, index4, value5) => {
      if (value5 instanceof Operator) {
        if (value5 !== this) {
          if (react) value5.targets().add(this);
          deps.push(value5);
        }
        argops.push({
          op: value5,
          name: name2,
          index: index4
        });
      } else {
        argval.set(name2, index4, value5);
      }
    };
    for (name in params2) {
      value4 = params2[name];
      if (name === PULSE) {
        array_default(value4).forEach((op) => {
          if (!(op instanceof Operator)) {
            error("Pulse parameters must be operator instances.");
          } else if (op !== this) {
            op.targets().add(this);
            deps.push(op);
          }
        });
        this.source = value4;
      } else if (isArray(value4)) {
        argval.set(name, -1, Array(n = value4.length));
        for (i = 0; i < n; ++i) add7(name, i, value4[i]);
      } else {
        add7(name, -1, value4);
      }
    }
    this.marshall().clear();
    if (initonly) argops.initonly = true;
    return deps;
  },
  /**
   * Internal method for marshalling parameter values.
   * Visits each operator dependency to pull the latest value.
   * @return {Parameters} A Parameters object to pass to the update function.
   */
  marshall(stamp) {
    const argval = this._argval || NO_PARAMS, argops = this._argops;
    let item, i, op, mod;
    if (argops) {
      const n = argops.length;
      for (i = 0; i < n; ++i) {
        item = argops[i];
        op = item.op;
        mod = op.modified() && op.stamp === stamp;
        argval.set(item.name, item.index, op.value, mod);
      }
      if (argops.initonly) {
        for (i = 0; i < n; ++i) {
          item = argops[i];
          item.op.targets().remove(this);
        }
        this._argops = null;
        this._update = null;
      }
    }
    return argval;
  },
  /**
   * Detach this operator from the dataflow.
   * Unregisters listeners on upstream dependencies.
   */
  detach() {
    const argops = this._argops;
    let i, n, item, op;
    if (argops) {
      for (i = 0, n = argops.length; i < n; ++i) {
        item = argops[i];
        op = item.op;
        if (op._targets) {
          op._targets.remove(this);
        }
      }
    }
    this.pulse = null;
    this.source = null;
  },
  /**
   * Delegate method to perform operator processing.
   * Subclasses can override this method to perform custom processing.
   * By default, it marshalls parameters and calls the update function
   * if that function is defined. If the update function does not
   * change the operator value then StopPropagation is returned.
   * If no update function is defined, this method does nothing.
   * @param {Pulse} pulse - the current dataflow pulse.
   * @return The output pulse or StopPropagation. A falsy return value
   *   (including undefined) will let the input pulse pass through.
   */
  evaluate(pulse2) {
    const update3 = this._update;
    if (update3) {
      const params2 = this.marshall(pulse2.stamp), v2 = update3.call(this, params2, pulse2);
      params2.clear();
      if (v2 !== this.value) {
        this.value = v2;
      } else if (!this.modified()) {
        return pulse2.StopPropagation;
      }
    }
  },
  /**
   * Run this operator for the current pulse. If this operator has already
   * been run at (or after) the pulse timestamp, returns StopPropagation.
   * Internally, this method calls {@link evaluate} to perform processing.
   * If {@link evaluate} returns a falsy value, the input pulse is returned.
   * This method should NOT be overridden, instead overrride {@link evaluate}.
   * @param {Pulse} pulse - the current dataflow pulse.
   * @return the output pulse for this operator (or StopPropagation)
   */
  run(pulse2) {
    if (pulse2.stamp < this.stamp) return pulse2.StopPropagation;
    let rv;
    if (this.skip()) {
      this.skip(false);
      rv = 0;
    } else {
      rv = this.evaluate(pulse2);
    }
    return this.pulse = rv || pulse2;
  }
};
function add(init2, update3, params2, react) {
  let shift = 1, op;
  if (init2 instanceof Operator) {
    op = init2;
  } else if (init2 && init2.prototype instanceof Operator) {
    op = new init2();
  } else if (isFunction(init2)) {
    op = new Operator(null, init2);
  } else {
    shift = 0;
    op = new Operator(init2, update3);
  }
  this.rank(op);
  if (shift) {
    react = params2;
    params2 = update3;
  }
  if (params2) this.connect(op, op.parameters(params2, react));
  this.touch(op);
  return op;
}
function connect(target2, sources) {
  const targetRank = target2.rank, n = sources.length;
  for (let i = 0; i < n; ++i) {
    if (targetRank < sources[i].rank) {
      this.rerank(target2);
      return;
    }
  }
}
var STREAM_ID = 0;
function EventStream(filter4, apply3, receive) {
  this.id = ++STREAM_ID;
  this.value = null;
  if (receive) this.receive = receive;
  if (filter4) this._filter = filter4;
  if (apply3) this._apply = apply3;
}
function stream(filter4, apply3, receive) {
  return new EventStream(filter4, apply3, receive);
}
EventStream.prototype = {
  _filter: truthy,
  _apply: identity,
  targets() {
    return this._targets || (this._targets = UniqueList(id));
  },
  consume(_) {
    if (!arguments.length) return !!this._consume;
    this._consume = !!_;
    return this;
  },
  receive(evt) {
    if (this._filter(evt)) {
      const val = this.value = this._apply(evt), trg = this._targets, n = trg ? trg.length : 0;
      for (let i = 0; i < n; ++i) trg[i].receive(val);
      if (this._consume) {
        evt.preventDefault();
        evt.stopPropagation();
      }
    }
  },
  filter(filter4) {
    const s = stream(filter4);
    this.targets().add(s);
    return s;
  },
  apply(apply3) {
    const s = stream(null, apply3);
    this.targets().add(s);
    return s;
  },
  merge() {
    const s = stream();
    this.targets().add(s);
    for (let i = 0, n = arguments.length; i < n; ++i) {
      arguments[i].targets().add(s);
    }
    return s;
  },
  throttle(pause) {
    let t = -1;
    return this.filter(() => {
      const now2 = Date.now();
      if (now2 - t > pause) {
        t = now2;
        return 1;
      } else {
        return 0;
      }
    });
  },
  debounce(delay) {
    const s = stream();
    this.targets().add(stream(null, null, debounce(delay, (e) => {
      const df = e.dataflow;
      s.receive(e);
      if (df && df.run) df.run();
    })));
    return s;
  },
  between(a3, b10) {
    let active = false;
    a3.targets().add(stream(null, null, () => active = true));
    b10.targets().add(stream(null, null, () => active = false));
    return this.filter(() => active);
  },
  detach() {
    this._filter = truthy;
    this._targets = null;
  }
};
function events(source4, type4, filter4, apply3) {
  const df = this, s = stream(filter4, apply3), send = function(e) {
    e.dataflow = df;
    try {
      s.receive(e);
    } catch (error3) {
      df.error(error3);
    } finally {
      df.run();
    }
  };
  let sources;
  if (typeof source4 === "string" && typeof document !== "undefined") {
    sources = document.querySelectorAll(source4);
  } else {
    sources = array_default(source4);
  }
  const n = sources.length;
  for (let i = 0; i < n; ++i) {
    sources[i].addEventListener(type4, send);
  }
  return s;
}
function parse2(data4, format6) {
  const locale4 = this.locale();
  return read(data4, format6, locale4.timeParse, locale4.utcParse);
}
function ingest(target2, data4, format6) {
  data4 = this.parse(data4, format6);
  return this.pulse(target2, this.changeset().insert(data4));
}
async function request(url, format6) {
  const df = this;
  let status = 0, data4;
  try {
    data4 = await df.loader().load(url, {
      context: "dataflow",
      response: responseType(format6 && format6.type)
    });
    try {
      data4 = df.parse(data4, format6);
    } catch (err) {
      status = -2;
      df.warn("Data ingestion failed", url, err);
    }
  } catch (err) {
    status = -1;
    df.warn("Loading failed", url, err);
  }
  return {
    data: data4,
    status
  };
}
async function preload(target2, url, format6) {
  const df = this, pending = df._pending || loadPending(df);
  pending.requests += 1;
  const res = await df.request(url, format6);
  df.pulse(target2, df.changeset().remove(truthy).insert(res.data || []));
  pending.done();
  return res;
}
function loadPending(df) {
  let accept;
  const pending = new Promise((a3) => accept = a3);
  pending.requests = 0;
  pending.done = () => {
    if (--pending.requests === 0) {
      df._pending = null;
      accept(df);
    }
  };
  return df._pending = pending;
}
var SKIP = {
  skip: true
};
function on(source4, target2, update3, params2, options) {
  const fn = source4 instanceof Operator ? onOperator : onStream;
  fn(this, source4, target2, update3, params2, options);
  return this;
}
function onStream(df, stream2, target2, update3, params2, options) {
  const opt = extend({}, options, SKIP);
  let func, op;
  if (!isFunction(target2)) target2 = constant_default(target2);
  if (update3 === void 0) {
    func = (e) => df.touch(target2(e));
  } else if (isFunction(update3)) {
    op = new Operator(null, update3, params2, false);
    func = (e) => {
      op.evaluate(e);
      const t = target2(e), v2 = op.value;
      isChangeSet(v2) ? df.pulse(t, v2, options) : df.update(t, v2, opt);
    };
  } else {
    func = (e) => df.update(target2(e), update3, opt);
  }
  stream2.apply(func);
}
function onOperator(df, source4, target2, update3, params2, options) {
  if (update3 === void 0) {
    source4.targets().add(target2);
  } else {
    const opt = options || {}, op = new Operator(null, updater(target2, update3), params2, false);
    op.modified(opt.force);
    op.rank = source4.rank;
    source4.targets().add(op);
    if (target2) {
      op.skip(true);
      op.value = target2.value;
      op.targets().add(target2);
      df.connect(target2, [op]);
    }
  }
}
function updater(target2, update3) {
  update3 = isFunction(update3) ? update3 : constant_default(update3);
  return target2 ? function(_, pulse2) {
    const value4 = update3(_, pulse2);
    if (!target2.skip()) {
      target2.skip(value4 !== this.value).value = value4;
    }
    return value4;
  } : update3;
}
function rank(op) {
  op.rank = ++this._rank;
}
function rerank(op) {
  const queue = [op];
  let cur, list, i;
  while (queue.length) {
    this.rank(cur = queue.pop());
    if (list = cur._targets) {
      for (i = list.length; --i >= 0; ) {
        queue.push(cur = list[i]);
        if (cur === op) error("Cycle detected in dataflow graph.");
      }
    }
  }
}
var StopPropagation = {};
var ADD = 1 << 0;
var REM = 1 << 1;
var MOD = 1 << 2;
var ADD_REM = ADD | REM;
var ADD_MOD = ADD | MOD;
var ALL = ADD | REM | MOD;
var REFLOW = 1 << 3;
var SOURCE = 1 << 4;
var NO_SOURCE = 1 << 5;
var NO_FIELDS = 1 << 6;
function Pulse(dataflow, stamp, encode3) {
  this.dataflow = dataflow;
  this.stamp = stamp == null ? -1 : stamp;
  this.add = [];
  this.rem = [];
  this.mod = [];
  this.fields = null;
  this.encode = encode3 || null;
}
function materialize(data4, filter4) {
  const out = [];
  visitArray(data4, filter4, (_) => out.push(_));
  return out;
}
function filter(pulse2, flags) {
  const map3 = {};
  pulse2.visit(flags, (t) => {
    map3[tupleid(t)] = 1;
  });
  return (t) => map3[tupleid(t)] ? null : t;
}
function addFilter(a3, b10) {
  return a3 ? (t, i) => a3(t, i) && b10(t, i) : b10;
}
Pulse.prototype = {
  /**
   * Sentinel value indicating pulse propagation should stop.
   */
  StopPropagation,
  /**
   * Boolean flag indicating ADD (added) tuples.
   */
  ADD,
  /**
   * Boolean flag indicating REM (removed) tuples.
   */
  REM,
  /**
   * Boolean flag indicating MOD (modified) tuples.
   */
  MOD,
  /**
   * Boolean flag indicating ADD (added) and REM (removed) tuples.
   */
  ADD_REM,
  /**
   * Boolean flag indicating ADD (added) and MOD (modified) tuples.
   */
  ADD_MOD,
  /**
   * Boolean flag indicating ADD, REM and MOD tuples.
   */
  ALL,
  /**
   * Boolean flag indicating all tuples in a data source
   * except for the ADD, REM and MOD tuples.
   */
  REFLOW,
  /**
   * Boolean flag indicating a 'pass-through' to a
   * backing data source, ignoring ADD, REM and MOD tuples.
   */
  SOURCE,
  /**
   * Boolean flag indicating that source data should be
   * suppressed when creating a forked pulse.
   */
  NO_SOURCE,
  /**
   * Boolean flag indicating that field modifications should be
   * suppressed when creating a forked pulse.
   */
  NO_FIELDS,
  /**
   * Creates a new pulse based on the values of this pulse.
   * The dataflow, time stamp and field modification values are copied over.
   * By default, new empty ADD, REM and MOD arrays are created.
   * @param {number} flags - Integer of boolean flags indicating which (if any)
   *   tuple arrays should be copied to the new pulse. The supported flag values
   *   are ADD, REM and MOD. Array references are copied directly: new array
   *   instances are not created.
   * @return {Pulse} - The forked pulse instance.
   * @see init
   */
  fork(flags) {
    return new Pulse(this.dataflow).init(this, flags);
  },
  /**
   * Creates a copy of this pulse with new materialized array
   * instances for the ADD, REM, MOD, and SOURCE arrays.
   * The dataflow, time stamp and field modification values are copied over.
   * @return {Pulse} - The cloned pulse instance.
   * @see init
   */
  clone() {
    const p = this.fork(ALL);
    p.add = p.add.slice();
    p.rem = p.rem.slice();
    p.mod = p.mod.slice();
    if (p.source) p.source = p.source.slice();
    return p.materialize(ALL | SOURCE);
  },
  /**
   * Returns a pulse that adds all tuples from a backing source. This is
   * useful for cases where operators are added to a dataflow after an
   * upstream data pipeline has already been processed, ensuring that
   * new operators can observe all tuples within a stream.
   * @return {Pulse} - A pulse instance with all source tuples included
   *   in the add array. If the current pulse already has all source
   *   tuples in its add array, it is returned directly. If the current
   *   pulse does not have a backing source, it is returned directly.
   */
  addAll() {
    let p = this;
    const reuse = !p.source || p.add === p.rem || !p.rem.length && p.source.length === p.add.length;
    if (reuse) {
      return p;
    } else {
      p = new Pulse(this.dataflow).init(this);
      p.add = p.source;
      p.rem = [];
      return p;
    }
  },
  /**
   * Initialize this pulse based on the values of another pulse. This method
   * is used internally by {@link fork} to initialize a new forked tuple.
   * The dataflow, time stamp and field modification values are copied over.
   * By default, new empty ADD, REM and MOD arrays are created.
   * @param {Pulse} src - The source pulse to copy from.
   * @param {number} flags - Integer of boolean flags indicating which (if any)
   *   tuple arrays should be copied to the new pulse. The supported flag values
   *   are ADD, REM and MOD. Array references are copied directly: new array
   *   instances are not created. By default, source data arrays are copied
   *   to the new pulse. Use the NO_SOURCE flag to enforce a null source.
   * @return {Pulse} - Returns this Pulse instance.
   */
  init(src, flags) {
    const p = this;
    p.stamp = src.stamp;
    p.encode = src.encode;
    if (src.fields && !(flags & NO_FIELDS)) {
      p.fields = src.fields;
    }
    if (flags & ADD) {
      p.addF = src.addF;
      p.add = src.add;
    } else {
      p.addF = null;
      p.add = [];
    }
    if (flags & REM) {
      p.remF = src.remF;
      p.rem = src.rem;
    } else {
      p.remF = null;
      p.rem = [];
    }
    if (flags & MOD) {
      p.modF = src.modF;
      p.mod = src.mod;
    } else {
      p.modF = null;
      p.mod = [];
    }
    if (flags & NO_SOURCE) {
      p.srcF = null;
      p.source = null;
    } else {
      p.srcF = src.srcF;
      p.source = src.source;
      if (src.cleans) p.cleans = src.cleans;
    }
    return p;
  },
  /**
   * Schedules a function to run after pulse propagation completes.
   * @param {function} func - The function to run.
   */
  runAfter(func) {
    this.dataflow.runAfter(func);
  },
  /**
   * Indicates if tuples have been added, removed or modified.
   * @param {number} [flags] - The tuple types (ADD, REM or MOD) to query.
   *   Defaults to ALL, returning true if any tuple type has changed.
   * @return {boolean} - Returns true if one or more queried tuple types have
   *   changed, false otherwise.
   */
  changed(flags) {
    const f = flags || ALL;
    return f & ADD && this.add.length || f & REM && this.rem.length || f & MOD && this.mod.length;
  },
  /**
   * Forces a "reflow" of tuple values, such that all tuples in the backing
   * source are added to the MOD set, unless already present in the ADD set.
   * @param {boolean} [fork=false] - If true, returns a forked copy of this
   *   pulse, and invokes reflow on that derived pulse.
   * @return {Pulse} - The reflowed pulse instance.
   */
  reflow(fork) {
    if (fork) return this.fork(ALL).reflow();
    const len = this.add.length, src = this.source && this.source.length;
    if (src && src !== len) {
      this.mod = this.source;
      if (len) this.filter(MOD, filter(this, ADD));
    }
    return this;
  },
  /**
   * Get/set metadata to pulse requesting garbage collection
   * to reclaim currently unused resources.
   */
  clean(value4) {
    if (arguments.length) {
      this.cleans = !!value4;
      return this;
    } else {
      return this.cleans;
    }
  },
  /**
   * Marks one or more data field names as modified to assist dependency
   * tracking and incremental processing by transform operators.
   * @param {string|Array<string>} _ - The field(s) to mark as modified.
   * @return {Pulse} - This pulse instance.
   */
  modifies(_) {
    const hash2 = this.fields || (this.fields = {});
    if (isArray(_)) {
      _.forEach((f) => hash2[f] = true);
    } else {
      hash2[_] = true;
    }
    return this;
  },
  /**
   * Checks if one or more data fields have been modified during this pulse
   * propagation timestamp.
   * @param {string|Array<string>} _ - The field(s) to check for modified.
   * @param {boolean} nomod - If true, will check the modified flag even if
   *   no mod tuples exist. If false (default), mod tuples must be present.
   * @return {boolean} - Returns true if any of the provided fields has been
   *   marked as modified, false otherwise.
   */
  modified(_, nomod) {
    const fields = this.fields;
    return !((nomod || this.mod.length) && fields) ? false : !arguments.length ? !!fields : isArray(_) ? _.some((f) => fields[f]) : fields[_];
  },
  /**
   * Adds a filter function to one more tuple sets. Filters are applied to
   * backing tuple arrays, to determine the actual set of tuples considered
   * added, removed or modified. They can be used to delay materialization of
   * a tuple set in order to avoid expensive array copies. In addition, the
   * filter functions can serve as value transformers: unlike standard predicate
   * function (which return boolean values), Pulse filters should return the
   * actual tuple value to process. If a tuple set is already filtered, the
   * new filter function will be appended into a conjuntive ('and') query.
   * @param {number} flags - Flags indicating the tuple set(s) to filter.
   * @param {function(*):object} filter - Filter function that will be applied
   *   to the tuple set array, and should return a data tuple if the value
   *   should be included in the tuple set, and falsy (or null) otherwise.
   * @return {Pulse} - Returns this pulse instance.
   */
  filter(flags, filter4) {
    const p = this;
    if (flags & ADD) p.addF = addFilter(p.addF, filter4);
    if (flags & REM) p.remF = addFilter(p.remF, filter4);
    if (flags & MOD) p.modF = addFilter(p.modF, filter4);
    if (flags & SOURCE) p.srcF = addFilter(p.srcF, filter4);
    return p;
  },
  /**
   * Materialize one or more tuple sets in this pulse. If the tuple set(s) have
   * a registered filter function, it will be applied and the tuple set(s) will
   * be replaced with materialized tuple arrays.
   * @param {number} flags - Flags indicating the tuple set(s) to materialize.
   * @return {Pulse} - Returns this pulse instance.
   */
  materialize(flags) {
    flags = flags || ALL;
    const p = this;
    if (flags & ADD && p.addF) {
      p.add = materialize(p.add, p.addF);
      p.addF = null;
    }
    if (flags & REM && p.remF) {
      p.rem = materialize(p.rem, p.remF);
      p.remF = null;
    }
    if (flags & MOD && p.modF) {
      p.mod = materialize(p.mod, p.modF);
      p.modF = null;
    }
    if (flags & SOURCE && p.srcF) {
      p.source = p.source.filter(p.srcF);
      p.srcF = null;
    }
    return p;
  },
  /**
   * Visit one or more tuple sets in this pulse.
   * @param {number} flags - Flags indicating the tuple set(s) to visit.
   *   Legal values are ADD, REM, MOD and SOURCE (if a backing data source
   *   has been set).
   * @param {function(object):*} - Visitor function invoked per-tuple.
   * @return {Pulse} - Returns this pulse instance.
   */
  visit(flags, visitor) {
    const p = this, v2 = visitor;
    if (flags & SOURCE) {
      visitArray(p.source, p.srcF, v2);
      return p;
    }
    if (flags & ADD) visitArray(p.add, p.addF, v2);
    if (flags & REM) visitArray(p.rem, p.remF, v2);
    if (flags & MOD) visitArray(p.mod, p.modF, v2);
    const src = p.source;
    if (flags & REFLOW && src) {
      const sum3 = p.add.length + p.mod.length;
      if (sum3 === src.length) ;
      else if (sum3) {
        visitArray(src, filter(p, ADD_MOD), v2);
      } else {
        visitArray(src, p.srcF, v2);
      }
    }
    return p;
  }
};
function MultiPulse(dataflow, stamp, pulses, encode3) {
  const p = this;
  let c3 = 0;
  this.dataflow = dataflow;
  this.stamp = stamp;
  this.fields = null;
  this.encode = encode3 || null;
  this.pulses = pulses;
  for (const pulse2 of pulses) {
    if (pulse2.stamp !== stamp) continue;
    if (pulse2.fields) {
      const hash2 = p.fields || (p.fields = {});
      for (const f in pulse2.fields) {
        hash2[f] = 1;
      }
    }
    if (pulse2.changed(p.ADD)) c3 |= p.ADD;
    if (pulse2.changed(p.REM)) c3 |= p.REM;
    if (pulse2.changed(p.MOD)) c3 |= p.MOD;
  }
  this.changes = c3;
}
inherits_default(MultiPulse, Pulse, {
  /**
   * Creates a new pulse based on the values of this pulse.
   * The dataflow, time stamp and field modification values are copied over.
   * @return {Pulse}
   */
  fork(flags) {
    const p = new Pulse(this.dataflow).init(this, flags & this.NO_FIELDS);
    if (flags !== void 0) {
      if (flags & p.ADD) this.visit(p.ADD, (t) => p.add.push(t));
      if (flags & p.REM) this.visit(p.REM, (t) => p.rem.push(t));
      if (flags & p.MOD) this.visit(p.MOD, (t) => p.mod.push(t));
    }
    return p;
  },
  changed(flags) {
    return this.changes & flags;
  },
  modified(_) {
    const p = this, fields = p.fields;
    return !(fields && p.changes & p.MOD) ? 0 : isArray(_) ? _.some((f) => fields[f]) : fields[_];
  },
  filter() {
    error("MultiPulse does not support filtering.");
  },
  materialize() {
    error("MultiPulse does not support materialization.");
  },
  visit(flags, visitor) {
    const p = this, pulses = p.pulses, n = pulses.length;
    let i = 0;
    if (flags & p.SOURCE) {
      for (; i < n; ++i) {
        pulses[i].visit(flags, visitor);
      }
    } else {
      for (; i < n; ++i) {
        if (pulses[i].stamp === p.stamp) {
          pulses[i].visit(flags, visitor);
        }
      }
    }
    return p;
  }
});
async function evaluate(encode3, prerun, postrun) {
  const df = this, async = [];
  if (df._pulse) return reentrant(df);
  if (df._pending) await df._pending;
  if (prerun) await asyncCallback(df, prerun);
  if (!df._touched.length) {
    df.debug("Dataflow invoked, but nothing to do.");
    return df;
  }
  const stamp = ++df._clock;
  df._pulse = new Pulse(df, stamp, encode3);
  df._touched.forEach((op2) => df._enqueue(op2, true));
  df._touched = UniqueList(id);
  let count2 = 0, op, next, error3;
  try {
    while (df._heap.size() > 0) {
      op = df._heap.pop();
      if (op.rank !== op.qrank) {
        df._enqueue(op, true);
        continue;
      }
      next = op.run(df._getPulse(op, encode3));
      if (next.then) {
        next = await next;
      } else if (next.async) {
        async.push(next.async);
        next = StopPropagation;
      }
      if (next !== StopPropagation) {
        if (op._targets) op._targets.forEach((op2) => df._enqueue(op2));
      }
      ++count2;
    }
  } catch (err) {
    df._heap.clear();
    error3 = err;
  }
  df._input = {};
  df._pulse = null;
  df.debug(`Pulse ${stamp}: ${count2} operators`);
  if (error3) {
    df._postrun = [];
    df.error(error3);
  }
  if (df._postrun.length) {
    const pr = df._postrun.sort((a3, b10) => b10.priority - a3.priority);
    df._postrun = [];
    for (let i = 0; i < pr.length; ++i) {
      await asyncCallback(df, pr[i].callback);
    }
  }
  if (postrun) await asyncCallback(df, postrun);
  if (async.length) {
    Promise.all(async).then((cb) => df.runAsync(null, () => {
      cb.forEach((f) => {
        try {
          f(df);
        } catch (err) {
          df.error(err);
        }
      });
    }));
  }
  return df;
}
async function runAsync(encode3, prerun, postrun) {
  while (this._running) await this._running;
  const clear2 = () => this._running = null;
  (this._running = this.evaluate(encode3, prerun, postrun)).then(clear2, clear2);
  return this._running;
}
function run(encode3, prerun, postrun) {
  return this._pulse ? reentrant(this) : (this.evaluate(encode3, prerun, postrun), this);
}
function runAfter(callback, enqueue2, priority) {
  if (this._pulse || enqueue2) {
    this._postrun.push({
      priority: priority || 0,
      callback
    });
  } else {
    try {
      callback(this);
    } catch (err) {
      this.error(err);
    }
  }
}
function reentrant(df) {
  df.error("Dataflow already running. Use runAsync() to chain invocations.");
  return df;
}
function enqueue(op, force) {
  const q = op.stamp < this._clock;
  if (q) op.stamp = this._clock;
  if (q || force) {
    op.qrank = op.rank;
    this._heap.push(op);
  }
}
function getPulse(op, encode3) {
  const s = op.source, stamp = this._clock;
  return s && isArray(s) ? new MultiPulse(this, stamp, s.map((_) => _.pulse), encode3) : this._input[op.id] || singlePulse(this._pulse, s && s.pulse);
}
function singlePulse(p, s) {
  if (s && s.stamp === p.stamp) {
    return s;
  }
  p = p.fork();
  if (s && s !== StopPropagation) {
    p.source = s.source;
  }
  return p;
}
var NO_OPT = {
  skip: false,
  force: false
};
function touch(op, options) {
  const opt = options || NO_OPT;
  if (this._pulse) {
    this._enqueue(op);
  } else {
    this._touched.add(op);
  }
  if (opt.skip) op.skip(true);
  return this;
}
function update(op, value4, options) {
  const opt = options || NO_OPT;
  if (op.set(value4) || opt.force) {
    this.touch(op, opt);
  }
  return this;
}
function pulse(op, changeset2, options) {
  this.touch(op, options || NO_OPT);
  const p = new Pulse(this, this._clock + (this._pulse ? 0 : 1)), t = op.pulse && op.pulse.source || [];
  p.target = op;
  this._input[op.id] = changeset2.pulse(p, t);
  return this;
}
function Heap(cmp) {
  let nodes = [];
  return {
    clear: () => nodes = [],
    size: () => nodes.length,
    peek: () => nodes[0],
    push: (x6) => {
      nodes.push(x6);
      return siftdown(nodes, 0, nodes.length - 1, cmp);
    },
    pop: () => {
      const last = nodes.pop();
      let item;
      if (nodes.length) {
        item = nodes[0];
        nodes[0] = last;
        siftup(nodes, 0, cmp);
      } else {
        item = last;
      }
      return item;
    }
  };
}
function siftdown(array4, start, idx, cmp) {
  let parent, pidx;
  const item = array4[idx];
  while (idx > start) {
    pidx = idx - 1 >> 1;
    parent = array4[pidx];
    if (cmp(item, parent) < 0) {
      array4[idx] = parent;
      idx = pidx;
      continue;
    }
    break;
  }
  return array4[idx] = item;
}
function siftup(array4, idx, cmp) {
  const start = idx, end = array4.length, item = array4[idx];
  let cidx = (idx << 1) + 1, ridx;
  while (cidx < end) {
    ridx = cidx + 1;
    if (ridx < end && cmp(array4[cidx], array4[ridx]) >= 0) {
      cidx = ridx;
    }
    array4[idx] = array4[cidx];
    idx = cidx;
    cidx = (idx << 1) + 1;
  }
  array4[idx] = item;
  return siftdown(array4, start, idx, cmp);
}
function Dataflow() {
  this.logger(logger_default());
  this.logLevel(Error2);
  this._clock = 0;
  this._rank = 0;
  this._locale = defaultLocale3();
  try {
    this._loader = loader();
  } catch (e) {
  }
  this._touched = UniqueList(id);
  this._input = {};
  this._pulse = null;
  this._heap = Heap((a3, b10) => a3.qrank - b10.qrank);
  this._postrun = [];
}
function logMethod(method2) {
  return function() {
    return this._log[method2].apply(this, arguments);
  };
}
Dataflow.prototype = {
  /**
   * The current timestamp of this dataflow. This value reflects the
   * timestamp of the previous dataflow run. The dataflow is initialized
   * with a stamp value of 0. The initial run of the dataflow will have
   * a timestap of 1, and so on. This value will match the
   * {@link Pulse.stamp} property.
   * @return {number} - The current timestamp value.
   */
  stamp() {
    return this._clock;
  },
  /**
   * Gets or sets the loader instance to use for data file loading. A
   * loader object must provide a "load" method for loading files and a
   * "sanitize" method for checking URL/filename validity. Both methods
   * should accept a URI and options hash as arguments, and return a Promise
   * that resolves to the loaded file contents (load) or a hash containing
   * sanitized URI data with the sanitized url assigned to the "href" property
   * (sanitize).
   * @param {object} _ - The loader instance to use.
   * @return {object|Dataflow} - If no arguments are provided, returns
   *   the current loader instance. Otherwise returns this Dataflow instance.
   */
  loader(_) {
    if (arguments.length) {
      this._loader = _;
      return this;
    } else {
      return this._loader;
    }
  },
  /**
   * Gets or sets the locale instance to use for formatting and parsing
   * string values. The locale object should be provided by the
   * vega-format library, and include methods such as format, timeFormat,
   * utcFormat, timeParse, and utcParse.
   * @param {object} _ - The locale instance to use.
   * @return {object|Dataflow} - If no arguments are provided, returns
   *   the current locale instance. Otherwise returns this Dataflow instance.
   */
  locale(_) {
    if (arguments.length) {
      this._locale = _;
      return this;
    } else {
      return this._locale;
    }
  },
  /**
   * Get or set the logger instance used to log messages. If no arguments are
   * provided, returns the current logger instance. Otherwise, sets the logger
   * and return this Dataflow instance. Provided loggers must support the full
   * API of logger objects generated by the vega-util logger method. Note that
   * by default the log level of the new logger will be used; use the logLevel
   * method to adjust the log level as needed.
   */
  logger(logger) {
    if (arguments.length) {
      this._log = logger;
      return this;
    } else {
      return this._log;
    }
  },
  /**
   * Logs an error message. By default, logged messages are written to console
   * output. The message will only be logged if the current log level is high
   * enough to permit error messages.
   */
  error: logMethod("error"),
  /**
   * Logs a warning message. By default, logged messages are written to console
   * output. The message will only be logged if the current log level is high
   * enough to permit warning messages.
   */
  warn: logMethod("warn"),
  /**
   * Logs an information message. By default, logged messages are written to
   * console output. The message will only be logged if the current log level is
   * high enough to permit information messages.
   */
  info: logMethod("info"),
  /**
   * Logs a debug message. By default, logged messages are written to console
   * output. The message will only be logged if the current log level is high
   * enough to permit debug messages.
   */
  debug: logMethod("debug"),
  /**
   * Get or set the current log level. If an argument is provided, it
   * will be used as the new log level.
   * @param {number} [level] - Should be one of None, Warn, Info
   * @return {number} - The current log level.
   */
  logLevel: logMethod("level"),
  /**
   * Empty entry threshold for garbage cleaning. Map data structures will
   * perform cleaning once the number of empty entries exceeds this value.
   */
  cleanThreshold: 1e4,
  // OPERATOR REGISTRATION
  add,
  connect,
  rank,
  rerank,
  // OPERATOR UPDATES
  pulse,
  touch,
  update,
  changeset,
  // DATA LOADING
  ingest,
  parse: parse2,
  preload,
  request,
  // EVENT HANDLING
  events,
  on,
  // PULSE PROPAGATION
  evaluate,
  run,
  runAsync,
  runAfter,
  _enqueue: enqueue,
  _getPulse: getPulse
};
function Transform(init2, params2) {
  Operator.call(this, init2, null, params2);
}
inherits_default(Transform, Operator, {
  /**
   * Overrides {@link Operator.evaluate} for transform operators.
   * Internally, this method calls {@link evaluate} to perform processing.
   * If {@link evaluate} returns a falsy value, the input pulse is returned.
   * This method should NOT be overridden, instead overrride {@link evaluate}.
   * @param {Pulse} pulse - the current dataflow pulse.
   * @return the output pulse for this operator (or StopPropagation)
   */
  run(pulse2) {
    if (pulse2.stamp < this.stamp) return pulse2.StopPropagation;
    let rv;
    if (this.skip()) {
      this.skip(false);
    } else {
      rv = this.evaluate(pulse2);
    }
    rv = rv || pulse2;
    if (rv.then) {
      rv = rv.then((_) => this.pulse = _);
    } else if (rv !== pulse2.StopPropagation) {
      this.pulse = rv;
    }
    return rv;
  },
  /**
   * Overrides {@link Operator.evaluate} for transform operators.
   * Marshalls parameter values and then invokes {@link transform}.
   * @param {Pulse} pulse - the current dataflow pulse.
   * @return {Pulse} The output pulse (or StopPropagation). A falsy return
       value (including undefined) will let the input pulse pass through.
  */
  evaluate(pulse2) {
    const params2 = this.marshall(pulse2.stamp), out = this.transform(params2, pulse2);
    params2.clear();
    return out;
  },
  /**
   * Process incoming pulses.
   * Subclasses should override this method to implement transforms.
   * @param {Parameters} _ - The operator parameter values.
   * @param {Pulse} pulse - The current dataflow pulse.
   * @return {Pulse} The output pulse (or StopPropagation). A falsy return
   *   value (including undefined) will let the input pulse pass through.
   */
  transform() {
  }
});
var transforms = {};
function definition(type4) {
  const t = transform(type4);
  return t && t.Definition || null;
}
function transform(type4) {
  type4 = type4 && type4.toLowerCase();
  return hasOwnProperty(transforms, type4) ? transforms[type4] : null;
}

// ../deepseek-harness/node_modules/.pnpm/vega-transforms@5.2.2/node_modules/vega-transforms/build/vega-transforms.js
var vega_transforms_exports = {};
__export(vega_transforms_exports, {
  aggregate: () => Aggregate,
  bin: () => Bin,
  collect: () => Collect,
  compare: () => Compare,
  countpattern: () => CountPattern,
  cross: () => Cross,
  density: () => Density,
  dotbin: () => DotBin,
  expression: () => Expression,
  extent: () => Extent,
  facet: () => Facet,
  field: () => Field,
  filter: () => Filter,
  flatten: () => Flatten,
  fold: () => Fold,
  formula: () => Formula,
  generate: () => Generate,
  impute: () => Impute,
  joinaggregate: () => JoinAggregate,
  kde: () => KDE,
  key: () => Key,
  load: () => Load,
  lookup: () => Lookup,
  multiextent: () => MultiExtent,
  multivalues: () => MultiValues,
  params: () => Params,
  pivot: () => Pivot,
  prefacet: () => PreFacet,
  project: () => Project,
  proxy: () => Proxy2,
  quantile: () => Quantile,
  relay: () => Relay,
  sample: () => Sample,
  sequence: () => Sequence,
  sieve: () => Sieve,
  subflow: () => Subflow,
  timeunit: () => TimeUnit,
  tupleindex: () => TupleIndex,
  values: () => Values,
  window: () => Window
});

// ../deepseek-harness/node_modules/.pnpm/vega-statistics@2.0.0/node_modules/vega-statistics/build/vega-statistics.js
function* numbers2(values4, valueof) {
  if (valueof == null) {
    for (let value4 of values4) {
      if (value4 != null && value4 !== "" && (value4 = +value4) >= value4) {
        yield value4;
      }
    }
  } else {
    let index4 = -1;
    for (let value4 of values4) {
      value4 = valueof(value4, ++index4, values4);
      if (value4 != null && value4 !== "" && (value4 = +value4) >= value4) {
        yield value4;
      }
    }
  }
}
function quantiles(array4, p, f) {
  const values4 = Float64Array.from(numbers2(array4, f));
  values4.sort(ascending2);
  return p.map((_) => quantileSorted(values4, _));
}
function quartiles(array4, f) {
  return quantiles(array4, [0.25, 0.5, 0.75], f);
}
function estimateBandwidth(array4, f) {
  const n = array4.length, d = deviation(array4, f), q = quartiles(array4, f), h3 = (q[2] - q[0]) / 1.34, v2 = Math.min(d, h3) || d || Math.abs(q[0]) || 1;
  return 1.06 * v2 * Math.pow(n, -0.2);
}
function bin2(_) {
  const maxb = _.maxbins || 20, base = _.base || 10, logb = Math.log(base), div = _.divide || [5, 2];
  let min4 = _.extent[0], max4 = _.extent[1], step, level, minstep, v2, i, n;
  const span = _.span || max4 - min4 || Math.abs(min4) || 1;
  if (_.step) {
    step = _.step;
  } else if (_.steps) {
    v2 = span / maxb;
    for (i = 0, n = _.steps.length; i < n && _.steps[i] < v2; ++i) ;
    step = _.steps[Math.max(0, i - 1)];
  } else {
    level = Math.ceil(Math.log(maxb) / logb);
    minstep = _.minstep || 0;
    step = Math.max(minstep, Math.pow(base, Math.round(Math.log(span) / logb) - level));
    while (Math.ceil(span / step) > maxb) {
      step *= base;
    }
    for (i = 0, n = div.length; i < n; ++i) {
      v2 = step / div[i];
      if (v2 >= minstep && span / v2 <= maxb) step = v2;
    }
  }
  v2 = Math.log(step);
  const precision = v2 >= 0 ? 0 : ~~(-v2 / logb) + 1, eps = Math.pow(base, -precision - 1);
  if (_.nice || _.nice === void 0) {
    v2 = Math.floor(min4 / step + eps) * step;
    min4 = min4 < v2 ? v2 - step : v2;
    max4 = Math.ceil(max4 / step) * step;
  }
  return {
    start: min4,
    stop: max4 === min4 ? min4 + step : max4,
    step
  };
}
var random = Math.random;
function bootstrapCI(array4, samples, alpha, f) {
  if (!array4.length) return [void 0, void 0];
  const values4 = Float64Array.from(numbers2(array4, f)), n = values4.length, m3 = samples;
  let a3, i, j, mu;
  for (j = 0, mu = Array(m3); j < m3; ++j) {
    for (a3 = 0, i = 0; i < n; ++i) {
      a3 += values4[~~(random() * n)];
    }
    mu[j] = a3 / n;
  }
  mu.sort(ascending2);
  return [quantile(mu, alpha / 2), quantile(mu, 1 - alpha / 2)];
}
function dotbin(array4, step, smooth, f) {
  f = f || ((_) => _);
  const n = array4.length, v2 = new Float64Array(n);
  let i = 0, j = 1, a3 = f(array4[0]), b10 = a3, w3 = a3 + step, x6;
  for (; j < n; ++j) {
    x6 = f(array4[j]);
    if (x6 >= w3) {
      b10 = (a3 + b10) / 2;
      for (; i < j; ++i) v2[i] = b10;
      w3 = x6 + step;
      a3 = x6;
    }
    b10 = x6;
  }
  b10 = (a3 + b10) / 2;
  for (; i < j; ++i) v2[i] = b10;
  return smooth ? smoothing(v2, step + step / 4) : v2;
}
function smoothing(v2, thresh) {
  const n = v2.length;
  let a3 = 0, b10 = 1, c3, d;
  while (v2[a3] === v2[b10]) ++b10;
  while (b10 < n) {
    c3 = b10 + 1;
    while (v2[b10] === v2[c3]) ++c3;
    if (v2[b10] - v2[b10 - 1] < thresh) {
      d = b10 + (a3 + c3 - b10 - b10 >> 1);
      while (d < b10) v2[d++] = v2[b10];
      while (d > b10) v2[d--] = v2[a3];
    }
    a3 = b10;
    b10 = c3;
  }
  return v2;
}
var SQRT2PI = Math.sqrt(2 * Math.PI);
var SQRT2 = Math.SQRT2;
var nextSample = NaN;
function sampleNormal(mean2, stdev) {
  mean2 = mean2 || 0;
  stdev = stdev == null ? 1 : stdev;
  let x6 = 0, y6 = 0, rds, c3;
  if (nextSample === nextSample) {
    x6 = nextSample;
    nextSample = NaN;
  } else {
    do {
      x6 = random() * 2 - 1;
      y6 = random() * 2 - 1;
      rds = x6 * x6 + y6 * y6;
    } while (rds === 0 || rds > 1);
    c3 = Math.sqrt(-2 * Math.log(rds) / rds);
    x6 *= c3;
    nextSample = y6 * c3;
  }
  return mean2 + x6 * stdev;
}
function densityNormal(value4, mean2, stdev) {
  stdev = stdev == null ? 1 : stdev;
  const z = (value4 - (mean2 || 0)) / stdev;
  return Math.exp(-0.5 * z * z) / (stdev * SQRT2PI);
}
function cumulativeNormal(value4, mean2, stdev) {
  mean2 = mean2 || 0;
  stdev = stdev == null ? 1 : stdev;
  const z = (value4 - mean2) / stdev, Z = Math.abs(z);
  let cd2;
  if (Z > 37) {
    cd2 = 0;
  } else {
    const exp4 = Math.exp(-Z * Z / 2);
    let sum3;
    if (Z < 7.07106781186547) {
      sum3 = 0.0352624965998911 * Z + 0.700383064443688;
      sum3 = sum3 * Z + 6.37396220353165;
      sum3 = sum3 * Z + 33.912866078383;
      sum3 = sum3 * Z + 112.079291497871;
      sum3 = sum3 * Z + 221.213596169931;
      sum3 = sum3 * Z + 220.206867912376;
      cd2 = exp4 * sum3;
      sum3 = 0.0883883476483184 * Z + 1.75566716318264;
      sum3 = sum3 * Z + 16.064177579207;
      sum3 = sum3 * Z + 86.7807322029461;
      sum3 = sum3 * Z + 296.564248779674;
      sum3 = sum3 * Z + 637.333633378831;
      sum3 = sum3 * Z + 793.826512519948;
      sum3 = sum3 * Z + 440.413735824752;
      cd2 = cd2 / sum3;
    } else {
      sum3 = Z + 0.65;
      sum3 = Z + 4 / sum3;
      sum3 = Z + 3 / sum3;
      sum3 = Z + 2 / sum3;
      sum3 = Z + 1 / sum3;
      cd2 = exp4 / sum3 / 2.506628274631;
    }
  }
  return z > 0 ? 1 - cd2 : cd2;
}
function quantileNormal(p, mean2, stdev) {
  if (p < 0 || p > 1) return NaN;
  return (mean2 || 0) + (stdev == null ? 1 : stdev) * SQRT2 * erfinv(2 * p - 1);
}
function erfinv(x6) {
  let w3 = -Math.log((1 - x6) * (1 + x6)), p;
  if (w3 < 6.25) {
    w3 -= 3.125;
    p = -364441206401782e-35;
    p = -16850591381820166e-35 + p * w3;
    p = 128584807152564e-32 + p * w3;
    p = 11157877678025181e-33 + p * w3;
    p = -1333171662854621e-31 + p * w3;
    p = 20972767875968562e-33 + p * w3;
    p = 6637638134358324e-30 + p * w3;
    p = -4054566272975207e-29 + p * w3;
    p = -8151934197605472e-29 + p * w3;
    p = 26335093153082323e-28 + p * w3;
    p = -12975133253453532e-27 + p * w3;
    p = -5415412054294628e-26 + p * w3;
    p = 10512122733215323e-25 + p * w3;
    p = -4112633980346984e-24 + p * w3;
    p = -29070369957882005e-24 + p * w3;
    p = 42347877827932404e-23 + p * w3;
    p = -13654692000834679e-22 + p * w3;
    p = -13882523362786469e-21 + p * w3;
    p = 18673420803405714e-20 + p * w3;
    p = -740702534166267e-18 + p * w3;
    p = -0.006033670871430149 + p * w3;
    p = 0.24015818242558962 + p * w3;
    p = 1.6536545626831027 + p * w3;
  } else if (w3 < 16) {
    w3 = Math.sqrt(w3) - 3.25;
    p = 22137376921775787e-25;
    p = 9075656193888539e-23 + p * w3;
    p = -27517406297064545e-23 + p * w3;
    p = 18239629214389228e-24 + p * w3;
    p = 15027403968909828e-22 + p * w3;
    p = -4013867526981546e-21 + p * w3;
    p = 29234449089955446e-22 + p * w3;
    p = 12475304481671779e-21 + p * w3;
    p = -47318229009055734e-21 + p * w3;
    p = 6828485145957318e-20 + p * w3;
    p = 24031110387097894e-21 + p * w3;
    p = -3550375203628475e-19 + p * w3;
    p = 9532893797373805e-19 + p * w3;
    p = -0.0016882755560235047 + p * w3;
    p = 0.002491442096107851 + p * w3;
    p = -0.003751208507569241 + p * w3;
    p = 0.005370914553590064 + p * w3;
    p = 1.0052589676941592 + p * w3;
    p = 3.0838856104922208 + p * w3;
  } else if (Number.isFinite(w3)) {
    w3 = Math.sqrt(w3) - 5;
    p = -27109920616438573e-27;
    p = -2555641816996525e-25 + p * w3;
    p = 15076572693500548e-25 + p * w3;
    p = -3789465440126737e-24 + p * w3;
    p = 761570120807834e-23 + p * w3;
    p = -1496002662714924e-23 + p * w3;
    p = 2914795345090108e-23 + p * w3;
    p = -6771199775845234e-23 + p * w3;
    p = 22900482228026655e-23 + p * w3;
    p = -99298272942317e-20 + p * w3;
    p = 4526062597223154e-21 + p * w3;
    p = -1968177810553167e-20 + p * w3;
    p = 7599527703001776e-20 + p * w3;
    p = -21503011930044477e-20 + p * w3;
    p = -13871931833623122e-20 + p * w3;
    p = 1.0103004648645344 + p * w3;
    p = 4.849906401408584 + p * w3;
  } else {
    p = Infinity;
  }
  return p * x6;
}
function gaussian(mean2, stdev) {
  let mu, sigma;
  const dist2 = {
    mean(_) {
      if (arguments.length) {
        mu = _ || 0;
        return dist2;
      } else {
        return mu;
      }
    },
    stdev(_) {
      if (arguments.length) {
        sigma = _ == null ? 1 : _;
        return dist2;
      } else {
        return sigma;
      }
    },
    sample: () => sampleNormal(mu, sigma),
    pdf: (value4) => densityNormal(value4, mu, sigma),
    cdf: (value4) => cumulativeNormal(value4, mu, sigma),
    icdf: (p) => quantileNormal(p, mu, sigma)
  };
  return dist2.mean(mean2).stdev(stdev);
}
function kde(support, bandwidth3) {
  const kernel = gaussian();
  let n = 0;
  const dist2 = {
    data(_) {
      if (arguments.length) {
        support = _;
        n = _ ? _.length : 0;
        return dist2.bandwidth(bandwidth3);
      } else {
        return support;
      }
    },
    bandwidth(_) {
      if (!arguments.length) return bandwidth3;
      bandwidth3 = _;
      if (!bandwidth3 && support) bandwidth3 = estimateBandwidth(support);
      return dist2;
    },
    sample() {
      return support[~~(random() * n)] + bandwidth3 * kernel.sample();
    },
    pdf(x6) {
      let y6 = 0, i = 0;
      for (; i < n; ++i) {
        y6 += kernel.pdf((x6 - support[i]) / bandwidth3);
      }
      return y6 / bandwidth3 / n;
    },
    cdf(x6) {
      let y6 = 0, i = 0;
      for (; i < n; ++i) {
        y6 += kernel.cdf((x6 - support[i]) / bandwidth3);
      }
      return y6 / n;
    },
    icdf() {
      throw Error("KDE icdf not supported.");
    }
  };
  return dist2.data(support);
}
function sampleLogNormal(mean2, stdev) {
  mean2 = mean2 || 0;
  stdev = stdev == null ? 1 : stdev;
  return Math.exp(mean2 + sampleNormal() * stdev);
}
function densityLogNormal(value4, mean2, stdev) {
  if (value4 <= 0) return 0;
  mean2 = mean2 || 0;
  stdev = stdev == null ? 1 : stdev;
  const z = (Math.log(value4) - mean2) / stdev;
  return Math.exp(-0.5 * z * z) / (stdev * SQRT2PI * value4);
}
function cumulativeLogNormal(value4, mean2, stdev) {
  return cumulativeNormal(Math.log(value4), mean2, stdev);
}
function quantileLogNormal(p, mean2, stdev) {
  return Math.exp(quantileNormal(p, mean2, stdev));
}
function lognormal(mean2, stdev) {
  let mu, sigma;
  const dist2 = {
    mean(_) {
      if (arguments.length) {
        mu = _ || 0;
        return dist2;
      } else {
        return mu;
      }
    },
    stdev(_) {
      if (arguments.length) {
        sigma = _ == null ? 1 : _;
        return dist2;
      } else {
        return sigma;
      }
    },
    sample: () => sampleLogNormal(mu, sigma),
    pdf: (value4) => densityLogNormal(value4, mu, sigma),
    cdf: (value4) => cumulativeLogNormal(value4, mu, sigma),
    icdf: (p) => quantileLogNormal(p, mu, sigma)
  };
  return dist2.mean(mean2).stdev(stdev);
}
function mixture(dists, weights) {
  let m3 = 0, w3;
  function normalize4(x6) {
    const w4 = [];
    let sum3 = 0, i;
    for (i = 0; i < m3; ++i) {
      sum3 += w4[i] = x6[i] == null ? 1 : +x6[i];
    }
    for (i = 0; i < m3; ++i) {
      w4[i] /= sum3;
    }
    return w4;
  }
  const dist2 = {
    weights(_) {
      if (arguments.length) {
        w3 = normalize4(weights = _ || []);
        return dist2;
      }
      return weights;
    },
    distributions(_) {
      if (arguments.length) {
        if (_) {
          m3 = _.length;
          dists = _;
        } else {
          m3 = 0;
          dists = [];
        }
        return dist2.weights(weights);
      }
      return dists;
    },
    sample() {
      const r = random();
      let d = dists[m3 - 1], v2 = w3[0], i = 0;
      for (; i < m3 - 1; v2 += w3[++i]) {
        if (r < v2) {
          d = dists[i];
          break;
        }
      }
      return d.sample();
    },
    pdf(x6) {
      let p = 0, i = 0;
      for (; i < m3; ++i) {
        p += w3[i] * dists[i].pdf(x6);
      }
      return p;
    },
    cdf(x6) {
      let p = 0, i = 0;
      for (; i < m3; ++i) {
        p += w3[i] * dists[i].cdf(x6);
      }
      return p;
    },
    icdf() {
      throw Error("Mixture icdf not supported.");
    }
  };
  return dist2.distributions(dists).weights(weights);
}
function sampleUniform(min4, max4) {
  if (max4 == null) {
    max4 = min4 == null ? 1 : min4;
    min4 = 0;
  }
  return min4 + (max4 - min4) * random();
}
function densityUniform(value4, min4, max4) {
  if (max4 == null) {
    max4 = min4 == null ? 1 : min4;
    min4 = 0;
  }
  return value4 >= min4 && value4 <= max4 ? 1 / (max4 - min4) : 0;
}
function cumulativeUniform(value4, min4, max4) {
  if (max4 == null) {
    max4 = min4 == null ? 1 : min4;
    min4 = 0;
  }
  return value4 < min4 ? 0 : value4 > max4 ? 1 : (value4 - min4) / (max4 - min4);
}
function quantileUniform(p, min4, max4) {
  if (max4 == null) {
    max4 = min4 == null ? 1 : min4;
    min4 = 0;
  }
  return p >= 0 && p <= 1 ? min4 + p * (max4 - min4) : NaN;
}
function uniform(min4, max4) {
  let a3, b10;
  const dist2 = {
    min(_) {
      if (arguments.length) {
        a3 = _ || 0;
        return dist2;
      } else {
        return a3;
      }
    },
    max(_) {
      if (arguments.length) {
        b10 = _ == null ? 1 : _;
        return dist2;
      } else {
        return b10;
      }
    },
    sample: () => sampleUniform(a3, b10),
    pdf: (value4) => densityUniform(value4, a3, b10),
    cdf: (value4) => cumulativeUniform(value4, a3, b10),
    icdf: (p) => quantileUniform(p, a3, b10)
  };
  if (max4 == null) {
    max4 = min4 == null ? 1 : min4;
    min4 = 0;
  }
  return dist2.min(min4).max(max4);
}
function constant(data4, x6, y6) {
  let mean2 = 0, n = 0;
  for (const d of data4) {
    const val = y6(d);
    if (x6(d) == null || val == null || isNaN(val)) continue;
    mean2 += (val - mean2) / ++n;
  }
  return {
    coef: [mean2],
    predict: () => mean2,
    rSquared: 0
  };
}
function ols(uX, uY, uXY, uX2) {
  const delta = uX2 - uX * uX, slope = Math.abs(delta) < 1e-24 ? 0 : (uXY - uX * uY) / delta, intercept = uY - slope * uX;
  return [intercept, slope];
}
function points(data4, x6, y6, sort4) {
  data4 = data4.filter((d2) => {
    let u4 = x6(d2), v2 = y6(d2);
    return u4 != null && (u4 = +u4) >= u4 && v2 != null && (v2 = +v2) >= v2;
  });
  if (sort4) {
    data4.sort((a3, b10) => x6(a3) - x6(b10));
  }
  const n = data4.length, X4 = new Float64Array(n), Y4 = new Float64Array(n);
  let i = 0, ux = 0, uy = 0, xv, yv, d;
  for (d of data4) {
    X4[i] = xv = +x6(d);
    Y4[i] = yv = +y6(d);
    ++i;
    ux += (xv - ux) / i;
    uy += (yv - uy) / i;
  }
  for (i = 0; i < n; ++i) {
    X4[i] -= ux;
    Y4[i] -= uy;
  }
  return [X4, Y4, ux, uy];
}
function visitPoints(data4, x6, y6, callback) {
  let i = -1, u4, v2;
  for (const d of data4) {
    u4 = x6(d);
    v2 = y6(d);
    if (u4 != null && (u4 = +u4) >= u4 && v2 != null && (v2 = +v2) >= v2) {
      callback(u4, v2, ++i);
    }
  }
}
function rSquared(data4, x6, y6, uY, predict) {
  let SSE = 0, SST = 0;
  visitPoints(data4, x6, y6, (dx, dy) => {
    const sse = dy - predict(dx), sst = dy - uY;
    SSE += sse * sse;
    SST += sst * sst;
  });
  return 1 - SSE / SST;
}
function linear(data4, x6, y6) {
  let X4 = 0, Y4 = 0, XY = 0, X24 = 0, n = 0;
  visitPoints(data4, x6, y6, (dx, dy) => {
    ++n;
    X4 += (dx - X4) / n;
    Y4 += (dy - Y4) / n;
    XY += (dx * dy - XY) / n;
    X24 += (dx * dx - X24) / n;
  });
  const coef = ols(X4, Y4, XY, X24), predict = (x7) => coef[0] + coef[1] * x7;
  return {
    coef,
    predict,
    rSquared: rSquared(data4, x6, y6, Y4, predict)
  };
}
function log3(data4, x6, y6) {
  let X4 = 0, Y4 = 0, XY = 0, X24 = 0, n = 0;
  visitPoints(data4, x6, y6, (dx, dy) => {
    ++n;
    dx = Math.log(dx);
    X4 += (dx - X4) / n;
    Y4 += (dy - Y4) / n;
    XY += (dx * dy - XY) / n;
    X24 += (dx * dx - X24) / n;
  });
  const coef = ols(X4, Y4, XY, X24), predict = (x7) => coef[0] + coef[1] * Math.log(x7);
  return {
    coef,
    predict,
    rSquared: rSquared(data4, x6, y6, Y4, predict)
  };
}
function exp2(data4, x6, y6) {
  const [xv, yv, ux, uy] = points(data4, x6, y6);
  let YL = 0, XY = 0, XYL = 0, X2Y = 0, n = 0, dx, ly3, xy;
  visitPoints(data4, x6, y6, (_, dy) => {
    dx = xv[n++];
    ly3 = Math.log(dy);
    xy = dx * dy;
    YL += (dy * ly3 - YL) / n;
    XY += (xy - XY) / n;
    XYL += (xy * ly3 - XYL) / n;
    X2Y += (dx * xy - X2Y) / n;
  });
  const [c0, c1] = ols(XY / uy, YL / uy, XYL / uy, X2Y / uy), predict = (x7) => Math.exp(c0 + c1 * (x7 - ux));
  return {
    coef: [Math.exp(c0 - c1 * ux), c1],
    predict,
    rSquared: rSquared(data4, x6, y6, uy, predict)
  };
}
function pow2(data4, x6, y6) {
  let X4 = 0, Y4 = 0, XY = 0, X24 = 0, YS = 0, n = 0;
  visitPoints(data4, x6, y6, (dx, dy) => {
    const lx3 = Math.log(dx), ly3 = Math.log(dy);
    ++n;
    X4 += (lx3 - X4) / n;
    Y4 += (ly3 - Y4) / n;
    XY += (lx3 * ly3 - XY) / n;
    X24 += (lx3 * lx3 - X24) / n;
    YS += (dy - YS) / n;
  });
  const coef = ols(X4, Y4, XY, X24), predict = (x7) => coef[0] * Math.pow(x7, coef[1]);
  coef[0] = Math.exp(coef[0]);
  return {
    coef,
    predict,
    rSquared: rSquared(data4, x6, y6, YS, predict)
  };
}
function quad(data4, x6, y6) {
  const [xv, yv, ux, uy] = points(data4, x6, y6), n = xv.length;
  let X24 = 0, X32 = 0, X4 = 0, XY = 0, X2Y = 0, i, dx, dy, x22;
  for (i = 0; i < n; ) {
    dx = xv[i];
    dy = yv[i++];
    x22 = dx * dx;
    X24 += (x22 - X24) / i;
    X32 += (x22 * dx - X32) / i;
    X4 += (x22 * x22 - X4) / i;
    XY += (dx * dy - XY) / i;
    X2Y += (x22 * dy - X2Y) / i;
  }
  const X2X2 = X4 - X24 * X24, d = X24 * X2X2 - X32 * X32, a3 = (X2Y * X24 - XY * X32) / d, b10 = (XY * X2X2 - X2Y * X32) / d, c3 = -a3 * X24, predict = (x7) => {
    x7 = x7 - ux;
    return a3 * x7 * x7 + b10 * x7 + c3 + uy;
  };
  return {
    coef: [c3 - b10 * ux + a3 * ux * ux + uy, b10 - 2 * a3 * ux, a3],
    predict,
    rSquared: rSquared(data4, x6, y6, uy, predict)
  };
}
function poly(data4, x6, y6, order) {
  if (order === 0) return constant(data4, x6, y6);
  if (order === 1) return linear(data4, x6, y6);
  if (order === 2) return quad(data4, x6, y6);
  const [xv, yv, ux, uy] = points(data4, x6, y6), n = xv.length, lhs = [], rhs = [], k = order + 1;
  let i, j, l, v2, c3;
  for (i = 0; i < k; ++i) {
    for (l = 0, v2 = 0; l < n; ++l) {
      v2 += Math.pow(xv[l], i) * yv[l];
    }
    lhs.push(v2);
    c3 = new Float64Array(k);
    for (j = 0; j < k; ++j) {
      for (l = 0, v2 = 0; l < n; ++l) {
        v2 += Math.pow(xv[l], i + j);
      }
      c3[j] = v2;
    }
    rhs.push(c3);
  }
  rhs.push(lhs);
  const coef = gaussianElimination(rhs), predict = (x7) => {
    x7 -= ux;
    let y7 = uy + coef[0] + coef[1] * x7 + coef[2] * x7 * x7;
    for (i = 3; i < k; ++i) y7 += coef[i] * Math.pow(x7, i);
    return y7;
  };
  return {
    coef: uncenter(k, coef, -ux, uy),
    predict,
    rSquared: rSquared(data4, x6, y6, uy, predict)
  };
}
function uncenter(k, a3, x6, y6) {
  const z = Array(k);
  let i, j, v2, c3;
  for (i = 0; i < k; ++i) z[i] = 0;
  for (i = k - 1; i >= 0; --i) {
    v2 = a3[i];
    c3 = 1;
    z[i] += v2;
    for (j = 1; j <= i; ++j) {
      c3 *= (i + 1 - j) / j;
      z[i - j] += v2 * Math.pow(x6, j) * c3;
    }
  }
  z[0] += y6;
  return z;
}
function gaussianElimination(matrix) {
  const n = matrix.length - 1, coef = [];
  let i, j, k, r, t;
  for (i = 0; i < n; ++i) {
    r = i;
    for (j = i + 1; j < n; ++j) {
      if (Math.abs(matrix[i][j]) > Math.abs(matrix[i][r])) {
        r = j;
      }
    }
    for (k = i; k < n + 1; ++k) {
      t = matrix[k][i];
      matrix[k][i] = matrix[k][r];
      matrix[k][r] = t;
    }
    for (j = i + 1; j < n; ++j) {
      for (k = n; k >= i; k--) {
        matrix[k][j] -= matrix[k][i] * matrix[i][j] / matrix[i][i];
      }
    }
  }
  for (j = n - 1; j >= 0; --j) {
    t = 0;
    for (k = j + 1; k < n; ++k) {
      t += matrix[k][j] * coef[k];
    }
    coef[j] = (matrix[n][j] - t) / matrix[j][j];
  }
  return coef;
}
var maxiters = 2;
var epsilon = 1e-12;
function loess(data4, x6, y6, bandwidth3) {
  const [xv, yv, ux, uy] = points(data4, x6, y6, true), n = xv.length, bw = Math.max(2, ~~(bandwidth3 * n)), yhat = new Float64Array(n), residuals = new Float64Array(n), robustWeights = new Float64Array(n).fill(1);
  for (let iter = -1; ++iter <= maxiters; ) {
    const interval3 = [0, bw - 1];
    for (let i = 0; i < n; ++i) {
      const dx = xv[i], i0 = interval3[0], i1 = interval3[1], edge = dx - xv[i0] > xv[i1] - dx ? i0 : i1;
      let W = 0, X4 = 0, Y4 = 0, XY = 0, X24 = 0;
      const denom = 1 / Math.abs(xv[edge] - dx || 1);
      for (let k = i0; k <= i1; ++k) {
        const xk = xv[k], yk = yv[k], w3 = tricube(Math.abs(dx - xk) * denom) * robustWeights[k], xkw = xk * w3;
        W += w3;
        X4 += xkw;
        Y4 += yk * w3;
        XY += yk * xkw;
        X24 += xk * xkw;
      }
      const [a3, b10] = ols(X4 / W, Y4 / W, XY / W, X24 / W);
      yhat[i] = a3 + b10 * dx;
      residuals[i] = Math.abs(yv[i] - yhat[i]);
      updateInterval(xv, i + 1, interval3);
    }
    if (iter === maxiters) {
      break;
    }
    const medianResidual = median(residuals);
    if (Math.abs(medianResidual) < epsilon) break;
    for (let i = 0, arg, w3; i < n; ++i) {
      arg = residuals[i] / (6 * medianResidual);
      robustWeights[i] = arg >= 1 ? epsilon : (w3 = 1 - arg * arg) * w3;
    }
  }
  return output(xv, yhat, ux, uy);
}
function tricube(x6) {
  return (x6 = 1 - x6 * x6 * x6) * x6 * x6;
}
function updateInterval(xv, i, interval3) {
  const val = xv[i];
  let left = interval3[0], right = interval3[1] + 1;
  if (right >= xv.length) return;
  while (i > left && xv[right] - val <= val - xv[left]) {
    interval3[0] = ++left;
    interval3[1] = right;
    ++right;
  }
}
function output(xv, yhat, ux, uy) {
  const n = xv.length, out = [];
  let i = 0, cnt = 0, prev = [], v2;
  for (; i < n; ++i) {
    v2 = xv[i] + ux;
    if (prev[0] === v2) {
      prev[1] += (yhat[i] - prev[1]) / ++cnt;
    } else {
      cnt = 0;
      prev[1] += uy;
      prev = [v2, yhat[i]];
      out.push(prev);
    }
  }
  prev[1] += uy;
  return out;
}
var MIN_RADIANS = 0.5 * Math.PI / 180;
function sampleCurve(f, extent2, minSteps, maxSteps) {
  minSteps = minSteps || 25;
  maxSteps = Math.max(minSteps, maxSteps || 200);
  const point9 = (x6) => [x6, f(x6)], minX = extent2[0], maxX = extent2[1], span = maxX - minX, stop2 = span / maxSteps, prev = [point9(minX)], next = [];
  if (minSteps === maxSteps) {
    for (let i = 1; i < maxSteps; ++i) {
      prev.push(point9(minX + i / minSteps * span));
    }
    prev.push(point9(maxX));
    return prev;
  } else {
    next.push(point9(maxX));
    for (let i = minSteps; --i > 0; ) {
      next.push(point9(minX + i / minSteps * span));
    }
  }
  let p02 = prev[0];
  let p1 = next[next.length - 1];
  const sx = 1 / span;
  const sy = scaleY(p02[1], next);
  while (p1) {
    const pm = point9((p02[0] + p1[0]) / 2);
    const dx = pm[0] - p02[0] >= stop2;
    if (dx && angleDelta(p02, pm, p1, sx, sy) > MIN_RADIANS) {
      next.push(pm);
    } else {
      p02 = p1;
      prev.push(p1);
      next.pop();
    }
    p1 = next[next.length - 1];
  }
  return prev;
}
function scaleY(init2, points2) {
  let ymin = init2;
  let ymax = init2;
  const n = points2.length;
  for (let i = 0; i < n; ++i) {
    const y6 = points2[i][1];
    if (y6 < ymin) ymin = y6;
    if (y6 > ymax) ymax = y6;
  }
  return 1 / (ymax - ymin);
}
function angleDelta(p, q, r, sx, sy) {
  const a0 = Math.atan2(sy * (r[1] - p[1]), sx * (r[0] - p[0])), a1 = Math.atan2(sy * (q[1] - p[1]), sx * (q[0] - p[0]));
  return Math.abs(a0 - a1);
}

// ../deepseek-harness/node_modules/.pnpm/vega-transforms@5.2.2/node_modules/vega-transforms/build/vega-transforms.js
function multikey(f) {
  return (x6) => {
    const n = f.length;
    let i = 1, k = String(f[0](x6));
    for (; i < n; ++i) {
      k += "|" + f[i](x6);
    }
    return k;
  };
}
function groupkey(fields) {
  return !fields || !fields.length ? function() {
    return "";
  } : fields.length === 1 ? fields[0] : multikey(fields);
}
function measureName(op, field3, as) {
  return as || op + (!field3 ? "" : "_" + field3);
}
var noop = () => {
};
var base_op = {
  init: noop,
  add: noop,
  rem: noop,
  idx: 0
};
var AggregateOps = {
  values: {
    init: (m3) => m3.cell.store = true,
    value: (m3) => m3.cell.data.values(),
    idx: -1
  },
  count: {
    value: (m3) => m3.cell.num
  },
  __count__: {
    value: (m3) => m3.missing + m3.valid
  },
  missing: {
    value: (m3) => m3.missing
  },
  valid: {
    value: (m3) => m3.valid
  },
  sum: {
    init: (m3) => m3.sum = 0,
    value: (m3) => m3.valid ? m3.sum : void 0,
    add: (m3, v2) => m3.sum += +v2,
    rem: (m3, v2) => m3.sum -= v2
  },
  product: {
    init: (m3) => m3.product = 1,
    value: (m3) => m3.valid ? m3.product : void 0,
    add: (m3, v2) => m3.product *= v2,
    rem: (m3, v2) => m3.product /= v2
  },
  mean: {
    init: (m3) => m3.mean = 0,
    value: (m3) => m3.valid ? m3.mean : void 0,
    add: (m3, v2) => (m3.mean_d = v2 - m3.mean, m3.mean += m3.mean_d / m3.valid),
    rem: (m3, v2) => (m3.mean_d = v2 - m3.mean, m3.mean -= m3.valid ? m3.mean_d / m3.valid : m3.mean)
  },
  average: {
    value: (m3) => m3.valid ? m3.mean : void 0,
    req: ["mean"],
    idx: 1
  },
  variance: {
    init: (m3) => m3.dev = 0,
    value: (m3) => m3.valid > 1 ? Math.max(0, m3.dev) / (m3.valid - 1) : void 0,
    add: (m3, v2) => m3.dev += m3.mean_d * (v2 - m3.mean),
    rem: (m3, v2) => m3.dev -= m3.mean_d * (v2 - m3.mean),
    req: ["mean"],
    idx: 1
  },
  variancep: {
    value: (m3) => m3.valid ? Math.max(0, m3.dev) / m3.valid : void 0,
    req: ["variance"],
    idx: 2
  },
  stdev: {
    value: (m3) => m3.valid > 1 ? Math.sqrt(Math.max(0, m3.dev) / (m3.valid - 1)) : void 0,
    req: ["variance"],
    idx: 2
  },
  stdevp: {
    value: (m3) => m3.valid ? Math.sqrt(Math.max(0, m3.dev) / m3.valid) : void 0,
    req: ["variance"],
    idx: 2
  },
  stderr: {
    value: (m3) => m3.valid > 1 ? Math.sqrt(Math.max(0, m3.dev) / (m3.valid * (m3.valid - 1))) : void 0,
    req: ["variance"],
    idx: 2
  },
  distinct: {
    value: (m3) => m3.cell.data.distinct(m3.get),
    req: ["values"],
    idx: 3
  },
  ci0: {
    value: (m3) => m3.cell.data.ci0(m3.get),
    req: ["values"],
    idx: 3
  },
  ci1: {
    value: (m3) => m3.cell.data.ci1(m3.get),
    req: ["values"],
    idx: 3
  },
  median: {
    value: (m3) => m3.cell.data.q2(m3.get),
    req: ["values"],
    idx: 3
  },
  q1: {
    value: (m3) => m3.cell.data.q1(m3.get),
    req: ["values"],
    idx: 3
  },
  q3: {
    value: (m3) => m3.cell.data.q3(m3.get),
    req: ["values"],
    idx: 3
  },
  min: {
    init: (m3) => m3.min = void 0,
    value: (m3) => m3.min = Number.isNaN(m3.min) ? m3.cell.data.min(m3.get) : m3.min,
    add: (m3, v2) => {
      if (v2 < m3.min || m3.min === void 0) m3.min = v2;
    },
    rem: (m3, v2) => {
      if (v2 <= m3.min) m3.min = NaN;
    },
    req: ["values"],
    idx: 4
  },
  max: {
    init: (m3) => m3.max = void 0,
    value: (m3) => m3.max = Number.isNaN(m3.max) ? m3.cell.data.max(m3.get) : m3.max,
    add: (m3, v2) => {
      if (v2 > m3.max || m3.max === void 0) m3.max = v2;
    },
    rem: (m3, v2) => {
      if (v2 >= m3.max) m3.max = NaN;
    },
    req: ["values"],
    idx: 4
  },
  argmin: {
    init: (m3) => m3.argmin = void 0,
    value: (m3) => m3.argmin || m3.cell.data.argmin(m3.get),
    add: (m3, v2, t) => {
      if (v2 < m3.min) m3.argmin = t;
    },
    rem: (m3, v2) => {
      if (v2 <= m3.min) m3.argmin = void 0;
    },
    req: ["min", "values"],
    idx: 3
  },
  argmax: {
    init: (m3) => m3.argmax = void 0,
    value: (m3) => m3.argmax || m3.cell.data.argmax(m3.get),
    add: (m3, v2, t) => {
      if (v2 > m3.max) m3.argmax = t;
    },
    rem: (m3, v2) => {
      if (v2 >= m3.max) m3.argmax = void 0;
    },
    req: ["max", "values"],
    idx: 3
  },
  exponential: {
    init: (m3, r) => {
      m3.exp = 0;
      m3.exp_r = r;
    },
    value: (m3) => m3.valid ? m3.exp * (1 - m3.exp_r) / (1 - m3.exp_r ** m3.valid) : void 0,
    add: (m3, v2) => m3.exp = m3.exp_r * m3.exp + v2,
    rem: (m3, v2) => m3.exp = (m3.exp - v2 / m3.exp_r ** (m3.valid - 1)) / m3.exp_r
  },
  exponentialb: {
    value: (m3) => m3.valid ? m3.exp * (1 - m3.exp_r) : void 0,
    req: ["exponential"],
    idx: 1
  }
};
var ValidAggregateOps = Object.keys(AggregateOps).filter((d) => d !== "__count__");
function measure(key2, value4) {
  return (out, aggregate_param) => extend({
    name: key2,
    aggregate_param,
    out: out || key2
  }, base_op, value4);
}
[...ValidAggregateOps, "__count__"].forEach((key2) => {
  AggregateOps[key2] = measure(key2, AggregateOps[key2]);
});
function createMeasure(op, param2, name) {
  return AggregateOps[op](name, param2);
}
function compareIndex(a3, b10) {
  return a3.idx - b10.idx;
}
function resolve(agg) {
  const map3 = {};
  agg.forEach((a3) => map3[a3.name] = a3);
  const getreqs = (a3) => {
    if (!a3.req) return;
    a3.req.forEach((key2) => {
      if (!map3[key2]) getreqs(map3[key2] = AggregateOps[key2]());
    });
  };
  agg.forEach(getreqs);
  return Object.values(map3).sort(compareIndex);
}
function init() {
  this.valid = 0;
  this.missing = 0;
  this._ops.forEach((op) => op.aggregate_param == null ? op.init(this) : op.init(this, op.aggregate_param));
}
function add2(v2, t) {
  if (v2 == null || v2 === "") {
    ++this.missing;
    return;
  }
  if (v2 !== v2) return;
  ++this.valid;
  this._ops.forEach((op) => op.add(this, v2, t));
}
function rem(v2, t) {
  if (v2 == null || v2 === "") {
    --this.missing;
    return;
  }
  if (v2 !== v2) return;
  --this.valid;
  this._ops.forEach((op) => op.rem(this, v2, t));
}
function set2(t) {
  this._out.forEach((op) => t[op.out] = op.value(this));
  return t;
}
function compileMeasures(agg, field3) {
  const get7 = field3 || identity, ops2 = resolve(agg), out = agg.slice().sort(compareIndex);
  function ctr(cell2) {
    this._ops = ops2;
    this._out = out;
    this.cell = cell2;
    this.init();
  }
  ctr.prototype.init = init;
  ctr.prototype.add = add2;
  ctr.prototype.rem = rem;
  ctr.prototype.set = set2;
  ctr.prototype.get = get7;
  ctr.fields = agg.map((op) => op.out);
  return ctr;
}
function TupleStore(key2) {
  this._key = key2 ? field(key2) : tupleid;
  this.reset();
}
var prototype$1 = TupleStore.prototype;
prototype$1.reset = function() {
  this._add = [];
  this._rem = [];
  this._ext = null;
  this._get = null;
  this._q = null;
};
prototype$1.add = function(v2) {
  this._add.push(v2);
};
prototype$1.rem = function(v2) {
  this._rem.push(v2);
};
prototype$1.values = function() {
  this._get = null;
  if (this._rem.length === 0) return this._add;
  const a3 = this._add, r = this._rem, k = this._key, n = a3.length, m3 = r.length, x6 = Array(n - m3), map3 = {};
  let i, j, v2;
  for (i = 0; i < m3; ++i) {
    map3[k(r[i])] = 1;
  }
  for (i = 0, j = 0; i < n; ++i) {
    if (map3[k(v2 = a3[i])]) {
      map3[k(v2)] = 0;
    } else {
      x6[j++] = v2;
    }
  }
  this._rem = [];
  return this._add = x6;
};
prototype$1.distinct = function(get7) {
  const v2 = this.values(), map3 = {};
  let n = v2.length, count2 = 0, s;
  while (--n >= 0) {
    s = get7(v2[n]) + "";
    if (!hasOwnProperty(map3, s)) {
      map3[s] = 1;
      ++count2;
    }
  }
  return count2;
};
prototype$1.extent = function(get7) {
  if (this._get !== get7 || !this._ext) {
    const v2 = this.values(), i = extentIndex(v2, get7);
    this._ext = [v2[i[0]], v2[i[1]]];
    this._get = get7;
  }
  return this._ext;
};
prototype$1.argmin = function(get7) {
  return this.extent(get7)[0] || {};
};
prototype$1.argmax = function(get7) {
  return this.extent(get7)[1] || {};
};
prototype$1.min = function(get7) {
  const m3 = this.extent(get7)[0];
  return m3 != null ? get7(m3) : void 0;
};
prototype$1.max = function(get7) {
  const m3 = this.extent(get7)[1];
  return m3 != null ? get7(m3) : void 0;
};
prototype$1.quartile = function(get7) {
  if (this._get !== get7 || !this._q) {
    this._q = quartiles(this.values(), get7);
    this._get = get7;
  }
  return this._q;
};
prototype$1.q1 = function(get7) {
  return this.quartile(get7)[0];
};
prototype$1.q2 = function(get7) {
  return this.quartile(get7)[1];
};
prototype$1.q3 = function(get7) {
  return this.quartile(get7)[2];
};
prototype$1.ci = function(get7) {
  if (this._get !== get7 || !this._ci) {
    this._ci = bootstrapCI(this.values(), 1e3, 0.05, get7);
    this._get = get7;
  }
  return this._ci;
};
prototype$1.ci0 = function(get7) {
  return this.ci(get7)[0];
};
prototype$1.ci1 = function(get7) {
  return this.ci(get7)[1];
};
function Aggregate(params2) {
  Transform.call(this, null, params2);
  this._adds = [];
  this._mods = [];
  this._alen = 0;
  this._mlen = 0;
  this._drop = true;
  this._cross = false;
  this._dims = [];
  this._dnames = [];
  this._measures = [];
  this._countOnly = false;
  this._counts = null;
  this._prev = null;
  this._inputs = null;
  this._outputs = null;
}
Aggregate.Definition = {
  "type": "Aggregate",
  "metadata": {
    "generates": true,
    "changes": true
  },
  "params": [{
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "ops",
    "type": "enum",
    "array": true,
    "values": ValidAggregateOps
  }, {
    "name": "aggregate_params",
    "type": "number",
    "null": true,
    "array": true
  }, {
    "name": "fields",
    "type": "field",
    "null": true,
    "array": true
  }, {
    "name": "as",
    "type": "string",
    "null": true,
    "array": true
  }, {
    "name": "drop",
    "type": "boolean",
    "default": true
  }, {
    "name": "cross",
    "type": "boolean",
    "default": false
  }, {
    "name": "key",
    "type": "field"
  }]
};
inherits_default(Aggregate, Transform, {
  transform(_, pulse2) {
    const aggr = this, out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS), mod = _.modified();
    aggr.stamp = out.stamp;
    if (aggr.value && (mod || pulse2.modified(aggr._inputs, true))) {
      aggr._prev = aggr.value;
      aggr.value = mod ? aggr.init(_) : /* @__PURE__ */ Object.create(null);
      pulse2.visit(pulse2.SOURCE, (t) => aggr.add(t));
    } else {
      aggr.value = aggr.value || aggr.init(_);
      pulse2.visit(pulse2.REM, (t) => aggr.rem(t));
      pulse2.visit(pulse2.ADD, (t) => aggr.add(t));
    }
    out.modifies(aggr._outputs);
    aggr._drop = _.drop !== false;
    if (_.cross && aggr._dims.length > 1) {
      aggr._drop = false;
      aggr.cross();
    }
    if (pulse2.clean() && aggr._drop) {
      out.clean(true).runAfter(() => this.clean());
    }
    return aggr.changes(out);
  },
  cross() {
    const aggr = this, curr = aggr.value, dims = aggr._dnames, vals2 = dims.map(() => ({})), n = dims.length;
    function collect2(cells) {
      let key2, i, t, v2;
      for (key2 in cells) {
        t = cells[key2].tuple;
        for (i = 0; i < n; ++i) {
          vals2[i][v2 = t[dims[i]]] = v2;
        }
      }
    }
    collect2(aggr._prev);
    collect2(curr);
    function generate2(base, tuple, index4) {
      const name = dims[index4], v2 = vals2[index4++];
      for (const k in v2) {
        const key2 = base ? base + "|" + k : k;
        tuple[name] = v2[k];
        if (index4 < n) generate2(key2, tuple, index4);
        else if (!curr[key2]) aggr.cell(key2, tuple);
      }
    }
    generate2("", {}, 0);
  },
  init(_) {
    const inputs = this._inputs = [], outputs = this._outputs = [], inputMap = {};
    function inputVisit(get7) {
      const fields2 = array_default(accessorFields(get7)), n2 = fields2.length;
      let i2 = 0, f;
      for (; i2 < n2; ++i2) {
        if (!inputMap[f = fields2[i2]]) {
          inputMap[f] = 1;
          inputs.push(f);
        }
      }
    }
    this._dims = array_default(_.groupby);
    this._dnames = this._dims.map((d) => {
      const dname = accessorName(d);
      inputVisit(d);
      outputs.push(dname);
      return dname;
    });
    this.cellkey = _.key ? _.key : groupkey(this._dims);
    this._countOnly = true;
    this._counts = [];
    this._measures = [];
    const fields = _.fields || [null], ops2 = _.ops || ["count"], aggregate_params = _.aggregate_params || [null], as = _.as || [], n = fields.length, map3 = {};
    let field3, op, aggregate_param, m3, mname, outname, i;
    if (n !== ops2.length) {
      error("Unmatched number of fields and aggregate ops.");
    }
    for (i = 0; i < n; ++i) {
      field3 = fields[i];
      op = ops2[i];
      aggregate_param = aggregate_params[i] || null;
      if (field3 == null && op !== "count") {
        error("Null aggregate field specified.");
      }
      mname = accessorName(field3);
      outname = measureName(op, mname, as[i]);
      outputs.push(outname);
      if (op === "count") {
        this._counts.push(outname);
        continue;
      }
      m3 = map3[mname];
      if (!m3) {
        inputVisit(field3);
        m3 = map3[mname] = [];
        m3.field = field3;
        this._measures.push(m3);
      }
      if (op !== "count") this._countOnly = false;
      m3.push(createMeasure(op, aggregate_param, outname));
    }
    this._measures = this._measures.map((m4) => compileMeasures(m4, m4.field));
    return /* @__PURE__ */ Object.create(null);
  },
  // -- Cell Management -----
  cellkey: groupkey(),
  cell(key2, t) {
    let cell2 = this.value[key2];
    if (!cell2) {
      cell2 = this.value[key2] = this.newcell(key2, t);
      this._adds[this._alen++] = cell2;
    } else if (cell2.num === 0 && this._drop && cell2.stamp < this.stamp) {
      cell2.stamp = this.stamp;
      this._adds[this._alen++] = cell2;
    } else if (cell2.stamp < this.stamp) {
      cell2.stamp = this.stamp;
      this._mods[this._mlen++] = cell2;
    }
    return cell2;
  },
  newcell(key2, t) {
    const cell2 = {
      key: key2,
      num: 0,
      agg: null,
      tuple: this.newtuple(t, this._prev && this._prev[key2]),
      stamp: this.stamp,
      store: false
    };
    if (!this._countOnly) {
      const measures = this._measures, n = measures.length;
      cell2.agg = Array(n);
      for (let i = 0; i < n; ++i) {
        cell2.agg[i] = new measures[i](cell2);
      }
    }
    if (cell2.store) {
      cell2.data = new TupleStore();
    }
    return cell2;
  },
  newtuple(t, p) {
    const names = this._dnames, dims = this._dims, n = dims.length, x6 = {};
    for (let i = 0; i < n; ++i) {
      x6[names[i]] = dims[i](t);
    }
    return p ? replace(p.tuple, x6) : ingest$1(x6);
  },
  clean() {
    const cells = this.value;
    for (const key2 in cells) {
      if (cells[key2].num === 0) {
        delete cells[key2];
      }
    }
  },
  // -- Process Tuples -----
  add(t) {
    const key2 = this.cellkey(t), cell2 = this.cell(key2, t);
    cell2.num += 1;
    if (this._countOnly) return;
    if (cell2.store) cell2.data.add(t);
    const agg = cell2.agg;
    for (let i = 0, n = agg.length; i < n; ++i) {
      agg[i].add(agg[i].get(t), t);
    }
  },
  rem(t) {
    const key2 = this.cellkey(t), cell2 = this.cell(key2, t);
    cell2.num -= 1;
    if (this._countOnly) return;
    if (cell2.store) cell2.data.rem(t);
    const agg = cell2.agg;
    for (let i = 0, n = agg.length; i < n; ++i) {
      agg[i].rem(agg[i].get(t), t);
    }
  },
  celltuple(cell2) {
    const tuple = cell2.tuple, counts = this._counts;
    if (cell2.store) {
      cell2.data.values();
    }
    for (let i = 0, n = counts.length; i < n; ++i) {
      tuple[counts[i]] = cell2.num;
    }
    if (!this._countOnly) {
      const agg = cell2.agg;
      for (let i = 0, n = agg.length; i < n; ++i) {
        agg[i].set(tuple);
      }
    }
    return tuple;
  },
  changes(out) {
    const adds = this._adds, mods = this._mods, prev = this._prev, drop = this._drop, add7 = out.add, rem2 = out.rem, mod = out.mod;
    let cell2, key2, i, n;
    if (prev) for (key2 in prev) {
      cell2 = prev[key2];
      if (!drop || cell2.num) rem2.push(cell2.tuple);
    }
    for (i = 0, n = this._alen; i < n; ++i) {
      add7.push(this.celltuple(adds[i]));
      adds[i] = null;
    }
    for (i = 0, n = this._mlen; i < n; ++i) {
      cell2 = mods[i];
      (cell2.num === 0 && drop ? rem2 : mod).push(this.celltuple(cell2));
      mods[i] = null;
    }
    this._alen = this._mlen = 0;
    this._prev = null;
    return out;
  }
});
var EPSILON$1 = 1e-14;
function Bin(params2) {
  Transform.call(this, null, params2);
}
Bin.Definition = {
  "type": "Bin",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "field",
    "type": "field",
    "required": true
  }, {
    "name": "interval",
    "type": "boolean",
    "default": true
  }, {
    "name": "anchor",
    "type": "number"
  }, {
    "name": "maxbins",
    "type": "number",
    "default": 20
  }, {
    "name": "base",
    "type": "number",
    "default": 10
  }, {
    "name": "divide",
    "type": "number",
    "array": true,
    "default": [5, 2]
  }, {
    "name": "extent",
    "type": "number",
    "array": true,
    "length": 2,
    "required": true
  }, {
    "name": "span",
    "type": "number"
  }, {
    "name": "step",
    "type": "number"
  }, {
    "name": "steps",
    "type": "number",
    "array": true
  }, {
    "name": "minstep",
    "type": "number",
    "default": 0
  }, {
    "name": "nice",
    "type": "boolean",
    "default": true
  }, {
    "name": "name",
    "type": "string"
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": 2,
    "default": ["bin0", "bin1"]
  }]
};
inherits_default(Bin, Transform, {
  transform(_, pulse2) {
    const band2 = _.interval !== false, bins2 = this._bins(_), start = bins2.start, step = bins2.step, as = _.as || ["bin0", "bin1"], b02 = as[0], b12 = as[1];
    let flag2;
    if (_.modified()) {
      pulse2 = pulse2.reflow(true);
      flag2 = pulse2.SOURCE;
    } else {
      flag2 = pulse2.modified(accessorFields(_.field)) ? pulse2.ADD_MOD : pulse2.ADD;
    }
    pulse2.visit(flag2, band2 ? (t) => {
      const v2 = bins2(t);
      t[b02] = v2;
      t[b12] = v2 == null ? null : start + step * (1 + (v2 - start) / step);
    } : (t) => t[b02] = bins2(t));
    return pulse2.modifies(band2 ? as : b02);
  },
  _bins(_) {
    if (this.value && !_.modified()) {
      return this.value;
    }
    const field3 = _.field, bins2 = bin2(_), step = bins2.step;
    let start = bins2.start, stop2 = start + Math.ceil((bins2.stop - start) / step) * step, a3, d;
    if ((a3 = _.anchor) != null) {
      d = a3 - (start + step * Math.floor((a3 - start) / step));
      start += d;
      stop2 += d;
    }
    const f = function(t) {
      let v2 = toNumber(field3(t));
      return v2 == null ? null : v2 < start ? -Infinity : v2 > stop2 ? Infinity : (v2 = Math.max(start, Math.min(v2, stop2 - step)), start + step * Math.floor(EPSILON$1 + (v2 - start) / step));
    };
    f.start = start;
    f.stop = stop2;
    f.step = step;
    return this.value = accessor(f, accessorFields(field3), _.name || "bin_" + accessorName(field3));
  }
});
function SortedList(idFunc, source4, input) {
  const $ = idFunc;
  let data4 = source4 || [], add7 = input || [], rem2 = {}, cnt = 0;
  return {
    add: (t) => add7.push(t),
    remove: (t) => rem2[$(t)] = ++cnt,
    size: () => data4.length,
    data: (compare4, resort) => {
      if (cnt) {
        data4 = data4.filter((t) => !rem2[$(t)]);
        rem2 = {};
        cnt = 0;
      }
      if (resort && compare4) {
        data4.sort(compare4);
      }
      if (add7.length) {
        data4 = compare4 ? merge(compare4, data4, add7.sort(compare4)) : data4.concat(add7);
        add7 = [];
      }
      return data4;
    }
  };
}
function Collect(params2) {
  Transform.call(this, [], params2);
}
Collect.Definition = {
  "type": "Collect",
  "metadata": {
    "source": true
  },
  "params": [{
    "name": "sort",
    "type": "compare"
  }]
};
inherits_default(Collect, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.ALL), list = SortedList(tupleid, this.value, out.materialize(out.ADD).add), sort4 = _.sort, mod = pulse2.changed() || sort4 && (_.modified("sort") || pulse2.modified(sort4.fields));
    out.visit(out.REM, list.remove);
    this.modified(mod);
    this.value = out.source = list.data(stableCompare(sort4), mod);
    if (pulse2.source && pulse2.source.root) {
      this.value.root = pulse2.source.root;
    }
    return out;
  }
});
function Compare(params2) {
  Operator.call(this, null, update$5, params2);
}
inherits_default(Compare, Operator);
function update$5(_) {
  return this.value && !_.modified() ? this.value : compare(_.fields, _.orders);
}
function CountPattern(params2) {
  Transform.call(this, null, params2);
}
CountPattern.Definition = {
  "type": "CountPattern",
  "metadata": {
    "generates": true,
    "changes": true
  },
  "params": [{
    "name": "field",
    "type": "field",
    "required": true
  }, {
    "name": "case",
    "type": "enum",
    "values": ["upper", "lower", "mixed"],
    "default": "mixed"
  }, {
    "name": "pattern",
    "type": "string",
    "default": '[\\w"]+'
  }, {
    "name": "stopwords",
    "type": "string",
    "default": ""
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": 2,
    "default": ["text", "count"]
  }]
};
function tokenize(text4, tcase, match3) {
  switch (tcase) {
    case "upper":
      text4 = text4.toUpperCase();
      break;
    case "lower":
      text4 = text4.toLowerCase();
      break;
  }
  return text4.match(match3);
}
inherits_default(CountPattern, Transform, {
  transform(_, pulse2) {
    const process = (update3) => (tuple) => {
      var tokens = tokenize(get7(tuple), _.case, match3) || [], t;
      for (var i = 0, n = tokens.length; i < n; ++i) {
        if (!stop2.test(t = tokens[i])) update3(t);
      }
    };
    const init2 = this._parameterCheck(_, pulse2), counts = this._counts, match3 = this._match, stop2 = this._stop, get7 = _.field, as = _.as || ["text", "count"], add7 = process((t) => counts[t] = 1 + (counts[t] || 0)), rem2 = process((t) => counts[t] -= 1);
    if (init2) {
      pulse2.visit(pulse2.SOURCE, add7);
    } else {
      pulse2.visit(pulse2.ADD, add7);
      pulse2.visit(pulse2.REM, rem2);
    }
    return this._finish(pulse2, as);
  },
  _parameterCheck(_, pulse2) {
    let init2 = false;
    if (_.modified("stopwords") || !this._stop) {
      this._stop = new RegExp("^" + (_.stopwords || "") + "$", "i");
      init2 = true;
    }
    if (_.modified("pattern") || !this._match) {
      this._match = new RegExp(_.pattern || "[\\w']+", "g");
      init2 = true;
    }
    if (_.modified("field") || pulse2.modified(_.field.fields)) {
      init2 = true;
    }
    if (init2) this._counts = {};
    return init2;
  },
  _finish(pulse2, as) {
    const counts = this._counts, tuples = this._tuples || (this._tuples = {}), text4 = as[0], count2 = as[1], out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS);
    let w3, t, c3;
    for (w3 in counts) {
      t = tuples[w3];
      c3 = counts[w3] || 0;
      if (!t && c3) {
        tuples[w3] = t = ingest$1({});
        t[text4] = w3;
        t[count2] = c3;
        out.add.push(t);
      } else if (c3 === 0) {
        if (t) out.rem.push(t);
        counts[w3] = null;
        tuples[w3] = null;
      } else if (t[count2] !== c3) {
        t[count2] = c3;
        out.mod.push(t);
      }
    }
    return out.modifies(as);
  }
});
function Cross(params2) {
  Transform.call(this, null, params2);
}
Cross.Definition = {
  "type": "Cross",
  "metadata": {
    "generates": true
  },
  "params": [{
    "name": "filter",
    "type": "expr"
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": 2,
    "default": ["a", "b"]
  }]
};
inherits_default(Cross, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.NO_SOURCE), as = _.as || ["a", "b"], a3 = as[0], b10 = as[1], reset3 = !this.value || pulse2.changed(pulse2.ADD_REM) || _.modified("as") || _.modified("filter");
    let data4 = this.value;
    if (reset3) {
      if (data4) out.rem = data4;
      data4 = pulse2.materialize(pulse2.SOURCE).source;
      out.add = this.value = cross(data4, a3, b10, _.filter || truthy);
    } else {
      out.mod = data4;
    }
    out.source = this.value;
    return out.modifies(as);
  }
});
function cross(input, a3, b10, filter4) {
  var data4 = [], t = {}, n = input.length, i = 0, j, left;
  for (; i < n; ++i) {
    t[a3] = left = input[i];
    for (j = 0; j < n; ++j) {
      t[b10] = input[j];
      if (filter4(t)) {
        data4.push(ingest$1(t));
        t = {};
        t[a3] = left;
      }
    }
  }
  return data4;
}
var Distributions = {
  kde,
  mixture,
  normal: gaussian,
  lognormal,
  uniform
};
var DISTRIBUTIONS = "distributions";
var FUNCTION = "function";
var FIELD = "field";
function parse3(def3, data4) {
  const func = def3[FUNCTION];
  if (!hasOwnProperty(Distributions, func)) {
    error("Unknown distribution function: " + func);
  }
  const d = Distributions[func]();
  for (const name in def3) {
    if (name === FIELD) {
      d.data((def3.from || data4()).map(def3[name]));
    } else if (name === DISTRIBUTIONS) {
      d[name](def3[name].map((_) => parse3(_, data4)));
    } else if (typeof d[name] === FUNCTION) {
      d[name](def3[name]);
    }
  }
  return d;
}
function Density(params2) {
  Transform.call(this, null, params2);
}
var distributions = [{
  "key": {
    "function": "normal"
  },
  "params": [{
    "name": "mean",
    "type": "number",
    "default": 0
  }, {
    "name": "stdev",
    "type": "number",
    "default": 1
  }]
}, {
  "key": {
    "function": "lognormal"
  },
  "params": [{
    "name": "mean",
    "type": "number",
    "default": 0
  }, {
    "name": "stdev",
    "type": "number",
    "default": 1
  }]
}, {
  "key": {
    "function": "uniform"
  },
  "params": [{
    "name": "min",
    "type": "number",
    "default": 0
  }, {
    "name": "max",
    "type": "number",
    "default": 1
  }]
}, {
  "key": {
    "function": "kde"
  },
  "params": [{
    "name": "field",
    "type": "field",
    "required": true
  }, {
    "name": "from",
    "type": "data"
  }, {
    "name": "bandwidth",
    "type": "number",
    "default": 0
  }]
}];
var mixture2 = {
  "key": {
    "function": "mixture"
  },
  "params": [{
    "name": "distributions",
    "type": "param",
    "array": true,
    "params": distributions
  }, {
    "name": "weights",
    "type": "number",
    "array": true
  }]
};
Density.Definition = {
  "type": "Density",
  "metadata": {
    "generates": true
  },
  "params": [{
    "name": "extent",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "steps",
    "type": "number"
  }, {
    "name": "minsteps",
    "type": "number",
    "default": 25
  }, {
    "name": "maxsteps",
    "type": "number",
    "default": 200
  }, {
    "name": "method",
    "type": "string",
    "default": "pdf",
    "values": ["pdf", "cdf"]
  }, {
    "name": "distribution",
    "type": "param",
    "params": distributions.concat(mixture2)
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "default": ["value", "density"]
  }]
};
inherits_default(Density, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS);
    if (!this.value || pulse2.changed() || _.modified()) {
      const dist2 = parse3(_.distribution, source(pulse2)), minsteps = _.steps || _.minsteps || 25, maxsteps = _.steps || _.maxsteps || 200;
      let method2 = _.method || "pdf";
      if (method2 !== "pdf" && method2 !== "cdf") {
        error("Invalid density method: " + method2);
      }
      if (!_.extent && !dist2.data) {
        error("Missing density extent parameter.");
      }
      method2 = dist2[method2];
      const as = _.as || ["value", "density"], domain4 = _.extent || extent(dist2.data()), values4 = sampleCurve(method2, domain4, minsteps, maxsteps).map((v2) => {
        const tuple = {};
        tuple[as[0]] = v2[0];
        tuple[as[1]] = v2[1];
        return ingest$1(tuple);
      });
      if (this.value) out.rem = this.value;
      this.value = out.add = out.source = values4;
    }
    return out;
  }
});
function source(pulse2) {
  return () => pulse2.materialize(pulse2.SOURCE).source;
}
function fieldNames(fields, as) {
  if (!fields) return null;
  return fields.map((f, i) => as[i] || accessorName(f));
}
function partition$1(data4, groupby, field3) {
  const groups = [], get7 = (f) => f(t);
  let map3, i, n, t, k, g;
  if (groupby == null) {
    groups.push(data4.map(field3));
  } else {
    for (map3 = {}, i = 0, n = data4.length; i < n; ++i) {
      t = data4[i];
      k = groupby.map(get7);
      g = map3[k];
      if (!g) {
        map3[k] = g = [];
        g.dims = k;
        groups.push(g);
      }
      g.push(field3(t));
    }
  }
  return groups;
}
var Output = "bin";
function DotBin(params2) {
  Transform.call(this, null, params2);
}
DotBin.Definition = {
  "type": "DotBin",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "field",
    "type": "field",
    "required": true
  }, {
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "step",
    "type": "number"
  }, {
    "name": "smooth",
    "type": "boolean",
    "default": false
  }, {
    "name": "as",
    "type": "string",
    "default": Output
  }]
};
var autostep = (data4, field3) => span_default(extent(data4, field3)) / 30;
inherits_default(DotBin, Transform, {
  transform(_, pulse2) {
    if (this.value && !(_.modified() || pulse2.changed())) {
      return pulse2;
    }
    const source4 = pulse2.materialize(pulse2.SOURCE).source, groups = partition$1(pulse2.source, _.groupby, identity), smooth = _.smooth || false, field3 = _.field, step = _.step || autostep(source4, field3), sort4 = stableCompare((a3, b10) => field3(a3) - field3(b10)), as = _.as || Output, n = groups.length;
    let min4 = Infinity, max4 = -Infinity, i = 0, j;
    for (; i < n; ++i) {
      const g = groups[i].sort(sort4);
      j = -1;
      for (const v2 of dotbin(g, step, smooth, field3)) {
        if (v2 < min4) min4 = v2;
        if (v2 > max4) max4 = v2;
        g[++j][as] = v2;
      }
    }
    this.value = {
      start: min4,
      stop: max4,
      step
    };
    return pulse2.reflow(true).modifies(as);
  }
});
function Expression(params2) {
  Operator.call(this, null, update$4, params2);
  this.modified(true);
}
inherits_default(Expression, Operator);
function update$4(_) {
  const expr2 = _.expr;
  return this.value && !_.modified("expr") ? this.value : accessor((datum3) => expr2(datum3, _), accessorFields(expr2), accessorName(expr2));
}
function Extent(params2) {
  Transform.call(this, [void 0, void 0], params2);
}
Extent.Definition = {
  "type": "Extent",
  "metadata": {},
  "params": [{
    "name": "field",
    "type": "field",
    "required": true
  }]
};
inherits_default(Extent, Transform, {
  transform(_, pulse2) {
    const extent2 = this.value, field3 = _.field, mod = pulse2.changed() || pulse2.modified(field3.fields) || _.modified("field");
    let min4 = extent2[0], max4 = extent2[1];
    if (mod || min4 == null) {
      min4 = Infinity;
      max4 = -Infinity;
    }
    pulse2.visit(mod ? pulse2.SOURCE : pulse2.ADD, (t) => {
      const v2 = toNumber(field3(t));
      if (v2 != null) {
        if (v2 < min4) min4 = v2;
        if (v2 > max4) max4 = v2;
      }
    });
    if (!Number.isFinite(min4) || !Number.isFinite(max4)) {
      let name = accessorName(field3);
      if (name) name = ` for field "${name}"`;
      pulse2.dataflow.warn(`Infinite extent${name}: [${min4}, ${max4}]`);
      min4 = max4 = void 0;
    }
    this.value = [min4, max4];
  }
});
function Subflow(pulse2, parent) {
  Operator.call(this, pulse2);
  this.parent = parent;
  this.count = 0;
}
inherits_default(Subflow, Operator, {
  /**
   * Routes pulses from this subflow to a target transform.
   * @param {Transform} target - A transform that receives the subflow of tuples.
   */
  connect(target2) {
    this.detachSubflow = target2.detachSubflow;
    this.targets().add(target2);
    return target2.source = this;
  },
  /**
   * Add an 'add' tuple to the subflow pulse.
   * @param {Tuple} t - The tuple being added.
   */
  add(t) {
    this.count += 1;
    this.value.add.push(t);
  },
  /**
   * Add a 'rem' tuple to the subflow pulse.
   * @param {Tuple} t - The tuple being removed.
   */
  rem(t) {
    this.count -= 1;
    this.value.rem.push(t);
  },
  /**
   * Add a 'mod' tuple to the subflow pulse.
   * @param {Tuple} t - The tuple being modified.
   */
  mod(t) {
    this.value.mod.push(t);
  },
  /**
   * Re-initialize this operator's pulse value.
   * @param {Pulse} pulse - The pulse to copy from.
   * @see Pulse.init
   */
  init(pulse2) {
    this.value.init(pulse2, pulse2.NO_SOURCE);
  },
  /**
   * Evaluate this operator. This method overrides the
   * default behavior to simply return the contained pulse value.
   * @return {Pulse}
   */
  evaluate() {
    return this.value;
  }
});
function Facet(params2) {
  Transform.call(this, {}, params2);
  this._keys = fastmap();
  const a3 = this._targets = [];
  a3.active = 0;
  a3.forEach = (f) => {
    for (let i = 0, n = a3.active; i < n; ++i) {
      f(a3[i], i, a3);
    }
  };
}
inherits_default(Facet, Transform, {
  activate(flow) {
    this._targets[this._targets.active++] = flow;
  },
  // parent argument provided by PreFacet subclass
  subflow(key2, flow, pulse2, parent) {
    const flows = this.value;
    let sf = hasOwnProperty(flows, key2) && flows[key2], df, p;
    if (!sf) {
      p = parent || (p = this._group[key2]) && p.tuple;
      df = pulse2.dataflow;
      sf = new Subflow(pulse2.fork(pulse2.NO_SOURCE), this);
      df.add(sf).connect(flow(df, key2, p));
      flows[key2] = sf;
      this.activate(sf);
    } else if (sf.value.stamp < pulse2.stamp) {
      sf.init(pulse2);
      this.activate(sf);
    }
    return sf;
  },
  clean() {
    const flows = this.value;
    let detached = 0;
    for (const key2 in flows) {
      if (flows[key2].count === 0) {
        const detach = flows[key2].detachSubflow;
        if (detach) detach();
        delete flows[key2];
        ++detached;
      }
    }
    if (detached) {
      const active = this._targets.filter((sf) => sf && sf.count > 0);
      this.initTargets(active);
    }
  },
  initTargets(act) {
    const a3 = this._targets, n = a3.length, m3 = act ? act.length : 0;
    let i = 0;
    for (; i < m3; ++i) {
      a3[i] = act[i];
    }
    for (; i < n && a3[i] != null; ++i) {
      a3[i] = null;
    }
    a3.active = m3;
  },
  transform(_, pulse2) {
    const df = pulse2.dataflow, key2 = _.key, flow = _.subflow, cache2 = this._keys, rekey = _.modified("key"), subflow = (key3) => this.subflow(key3, flow, pulse2);
    this._group = _.group || {};
    this.initTargets();
    pulse2.visit(pulse2.REM, (t) => {
      const id2 = tupleid(t), k = cache2.get(id2);
      if (k !== void 0) {
        cache2.delete(id2);
        subflow(k).rem(t);
      }
    });
    pulse2.visit(pulse2.ADD, (t) => {
      const k = key2(t);
      cache2.set(tupleid(t), k);
      subflow(k).add(t);
    });
    if (rekey || pulse2.modified(key2.fields)) {
      pulse2.visit(pulse2.MOD, (t) => {
        const id2 = tupleid(t), k0 = cache2.get(id2), k1 = key2(t);
        if (k0 === k1) {
          subflow(k1).mod(t);
        } else {
          cache2.set(id2, k1);
          subflow(k0).rem(t);
          subflow(k1).add(t);
        }
      });
    } else if (pulse2.changed(pulse2.MOD)) {
      pulse2.visit(pulse2.MOD, (t) => {
        subflow(cache2.get(tupleid(t))).mod(t);
      });
    }
    if (rekey) {
      pulse2.visit(pulse2.REFLOW, (t) => {
        const id2 = tupleid(t), k0 = cache2.get(id2), k1 = key2(t);
        if (k0 !== k1) {
          cache2.set(id2, k1);
          subflow(k0).rem(t);
          subflow(k1).add(t);
        }
      });
    }
    if (pulse2.clean()) {
      df.runAfter(() => {
        this.clean();
        cache2.clean();
      });
    } else if (cache2.empty > df.cleanThreshold) {
      df.runAfter(cache2.clean);
    }
    return pulse2;
  }
});
function Field(params2) {
  Operator.call(this, null, update$3, params2);
}
inherits_default(Field, Operator);
function update$3(_) {
  return this.value && !_.modified() ? this.value : isArray(_.name) ? array_default(_.name).map((f) => field(f)) : field(_.name, _.as);
}
function Filter(params2) {
  Transform.call(this, fastmap(), params2);
}
Filter.Definition = {
  "type": "Filter",
  "metadata": {
    "changes": true
  },
  "params": [{
    "name": "expr",
    "type": "expr",
    "required": true
  }]
};
inherits_default(Filter, Transform, {
  transform(_, pulse2) {
    const df = pulse2.dataflow, cache2 = this.value, output3 = pulse2.fork(), add7 = output3.add, rem2 = output3.rem, mod = output3.mod, test2 = _.expr;
    let isMod = true;
    pulse2.visit(pulse2.REM, (t) => {
      const id2 = tupleid(t);
      if (!cache2.has(id2)) rem2.push(t);
      else cache2.delete(id2);
    });
    pulse2.visit(pulse2.ADD, (t) => {
      if (test2(t, _)) add7.push(t);
      else cache2.set(tupleid(t), 1);
    });
    function revisit(t) {
      const id2 = tupleid(t), b10 = test2(t, _), s = cache2.get(id2);
      if (b10 && s) {
        cache2.delete(id2);
        add7.push(t);
      } else if (!b10 && !s) {
        cache2.set(id2, 1);
        rem2.push(t);
      } else if (isMod && b10 && !s) {
        mod.push(t);
      }
    }
    pulse2.visit(pulse2.MOD, revisit);
    if (_.modified()) {
      isMod = false;
      pulse2.visit(pulse2.REFLOW, revisit);
    }
    if (cache2.empty > df.cleanThreshold) df.runAfter(cache2.clean);
    return output3;
  }
});
function Flatten(params2) {
  Transform.call(this, [], params2);
}
Flatten.Definition = {
  "type": "Flatten",
  "metadata": {
    "generates": true
  },
  "params": [{
    "name": "fields",
    "type": "field",
    "array": true,
    "required": true
  }, {
    "name": "index",
    "type": "string"
  }, {
    "name": "as",
    "type": "string",
    "array": true
  }]
};
inherits_default(Flatten, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.NO_SOURCE), fields = _.fields, as = fieldNames(fields, _.as || []), index4 = _.index || null, m3 = as.length;
    out.rem = this.value;
    pulse2.visit(pulse2.SOURCE, (t) => {
      const arrays = fields.map((f) => f(t)), maxlen = arrays.reduce((l, a3) => Math.max(l, a3.length), 0);
      let i = 0, j, d, v2;
      for (; i < maxlen; ++i) {
        d = derive(t);
        for (j = 0; j < m3; ++j) {
          d[as[j]] = (v2 = arrays[j][i]) == null ? null : v2;
        }
        if (index4) {
          d[index4] = i;
        }
        out.add.push(d);
      }
    });
    this.value = out.source = out.add;
    if (index4) out.modifies(index4);
    return out.modifies(as);
  }
});
function Fold(params2) {
  Transform.call(this, [], params2);
}
Fold.Definition = {
  "type": "Fold",
  "metadata": {
    "generates": true
  },
  "params": [{
    "name": "fields",
    "type": "field",
    "array": true,
    "required": true
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": 2,
    "default": ["key", "value"]
  }]
};
inherits_default(Fold, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.NO_SOURCE), fields = _.fields, fnames = fields.map(accessorName), as = _.as || ["key", "value"], k = as[0], v2 = as[1], n = fields.length;
    out.rem = this.value;
    pulse2.visit(pulse2.SOURCE, (t) => {
      for (let i = 0, d; i < n; ++i) {
        d = derive(t);
        d[k] = fnames[i];
        d[v2] = fields[i](t);
        out.add.push(d);
      }
    });
    this.value = out.source = out.add;
    return out.modifies(as);
  }
});
function Formula(params2) {
  Transform.call(this, null, params2);
}
Formula.Definition = {
  "type": "Formula",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "expr",
    "type": "expr",
    "required": true
  }, {
    "name": "as",
    "type": "string",
    "required": true
  }, {
    "name": "initonly",
    "type": "boolean"
  }]
};
inherits_default(Formula, Transform, {
  transform(_, pulse2) {
    const func = _.expr, as = _.as, mod = _.modified(), flag2 = _.initonly ? pulse2.ADD : mod ? pulse2.SOURCE : pulse2.modified(func.fields) || pulse2.modified(as) ? pulse2.ADD_MOD : pulse2.ADD;
    if (mod) {
      pulse2 = pulse2.materialize().reflow(true);
    }
    if (!_.initonly) {
      pulse2.modifies(as);
    }
    return pulse2.visit(flag2, (t) => t[as] = func(t, _));
  }
});
function Generate(params2) {
  Transform.call(this, [], params2);
}
inherits_default(Generate, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.ALL), gen = _.generator;
    let data4 = this.value, num = _.size - data4.length, add7, rem2, t;
    if (num > 0) {
      for (add7 = []; --num >= 0; ) {
        add7.push(t = ingest$1(gen(_)));
        data4.push(t);
      }
      out.add = out.add.length ? out.materialize(out.ADD).add.concat(add7) : add7;
    } else {
      rem2 = data4.slice(0, -num);
      out.rem = out.rem.length ? out.materialize(out.REM).rem.concat(rem2) : rem2;
      data4 = data4.slice(-num);
    }
    out.source = this.value = data4;
    return out;
  }
});
var Methods = {
  value: "value",
  median,
  mean,
  min,
  max
};
var Empty = [];
function Impute(params2) {
  Transform.call(this, [], params2);
}
Impute.Definition = {
  "type": "Impute",
  "metadata": {
    "changes": true
  },
  "params": [{
    "name": "field",
    "type": "field",
    "required": true
  }, {
    "name": "key",
    "type": "field",
    "required": true
  }, {
    "name": "keyvals",
    "array": true
  }, {
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "method",
    "type": "enum",
    "default": "value",
    "values": ["value", "mean", "median", "max", "min"]
  }, {
    "name": "value",
    "default": 0
  }]
};
function getValue(_) {
  var m3 = _.method || Methods.value, v2;
  if (Methods[m3] == null) {
    error("Unrecognized imputation method: " + m3);
  } else if (m3 === Methods.value) {
    v2 = _.value !== void 0 ? _.value : 0;
    return () => v2;
  } else {
    return Methods[m3];
  }
}
function getField(_) {
  const f = _.field;
  return (t) => t ? f(t) : NaN;
}
inherits_default(Impute, Transform, {
  transform(_, pulse2) {
    var out = pulse2.fork(pulse2.ALL), impute = getValue(_), field3 = getField(_), fName = accessorName(_.field), kName = accessorName(_.key), gNames = (_.groupby || []).map(accessorName), groups = partition(pulse2.source, _.groupby, _.key, _.keyvals), curr = [], prev = this.value, m3 = groups.domain.length, group3, value4, gVals, kVal, g, i, j, l, n, t;
    for (g = 0, l = groups.length; g < l; ++g) {
      group3 = groups[g];
      gVals = group3.values;
      value4 = NaN;
      for (j = 0; j < m3; ++j) {
        if (group3[j] != null) continue;
        kVal = groups.domain[j];
        t = {
          _impute: true
        };
        for (i = 0, n = gVals.length; i < n; ++i) t[gNames[i]] = gVals[i];
        t[kName] = kVal;
        t[fName] = Number.isNaN(value4) ? value4 = impute(group3, field3) : value4;
        curr.push(ingest$1(t));
      }
    }
    if (curr.length) out.add = out.materialize(out.ADD).add.concat(curr);
    if (prev.length) out.rem = out.materialize(out.REM).rem.concat(prev);
    this.value = curr;
    return out;
  }
});
function partition(data4, groupby, key2, keyvals) {
  var get7 = (f) => f(t), groups = [], domain4 = keyvals ? keyvals.slice() : [], kMap = {}, gMap = {}, gVals, gKey, group3, i, j, k, n, t;
  domain4.forEach((k2, i2) => kMap[k2] = i2 + 1);
  for (i = 0, n = data4.length; i < n; ++i) {
    t = data4[i];
    k = key2(t);
    j = kMap[k] || (kMap[k] = domain4.push(k));
    gKey = (gVals = groupby ? groupby.map(get7) : Empty) + "";
    if (!(group3 = gMap[gKey])) {
      group3 = gMap[gKey] = [];
      groups.push(group3);
      group3.values = gVals;
    }
    group3[j - 1] = t;
  }
  groups.domain = domain4;
  return groups;
}
function JoinAggregate(params2) {
  Aggregate.call(this, params2);
}
JoinAggregate.Definition = {
  "type": "JoinAggregate",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "fields",
    "type": "field",
    "null": true,
    "array": true
  }, {
    "name": "ops",
    "type": "enum",
    "array": true,
    "values": ValidAggregateOps
  }, {
    "name": "as",
    "type": "string",
    "null": true,
    "array": true
  }, {
    "name": "key",
    "type": "field"
  }]
};
inherits_default(JoinAggregate, Aggregate, {
  transform(_, pulse2) {
    const aggr = this, mod = _.modified();
    let cells;
    if (aggr.value && (mod || pulse2.modified(aggr._inputs, true))) {
      cells = aggr.value = mod ? aggr.init(_) : {};
      pulse2.visit(pulse2.SOURCE, (t) => aggr.add(t));
    } else {
      cells = aggr.value = aggr.value || this.init(_);
      pulse2.visit(pulse2.REM, (t) => aggr.rem(t));
      pulse2.visit(pulse2.ADD, (t) => aggr.add(t));
    }
    aggr.changes();
    pulse2.visit(pulse2.SOURCE, (t) => {
      extend(t, cells[aggr.cellkey(t)].tuple);
    });
    return pulse2.reflow(mod).modifies(this._outputs);
  },
  changes() {
    const adds = this._adds, mods = this._mods;
    let i, n;
    for (i = 0, n = this._alen; i < n; ++i) {
      this.celltuple(adds[i]);
      adds[i] = null;
    }
    for (i = 0, n = this._mlen; i < n; ++i) {
      this.celltuple(mods[i]);
      mods[i] = null;
    }
    this._alen = this._mlen = 0;
  }
});
function KDE(params2) {
  Transform.call(this, null, params2);
}
KDE.Definition = {
  "type": "KDE",
  "metadata": {
    "generates": true
  },
  "params": [{
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "field",
    "type": "field",
    "required": true
  }, {
    "name": "cumulative",
    "type": "boolean",
    "default": false
  }, {
    "name": "counts",
    "type": "boolean",
    "default": false
  }, {
    "name": "bandwidth",
    "type": "number",
    "default": 0
  }, {
    "name": "extent",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "resolve",
    "type": "enum",
    "values": ["shared", "independent"],
    "default": "independent"
  }, {
    "name": "steps",
    "type": "number"
  }, {
    "name": "minsteps",
    "type": "number",
    "default": 25
  }, {
    "name": "maxsteps",
    "type": "number",
    "default": 200
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "default": ["value", "density"]
  }]
};
inherits_default(KDE, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS);
    if (!this.value || pulse2.changed() || _.modified()) {
      const source4 = pulse2.materialize(pulse2.SOURCE).source, groups = partition$1(source4, _.groupby, _.field), names = (_.groupby || []).map(accessorName), bandwidth3 = _.bandwidth, method2 = _.cumulative ? "cdf" : "pdf", as = _.as || ["value", "density"], values4 = [];
      let domain4 = _.extent, minsteps = _.steps || _.minsteps || 25, maxsteps = _.steps || _.maxsteps || 200;
      if (method2 !== "pdf" && method2 !== "cdf") {
        error("Invalid density method: " + method2);
      }
      if (_.resolve === "shared") {
        if (!domain4) domain4 = extent(source4, _.field);
        minsteps = maxsteps = _.steps || maxsteps;
      }
      groups.forEach((g) => {
        const density = kde(g, bandwidth3)[method2], scale9 = _.counts ? g.length : 1, local = domain4 || extent(g);
        sampleCurve(density, local, minsteps, maxsteps).forEach((v2) => {
          const t = {};
          for (let i = 0; i < names.length; ++i) {
            t[names[i]] = g.dims[i];
          }
          t[as[0]] = v2[0];
          t[as[1]] = v2[1] * scale9;
          values4.push(ingest$1(t));
        });
      });
      if (this.value) out.rem = this.value;
      this.value = out.add = out.source = values4;
    }
    return out;
  }
});
function Key(params2) {
  Operator.call(this, null, update$2, params2);
}
inherits_default(Key, Operator);
function update$2(_) {
  return this.value && !_.modified() ? this.value : key(_.fields, _.flat);
}
function Load(params2) {
  Transform.call(this, [], params2);
  this._pending = null;
}
inherits_default(Load, Transform, {
  transform(_, pulse2) {
    const df = pulse2.dataflow;
    if (this._pending) {
      return output2(this, pulse2, this._pending);
    }
    if (stop(_)) return pulse2.StopPropagation;
    if (_.values) {
      return output2(this, pulse2, df.parse(_.values, _.format));
    } else if (_.async) {
      const p = df.request(_.url, _.format).then((res) => {
        this._pending = array_default(res.data);
        return (df2) => df2.touch(this);
      });
      return {
        async: p
      };
    } else {
      return df.request(_.url, _.format).then((res) => output2(this, pulse2, array_default(res.data)));
    }
  }
});
function stop(_) {
  return _.modified("async") && !(_.modified("values") || _.modified("url") || _.modified("format"));
}
function output2(op, pulse2, data4) {
  data4.forEach(ingest$1);
  const out = pulse2.fork(pulse2.NO_FIELDS & pulse2.NO_SOURCE);
  out.rem = op.value;
  op.value = out.source = out.add = data4;
  op._pending = null;
  if (out.rem.length) out.clean(true);
  return out;
}
function Lookup(params2) {
  Transform.call(this, {}, params2);
}
Lookup.Definition = {
  "type": "Lookup",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "index",
    "type": "index",
    "params": [{
      "name": "from",
      "type": "data",
      "required": true
    }, {
      "name": "key",
      "type": "field",
      "required": true
    }]
  }, {
    "name": "values",
    "type": "field",
    "array": true
  }, {
    "name": "fields",
    "type": "field",
    "array": true,
    "required": true
  }, {
    "name": "as",
    "type": "string",
    "array": true
  }, {
    "name": "default",
    "default": null
  }]
};
inherits_default(Lookup, Transform, {
  transform(_, pulse2) {
    const keys4 = _.fields, index4 = _.index, values4 = _.values, defaultValue = _.default == null ? null : _.default, reset3 = _.modified(), n = keys4.length;
    let flag2 = reset3 ? pulse2.SOURCE : pulse2.ADD, out = pulse2, as = _.as, set7, m3, mods;
    if (values4) {
      m3 = values4.length;
      if (n > 1 && !as) {
        error('Multi-field lookup requires explicit "as" parameter.');
      }
      if (as && as.length !== n * m3) {
        error('The "as" parameter has too few output field names.');
      }
      as = as || values4.map(accessorName);
      set7 = function(t) {
        for (var i = 0, k = 0, j, v2; i < n; ++i) {
          v2 = index4.get(keys4[i](t));
          if (v2 == null) for (j = 0; j < m3; ++j, ++k) t[as[k]] = defaultValue;
          else for (j = 0; j < m3; ++j, ++k) t[as[k]] = values4[j](v2);
        }
      };
    } else {
      if (!as) {
        error("Missing output field names.");
      }
      set7 = function(t) {
        for (var i = 0, v2; i < n; ++i) {
          v2 = index4.get(keys4[i](t));
          t[as[i]] = v2 == null ? defaultValue : v2;
        }
      };
    }
    if (reset3) {
      out = pulse2.reflow(true);
    } else {
      mods = keys4.some((k) => pulse2.modified(k.fields));
      flag2 |= mods ? pulse2.MOD : 0;
    }
    pulse2.visit(flag2, set7);
    return out.modifies(as);
  }
});
function MultiExtent(params2) {
  Operator.call(this, null, update$1, params2);
}
inherits_default(MultiExtent, Operator);
function update$1(_) {
  if (this.value && !_.modified()) {
    return this.value;
  }
  const ext = _.extents, n = ext.length;
  let min4 = Infinity, max4 = -Infinity, i, e;
  for (i = 0; i < n; ++i) {
    e = ext[i];
    if (e[0] < min4) min4 = e[0];
    if (e[1] > max4) max4 = e[1];
  }
  return [min4, max4];
}
function MultiValues(params2) {
  Operator.call(this, null, update2, params2);
}
inherits_default(MultiValues, Operator);
function update2(_) {
  return this.value && !_.modified() ? this.value : _.values.reduce((data4, _2) => data4.concat(_2), []);
}
function Params(params2) {
  Transform.call(this, null, params2);
}
inherits_default(Params, Transform, {
  transform(_, pulse2) {
    this.modified(_.modified());
    this.value = _;
    return pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS);
  }
});
function Pivot(params2) {
  Aggregate.call(this, params2);
}
Pivot.Definition = {
  "type": "Pivot",
  "metadata": {
    "generates": true,
    "changes": true
  },
  "params": [{
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "field",
    "type": "field",
    "required": true
  }, {
    "name": "value",
    "type": "field",
    "required": true
  }, {
    "name": "op",
    "type": "enum",
    "values": ValidAggregateOps,
    "default": "sum"
  }, {
    "name": "limit",
    "type": "number",
    "default": 0
  }, {
    "name": "key",
    "type": "field"
  }]
};
inherits_default(Pivot, Aggregate, {
  _transform: Aggregate.prototype.transform,
  transform(_, pulse2) {
    return this._transform(aggregateParams(_, pulse2), pulse2);
  }
});
function aggregateParams(_, pulse2) {
  const key2 = _.field, value4 = _.value, op = (_.op === "count" ? "__count__" : _.op) || "sum", fields = accessorFields(key2).concat(accessorFields(value4)), keys4 = pivotKeys(key2, _.limit || 0, pulse2);
  if (pulse2.changed()) _.set("__pivot__", null, null, true);
  return {
    key: _.key,
    groupby: _.groupby,
    ops: keys4.map(() => op),
    fields: keys4.map((k) => get(k, key2, value4, fields)),
    as: keys4.map((k) => k + ""),
    modified: _.modified.bind(_)
  };
}
function get(k, key2, value4, fields) {
  return accessor((d) => key2(d) === k ? value4(d) : NaN, fields, k + "");
}
function pivotKeys(key2, limit, pulse2) {
  const map3 = {}, list = [];
  pulse2.visit(pulse2.SOURCE, (t) => {
    const k = key2(t);
    if (!map3[k]) {
      map3[k] = 1;
      list.push(k);
    }
  });
  list.sort(ascending);
  return limit ? list.slice(0, limit) : list;
}
function PreFacet(params2) {
  Facet.call(this, params2);
}
inherits_default(PreFacet, Facet, {
  transform(_, pulse2) {
    const flow = _.subflow, field3 = _.field, subflow = (t) => this.subflow(tupleid(t), flow, pulse2, t);
    if (_.modified("field") || field3 && pulse2.modified(accessorFields(field3))) {
      error("PreFacet does not support field modification.");
    }
    this.initTargets();
    if (field3) {
      pulse2.visit(pulse2.MOD, (t) => {
        const sf = subflow(t);
        field3(t).forEach((_2) => sf.mod(_2));
      });
      pulse2.visit(pulse2.ADD, (t) => {
        const sf = subflow(t);
        field3(t).forEach((_2) => sf.add(ingest$1(_2)));
      });
      pulse2.visit(pulse2.REM, (t) => {
        const sf = subflow(t);
        field3(t).forEach((_2) => sf.rem(_2));
      });
    } else {
      pulse2.visit(pulse2.MOD, (t) => subflow(t).mod(t));
      pulse2.visit(pulse2.ADD, (t) => subflow(t).add(t));
      pulse2.visit(pulse2.REM, (t) => subflow(t).rem(t));
    }
    if (pulse2.clean()) {
      pulse2.runAfter(() => this.clean());
    }
    return pulse2;
  }
});
function Project(params2) {
  Transform.call(this, null, params2);
}
Project.Definition = {
  "type": "Project",
  "metadata": {
    "generates": true,
    "changes": true
  },
  "params": [{
    "name": "fields",
    "type": "field",
    "array": true
  }, {
    "name": "as",
    "type": "string",
    "null": true,
    "array": true
  }]
};
inherits_default(Project, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.NO_SOURCE), fields = _.fields, as = fieldNames(_.fields, _.as || []), derive2 = fields ? (s, t) => project(s, t, fields, as) : rederive;
    let lut;
    if (this.value) {
      lut = this.value;
    } else {
      pulse2 = pulse2.addAll();
      lut = this.value = {};
    }
    pulse2.visit(pulse2.REM, (t) => {
      const id2 = tupleid(t);
      out.rem.push(lut[id2]);
      lut[id2] = null;
    });
    pulse2.visit(pulse2.ADD, (t) => {
      const dt = derive2(t, ingest$1({}));
      lut[tupleid(t)] = dt;
      out.add.push(dt);
    });
    pulse2.visit(pulse2.MOD, (t) => {
      out.mod.push(derive2(t, lut[tupleid(t)]));
    });
    return out;
  }
});
function project(s, t, fields, as) {
  for (let i = 0, n = fields.length; i < n; ++i) {
    t[as[i]] = fields[i](s);
  }
  return t;
}
function Proxy2(params2) {
  Transform.call(this, null, params2);
}
inherits_default(Proxy2, Transform, {
  transform(_, pulse2) {
    this.value = _.value;
    return _.modified("value") ? pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS) : pulse2.StopPropagation;
  }
});
function Quantile(params2) {
  Transform.call(this, null, params2);
}
Quantile.Definition = {
  "type": "Quantile",
  "metadata": {
    "generates": true,
    "changes": true
  },
  "params": [{
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "field",
    "type": "field",
    "required": true
  }, {
    "name": "probs",
    "type": "number",
    "array": true
  }, {
    "name": "step",
    "type": "number",
    "default": 0.01
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "default": ["prob", "value"]
  }]
};
var EPSILON = 1e-14;
inherits_default(Quantile, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS), as = _.as || ["prob", "value"];
    if (this.value && !_.modified() && !pulse2.changed()) {
      out.source = this.value;
      return out;
    }
    const source4 = pulse2.materialize(pulse2.SOURCE).source, groups = partition$1(source4, _.groupby, _.field), names = (_.groupby || []).map(accessorName), values4 = [], step = _.step || 0.01, p = _.probs || range(step / 2, 1 - EPSILON, step), n = p.length;
    groups.forEach((g) => {
      const q = quantiles(g, p);
      for (let i = 0; i < n; ++i) {
        const t = {};
        for (let i2 = 0; i2 < names.length; ++i2) {
          t[names[i2]] = g.dims[i2];
        }
        t[as[0]] = p[i];
        t[as[1]] = q[i];
        values4.push(ingest$1(t));
      }
    });
    if (this.value) out.rem = this.value;
    this.value = out.add = out.source = values4;
    return out;
  }
});
function Relay(params2) {
  Transform.call(this, null, params2);
}
inherits_default(Relay, Transform, {
  transform(_, pulse2) {
    let out, lut;
    if (this.value) {
      lut = this.value;
    } else {
      out = pulse2 = pulse2.addAll();
      lut = this.value = {};
    }
    if (_.derive) {
      out = pulse2.fork(pulse2.NO_SOURCE);
      pulse2.visit(pulse2.REM, (t) => {
        const id2 = tupleid(t);
        out.rem.push(lut[id2]);
        lut[id2] = null;
      });
      pulse2.visit(pulse2.ADD, (t) => {
        const dt = derive(t);
        lut[tupleid(t)] = dt;
        out.add.push(dt);
      });
      pulse2.visit(pulse2.MOD, (t) => {
        const dt = lut[tupleid(t)];
        for (const k in t) {
          dt[k] = t[k];
          out.modifies(k);
        }
        out.mod.push(dt);
      });
    }
    return out;
  }
});
function Sample(params2) {
  Transform.call(this, [], params2);
  this.count = 0;
}
Sample.Definition = {
  "type": "Sample",
  "metadata": {},
  "params": [{
    "name": "size",
    "type": "number",
    "default": 1e3
  }]
};
inherits_default(Sample, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.NO_SOURCE), mod = _.modified("size"), num = _.size, map3 = this.value.reduce((m3, t) => (m3[tupleid(t)] = 1, m3), {});
    let res = this.value, cnt = this.count, cap = 0;
    function update3(t) {
      let p, idx;
      if (res.length < num) {
        res.push(t);
      } else {
        idx = ~~((cnt + 1) * random());
        if (idx < res.length && idx >= cap) {
          p = res[idx];
          if (map3[tupleid(p)]) out.rem.push(p);
          res[idx] = t;
        }
      }
      ++cnt;
    }
    if (pulse2.rem.length) {
      pulse2.visit(pulse2.REM, (t) => {
        const id2 = tupleid(t);
        if (map3[id2]) {
          map3[id2] = -1;
          out.rem.push(t);
        }
        --cnt;
      });
      res = res.filter((t) => map3[tupleid(t)] !== -1);
    }
    if ((pulse2.rem.length || mod) && res.length < num && pulse2.source) {
      cap = cnt = res.length;
      pulse2.visit(pulse2.SOURCE, (t) => {
        if (!map3[tupleid(t)]) update3(t);
      });
      cap = -1;
    }
    if (mod && res.length > num) {
      const n = res.length - num;
      for (let i = 0; i < n; ++i) {
        map3[tupleid(res[i])] = -1;
        out.rem.push(res[i]);
      }
      res = res.slice(n);
    }
    if (pulse2.mod.length) {
      pulse2.visit(pulse2.MOD, (t) => {
        if (map3[tupleid(t)]) out.mod.push(t);
      });
    }
    if (pulse2.add.length) {
      pulse2.visit(pulse2.ADD, update3);
    }
    if (pulse2.add.length || cap < 0) {
      out.add = res.filter((t) => !map3[tupleid(t)]);
    }
    this.count = cnt;
    this.value = out.source = res;
    return out;
  }
});
function Sequence(params2) {
  Transform.call(this, null, params2);
}
Sequence.Definition = {
  "type": "Sequence",
  "metadata": {
    "generates": true,
    "changes": true
  },
  "params": [{
    "name": "start",
    "type": "number",
    "required": true
  }, {
    "name": "stop",
    "type": "number",
    "required": true
  }, {
    "name": "step",
    "type": "number",
    "default": 1
  }, {
    "name": "as",
    "type": "string",
    "default": "data"
  }]
};
inherits_default(Sequence, Transform, {
  transform(_, pulse2) {
    if (this.value && !_.modified()) return;
    const out = pulse2.materialize().fork(pulse2.MOD), as = _.as || "data";
    out.rem = this.value ? pulse2.rem.concat(this.value) : pulse2.rem;
    this.value = range(_.start, _.stop, _.step || 1).map((v2) => {
      const t = {};
      t[as] = v2;
      return ingest$1(t);
    });
    out.add = pulse2.add.concat(this.value);
    return out;
  }
});
function Sieve(params2) {
  Transform.call(this, null, params2);
  this.modified(true);
}
inherits_default(Sieve, Transform, {
  transform(_, pulse2) {
    this.value = pulse2.source;
    return pulse2.changed() ? pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS) : pulse2.StopPropagation;
  }
});
function TimeUnit(params2) {
  Transform.call(this, null, params2);
}
var OUTPUT = ["unit0", "unit1"];
TimeUnit.Definition = {
  "type": "TimeUnit",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "field",
    "type": "field",
    "required": true
  }, {
    "name": "interval",
    "type": "boolean",
    "default": true
  }, {
    "name": "units",
    "type": "enum",
    "values": TIME_UNITS,
    "array": true
  }, {
    "name": "step",
    "type": "number",
    "default": 1
  }, {
    "name": "maxbins",
    "type": "number",
    "default": 40
  }, {
    "name": "extent",
    "type": "date",
    "array": true
  }, {
    "name": "inferUnits",
    "type": "boolean",
    "default": false
  }, {
    "name": "timezone",
    "type": "enum",
    "default": "local",
    "values": ["local", "utc"]
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": 2,
    "default": OUTPUT
  }]
};
inherits_default(TimeUnit, Transform, {
  transform(_, pulse2) {
    const field3 = _.field, band2 = _.interval !== false, utc = _.timezone === "utc", floor2 = this._floor(_, pulse2), offset6 = (utc ? utcInterval : timeInterval2)(floor2.unit).offset, as = _.as || OUTPUT, u0 = as[0], u1 = as[1], step = floor2.step;
    let min4 = floor2.start || Infinity, max4 = floor2.stop || -Infinity, flag2 = pulse2.ADD;
    if (_.modified() || pulse2.changed(pulse2.REM) || pulse2.modified(accessorFields(field3))) {
      pulse2 = pulse2.reflow(true);
      flag2 = pulse2.SOURCE;
      min4 = Infinity;
      max4 = -Infinity;
    }
    pulse2.visit(flag2, (t) => {
      const v2 = field3(t);
      let a3, b10;
      if (v2 == null) {
        t[u0] = null;
        if (band2) t[u1] = null;
      } else {
        t[u0] = a3 = b10 = floor2(v2);
        if (band2) t[u1] = b10 = offset6(a3, step);
        if (a3 < min4) min4 = a3;
        if (b10 > max4) max4 = b10;
      }
    });
    floor2.start = min4;
    floor2.stop = max4;
    return pulse2.modifies(band2 ? as : u0);
  },
  _floor(_, pulse2) {
    const utc = _.timezone === "utc";
    if (_.inferUnits && (_.units || _.step != null || _.maxbins != null || _.extent)) {
      pulse2.dataflow.warn("TimeUnit inferUnits overrides units, step, maxbins and extent.");
    }
    const {
      units,
      step
    } = _.inferUnits ? detectTimeUnits(pulse2.materialize(pulse2.SOURCE).source, _.field, utc) : _.units ? {
      units: _.units,
      step: _.step || 1
    } : bin({
      extent: _.extent || extent(pulse2.materialize(pulse2.SOURCE).source, _.field),
      maxbins: _.maxbins
    });
    const tunits = timeUnits(units), prev = this.value || {}, floor2 = (utc ? utcFloor : timeFloor)(tunits, step);
    floor2.unit = peek(tunits);
    floor2.units = tunits;
    floor2.step = step;
    floor2.start = prev.start;
    floor2.stop = prev.stop;
    return this.value = floor2;
  }
});
function TupleIndex(params2) {
  Transform.call(this, fastmap(), params2);
}
inherits_default(TupleIndex, Transform, {
  transform(_, pulse2) {
    const df = pulse2.dataflow, field3 = _.field, index4 = this.value, set7 = (t) => index4.set(field3(t), t);
    let mod = true;
    if (_.modified("field") || pulse2.modified(field3.fields)) {
      index4.clear();
      pulse2.visit(pulse2.SOURCE, set7);
    } else if (pulse2.changed()) {
      pulse2.visit(pulse2.REM, (t) => index4.delete(field3(t)));
      pulse2.visit(pulse2.ADD, set7);
    } else {
      mod = false;
    }
    this.modified(mod);
    if (index4.empty > df.cleanThreshold) df.runAfter(index4.clean);
    return pulse2.fork();
  }
});
function Values(params2) {
  Transform.call(this, null, params2);
}
inherits_default(Values, Transform, {
  transform(_, pulse2) {
    const run2 = !this.value || _.modified("field") || _.modified("sort") || pulse2.changed() || _.sort && pulse2.modified(_.sort.fields);
    if (run2) {
      this.value = (_.sort ? pulse2.source.slice().sort(stableCompare(_.sort)) : pulse2.source).map(_.field);
    }
  }
});
function WindowOp(op, field3, param2, as) {
  const fn = WindowOps[op](field3, param2);
  return {
    init: fn.init || zero,
    update: function(w3, t) {
      t[as] = fn.next(w3);
    }
  };
}
var WindowOps = {
  row_number: function() {
    return {
      next: (w3) => w3.index + 1
    };
  },
  rank: function() {
    let rank2;
    return {
      init: () => rank2 = 1,
      next: (w3) => {
        const i = w3.index, data4 = w3.data;
        return i && w3.compare(data4[i - 1], data4[i]) ? rank2 = i + 1 : rank2;
      }
    };
  },
  dense_rank: function() {
    let drank;
    return {
      init: () => drank = 1,
      next: (w3) => {
        const i = w3.index, d = w3.data;
        return i && w3.compare(d[i - 1], d[i]) ? ++drank : drank;
      }
    };
  },
  percent_rank: function() {
    const rank2 = WindowOps.rank(), next = rank2.next;
    return {
      init: rank2.init,
      next: (w3) => (next(w3) - 1) / (w3.data.length - 1)
    };
  },
  cume_dist: function() {
    let cume;
    return {
      init: () => cume = 0,
      next: (w3) => {
        const d = w3.data, c3 = w3.compare;
        let i = w3.index;
        if (cume < i) {
          while (i + 1 < d.length && !c3(d[i], d[i + 1])) ++i;
          cume = i;
        }
        return (1 + cume) / d.length;
      }
    };
  },
  ntile: function(field3, num) {
    num = +num;
    if (!(num > 0)) error("ntile num must be greater than zero.");
    const cume = WindowOps.cume_dist(), next = cume.next;
    return {
      init: cume.init,
      next: (w3) => Math.ceil(num * next(w3))
    };
  },
  lag: function(field3, offset6) {
    offset6 = +offset6 || 1;
    return {
      next: (w3) => {
        const i = w3.index - offset6;
        return i >= 0 ? field3(w3.data[i]) : null;
      }
    };
  },
  lead: function(field3, offset6) {
    offset6 = +offset6 || 1;
    return {
      next: (w3) => {
        const i = w3.index + offset6, d = w3.data;
        return i < d.length ? field3(d[i]) : null;
      }
    };
  },
  first_value: function(field3) {
    return {
      next: (w3) => field3(w3.data[w3.i0])
    };
  },
  last_value: function(field3) {
    return {
      next: (w3) => field3(w3.data[w3.i1 - 1])
    };
  },
  nth_value: function(field3, nth) {
    nth = +nth;
    if (!(nth > 0)) error("nth_value nth must be greater than zero.");
    return {
      next: (w3) => {
        const i = w3.i0 + (nth - 1);
        return i < w3.i1 ? field3(w3.data[i]) : null;
      }
    };
  },
  prev_value: function(field3) {
    let prev;
    return {
      init: () => prev = null,
      next: (w3) => {
        const v2 = field3(w3.data[w3.index]);
        return v2 != null ? prev = v2 : prev;
      }
    };
  },
  next_value: function(field3) {
    let v2, i;
    return {
      init: () => (v2 = null, i = -1),
      next: (w3) => {
        const d = w3.data;
        return w3.index <= i ? v2 : (i = find(field3, d, w3.index)) < 0 ? (i = d.length, v2 = null) : v2 = field3(d[i]);
      }
    };
  }
};
function find(field3, data4, index4) {
  for (let n = data4.length; index4 < n; ++index4) {
    const v2 = field3(data4[index4]);
    if (v2 != null) return index4;
  }
  return -1;
}
var ValidWindowOps = Object.keys(WindowOps);
function WindowState(_) {
  const ops2 = array_default(_.ops), fields = array_default(_.fields), params2 = array_default(_.params), aggregate_params = array_default(_.aggregate_params), as = array_default(_.as), outputs = this.outputs = [], windows = this.windows = [], inputs = {}, map3 = {}, counts = [], measures = [];
  let countOnly = true;
  function visitInputs(f) {
    array_default(accessorFields(f)).forEach((_2) => inputs[_2] = 1);
  }
  visitInputs(_.sort);
  ops2.forEach((op, i) => {
    const field3 = fields[i], param2 = params2[i], aggregate_param = aggregate_params[i] || null, mname = accessorName(field3), name = measureName(op, mname, as[i]);
    visitInputs(field3);
    outputs.push(name);
    if (hasOwnProperty(WindowOps, op)) {
      windows.push(WindowOp(op, field3, param2, name));
    } else {
      if (field3 == null && op !== "count") {
        error("Null aggregate field specified.");
      }
      if (op === "count") {
        counts.push(name);
        return;
      }
      countOnly = false;
      let m3 = map3[mname];
      if (!m3) {
        m3 = map3[mname] = [];
        m3.field = field3;
        measures.push(m3);
      }
      m3.push(createMeasure(op, aggregate_param, name));
    }
  });
  if (counts.length || measures.length) {
    this.cell = cell(measures, counts, countOnly);
  }
  this.inputs = Object.keys(inputs);
}
var prototype = WindowState.prototype;
prototype.init = function() {
  this.windows.forEach((_) => _.init());
  if (this.cell) this.cell.init();
};
prototype.update = function(w3, t) {
  const cell2 = this.cell, wind = this.windows, data4 = w3.data, m3 = wind && wind.length;
  let j;
  if (cell2) {
    for (j = w3.p0; j < w3.i0; ++j) cell2.rem(data4[j]);
    for (j = w3.p1; j < w3.i1; ++j) cell2.add(data4[j]);
    cell2.set(t);
  }
  for (j = 0; j < m3; ++j) wind[j].update(w3, t);
};
function cell(measures, counts, countOnly) {
  measures = measures.map((m3) => compileMeasures(m3, m3.field));
  const cell2 = {
    num: 0,
    agg: null,
    store: false,
    count: counts
  };
  if (!countOnly) {
    var n = measures.length, a3 = cell2.agg = Array(n), i = 0;
    for (; i < n; ++i) a3[i] = new measures[i](cell2);
  }
  if (cell2.store) {
    var store = cell2.data = new TupleStore();
  }
  cell2.add = function(t) {
    cell2.num += 1;
    if (countOnly) return;
    if (store) store.add(t);
    for (let i2 = 0; i2 < n; ++i2) {
      a3[i2].add(a3[i2].get(t), t);
    }
  };
  cell2.rem = function(t) {
    cell2.num -= 1;
    if (countOnly) return;
    if (store) store.rem(t);
    for (let i2 = 0; i2 < n; ++i2) {
      a3[i2].rem(a3[i2].get(t), t);
    }
  };
  cell2.set = function(t) {
    let i2, n2;
    if (store) store.values();
    for (i2 = 0, n2 = counts.length; i2 < n2; ++i2) t[counts[i2]] = cell2.num;
    if (!countOnly) for (i2 = 0, n2 = a3.length; i2 < n2; ++i2) a3[i2].set(t);
  };
  cell2.init = function() {
    cell2.num = 0;
    if (store) store.reset();
    for (let i2 = 0; i2 < n; ++i2) a3[i2].init();
  };
  return cell2;
}
function Window(params2) {
  Transform.call(this, {}, params2);
  this._mlen = 0;
  this._mods = [];
}
Window.Definition = {
  "type": "Window",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "sort",
    "type": "compare"
  }, {
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "ops",
    "type": "enum",
    "array": true,
    "values": ValidWindowOps.concat(ValidAggregateOps)
  }, {
    "name": "params",
    "type": "number",
    "null": true,
    "array": true
  }, {
    "name": "aggregate_params",
    "type": "number",
    "null": true,
    "array": true
  }, {
    "name": "fields",
    "type": "field",
    "null": true,
    "array": true
  }, {
    "name": "as",
    "type": "string",
    "null": true,
    "array": true
  }, {
    "name": "frame",
    "type": "number",
    "null": true,
    "array": true,
    "length": 2,
    "default": [null, 0]
  }, {
    "name": "ignorePeers",
    "type": "boolean",
    "default": false
  }]
};
inherits_default(Window, Transform, {
  transform(_, pulse2) {
    this.stamp = pulse2.stamp;
    const mod = _.modified(), cmp = stableCompare(_.sort), key2 = groupkey(_.groupby), group3 = (t) => this.group(key2(t));
    let state = this.state;
    if (!state || mod) {
      state = this.state = new WindowState(_);
    }
    if (mod || pulse2.modified(state.inputs)) {
      this.value = {};
      pulse2.visit(pulse2.SOURCE, (t) => group3(t).add(t));
    } else {
      pulse2.visit(pulse2.REM, (t) => group3(t).remove(t));
      pulse2.visit(pulse2.ADD, (t) => group3(t).add(t));
    }
    for (let i = 0, n = this._mlen; i < n; ++i) {
      processPartition(this._mods[i], state, cmp, _);
    }
    this._mlen = 0;
    this._mods = [];
    return pulse2.reflow(mod).modifies(state.outputs);
  },
  group(key2) {
    let group3 = this.value[key2];
    if (!group3) {
      group3 = this.value[key2] = SortedList(tupleid);
      group3.stamp = -1;
    }
    if (group3.stamp < this.stamp) {
      group3.stamp = this.stamp;
      this._mods[this._mlen++] = group3;
    }
    return group3;
  }
});
function processPartition(list, state, cmp, _) {
  const sort4 = _.sort, range7 = sort4 && !_.ignorePeers, frame2 = _.frame || [null, 0], data4 = list.data(cmp), n = data4.length, b10 = range7 ? bisector(sort4) : null, w3 = {
    i0: 0,
    i1: 0,
    p0: 0,
    p1: 0,
    index: 0,
    data: data4,
    compare: sort4 || constant_default(-1)
  };
  state.init();
  for (let i = 0; i < n; ++i) {
    setWindow(w3, frame2, i, n);
    if (range7) adjustRange(w3, b10);
    state.update(w3, data4[i]);
  }
}
function clamp(x6, lo, hi) {
  return x6 < lo ? lo : x6 > hi ? hi : x6;
}
function setWindow(w3, f, i, n) {
  w3.p0 = w3.i0;
  w3.p1 = w3.i1;
  const start = f[0] == null ? 0 : i + f[0];
  const endExclusive = f[1] == null ? n : i + f[1] + 1;
  w3.i0 = clamp(start, 0, n);
  w3.i1 = clamp(endExclusive, 0, n);
  w3.index = i;
}
function adjustRange(w3, bisect2) {
  const r0 = w3.i0, r1 = w3.i1 - 1, c3 = w3.compare, d = w3.data, n = d.length - 1;
  if (r0 > 0 && !c3(d[r0], d[r0 - 1])) w3.i0 = bisect2.left(d, d[r0]);
  if (r1 < n && !c3(d[r1], d[r1 + 1])) w3.i1 = bisect2.right(d, d[r1]);
}

// ../deepseek-harness/node_modules/.pnpm/vega-view-transforms@5.2.2/node_modules/vega-view-transforms/build/vega-view-transforms.js
var vega_view_transforms_exports = {};
__export(vega_view_transforms_exports, {
  bound: () => Bound,
  identifier: () => Identifier,
  mark: () => Mark,
  overlap: () => Overlap,
  render: () => Render,
  viewlayout: () => ViewLayout
});

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/constant.js
function constant_default2(x6) {
  return function constant2() {
    return x6;
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/math.js
var abs = Math.abs;
var atan2 = Math.atan2;
var cos = Math.cos;
var max2 = Math.max;
var min2 = Math.min;
var sin = Math.sin;
var sqrt = Math.sqrt;
var epsilon2 = 1e-12;
var pi = Math.PI;
var halfPi = pi / 2;
var tau = 2 * pi;
function acos(x6) {
  return x6 > 1 ? 0 : x6 < -1 ? pi : Math.acos(x6);
}
function asin(x6) {
  return x6 >= 1 ? halfPi : x6 <= -1 ? -halfPi : Math.asin(x6);
}

// ../deepseek-harness/node_modules/.pnpm/d3-path@3.1.0/node_modules/d3-path/src/path.js
var pi2 = Math.PI;
var tau2 = 2 * pi2;
var epsilon3 = 1e-6;
var tauEpsilon = tau2 - epsilon3;
function append(strings) {
  this._ += strings[0];
  for (let i = 1, n = strings.length; i < n; ++i) {
    this._ += arguments[i] + strings[i];
  }
}
function appendRound(digits) {
  let d = Math.floor(digits);
  if (!(d >= 0)) throw new Error(`invalid digits: ${digits}`);
  if (d > 15) return append;
  const k = 10 ** d;
  return function(strings) {
    this._ += strings[0];
    for (let i = 1, n = strings.length; i < n; ++i) {
      this._ += Math.round(arguments[i] * k) / k + strings[i];
    }
  };
}
var Path = class {
  constructor(digits) {
    this._x0 = this._y0 = // start of current subpath
    this._x1 = this._y1 = null;
    this._ = "";
    this._append = digits == null ? append : appendRound(digits);
  }
  moveTo(x6, y6) {
    this._append`M${this._x0 = this._x1 = +x6},${this._y0 = this._y1 = +y6}`;
  }
  closePath() {
    if (this._x1 !== null) {
      this._x1 = this._x0, this._y1 = this._y0;
      this._append`Z`;
    }
  }
  lineTo(x6, y6) {
    this._append`L${this._x1 = +x6},${this._y1 = +y6}`;
  }
  quadraticCurveTo(x12, y12, x6, y6) {
    this._append`Q${+x12},${+y12},${this._x1 = +x6},${this._y1 = +y6}`;
  }
  bezierCurveTo(x12, y12, x22, y22, x6, y6) {
    this._append`C${+x12},${+y12},${+x22},${+y22},${this._x1 = +x6},${this._y1 = +y6}`;
  }
  arcTo(x12, y12, x22, y22, r) {
    x12 = +x12, y12 = +y12, x22 = +x22, y22 = +y22, r = +r;
    if (r < 0) throw new Error(`negative radius: ${r}`);
    let x06 = this._x1, y06 = this._y1, x21 = x22 - x12, y21 = y22 - y12, x01 = x06 - x12, y01 = y06 - y12, l01_2 = x01 * x01 + y01 * y01;
    if (this._x1 === null) {
      this._append`M${this._x1 = x12},${this._y1 = y12}`;
    } else if (!(l01_2 > epsilon3)) ;
    else if (!(Math.abs(y01 * x21 - y21 * x01) > epsilon3) || !r) {
      this._append`L${this._x1 = x12},${this._y1 = y12}`;
    } else {
      let x20 = x22 - x06, y20 = y22 - y06, l21_2 = x21 * x21 + y21 * y21, l20_2 = x20 * x20 + y20 * y20, l21 = Math.sqrt(l21_2), l01 = Math.sqrt(l01_2), l = r * Math.tan((pi2 - Math.acos((l21_2 + l01_2 - l20_2) / (2 * l21 * l01))) / 2), t01 = l / l01, t21 = l / l21;
      if (Math.abs(t01 - 1) > epsilon3) {
        this._append`L${x12 + t01 * x01},${y12 + t01 * y01}`;
      }
      this._append`A${r},${r},0,0,${+(y01 * x20 > x01 * y20)},${this._x1 = x12 + t21 * x21},${this._y1 = y12 + t21 * y21}`;
    }
  }
  arc(x6, y6, r, a0, a1, ccw) {
    x6 = +x6, y6 = +y6, r = +r, ccw = !!ccw;
    if (r < 0) throw new Error(`negative radius: ${r}`);
    let dx = r * Math.cos(a0), dy = r * Math.sin(a0), x06 = x6 + dx, y06 = y6 + dy, cw2 = 1 ^ ccw, da2 = ccw ? a0 - a1 : a1 - a0;
    if (this._x1 === null) {
      this._append`M${x06},${y06}`;
    } else if (Math.abs(this._x1 - x06) > epsilon3 || Math.abs(this._y1 - y06) > epsilon3) {
      this._append`L${x06},${y06}`;
    }
    if (!r) return;
    if (da2 < 0) da2 = da2 % tau2 + tau2;
    if (da2 > tauEpsilon) {
      this._append`A${r},${r},0,1,${cw2},${x6 - dx},${y6 - dy}A${r},${r},0,1,${cw2},${this._x1 = x06},${this._y1 = y06}`;
    } else if (da2 > epsilon3) {
      this._append`A${r},${r},0,${+(da2 >= pi2)},${cw2},${this._x1 = x6 + r * Math.cos(a1)},${this._y1 = y6 + r * Math.sin(a1)}`;
    }
  }
  rect(x6, y6, w3, h3) {
    this._append`M${this._x0 = this._x1 = +x6},${this._y0 = this._y1 = +y6}h${w3 = +w3}v${+h3}h${-w3}Z`;
  }
  toString() {
    return this._;
  }
};
function path() {
  return new Path();
}
path.prototype = Path.prototype;

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/path.js
function withPath(shape3) {
  let digits = 3;
  shape3.digits = function(_) {
    if (!arguments.length) return digits;
    if (_ == null) {
      digits = null;
    } else {
      const d = Math.floor(_);
      if (!(d >= 0)) throw new RangeError(`invalid digits: ${_}`);
      digits = d;
    }
    return shape3;
  };
  return () => new Path(digits);
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/arc.js
function arcInnerRadius(d) {
  return d.innerRadius;
}
function arcOuterRadius(d) {
  return d.outerRadius;
}
function arcStartAngle(d) {
  return d.startAngle;
}
function arcEndAngle(d) {
  return d.endAngle;
}
function arcPadAngle(d) {
  return d && d.padAngle;
}
function intersect(x06, y06, x12, y12, x22, y22, x32, y32) {
  var x10 = x12 - x06, y10 = y12 - y06, x322 = x32 - x22, y322 = y32 - y22, t = y322 * x10 - x322 * y10;
  if (t * t < epsilon2) return;
  t = (x322 * (y06 - y22) - y322 * (x06 - x22)) / t;
  return [x06 + t * x10, y06 + t * y10];
}
function cornerTangents(x06, y06, x12, y12, r1, rc, cw2) {
  var x01 = x06 - x12, y01 = y06 - y12, lo = (cw2 ? rc : -rc) / sqrt(x01 * x01 + y01 * y01), ox = lo * y01, oy = -lo * x01, x11 = x06 + ox, y11 = y06 + oy, x10 = x12 + ox, y10 = y12 + oy, x004 = (x11 + x10) / 2, y004 = (y11 + y10) / 2, dx = x10 - x11, dy = y10 - y11, d2 = dx * dx + dy * dy, r = r1 - rc, D3 = x11 * y10 - x10 * y11, d = (dy < 0 ? -1 : 1) * sqrt(max2(0, r * r * d2 - D3 * D3)), cx0 = (D3 * dy - dx * d) / d2, cy0 = (-D3 * dx - dy * d) / d2, cx1 = (D3 * dy + dx * d) / d2, cy1 = (-D3 * dx + dy * d) / d2, dx0 = cx0 - x004, dy0 = cy0 - y004, dx1 = cx1 - x004, dy1 = cy1 - y004;
  if (dx0 * dx0 + dy0 * dy0 > dx1 * dx1 + dy1 * dy1) cx0 = cx1, cy0 = cy1;
  return {
    cx: cx0,
    cy: cy0,
    x01: -ox,
    y01: -oy,
    x11: cx0 * (r1 / r - 1),
    y11: cy0 * (r1 / r - 1)
  };
}
function arc_default() {
  var innerRadius = arcInnerRadius, outerRadius = arcOuterRadius, cornerRadius = constant_default2(0), padRadius = null, startAngle = arcStartAngle, endAngle = arcEndAngle, padAngle = arcPadAngle, context4 = null, path4 = withPath(arc5);
  function arc5() {
    var buffer, r, r0 = +innerRadius.apply(this, arguments), r1 = +outerRadius.apply(this, arguments), a0 = startAngle.apply(this, arguments) - halfPi, a1 = endAngle.apply(this, arguments) - halfPi, da2 = abs(a1 - a0), cw2 = a1 > a0;
    if (!context4) context4 = buffer = path4();
    if (r1 < r0) r = r1, r1 = r0, r0 = r;
    if (!(r1 > epsilon2)) context4.moveTo(0, 0);
    else if (da2 > tau - epsilon2) {
      context4.moveTo(r1 * cos(a0), r1 * sin(a0));
      context4.arc(0, 0, r1, a0, a1, !cw2);
      if (r0 > epsilon2) {
        context4.moveTo(r0 * cos(a1), r0 * sin(a1));
        context4.arc(0, 0, r0, a1, a0, cw2);
      }
    } else {
      var a01 = a0, a11 = a1, a00 = a0, a10 = a1, da0 = da2, da1 = da2, ap = padAngle.apply(this, arguments) / 2, rp = ap > epsilon2 && (padRadius ? +padRadius.apply(this, arguments) : sqrt(r0 * r0 + r1 * r1)), rc = min2(abs(r1 - r0) / 2, +cornerRadius.apply(this, arguments)), rc0 = rc, rc1 = rc, t05, t13;
      if (rp > epsilon2) {
        var p02 = asin(rp / r0 * sin(ap)), p1 = asin(rp / r1 * sin(ap));
        if ((da0 -= p02 * 2) > epsilon2) p02 *= cw2 ? 1 : -1, a00 += p02, a10 -= p02;
        else da0 = 0, a00 = a10 = (a0 + a1) / 2;
        if ((da1 -= p1 * 2) > epsilon2) p1 *= cw2 ? 1 : -1, a01 += p1, a11 -= p1;
        else da1 = 0, a01 = a11 = (a0 + a1) / 2;
      }
      var x01 = r1 * cos(a01), y01 = r1 * sin(a01), x10 = r0 * cos(a10), y10 = r0 * sin(a10);
      if (rc > epsilon2) {
        var x11 = r1 * cos(a11), y11 = r1 * sin(a11), x004 = r0 * cos(a00), y004 = r0 * sin(a00), oc;
        if (da2 < pi) {
          if (oc = intersect(x01, y01, x004, y004, x11, y11, x10, y10)) {
            var ax = x01 - oc[0], ay = y01 - oc[1], bx = x11 - oc[0], by = y11 - oc[1], kc = 1 / sin(acos((ax * bx + ay * by) / (sqrt(ax * ax + ay * ay) * sqrt(bx * bx + by * by))) / 2), lc = sqrt(oc[0] * oc[0] + oc[1] * oc[1]);
            rc0 = min2(rc, (r0 - lc) / (kc - 1));
            rc1 = min2(rc, (r1 - lc) / (kc + 1));
          } else {
            rc0 = rc1 = 0;
          }
        }
      }
      if (!(da1 > epsilon2)) context4.moveTo(x01, y01);
      else if (rc1 > epsilon2) {
        t05 = cornerTangents(x004, y004, x01, y01, r1, rc1, cw2);
        t13 = cornerTangents(x11, y11, x10, y10, r1, rc1, cw2);
        context4.moveTo(t05.cx + t05.x01, t05.cy + t05.y01);
        if (rc1 < rc) context4.arc(t05.cx, t05.cy, rc1, atan2(t05.y01, t05.x01), atan2(t13.y01, t13.x01), !cw2);
        else {
          context4.arc(t05.cx, t05.cy, rc1, atan2(t05.y01, t05.x01), atan2(t05.y11, t05.x11), !cw2);
          context4.arc(0, 0, r1, atan2(t05.cy + t05.y11, t05.cx + t05.x11), atan2(t13.cy + t13.y11, t13.cx + t13.x11), !cw2);
          context4.arc(t13.cx, t13.cy, rc1, atan2(t13.y11, t13.x11), atan2(t13.y01, t13.x01), !cw2);
        }
      } else context4.moveTo(x01, y01), context4.arc(0, 0, r1, a01, a11, !cw2);
      if (!(r0 > epsilon2) || !(da0 > epsilon2)) context4.lineTo(x10, y10);
      else if (rc0 > epsilon2) {
        t05 = cornerTangents(x10, y10, x11, y11, r0, -rc0, cw2);
        t13 = cornerTangents(x01, y01, x004, y004, r0, -rc0, cw2);
        context4.lineTo(t05.cx + t05.x01, t05.cy + t05.y01);
        if (rc0 < rc) context4.arc(t05.cx, t05.cy, rc0, atan2(t05.y01, t05.x01), atan2(t13.y01, t13.x01), !cw2);
        else {
          context4.arc(t05.cx, t05.cy, rc0, atan2(t05.y01, t05.x01), atan2(t05.y11, t05.x11), !cw2);
          context4.arc(0, 0, r0, atan2(t05.cy + t05.y11, t05.cx + t05.x11), atan2(t13.cy + t13.y11, t13.cx + t13.x11), cw2);
          context4.arc(t13.cx, t13.cy, rc0, atan2(t13.y11, t13.x11), atan2(t13.y01, t13.x01), !cw2);
        }
      } else context4.arc(0, 0, r0, a10, a00, cw2);
    }
    context4.closePath();
    if (buffer) return context4 = null, buffer + "" || null;
  }
  arc5.centroid = function() {
    var r = (+innerRadius.apply(this, arguments) + +outerRadius.apply(this, arguments)) / 2, a3 = (+startAngle.apply(this, arguments) + +endAngle.apply(this, arguments)) / 2 - pi / 2;
    return [cos(a3) * r, sin(a3) * r];
  };
  arc5.innerRadius = function(_) {
    return arguments.length ? (innerRadius = typeof _ === "function" ? _ : constant_default2(+_), arc5) : innerRadius;
  };
  arc5.outerRadius = function(_) {
    return arguments.length ? (outerRadius = typeof _ === "function" ? _ : constant_default2(+_), arc5) : outerRadius;
  };
  arc5.cornerRadius = function(_) {
    return arguments.length ? (cornerRadius = typeof _ === "function" ? _ : constant_default2(+_), arc5) : cornerRadius;
  };
  arc5.padRadius = function(_) {
    return arguments.length ? (padRadius = _ == null ? null : typeof _ === "function" ? _ : constant_default2(+_), arc5) : padRadius;
  };
  arc5.startAngle = function(_) {
    return arguments.length ? (startAngle = typeof _ === "function" ? _ : constant_default2(+_), arc5) : startAngle;
  };
  arc5.endAngle = function(_) {
    return arguments.length ? (endAngle = typeof _ === "function" ? _ : constant_default2(+_), arc5) : endAngle;
  };
  arc5.padAngle = function(_) {
    return arguments.length ? (padAngle = typeof _ === "function" ? _ : constant_default2(+_), arc5) : padAngle;
  };
  arc5.context = function(_) {
    return arguments.length ? (context4 = _ == null ? null : _, arc5) : context4;
  };
  return arc5;
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/array.js
var slice = Array.prototype.slice;
function array_default2(x6) {
  return typeof x6 === "object" && "length" in x6 ? x6 : Array.from(x6);
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/linear.js
function Linear(context4) {
  this._context = context4;
}
Linear.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x6, y6) : this._context.moveTo(x6, y6);
        break;
      case 1:
        this._point = 2;
      // falls through
      default:
        this._context.lineTo(x6, y6);
        break;
    }
  }
};
function linear_default(context4) {
  return new Linear(context4);
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/point.js
function x(p) {
  return p[0];
}
function y(p) {
  return p[1];
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/line.js
function line_default(x6, y6) {
  var defined2 = constant_default2(true), context4 = null, curve2 = linear_default, output3 = null, path4 = withPath(line5);
  x6 = typeof x6 === "function" ? x6 : x6 === void 0 ? x : constant_default2(x6);
  y6 = typeof y6 === "function" ? y6 : y6 === void 0 ? y : constant_default2(y6);
  function line5(data4) {
    var i, n = (data4 = array_default2(data4)).length, d, defined0 = false, buffer;
    if (context4 == null) output3 = curve2(buffer = path4());
    for (i = 0; i <= n; ++i) {
      if (!(i < n && defined2(d = data4[i], i, data4)) === defined0) {
        if (defined0 = !defined0) output3.lineStart();
        else output3.lineEnd();
      }
      if (defined0) output3.point(+x6(d, i, data4), +y6(d, i, data4));
    }
    if (buffer) return output3 = null, buffer + "" || null;
  }
  line5.x = function(_) {
    return arguments.length ? (x6 = typeof _ === "function" ? _ : constant_default2(+_), line5) : x6;
  };
  line5.y = function(_) {
    return arguments.length ? (y6 = typeof _ === "function" ? _ : constant_default2(+_), line5) : y6;
  };
  line5.defined = function(_) {
    return arguments.length ? (defined2 = typeof _ === "function" ? _ : constant_default2(!!_), line5) : defined2;
  };
  line5.curve = function(_) {
    return arguments.length ? (curve2 = _, context4 != null && (output3 = curve2(context4)), line5) : curve2;
  };
  line5.context = function(_) {
    return arguments.length ? (_ == null ? context4 = output3 = null : output3 = curve2(context4 = _), line5) : context4;
  };
  return line5;
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/area.js
function area_default(x06, y06, y12) {
  var x12 = null, defined2 = constant_default2(true), context4 = null, curve2 = linear_default, output3 = null, path4 = withPath(area5);
  x06 = typeof x06 === "function" ? x06 : x06 === void 0 ? x : constant_default2(+x06);
  y06 = typeof y06 === "function" ? y06 : y06 === void 0 ? constant_default2(0) : constant_default2(+y06);
  y12 = typeof y12 === "function" ? y12 : y12 === void 0 ? y : constant_default2(+y12);
  function area5(data4) {
    var i, j, k, n = (data4 = array_default2(data4)).length, d, defined0 = false, buffer, x0z = new Array(n), y0z = new Array(n);
    if (context4 == null) output3 = curve2(buffer = path4());
    for (i = 0; i <= n; ++i) {
      if (!(i < n && defined2(d = data4[i], i, data4)) === defined0) {
        if (defined0 = !defined0) {
          j = i;
          output3.areaStart();
          output3.lineStart();
        } else {
          output3.lineEnd();
          output3.lineStart();
          for (k = i - 1; k >= j; --k) {
            output3.point(x0z[k], y0z[k]);
          }
          output3.lineEnd();
          output3.areaEnd();
        }
      }
      if (defined0) {
        x0z[i] = +x06(d, i, data4), y0z[i] = +y06(d, i, data4);
        output3.point(x12 ? +x12(d, i, data4) : x0z[i], y12 ? +y12(d, i, data4) : y0z[i]);
      }
    }
    if (buffer) return output3 = null, buffer + "" || null;
  }
  function arealine() {
    return line_default().defined(defined2).curve(curve2).context(context4);
  }
  area5.x = function(_) {
    return arguments.length ? (x06 = typeof _ === "function" ? _ : constant_default2(+_), x12 = null, area5) : x06;
  };
  area5.x0 = function(_) {
    return arguments.length ? (x06 = typeof _ === "function" ? _ : constant_default2(+_), area5) : x06;
  };
  area5.x1 = function(_) {
    return arguments.length ? (x12 = _ == null ? null : typeof _ === "function" ? _ : constant_default2(+_), area5) : x12;
  };
  area5.y = function(_) {
    return arguments.length ? (y06 = typeof _ === "function" ? _ : constant_default2(+_), y12 = null, area5) : y06;
  };
  area5.y0 = function(_) {
    return arguments.length ? (y06 = typeof _ === "function" ? _ : constant_default2(+_), area5) : y06;
  };
  area5.y1 = function(_) {
    return arguments.length ? (y12 = _ == null ? null : typeof _ === "function" ? _ : constant_default2(+_), area5) : y12;
  };
  area5.lineX0 = area5.lineY0 = function() {
    return arealine().x(x06).y(y06);
  };
  area5.lineY1 = function() {
    return arealine().x(x06).y(y12);
  };
  area5.lineX1 = function() {
    return arealine().x(x12).y(y06);
  };
  area5.defined = function(_) {
    return arguments.length ? (defined2 = typeof _ === "function" ? _ : constant_default2(!!_), area5) : defined2;
  };
  area5.curve = function(_) {
    return arguments.length ? (curve2 = _, context4 != null && (output3 = curve2(context4)), area5) : curve2;
  };
  area5.context = function(_) {
    return arguments.length ? (_ == null ? context4 = output3 = null : output3 = curve2(context4 = _), area5) : context4;
  };
  return area5;
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/symbol/circle.js
var circle_default = {
  draw(context4, size) {
    const r = sqrt(size / pi);
    context4.moveTo(r, 0);
    context4.arc(0, 0, r, 0, tau);
  }
};

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/symbol.js
function Symbol2(type4, size) {
  let context4 = null, path4 = withPath(symbol3);
  type4 = typeof type4 === "function" ? type4 : constant_default2(type4 || circle_default);
  size = typeof size === "function" ? size : constant_default2(size === void 0 ? 64 : +size);
  function symbol3() {
    let buffer;
    if (!context4) context4 = buffer = path4();
    type4.apply(this, arguments).draw(context4, +size.apply(this, arguments));
    if (buffer) return context4 = null, buffer + "" || null;
  }
  symbol3.type = function(_) {
    return arguments.length ? (type4 = typeof _ === "function" ? _ : constant_default2(_), symbol3) : type4;
  };
  symbol3.size = function(_) {
    return arguments.length ? (size = typeof _ === "function" ? _ : constant_default2(+_), symbol3) : size;
  };
  symbol3.context = function(_) {
    return arguments.length ? (context4 = _ == null ? null : _, symbol3) : context4;
  };
  return symbol3;
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/noop.js
function noop_default() {
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/basis.js
function point(that, x6, y6) {
  that._context.bezierCurveTo(
    (2 * that._x0 + that._x1) / 3,
    (2 * that._y0 + that._y1) / 3,
    (that._x0 + 2 * that._x1) / 3,
    (that._y0 + 2 * that._y1) / 3,
    (that._x0 + 4 * that._x1 + x6) / 6,
    (that._y0 + 4 * that._y1 + y6) / 6
  );
}
function Basis(context4) {
  this._context = context4;
}
Basis.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 3:
        point(this, this._x1, this._y1);
      // falls through
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x6, y6) : this._context.moveTo(x6, y6);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._context.lineTo((5 * this._x0 + this._x1) / 6, (5 * this._y0 + this._y1) / 6);
      // falls through
      default:
        point(this, x6, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = x6;
    this._y0 = this._y1, this._y1 = y6;
  }
};
function basis_default(context4) {
  return new Basis(context4);
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/basisClosed.js
function BasisClosed(context4) {
  this._context = context4;
}
BasisClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x2, this._y2);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.moveTo((this._x2 + 2 * this._x3) / 3, (this._y2 + 2 * this._y3) / 3);
        this._context.lineTo((this._x3 + 2 * this._x2) / 3, (this._y3 + 2 * this._y2) / 3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x2, this._y2);
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        break;
      }
    }
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x2 = x6, this._y2 = y6;
        break;
      case 1:
        this._point = 2;
        this._x3 = x6, this._y3 = y6;
        break;
      case 2:
        this._point = 3;
        this._x4 = x6, this._y4 = y6;
        this._context.moveTo((this._x0 + 4 * this._x1 + x6) / 6, (this._y0 + 4 * this._y1 + y6) / 6);
        break;
      default:
        point(this, x6, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = x6;
    this._y0 = this._y1, this._y1 = y6;
  }
};
function basisClosed_default(context4) {
  return new BasisClosed(context4);
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/basisOpen.js
function BasisOpen(context4) {
  this._context = context4;
}
BasisOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        var x06 = (this._x0 + 4 * this._x1 + x6) / 6, y06 = (this._y0 + 4 * this._y1 + y6) / 6;
        this._line ? this._context.lineTo(x06, y06) : this._context.moveTo(x06, y06);
        break;
      case 3:
        this._point = 4;
      // falls through
      default:
        point(this, x6, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = x6;
    this._y0 = this._y1, this._y1 = y6;
  }
};
function basisOpen_default(context4) {
  return new BasisOpen(context4);
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/bundle.js
function Bundle(context4, beta) {
  this._basis = new Basis(context4);
  this._beta = beta;
}
Bundle.prototype = {
  lineStart: function() {
    this._x = [];
    this._y = [];
    this._basis.lineStart();
  },
  lineEnd: function() {
    var x6 = this._x, y6 = this._y, j = x6.length - 1;
    if (j > 0) {
      var x06 = x6[0], y06 = y6[0], dx = x6[j] - x06, dy = y6[j] - y06, i = -1, t;
      while (++i <= j) {
        t = i / j;
        this._basis.point(
          this._beta * x6[i] + (1 - this._beta) * (x06 + t * dx),
          this._beta * y6[i] + (1 - this._beta) * (y06 + t * dy)
        );
      }
    }
    this._x = this._y = null;
    this._basis.lineEnd();
  },
  point: function(x6, y6) {
    this._x.push(+x6);
    this._y.push(+y6);
  }
};
var bundle_default = (function custom(beta) {
  function bundle3(context4) {
    return beta === 1 ? new Basis(context4) : new Bundle(context4, beta);
  }
  bundle3.beta = function(beta2) {
    return custom(+beta2);
  };
  return bundle3;
})(0.85);

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/cardinal.js
function point2(that, x6, y6) {
  that._context.bezierCurveTo(
    that._x1 + that._k * (that._x2 - that._x0),
    that._y1 + that._k * (that._y2 - that._y0),
    that._x2 + that._k * (that._x1 - x6),
    that._y2 + that._k * (that._y1 - y6),
    that._x2,
    that._y2
  );
}
function Cardinal(context4, tension) {
  this._context = context4;
  this._k = (1 - tension) / 6;
}
Cardinal.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x2, this._y2);
        break;
      case 3:
        point2(this, this._x1, this._y1);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x6, y6) : this._context.moveTo(x6, y6);
        break;
      case 1:
        this._point = 2;
        this._x1 = x6, this._y1 = y6;
        break;
      case 2:
        this._point = 3;
      // falls through
      default:
        point2(this, x6, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x6;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var cardinal_default = (function custom2(tension) {
  function cardinal(context4) {
    return new Cardinal(context4, tension);
  }
  cardinal.tension = function(tension2) {
    return custom2(+tension2);
  };
  return cardinal;
})(0);

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/cardinalClosed.js
function CardinalClosed(context4, tension) {
  this._context = context4;
  this._k = (1 - tension) / 6;
}
CardinalClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.lineTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        this.point(this._x5, this._y5);
        break;
      }
    }
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x3 = x6, this._y3 = y6;
        break;
      case 1:
        this._point = 2;
        this._context.moveTo(this._x4 = x6, this._y4 = y6);
        break;
      case 2:
        this._point = 3;
        this._x5 = x6, this._y5 = y6;
        break;
      default:
        point2(this, x6, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x6;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var cardinalClosed_default = (function custom3(tension) {
  function cardinal(context4) {
    return new CardinalClosed(context4, tension);
  }
  cardinal.tension = function(tension2) {
    return custom3(+tension2);
  };
  return cardinal;
})(0);

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/cardinalOpen.js
function CardinalOpen(context4, tension) {
  this._context = context4;
  this._k = (1 - tension) / 6;
}
CardinalOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2);
        break;
      case 3:
        this._point = 4;
      // falls through
      default:
        point2(this, x6, y6);
        break;
    }
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x6;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var cardinalOpen_default = (function custom4(tension) {
  function cardinal(context4) {
    return new CardinalOpen(context4, tension);
  }
  cardinal.tension = function(tension2) {
    return custom4(+tension2);
  };
  return cardinal;
})(0);

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/catmullRom.js
function point3(that, x6, y6) {
  var x12 = that._x1, y12 = that._y1, x22 = that._x2, y22 = that._y2;
  if (that._l01_a > epsilon2) {
    var a3 = 2 * that._l01_2a + 3 * that._l01_a * that._l12_a + that._l12_2a, n = 3 * that._l01_a * (that._l01_a + that._l12_a);
    x12 = (x12 * a3 - that._x0 * that._l12_2a + that._x2 * that._l01_2a) / n;
    y12 = (y12 * a3 - that._y0 * that._l12_2a + that._y2 * that._l01_2a) / n;
  }
  if (that._l23_a > epsilon2) {
    var b10 = 2 * that._l23_2a + 3 * that._l23_a * that._l12_a + that._l12_2a, m3 = 3 * that._l23_a * (that._l23_a + that._l12_a);
    x22 = (x22 * b10 + that._x1 * that._l23_2a - x6 * that._l12_2a) / m3;
    y22 = (y22 * b10 + that._y1 * that._l23_2a - y6 * that._l12_2a) / m3;
  }
  that._context.bezierCurveTo(x12, y12, x22, y22, that._x2, that._y2);
}
function CatmullRom(context4, alpha) {
  this._context = context4;
  this._alpha = alpha;
}
CatmullRom.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x2, this._y2);
        break;
      case 3:
        this.point(this._x2, this._y2);
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    if (this._point) {
      var x23 = this._x2 - x6, y23 = this._y2 - y6;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x6, y6) : this._context.moveTo(x6, y6);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
      // falls through
      default:
        point3(this, x6, y6);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x6;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var catmullRom_default = (function custom5(alpha) {
  function catmullRom(context4) {
    return alpha ? new CatmullRom(context4, alpha) : new Cardinal(context4, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom5(+alpha2);
  };
  return catmullRom;
})(0.5);

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/catmullRomClosed.js
function CatmullRomClosed(context4, alpha) {
  this._context = context4;
  this._alpha = alpha;
}
CatmullRomClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._x3 = this._x4 = this._x5 = this._y0 = this._y1 = this._y2 = this._y3 = this._y4 = this._y5 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 1: {
        this._context.moveTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 2: {
        this._context.lineTo(this._x3, this._y3);
        this._context.closePath();
        break;
      }
      case 3: {
        this.point(this._x3, this._y3);
        this.point(this._x4, this._y4);
        this.point(this._x5, this._y5);
        break;
      }
    }
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    if (this._point) {
      var x23 = this._x2 - x6, y23 = this._y2 - y6;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        this._x3 = x6, this._y3 = y6;
        break;
      case 1:
        this._point = 2;
        this._context.moveTo(this._x4 = x6, this._y4 = y6);
        break;
      case 2:
        this._point = 3;
        this._x5 = x6, this._y5 = y6;
        break;
      default:
        point3(this, x6, y6);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x6;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var catmullRomClosed_default = (function custom6(alpha) {
  function catmullRom(context4) {
    return alpha ? new CatmullRomClosed(context4, alpha) : new CardinalClosed(context4, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom6(+alpha2);
  };
  return catmullRom;
})(0.5);

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/catmullRomOpen.js
function CatmullRomOpen(context4, alpha) {
  this._context = context4;
  this._alpha = alpha;
}
CatmullRomOpen.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._x2 = this._y0 = this._y1 = this._y2 = NaN;
    this._l01_a = this._l12_a = this._l23_a = this._l01_2a = this._l12_2a = this._l23_2a = this._point = 0;
  },
  lineEnd: function() {
    if (this._line || this._line !== 0 && this._point === 3) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    if (this._point) {
      var x23 = this._x2 - x6, y23 = this._y2 - y6;
      this._l23_a = Math.sqrt(this._l23_2a = Math.pow(x23 * x23 + y23 * y23, this._alpha));
    }
    switch (this._point) {
      case 0:
        this._point = 1;
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        this._line ? this._context.lineTo(this._x2, this._y2) : this._context.moveTo(this._x2, this._y2);
        break;
      case 3:
        this._point = 4;
      // falls through
      default:
        point3(this, x6, y6);
        break;
    }
    this._l01_a = this._l12_a, this._l12_a = this._l23_a;
    this._l01_2a = this._l12_2a, this._l12_2a = this._l23_2a;
    this._x0 = this._x1, this._x1 = this._x2, this._x2 = x6;
    this._y0 = this._y1, this._y1 = this._y2, this._y2 = y6;
  }
};
var catmullRomOpen_default = (function custom7(alpha) {
  function catmullRom(context4) {
    return alpha ? new CatmullRomOpen(context4, alpha) : new CardinalOpen(context4, 0);
  }
  catmullRom.alpha = function(alpha2) {
    return custom7(+alpha2);
  };
  return catmullRom;
})(0.5);

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/linearClosed.js
function LinearClosed(context4) {
  this._context = context4;
}
LinearClosed.prototype = {
  areaStart: noop_default,
  areaEnd: noop_default,
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._point) this._context.closePath();
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    if (this._point) this._context.lineTo(x6, y6);
    else this._point = 1, this._context.moveTo(x6, y6);
  }
};
function linearClosed_default(context4) {
  return new LinearClosed(context4);
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/monotone.js
function sign(x6) {
  return x6 < 0 ? -1 : 1;
}
function slope3(that, x22, y22) {
  var h0 = that._x1 - that._x0, h1 = x22 - that._x1, s0 = (that._y1 - that._y0) / (h0 || h1 < 0 && -0), s1 = (y22 - that._y1) / (h1 || h0 < 0 && -0), p = (s0 * h1 + s1 * h0) / (h0 + h1);
  return (sign(s0) + sign(s1)) * Math.min(Math.abs(s0), Math.abs(s1), 0.5 * Math.abs(p)) || 0;
}
function slope2(that, t) {
  var h3 = that._x1 - that._x0;
  return h3 ? (3 * (that._y1 - that._y0) / h3 - t) / 2 : t;
}
function point4(that, t05, t13) {
  var x06 = that._x0, y06 = that._y0, x12 = that._x1, y12 = that._y1, dx = (x12 - x06) / 3;
  that._context.bezierCurveTo(x06 + dx, y06 + dx * t05, x12 - dx, y12 - dx * t13, x12, y12);
}
function MonotoneX(context4) {
  this._context = context4;
}
MonotoneX.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x0 = this._x1 = this._y0 = this._y1 = this._t0 = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    switch (this._point) {
      case 2:
        this._context.lineTo(this._x1, this._y1);
        break;
      case 3:
        point4(this, this._t0, slope2(this, this._t0));
        break;
    }
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    this._line = 1 - this._line;
  },
  point: function(x6, y6) {
    var t13 = NaN;
    x6 = +x6, y6 = +y6;
    if (x6 === this._x1 && y6 === this._y1) return;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x6, y6) : this._context.moveTo(x6, y6);
        break;
      case 1:
        this._point = 2;
        break;
      case 2:
        this._point = 3;
        point4(this, slope2(this, t13 = slope3(this, x6, y6)), t13);
        break;
      default:
        point4(this, this._t0, t13 = slope3(this, x6, y6));
        break;
    }
    this._x0 = this._x1, this._x1 = x6;
    this._y0 = this._y1, this._y1 = y6;
    this._t0 = t13;
  }
};
function MonotoneY(context4) {
  this._context = new ReflectContext(context4);
}
(MonotoneY.prototype = Object.create(MonotoneX.prototype)).point = function(x6, y6) {
  MonotoneX.prototype.point.call(this, y6, x6);
};
function ReflectContext(context4) {
  this._context = context4;
}
ReflectContext.prototype = {
  moveTo: function(x6, y6) {
    this._context.moveTo(y6, x6);
  },
  closePath: function() {
    this._context.closePath();
  },
  lineTo: function(x6, y6) {
    this._context.lineTo(y6, x6);
  },
  bezierCurveTo: function(x12, y12, x22, y22, x6, y6) {
    this._context.bezierCurveTo(y12, x12, y22, x22, y6, x6);
  }
};
function monotoneX(context4) {
  return new MonotoneX(context4);
}
function monotoneY(context4) {
  return new MonotoneY(context4);
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/natural.js
function Natural(context4) {
  this._context = context4;
}
Natural.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = [];
    this._y = [];
  },
  lineEnd: function() {
    var x6 = this._x, y6 = this._y, n = x6.length;
    if (n) {
      this._line ? this._context.lineTo(x6[0], y6[0]) : this._context.moveTo(x6[0], y6[0]);
      if (n === 2) {
        this._context.lineTo(x6[1], y6[1]);
      } else {
        var px3 = controlPoints(x6), py3 = controlPoints(y6);
        for (var i0 = 0, i1 = 1; i1 < n; ++i0, ++i1) {
          this._context.bezierCurveTo(px3[0][i0], py3[0][i0], px3[1][i0], py3[1][i0], x6[i1], y6[i1]);
        }
      }
    }
    if (this._line || this._line !== 0 && n === 1) this._context.closePath();
    this._line = 1 - this._line;
    this._x = this._y = null;
  },
  point: function(x6, y6) {
    this._x.push(+x6);
    this._y.push(+y6);
  }
};
function controlPoints(x6) {
  var i, n = x6.length - 1, m3, a3 = new Array(n), b10 = new Array(n), r = new Array(n);
  a3[0] = 0, b10[0] = 2, r[0] = x6[0] + 2 * x6[1];
  for (i = 1; i < n - 1; ++i) a3[i] = 1, b10[i] = 4, r[i] = 4 * x6[i] + 2 * x6[i + 1];
  a3[n - 1] = 2, b10[n - 1] = 7, r[n - 1] = 8 * x6[n - 1] + x6[n];
  for (i = 1; i < n; ++i) m3 = a3[i] / b10[i - 1], b10[i] -= m3, r[i] -= m3 * r[i - 1];
  a3[n - 1] = r[n - 1] / b10[n - 1];
  for (i = n - 2; i >= 0; --i) a3[i] = (r[i] - a3[i + 1]) / b10[i];
  b10[n - 1] = (x6[n] + a3[n - 1]) / 2;
  for (i = 0; i < n - 1; ++i) b10[i] = 2 * x6[i + 1] - a3[i + 1];
  return [a3, b10];
}
function natural_default(context4) {
  return new Natural(context4);
}

// ../deepseek-harness/node_modules/.pnpm/d3-shape@3.2.0/node_modules/d3-shape/src/curve/step.js
function Step(context4, t) {
  this._context = context4;
  this._t = t;
}
Step.prototype = {
  areaStart: function() {
    this._line = 0;
  },
  areaEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._x = this._y = NaN;
    this._point = 0;
  },
  lineEnd: function() {
    if (0 < this._t && this._t < 1 && this._point === 2) this._context.lineTo(this._x, this._y);
    if (this._line || this._line !== 0 && this._point === 1) this._context.closePath();
    if (this._line >= 0) this._t = 1 - this._t, this._line = 1 - this._line;
  },
  point: function(x6, y6) {
    x6 = +x6, y6 = +y6;
    switch (this._point) {
      case 0:
        this._point = 1;
        this._line ? this._context.lineTo(x6, y6) : this._context.moveTo(x6, y6);
        break;
      case 1:
        this._point = 2;
      // falls through
      default: {
        if (this._t <= 0) {
          this._context.lineTo(this._x, y6);
          this._context.lineTo(x6, y6);
        } else {
          var x12 = this._x * (1 - this._t) + x6 * this._t;
          this._context.lineTo(x12, this._y);
          this._context.lineTo(x12, y6);
        }
        break;
      }
    }
    this._x = x6, this._y = y6;
  }
};
function step_default(context4) {
  return new Step(context4, 0.5);
}
function stepBefore(context4) {
  return new Step(context4, 0);
}
function stepAfter(context4) {
  return new Step(context4, 1);
}

// ../deepseek-harness/node_modules/.pnpm/vega-canvas@2.0.0/node_modules/vega-canvas/build/vega-canvas.browser.js
function domCanvas(w3, h3) {
  if (typeof document !== "undefined" && document.createElement) {
    const c3 = document.createElement("canvas");
    if (c3 && c3.getContext) {
      c3.width = w3;
      c3.height = h3;
      return c3;
    }
  }
  return null;
}
var domImage = () => typeof Image !== "undefined" ? Image : null;

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/init.js
function initRange(domain4, range7) {
  switch (arguments.length) {
    case 0:
      break;
    case 1:
      this.range(domain4);
      break;
    default:
      this.range(range7).domain(domain4);
      break;
  }
  return this;
}
function initInterpolator(domain4, interpolator) {
  switch (arguments.length) {
    case 0:
      break;
    case 1: {
      if (typeof domain4 === "function") this.interpolator(domain4);
      else this.range(domain4);
      break;
    }
    default: {
      this.domain(domain4);
      if (typeof interpolator === "function") this.interpolator(interpolator);
      else this.range(interpolator);
      break;
    }
  }
  return this;
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/ordinal.js
var implicit = Symbol("implicit");
function ordinal() {
  var index4 = new InternMap(), domain4 = [], range7 = [], unknown = implicit;
  function scale9(d) {
    let i = index4.get(d);
    if (i === void 0) {
      if (unknown !== implicit) return unknown;
      index4.set(d, i = domain4.push(d) - 1);
    }
    return range7[i % range7.length];
  }
  scale9.domain = function(_) {
    if (!arguments.length) return domain4.slice();
    domain4 = [], index4 = new InternMap();
    for (const value4 of _) {
      if (index4.has(value4)) continue;
      index4.set(value4, domain4.push(value4) - 1);
    }
    return scale9;
  };
  scale9.range = function(_) {
    return arguments.length ? (range7 = Array.from(_), scale9) : range7.slice();
  };
  scale9.unknown = function(_) {
    return arguments.length ? (unknown = _, scale9) : unknown;
  };
  scale9.copy = function() {
    return ordinal(domain4, range7).unknown(unknown);
  };
  initRange.apply(scale9, arguments);
  return scale9;
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/index.js
var src_exports = {};
__export(src_exports, {
  interpolate: () => value_default,
  interpolateArray: () => array_default3,
  interpolateBasis: () => basis_default2,
  interpolateBasisClosed: () => basisClosed_default2,
  interpolateCubehelix: () => cubehelix_default,
  interpolateCubehelixLong: () => cubehelixLong,
  interpolateDate: () => date_default,
  interpolateDiscrete: () => discrete_default,
  interpolateHcl: () => hcl_default,
  interpolateHclLong: () => hclLong,
  interpolateHsl: () => hsl_default,
  interpolateHslLong: () => hslLong,
  interpolateHue: () => hue_default,
  interpolateLab: () => lab2,
  interpolateNumber: () => number_default,
  interpolateNumberArray: () => numberArray_default,
  interpolateObject: () => object_default,
  interpolateRgb: () => rgb_default,
  interpolateRgbBasis: () => rgbBasis,
  interpolateRgbBasisClosed: () => rgbBasisClosed,
  interpolateRound: () => round_default,
  interpolateString: () => string_default,
  interpolateTransformCss: () => interpolateTransformCss,
  interpolateTransformSvg: () => interpolateTransformSvg,
  interpolateZoom: () => zoom_default,
  piecewise: () => piecewise,
  quantize: () => quantize_default2
});

// ../deepseek-harness/node_modules/.pnpm/d3-color@3.1.0/node_modules/d3-color/src/define.js
function define_default(constructor, factory, prototype2) {
  constructor.prototype = factory.prototype = prototype2;
  prototype2.constructor = constructor;
}
function extend2(parent, definition3) {
  var prototype2 = Object.create(parent.prototype);
  for (var key2 in definition3) prototype2[key2] = definition3[key2];
  return prototype2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-color@3.1.0/node_modules/d3-color/src/color.js
function Color() {
}
var darker = 0.7;
var brighter = 1 / darker;
var reI = "\\s*([+-]?\\d+)\\s*";
var reN = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)\\s*";
var reP = "\\s*([+-]?(?:\\d*\\.)?\\d+(?:[eE][+-]?\\d+)?)%\\s*";
var reHex = /^#([0-9a-f]{3,8})$/;
var reRgbInteger = new RegExp(`^rgb\\(${reI},${reI},${reI}\\)$`);
var reRgbPercent = new RegExp(`^rgb\\(${reP},${reP},${reP}\\)$`);
var reRgbaInteger = new RegExp(`^rgba\\(${reI},${reI},${reI},${reN}\\)$`);
var reRgbaPercent = new RegExp(`^rgba\\(${reP},${reP},${reP},${reN}\\)$`);
var reHslPercent = new RegExp(`^hsl\\(${reN},${reP},${reP}\\)$`);
var reHslaPercent = new RegExp(`^hsla\\(${reN},${reP},${reP},${reN}\\)$`);
var named = {
  aliceblue: 15792383,
  antiquewhite: 16444375,
  aqua: 65535,
  aquamarine: 8388564,
  azure: 15794175,
  beige: 16119260,
  bisque: 16770244,
  black: 0,
  blanchedalmond: 16772045,
  blue: 255,
  blueviolet: 9055202,
  brown: 10824234,
  burlywood: 14596231,
  cadetblue: 6266528,
  chartreuse: 8388352,
  chocolate: 13789470,
  coral: 16744272,
  cornflowerblue: 6591981,
  cornsilk: 16775388,
  crimson: 14423100,
  cyan: 65535,
  darkblue: 139,
  darkcyan: 35723,
  darkgoldenrod: 12092939,
  darkgray: 11119017,
  darkgreen: 25600,
  darkgrey: 11119017,
  darkkhaki: 12433259,
  darkmagenta: 9109643,
  darkolivegreen: 5597999,
  darkorange: 16747520,
  darkorchid: 10040012,
  darkred: 9109504,
  darksalmon: 15308410,
  darkseagreen: 9419919,
  darkslateblue: 4734347,
  darkslategray: 3100495,
  darkslategrey: 3100495,
  darkturquoise: 52945,
  darkviolet: 9699539,
  deeppink: 16716947,
  deepskyblue: 49151,
  dimgray: 6908265,
  dimgrey: 6908265,
  dodgerblue: 2003199,
  firebrick: 11674146,
  floralwhite: 16775920,
  forestgreen: 2263842,
  fuchsia: 16711935,
  gainsboro: 14474460,
  ghostwhite: 16316671,
  gold: 16766720,
  goldenrod: 14329120,
  gray: 8421504,
  green: 32768,
  greenyellow: 11403055,
  grey: 8421504,
  honeydew: 15794160,
  hotpink: 16738740,
  indianred: 13458524,
  indigo: 4915330,
  ivory: 16777200,
  khaki: 15787660,
  lavender: 15132410,
  lavenderblush: 16773365,
  lawngreen: 8190976,
  lemonchiffon: 16775885,
  lightblue: 11393254,
  lightcoral: 15761536,
  lightcyan: 14745599,
  lightgoldenrodyellow: 16448210,
  lightgray: 13882323,
  lightgreen: 9498256,
  lightgrey: 13882323,
  lightpink: 16758465,
  lightsalmon: 16752762,
  lightseagreen: 2142890,
  lightskyblue: 8900346,
  lightslategray: 7833753,
  lightslategrey: 7833753,
  lightsteelblue: 11584734,
  lightyellow: 16777184,
  lime: 65280,
  limegreen: 3329330,
  linen: 16445670,
  magenta: 16711935,
  maroon: 8388608,
  mediumaquamarine: 6737322,
  mediumblue: 205,
  mediumorchid: 12211667,
  mediumpurple: 9662683,
  mediumseagreen: 3978097,
  mediumslateblue: 8087790,
  mediumspringgreen: 64154,
  mediumturquoise: 4772300,
  mediumvioletred: 13047173,
  midnightblue: 1644912,
  mintcream: 16121850,
  mistyrose: 16770273,
  moccasin: 16770229,
  navajowhite: 16768685,
  navy: 128,
  oldlace: 16643558,
  olive: 8421376,
  olivedrab: 7048739,
  orange: 16753920,
  orangered: 16729344,
  orchid: 14315734,
  palegoldenrod: 15657130,
  palegreen: 10025880,
  paleturquoise: 11529966,
  palevioletred: 14381203,
  papayawhip: 16773077,
  peachpuff: 16767673,
  peru: 13468991,
  pink: 16761035,
  plum: 14524637,
  powderblue: 11591910,
  purple: 8388736,
  rebeccapurple: 6697881,
  red: 16711680,
  rosybrown: 12357519,
  royalblue: 4286945,
  saddlebrown: 9127187,
  salmon: 16416882,
  sandybrown: 16032864,
  seagreen: 3050327,
  seashell: 16774638,
  sienna: 10506797,
  silver: 12632256,
  skyblue: 8900331,
  slateblue: 6970061,
  slategray: 7372944,
  slategrey: 7372944,
  snow: 16775930,
  springgreen: 65407,
  steelblue: 4620980,
  tan: 13808780,
  teal: 32896,
  thistle: 14204888,
  tomato: 16737095,
  turquoise: 4251856,
  violet: 15631086,
  wheat: 16113331,
  white: 16777215,
  whitesmoke: 16119285,
  yellow: 16776960,
  yellowgreen: 10145074
};
define_default(Color, color, {
  copy(channels) {
    return Object.assign(new this.constructor(), this, channels);
  },
  displayable() {
    return this.rgb().displayable();
  },
  hex: color_formatHex,
  // Deprecated! Use color.formatHex.
  formatHex: color_formatHex,
  formatHex8: color_formatHex8,
  formatHsl: color_formatHsl,
  formatRgb: color_formatRgb,
  toString: color_formatRgb
});
function color_formatHex() {
  return this.rgb().formatHex();
}
function color_formatHex8() {
  return this.rgb().formatHex8();
}
function color_formatHsl() {
  return hslConvert(this).formatHsl();
}
function color_formatRgb() {
  return this.rgb().formatRgb();
}
function color(format6) {
  var m3, l;
  format6 = (format6 + "").trim().toLowerCase();
  return (m3 = reHex.exec(format6)) ? (l = m3[1].length, m3 = parseInt(m3[1], 16), l === 6 ? rgbn(m3) : l === 3 ? new Rgb(m3 >> 8 & 15 | m3 >> 4 & 240, m3 >> 4 & 15 | m3 & 240, (m3 & 15) << 4 | m3 & 15, 1) : l === 8 ? rgba(m3 >> 24 & 255, m3 >> 16 & 255, m3 >> 8 & 255, (m3 & 255) / 255) : l === 4 ? rgba(m3 >> 12 & 15 | m3 >> 8 & 240, m3 >> 8 & 15 | m3 >> 4 & 240, m3 >> 4 & 15 | m3 & 240, ((m3 & 15) << 4 | m3 & 15) / 255) : null) : (m3 = reRgbInteger.exec(format6)) ? new Rgb(m3[1], m3[2], m3[3], 1) : (m3 = reRgbPercent.exec(format6)) ? new Rgb(m3[1] * 255 / 100, m3[2] * 255 / 100, m3[3] * 255 / 100, 1) : (m3 = reRgbaInteger.exec(format6)) ? rgba(m3[1], m3[2], m3[3], m3[4]) : (m3 = reRgbaPercent.exec(format6)) ? rgba(m3[1] * 255 / 100, m3[2] * 255 / 100, m3[3] * 255 / 100, m3[4]) : (m3 = reHslPercent.exec(format6)) ? hsla(m3[1], m3[2] / 100, m3[3] / 100, 1) : (m3 = reHslaPercent.exec(format6)) ? hsla(m3[1], m3[2] / 100, m3[3] / 100, m3[4]) : named.hasOwnProperty(format6) ? rgbn(named[format6]) : format6 === "transparent" ? new Rgb(NaN, NaN, NaN, 0) : null;
}
function rgbn(n) {
  return new Rgb(n >> 16 & 255, n >> 8 & 255, n & 255, 1);
}
function rgba(r, g, b10, a3) {
  if (a3 <= 0) r = g = b10 = NaN;
  return new Rgb(r, g, b10, a3);
}
function rgbConvert(o) {
  if (!(o instanceof Color)) o = color(o);
  if (!o) return new Rgb();
  o = o.rgb();
  return new Rgb(o.r, o.g, o.b, o.opacity);
}
function rgb(r, g, b10, opacity2) {
  return arguments.length === 1 ? rgbConvert(r) : new Rgb(r, g, b10, opacity2 == null ? 1 : opacity2);
}
function Rgb(r, g, b10, opacity2) {
  this.r = +r;
  this.g = +g;
  this.b = +b10;
  this.opacity = +opacity2;
}
define_default(Rgb, rgb, extend2(Color, {
  brighter(k) {
    k = k == null ? brighter : Math.pow(brighter, k);
    return new Rgb(this.r * k, this.g * k, this.b * k, this.opacity);
  },
  darker(k) {
    k = k == null ? darker : Math.pow(darker, k);
    return new Rgb(this.r * k, this.g * k, this.b * k, this.opacity);
  },
  rgb() {
    return this;
  },
  clamp() {
    return new Rgb(clampi(this.r), clampi(this.g), clampi(this.b), clampa(this.opacity));
  },
  displayable() {
    return -0.5 <= this.r && this.r < 255.5 && (-0.5 <= this.g && this.g < 255.5) && (-0.5 <= this.b && this.b < 255.5) && (0 <= this.opacity && this.opacity <= 1);
  },
  hex: rgb_formatHex,
  // Deprecated! Use color.formatHex.
  formatHex: rgb_formatHex,
  formatHex8: rgb_formatHex8,
  formatRgb: rgb_formatRgb,
  toString: rgb_formatRgb
}));
function rgb_formatHex() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}`;
}
function rgb_formatHex8() {
  return `#${hex(this.r)}${hex(this.g)}${hex(this.b)}${hex((isNaN(this.opacity) ? 1 : this.opacity) * 255)}`;
}
function rgb_formatRgb() {
  const a3 = clampa(this.opacity);
  return `${a3 === 1 ? "rgb(" : "rgba("}${clampi(this.r)}, ${clampi(this.g)}, ${clampi(this.b)}${a3 === 1 ? ")" : `, ${a3})`}`;
}
function clampa(opacity2) {
  return isNaN(opacity2) ? 1 : Math.max(0, Math.min(1, opacity2));
}
function clampi(value4) {
  return Math.max(0, Math.min(255, Math.round(value4) || 0));
}
function hex(value4) {
  value4 = clampi(value4);
  return (value4 < 16 ? "0" : "") + value4.toString(16);
}
function hsla(h3, s, l, a3) {
  if (a3 <= 0) h3 = s = l = NaN;
  else if (l <= 0 || l >= 1) h3 = s = NaN;
  else if (s <= 0) h3 = NaN;
  return new Hsl(h3, s, l, a3);
}
function hslConvert(o) {
  if (o instanceof Hsl) return new Hsl(o.h, o.s, o.l, o.opacity);
  if (!(o instanceof Color)) o = color(o);
  if (!o) return new Hsl();
  if (o instanceof Hsl) return o;
  o = o.rgb();
  var r = o.r / 255, g = o.g / 255, b10 = o.b / 255, min4 = Math.min(r, g, b10), max4 = Math.max(r, g, b10), h3 = NaN, s = max4 - min4, l = (max4 + min4) / 2;
  if (s) {
    if (r === max4) h3 = (g - b10) / s + (g < b10) * 6;
    else if (g === max4) h3 = (b10 - r) / s + 2;
    else h3 = (r - g) / s + 4;
    s /= l < 0.5 ? max4 + min4 : 2 - max4 - min4;
    h3 *= 60;
  } else {
    s = l > 0 && l < 1 ? 0 : h3;
  }
  return new Hsl(h3, s, l, o.opacity);
}
function hsl(h3, s, l, opacity2) {
  return arguments.length === 1 ? hslConvert(h3) : new Hsl(h3, s, l, opacity2 == null ? 1 : opacity2);
}
function Hsl(h3, s, l, opacity2) {
  this.h = +h3;
  this.s = +s;
  this.l = +l;
  this.opacity = +opacity2;
}
define_default(Hsl, hsl, extend2(Color, {
  brighter(k) {
    k = k == null ? brighter : Math.pow(brighter, k);
    return new Hsl(this.h, this.s, this.l * k, this.opacity);
  },
  darker(k) {
    k = k == null ? darker : Math.pow(darker, k);
    return new Hsl(this.h, this.s, this.l * k, this.opacity);
  },
  rgb() {
    var h3 = this.h % 360 + (this.h < 0) * 360, s = isNaN(h3) || isNaN(this.s) ? 0 : this.s, l = this.l, m22 = l + (l < 0.5 ? l : 1 - l) * s, m1 = 2 * l - m22;
    return new Rgb(
      hsl2rgb(h3 >= 240 ? h3 - 240 : h3 + 120, m1, m22),
      hsl2rgb(h3, m1, m22),
      hsl2rgb(h3 < 120 ? h3 + 240 : h3 - 120, m1, m22),
      this.opacity
    );
  },
  clamp() {
    return new Hsl(clamph(this.h), clampt(this.s), clampt(this.l), clampa(this.opacity));
  },
  displayable() {
    return (0 <= this.s && this.s <= 1 || isNaN(this.s)) && (0 <= this.l && this.l <= 1) && (0 <= this.opacity && this.opacity <= 1);
  },
  formatHsl() {
    const a3 = clampa(this.opacity);
    return `${a3 === 1 ? "hsl(" : "hsla("}${clamph(this.h)}, ${clampt(this.s) * 100}%, ${clampt(this.l) * 100}%${a3 === 1 ? ")" : `, ${a3})`}`;
  }
}));
function clamph(value4) {
  value4 = (value4 || 0) % 360;
  return value4 < 0 ? value4 + 360 : value4;
}
function clampt(value4) {
  return Math.max(0, Math.min(1, value4 || 0));
}
function hsl2rgb(h3, m1, m22) {
  return (h3 < 60 ? m1 + (m22 - m1) * h3 / 60 : h3 < 180 ? m22 : h3 < 240 ? m1 + (m22 - m1) * (240 - h3) / 60 : m1) * 255;
}

// ../deepseek-harness/node_modules/.pnpm/d3-color@3.1.0/node_modules/d3-color/src/math.js
var radians = Math.PI / 180;
var degrees = 180 / Math.PI;

// ../deepseek-harness/node_modules/.pnpm/d3-color@3.1.0/node_modules/d3-color/src/lab.js
var K = 18;
var Xn = 0.96422;
var Yn = 1;
var Zn = 0.82521;
var t03 = 4 / 29;
var t12 = 6 / 29;
var t2 = 3 * t12 * t12;
var t3 = t12 * t12 * t12;
function labConvert(o) {
  if (o instanceof Lab) return new Lab(o.l, o.a, o.b, o.opacity);
  if (o instanceof Hcl) return hcl2lab(o);
  if (!(o instanceof Rgb)) o = rgbConvert(o);
  var r = rgb2lrgb(o.r), g = rgb2lrgb(o.g), b10 = rgb2lrgb(o.b), y6 = xyz2lab((0.2225045 * r + 0.7168786 * g + 0.0606169 * b10) / Yn), x6, z;
  if (r === g && g === b10) x6 = z = y6;
  else {
    x6 = xyz2lab((0.4360747 * r + 0.3850649 * g + 0.1430804 * b10) / Xn);
    z = xyz2lab((0.0139322 * r + 0.0971045 * g + 0.7141733 * b10) / Zn);
  }
  return new Lab(116 * y6 - 16, 500 * (x6 - y6), 200 * (y6 - z), o.opacity);
}
function lab(l, a3, b10, opacity2) {
  return arguments.length === 1 ? labConvert(l) : new Lab(l, a3, b10, opacity2 == null ? 1 : opacity2);
}
function Lab(l, a3, b10, opacity2) {
  this.l = +l;
  this.a = +a3;
  this.b = +b10;
  this.opacity = +opacity2;
}
define_default(Lab, lab, extend2(Color, {
  brighter(k) {
    return new Lab(this.l + K * (k == null ? 1 : k), this.a, this.b, this.opacity);
  },
  darker(k) {
    return new Lab(this.l - K * (k == null ? 1 : k), this.a, this.b, this.opacity);
  },
  rgb() {
    var y6 = (this.l + 16) / 116, x6 = isNaN(this.a) ? y6 : y6 + this.a / 500, z = isNaN(this.b) ? y6 : y6 - this.b / 200;
    x6 = Xn * lab2xyz(x6);
    y6 = Yn * lab2xyz(y6);
    z = Zn * lab2xyz(z);
    return new Rgb(
      lrgb2rgb(3.1338561 * x6 - 1.6168667 * y6 - 0.4906146 * z),
      lrgb2rgb(-0.9787684 * x6 + 1.9161415 * y6 + 0.033454 * z),
      lrgb2rgb(0.0719453 * x6 - 0.2289914 * y6 + 1.4052427 * z),
      this.opacity
    );
  }
}));
function xyz2lab(t) {
  return t > t3 ? Math.pow(t, 1 / 3) : t / t2 + t03;
}
function lab2xyz(t) {
  return t > t12 ? t * t * t : t2 * (t - t03);
}
function lrgb2rgb(x6) {
  return 255 * (x6 <= 31308e-7 ? 12.92 * x6 : 1.055 * Math.pow(x6, 1 / 2.4) - 0.055);
}
function rgb2lrgb(x6) {
  return (x6 /= 255) <= 0.04045 ? x6 / 12.92 : Math.pow((x6 + 0.055) / 1.055, 2.4);
}
function hclConvert(o) {
  if (o instanceof Hcl) return new Hcl(o.h, o.c, o.l, o.opacity);
  if (!(o instanceof Lab)) o = labConvert(o);
  if (o.a === 0 && o.b === 0) return new Hcl(NaN, 0 < o.l && o.l < 100 ? 0 : NaN, o.l, o.opacity);
  var h3 = Math.atan2(o.b, o.a) * degrees;
  return new Hcl(h3 < 0 ? h3 + 360 : h3, Math.sqrt(o.a * o.a + o.b * o.b), o.l, o.opacity);
}
function hcl(h3, c3, l, opacity2) {
  return arguments.length === 1 ? hclConvert(h3) : new Hcl(h3, c3, l, opacity2 == null ? 1 : opacity2);
}
function Hcl(h3, c3, l, opacity2) {
  this.h = +h3;
  this.c = +c3;
  this.l = +l;
  this.opacity = +opacity2;
}
function hcl2lab(o) {
  if (isNaN(o.h)) return new Lab(o.l, 0, 0, o.opacity);
  var h3 = o.h * radians;
  return new Lab(o.l, Math.cos(h3) * o.c, Math.sin(h3) * o.c, o.opacity);
}
define_default(Hcl, hcl, extend2(Color, {
  brighter(k) {
    return new Hcl(this.h, this.c, this.l + K * (k == null ? 1 : k), this.opacity);
  },
  darker(k) {
    return new Hcl(this.h, this.c, this.l - K * (k == null ? 1 : k), this.opacity);
  },
  rgb() {
    return hcl2lab(this).rgb();
  }
}));

// ../deepseek-harness/node_modules/.pnpm/d3-color@3.1.0/node_modules/d3-color/src/cubehelix.js
var A = -0.14861;
var B = 1.78277;
var C = -0.29227;
var D = -0.90649;
var E = 1.97294;
var ED = E * D;
var EB = E * B;
var BC_DA = B * C - D * A;
function cubehelixConvert(o) {
  if (o instanceof Cubehelix) return new Cubehelix(o.h, o.s, o.l, o.opacity);
  if (!(o instanceof Rgb)) o = rgbConvert(o);
  var r = o.r / 255, g = o.g / 255, b10 = o.b / 255, l = (BC_DA * b10 + ED * r - EB * g) / (BC_DA + ED - EB), bl3 = b10 - l, k = (E * (g - l) - C * bl3) / D, s = Math.sqrt(k * k + bl3 * bl3) / (E * l * (1 - l)), h3 = s ? Math.atan2(k, bl3) * degrees - 120 : NaN;
  return new Cubehelix(h3 < 0 ? h3 + 360 : h3, s, l, o.opacity);
}
function cubehelix(h3, s, l, opacity2) {
  return arguments.length === 1 ? cubehelixConvert(h3) : new Cubehelix(h3, s, l, opacity2 == null ? 1 : opacity2);
}
function Cubehelix(h3, s, l, opacity2) {
  this.h = +h3;
  this.s = +s;
  this.l = +l;
  this.opacity = +opacity2;
}
define_default(Cubehelix, cubehelix, extend2(Color, {
  brighter(k) {
    k = k == null ? brighter : Math.pow(brighter, k);
    return new Cubehelix(this.h, this.s, this.l * k, this.opacity);
  },
  darker(k) {
    k = k == null ? darker : Math.pow(darker, k);
    return new Cubehelix(this.h, this.s, this.l * k, this.opacity);
  },
  rgb() {
    var h3 = isNaN(this.h) ? 0 : (this.h + 120) * radians, l = +this.l, a3 = isNaN(this.s) ? 0 : this.s * l * (1 - l), cosh2 = Math.cos(h3), sinh2 = Math.sin(h3);
    return new Rgb(
      255 * (l + a3 * (A * cosh2 + B * sinh2)),
      255 * (l + a3 * (C * cosh2 + D * sinh2)),
      255 * (l + a3 * (E * cosh2)),
      this.opacity
    );
  }
}));

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/basis.js
function basis(t13, v0, v1, v2, v3) {
  var t22 = t13 * t13, t32 = t22 * t13;
  return ((1 - 3 * t13 + 3 * t22 - t32) * v0 + (4 - 6 * t22 + 3 * t32) * v1 + (1 + 3 * t13 + 3 * t22 - 3 * t32) * v2 + t32 * v3) / 6;
}
function basis_default2(values4) {
  var n = values4.length - 1;
  return function(t) {
    var i = t <= 0 ? t = 0 : t >= 1 ? (t = 1, n - 1) : Math.floor(t * n), v1 = values4[i], v2 = values4[i + 1], v0 = i > 0 ? values4[i - 1] : 2 * v1 - v2, v3 = i < n - 1 ? values4[i + 2] : 2 * v2 - v1;
    return basis((t - i / n) * n, v0, v1, v2, v3);
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/basisClosed.js
function basisClosed_default2(values4) {
  var n = values4.length;
  return function(t) {
    var i = Math.floor(((t %= 1) < 0 ? ++t : t) * n), v0 = values4[(i + n - 1) % n], v1 = values4[i % n], v2 = values4[(i + 1) % n], v3 = values4[(i + 2) % n];
    return basis((t - i / n) * n, v0, v1, v2, v3);
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/constant.js
var constant_default3 = (x6) => () => x6;

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/color.js
function linear2(a3, d) {
  return function(t) {
    return a3 + t * d;
  };
}
function exponential(a3, b10, y6) {
  return a3 = Math.pow(a3, y6), b10 = Math.pow(b10, y6) - a3, y6 = 1 / y6, function(t) {
    return Math.pow(a3 + t * b10, y6);
  };
}
function hue(a3, b10) {
  var d = b10 - a3;
  return d ? linear2(a3, d > 180 || d < -180 ? d - 360 * Math.round(d / 360) : d) : constant_default3(isNaN(a3) ? b10 : a3);
}
function gamma(y6) {
  return (y6 = +y6) === 1 ? nogamma : function(a3, b10) {
    return b10 - a3 ? exponential(a3, b10, y6) : constant_default3(isNaN(a3) ? b10 : a3);
  };
}
function nogamma(a3, b10) {
  var d = b10 - a3;
  return d ? linear2(a3, d) : constant_default3(isNaN(a3) ? b10 : a3);
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/rgb.js
var rgb_default = (function rgbGamma(y6) {
  var color6 = gamma(y6);
  function rgb2(start, end) {
    var r = color6((start = rgb(start)).r, (end = rgb(end)).r), g = color6(start.g, end.g), b10 = color6(start.b, end.b), opacity2 = nogamma(start.opacity, end.opacity);
    return function(t) {
      start.r = r(t);
      start.g = g(t);
      start.b = b10(t);
      start.opacity = opacity2(t);
      return start + "";
    };
  }
  rgb2.gamma = rgbGamma;
  return rgb2;
})(1);
function rgbSpline(spline) {
  return function(colors2) {
    var n = colors2.length, r = new Array(n), g = new Array(n), b10 = new Array(n), i, color6;
    for (i = 0; i < n; ++i) {
      color6 = rgb(colors2[i]);
      r[i] = color6.r || 0;
      g[i] = color6.g || 0;
      b10[i] = color6.b || 0;
    }
    r = spline(r);
    g = spline(g);
    b10 = spline(b10);
    color6.opacity = 1;
    return function(t) {
      color6.r = r(t);
      color6.g = g(t);
      color6.b = b10(t);
      return color6 + "";
    };
  };
}
var rgbBasis = rgbSpline(basis_default2);
var rgbBasisClosed = rgbSpline(basisClosed_default2);

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/numberArray.js
function numberArray_default(a3, b10) {
  if (!b10) b10 = [];
  var n = a3 ? Math.min(b10.length, a3.length) : 0, c3 = b10.slice(), i;
  return function(t) {
    for (i = 0; i < n; ++i) c3[i] = a3[i] * (1 - t) + b10[i] * t;
    return c3;
  };
}
function isNumberArray(x6) {
  return ArrayBuffer.isView(x6) && !(x6 instanceof DataView);
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/array.js
function array_default3(a3, b10) {
  return (isNumberArray(b10) ? numberArray_default : genericArray)(a3, b10);
}
function genericArray(a3, b10) {
  var nb = b10 ? b10.length : 0, na = a3 ? Math.min(nb, a3.length) : 0, x6 = new Array(na), c3 = new Array(nb), i;
  for (i = 0; i < na; ++i) x6[i] = value_default(a3[i], b10[i]);
  for (; i < nb; ++i) c3[i] = b10[i];
  return function(t) {
    for (i = 0; i < na; ++i) c3[i] = x6[i](t);
    return c3;
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/date.js
function date_default(a3, b10) {
  var d = /* @__PURE__ */ new Date();
  return a3 = +a3, b10 = +b10, function(t) {
    return d.setTime(a3 * (1 - t) + b10 * t), d;
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/number.js
function number_default(a3, b10) {
  return a3 = +a3, b10 = +b10, function(t) {
    return a3 * (1 - t) + b10 * t;
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/object.js
function object_default(a3, b10) {
  var i = {}, c3 = {}, k;
  if (a3 === null || typeof a3 !== "object") a3 = {};
  if (b10 === null || typeof b10 !== "object") b10 = {};
  for (k in b10) {
    if (k in a3) {
      i[k] = value_default(a3[k], b10[k]);
    } else {
      c3[k] = b10[k];
    }
  }
  return function(t) {
    for (k in i) c3[k] = i[k](t);
    return c3;
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/string.js
var reA = /[-+]?(?:\d+\.?\d*|\.?\d+)(?:[eE][-+]?\d+)?/g;
var reB = new RegExp(reA.source, "g");
function zero3(b10) {
  return function() {
    return b10;
  };
}
function one2(b10) {
  return function(t) {
    return b10(t) + "";
  };
}
function string_default(a3, b10) {
  var bi = reA.lastIndex = reB.lastIndex = 0, am, bm, bs, i = -1, s = [], q = [];
  a3 = a3 + "", b10 = b10 + "";
  while ((am = reA.exec(a3)) && (bm = reB.exec(b10))) {
    if ((bs = bm.index) > bi) {
      bs = b10.slice(bi, bs);
      if (s[i]) s[i] += bs;
      else s[++i] = bs;
    }
    if ((am = am[0]) === (bm = bm[0])) {
      if (s[i]) s[i] += bm;
      else s[++i] = bm;
    } else {
      s[++i] = null;
      q.push({ i, x: number_default(am, bm) });
    }
    bi = reB.lastIndex;
  }
  if (bi < b10.length) {
    bs = b10.slice(bi);
    if (s[i]) s[i] += bs;
    else s[++i] = bs;
  }
  return s.length < 2 ? q[0] ? one2(q[0].x) : zero3(b10) : (b10 = q.length, function(t) {
    for (var i2 = 0, o; i2 < b10; ++i2) s[(o = q[i2]).i] = o.x(t);
    return s.join("");
  });
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/value.js
function value_default(a3, b10) {
  var t = typeof b10, c3;
  return b10 == null || t === "boolean" ? constant_default3(b10) : (t === "number" ? number_default : t === "string" ? (c3 = color(b10)) ? (b10 = c3, rgb_default) : string_default : b10 instanceof color ? rgb_default : b10 instanceof Date ? date_default : isNumberArray(b10) ? numberArray_default : Array.isArray(b10) ? genericArray : typeof b10.valueOf !== "function" && typeof b10.toString !== "function" || isNaN(b10) ? object_default : number_default)(a3, b10);
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/discrete.js
function discrete_default(range7) {
  var n = range7.length;
  return function(t) {
    return range7[Math.max(0, Math.min(n - 1, Math.floor(t * n)))];
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/hue.js
function hue_default(a3, b10) {
  var i = hue(+a3, +b10);
  return function(t) {
    var x6 = i(t);
    return x6 - 360 * Math.floor(x6 / 360);
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/round.js
function round_default(a3, b10) {
  return a3 = +a3, b10 = +b10, function(t) {
    return Math.round(a3 * (1 - t) + b10 * t);
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/transform/decompose.js
var degrees2 = 180 / Math.PI;
var identity3 = {
  translateX: 0,
  translateY: 0,
  rotate: 0,
  skewX: 0,
  scaleX: 1,
  scaleY: 1
};
function decompose_default(a3, b10, c3, d, e, f) {
  var scaleX, scaleY2, skewX;
  if (scaleX = Math.sqrt(a3 * a3 + b10 * b10)) a3 /= scaleX, b10 /= scaleX;
  if (skewX = a3 * c3 + b10 * d) c3 -= a3 * skewX, d -= b10 * skewX;
  if (scaleY2 = Math.sqrt(c3 * c3 + d * d)) c3 /= scaleY2, d /= scaleY2, skewX /= scaleY2;
  if (a3 * d < b10 * c3) a3 = -a3, b10 = -b10, skewX = -skewX, scaleX = -scaleX;
  return {
    translateX: e,
    translateY: f,
    rotate: Math.atan2(b10, a3) * degrees2,
    skewX: Math.atan(skewX) * degrees2,
    scaleX,
    scaleY: scaleY2
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/transform/parse.js
var svgNode;
function parseCss(value4) {
  const m3 = new (typeof DOMMatrix === "function" ? DOMMatrix : WebKitCSSMatrix)(value4 + "");
  return m3.isIdentity ? identity3 : decompose_default(m3.a, m3.b, m3.c, m3.d, m3.e, m3.f);
}
function parseSvg(value4) {
  if (value4 == null) return identity3;
  if (!svgNode) svgNode = document.createElementNS("http://www.w3.org/2000/svg", "g");
  svgNode.setAttribute("transform", value4);
  if (!(value4 = svgNode.transform.baseVal.consolidate())) return identity3;
  value4 = value4.matrix;
  return decompose_default(value4.a, value4.b, value4.c, value4.d, value4.e, value4.f);
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/transform/index.js
function interpolateTransform(parse8, pxComma, pxParen, degParen) {
  function pop(s) {
    return s.length ? s.pop() + " " : "";
  }
  function translate5(xa, ya, xb, yb, s, q) {
    if (xa !== xb || ya !== yb) {
      var i = s.push("translate(", null, pxComma, null, pxParen);
      q.push({ i: i - 4, x: number_default(xa, xb) }, { i: i - 2, x: number_default(ya, yb) });
    } else if (xb || yb) {
      s.push("translate(" + xb + pxComma + yb + pxParen);
    }
  }
  function rotate3(a3, b10, s, q) {
    if (a3 !== b10) {
      if (a3 - b10 > 180) b10 += 360;
      else if (b10 - a3 > 180) a3 += 360;
      q.push({ i: s.push(pop(s) + "rotate(", null, degParen) - 2, x: number_default(a3, b10) });
    } else if (b10) {
      s.push(pop(s) + "rotate(" + b10 + degParen);
    }
  }
  function skewX(a3, b10, s, q) {
    if (a3 !== b10) {
      q.push({ i: s.push(pop(s) + "skewX(", null, degParen) - 2, x: number_default(a3, b10) });
    } else if (b10) {
      s.push(pop(s) + "skewX(" + b10 + degParen);
    }
  }
  function scale9(xa, ya, xb, yb, s, q) {
    if (xa !== xb || ya !== yb) {
      var i = s.push(pop(s) + "scale(", null, ",", null, ")");
      q.push({ i: i - 4, x: number_default(xa, xb) }, { i: i - 2, x: number_default(ya, yb) });
    } else if (xb !== 1 || yb !== 1) {
      s.push(pop(s) + "scale(" + xb + "," + yb + ")");
    }
  }
  return function(a3, b10) {
    var s = [], q = [];
    a3 = parse8(a3), b10 = parse8(b10);
    translate5(a3.translateX, a3.translateY, b10.translateX, b10.translateY, s, q);
    rotate3(a3.rotate, b10.rotate, s, q);
    skewX(a3.skewX, b10.skewX, s, q);
    scale9(a3.scaleX, a3.scaleY, b10.scaleX, b10.scaleY, s, q);
    a3 = b10 = null;
    return function(t) {
      var i = -1, n = q.length, o;
      while (++i < n) s[(o = q[i]).i] = o.x(t);
      return s.join("");
    };
  };
}
var interpolateTransformCss = interpolateTransform(parseCss, "px, ", "px)", "deg)");
var interpolateTransformSvg = interpolateTransform(parseSvg, ", ", ")", ")");

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/zoom.js
var epsilon22 = 1e-12;
function cosh(x6) {
  return ((x6 = Math.exp(x6)) + 1 / x6) / 2;
}
function sinh(x6) {
  return ((x6 = Math.exp(x6)) - 1 / x6) / 2;
}
function tanh(x6) {
  return ((x6 = Math.exp(2 * x6)) - 1) / (x6 + 1);
}
var zoom_default = (function zoomRho(rho, rho2, rho4) {
  function zoom3(p02, p1) {
    var ux0 = p02[0], uy0 = p02[1], w0 = p02[2], ux1 = p1[0], uy1 = p1[1], w1 = p1[2], dx = ux1 - ux0, dy = uy1 - uy0, d2 = dx * dx + dy * dy, i, S;
    if (d2 < epsilon22) {
      S = Math.log(w1 / w0) / rho;
      i = function(t) {
        return [
          ux0 + t * dx,
          uy0 + t * dy,
          w0 * Math.exp(rho * t * S)
        ];
      };
    } else {
      var d1 = Math.sqrt(d2), b02 = (w1 * w1 - w0 * w0 + rho4 * d2) / (2 * w0 * rho2 * d1), b12 = (w1 * w1 - w0 * w0 - rho4 * d2) / (2 * w1 * rho2 * d1), r0 = Math.log(Math.sqrt(b02 * b02 + 1) - b02), r1 = Math.log(Math.sqrt(b12 * b12 + 1) - b12);
      S = (r1 - r0) / rho;
      i = function(t) {
        var s = t * S, coshr0 = cosh(r0), u4 = w0 / (rho2 * d1) * (coshr0 * tanh(rho * s + r0) - sinh(r0));
        return [
          ux0 + u4 * dx,
          uy0 + u4 * dy,
          w0 * coshr0 / cosh(rho * s + r0)
        ];
      };
    }
    i.duration = S * 1e3 * rho / Math.SQRT2;
    return i;
  }
  zoom3.rho = function(_) {
    var _1 = Math.max(1e-3, +_), _2 = _1 * _1, _4 = _2 * _2;
    return zoomRho(_1, _2, _4);
  };
  return zoom3;
})(Math.SQRT2, 2, 4);

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/hsl.js
function hsl2(hue2) {
  return function(start, end) {
    var h3 = hue2((start = hsl(start)).h, (end = hsl(end)).h), s = nogamma(start.s, end.s), l = nogamma(start.l, end.l), opacity2 = nogamma(start.opacity, end.opacity);
    return function(t) {
      start.h = h3(t);
      start.s = s(t);
      start.l = l(t);
      start.opacity = opacity2(t);
      return start + "";
    };
  };
}
var hsl_default = hsl2(hue);
var hslLong = hsl2(nogamma);

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/lab.js
function lab2(start, end) {
  var l = nogamma((start = lab(start)).l, (end = lab(end)).l), a3 = nogamma(start.a, end.a), b10 = nogamma(start.b, end.b), opacity2 = nogamma(start.opacity, end.opacity);
  return function(t) {
    start.l = l(t);
    start.a = a3(t);
    start.b = b10(t);
    start.opacity = opacity2(t);
    return start + "";
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/hcl.js
function hcl2(hue2) {
  return function(start, end) {
    var h3 = hue2((start = hcl(start)).h, (end = hcl(end)).h), c3 = nogamma(start.c, end.c), l = nogamma(start.l, end.l), opacity2 = nogamma(start.opacity, end.opacity);
    return function(t) {
      start.h = h3(t);
      start.c = c3(t);
      start.l = l(t);
      start.opacity = opacity2(t);
      return start + "";
    };
  };
}
var hcl_default = hcl2(hue);
var hclLong = hcl2(nogamma);

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/cubehelix.js
function cubehelix2(hue2) {
  return (function cubehelixGamma(y6) {
    y6 = +y6;
    function cubehelix3(start, end) {
      var h3 = hue2((start = cubehelix(start)).h, (end = cubehelix(end)).h), s = nogamma(start.s, end.s), l = nogamma(start.l, end.l), opacity2 = nogamma(start.opacity, end.opacity);
      return function(t) {
        start.h = h3(t);
        start.s = s(t);
        start.l = l(Math.pow(t, y6));
        start.opacity = opacity2(t);
        return start + "";
      };
    }
    cubehelix3.gamma = cubehelixGamma;
    return cubehelix3;
  })(1);
}
var cubehelix_default = cubehelix2(hue);
var cubehelixLong = cubehelix2(nogamma);

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/piecewise.js
function piecewise(interpolate3, values4) {
  if (values4 === void 0) values4 = interpolate3, interpolate3 = value_default;
  var i = 0, n = values4.length - 1, v2 = values4[0], I = new Array(n < 0 ? 0 : n);
  while (i < n) I[i] = interpolate3(v2, v2 = values4[++i]);
  return function(t) {
    var i2 = Math.max(0, Math.min(n - 1, Math.floor(t *= n)));
    return I[i2](t - i2);
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-interpolate@3.0.1/node_modules/d3-interpolate/src/quantize.js
function quantize_default2(interpolator, n) {
  var samples = new Array(n);
  for (var i = 0; i < n; ++i) samples[i] = interpolator(i / (n - 1));
  return samples;
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/constant.js
function constants(x6) {
  return function() {
    return x6;
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/number.js
function number2(x6) {
  return +x6;
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/continuous.js
var unit = [0, 1];
function identity4(x6) {
  return x6;
}
function normalize(a3, b10) {
  return (b10 -= a3 = +a3) ? function(x6) {
    return (x6 - a3) / b10;
  } : constants(isNaN(b10) ? NaN : 0.5);
}
function clamper(a3, b10) {
  var t;
  if (a3 > b10) t = a3, a3 = b10, b10 = t;
  return function(x6) {
    return Math.max(a3, Math.min(b10, x6));
  };
}
function bimap(domain4, range7, interpolate3) {
  var d0 = domain4[0], d1 = domain4[1], r0 = range7[0], r1 = range7[1];
  if (d1 < d0) d0 = normalize(d1, d0), r0 = interpolate3(r1, r0);
  else d0 = normalize(d0, d1), r0 = interpolate3(r0, r1);
  return function(x6) {
    return r0(d0(x6));
  };
}
function polymap(domain4, range7, interpolate3) {
  var j = Math.min(domain4.length, range7.length) - 1, d = new Array(j), r = new Array(j), i = -1;
  if (domain4[j] < domain4[0]) {
    domain4 = domain4.slice().reverse();
    range7 = range7.slice().reverse();
  }
  while (++i < j) {
    d[i] = normalize(domain4[i], domain4[i + 1]);
    r[i] = interpolate3(range7[i], range7[i + 1]);
  }
  return function(x6) {
    var i2 = bisect_default2(domain4, x6, 1, j) - 1;
    return r[i2](d[i2](x6));
  };
}
function copy(source4, target2) {
  return target2.domain(source4.domain()).range(source4.range()).interpolate(source4.interpolate()).clamp(source4.clamp()).unknown(source4.unknown());
}
function transformer() {
  var domain4 = unit, range7 = unit, interpolate3 = value_default, transform4, untransform, unknown, clamp4 = identity4, piecewise2, output3, input;
  function rescale() {
    var n = Math.min(domain4.length, range7.length);
    if (clamp4 !== identity4) clamp4 = clamper(domain4[0], domain4[n - 1]);
    piecewise2 = n > 2 ? polymap : bimap;
    output3 = input = null;
    return scale9;
  }
  function scale9(x6) {
    return x6 == null || isNaN(x6 = +x6) ? unknown : (output3 || (output3 = piecewise2(domain4.map(transform4), range7, interpolate3)))(transform4(clamp4(x6)));
  }
  scale9.invert = function(y6) {
    return clamp4(untransform((input || (input = piecewise2(range7, domain4.map(transform4), number_default)))(y6)));
  };
  scale9.domain = function(_) {
    return arguments.length ? (domain4 = Array.from(_, number2), rescale()) : domain4.slice();
  };
  scale9.range = function(_) {
    return arguments.length ? (range7 = Array.from(_), rescale()) : range7.slice();
  };
  scale9.rangeRound = function(_) {
    return range7 = Array.from(_), interpolate3 = round_default, rescale();
  };
  scale9.clamp = function(_) {
    return arguments.length ? (clamp4 = _ ? true : identity4, rescale()) : clamp4 !== identity4;
  };
  scale9.interpolate = function(_) {
    return arguments.length ? (interpolate3 = _, rescale()) : interpolate3;
  };
  scale9.unknown = function(_) {
    return arguments.length ? (unknown = _, scale9) : unknown;
  };
  return function(t, u4) {
    transform4 = t, untransform = u4;
    return rescale();
  };
}
function continuous() {
  return transformer()(identity4, identity4);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/tickFormat.js
function tickFormat(start, stop2, count2, specifier) {
  var step = tickStep(start, stop2, count2), precision;
  specifier = formatSpecifier(specifier == null ? ",f" : specifier);
  switch (specifier.type) {
    case "s": {
      var value4 = Math.max(Math.abs(start), Math.abs(stop2));
      if (specifier.precision == null && !isNaN(precision = precisionPrefix_default(step, value4))) specifier.precision = precision;
      return formatPrefix(specifier, value4);
    }
    case "":
    case "e":
    case "g":
    case "p":
    case "r": {
      if (specifier.precision == null && !isNaN(precision = precisionRound_default(step, Math.max(Math.abs(start), Math.abs(stop2))))) specifier.precision = precision - (specifier.type === "e");
      break;
    }
    case "f":
    case "%": {
      if (specifier.precision == null && !isNaN(precision = precisionFixed_default(step))) specifier.precision = precision - (specifier.type === "%") * 2;
      break;
    }
  }
  return format(specifier);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/linear.js
function linearish(scale9) {
  var domain4 = scale9.domain;
  scale9.ticks = function(count2) {
    var d = domain4();
    return ticks(d[0], d[d.length - 1], count2 == null ? 10 : count2);
  };
  scale9.tickFormat = function(count2, specifier) {
    var d = domain4();
    return tickFormat(d[0], d[d.length - 1], count2 == null ? 10 : count2, specifier);
  };
  scale9.nice = function(count2) {
    if (count2 == null) count2 = 10;
    var d = domain4();
    var i0 = 0;
    var i1 = d.length - 1;
    var start = d[i0];
    var stop2 = d[i1];
    var prestep;
    var step;
    var maxIter = 10;
    if (stop2 < start) {
      step = start, start = stop2, stop2 = step;
      step = i0, i0 = i1, i1 = step;
    }
    while (maxIter-- > 0) {
      step = tickIncrement(start, stop2, count2);
      if (step === prestep) {
        d[i0] = start;
        d[i1] = stop2;
        return domain4(d);
      } else if (step > 0) {
        start = Math.floor(start / step) * step;
        stop2 = Math.ceil(stop2 / step) * step;
      } else if (step < 0) {
        start = Math.ceil(start * step) / step;
        stop2 = Math.floor(stop2 * step) / step;
      } else {
        break;
      }
      prestep = step;
    }
    return scale9;
  };
  return scale9;
}
function linear3() {
  var scale9 = continuous();
  scale9.copy = function() {
    return copy(scale9, linear3());
  };
  initRange.apply(scale9, arguments);
  return linearish(scale9);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/identity.js
function identity5(domain4) {
  var unknown;
  function scale9(x6) {
    return x6 == null || isNaN(x6 = +x6) ? unknown : x6;
  }
  scale9.invert = scale9;
  scale9.domain = scale9.range = function(_) {
    return arguments.length ? (domain4 = Array.from(_, number2), scale9) : domain4.slice();
  };
  scale9.unknown = function(_) {
    return arguments.length ? (unknown = _, scale9) : unknown;
  };
  scale9.copy = function() {
    return identity5(domain4).unknown(unknown);
  };
  domain4 = arguments.length ? Array.from(domain4, number2) : [0, 1];
  return linearish(scale9);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/nice.js
function nice(domain4, interval3) {
  domain4 = domain4.slice();
  var i0 = 0, i1 = domain4.length - 1, x06 = domain4[i0], x12 = domain4[i1], t;
  if (x12 < x06) {
    t = i0, i0 = i1, i1 = t;
    t = x06, x06 = x12, x12 = t;
  }
  domain4[i0] = interval3.floor(x06);
  domain4[i1] = interval3.ceil(x12);
  return domain4;
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/log.js
function transformLog(x6) {
  return Math.log(x6);
}
function transformExp(x6) {
  return Math.exp(x6);
}
function transformLogn(x6) {
  return -Math.log(-x6);
}
function transformExpn(x6) {
  return -Math.exp(-x6);
}
function pow10(x6) {
  return isFinite(x6) ? +("1e" + x6) : x6 < 0 ? 0 : x6;
}
function powp(base) {
  return base === 10 ? pow10 : base === Math.E ? Math.exp : (x6) => Math.pow(base, x6);
}
function logp(base) {
  return base === Math.E ? Math.log : base === 10 && Math.log10 || base === 2 && Math.log2 || (base = Math.log(base), (x6) => Math.log(x6) / base);
}
function reflect(f) {
  return (x6, k) => -f(-x6, k);
}
function loggish(transform4) {
  const scale9 = transform4(transformLog, transformExp);
  const domain4 = scale9.domain;
  let base = 10;
  let logs;
  let pows;
  function rescale() {
    logs = logp(base), pows = powp(base);
    if (domain4()[0] < 0) {
      logs = reflect(logs), pows = reflect(pows);
      transform4(transformLogn, transformExpn);
    } else {
      transform4(transformLog, transformExp);
    }
    return scale9;
  }
  scale9.base = function(_) {
    return arguments.length ? (base = +_, rescale()) : base;
  };
  scale9.domain = function(_) {
    return arguments.length ? (domain4(_), rescale()) : domain4();
  };
  scale9.ticks = (count2) => {
    const d = domain4();
    let u4 = d[0];
    let v2 = d[d.length - 1];
    const r = v2 < u4;
    if (r) [u4, v2] = [v2, u4];
    let i = logs(u4);
    let j = logs(v2);
    let k;
    let t;
    const n = count2 == null ? 10 : +count2;
    let z = [];
    if (!(base % 1) && j - i < n) {
      i = Math.floor(i), j = Math.ceil(j);
      if (u4 > 0) for (; i <= j; ++i) {
        for (k = 1; k < base; ++k) {
          t = i < 0 ? k / pows(-i) : k * pows(i);
          if (t < u4) continue;
          if (t > v2) break;
          z.push(t);
        }
      }
      else for (; i <= j; ++i) {
        for (k = base - 1; k >= 1; --k) {
          t = i > 0 ? k / pows(-i) : k * pows(i);
          if (t < u4) continue;
          if (t > v2) break;
          z.push(t);
        }
      }
      if (z.length * 2 < n) z = ticks(u4, v2, n);
    } else {
      z = ticks(i, j, Math.min(j - i, n)).map(pows);
    }
    return r ? z.reverse() : z;
  };
  scale9.tickFormat = (count2, specifier) => {
    if (count2 == null) count2 = 10;
    if (specifier == null) specifier = base === 10 ? "s" : ",";
    if (typeof specifier !== "function") {
      if (!(base % 1) && (specifier = formatSpecifier(specifier)).precision == null) specifier.trim = true;
      specifier = format(specifier);
    }
    if (count2 === Infinity) return specifier;
    const k = Math.max(1, base * count2 / scale9.ticks().length);
    return (d) => {
      let i = d / pows(Math.round(logs(d)));
      if (i * base < base - 0.5) i *= base;
      return i <= k ? specifier(d) : "";
    };
  };
  scale9.nice = () => {
    return domain4(nice(domain4(), {
      floor: (x6) => pows(Math.floor(logs(x6))),
      ceil: (x6) => pows(Math.ceil(logs(x6)))
    }));
  };
  return scale9;
}
function log4() {
  const scale9 = loggish(transformer()).domain([1, 10]);
  scale9.copy = () => copy(scale9, log4()).base(scale9.base());
  initRange.apply(scale9, arguments);
  return scale9;
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/symlog.js
function transformSymlog(c3) {
  return function(x6) {
    return Math.sign(x6) * Math.log1p(Math.abs(x6 / c3));
  };
}
function transformSymexp(c3) {
  return function(x6) {
    return Math.sign(x6) * Math.expm1(Math.abs(x6)) * c3;
  };
}
function symlogish(transform4) {
  var c3 = 1, scale9 = transform4(transformSymlog(c3), transformSymexp(c3));
  scale9.constant = function(_) {
    return arguments.length ? transform4(transformSymlog(c3 = +_), transformSymexp(c3)) : c3;
  };
  return linearish(scale9);
}
function symlog2() {
  var scale9 = symlogish(transformer());
  scale9.copy = function() {
    return copy(scale9, symlog2()).constant(scale9.constant());
  };
  return initRange.apply(scale9, arguments);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/pow.js
function transformPow(exponent2) {
  return function(x6) {
    return x6 < 0 ? -Math.pow(-x6, exponent2) : Math.pow(x6, exponent2);
  };
}
function transformSqrt(x6) {
  return x6 < 0 ? -Math.sqrt(-x6) : Math.sqrt(x6);
}
function transformSquare(x6) {
  return x6 < 0 ? -x6 * x6 : x6 * x6;
}
function powish(transform4) {
  var scale9 = transform4(identity4, identity4), exponent2 = 1;
  function rescale() {
    return exponent2 === 1 ? transform4(identity4, identity4) : exponent2 === 0.5 ? transform4(transformSqrt, transformSquare) : transform4(transformPow(exponent2), transformPow(1 / exponent2));
  }
  scale9.exponent = function(_) {
    return arguments.length ? (exponent2 = +_, rescale()) : exponent2;
  };
  return linearish(scale9);
}
function pow3() {
  var scale9 = powish(transformer());
  scale9.copy = function() {
    return copy(scale9, pow3()).exponent(scale9.exponent());
  };
  initRange.apply(scale9, arguments);
  return scale9;
}
function sqrt2() {
  return pow3.apply(null, arguments).exponent(0.5);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/quantile.js
function quantile2() {
  var domain4 = [], range7 = [], thresholds = [], unknown;
  function rescale() {
    var i = 0, n = Math.max(1, range7.length);
    thresholds = new Array(n - 1);
    while (++i < n) thresholds[i - 1] = quantileSorted(domain4, i / n);
    return scale9;
  }
  function scale9(x6) {
    return x6 == null || isNaN(x6 = +x6) ? unknown : range7[bisect_default2(thresholds, x6)];
  }
  scale9.invertExtent = function(y6) {
    var i = range7.indexOf(y6);
    return i < 0 ? [NaN, NaN] : [
      i > 0 ? thresholds[i - 1] : domain4[0],
      i < thresholds.length ? thresholds[i] : domain4[domain4.length - 1]
    ];
  };
  scale9.domain = function(_) {
    if (!arguments.length) return domain4.slice();
    domain4 = [];
    for (let d of _) if (d != null && !isNaN(d = +d)) domain4.push(d);
    domain4.sort(ascending2);
    return rescale();
  };
  scale9.range = function(_) {
    return arguments.length ? (range7 = Array.from(_), rescale()) : range7.slice();
  };
  scale9.unknown = function(_) {
    return arguments.length ? (unknown = _, scale9) : unknown;
  };
  scale9.quantiles = function() {
    return thresholds.slice();
  };
  scale9.copy = function() {
    return quantile2().domain(domain4).range(range7).unknown(unknown);
  };
  return initRange.apply(scale9, arguments);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/quantize.js
function quantize() {
  var x06 = 0, x12 = 1, n = 1, domain4 = [0.5], range7 = [0, 1], unknown;
  function scale9(x6) {
    return x6 != null && x6 <= x6 ? range7[bisect_default2(domain4, x6, 0, n)] : unknown;
  }
  function rescale() {
    var i = -1;
    domain4 = new Array(n);
    while (++i < n) domain4[i] = ((i + 1) * x12 - (i - n) * x06) / (n + 1);
    return scale9;
  }
  scale9.domain = function(_) {
    return arguments.length ? ([x06, x12] = _, x06 = +x06, x12 = +x12, rescale()) : [x06, x12];
  };
  scale9.range = function(_) {
    return arguments.length ? (n = (range7 = Array.from(_)).length - 1, rescale()) : range7.slice();
  };
  scale9.invertExtent = function(y6) {
    var i = range7.indexOf(y6);
    return i < 0 ? [NaN, NaN] : i < 1 ? [x06, domain4[0]] : i >= n ? [domain4[n - 1], x12] : [domain4[i - 1], domain4[i]];
  };
  scale9.unknown = function(_) {
    return arguments.length ? (unknown = _, scale9) : scale9;
  };
  scale9.thresholds = function() {
    return domain4.slice();
  };
  scale9.copy = function() {
    return quantize().domain([x06, x12]).range(range7).unknown(unknown);
  };
  return initRange.apply(linearish(scale9), arguments);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/threshold.js
function threshold() {
  var domain4 = [0.5], range7 = [0, 1], unknown, n = 1;
  function scale9(x6) {
    return x6 != null && x6 <= x6 ? range7[bisect_default2(domain4, x6, 0, n)] : unknown;
  }
  scale9.domain = function(_) {
    return arguments.length ? (domain4 = Array.from(_), n = Math.min(domain4.length, range7.length - 1), scale9) : domain4.slice();
  };
  scale9.range = function(_) {
    return arguments.length ? (range7 = Array.from(_), n = Math.min(domain4.length, range7.length - 1), scale9) : range7.slice();
  };
  scale9.invertExtent = function(y6) {
    var i = range7.indexOf(y6);
    return [domain4[i - 1], domain4[i]];
  };
  scale9.unknown = function(_) {
    return arguments.length ? (unknown = _, scale9) : unknown;
  };
  scale9.copy = function() {
    return threshold().domain(domain4).range(range7).unknown(unknown);
  };
  return initRange.apply(scale9, arguments);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/time.js
function date(t) {
  return new Date(t);
}
function number3(t) {
  return t instanceof Date ? +t : +/* @__PURE__ */ new Date(+t);
}
function calendar(ticks2, tickInterval, year, month, week3, day, hour, minute, second2, format6) {
  var scale9 = continuous(), invert3 = scale9.invert, domain4 = scale9.domain;
  var formatMillisecond = format6(".%L"), formatSecond = format6(":%S"), formatMinute = format6("%I:%M"), formatHour = format6("%I %p"), formatDay = format6("%a %d"), formatWeek = format6("%b %d"), formatMonth = format6("%B"), formatYear3 = format6("%Y");
  function tickFormat3(date2) {
    return (second2(date2) < date2 ? formatMillisecond : minute(date2) < date2 ? formatSecond : hour(date2) < date2 ? formatMinute : day(date2) < date2 ? formatHour : month(date2) < date2 ? week3(date2) < date2 ? formatDay : formatWeek : year(date2) < date2 ? formatMonth : formatYear3)(date2);
  }
  scale9.invert = function(y6) {
    return new Date(invert3(y6));
  };
  scale9.domain = function(_) {
    return arguments.length ? domain4(Array.from(_, number3)) : domain4().map(date);
  };
  scale9.ticks = function(interval3) {
    var d = domain4();
    return ticks2(d[0], d[d.length - 1], interval3 == null ? 10 : interval3);
  };
  scale9.tickFormat = function(count2, specifier) {
    return specifier == null ? tickFormat3 : format6(specifier);
  };
  scale9.nice = function(interval3) {
    var d = domain4();
    if (!interval3 || typeof interval3.range !== "function") interval3 = tickInterval(d[0], d[d.length - 1], interval3 == null ? 10 : interval3);
    return interval3 ? domain4(nice(d, interval3)) : scale9;
  };
  scale9.copy = function() {
    return copy(scale9, calendar(ticks2, tickInterval, year, month, week3, day, hour, minute, second2, format6));
  };
  return scale9;
}
function time() {
  return initRange.apply(calendar(timeTicks, timeTickInterval, timeYear, timeMonth, timeSunday, timeDay, timeHour, timeMinute, second, timeFormat).domain([new Date(2e3, 0, 1), new Date(2e3, 0, 2)]), arguments);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/utcTime.js
function utcTime() {
  return initRange.apply(calendar(utcTicks, utcTickInterval, utcYear, utcMonth, utcSunday, utcDay, utcHour, utcMinute, second, utcFormat).domain([Date.UTC(2e3, 0, 1), Date.UTC(2e3, 0, 2)]), arguments);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/sequential.js
function transformer2() {
  var x06 = 0, x12 = 1, t05, t13, k10, transform4, interpolator = identity4, clamp4 = false, unknown;
  function scale9(x6) {
    return x6 == null || isNaN(x6 = +x6) ? unknown : interpolator(k10 === 0 ? 0.5 : (x6 = (transform4(x6) - t05) * k10, clamp4 ? Math.max(0, Math.min(1, x6)) : x6));
  }
  scale9.domain = function(_) {
    return arguments.length ? ([x06, x12] = _, t05 = transform4(x06 = +x06), t13 = transform4(x12 = +x12), k10 = t05 === t13 ? 0 : 1 / (t13 - t05), scale9) : [x06, x12];
  };
  scale9.clamp = function(_) {
    return arguments.length ? (clamp4 = !!_, scale9) : clamp4;
  };
  scale9.interpolator = function(_) {
    return arguments.length ? (interpolator = _, scale9) : interpolator;
  };
  function range7(interpolate3) {
    return function(_) {
      var r0, r1;
      return arguments.length ? ([r0, r1] = _, interpolator = interpolate3(r0, r1), scale9) : [interpolator(0), interpolator(1)];
    };
  }
  scale9.range = range7(value_default);
  scale9.rangeRound = range7(round_default);
  scale9.unknown = function(_) {
    return arguments.length ? (unknown = _, scale9) : unknown;
  };
  return function(t) {
    transform4 = t, t05 = t(x06), t13 = t(x12), k10 = t05 === t13 ? 0 : 1 / (t13 - t05);
    return scale9;
  };
}
function copy2(source4, target2) {
  return target2.domain(source4.domain()).interpolator(source4.interpolator()).clamp(source4.clamp()).unknown(source4.unknown());
}
function sequential() {
  var scale9 = linearish(transformer2()(identity4));
  scale9.copy = function() {
    return copy2(scale9, sequential());
  };
  return initInterpolator.apply(scale9, arguments);
}
function sequentialLog() {
  var scale9 = loggish(transformer2()).domain([1, 10]);
  scale9.copy = function() {
    return copy2(scale9, sequentialLog()).base(scale9.base());
  };
  return initInterpolator.apply(scale9, arguments);
}
function sequentialSymlog() {
  var scale9 = symlogish(transformer2());
  scale9.copy = function() {
    return copy2(scale9, sequentialSymlog()).constant(scale9.constant());
  };
  return initInterpolator.apply(scale9, arguments);
}
function sequentialPow() {
  var scale9 = powish(transformer2());
  scale9.copy = function() {
    return copy2(scale9, sequentialPow()).exponent(scale9.exponent());
  };
  return initInterpolator.apply(scale9, arguments);
}
function sequentialSqrt() {
  return sequentialPow.apply(null, arguments).exponent(0.5);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale@4.0.2/node_modules/d3-scale/src/diverging.js
function transformer3() {
  var x06 = 0, x12 = 0.5, x22 = 1, s = 1, t05, t13, t22, k10, k21, interpolator = identity4, transform4, clamp4 = false, unknown;
  function scale9(x6) {
    return isNaN(x6 = +x6) ? unknown : (x6 = 0.5 + ((x6 = +transform4(x6)) - t13) * (s * x6 < s * t13 ? k10 : k21), interpolator(clamp4 ? Math.max(0, Math.min(1, x6)) : x6));
  }
  scale9.domain = function(_) {
    return arguments.length ? ([x06, x12, x22] = _, t05 = transform4(x06 = +x06), t13 = transform4(x12 = +x12), t22 = transform4(x22 = +x22), k10 = t05 === t13 ? 0 : 0.5 / (t13 - t05), k21 = t13 === t22 ? 0 : 0.5 / (t22 - t13), s = t13 < t05 ? -1 : 1, scale9) : [x06, x12, x22];
  };
  scale9.clamp = function(_) {
    return arguments.length ? (clamp4 = !!_, scale9) : clamp4;
  };
  scale9.interpolator = function(_) {
    return arguments.length ? (interpolator = _, scale9) : interpolator;
  };
  function range7(interpolate3) {
    return function(_) {
      var r0, r1, r2;
      return arguments.length ? ([r0, r1, r2] = _, interpolator = piecewise(interpolate3, [r0, r1, r2]), scale9) : [interpolator(0), interpolator(0.5), interpolator(1)];
    };
  }
  scale9.range = range7(value_default);
  scale9.rangeRound = range7(round_default);
  scale9.unknown = function(_) {
    return arguments.length ? (unknown = _, scale9) : unknown;
  };
  return function(t) {
    transform4 = t, t05 = t(x06), t13 = t(x12), t22 = t(x22), k10 = t05 === t13 ? 0 : 0.5 / (t13 - t05), k21 = t13 === t22 ? 0 : 0.5 / (t22 - t13), s = t13 < t05 ? -1 : 1;
    return scale9;
  };
}
function diverging() {
  var scale9 = linearish(transformer3()(identity4));
  scale9.copy = function() {
    return copy2(scale9, diverging());
  };
  return initInterpolator.apply(scale9, arguments);
}
function divergingLog() {
  var scale9 = loggish(transformer3()).domain([0.1, 1, 10]);
  scale9.copy = function() {
    return copy2(scale9, divergingLog()).base(scale9.base());
  };
  return initInterpolator.apply(scale9, arguments);
}
function divergingSymlog() {
  var scale9 = symlogish(transformer3());
  scale9.copy = function() {
    return copy2(scale9, divergingSymlog()).constant(scale9.constant());
  };
  return initInterpolator.apply(scale9, arguments);
}
function divergingPow() {
  var scale9 = powish(transformer3());
  scale9.copy = function() {
    return copy2(scale9, divergingPow()).exponent(scale9.exponent());
  };
  return initInterpolator.apply(scale9, arguments);
}
function divergingSqrt() {
  return divergingPow.apply(null, arguments).exponent(0.5);
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale-chromatic@3.1.0/node_modules/d3-scale-chromatic/src/colors.js
function colors_default(specifier) {
  var n = specifier.length / 6 | 0, colors2 = new Array(n), i = 0;
  while (i < n) colors2[i] = "#" + specifier.slice(i * 6, ++i * 6);
  return colors2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-scale-chromatic@3.1.0/node_modules/d3-scale-chromatic/src/categorical/category10.js
var category10_default = colors_default("1f77b4ff7f0e2ca02cd627289467bd8c564be377c27f7f7fbcbd2217becf");

// ../deepseek-harness/node_modules/.pnpm/d3-scale-chromatic@3.1.0/node_modules/d3-scale-chromatic/src/categorical/Accent.js
var Accent_default = colors_default("7fc97fbeaed4fdc086ffff99386cb0f0027fbf5b17666666");

// ../deepseek-harness/node_modules/.pnpm/d3-scale-chromatic@3.1.0/node_modules/d3-scale-chromatic/src/categorical/Dark2.js
var Dark2_default = colors_default("1b9e77d95f027570b3e7298a66a61ee6ab02a6761d666666");

// ../deepseek-harness/node_modules/.pnpm/d3-scale-chromatic@3.1.0/node_modules/d3-scale-chromatic/src/categorical/observable10.js
var observable10_default = colors_default("4269d0efb118ff725c6cc5b03ca951ff8ab7a463f297bbf59c6b4e9498a0");

// ../deepseek-harness/node_modules/.pnpm/d3-scale-chromatic@3.1.0/node_modules/d3-scale-chromatic/src/categorical/Paired.js
var Paired_default = colors_default("a6cee31f78b4b2df8a33a02cfb9a99e31a1cfdbf6fff7f00cab2d66a3d9affff99b15928");

// ../deepseek-harness/node_modules/.pnpm/d3-scale-chromatic@3.1.0/node_modules/d3-scale-chromatic/src/categorical/Pastel1.js
var Pastel1_default = colors_default("fbb4aeb3cde3ccebc5decbe4fed9a6ffffcce5d8bdfddaecf2f2f2");

// ../deepseek-harness/node_modules/.pnpm/d3-scale-chromatic@3.1.0/node_modules/d3-scale-chromatic/src/categorical/Pastel2.js
var Pastel2_default = colors_default("b3e2cdfdcdaccbd5e8f4cae4e6f5c9fff2aef1e2cccccccc");

// ../deepseek-harness/node_modules/.pnpm/d3-scale-chromatic@3.1.0/node_modules/d3-scale-chromatic/src/categorical/Set1.js
var Set1_default = colors_default("e41a1c377eb84daf4a984ea3ff7f00ffff33a65628f781bf999999");

// ../deepseek-harness/node_modules/.pnpm/d3-scale-chromatic@3.1.0/node_modules/d3-scale-chromatic/src/categorical/Set2.js
var Set2_default = colors_default("66c2a5fc8d628da0cbe78ac3a6d854ffd92fe5c494b3b3b3");

// ../deepseek-harness/node_modules/.pnpm/d3-scale-chromatic@3.1.0/node_modules/d3-scale-chromatic/src/categorical/Set3.js
var Set3_default = colors_default("8dd3c7ffffb3bebadafb807280b1d3fdb462b3de69fccde5d9d9d9bc80bdccebc5ffed6f");

// ../deepseek-harness/node_modules/.pnpm/vega-scale@8.1.3/node_modules/vega-scale/build/vega-scale.js
function bandSpace(count2, paddingInner2, paddingOuter2) {
  const space = count2 - paddingInner2 + paddingOuter2 * 2;
  return count2 ? space > 0 ? space : 1 : 0;
}
var Identity = "identity";
var Linear2 = "linear";
var Log = "log";
var Pow = "pow";
var Sqrt = "sqrt";
var Symlog = "symlog";
var Time = "time";
var UTC = "utc";
var Sequential = "sequential";
var Diverging = "diverging";
var Quantile2 = "quantile";
var Quantize = "quantize";
var Threshold = "threshold";
var Ordinal = "ordinal";
var Point = "point";
var Band = "band";
var BinOrdinal = "bin-ordinal";
var Continuous = "continuous";
var Discrete = "discrete";
var Discretizing = "discretizing";
var Interpolating = "interpolating";
var Temporal = "temporal";
function invertRange(scale9) {
  return function(_) {
    let lo = _[0], hi = _[1], t;
    if (hi < lo) {
      t = lo;
      lo = hi;
      hi = t;
    }
    return [scale9.invert(lo), scale9.invert(hi)];
  };
}
function invertRangeExtent(scale9) {
  return function(_) {
    const range7 = scale9.range();
    let lo = _[0], hi = _[1], min4 = -1, max4, t, i, n;
    if (hi < lo) {
      t = lo;
      lo = hi;
      hi = t;
    }
    for (i = 0, n = range7.length; i < n; ++i) {
      if (range7[i] >= lo && range7[i] <= hi) {
        if (min4 < 0) min4 = i;
        max4 = i;
      }
    }
    if (min4 < 0) return void 0;
    lo = scale9.invertExtent(range7[min4]);
    hi = scale9.invertExtent(range7[max4]);
    return [lo[0] === void 0 ? lo[1] : lo[0], hi[1] === void 0 ? hi[0] : hi[1]];
  };
}
function band() {
  const scale9 = ordinal().unknown(void 0), domain4 = scale9.domain, ordinalRange = scale9.range;
  let range$1 = [0, 1], step, bandwidth3, round = false, paddingInner2 = 0, paddingOuter2 = 0, align2 = 0.5;
  delete scale9.unknown;
  function rescale() {
    const n = domain4().length, reverse4 = range$1[1] < range$1[0], stop2 = range$1[1 - reverse4], space = bandSpace(n, paddingInner2, paddingOuter2);
    let start = range$1[reverse4 - 0];
    step = (stop2 - start) / (space || 1);
    if (round) {
      step = Math.floor(step);
    }
    start += (stop2 - start - step * (n - paddingInner2)) * align2;
    bandwidth3 = step * (1 - paddingInner2);
    if (round) {
      start = Math.round(start);
      bandwidth3 = Math.round(bandwidth3);
    }
    const values4 = range(n).map((i) => start + step * i);
    return ordinalRange(reverse4 ? values4.reverse() : values4);
  }
  scale9.domain = function(_) {
    if (arguments.length) {
      domain4(_);
      return rescale();
    } else {
      return domain4();
    }
  };
  scale9.range = function(_) {
    if (arguments.length) {
      range$1 = [+_[0], +_[1]];
      return rescale();
    } else {
      return range$1.slice();
    }
  };
  scale9.rangeRound = function(_) {
    range$1 = [+_[0], +_[1]];
    round = true;
    return rescale();
  };
  scale9.bandwidth = function() {
    return bandwidth3;
  };
  scale9.step = function() {
    return step;
  };
  scale9.round = function(_) {
    if (arguments.length) {
      round = !!_;
      return rescale();
    } else {
      return round;
    }
  };
  scale9.padding = function(_) {
    if (arguments.length) {
      paddingOuter2 = Math.max(0, Math.min(1, _));
      paddingInner2 = paddingOuter2;
      return rescale();
    } else {
      return paddingInner2;
    }
  };
  scale9.paddingInner = function(_) {
    if (arguments.length) {
      paddingInner2 = Math.max(0, Math.min(1, _));
      return rescale();
    } else {
      return paddingInner2;
    }
  };
  scale9.paddingOuter = function(_) {
    if (arguments.length) {
      paddingOuter2 = Math.max(0, Math.min(1, _));
      return rescale();
    } else {
      return paddingOuter2;
    }
  };
  scale9.align = function(_) {
    if (arguments.length) {
      align2 = Math.max(0, Math.min(1, _));
      return rescale();
    } else {
      return align2;
    }
  };
  scale9.invertRange = function(_) {
    if (_[0] == null || _[1] == null) return;
    const reverse4 = range$1[1] < range$1[0], values4 = reverse4 ? ordinalRange().reverse() : ordinalRange(), n = values4.length - 1;
    let lo = +_[0], hi = +_[1], a3, b10, t;
    if (lo !== lo || hi !== hi) return;
    if (hi < lo) {
      t = lo;
      lo = hi;
      hi = t;
    }
    if (hi < values4[0] || lo > range$1[1 - reverse4]) return;
    a3 = Math.max(0, bisectRight(values4, lo) - 1);
    b10 = lo === hi ? a3 : bisectRight(values4, hi) - 1;
    if (lo - values4[a3] > bandwidth3 + 1e-10) ++a3;
    if (reverse4) {
      t = a3;
      a3 = n - b10;
      b10 = n - t;
    }
    return a3 > b10 ? void 0 : domain4().slice(a3, b10 + 1);
  };
  scale9.invert = function(_) {
    const value4 = scale9.invertRange([_, _]);
    return value4 ? value4[0] : value4;
  };
  scale9.copy = function() {
    return band().domain(domain4()).range(range$1).round(round).paddingInner(paddingInner2).paddingOuter(paddingOuter2).align(align2);
  };
  return rescale();
}
function pointish(scale9) {
  const copy5 = scale9.copy;
  scale9.padding = scale9.paddingOuter;
  delete scale9.paddingInner;
  scale9.copy = function() {
    return pointish(copy5());
  };
  return scale9;
}
function point5() {
  return pointish(band().paddingInner(1));
}
var map2 = Array.prototype.map;
function numbers3(_) {
  return map2.call(_, toNumber);
}
var slice2 = Array.prototype.slice;
function scaleBinOrdinal() {
  let domain4 = [], range7 = [];
  function scale9(x6) {
    return x6 == null || x6 !== x6 ? void 0 : range7[(bisect_default2(domain4, x6) - 1) % range7.length];
  }
  scale9.domain = function(_) {
    if (arguments.length) {
      domain4 = numbers3(_);
      return scale9;
    } else {
      return domain4.slice();
    }
  };
  scale9.range = function(_) {
    if (arguments.length) {
      range7 = slice2.call(_);
      return scale9;
    } else {
      return range7.slice();
    }
  };
  scale9.tickFormat = function(count2, specifier) {
    return tickFormat(domain4[0], peek(domain4), count2 == null ? 10 : count2, specifier);
  };
  scale9.copy = function() {
    return scaleBinOrdinal().domain(scale9.domain()).range(scale9.range());
  };
  return scale9;
}
var scales = /* @__PURE__ */ new Map();
var VEGA_SCALE = Symbol("vega_scale");
function registerScale(scale9) {
  scale9[VEGA_SCALE] = true;
  return scale9;
}
function isRegisteredScale(scale9) {
  return scale9 && scale9[VEGA_SCALE] === true;
}
function create(type4, constructor, metadata3) {
  const ctr = function scale9() {
    const s = constructor();
    if (!s.invertRange) {
      s.invertRange = s.invert ? invertRange(s) : s.invertExtent ? invertRangeExtent(s) : void 0;
    }
    s.type = type4;
    return registerScale(s);
  };
  ctr.metadata = toSet(array_default(metadata3));
  return ctr;
}
function scale(type4, scale9, metadata3) {
  if (arguments.length > 1) {
    scales.set(type4, create(type4, scale9, metadata3));
    return this;
  } else {
    return isValidScaleType(type4) ? scales.get(type4) : void 0;
  }
}
scale(Identity, identity5);
scale(Linear2, linear3, Continuous);
scale(Log, log4, [Continuous, Log]);
scale(Pow, pow3, Continuous);
scale(Sqrt, sqrt2, Continuous);
scale(Symlog, symlog2, Continuous);
scale(Time, time, [Continuous, Temporal]);
scale(UTC, utcTime, [Continuous, Temporal]);
scale(Sequential, sequential, [Continuous, Interpolating]);
scale(`${Sequential}-${Linear2}`, sequential, [Continuous, Interpolating]);
scale(`${Sequential}-${Log}`, sequentialLog, [Continuous, Interpolating, Log]);
scale(`${Sequential}-${Pow}`, sequentialPow, [Continuous, Interpolating]);
scale(`${Sequential}-${Sqrt}`, sequentialSqrt, [Continuous, Interpolating]);
scale(`${Sequential}-${Symlog}`, sequentialSymlog, [Continuous, Interpolating]);
scale(`${Diverging}-${Linear2}`, diverging, [Continuous, Interpolating]);
scale(`${Diverging}-${Log}`, divergingLog, [Continuous, Interpolating, Log]);
scale(`${Diverging}-${Pow}`, divergingPow, [Continuous, Interpolating]);
scale(`${Diverging}-${Sqrt}`, divergingSqrt, [Continuous, Interpolating]);
scale(`${Diverging}-${Symlog}`, divergingSymlog, [Continuous, Interpolating]);
scale(Quantile2, quantile2, [Discretizing, Quantile2]);
scale(Quantize, quantize, Discretizing);
scale(Threshold, threshold, Discretizing);
scale(BinOrdinal, scaleBinOrdinal, [Discrete, Discretizing]);
scale(Ordinal, ordinal, Discrete);
scale(Band, band, Discrete);
scale(Point, point5, Discrete);
function isValidScaleType(type4) {
  return scales.has(type4);
}
function hasType(key2, type4) {
  const s = scales.get(key2);
  return s && s.metadata[type4];
}
function isContinuous(key2) {
  return hasType(key2, Continuous);
}
function isDiscrete(key2) {
  return hasType(key2, Discrete);
}
function isDiscretizing(key2) {
  return hasType(key2, Discretizing);
}
function isLogarithmic(key2) {
  return hasType(key2, Log);
}
function isTemporal(key2) {
  return hasType(key2, Temporal);
}
function isInterpolating(key2) {
  return hasType(key2, Interpolating);
}
function isQuantile(key2) {
  return hasType(key2, Quantile2);
}
var scaleProps = ["clamp", "base", "constant", "exponent"];
function interpolateRange(interpolator, range7) {
  const start = range7[0], span = peek(range7) - start;
  return function(i) {
    return interpolator(start + i * span);
  };
}
function interpolateColors(colors2, type4, gamma2) {
  return piecewise(interpolate(type4 || "rgb", gamma2), colors2);
}
function quantizeInterpolator(interpolator, count2) {
  const samples = new Array(count2), n = count2 + 1;
  for (let i = 0; i < count2; ) samples[i] = interpolator(++i / n);
  return samples;
}
function scaleFraction(scale$13, min4, max4) {
  const delta = max4 - min4;
  let i, t, s;
  if (!delta || !Number.isFinite(delta)) {
    return constant_default(0.5);
  } else {
    i = (t = scale$13.type).indexOf("-");
    t = i < 0 ? t : t.slice(i + 1);
    s = scale(t)().domain([min4, max4]).range([0, 1]);
    scaleProps.forEach((m3) => scale$13[m3] ? s[m3](scale$13[m3]()) : 0);
    return s;
  }
}
function interpolate(type4, gamma2) {
  const interp = src_exports[method(type4)];
  return gamma2 != null && interp && interp.gamma ? interp.gamma(gamma2) : interp;
}
function method(type4) {
  return "interpolate" + type4.toLowerCase().split("-").map((s) => s[0].toUpperCase() + s.slice(1)).join("");
}
var continuous2 = {
  blues: "cfe1f2bed8eca8cee58fc1de74b2d75ba3cf4592c63181bd206fb2125ca40a4a90",
  greens: "d3eecdc0e6baabdda594d3917bc77d60ba6c46ab5e329a512089430e7735036429",
  greys: "e2e2e2d4d4d4c4c4c4b1b1b19d9d9d8888887575756262624d4d4d3535351e1e1e",
  oranges: "fdd8b3fdc998fdb87bfda55efc9244f87f2cf06b18e4580bd14904b93d029f3303",
  purples: "e2e1efd4d4e8c4c5e0b4b3d6a3a0cc928ec3827cb97566ae684ea25c3696501f8c",
  reds: "fdc9b4fcb49afc9e80fc8767fa7051f6573fec3f2fdc2a25c81b1db21218970b13",
  blueGreen: "d5efedc1e8e0a7ddd18bd2be70c6a958ba9144ad77319c5d2089460e7736036429",
  bluePurple: "ccddecbad0e4a8c2dd9ab0d4919cc98d85be8b6db28a55a6873c99822287730f71",
  greenBlue: "d3eecec5e8c3b1e1bb9bd8bb82cec269c2ca51b2cd3c9fc7288abd1675b10b60a1",
  orangeRed: "fddcaffdcf9bfdc18afdad77fb9562f67d53ee6545e24932d32d1ebf130da70403",
  purpleBlue: "dbdaebc8cee4b1c3de97b7d87bacd15b9fc93a90c01e7fb70b70ab056199045281",
  purpleBlueGreen: "dbd8eac8cee4b0c3de93b7d872acd1549fc83892bb1c88a3097f8702736b016353",
  purpleRed: "dcc9e2d3b3d7ce9eccd186c0da6bb2e14da0e23189d91e6fc61159ab07498f023a",
  redPurple: "fccfccfcbec0faa9b8f98faff571a5ec539ddb3695c41b8aa908808d0179700174",
  yellowGreen: "e4f4acd1eca0b9e2949ed68880c97c62bb6e47aa5e3297502083440e723b036034",
  yellowOrangeBrown: "feeaa1fedd84fecc63feb746fca031f68921eb7215db5e0bc54c05ab3d038f3204",
  yellowOrangeRed: "fee087fed16ffebd59fea849fd903efc7335f9522bee3423de1b20ca0b22af0225",
  blueOrange: "134b852f78b35da2cb9dcae1d2e5eff2f0ebfce0bafbbf74e8932fc5690d994a07",
  brownBlueGreen: "704108a0651ac79548e3c78af3e6c6eef1eac9e9e48ed1c74da79e187a72025147",
  purpleGreen: "5b1667834792a67fb6c9aed3e6d6e8eff0efd9efd5aedda971bb75368e490e5e29",
  purpleOrange: "4114696647968f83b7b9b4d6dadbebf3eeeafce0bafbbf74e8932fc5690d994a07",
  redBlue: "8c0d25bf363adf745ef4ae91fbdbc9f2efeed2e5ef9dcae15da2cb2f78b3134b85",
  redGrey: "8c0d25bf363adf745ef4ae91fcdccbfaf4f1e2e2e2c0c0c0969696646464343434",
  yellowGreenBlue: "eff9bddbf1b4bde5b594d5b969c5be45b4c22c9ec02182b82163aa23479c1c3185",
  redYellowBlue: "a50026d4322cf16e43fcac64fedd90faf8c1dcf1ecabd6e875abd04a74b4313695",
  redYellowGreen: "a50026d4322cf16e43fcac63fedd8df9f7aed7ee8ea4d86e64bc6122964f006837",
  pinkYellowGreen: "8e0152c0267edd72adf0b3d6faddedf5f3efe1f2cab6de8780bb474f9125276419",
  spectral: "9e0142d13c4bf0704afcac63fedd8dfbf8b0e0f3a1a9dda269bda94288b55e4fa2",
  viridis: "440154470e61481a6c482575472f7d443a834144873d4e8a39568c35608d31688e2d708e2a788e27818e23888e21918d1f988b1fa08822a8842ab07f35b77943bf7154c56866cc5d7ad1518fd744a5db36bcdf27d2e21be9e51afde725",
  magma: "0000040404130b0924150e3720114b2c11603b0f704a107957157e651a80721f817f24828c29819a2e80a8327db6377ac43c75d1426fde4968e95462f1605df76f5cfa7f5efc8f65fe9f6dfeaf78febf84fece91fddea0fcedaffcfdbf",
  inferno: "0000040403130c0826170c3b240c4f330a5f420a68500d6c5d126e6b176e781c6d86216b932667a12b62ae305cbb3755c73e4cd24644dd513ae65c30ed6925f3771af8850ffb9506fca50afcb519fac62df6d645f2e661f3f484fcffa4",
  plasma: "0d088723069033059742039d5002a25d01a66a00a87801a88405a7900da49c179ea72198b12a90ba3488c33d80cb4779d35171da5a69e16462e76e5bed7953f2834cf68f44fa9a3dfca636fdb32ffec029fcce25f9dc24f5ea27f0f921",
  cividis: "",
  rainbow: "6e40aa883eb1a43db3bf3cafd83fa4ee4395fe4b83ff576eff6659ff7847ff8c38f3a130e2b72fcfcc36bee044aff05b8ff4576ff65b52f6673af27828ea8d1ddfa319d0b81cbecb23abd82f96e03d82e14c6edb5a5dd0664dbf6e40aa",
  sinebow: "ff4040fc582af47218e78d0bd5a703bfbf00a7d5038de70b72f41858fc2a40ff402afc5818f4720be78d03d5a700bfbf03a7d50b8de71872f42a58fc4040ff582afc7218f48d0be7a703d5bf00bfd503a7e70b8df41872fc2a58ff4040",
  turbo: "",
  browns: "eedbbdecca96e9b97ae4a865dc9856d18954c7784cc0673fb85536ad44339f3632",
  tealBlues: "bce4d89dd3d181c3cb65b3c245a2b9368fae347da0306a932c5985",
  teals: "bbdfdfa2d4d58ac9c975bcbb61b0af4da5a43799982b8b8c1e7f7f127273006667",
  warmGreys: "dcd4d0cec5c1c0b8b4b3aaa7a59c9998908c8b827f7e7673726866665c5a59504e",
  goldGreen: "f4d166d5ca60b6c35c98bb597cb25760a6564b9c533f8f4f33834a257740146c36",
  goldOrange: "f4d166f8be5cf8aa4cf5983bf3852aef701be2621fd65322c54923b142239e3a26",
  goldRed: "f4d166f6be59f9aa51fc964ef6834bee734ae56249db5247cf4244c43141b71d3e",
  lightGreyRed: "efe9e6e1dad7d5cbc8c8bdb9bbaea9cd967ddc7b43e15f19df4011dc000b",
  lightGreyTeal: "e4eaead6dcddc8ced2b7c2c7a6b4bc64b0bf22a6c32295c11f85be1876bc",
  lightMulti: "e0f1f2c4e9d0b0de9fd0e181f6e072f6c053f3993ef77440ef4a3c",
  lightOrange: "f2e7daf7d5baf9c499fab184fa9c73f68967ef7860e8645bde515bd43d5b",
  lightTealBlue: "e3e9e0c0dccf9aceca7abfc859afc0389fb9328dad2f7ca0276b95255988",
  darkBlue: "3232322d46681a5c930074af008cbf05a7ce25c0dd38daed50f3faffffff",
  darkGold: "3c3c3c584b37725e348c7631ae8b2bcfa424ecc31ef9de30fff184ffffff",
  darkGreen: "3a3a3a215748006f4d048942489e4276b340a6c63dd2d836ffeb2cffffaa",
  darkMulti: "3737371f5287197d8c29a86995ce3fffe800ffffff",
  darkRed: "3434347036339e3c38cc4037e75d1eec8620eeab29f0ce32ffeb2c"
};
var discrete = {
  accent: Accent_default,
  category10: category10_default,
  category20: "1f77b4aec7e8ff7f0effbb782ca02c98df8ad62728ff98969467bdc5b0d58c564bc49c94e377c2f7b6d27f7f7fc7c7c7bcbd22dbdb8d17becf9edae5",
  category20b: "393b795254a36b6ecf9c9ede6379398ca252b5cf6bcedb9c8c6d31bd9e39e7ba52e7cb94843c39ad494ad6616be7969c7b4173a55194ce6dbdde9ed6",
  category20c: "3182bd6baed69ecae1c6dbefe6550dfd8d3cfdae6bfdd0a231a35474c476a1d99bc7e9c0756bb19e9ac8bcbddcdadaeb636363969696bdbdbdd9d9d9",
  dark2: Dark2_default,
  observable10: observable10_default,
  paired: Paired_default,
  pastel1: Pastel1_default,
  pastel2: Pastel2_default,
  set1: Set1_default,
  set2: Set2_default,
  set3: Set3_default,
  tableau10: "4c78a8f58518e4575672b7b254a24beeca3bb279a2ff9da69d755dbab0ac",
  tableau20: "4c78a89ecae9f58518ffbf7954a24b88d27ab79a20f2cf5b43989483bcb6e45756ff9d9879706ebab0acd67195fcbfd2b279a2d6a5c99e765fd8b5a5"
};
function colors(palette) {
  if (isArray(palette)) return palette;
  const n = palette.length / 6 | 0, c3 = new Array(n);
  for (let i = 0; i < n; ) {
    c3[i] = "#" + palette.slice(i * 6, ++i * 6);
  }
  return c3;
}
function apply(_, f) {
  for (const k in _) scheme(k, f(_[k]));
}
var schemes = {};
apply(discrete, colors);
apply(continuous2, (_) => interpolateColors(colors(_)));
function scheme(name, scheme3) {
  name = name && name.toLowerCase();
  if (arguments.length > 1) {
    schemes[name] = scheme3;
    return this;
  } else {
    return schemes[name];
  }
}
var SymbolLegend = "symbol";
var DiscreteLegend = "discrete";
var GradientLegend = "gradient";
var defaultFormatter = (value4) => isArray(value4) ? value4.map((v2) => String(v2)) : String(value4);
var ascending3 = (a3, b10) => a3[1] - b10[1];
var descending2 = (a3, b10) => b10[1] - a3[1];
function tickCount(scale9, count2, minStep) {
  let step;
  if (isNumber(count2)) {
    if (scale9.bins) {
      count2 = Math.max(count2, scale9.bins.length);
    }
    if (minStep != null) {
      const domain4 = scale9.domain(), lo = Math.min(domain4[0], peek(domain4)), hi = Math.max(domain4[0], peek(domain4));
      count2 = Math.min(count2, Math.floor((hi - lo) / minStep || 1) + 1);
      if (!scale9.bins && !isLogarithmic(scale9.type) && !isTemporal(scale9.type) && lo < hi) {
        while (count2 > 1 && tickStep(lo, hi, count2) < minStep) --count2;
      }
    }
  }
  if (isObject(count2)) {
    step = count2.step;
    count2 = count2.interval;
  }
  if (isString(count2)) {
    count2 = scale9.type === Time ? timeInterval2(count2) : scale9.type == UTC ? utcInterval(count2) : error("Only time and utc scales accept interval strings.");
    if (step) count2 = count2.every(step);
  }
  return count2;
}
function validTicks(scale9, ticks2, count2) {
  let range7 = scale9.range(), lo = range7[0], hi = peek(range7), cmp = ascending3;
  if (lo > hi) {
    range7 = hi;
    hi = lo;
    lo = range7;
    cmp = descending2;
  }
  lo = Math.floor(lo);
  hi = Math.ceil(hi);
  ticks2 = ticks2.map((v2) => [v2, scale9(v2)]).filter((_) => lo <= _[1] && _[1] <= hi).sort(cmp).map((_) => _[0]);
  if (count2 > 0 && ticks2.length > 1) {
    const endpoints = [ticks2[0], peek(ticks2)];
    while (ticks2.length > count2 && ticks2.length >= 3) {
      ticks2 = ticks2.filter((_, i) => !(i % 2));
    }
    if (ticks2.length < 3) {
      ticks2 = endpoints;
    }
  }
  return ticks2;
}
function tickValues(scale9, count2) {
  return scale9.bins ? validTicks(scale9, scale9.bins, count2) : scale9.ticks ? scale9.ticks(count2) : scale9.domain();
}
function tickFormat2(locale4, scale9, count2, specifier, formatType, noSkip) {
  const type4 = scale9.type;
  let format6 = defaultFormatter;
  if (type4 === Time || formatType === Time) {
    format6 = locale4.timeFormat(specifier);
  } else if (type4 === UTC || formatType === UTC) {
    format6 = locale4.utcFormat(specifier);
  } else if (isLogarithmic(type4)) {
    const varfmt = locale4.formatFloat(specifier);
    if (noSkip || scale9.bins) {
      format6 = varfmt;
    } else {
      const test2 = tickLog(scale9, count2, false);
      format6 = (_) => test2(_) ? varfmt(_) : "";
    }
  } else if (scale9.tickFormat) {
    const d = scale9.domain();
    format6 = locale4.formatSpan(d[0], d[d.length - 1], count2, specifier);
  } else if (specifier) {
    format6 = locale4.format(specifier);
  }
  return format6;
}
function tickLog(scale9, count2, values4) {
  const ticks2 = tickValues(scale9, count2), base = scale9.base(), logb = Math.log(base), k = Math.max(1, base * count2 / ticks2.length);
  const test2 = (d) => {
    let i = d / Math.pow(base, Math.round(Math.log(d) / logb));
    if (i * base < base - 0.5) i *= base;
    return i <= k;
  };
  return values4 ? ticks2.filter(test2) : test2;
}
var symbols = {
  [Quantile2]: "quantiles",
  [Quantize]: "thresholds",
  [Threshold]: "domain"
};
var formats2 = {
  [Quantile2]: "quantiles",
  [Quantize]: "domain"
};
function labelValues(scale9, count2) {
  return scale9.bins ? binValues(scale9.bins) : scale9.type === Log ? tickLog(scale9, count2, true) : symbols[scale9.type] ? thresholdValues(scale9[symbols[scale9.type]]()) : tickValues(scale9, count2);
}
function thresholdFormat(locale4, scale9, specifier) {
  const _ = scale9[formats2[scale9.type]](), n = _.length;
  let d = n > 1 ? _[1] - _[0] : _[0], i;
  for (i = 1; i < n; ++i) {
    d = Math.min(d, _[i] - _[i - 1]);
  }
  return locale4.formatSpan(0, d, 3 * 10, specifier);
}
function thresholdValues(thresholds) {
  const values4 = [-Infinity].concat(thresholds);
  values4.max = Infinity;
  return values4;
}
function binValues(bins2) {
  const values4 = bins2.slice(0, -1);
  values4.max = peek(bins2);
  return values4;
}
var isDiscreteRange = (scale9) => symbols[scale9.type] || scale9.bins;
function labelFormat(locale4, scale9, count2, type4, specifier, formatType, noSkip) {
  const format6 = formats2[scale9.type] && formatType !== Time && formatType !== UTC ? thresholdFormat(locale4, scale9, specifier) : tickFormat2(locale4, scale9, count2, specifier, formatType, noSkip);
  return type4 === SymbolLegend && isDiscreteRange(scale9) ? formatRange(format6) : type4 === DiscreteLegend ? formatDiscrete(format6) : formatPoint(format6);
}
var formatRange = (format6) => (value4, index4, array4) => {
  const limit = get2(array4[index4 + 1], get2(array4.max, Infinity)), lo = formatValue(value4, format6), hi = formatValue(limit, format6);
  return lo && hi ? lo + " \u2013 " + hi : hi ? "< " + hi : "\u2265 " + lo;
};
var get2 = (value4, dflt) => value4 != null ? value4 : dflt;
var formatDiscrete = (format6) => (value4, index4) => index4 ? format6(value4) : null;
var formatPoint = (format6) => (value4) => format6(value4);
var formatValue = (value4, format6) => Number.isFinite(value4) ? format6(value4) : null;
function labelFraction(scale9) {
  const domain4 = scale9.domain(), count2 = domain4.length - 1;
  let lo = +domain4[0], hi = +peek(domain4), span = hi - lo;
  if (scale9.type === Threshold) {
    const adjust = count2 ? span / count2 : 0.1;
    lo -= adjust;
    hi += adjust;
    span = hi - lo;
  }
  return (value4) => (value4 - lo) / span;
}
function format3(locale4, scale9, specifier, formatType) {
  const type4 = formatType || scale9.type;
  if (isString(specifier) && isTemporal(type4)) {
    specifier = specifier.replace(/%a/g, "%A").replace(/%b/g, "%B");
  }
  return !specifier && type4 === Time ? locale4.timeFormat("%A, %d %B %Y, %X") : !specifier && type4 === UTC ? locale4.utcFormat("%A, %d %B %Y, %X UTC") : labelFormat(locale4, scale9, 5, null, specifier, formatType, true);
}
function domainCaption(locale4, scale9, opt) {
  opt = opt || {};
  const max4 = Math.max(3, opt.maxlen || 7), fmt = format3(locale4, scale9, opt.format, opt.formatType);
  if (isDiscretizing(scale9.type)) {
    const v2 = labelValues(scale9).slice(1).map(fmt), n = v2.length;
    return `${n} boundar${n === 1 ? "y" : "ies"}: ${v2.join(", ")}`;
  } else if (isDiscrete(scale9.type)) {
    const d = scale9.domain(), n = d.length, v2 = n > max4 ? d.slice(0, max4 - 2).map(fmt).join(", ") + ", ending with " + d.slice(-1).map(fmt) : d.map(fmt).join(", ");
    return `${n} value${n === 1 ? "" : "s"}: ${v2}`;
  } else {
    const d = scale9.domain();
    return `values from ${fmt(d[0])} to ${fmt(peek(d))}`;
  }
}

// ../deepseek-harness/node_modules/.pnpm/vega-scenegraph@5.3.0/node_modules/vega-scenegraph/build/vega-scenegraph.js
var gradient_id = 0;
var patternPrefix = "p_";
function isGradient(value4) {
  return value4 && value4.gradient;
}
function gradientRef(g, defs, base) {
  const type4 = g.gradient;
  let id2 = g.id, prefix = type4 === "radial" ? patternPrefix : "";
  if (!id2) {
    id2 = g.id = "gradient_" + gradient_id++;
    if (type4 === "radial") {
      g.x1 = get3(g.x1, 0.5);
      g.y1 = get3(g.y1, 0.5);
      g.r1 = get3(g.r1, 0);
      g.x2 = get3(g.x2, 0.5);
      g.y2 = get3(g.y2, 0.5);
      g.r2 = get3(g.r2, 0.5);
      prefix = patternPrefix;
    } else {
      g.x1 = get3(g.x1, 0);
      g.y1 = get3(g.y1, 0);
      g.x2 = get3(g.x2, 1);
      g.y2 = get3(g.y2, 0);
    }
  }
  defs[id2] = g;
  return "url(" + (base || "") + "#" + prefix + id2 + ")";
}
function get3(val, def3) {
  return val != null ? val : def3;
}
function Gradient(p02, p1) {
  var stops = [], gradient5;
  return gradient5 = {
    gradient: "linear",
    x1: p02 ? p02[0] : 0,
    y1: p02 ? p02[1] : 0,
    x2: p1 ? p1[0] : 1,
    y2: p1 ? p1[1] : 0,
    stops,
    stop: function(offset6, color6) {
      stops.push({
        offset: offset6,
        color: color6
      });
      return gradient5;
    }
  };
}
var lookup = {
  "basis": {
    curve: basis_default
  },
  "basis-closed": {
    curve: basisClosed_default
  },
  "basis-open": {
    curve: basisOpen_default
  },
  "bundle": {
    curve: bundle_default,
    tension: "beta",
    value: 0.85
  },
  "cardinal": {
    curve: cardinal_default,
    tension: "tension",
    value: 0
  },
  "cardinal-open": {
    curve: cardinalOpen_default,
    tension: "tension",
    value: 0
  },
  "cardinal-closed": {
    curve: cardinalClosed_default,
    tension: "tension",
    value: 0
  },
  "catmull-rom": {
    curve: catmullRom_default,
    tension: "alpha",
    value: 0.5
  },
  "catmull-rom-closed": {
    curve: catmullRomClosed_default,
    tension: "alpha",
    value: 0.5
  },
  "catmull-rom-open": {
    curve: catmullRomOpen_default,
    tension: "alpha",
    value: 0.5
  },
  "linear": {
    curve: linear_default
  },
  "linear-closed": {
    curve: linearClosed_default
  },
  "monotone": {
    horizontal: monotoneY,
    vertical: monotoneX
  },
  "natural": {
    curve: natural_default
  },
  "step": {
    curve: step_default
  },
  "step-after": {
    curve: stepAfter
  },
  "step-before": {
    curve: stepBefore
  }
};
function curves(type4, orientation, tension) {
  var entry2 = hasOwnProperty(lookup, type4) && lookup[type4], curve2 = null;
  if (entry2) {
    curve2 = entry2.curve || entry2[orientation || "vertical"];
    if (entry2.tension && tension != null) {
      curve2 = curve2[entry2.tension](tension);
    }
  }
  return curve2;
}
var paramCounts = {
  m: 2,
  l: 2,
  h: 1,
  v: 1,
  z: 0,
  c: 6,
  s: 4,
  q: 4,
  t: 2,
  a: 7
};
var commandPattern = /[mlhvzcsqta]([^mlhvzcsqta]*)/gi;
var numberPattern = /^[+-]?(([0-9]*\.[0-9]+)|([0-9]+\.)|([0-9]+))([eE][+-]?[0-9]+)?/;
var spacePattern = /^((\s+,?\s*)|(,\s*))/;
var flagPattern = /^[01]/;
function parse4(path4) {
  const commands = [];
  const matches = path4.match(commandPattern) || [];
  matches.forEach((str) => {
    let cmd = str[0];
    const type4 = cmd.toLowerCase();
    const paramCount = paramCounts[type4];
    const params2 = parseParams(type4, paramCount, str.slice(1).trim());
    const count2 = params2.length;
    if (count2 < paramCount || count2 && count2 % paramCount !== 0) {
      throw Error("Invalid SVG path, incorrect parameter count");
    }
    commands.push([cmd, ...params2.slice(0, paramCount)]);
    if (count2 === paramCount) {
      return;
    }
    if (type4 === "m") {
      cmd = cmd === "M" ? "L" : "l";
    }
    for (let i = paramCount; i < count2; i += paramCount) {
      commands.push([cmd, ...params2.slice(i, i + paramCount)]);
    }
  });
  return commands;
}
function parseParams(type4, paramCount, segment) {
  const params2 = [];
  for (let index4 = 0; paramCount && index4 < segment.length; ) {
    for (let i = 0; i < paramCount; ++i) {
      const pattern = type4 === "a" && (i === 3 || i === 4) ? flagPattern : numberPattern;
      const match3 = segment.slice(index4).match(pattern);
      if (match3 === null) {
        throw Error("Invalid SVG path, incorrect parameter type");
      }
      index4 += match3[0].length;
      params2.push(+match3[0]);
      const ws = segment.slice(index4).match(spacePattern);
      if (ws !== null) {
        index4 += ws[0].length;
      }
    }
  }
  return params2;
}
var DegToRad = Math.PI / 180;
var Epsilon = 1e-14;
var HalfPi = Math.PI / 2;
var Tau = Math.PI * 2;
var HalfSqrt3 = Math.sqrt(3) / 2;
var segmentCache = {};
var bezierCache = {};
var join = [].join;
function segments(x6, y6, rx, ry, large, sweep, rotateX, ox, oy) {
  const key2 = join.call(arguments);
  if (segmentCache[key2]) {
    return segmentCache[key2];
  }
  const th = rotateX * DegToRad;
  const sin_th = Math.sin(th);
  const cos_th = Math.cos(th);
  rx = Math.abs(rx);
  ry = Math.abs(ry);
  const px3 = cos_th * (ox - x6) * 0.5 + sin_th * (oy - y6) * 0.5;
  const py3 = cos_th * (oy - y6) * 0.5 - sin_th * (ox - x6) * 0.5;
  let pl = px3 * px3 / (rx * rx) + py3 * py3 / (ry * ry);
  if (pl > 1) {
    pl = Math.sqrt(pl);
    rx *= pl;
    ry *= pl;
  }
  const a00 = cos_th / rx;
  const a01 = sin_th / rx;
  const a10 = -sin_th / ry;
  const a11 = cos_th / ry;
  const x06 = a00 * ox + a01 * oy;
  const y06 = a10 * ox + a11 * oy;
  const x12 = a00 * x6 + a01 * y6;
  const y12 = a10 * x6 + a11 * y6;
  const d = (x12 - x06) * (x12 - x06) + (y12 - y06) * (y12 - y06);
  let sfactor_sq = 1 / d - 0.25;
  if (sfactor_sq < 0) sfactor_sq = 0;
  let sfactor = Math.sqrt(sfactor_sq);
  if (sweep == large) sfactor = -sfactor;
  const xc = 0.5 * (x06 + x12) - sfactor * (y12 - y06);
  const yc = 0.5 * (y06 + y12) + sfactor * (x12 - x06);
  const th0 = Math.atan2(y06 - yc, x06 - xc);
  const th1 = Math.atan2(y12 - yc, x12 - xc);
  let th_arc = th1 - th0;
  if (th_arc < 0 && sweep === 1) {
    th_arc += Tau;
  } else if (th_arc > 0 && sweep === 0) {
    th_arc -= Tau;
  }
  const segs = Math.ceil(Math.abs(th_arc / (HalfPi + 1e-3)));
  const result = [];
  for (let i = 0; i < segs; ++i) {
    const th2 = th0 + i * th_arc / segs;
    const th3 = th0 + (i + 1) * th_arc / segs;
    result[i] = [xc, yc, th2, th3, rx, ry, sin_th, cos_th];
  }
  return segmentCache[key2] = result;
}
function bezier(params2) {
  const key2 = join.call(params2);
  if (bezierCache[key2]) {
    return bezierCache[key2];
  }
  var cx = params2[0], cy = params2[1], th0 = params2[2], th1 = params2[3], rx = params2[4], ry = params2[5], sin_th = params2[6], cos_th = params2[7];
  const a00 = cos_th * rx;
  const a01 = -sin_th * ry;
  const a10 = sin_th * rx;
  const a11 = cos_th * ry;
  const cos_th0 = Math.cos(th0);
  const sin_th0 = Math.sin(th0);
  const cos_th1 = Math.cos(th1);
  const sin_th1 = Math.sin(th1);
  const th_half = 0.5 * (th1 - th0);
  const sin_th_h2 = Math.sin(th_half * 0.5);
  const t = 8 / 3 * sin_th_h2 * sin_th_h2 / Math.sin(th_half);
  const x12 = cx + cos_th0 - t * sin_th0;
  const y12 = cy + sin_th0 + t * cos_th0;
  const x32 = cx + cos_th1;
  const y32 = cy + sin_th1;
  const x22 = x32 + t * sin_th1;
  const y22 = y32 - t * cos_th1;
  return bezierCache[key2] = [a00 * x12 + a01 * y12, a10 * x12 + a11 * y12, a00 * x22 + a01 * y22, a10 * x22 + a11 * y22, a00 * x32 + a01 * y32, a10 * x32 + a11 * y32];
}
var temp = ["l", 0, 0, 0, 0, 0, 0, 0];
function scale$1(current2, sX, sY) {
  const c3 = temp[0] = current2[0];
  if (c3 === "a" || c3 === "A") {
    temp[1] = sX * current2[1];
    temp[2] = sY * current2[2];
    temp[3] = current2[3];
    temp[4] = current2[4];
    temp[5] = current2[5];
    temp[6] = sX * current2[6];
    temp[7] = sY * current2[7];
  } else if (c3 === "h" || c3 === "H") {
    temp[1] = sX * current2[1];
  } else if (c3 === "v" || c3 === "V") {
    temp[1] = sY * current2[1];
  } else {
    for (var i = 1, n = current2.length; i < n; ++i) {
      temp[i] = (i % 2 == 1 ? sX : sY) * current2[i];
    }
  }
  return temp;
}
function pathRender(context4, path4, l, t, sX, sY) {
  var current2, previous = null, x6 = 0, y6 = 0, controlX = 0, controlY = 0, tempX, tempY, tempControlX, tempControlY, anchorX = 0, anchorY = 0;
  if (l == null) l = 0;
  if (t == null) t = 0;
  if (sX == null) sX = 1;
  if (sY == null) sY = sX;
  if (context4.beginPath) context4.beginPath();
  for (var i = 0, len = path4.length; i < len; ++i) {
    current2 = path4[i];
    if (sX !== 1 || sY !== 1) {
      current2 = scale$1(current2, sX, sY);
    }
    switch (current2[0]) {
      // first letter
      case "l":
        x6 += current2[1];
        y6 += current2[2];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "L":
        x6 = current2[1];
        y6 = current2[2];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "h":
        x6 += current2[1];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "H":
        x6 = current2[1];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "v":
        y6 += current2[1];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "V":
        y6 = current2[1];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "m":
        x6 += current2[1];
        y6 += current2[2];
        anchorX = x6;
        anchorY = y6;
        context4.moveTo(x6 + l, y6 + t);
        break;
      case "M":
        x6 = current2[1];
        y6 = current2[2];
        anchorX = x6;
        anchorY = y6;
        context4.moveTo(x6 + l, y6 + t);
        break;
      case "c":
        tempX = x6 + current2[5];
        tempY = y6 + current2[6];
        controlX = x6 + current2[3];
        controlY = y6 + current2[4];
        context4.bezierCurveTo(
          x6 + current2[1] + l,
          // x1
          y6 + current2[2] + t,
          // y1
          controlX + l,
          // x2
          controlY + t,
          // y2
          tempX + l,
          tempY + t
        );
        x6 = tempX;
        y6 = tempY;
        break;
      case "C":
        x6 = current2[5];
        y6 = current2[6];
        controlX = current2[3];
        controlY = current2[4];
        context4.bezierCurveTo(current2[1] + l, current2[2] + t, controlX + l, controlY + t, x6 + l, y6 + t);
        break;
      case "s":
        tempX = x6 + current2[3];
        tempY = y6 + current2[4];
        controlX = 2 * x6 - controlX;
        controlY = 2 * y6 - controlY;
        context4.bezierCurveTo(controlX + l, controlY + t, x6 + current2[1] + l, y6 + current2[2] + t, tempX + l, tempY + t);
        controlX = x6 + current2[1];
        controlY = y6 + current2[2];
        x6 = tempX;
        y6 = tempY;
        break;
      case "S":
        tempX = current2[3];
        tempY = current2[4];
        controlX = 2 * x6 - controlX;
        controlY = 2 * y6 - controlY;
        context4.bezierCurveTo(controlX + l, controlY + t, current2[1] + l, current2[2] + t, tempX + l, tempY + t);
        x6 = tempX;
        y6 = tempY;
        controlX = current2[1];
        controlY = current2[2];
        break;
      case "q":
        tempX = x6 + current2[3];
        tempY = y6 + current2[4];
        controlX = x6 + current2[1];
        controlY = y6 + current2[2];
        context4.quadraticCurveTo(controlX + l, controlY + t, tempX + l, tempY + t);
        x6 = tempX;
        y6 = tempY;
        break;
      case "Q":
        tempX = current2[3];
        tempY = current2[4];
        context4.quadraticCurveTo(current2[1] + l, current2[2] + t, tempX + l, tempY + t);
        x6 = tempX;
        y6 = tempY;
        controlX = current2[1];
        controlY = current2[2];
        break;
      case "t":
        tempX = x6 + current2[1];
        tempY = y6 + current2[2];
        if (previous[0].match(/[QqTt]/) === null) {
          controlX = x6;
          controlY = y6;
        } else if (previous[0] === "t") {
          controlX = 2 * x6 - tempControlX;
          controlY = 2 * y6 - tempControlY;
        } else if (previous[0] === "q") {
          controlX = 2 * x6 - controlX;
          controlY = 2 * y6 - controlY;
        }
        tempControlX = controlX;
        tempControlY = controlY;
        context4.quadraticCurveTo(controlX + l, controlY + t, tempX + l, tempY + t);
        x6 = tempX;
        y6 = tempY;
        controlX = x6 + current2[1];
        controlY = y6 + current2[2];
        break;
      case "T":
        tempX = current2[1];
        tempY = current2[2];
        controlX = 2 * x6 - controlX;
        controlY = 2 * y6 - controlY;
        context4.quadraticCurveTo(controlX + l, controlY + t, tempX + l, tempY + t);
        x6 = tempX;
        y6 = tempY;
        break;
      case "a":
        drawArc(context4, x6 + l, y6 + t, [current2[1], current2[2], current2[3], current2[4], current2[5], current2[6] + x6 + l, current2[7] + y6 + t]);
        x6 += current2[6];
        y6 += current2[7];
        break;
      case "A":
        drawArc(context4, x6 + l, y6 + t, [current2[1], current2[2], current2[3], current2[4], current2[5], current2[6] + l, current2[7] + t]);
        x6 = current2[6];
        y6 = current2[7];
        break;
      case "z":
      case "Z":
        x6 = anchorX;
        y6 = anchorY;
        context4.closePath();
        break;
    }
    previous = current2;
  }
}
function drawArc(context4, x6, y6, coords) {
  const seg = segments(
    coords[5],
    // end x
    coords[6],
    // end y
    coords[0],
    // radius x
    coords[1],
    // radius y
    coords[3],
    // large flag
    coords[4],
    // sweep flag
    coords[2],
    // rotation
    x6,
    y6
  );
  for (let i = 0; i < seg.length; ++i) {
    const bez = bezier(seg[i]);
    context4.bezierCurveTo(bez[0], bez[1], bez[2], bez[3], bez[4], bez[5]);
  }
}
var Tan30 = 0.5773502691896257;
var builtins = {
  "circle": {
    draw: function(context4, size) {
      const r = Math.sqrt(size) / 2;
      context4.moveTo(r, 0);
      context4.arc(0, 0, r, 0, Tau);
    }
  },
  "cross": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, s = r / 2.5;
      context4.moveTo(-r, -s);
      context4.lineTo(-r, s);
      context4.lineTo(-s, s);
      context4.lineTo(-s, r);
      context4.lineTo(s, r);
      context4.lineTo(s, s);
      context4.lineTo(r, s);
      context4.lineTo(r, -s);
      context4.lineTo(s, -s);
      context4.lineTo(s, -r);
      context4.lineTo(-s, -r);
      context4.lineTo(-s, -s);
      context4.closePath();
    }
  },
  "diamond": {
    draw: function(context4, size) {
      const r = Math.sqrt(size) / 2;
      context4.moveTo(-r, 0);
      context4.lineTo(0, -r);
      context4.lineTo(r, 0);
      context4.lineTo(0, r);
      context4.closePath();
    }
  },
  "square": {
    draw: function(context4, size) {
      var w3 = Math.sqrt(size), x6 = -w3 / 2;
      context4.rect(x6, x6, w3, w3);
    }
  },
  "arrow": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, s = r / 7, t = r / 2.5, v2 = r / 8;
      context4.moveTo(-s, r);
      context4.lineTo(s, r);
      context4.lineTo(s, -v2);
      context4.lineTo(t, -v2);
      context4.lineTo(0, -r);
      context4.lineTo(-t, -v2);
      context4.lineTo(-s, -v2);
      context4.closePath();
    }
  },
  "wedge": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt3 * r, o = h3 - r * Tan30, b10 = r / 4;
      context4.moveTo(0, -h3 - o);
      context4.lineTo(-b10, h3 - o);
      context4.lineTo(b10, h3 - o);
      context4.closePath();
    }
  },
  "triangle": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt3 * r, o = h3 - r * Tan30;
      context4.moveTo(0, -h3 - o);
      context4.lineTo(-r, h3 - o);
      context4.lineTo(r, h3 - o);
      context4.closePath();
    }
  },
  "triangle-up": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt3 * r;
      context4.moveTo(0, -h3);
      context4.lineTo(-r, h3);
      context4.lineTo(r, h3);
      context4.closePath();
    }
  },
  "triangle-down": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt3 * r;
      context4.moveTo(0, h3);
      context4.lineTo(-r, -h3);
      context4.lineTo(r, -h3);
      context4.closePath();
    }
  },
  "triangle-right": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt3 * r;
      context4.moveTo(h3, 0);
      context4.lineTo(-h3, -r);
      context4.lineTo(-h3, r);
      context4.closePath();
    }
  },
  "triangle-left": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt3 * r;
      context4.moveTo(-h3, 0);
      context4.lineTo(h3, -r);
      context4.lineTo(h3, r);
      context4.closePath();
    }
  },
  "stroke": {
    draw: function(context4, size) {
      const r = Math.sqrt(size) / 2;
      context4.moveTo(-r, 0);
      context4.lineTo(r, 0);
    }
  }
};
function symbols2(_) {
  return hasOwnProperty(builtins, _) ? builtins[_] : customSymbol(_);
}
var custom8 = {};
function customSymbol(path4) {
  if (!hasOwnProperty(custom8, path4)) {
    const parsed = parse4(path4);
    custom8[path4] = {
      draw: function(context4, size) {
        pathRender(context4, parsed, 0, 0, Math.sqrt(size) / 2);
      }
    };
  }
  return custom8[path4];
}
var C2 = 0.448084975506;
function rectangleX(d) {
  return d.x;
}
function rectangleY(d) {
  return d.y;
}
function rectangleWidth(d) {
  return d.width;
}
function rectangleHeight(d) {
  return d.height;
}
function number4(_) {
  return typeof _ === "function" ? _ : () => +_;
}
function clamp2(value4, min4, max4) {
  return Math.max(min4, Math.min(value4, max4));
}
function vg_rect() {
  var x6 = rectangleX, y6 = rectangleY, width2 = rectangleWidth, height2 = rectangleHeight, crTL = number4(0), crTR = crTL, crBL = crTL, crBR = crTL, context4 = null;
  function rectangle3(_, x06, y06) {
    var buffer, x12 = x06 != null ? x06 : +x6.call(this, _), y12 = y06 != null ? y06 : +y6.call(this, _), w3 = +width2.call(this, _), h3 = +height2.call(this, _), s = Math.min(w3, h3) / 2, tl3 = clamp2(+crTL.call(this, _), 0, s), tr3 = clamp2(+crTR.call(this, _), 0, s), bl3 = clamp2(+crBL.call(this, _), 0, s), br3 = clamp2(+crBR.call(this, _), 0, s);
    if (!context4) context4 = buffer = path();
    if (tl3 <= 0 && tr3 <= 0 && bl3 <= 0 && br3 <= 0) {
      context4.rect(x12, y12, w3, h3);
    } else {
      var x22 = x12 + w3, y22 = y12 + h3;
      context4.moveTo(x12 + tl3, y12);
      context4.lineTo(x22 - tr3, y12);
      context4.bezierCurveTo(x22 - C2 * tr3, y12, x22, y12 + C2 * tr3, x22, y12 + tr3);
      context4.lineTo(x22, y22 - br3);
      context4.bezierCurveTo(x22, y22 - C2 * br3, x22 - C2 * br3, y22, x22 - br3, y22);
      context4.lineTo(x12 + bl3, y22);
      context4.bezierCurveTo(x12 + C2 * bl3, y22, x12, y22 - C2 * bl3, x12, y22 - bl3);
      context4.lineTo(x12, y12 + tl3);
      context4.bezierCurveTo(x12, y12 + C2 * tl3, x12 + C2 * tl3, y12, x12 + tl3, y12);
      context4.closePath();
    }
    if (buffer) {
      context4 = null;
      return buffer + "" || null;
    }
  }
  rectangle3.x = function(_) {
    if (arguments.length) {
      x6 = number4(_);
      return rectangle3;
    } else {
      return x6;
    }
  };
  rectangle3.y = function(_) {
    if (arguments.length) {
      y6 = number4(_);
      return rectangle3;
    } else {
      return y6;
    }
  };
  rectangle3.width = function(_) {
    if (arguments.length) {
      width2 = number4(_);
      return rectangle3;
    } else {
      return width2;
    }
  };
  rectangle3.height = function(_) {
    if (arguments.length) {
      height2 = number4(_);
      return rectangle3;
    } else {
      return height2;
    }
  };
  rectangle3.cornerRadius = function(tl3, tr3, br3, bl3) {
    if (arguments.length) {
      crTL = number4(tl3);
      crTR = tr3 != null ? number4(tr3) : crTL;
      crBR = br3 != null ? number4(br3) : crTL;
      crBL = bl3 != null ? number4(bl3) : crTR;
      return rectangle3;
    } else {
      return crTL;
    }
  };
  rectangle3.context = function(_) {
    if (arguments.length) {
      context4 = _ == null ? null : _;
      return rectangle3;
    } else {
      return context4;
    }
  };
  return rectangle3;
}
function vg_trail() {
  var x6, y6, size, defined2, context4 = null, ready, x12, y12, r1;
  function point9(x22, y22, w22) {
    const r2 = w22 / 2;
    if (ready) {
      var ux = y12 - y22, uy = x22 - x12;
      if (ux || uy) {
        var ud = Math.hypot(ux, uy), rx = (ux /= ud) * r1, ry = (uy /= ud) * r1, t = Math.atan2(uy, ux);
        context4.moveTo(x12 - rx, y12 - ry);
        context4.lineTo(x22 - ux * r2, y22 - uy * r2);
        context4.arc(x22, y22, r2, t - Math.PI, t);
        context4.lineTo(x12 + rx, y12 + ry);
        context4.arc(x12, y12, r1, t, t + Math.PI);
      } else {
        context4.arc(x22, y22, r2, 0, Tau);
      }
      context4.closePath();
    } else {
      ready = 1;
    }
    x12 = x22;
    y12 = y22;
    r1 = r2;
  }
  function trail4(data4) {
    var i, n = data4.length, d, defined0 = false, buffer;
    if (context4 == null) context4 = buffer = path();
    for (i = 0; i <= n; ++i) {
      if (!(i < n && defined2(d = data4[i], i, data4)) === defined0) {
        if (defined0 = !defined0) ready = 0;
      }
      if (defined0) point9(+x6(d, i, data4), +y6(d, i, data4), +size(d, i, data4));
    }
    if (buffer) {
      context4 = null;
      return buffer + "" || null;
    }
  }
  trail4.x = function(_) {
    if (arguments.length) {
      x6 = _;
      return trail4;
    } else {
      return x6;
    }
  };
  trail4.y = function(_) {
    if (arguments.length) {
      y6 = _;
      return trail4;
    } else {
      return y6;
    }
  };
  trail4.size = function(_) {
    if (arguments.length) {
      size = _;
      return trail4;
    } else {
      return size;
    }
  };
  trail4.defined = function(_) {
    if (arguments.length) {
      defined2 = _;
      return trail4;
    } else {
      return defined2;
    }
  };
  trail4.context = function(_) {
    if (arguments.length) {
      if (_ == null) {
        context4 = null;
      } else {
        context4 = _;
      }
      return trail4;
    } else {
      return context4;
    }
  };
  return trail4;
}
function value$1(a3, b10) {
  return a3 != null ? a3 : b10;
}
var x2 = (item) => item.x || 0;
var y2 = (item) => item.y || 0;
var w = (item) => item.width || 0;
var h = (item) => item.height || 0;
var xw = (item) => (item.x || 0) + (item.width || 0);
var yh = (item) => (item.y || 0) + (item.height || 0);
var sa = (item) => item.startAngle || 0;
var ea = (item) => item.endAngle || 0;
var pa = (item) => item.padAngle || 0;
var ir = (item) => item.innerRadius || 0;
var or = (item) => item.outerRadius || 0;
var cr = (item) => item.cornerRadius || 0;
var tl = (item) => value$1(item.cornerRadiusTopLeft, item.cornerRadius) || 0;
var tr = (item) => value$1(item.cornerRadiusTopRight, item.cornerRadius) || 0;
var br = (item) => value$1(item.cornerRadiusBottomRight, item.cornerRadius) || 0;
var bl = (item) => value$1(item.cornerRadiusBottomLeft, item.cornerRadius) || 0;
var sz = (item) => value$1(item.size, 64);
var ts = (item) => item.size || 1;
var def = (item) => !(item.defined === false);
var type = (item) => symbols2(item.shape || "circle");
var arcShape = arc_default().startAngle(sa).endAngle(ea).padAngle(pa).innerRadius(ir).outerRadius(or).cornerRadius(cr);
var areavShape = area_default().x(x2).y1(y2).y0(yh).defined(def);
var areahShape = area_default().y(y2).x1(x2).x0(xw).defined(def);
var lineShape = line_default().x(x2).y(y2).defined(def);
var rectShape = vg_rect().x(x2).y(y2).width(w).height(h).cornerRadius(tl, tr, br, bl);
var symbolShape = Symbol2().type(type).size(sz);
var trailShape = vg_trail().x(x2).y(y2).defined(def).size(ts);
function hasCornerRadius(item) {
  return item.cornerRadius || item.cornerRadiusTopLeft || item.cornerRadiusTopRight || item.cornerRadiusBottomRight || item.cornerRadiusBottomLeft;
}
function arc$1(context4, item) {
  return arcShape.context(context4)(item);
}
function area$1(context4, items) {
  const item = items[0], interp = item.interpolate || "linear";
  return (item.orient === "horizontal" ? areahShape : areavShape).curve(curves(interp, item.orient, item.tension)).context(context4)(items);
}
function line$1(context4, items) {
  const item = items[0], interp = item.interpolate || "linear";
  return lineShape.curve(curves(interp, item.orient, item.tension)).context(context4)(items);
}
function rectangle(context4, item, x6, y6) {
  return rectShape.context(context4)(item, x6, y6);
}
function shape$1(context4, item) {
  return (item.mark.shape || item.shape).context(context4)(item);
}
function symbol$1(context4, item) {
  return symbolShape.context(context4)(item);
}
function trail$1(context4, items) {
  return trailShape.context(context4)(items);
}
var clip_id = 1;
function clip$1(renderer, item, size) {
  var clip4 = item.clip, defs = renderer._defs, id2 = item.clip_id || (item.clip_id = "clip" + clip_id++), c3 = defs.clipping[id2] || (defs.clipping[id2] = {
    id: id2
  });
  if (isFunction(clip4)) {
    c3.path = clip4(null);
  } else if (hasCornerRadius(size)) {
    c3.path = rectangle(null, size, 0, 0);
  } else {
    c3.width = size.width || 0;
    c3.height = size.height || 0;
  }
  return "url(#" + id2 + ")";
}
function Bounds(b10) {
  this.clear();
  if (b10) this.union(b10);
}
Bounds.prototype = {
  clone() {
    return new Bounds(this);
  },
  clear() {
    this.x1 = +Number.MAX_VALUE;
    this.y1 = +Number.MAX_VALUE;
    this.x2 = -Number.MAX_VALUE;
    this.y2 = -Number.MAX_VALUE;
    return this;
  },
  empty() {
    return this.x1 === +Number.MAX_VALUE && this.y1 === +Number.MAX_VALUE && this.x2 === -Number.MAX_VALUE && this.y2 === -Number.MAX_VALUE;
  },
  equals(b10) {
    return this.x1 === b10.x1 && this.y1 === b10.y1 && this.x2 === b10.x2 && this.y2 === b10.y2;
  },
  set(x12, y12, x22, y22) {
    if (x22 < x12) {
      this.x2 = x12;
      this.x1 = x22;
    } else {
      this.x1 = x12;
      this.x2 = x22;
    }
    if (y22 < y12) {
      this.y2 = y12;
      this.y1 = y22;
    } else {
      this.y1 = y12;
      this.y2 = y22;
    }
    return this;
  },
  add(x6, y6) {
    if (x6 < this.x1) this.x1 = x6;
    if (y6 < this.y1) this.y1 = y6;
    if (x6 > this.x2) this.x2 = x6;
    if (y6 > this.y2) this.y2 = y6;
    return this;
  },
  expand(d) {
    this.x1 -= d;
    this.y1 -= d;
    this.x2 += d;
    this.y2 += d;
    return this;
  },
  round() {
    this.x1 = Math.floor(this.x1);
    this.y1 = Math.floor(this.y1);
    this.x2 = Math.ceil(this.x2);
    this.y2 = Math.ceil(this.y2);
    return this;
  },
  scale(s) {
    this.x1 *= s;
    this.y1 *= s;
    this.x2 *= s;
    this.y2 *= s;
    return this;
  },
  translate(dx, dy) {
    this.x1 += dx;
    this.x2 += dx;
    this.y1 += dy;
    this.y2 += dy;
    return this;
  },
  rotate(angle2, x6, y6) {
    const p = this.rotatedPoints(angle2, x6, y6);
    return this.clear().add(p[0], p[1]).add(p[2], p[3]).add(p[4], p[5]).add(p[6], p[7]);
  },
  rotatedPoints(angle2, x6, y6) {
    var {
      x1: x12,
      y1: y12,
      x2: x22,
      y2: y22
    } = this, cos4 = Math.cos(angle2), sin4 = Math.sin(angle2), cx = x6 - x6 * cos4 + y6 * sin4, cy = y6 - x6 * sin4 - y6 * cos4;
    return [cos4 * x12 - sin4 * y12 + cx, sin4 * x12 + cos4 * y12 + cy, cos4 * x12 - sin4 * y22 + cx, sin4 * x12 + cos4 * y22 + cy, cos4 * x22 - sin4 * y12 + cx, sin4 * x22 + cos4 * y12 + cy, cos4 * x22 - sin4 * y22 + cx, sin4 * x22 + cos4 * y22 + cy];
  },
  union(b10) {
    if (b10.x1 < this.x1) this.x1 = b10.x1;
    if (b10.y1 < this.y1) this.y1 = b10.y1;
    if (b10.x2 > this.x2) this.x2 = b10.x2;
    if (b10.y2 > this.y2) this.y2 = b10.y2;
    return this;
  },
  intersect(b10) {
    if (b10.x1 > this.x1) this.x1 = b10.x1;
    if (b10.y1 > this.y1) this.y1 = b10.y1;
    if (b10.x2 < this.x2) this.x2 = b10.x2;
    if (b10.y2 < this.y2) this.y2 = b10.y2;
    return this;
  },
  encloses(b10) {
    return b10 && this.x1 <= b10.x1 && this.x2 >= b10.x2 && this.y1 <= b10.y1 && this.y2 >= b10.y2;
  },
  alignsWith(b10) {
    return b10 && (this.x1 == b10.x1 || this.x2 == b10.x2 || this.y1 == b10.y1 || this.y2 == b10.y2);
  },
  intersects(b10) {
    return b10 && !(this.x2 < b10.x1 || this.x1 > b10.x2 || this.y2 < b10.y1 || this.y1 > b10.y2);
  },
  contains(x6, y6) {
    return !(x6 < this.x1 || x6 > this.x2 || y6 < this.y1 || y6 > this.y2);
  },
  width() {
    return this.x2 - this.x1;
  },
  height() {
    return this.y2 - this.y1;
  }
};
function Item(mark) {
  this.mark = mark;
  this.bounds = this.bounds || new Bounds();
}
function GroupItem(mark) {
  Item.call(this, mark);
  this.items = this.items || [];
}
inherits_default(GroupItem, Item);
var ResourceLoader = class {
  constructor(customLoader) {
    this._pending = 0;
    this._loader = customLoader || loader();
  }
  pending() {
    return this._pending;
  }
  sanitizeURL(uri) {
    const loader2 = this;
    increment(loader2);
    return loader2._loader.sanitize(uri, {
      context: "href"
    }).then((opt) => {
      decrement(loader2);
      return opt;
    }).catch(() => {
      decrement(loader2);
      return null;
    });
  }
  loadImage(uri) {
    const loader2 = this, Image2 = domImage();
    increment(loader2);
    return loader2._loader.sanitize(uri, {
      context: "image"
    }).then((opt) => {
      const url = opt.href;
      if (!url || !Image2) throw {
        url
      };
      const img = new Image2();
      const cors = hasOwnProperty(opt, "crossOrigin") ? opt.crossOrigin : "anonymous";
      if (cors != null) img.crossOrigin = cors;
      img.onload = () => decrement(loader2);
      img.onerror = () => decrement(loader2);
      img.src = url;
      return img;
    }).catch((e) => {
      decrement(loader2);
      return {
        complete: false,
        width: 0,
        height: 0,
        src: e && e.url || ""
      };
    });
  }
  ready() {
    const loader2 = this;
    return new Promise((accept) => {
      function poll(value4) {
        if (!loader2.pending()) accept(value4);
        else setTimeout(() => {
          poll(true);
        }, 10);
      }
      poll(false);
    });
  }
};
function increment(loader2) {
  loader2._pending += 1;
}
function decrement(loader2) {
  loader2._pending -= 1;
}
function boundStroke(bounds3, item, miter) {
  if (item.stroke && item.opacity !== 0 && item.strokeOpacity !== 0) {
    const sw = item.strokeWidth != null ? +item.strokeWidth : 1;
    let e = (item.strokeCap === "square" ? Math.SQRT2 : 1) * sw / 2;
    if (miter && (!item.strokeJoin || item.strokeJoin === "miter")) {
      e = Math.max(e, (item.strokeMiterLimit != null ? +item.strokeMiterLimit : 4) * sw / 2);
    }
    bounds3.expand(e);
  }
  return bounds3;
}
var circleThreshold = Tau - 1e-8;
var bounds;
var lx;
var ly;
var rot;
var ma;
var mb;
var mc;
var md;
var add3 = (x6, y6) => bounds.add(x6, y6);
var addL = (x6, y6) => add3(lx = x6, ly = y6);
var addX = (x6) => add3(x6, bounds.y1);
var addY = (y6) => add3(bounds.x1, y6);
var px = (x6, y6) => ma * x6 + mc * y6;
var py = (x6, y6) => mb * x6 + md * y6;
var addp = (x6, y6) => add3(px(x6, y6), py(x6, y6));
var addpL = (x6, y6) => addL(px(x6, y6), py(x6, y6));
function boundContext(_, deg) {
  bounds = _;
  if (deg) {
    rot = deg * DegToRad;
    ma = md = Math.cos(rot);
    mb = Math.sin(rot);
    mc = -mb;
  } else {
    ma = md = 1;
    rot = mb = mc = 0;
  }
  return context$1;
}
var context$1 = {
  beginPath() {
  },
  closePath() {
  },
  moveTo: addpL,
  lineTo: addpL,
  rect(x6, y6, w3, h3) {
    if (rot) {
      addp(x6 + w3, y6);
      addp(x6 + w3, y6 + h3);
      addp(x6, y6 + h3);
      addpL(x6, y6);
    } else {
      add3(x6 + w3, y6 + h3);
      addL(x6, y6);
    }
  },
  quadraticCurveTo(x12, y12, x22, y22) {
    const px1 = px(x12, y12), py1 = py(x12, y12), px22 = px(x22, y22), py22 = py(x22, y22);
    quadExtrema(lx, px1, px22, addX);
    quadExtrema(ly, py1, py22, addY);
    addL(px22, py22);
  },
  bezierCurveTo(x12, y12, x22, y22, x32, y32) {
    const px1 = px(x12, y12), py1 = py(x12, y12), px22 = px(x22, y22), py22 = py(x22, y22), px3 = px(x32, y32), py3 = py(x32, y32);
    cubicExtrema(lx, px1, px22, px3, addX);
    cubicExtrema(ly, py1, py22, py3, addY);
    addL(px3, py3);
  },
  arc(cx, cy, r, sa3, ea4, ccw) {
    sa3 += rot;
    ea4 += rot;
    lx = r * Math.cos(ea4) + cx;
    ly = r * Math.sin(ea4) + cy;
    if (Math.abs(ea4 - sa3) > circleThreshold) {
      add3(cx - r, cy - r);
      add3(cx + r, cy + r);
    } else {
      const update3 = (a3) => add3(r * Math.cos(a3) + cx, r * Math.sin(a3) + cy);
      let s, i;
      update3(sa3);
      update3(ea4);
      if (ea4 !== sa3) {
        sa3 = sa3 % Tau;
        if (sa3 < 0) sa3 += Tau;
        ea4 = ea4 % Tau;
        if (ea4 < 0) ea4 += Tau;
        if (ea4 < sa3) {
          ccw = !ccw;
          s = sa3;
          sa3 = ea4;
          ea4 = s;
        }
        if (ccw) {
          ea4 -= Tau;
          s = sa3 - sa3 % HalfPi;
          for (i = 0; i < 4 && s > ea4; ++i, s -= HalfPi) update3(s);
        } else {
          s = sa3 - sa3 % HalfPi + HalfPi;
          for (i = 0; i < 4 && s < ea4; ++i, s = s + HalfPi) update3(s);
        }
      }
    }
  }
};
function quadExtrema(x06, x12, x22, cb) {
  const t = (x06 - x12) / (x06 + x22 - 2 * x12);
  if (0 < t && t < 1) cb(x06 + (x12 - x06) * t);
}
function cubicExtrema(x06, x12, x22, x32, cb) {
  const a3 = x32 - x06 + 3 * x12 - 3 * x22, b10 = x06 + x22 - 2 * x12, c3 = x06 - x12;
  let t05 = 0, t13 = 0, r;
  if (Math.abs(a3) > Epsilon) {
    r = b10 * b10 + c3 * a3;
    if (r >= 0) {
      r = Math.sqrt(r);
      t05 = (-b10 + r) / a3;
      t13 = (-b10 - r) / a3;
    }
  } else {
    t05 = 0.5 * c3 / b10;
  }
  if (0 < t05 && t05 < 1) cb(cubic(t05, x06, x12, x22, x32));
  if (0 < t13 && t13 < 1) cb(cubic(t13, x06, x12, x22, x32));
}
function cubic(t, x06, x12, x22, x32) {
  const s = 1 - t, s2 = s * s, t22 = t * t;
  return s2 * s * x06 + 3 * s2 * t * x12 + 3 * s * t22 * x22 + t22 * t * x32;
}
var context = (context = domCanvas(1, 1)) ? context.getContext("2d") : null;
var b = new Bounds();
function intersectPath(draw4) {
  return function(item, brush) {
    if (!context) return true;
    draw4(context, item);
    b.clear().union(item.bounds).intersect(brush).round();
    const {
      x1: x12,
      y1: y12,
      x2: x22,
      y2: y22
    } = b;
    for (let y6 = y12; y6 <= y22; ++y6) {
      for (let x6 = x12; x6 <= x22; ++x6) {
        if (context.isPointInPath(x6, y6)) {
          return true;
        }
      }
    }
    return false;
  };
}
function intersectPoint(item, box) {
  return box.contains(item.x || 0, item.y || 0);
}
function intersectRect(item, box) {
  const x6 = item.x || 0, y6 = item.y || 0, w3 = item.width || 0, h3 = item.height || 0;
  return box.intersects(b.set(x6, y6, x6 + w3, y6 + h3));
}
function intersectRule(item, box) {
  const x6 = item.x || 0, y6 = item.y || 0, x22 = item.x2 != null ? item.x2 : x6, y22 = item.y2 != null ? item.y2 : y6;
  return intersectBoxLine(box, x6, y6, x22, y22);
}
function intersectBoxLine(box, x6, y6, u4, v2) {
  const {
    x1: x12,
    y1: y12,
    x2: x22,
    y2: y22
  } = box, dx = u4 - x6, dy = v2 - y6;
  let t05 = 0, t13 = 1, p, q, r, e;
  for (e = 0; e < 4; ++e) {
    if (e === 0) {
      p = -dx;
      q = -(x12 - x6);
    }
    if (e === 1) {
      p = dx;
      q = x22 - x6;
    }
    if (e === 2) {
      p = -dy;
      q = -(y12 - y6);
    }
    if (e === 3) {
      p = dy;
      q = y22 - y6;
    }
    if (Math.abs(p) < 1e-10 && q < 0) return false;
    r = q / p;
    if (p < 0) {
      if (r > t13) return false;
      else if (r > t05) t05 = r;
    } else if (p > 0) {
      if (r < t05) return false;
      else if (r < t13) t13 = r;
    }
  }
  return true;
}
function blend(context4, item) {
  context4.globalCompositeOperation = item.blend || "source-over";
}
function value(value4, dflt) {
  return value4 == null ? dflt : value4;
}
function addStops(gradient5, stops) {
  const n = stops.length;
  for (let i = 0; i < n; ++i) {
    gradient5.addColorStop(stops[i].offset, stops[i].color);
  }
  return gradient5;
}
function gradient(context4, spec, bounds3) {
  const w3 = bounds3.width(), h3 = bounds3.height();
  let gradient5;
  if (spec.gradient === "radial") {
    gradient5 = context4.createRadialGradient(bounds3.x1 + value(spec.x1, 0.5) * w3, bounds3.y1 + value(spec.y1, 0.5) * h3, Math.max(w3, h3) * value(spec.r1, 0), bounds3.x1 + value(spec.x2, 0.5) * w3, bounds3.y1 + value(spec.y2, 0.5) * h3, Math.max(w3, h3) * value(spec.r2, 0.5));
  } else {
    const x12 = value(spec.x1, 0), y12 = value(spec.y1, 0), x22 = value(spec.x2, 1), y22 = value(spec.y2, 0);
    if (x12 === x22 || y12 === y22 || w3 === h3) {
      gradient5 = context4.createLinearGradient(bounds3.x1 + x12 * w3, bounds3.y1 + y12 * h3, bounds3.x1 + x22 * w3, bounds3.y1 + y22 * h3);
    } else {
      const image4 = domCanvas(Math.ceil(w3), Math.ceil(h3)), ictx = image4.getContext("2d");
      ictx.scale(w3, h3);
      ictx.fillStyle = addStops(ictx.createLinearGradient(x12, y12, x22, y22), spec.stops);
      ictx.fillRect(0, 0, w3, h3);
      return context4.createPattern(image4, "no-repeat");
    }
  }
  return addStops(gradient5, spec.stops);
}
function color2(context4, item, value4) {
  return isGradient(value4) ? gradient(context4, value4, item.bounds) : value4;
}
function fill(context4, item, opacity2) {
  opacity2 *= item.fillOpacity == null ? 1 : item.fillOpacity;
  if (opacity2 > 0) {
    context4.globalAlpha = opacity2;
    context4.fillStyle = color2(context4, item, item.fill);
    return true;
  } else {
    return false;
  }
}
var Empty2 = [];
function stroke(context4, item, opacity2) {
  var lw = (lw = item.strokeWidth) != null ? lw : 1;
  if (lw <= 0) return false;
  opacity2 *= item.strokeOpacity == null ? 1 : item.strokeOpacity;
  if (opacity2 > 0) {
    context4.globalAlpha = opacity2;
    context4.strokeStyle = color2(context4, item, item.stroke);
    context4.lineWidth = lw;
    context4.lineCap = item.strokeCap || "butt";
    context4.lineJoin = item.strokeJoin || "miter";
    context4.miterLimit = item.strokeMiterLimit != null ? item.strokeMiterLimit : 4;
    if (context4.setLineDash) {
      context4.setLineDash(item.strokeDash || Empty2);
      context4.lineDashOffset = item.strokeDashOffset || 0;
    }
    return true;
  } else {
    return false;
  }
}
function compare2(a3, b10) {
  return a3.zindex - b10.zindex || a3.index - b10.index;
}
function zorder(scene) {
  if (!scene.zdirty) return scene.zitems;
  var items = scene.items, output3 = [], item, i, n;
  for (i = 0, n = items.length; i < n; ++i) {
    item = items[i];
    item.index = i;
    if (item.zindex) output3.push(item);
  }
  scene.zdirty = false;
  return scene.zitems = output3.sort(compare2);
}
function visit(scene, visitor) {
  var items = scene.items, i, n;
  if (!items || !items.length) return;
  const zitems = zorder(scene);
  if (zitems && zitems.length) {
    for (i = 0, n = items.length; i < n; ++i) {
      if (!items[i].zindex) visitor(items[i]);
    }
    items = zitems;
  }
  for (i = 0, n = items.length; i < n; ++i) {
    visitor(items[i]);
  }
}
function pickVisit(scene, visitor) {
  var items = scene.items, hit3, i;
  if (!items || !items.length) return null;
  const zitems = zorder(scene);
  if (zitems && zitems.length) items = zitems;
  for (i = items.length; --i >= 0; ) {
    if (hit3 = visitor(items[i])) return hit3;
  }
  if (items === zitems) {
    for (items = scene.items, i = items.length; --i >= 0; ) {
      if (!items[i].zindex) {
        if (hit3 = visitor(items[i])) return hit3;
      }
    }
  }
  return null;
}
function drawAll(path4) {
  return function(context4, scene, bounds3) {
    visit(scene, (item) => {
      if (!bounds3 || bounds3.intersects(item.bounds)) {
        drawPath(path4, context4, item, item);
      }
    });
  };
}
function drawOne(path4) {
  return function(context4, scene, bounds3) {
    if (scene.items.length && (!bounds3 || bounds3.intersects(scene.bounds))) {
      drawPath(path4, context4, scene.items[0], scene.items);
    }
  };
}
function drawPath(path4, context4, item, items) {
  var opacity2 = item.opacity == null ? 1 : item.opacity;
  if (opacity2 === 0) return;
  if (path4(context4, items)) return;
  blend(context4, item);
  if (item.fill && fill(context4, item, opacity2)) {
    context4.fill();
  }
  if (item.stroke && stroke(context4, item, opacity2)) {
    context4.stroke();
  }
}
function pick$1(test2) {
  test2 = test2 || truthy;
  return function(context4, scene, x6, y6, gx, gy) {
    x6 *= context4.pixelRatio;
    y6 *= context4.pixelRatio;
    return pickVisit(scene, (item) => {
      const b10 = item.bounds;
      if (b10 && !b10.contains(gx, gy) || !b10) return;
      if (test2(context4, item, x6, y6, gx, gy)) return item;
    });
  };
}
function hitPath(path4, filled) {
  return function(context4, o, x6, y6) {
    var item = Array.isArray(o) ? o[0] : o, fill3 = filled == null ? item.fill : filled, stroke3 = item.stroke && context4.isPointInStroke, lw, lc;
    if (stroke3) {
      lw = item.strokeWidth;
      lc = item.strokeCap;
      context4.lineWidth = lw != null ? lw : 1;
      context4.lineCap = lc != null ? lc : "butt";
    }
    return path4(context4, o) ? false : fill3 && context4.isPointInPath(x6, y6) || stroke3 && context4.isPointInStroke(x6, y6);
  };
}
function pickPath(path4) {
  return pick$1(hitPath(path4));
}
function translate(x6, y6) {
  return "translate(" + x6 + "," + y6 + ")";
}
function rotate(a3) {
  return "rotate(" + a3 + ")";
}
function scale2(scaleX, scaleY2) {
  return "scale(" + scaleX + "," + scaleY2 + ")";
}
function translateItem(item) {
  return translate(item.x || 0, item.y || 0);
}
function rotateItem(item) {
  return translate(item.x || 0, item.y || 0) + (item.angle ? " " + rotate(item.angle) : "");
}
function transformItem(item) {
  return translate(item.x || 0, item.y || 0) + (item.angle ? " " + rotate(item.angle) : "") + (item.scaleX || item.scaleY ? " " + scale2(item.scaleX || 1, item.scaleY || 1) : "");
}
function markItemPath(type4, shape3, isect) {
  function attr3(emit3, item) {
    emit3("transform", rotateItem(item));
    emit3("d", shape3(null, item));
  }
  function bound3(bounds3, item) {
    shape3(boundContext(bounds3, item.angle), item);
    return boundStroke(bounds3, item, true).translate(item.x || 0, item.y || 0);
  }
  function draw4(context4, item) {
    var x6 = item.x || 0, y6 = item.y || 0, a3 = item.angle || 0;
    context4.translate(x6, y6);
    if (a3) context4.rotate(a3 *= DegToRad);
    context4.beginPath();
    shape3(context4, item);
    if (a3) context4.rotate(-a3);
    context4.translate(-x6, -y6);
  }
  return {
    type: type4,
    tag: "path",
    nested: false,
    attr: attr3,
    bound: bound3,
    draw: drawAll(draw4),
    pick: pickPath(draw4),
    isect: isect || intersectPath(draw4)
  };
}
var arc = markItemPath("arc", arc$1);
function pickArea(a3, p) {
  var v2 = a3[0].orient === "horizontal" ? p[1] : p[0], z = a3[0].orient === "horizontal" ? "y" : "x", i = a3.length, min4 = Infinity, hit3, d;
  while (--i >= 0) {
    if (a3[i].defined === false) continue;
    d = Math.abs(a3[i][z] - v2);
    if (d < min4) {
      min4 = d;
      hit3 = a3[i];
    }
  }
  return hit3;
}
function pickLine(a3, p) {
  var t = Math.pow(a3[0].strokeWidth || 1, 2), i = a3.length, dx, dy, dd;
  while (--i >= 0) {
    if (a3[i].defined === false) continue;
    dx = a3[i].x - p[0];
    dy = a3[i].y - p[1];
    dd = dx * dx + dy * dy;
    if (dd < t) return a3[i];
  }
  return null;
}
function pickTrail(a3, p) {
  var i = a3.length, dx, dy, dd;
  while (--i >= 0) {
    if (a3[i].defined === false) continue;
    dx = a3[i].x - p[0];
    dy = a3[i].y - p[1];
    dd = dx * dx + dy * dy;
    dx = a3[i].size || 1;
    if (dd < dx * dx) return a3[i];
  }
  return null;
}
function markMultiItemPath(type4, shape3, tip) {
  function attr3(emit3, item) {
    var items = item.mark.items;
    if (items.length) emit3("d", shape3(null, items));
  }
  function bound3(bounds3, mark) {
    var items = mark.items;
    if (items.length === 0) {
      return bounds3;
    } else {
      shape3(boundContext(bounds3), items);
      return boundStroke(bounds3, items[0], true);
    }
  }
  function draw4(context4, items) {
    context4.beginPath();
    shape3(context4, items);
  }
  const hit3 = hitPath(draw4);
  function pick4(context4, scene, x6, y6, gx, gy) {
    var items = scene.items, b10 = scene.bounds;
    if (!items || !items.length || b10 && !b10.contains(gx, gy)) {
      return null;
    }
    x6 *= context4.pixelRatio;
    y6 *= context4.pixelRatio;
    return hit3(context4, items, x6, y6) ? items[0] : null;
  }
  return {
    type: type4,
    tag: "path",
    nested: true,
    attr: attr3,
    bound: bound3,
    draw: drawOne(draw4),
    pick: pick4,
    isect: intersectPoint,
    tip
  };
}
var area = markMultiItemPath("area", area$1, pickArea);
function clip(context4, scene) {
  var clip4 = scene.clip;
  context4.save();
  if (isFunction(clip4)) {
    context4.beginPath();
    clip4(context4);
    context4.clip();
  } else {
    clipGroup(context4, scene.group);
  }
}
function clipGroup(context4, group3) {
  context4.beginPath();
  hasCornerRadius(group3) ? rectangle(context4, group3, 0, 0) : context4.rect(0, 0, group3.width || 0, group3.height || 0);
  context4.clip();
}
function offset$1(item) {
  const sw = value(item.strokeWidth, 1);
  return item.strokeOffset != null ? item.strokeOffset : item.stroke && sw > 0.5 && sw < 1.5 ? 0.5 - Math.abs(sw - 1) : 0;
}
function attr$5(emit3, item) {
  emit3("transform", translateItem(item));
}
function emitRectangle(emit3, item) {
  const off = offset$1(item);
  emit3("d", rectangle(null, item, off, off));
}
function background(emit3, item) {
  emit3("class", "background");
  emit3("aria-hidden", true);
  emitRectangle(emit3, item);
}
function foreground(emit3, item) {
  emit3("class", "foreground");
  emit3("aria-hidden", true);
  if (item.strokeForeground) {
    emitRectangle(emit3, item);
  } else {
    emit3("d", "");
  }
}
function content(emit3, item, renderer) {
  const url = item.clip ? clip$1(renderer, item, item) : null;
  emit3("clip-path", url);
}
function bound$5(bounds3, group3) {
  if (!group3.clip && group3.items) {
    const items = group3.items, m3 = items.length;
    for (let j = 0; j < m3; ++j) {
      bounds3.union(items[j].bounds);
    }
  }
  if ((group3.clip || group3.width || group3.height) && !group3.noBound) {
    bounds3.add(0, 0).add(group3.width || 0, group3.height || 0);
  }
  boundStroke(bounds3, group3);
  return bounds3.translate(group3.x || 0, group3.y || 0);
}
function rectanglePath(context4, group3, x6, y6) {
  const off = offset$1(group3);
  context4.beginPath();
  rectangle(context4, group3, (x6 || 0) + off, (y6 || 0) + off);
}
var hitBackground = hitPath(rectanglePath);
var hitForeground = hitPath(rectanglePath, false);
var hitCorner = hitPath(rectanglePath, true);
function draw$4(context4, scene, bounds3, markTypes) {
  visit(scene, (group3) => {
    const gx = group3.x || 0, gy = group3.y || 0, fore = group3.strokeForeground, opacity2 = group3.opacity == null ? 1 : group3.opacity;
    if ((group3.stroke || group3.fill) && opacity2) {
      rectanglePath(context4, group3, gx, gy);
      blend(context4, group3);
      if (group3.fill && fill(context4, group3, opacity2)) {
        context4.fill();
      }
      if (group3.stroke && !fore && stroke(context4, group3, opacity2)) {
        context4.stroke();
      }
    }
    context4.save();
    context4.translate(gx, gy);
    if (group3.clip) clipGroup(context4, group3);
    if (bounds3) bounds3.translate(-gx, -gy);
    visit(group3, (item) => {
      if (item.marktype === "group" || markTypes == null || markTypes.includes(item.marktype)) {
        this.draw(context4, item, bounds3, markTypes);
      }
    });
    if (bounds3) bounds3.translate(gx, gy);
    context4.restore();
    if (fore && group3.stroke && opacity2) {
      rectanglePath(context4, group3, gx, gy);
      blend(context4, group3);
      if (stroke(context4, group3, opacity2)) {
        context4.stroke();
      }
    }
  });
}
function pick(context4, scene, x6, y6, gx, gy) {
  if (scene.bounds && !scene.bounds.contains(gx, gy) || !scene.items) {
    return null;
  }
  const cx = x6 * context4.pixelRatio, cy = y6 * context4.pixelRatio;
  return pickVisit(scene, (group3) => {
    let hit3, dx, dy;
    const b10 = group3.bounds;
    if (b10 && !b10.contains(gx, gy)) return;
    dx = group3.x || 0;
    dy = group3.y || 0;
    const dw = dx + (group3.width || 0), dh = dy + (group3.height || 0), c3 = group3.clip;
    if (c3 && (gx < dx || gx > dw || gy < dy || gy > dh)) return;
    context4.save();
    context4.translate(dx, dy);
    dx = gx - dx;
    dy = gy - dy;
    if (c3 && hasCornerRadius(group3) && !hitCorner(context4, group3, cx, cy)) {
      context4.restore();
      return null;
    }
    const fore = group3.strokeForeground, ix = scene.interactive !== false;
    if (ix && fore && group3.stroke && hitForeground(context4, group3, cx, cy)) {
      context4.restore();
      return group3;
    }
    hit3 = pickVisit(group3, (mark) => pickMark(mark, dx, dy) ? this.pick(mark, x6, y6, dx, dy) : null);
    if (!hit3 && ix && (group3.fill || !fore && group3.stroke) && hitBackground(context4, group3, cx, cy)) {
      hit3 = group3;
    }
    context4.restore();
    return hit3 || null;
  });
}
function pickMark(mark, x6, y6) {
  return (mark.interactive !== false || mark.marktype === "group") && mark.bounds && mark.bounds.contains(x6, y6);
}
var group = {
  type: "group",
  tag: "g",
  nested: false,
  attr: attr$5,
  bound: bound$5,
  draw: draw$4,
  pick,
  isect: intersectRect,
  content,
  background,
  foreground
};
var metadata = {
  "xmlns": "http://www.w3.org/2000/svg",
  "xmlns:xlink": "http://www.w3.org/1999/xlink",
  "version": "1.1"
};
function getImage(item, renderer) {
  var image4 = item.image;
  if (!image4 || item.url && item.url !== image4.url) {
    image4 = {
      complete: false,
      width: 0,
      height: 0
    };
    renderer.loadImage(item.url).then((image5) => {
      item.image = image5;
      item.image.url = item.url;
    });
  }
  return image4;
}
function imageWidth(item, image4) {
  return item.width != null ? item.width : !image4 || !image4.width ? 0 : item.aspect !== false && item.height ? item.height * image4.width / image4.height : image4.width;
}
function imageHeight(item, image4) {
  return item.height != null ? item.height : !image4 || !image4.height ? 0 : item.aspect !== false && item.width ? item.width * image4.height / image4.width : image4.height;
}
function imageXOffset(align2, w3) {
  return align2 === "center" ? w3 / 2 : align2 === "right" ? w3 : 0;
}
function imageYOffset(baseline3, h3) {
  return baseline3 === "middle" ? h3 / 2 : baseline3 === "bottom" ? h3 : 0;
}
function attr$4(emit3, item, renderer) {
  const img = getImage(item, renderer), w3 = imageWidth(item, img), h3 = imageHeight(item, img), x6 = (item.x || 0) - imageXOffset(item.align, w3), y6 = (item.y || 0) - imageYOffset(item.baseline, h3), i = !img.src && img.toDataURL ? img.toDataURL() : img.src || "";
  emit3("href", i, metadata["xmlns:xlink"], "xlink:href");
  emit3("transform", translate(x6, y6));
  emit3("width", w3);
  emit3("height", h3);
  emit3("preserveAspectRatio", item.aspect === false ? "none" : "xMidYMid");
}
function bound$4(bounds3, item) {
  const img = item.image, w3 = imageWidth(item, img), h3 = imageHeight(item, img), x6 = (item.x || 0) - imageXOffset(item.align, w3), y6 = (item.y || 0) - imageYOffset(item.baseline, h3);
  return bounds3.set(x6, y6, x6 + w3, y6 + h3);
}
function draw$3(context4, scene, bounds3) {
  visit(scene, (item) => {
    if (bounds3 && !bounds3.intersects(item.bounds)) return;
    const img = getImage(item, this);
    let w3 = imageWidth(item, img);
    let h3 = imageHeight(item, img);
    if (w3 === 0 || h3 === 0) return;
    let x6 = (item.x || 0) - imageXOffset(item.align, w3), y6 = (item.y || 0) - imageYOffset(item.baseline, h3), opacity2, ar0, ar1, t;
    if (item.aspect !== false) {
      ar0 = img.width / img.height;
      ar1 = item.width / item.height;
      if (ar0 === ar0 && ar1 === ar1 && ar0 !== ar1) {
        if (ar1 < ar0) {
          t = w3 / ar0;
          y6 += (h3 - t) / 2;
          h3 = t;
        } else {
          t = h3 * ar0;
          x6 += (w3 - t) / 2;
          w3 = t;
        }
      }
    }
    if (img.complete || img.toDataURL) {
      blend(context4, item);
      context4.globalAlpha = (opacity2 = item.opacity) != null ? opacity2 : 1;
      context4.imageSmoothingEnabled = item.smooth !== false;
      context4.drawImage(img, x6, y6, w3, h3);
    }
  });
}
var image = {
  type: "image",
  tag: "image",
  nested: false,
  attr: attr$4,
  bound: bound$4,
  draw: draw$3,
  pick: pick$1(),
  isect: truthy,
  // bounds check is sufficient
  get: getImage,
  xOffset: imageXOffset,
  yOffset: imageYOffset
};
var line = markMultiItemPath("line", line$1, pickLine);
function attr$3(emit3, item) {
  var sx = item.scaleX || 1, sy = item.scaleY || 1;
  if (sx !== 1 || sy !== 1) {
    emit3("vector-effect", "non-scaling-stroke");
  }
  emit3("transform", transformItem(item));
  emit3("d", item.path);
}
function path$1(context4, item) {
  var path4 = item.path;
  if (path4 == null) return true;
  var x6 = item.x || 0, y6 = item.y || 0, sx = item.scaleX || 1, sy = item.scaleY || 1, a3 = (item.angle || 0) * DegToRad, cache2 = item.pathCache;
  if (!cache2 || cache2.path !== path4) {
    (item.pathCache = cache2 = parse4(path4)).path = path4;
  }
  if (a3 && context4.rotate && context4.translate) {
    context4.translate(x6, y6);
    context4.rotate(a3);
    pathRender(context4, cache2, 0, 0, sx, sy);
    context4.rotate(-a3);
    context4.translate(-x6, -y6);
  } else {
    pathRender(context4, cache2, x6, y6, sx, sy);
  }
}
function bound$3(bounds3, item) {
  return path$1(boundContext(bounds3, item.angle), item) ? bounds3.set(0, 0, 0, 0) : boundStroke(bounds3, item, true);
}
var path$2 = {
  type: "path",
  tag: "path",
  nested: false,
  attr: attr$3,
  bound: bound$3,
  draw: drawAll(path$1),
  pick: pickPath(path$1),
  isect: intersectPath(path$1)
};
function attr$2(emit3, item) {
  emit3("d", rectangle(null, item));
}
function bound$2(bounds3, item) {
  var x6, y6;
  return boundStroke(bounds3.set(x6 = item.x || 0, y6 = item.y || 0, x6 + item.width || 0, y6 + item.height || 0), item);
}
function draw$2(context4, item) {
  context4.beginPath();
  rectangle(context4, item);
}
var rect = {
  type: "rect",
  tag: "path",
  nested: false,
  attr: attr$2,
  bound: bound$2,
  draw: drawAll(draw$2),
  pick: pickPath(draw$2),
  isect: intersectRect
};
function attr$1(emit3, item) {
  emit3("transform", translateItem(item));
  emit3("x2", item.x2 != null ? item.x2 - (item.x || 0) : 0);
  emit3("y2", item.y2 != null ? item.y2 - (item.y || 0) : 0);
}
function bound$1(bounds3, item) {
  var x12, y12;
  return boundStroke(bounds3.set(x12 = item.x || 0, y12 = item.y || 0, item.x2 != null ? item.x2 : x12, item.y2 != null ? item.y2 : y12), item);
}
function path2(context4, item, opacity2) {
  var x12, y12, x22, y22;
  if (item.stroke && stroke(context4, item, opacity2)) {
    x12 = item.x || 0;
    y12 = item.y || 0;
    x22 = item.x2 != null ? item.x2 : x12;
    y22 = item.y2 != null ? item.y2 : y12;
    context4.beginPath();
    context4.moveTo(x12, y12);
    context4.lineTo(x22, y22);
    return true;
  }
  return false;
}
function draw$1(context4, scene, bounds3) {
  visit(scene, (item) => {
    if (bounds3 && !bounds3.intersects(item.bounds)) return;
    var opacity2 = item.opacity == null ? 1 : item.opacity;
    if (opacity2 && path2(context4, item, opacity2)) {
      blend(context4, item);
      context4.stroke();
    }
  });
}
function hit$1(context4, item, x6, y6) {
  if (!context4.isPointInStroke) return false;
  return path2(context4, item, 1) && context4.isPointInStroke(x6, y6);
}
var rule = {
  type: "rule",
  tag: "line",
  nested: false,
  attr: attr$1,
  bound: bound$1,
  draw: draw$1,
  pick: pick$1(hit$1),
  isect: intersectRule
};
var shape = markItemPath("shape", shape$1);
var symbol = markItemPath("symbol", symbol$1, intersectPoint);
var widthCache = lruCache_default();
var textMetrics = {
  height: fontSize,
  measureWidth,
  estimateWidth,
  width: estimateWidth,
  canvas: useCanvas
};
useCanvas(true);
function useCanvas(use) {
  textMetrics.width = use && context ? measureWidth : estimateWidth;
}
function estimateWidth(item, text4) {
  return _estimateWidth(textValue(item, text4), fontSize(item));
}
function _estimateWidth(text4, currentFontHeight) {
  return ~~(0.8 * text4.length * currentFontHeight);
}
function measureWidth(item, text4) {
  return fontSize(item) <= 0 || !(text4 = textValue(item, text4)) ? 0 : _measureWidth(text4, font(item));
}
function _measureWidth(text4, currentFont) {
  const key2 = `(${currentFont}) ${text4}`;
  let width2 = widthCache.get(key2);
  if (width2 === void 0) {
    context.font = currentFont;
    width2 = context.measureText(text4).width;
    widthCache.set(key2, width2);
  }
  return width2;
}
function fontSize(item) {
  return item.fontSize != null ? +item.fontSize || 0 : 11;
}
function lineHeight(item) {
  return item.lineHeight != null ? item.lineHeight : fontSize(item) + 2;
}
function lineArray(_) {
  return isArray(_) ? _.length > 1 ? _ : _[0] : _;
}
function textLines(item) {
  return lineArray(item.lineBreak && item.text && !isArray(item.text) ? item.text.split(item.lineBreak) : item.text);
}
function multiLineOffset(item) {
  const tl3 = textLines(item);
  return (isArray(tl3) ? tl3.length - 1 : 0) * lineHeight(item);
}
function textValue(item, line5) {
  const text4 = line5 == null ? "" : (line5 + "").trim();
  return item.limit > 0 && text4.length ? truncate2(item, text4) : text4;
}
function widthGetter(item) {
  if (textMetrics.width === measureWidth) {
    const currentFont = font(item);
    return (text4) => _measureWidth(text4, currentFont);
  } else if (textMetrics.width === estimateWidth) {
    const currentFontHeight = fontSize(item);
    return (text4) => _estimateWidth(text4, currentFontHeight);
  } else {
    return (text4) => textMetrics.width(item, text4);
  }
}
function truncate2(item, text4) {
  var limit = +item.limit, width2 = widthGetter(item);
  if (width2(text4) < limit) return text4;
  var ellipsis = item.ellipsis || "\u2026", rtl = item.dir === "rtl", lo = 0, hi = text4.length, mid;
  limit -= width2(ellipsis);
  if (rtl) {
    while (lo < hi) {
      mid = lo + hi >>> 1;
      if (width2(text4.slice(mid)) > limit) lo = mid + 1;
      else hi = mid;
    }
    return ellipsis + text4.slice(lo);
  } else {
    while (lo < hi) {
      mid = 1 + (lo + hi >>> 1);
      if (width2(text4.slice(0, mid)) < limit) lo = mid;
      else hi = mid - 1;
    }
    return text4.slice(0, lo) + ellipsis;
  }
}
function fontFamily(item, quote) {
  var font3 = item.font;
  return (quote && font3 ? String(font3).replace(/"/g, "'") : font3) || "sans-serif";
}
function font(item, quote) {
  return (item.fontStyle ? item.fontStyle + " " : "") + (item.fontVariant ? item.fontVariant + " " : "") + (item.fontWeight ? item.fontWeight + " " : "") + fontSize(item) + "px " + fontFamily(item, quote);
}
function offset2(item) {
  var baseline3 = item.baseline, h3 = fontSize(item);
  return Math.round(baseline3 === "top" ? 0.79 * h3 : baseline3 === "middle" ? 0.3 * h3 : baseline3 === "bottom" ? -0.21 * h3 : baseline3 === "line-top" ? 0.29 * h3 + 0.5 * lineHeight(item) : baseline3 === "line-bottom" ? 0.29 * h3 - 0.5 * lineHeight(item) : 0);
}
var textAlign = {
  "left": "start",
  "center": "middle",
  "right": "end"
};
var tempBounds = new Bounds();
function anchorPoint(item) {
  var x6 = item.x || 0, y6 = item.y || 0, r = item.radius || 0, t;
  if (r) {
    t = (item.theta || 0) - HalfPi;
    x6 += r * Math.cos(t);
    y6 += r * Math.sin(t);
  }
  tempBounds.x1 = x6;
  tempBounds.y1 = y6;
  return tempBounds;
}
function attr(emit3, item) {
  var dx = item.dx || 0, dy = (item.dy || 0) + offset2(item), p = anchorPoint(item), x6 = p.x1, y6 = p.y1, a3 = item.angle || 0, t;
  emit3("text-anchor", textAlign[item.align] || "start");
  if (a3) {
    t = translate(x6, y6) + " " + rotate(a3);
    if (dx || dy) t += " " + translate(dx, dy);
  } else {
    t = translate(x6 + dx, y6 + dy);
  }
  emit3("transform", t);
}
function bound(bounds3, item, mode) {
  var h3 = textMetrics.height(item), a3 = item.align, p = anchorPoint(item), x6 = p.x1, y6 = p.y1, dx = item.dx || 0, dy = (item.dy || 0) + offset2(item) - Math.round(0.8 * h3), tl3 = textLines(item), w3;
  if (isArray(tl3)) {
    h3 += lineHeight(item) * (tl3.length - 1);
    w3 = tl3.reduce((w4, t) => Math.max(w4, textMetrics.width(item, t)), 0);
  } else {
    w3 = textMetrics.width(item, tl3);
  }
  if (a3 === "center") {
    dx -= w3 / 2;
  } else if (a3 === "right") {
    dx -= w3;
  } else ;
  bounds3.set(dx += x6, dy += y6, dx + w3, dy + h3);
  if (item.angle && !mode) {
    bounds3.rotate(item.angle * DegToRad, x6, y6);
  } else if (mode === 2) {
    return bounds3.rotatedPoints(item.angle * DegToRad, x6, y6);
  }
  return bounds3;
}
function draw(context4, scene, bounds3) {
  visit(scene, (item) => {
    var opacity2 = item.opacity == null ? 1 : item.opacity, p, x6, y6, i, lh, tl3, str;
    if (bounds3 && !bounds3.intersects(item.bounds) || // bounds check
    opacity2 === 0 || item.fontSize <= 0 || item.text == null || item.text.length === 0) return;
    context4.font = font(item);
    context4.textAlign = item.align || "left";
    p = anchorPoint(item);
    x6 = p.x1, y6 = p.y1;
    if (item.angle) {
      context4.save();
      context4.translate(x6, y6);
      context4.rotate(item.angle * DegToRad);
      x6 = y6 = 0;
    }
    x6 += item.dx || 0;
    y6 += (item.dy || 0) + offset2(item);
    tl3 = textLines(item);
    blend(context4, item);
    if (isArray(tl3)) {
      lh = lineHeight(item);
      for (i = 0; i < tl3.length; ++i) {
        str = textValue(item, tl3[i]);
        if (item.fill && fill(context4, item, opacity2)) {
          context4.fillText(str, x6, y6);
        }
        if (item.stroke && stroke(context4, item, opacity2)) {
          context4.strokeText(str, x6, y6);
        }
        y6 += lh;
      }
    } else {
      str = textValue(item, tl3);
      if (item.fill && fill(context4, item, opacity2)) {
        context4.fillText(str, x6, y6);
      }
      if (item.stroke && stroke(context4, item, opacity2)) {
        context4.strokeText(str, x6, y6);
      }
    }
    if (item.angle) context4.restore();
  });
}
function hit(context4, item, x6, y6, gx, gy) {
  if (item.fontSize <= 0) return false;
  if (!item.angle) return true;
  var p = anchorPoint(item), ax = p.x1, ay = p.y1, b10 = bound(tempBounds, item, 1), a3 = -item.angle * DegToRad, cos4 = Math.cos(a3), sin4 = Math.sin(a3), px3 = cos4 * gx - sin4 * gy + (ax - cos4 * ax + sin4 * ay), py3 = sin4 * gx + cos4 * gy + (ay - sin4 * ax - cos4 * ay);
  return b10.contains(px3, py3);
}
function intersectText(item, box) {
  const p = bound(tempBounds, item, 2);
  return intersectBoxLine(box, p[0], p[1], p[2], p[3]) || intersectBoxLine(box, p[0], p[1], p[4], p[5]) || intersectBoxLine(box, p[4], p[5], p[6], p[7]) || intersectBoxLine(box, p[2], p[3], p[6], p[7]);
}
var text = {
  type: "text",
  tag: "text",
  nested: false,
  attr,
  bound,
  draw,
  pick: pick$1(hit),
  isect: intersectText
};
var trail = markMultiItemPath("trail", trail$1, pickTrail);
var Marks = {
  arc,
  area,
  group,
  image,
  line,
  path: path$2,
  rect,
  rule,
  shape,
  symbol,
  text,
  trail
};
function domCreate(doc, tag, ns) {
  if (!doc && typeof document !== "undefined" && document.createElement) {
    doc = document;
  }
  return doc ? ns ? doc.createElementNS(ns, tag) : doc.createElement(tag) : null;
}
function domFind(el, tag) {
  tag = tag.toLowerCase();
  var nodes = el.childNodes, i = 0, n = nodes.length;
  for (; i < n; ++i) if (nodes[i].tagName.toLowerCase() === tag) {
    return nodes[i];
  }
}
function domChild(el, index4, tag, ns) {
  var a3 = el.childNodes[index4], b10;
  if (!a3 || a3.tagName.toLowerCase() !== tag.toLowerCase()) {
    b10 = a3 || null;
    a3 = domCreate(el.ownerDocument, tag, ns);
    el.insertBefore(a3, b10);
  }
  return a3;
}
function domClear(el, index4) {
  var nodes = el.childNodes, curr = nodes.length;
  while (curr > index4) el.removeChild(nodes[--curr]);
  return el;
}
function cssClass(mark) {
  return "mark-" + mark.marktype + (mark.role ? " role-" + mark.role : "") + (mark.name ? " " + mark.name : "");
}
function point6(event2, el) {
  if (el.getScreenCTM) {
    const p = ctmPoint(event2, el.getScreenCTM());
    if (p) return p;
  }
  return rectPoint(event2, el, el.getBoundingClientRect());
}
function ctmPoint(event2, ctm) {
  if (!ctm) return null;
  const det = ctm.a * ctm.d - ctm.b * ctm.c;
  if (!det) return null;
  const x6 = event2.clientX - ctm.e, y6 = event2.clientY - ctm.f;
  return [(ctm.d * x6 - ctm.c * y6) / det, (ctm.a * y6 - ctm.b * x6) / det];
}
function rectPoint(event2, el, rect4) {
  const sx = el.offsetWidth ? rect4.width / el.offsetWidth : 1, sy = el.offsetHeight ? rect4.height / el.offsetHeight : 1;
  return [(event2.clientX - rect4.left) / sx - (el.clientLeft || 0), (event2.clientY - rect4.top) / sy - (el.clientTop || 0)];
}
function resolveItem(item, event2, el, origin) {
  var mark = item && item.mark, mdef, p;
  if (mark && (mdef = Marks[mark.marktype]).tip) {
    p = point6(event2, el);
    p[0] -= origin[0];
    p[1] -= origin[1];
    while (item = item.mark.group) {
      p[0] -= item.x || 0;
      p[1] -= item.y || 0;
    }
    item = mdef.tip(mark.items, p);
  }
  return item;
}
var Handler = class {
  /**
   * Create a new Handler instance.
   * @param {object} [customLoader] - Optional loader instance for
   *   href URL sanitization. If not specified, a standard loader
   *   instance will be generated.
   * @param {function} [customTooltip] - Optional tooltip handler
   *   function for custom tooltip display.
   * @constructor
   */
  constructor(customLoader, customTooltip) {
    this._active = null;
    this._handlers = {};
    this._loader = customLoader || loader();
    this._tooltip = customTooltip || defaultTooltip;
  }
  /**
   * Initialize a new Handler instance.
   * @param {DOMElement} el - The containing DOM element for the display.
   * @param {Array<number>} origin - The origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {object} [obj] - Optional context object that should serve as
   *   the "this" context for event callbacks.
   * @return {Handler} - This handler instance.
   */
  initialize(el, origin, obj) {
    this._el = el;
    this._obj = obj || null;
    return this.origin(origin);
  }
  /**
   * Returns the parent container element for a visualization.
   * @return {DOMElement} - The containing DOM element.
   */
  element() {
    return this._el;
  }
  /**
   * Returns the scene element (e.g., canvas or SVG) of the visualization
   * Subclasses must override if the first child is not the scene element.
   * @return {DOMElement} - The scene (e.g., canvas or SVG) element.
   */
  canvas() {
    return this._el && this._el.firstChild;
  }
  /**
   * Get / set the origin coordinates of the visualization.
   */
  origin(origin) {
    if (arguments.length) {
      this._origin = origin || [0, 0];
      return this;
    } else {
      return this._origin.slice();
    }
  }
  /**
   * Get / set the scenegraph root.
   */
  scene(scene) {
    if (!arguments.length) return this._scene;
    this._scene = scene;
    return this;
  }
  /**
   * Add an event handler. Subclasses should override this method.
   */
  on() {
  }
  /**
   * Remove an event handler. Subclasses should override this method.
   */
  off() {
  }
  /**
   * Utility method for finding the array index of an event handler.
   * @param {Array} h - An array of registered event handlers.
   * @param {string} type - The event type.
   * @param {function} handler - The event handler instance to find.
   * @return {number} - The handler's array index or -1 if not registered.
   */
  _handlerIndex(h3, type4, handler) {
    for (let i = h3 ? h3.length : 0; --i >= 0; ) {
      if (h3[i].type === type4 && (!handler || h3[i].handler === handler)) {
        return i;
      }
    }
    return -1;
  }
  /**
   * Returns an array with registered event handlers.
   * @param {string} [type] - The event type to query. Any annotations
   *   are ignored; for example, for the argument "click.foo", ".foo" will
   *   be ignored and the method returns all "click" handlers. If type is
   *   null or unspecified, this method returns handlers for all types.
   * @return {Array} - A new array containing all registered event handlers.
   */
  handlers(type4) {
    const h3 = this._handlers, a3 = [];
    if (type4) {
      a3.push(...h3[this.eventName(type4)]);
    } else {
      for (const k in h3) {
        a3.push(...h3[k]);
      }
    }
    return a3;
  }
  /**
   * Parses an event name string to return the specific event type.
   * For example, given "click.foo" returns "click"
   * @param {string} name - The input event type string.
   * @return {string} - A string with the event type only.
   */
  eventName(name) {
    const i = name.indexOf(".");
    return i < 0 ? name : name.slice(0, i);
  }
  /**
   * Handle hyperlink navigation in response to an item.href value.
   * @param {Event} event - The event triggering hyperlink navigation.
   * @param {Item} item - The scenegraph item.
   * @param {string} href - The URL to navigate to.
   */
  handleHref(event2, item, href3) {
    this._loader.sanitize(href3, {
      context: "href"
    }).then((opt) => {
      const e = new MouseEvent(event2.type, event2), a3 = domCreate(null, "a");
      for (const name in opt) a3.setAttribute(name, opt[name]);
      a3.dispatchEvent(e);
    }).catch(() => {
    });
  }
  /**
   * Handle tooltip display in response to an item.tooltip value.
   * @param {Event} event - The event triggering tooltip display.
   * @param {Item} item - The scenegraph item.
   * @param {boolean} show - A boolean flag indicating whether
   *   to show or hide a tooltip for the given item.
   */
  handleTooltip(event2, item, show) {
    if (item && item.tooltip != null) {
      item = resolveItem(item, event2, this.canvas(), this._origin);
      const value4 = show && item && item.tooltip || null;
      this._tooltip.call(this._obj, this, event2, item, value4);
    }
  }
  /**
   * Returns the size of a scenegraph item and its position relative
   * to the viewport.
   * @param {Item} item - The scenegraph item.
   * @return {object} - A bounding box object (compatible with the
   *   DOMRect type) consisting of x, y, width, heigh, top, left,
   *   right, and bottom properties.
   */
  getItemBoundingClientRect(item) {
    const el = this.canvas();
    if (!el) return;
    const rect4 = el.getBoundingClientRect(), origin = this._origin, bounds3 = item.bounds, width2 = bounds3.width(), height2 = bounds3.height();
    let x6 = bounds3.x1 + origin[0] + rect4.left, y6 = bounds3.y1 + origin[1] + rect4.top;
    while (item.mark && (item = item.mark.group)) {
      x6 += item.x || 0;
      y6 += item.y || 0;
    }
    return {
      x: x6,
      y: y6,
      width: width2,
      height: height2,
      left: x6,
      top: y6,
      right: x6 + width2,
      bottom: y6 + height2
    };
  }
};
function defaultTooltip(handler, event2, item, value4) {
  handler.element().setAttribute("title", value4 || "");
}
var Renderer = class {
  /**
   * Create a new Renderer instance.
   * @param {object} [loader] - Optional loader instance for
   *   image and href URL sanitization. If not specified, a
   *   standard loader instance will be generated.
   * @constructor
   */
  constructor(loader2) {
    this._el = null;
    this._bgcolor = null;
    this._loader = new ResourceLoader(loader2);
  }
  /**
   * Initialize a new Renderer instance.
   * @param {DOMElement} el - The containing DOM element for the display.
   * @param {number} width - The coordinate width of the display, in pixels.
   * @param {number} height - The coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {Renderer} - This renderer instance.
   */
  initialize(el, width2, height2, origin, scaleFactor) {
    this._el = el;
    return this.resize(width2, height2, origin, scaleFactor);
  }
  /**
   * Returns the parent container element for a visualization.
   * @return {DOMElement} - The containing DOM element.
   */
  element() {
    return this._el;
  }
  /**
   * Returns the scene element (e.g., canvas or SVG) of the visualization
   * Subclasses must override if the first child is not the scene element.
   * @return {DOMElement} - The scene (e.g., canvas or SVG) element.
   */
  canvas() {
    return this._el && this._el.firstChild;
  }
  /**
   * Get / set the background color.
   */
  background(bgcolor) {
    if (arguments.length === 0) return this._bgcolor;
    this._bgcolor = bgcolor;
    return this;
  }
  /**
   * Resize the display.
   * @param {number} width - The new coordinate width of the display, in pixels.
   * @param {number} height - The new coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The new origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {Renderer} - This renderer instance;
   */
  resize(width2, height2, origin, scaleFactor) {
    this._width = width2;
    this._height = height2;
    this._origin = origin || [0, 0];
    this._scale = scaleFactor || 1;
    return this;
  }
  /**
   * Report a dirty item whose bounds should be redrawn.
   * This base class method does nothing. Subclasses that perform
   * incremental should implement this method.
   * @param {Item} item - The dirty item whose bounds should be redrawn.
   */
  dirty() {
  }
  /**
   * Render an input scenegraph, potentially with a set of dirty items.
   * This method will perform an immediate rendering with available resources.
   * The renderer may also need to perform image loading to perform a complete
   * render. This process can lead to asynchronous re-rendering of the scene
   * after this method returns. To receive notification when rendering is
   * complete, use the renderAsync method instead.
   * @param {object} scene - The root mark of a scenegraph to render.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   * @return {Renderer} - This renderer instance.
   */
  render(scene, markTypes) {
    const r = this;
    r._call = function() {
      r._render(scene, markTypes);
    };
    r._call();
    r._call = null;
    return r;
  }
  /**
   * Internal rendering method. Renderer subclasses should override this
   * method to actually perform rendering.
   * @param {object} scene - The root mark of a scenegraph to render.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   */
  _render() {
  }
  /**
   * Asynchronous rendering method. Similar to render, but returns a Promise
   * that resolves when all rendering is completed. Sometimes a renderer must
   * perform image loading to get a complete rendering. The returned
   * Promise will not resolve until this process completes.
   * @param {object} scene - The root mark of a scenegraph to render.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   * @return {Promise} - A Promise that resolves when rendering is complete.
   */
  renderAsync(scene, markTypes) {
    const r = this.render(scene, markTypes);
    return this._ready ? this._ready.then(() => r) : Promise.resolve(r);
  }
  /**
   * Internal method for asynchronous resource loading.
   * Proxies method calls to the ImageLoader, and tracks loading
   * progress to invoke a re-render once complete.
   * @param {string} method - The method name to invoke on the ImageLoader.
   * @param {string} uri - The URI for the requested resource.
   * @return {Promise} - A Promise that resolves to the requested resource.
   */
  _load(method2, uri) {
    var r = this, p = r._loader[method2](uri);
    if (!r._ready) {
      const call = r._call;
      r._ready = r._loader.ready().then((redraw) => {
        if (redraw) call();
        r._ready = null;
      });
    }
    return p;
  }
  /**
   * Sanitize a URL to include as a hyperlink in the rendered scene.
   * This method proxies a call to ImageLoader.sanitizeURL, but also tracks
   * image loading progress and invokes a re-render once complete.
   * @param {string} uri - The URI string to sanitize.
   * @return {Promise} - A Promise that resolves to the sanitized URL.
   */
  sanitizeURL(uri) {
    return this._load("sanitizeURL", uri);
  }
  /**
   * Requests an image to include in the rendered scene.
   * This method proxies a call to ImageLoader.loadImage, but also tracks
   * image loading progress and invokes a re-render once complete.
   * @param {string} uri - The URI string of the image.
   * @return {Promise} - A Promise that resolves to the loaded Image.
   */
  loadImage(uri) {
    return this._load("loadImage", uri);
  }
};
var KeyDownEvent = "keydown";
var KeyPressEvent = "keypress";
var KeyUpEvent = "keyup";
var DragEnterEvent = "dragenter";
var DragLeaveEvent = "dragleave";
var DragOverEvent = "dragover";
var PointerDownEvent = "pointerdown";
var PointerUpEvent = "pointerup";
var PointerMoveEvent = "pointermove";
var PointerOutEvent = "pointerout";
var PointerOverEvent = "pointerover";
var MouseDownEvent = "mousedown";
var MouseUpEvent = "mouseup";
var MouseMoveEvent = "mousemove";
var MouseOutEvent = "mouseout";
var MouseOverEvent = "mouseover";
var ClickEvent = "click";
var DoubleClickEvent = "dblclick";
var WheelEvent = "wheel";
var MouseWheelEvent = "mousewheel";
var TouchStartEvent = "touchstart";
var TouchMoveEvent = "touchmove";
var TouchEndEvent = "touchend";
var Events = [KeyDownEvent, KeyPressEvent, KeyUpEvent, DragEnterEvent, DragLeaveEvent, DragOverEvent, PointerDownEvent, PointerUpEvent, PointerMoveEvent, PointerOutEvent, PointerOverEvent, MouseDownEvent, MouseUpEvent, MouseMoveEvent, MouseOutEvent, MouseOverEvent, ClickEvent, DoubleClickEvent, WheelEvent, MouseWheelEvent, TouchStartEvent, TouchMoveEvent, TouchEndEvent];
var TooltipShowEvent = PointerMoveEvent;
var TooltipHideEvent = MouseOutEvent;
var HrefEvent = ClickEvent;
var CanvasHandler = class extends Handler {
  constructor(loader2, tooltip2) {
    super(loader2, tooltip2);
    this._down = null;
    this._touch = null;
    this._first = true;
    this._events = {};
    this.events = Events;
    this.pointermove = move([PointerMoveEvent, MouseMoveEvent], [PointerOverEvent, MouseOverEvent], [PointerOutEvent, MouseOutEvent]);
    this.dragover = move([DragOverEvent], [DragEnterEvent], [DragLeaveEvent]), this.pointerout = inactive([PointerOutEvent, MouseOutEvent]);
    this.dragleave = inactive([DragLeaveEvent]);
  }
  initialize(el, origin, obj) {
    this._canvas = el && domFind(el, "canvas");
    [ClickEvent, MouseDownEvent, PointerDownEvent, PointerMoveEvent, PointerOutEvent, DragLeaveEvent].forEach((type4) => eventListenerCheck(this, type4));
    return super.initialize(el, origin, obj);
  }
  // return the backing canvas instance
  canvas() {
    return this._canvas;
  }
  // retrieve the current canvas context
  context() {
    return this._canvas.getContext("2d");
  }
  // to keep old versions of firefox happy
  DOMMouseScroll(evt) {
    this.fire(MouseWheelEvent, evt);
  }
  pointerdown(evt) {
    this._down = this._active;
    this.fire(PointerDownEvent, evt);
  }
  mousedown(evt) {
    this._down = this._active;
    this.fire(MouseDownEvent, evt);
  }
  click(evt) {
    if (this._down === this._active) {
      this.fire(ClickEvent, evt);
      this._down = null;
    }
  }
  touchstart(evt) {
    this._touch = this.pickEvent(evt.changedTouches[0]);
    if (this._first) {
      this._active = this._touch;
      this._first = false;
    }
    this.fire(TouchStartEvent, evt, true);
  }
  touchmove(evt) {
    this.fire(TouchMoveEvent, evt, true);
  }
  touchend(evt) {
    this.fire(TouchEndEvent, evt, true);
    this._touch = null;
  }
  // fire an event
  fire(type4, evt, touch2) {
    const a3 = touch2 ? this._touch : this._active, h3 = this._handlers[type4];
    evt.vegaType = type4;
    if (type4 === HrefEvent && a3 && a3.href) {
      this.handleHref(evt, a3, a3.href);
    } else if (type4 === TooltipShowEvent || type4 === TooltipHideEvent) {
      this.handleTooltip(evt, a3, type4 !== TooltipHideEvent);
    }
    if (h3) {
      for (let i = 0, len = h3.length; i < len; ++i) {
        h3[i].handler.call(this._obj, evt, a3);
      }
    }
  }
  // add an event handler
  on(type4, handler) {
    const name = this.eventName(type4), h3 = this._handlers, i = this._handlerIndex(h3[name], type4, handler);
    if (i < 0) {
      eventListenerCheck(this, type4);
      (h3[name] || (h3[name] = [])).push({
        type: type4,
        handler
      });
    }
    return this;
  }
  // remove an event handler
  off(type4, handler) {
    const name = this.eventName(type4), h3 = this._handlers[name], i = this._handlerIndex(h3, type4, handler);
    if (i >= 0) {
      h3.splice(i, 1);
    }
    return this;
  }
  pickEvent(evt) {
    const p = point6(evt, this._canvas), o = this._origin;
    return this.pick(this._scene, p[0], p[1], p[0] - o[0], p[1] - o[1]);
  }
  // find the scenegraph item at the current pointer position
  // x, y -- the absolute x, y pointer coordinates on the canvas element
  // gx, gy -- the relative coordinates within the current group
  pick(scene, x6, y6, gx, gy) {
    const g = this.context(), mark = Marks[scene.marktype];
    return mark.pick.call(this, g, scene, x6, y6, gx, gy);
  }
};
var eventBundle = (type4) => type4 === TouchStartEvent || type4 === TouchMoveEvent || type4 === TouchEndEvent ? [TouchStartEvent, TouchMoveEvent, TouchEndEvent] : [type4];
function eventListenerCheck(handler, type4) {
  eventBundle(type4).forEach((_) => addEventListener(handler, _));
}
function addEventListener(handler, type4) {
  const canvas = handler.canvas();
  if (canvas && !handler._events[type4]) {
    handler._events[type4] = 1;
    canvas.addEventListener(type4, handler[type4] ? (evt) => handler[type4](evt) : (evt) => handler.fire(type4, evt));
  }
}
function fireAll(handler, types, event2) {
  types.forEach((type4) => handler.fire(type4, event2));
}
function move(moveEvents, overEvents, outEvents) {
  return function(evt) {
    const a3 = this._active, p = this.pickEvent(evt);
    if (p === a3) {
      fireAll(this, moveEvents, evt);
    } else {
      if (!a3 || !a3.exit) {
        fireAll(this, outEvents, evt);
      }
      this._active = p;
      fireAll(this, overEvents, evt);
      fireAll(this, moveEvents, evt);
    }
  };
}
function inactive(types) {
  return function(evt) {
    fireAll(this, types, evt);
    this._active = null;
  };
}
function devicePixelRatio() {
  return typeof window !== "undefined" ? window.devicePixelRatio || 1 : 1;
}
function resize(canvas, width2, height2, origin, scaleFactor, opt) {
  const inDOM = typeof HTMLElement !== "undefined" && canvas instanceof HTMLElement && canvas.parentNode != null, context4 = canvas.getContext("2d"), ratio = inDOM ? devicePixelRatio() : scaleFactor;
  canvas.width = width2 * ratio;
  canvas.height = height2 * ratio;
  for (const key2 in opt) {
    context4[key2] = opt[key2];
  }
  if (inDOM) {
    canvas.style.width = width2 + "px";
    canvas.style.height = height2 + "px";
  }
  context4.pixelRatio = ratio;
  context4.setTransform(ratio, 0, 0, ratio, ratio * origin[0], ratio * origin[1]);
  return canvas;
}
var CanvasRenderer = class extends Renderer {
  constructor(loader2) {
    super(loader2);
    this._options = {};
    this._redraw = false;
    this._dirty = new Bounds();
    this._tempb = new Bounds();
  }
  initialize(el, width2, height2, origin, scaleFactor, options) {
    this._options = options || {};
    this._canvas = this._options.externalContext ? null : domCanvas(1, 1, this._options.type);
    if (el && this._canvas) {
      domClear(el, 0).appendChild(this._canvas);
      this._canvas.setAttribute("class", "marks");
      this._canvas.style.setProperty("vertical-align", "bottom");
    }
    return super.initialize(el, width2, height2, origin, scaleFactor);
  }
  resize(width2, height2, origin, scaleFactor) {
    super.resize(width2, height2, origin, scaleFactor);
    if (this._canvas) {
      resize(this._canvas, this._width, this._height, this._origin, this._scale, this._options.context);
    } else {
      const ctx = this._options.externalContext;
      if (!ctx) error("CanvasRenderer is missing a valid canvas or context");
      ctx.scale(this._scale, this._scale);
      ctx.translate(this._origin[0], this._origin[1]);
    }
    this._redraw = true;
    return this;
  }
  canvas() {
    return this._canvas;
  }
  context() {
    return this._options.externalContext || (this._canvas ? this._canvas.getContext("2d") : null);
  }
  dirty(item) {
    const b10 = this._tempb.clear().union(item.bounds);
    let g = item.mark.group;
    while (g) {
      b10.translate(g.x || 0, g.y || 0);
      g = g.mark.group;
    }
    this._dirty.union(b10);
  }
  _render(scene, markTypes) {
    const g = this.context(), o = this._origin, w3 = this._width, h3 = this._height, db = this._dirty, vb = viewBounds(o, w3, h3);
    g.save();
    const b10 = this._redraw || db.empty() ? (this._redraw = false, vb.expand(1)) : clipToBounds(g, vb.intersect(db), o);
    this.clear(-o[0], -o[1], w3, h3);
    this.draw(g, scene, b10, markTypes);
    g.restore();
    db.clear();
    return this;
  }
  draw(ctx, scene, bounds3, markTypes) {
    if (scene.marktype !== "group" && markTypes != null && !markTypes.includes(scene.marktype)) {
      return;
    }
    const mark = Marks[scene.marktype];
    if (scene.clip) clip(ctx, scene);
    mark.draw.call(this, ctx, scene, bounds3, markTypes);
    if (scene.clip) ctx.restore();
  }
  clear(x6, y6, w3, h3) {
    const opt = this._options, g = this.context();
    if (opt.type !== "pdf" && !opt.externalContext) {
      g.clearRect(x6, y6, w3, h3);
    }
    if (this._bgcolor != null) {
      g.fillStyle = this._bgcolor;
      g.fillRect(x6, y6, w3, h3);
    }
  }
};
var viewBounds = (origin, width2, height2) => new Bounds().set(0, 0, width2, height2).translate(-origin[0], -origin[1]);
function clipToBounds(g, b10, origin) {
  b10.expand(1).round();
  if (g.pixelRatio % 1) {
    b10.scale(g.pixelRatio).round().scale(1 / g.pixelRatio);
  }
  b10.translate(-(origin[0] % 1), -(origin[1] % 1));
  g.beginPath();
  g.rect(b10.x1, b10.y1, b10.width(), b10.height());
  g.clip();
  return b10;
}
var SVGHandler = class extends Handler {
  constructor(loader2, tooltip2) {
    super(loader2, tooltip2);
    const h3 = this;
    h3._hrefHandler = listener(h3, (evt, item) => {
      if (item && item.href) h3.handleHref(evt, item, item.href);
    });
    h3._tooltipHandler = listener(h3, (evt, item) => {
      h3.handleTooltip(evt, item, evt.type !== TooltipHideEvent);
    });
  }
  initialize(el, origin, obj) {
    let svg = this._svg;
    if (svg) {
      svg.removeEventListener(HrefEvent, this._hrefHandler);
      svg.removeEventListener(TooltipShowEvent, this._tooltipHandler);
      svg.removeEventListener(TooltipHideEvent, this._tooltipHandler);
    }
    this._svg = svg = el && domFind(el, "svg");
    if (svg) {
      svg.addEventListener(HrefEvent, this._hrefHandler);
      svg.addEventListener(TooltipShowEvent, this._tooltipHandler);
      svg.addEventListener(TooltipHideEvent, this._tooltipHandler);
    }
    return super.initialize(el, origin, obj);
  }
  canvas() {
    return this._svg;
  }
  // add an event handler
  on(type4, handler) {
    const name = this.eventName(type4), h3 = this._handlers, i = this._handlerIndex(h3[name], type4, handler);
    if (i < 0) {
      const x6 = {
        type: type4,
        handler,
        listener: listener(this, handler)
      };
      (h3[name] || (h3[name] = [])).push(x6);
      if (this._svg) {
        this._svg.addEventListener(name, x6.listener);
      }
    }
    return this;
  }
  // remove an event handler
  off(type4, handler) {
    const name = this.eventName(type4), h3 = this._handlers[name], i = this._handlerIndex(h3, type4, handler);
    if (i >= 0) {
      if (this._svg) {
        this._svg.removeEventListener(name, h3[i].listener);
      }
      h3.splice(i, 1);
    }
    return this;
  }
};
var listener = (context4, handler) => (evt) => {
  let item = evt.target.__data__;
  item = Array.isArray(item) ? item[0] : item;
  evt.vegaType = evt.type;
  handler.call(context4._obj, evt, item);
};
var ARIA_HIDDEN = "aria-hidden";
var ARIA_LABEL = "aria-label";
var ARIA_ROLE = "role";
var ARIA_ROLEDESCRIPTION = "aria-roledescription";
var GRAPHICS_OBJECT = "graphics-object";
var GRAPHICS_SYMBOL = "graphics-symbol";
var bundle = (role, roledesc, label) => ({
  [ARIA_ROLE]: role,
  [ARIA_ROLEDESCRIPTION]: roledesc,
  [ARIA_LABEL]: label || void 0
});
var AriaIgnore = toSet(["axis-domain", "axis-grid", "axis-label", "axis-tick", "axis-title", "legend-band", "legend-entry", "legend-gradient", "legend-label", "legend-title", "legend-symbol", "title"]);
var AriaGuides = {
  "axis": {
    desc: "axis",
    caption: axisCaption
  },
  "legend": {
    desc: "legend",
    caption: legendCaption
  },
  "title-text": {
    desc: "title",
    caption: (item) => `Title text '${titleCaption(item)}'`
  },
  "title-subtitle": {
    desc: "subtitle",
    caption: (item) => `Subtitle text '${titleCaption(item)}'`
  }
};
var AriaEncode = {
  ariaRole: ARIA_ROLE,
  ariaRoleDescription: ARIA_ROLEDESCRIPTION,
  description: ARIA_LABEL
};
function ariaItemAttributes(emit3, item) {
  const hide = item.aria === false;
  emit3(ARIA_HIDDEN, hide || void 0);
  if (hide || item.description == null) {
    for (const prop in AriaEncode) {
      emit3(AriaEncode[prop], void 0);
    }
  } else {
    const type4 = item.mark.marktype;
    emit3(ARIA_LABEL, item.description);
    emit3(ARIA_ROLE, item.ariaRole || (type4 === "group" ? GRAPHICS_OBJECT : GRAPHICS_SYMBOL));
    emit3(ARIA_ROLEDESCRIPTION, item.ariaRoleDescription || `${type4} mark`);
  }
}
function ariaMarkAttributes(mark) {
  return mark.aria === false ? {
    [ARIA_HIDDEN]: true
  } : AriaIgnore[mark.role] ? null : AriaGuides[mark.role] ? ariaGuide(mark, AriaGuides[mark.role]) : ariaMark(mark);
}
function ariaMark(mark) {
  const type4 = mark.marktype;
  const recurse3 = type4 === "group" || type4 === "text" || mark.items.some((_) => _.description != null && _.aria !== false);
  return bundle(recurse3 ? GRAPHICS_OBJECT : GRAPHICS_SYMBOL, `${type4} mark container`, mark.description);
}
function ariaGuide(mark, opt) {
  try {
    const item = mark.items[0], caption = opt.caption || (() => "");
    return bundle(opt.role || GRAPHICS_SYMBOL, opt.desc, mark.description || item.description || caption(item));
  } catch (err) {
    return null;
  }
}
function titleCaption(item) {
  return array_default(item.text).join(" ");
}
function axisCaption(item) {
  const datum3 = item.datum, orient2 = item.orient, title2 = datum3.title ? extractTitle(item) : null, ctx = item.context, scale9 = ctx.scales[datum3.scale].value, locale4 = ctx.dataflow.locale(), type4 = scale9.type, xy = orient2 === "left" || orient2 === "right" ? "Y" : "X";
  return `${xy}-axis` + (title2 ? ` titled '${title2}'` : "") + ` for a ${isDiscrete(type4) ? "discrete" : type4} scale with ${domainCaption(locale4, scale9, item)}`;
}
function legendCaption(item) {
  const datum3 = item.datum, title2 = datum3.title ? extractTitle(item) : null, type4 = `${datum3.type || ""} legend`.trim(), scales2 = datum3.scales, props = Object.keys(scales2), ctx = item.context, scale9 = ctx.scales[scales2[props[0]]].value, locale4 = ctx.dataflow.locale();
  return capitalize(type4) + (title2 ? ` titled '${title2}'` : "") + ` for ${channelCaption(props)} with ${domainCaption(locale4, scale9, item)}`;
}
function extractTitle(item) {
  try {
    return array_default(peek(item.items).items[0].text).join(" ");
  } catch (err) {
    return null;
  }
}
function channelCaption(props) {
  props = props.map((p) => p + (p === "fill" || p === "stroke" ? " color" : ""));
  return props.length < 2 ? props[0] : props.slice(0, -1).join(", ") + " and " + peek(props);
}
function capitalize(s) {
  return s.length ? s[0].toUpperCase() + s.slice(1) : s;
}
var innerText = (val) => (val + "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
var attrText = (val) => innerText(val).replace(/"/g, "&quot;").replace(/\t/g, "&#x9;").replace(/\n/g, "&#xA;").replace(/\r/g, "&#xD;");
function markup() {
  let buf = "", outer = "", inner = "";
  const stack2 = [], clear2 = () => outer = inner = "", push = (tag) => {
    if (outer) {
      buf += `${outer}>${inner}`;
      clear2();
    }
    stack2.push(tag);
  }, attr3 = (name, value4) => {
    if (value4 != null) outer += ` ${name}="${attrText(value4)}"`;
    return m3;
  }, m3 = {
    open(tag, ...attrs) {
      push(tag);
      outer = "<" + tag;
      for (const set7 of attrs) {
        for (const key2 in set7) attr3(key2, set7[key2]);
      }
      return m3;
    },
    close() {
      const tag = stack2.pop();
      if (outer) {
        buf += outer + (inner ? `>${inner}</${tag}>` : "/>");
      } else {
        buf += `</${tag}>`;
      }
      clear2();
      return m3;
    },
    attr: attr3,
    text: (t) => (inner += innerText(t), m3),
    toString: () => buf
  };
  return m3;
}
var serializeXML = (node) => _serialize(markup(), node) + "";
function _serialize(m3, node) {
  m3.open(node.tagName);
  if (node.hasAttributes()) {
    const attrs = node.attributes, n = attrs.length;
    for (let i = 0; i < n; ++i) {
      m3.attr(attrs[i].name, attrs[i].value);
    }
  }
  if (node.hasChildNodes()) {
    const children4 = node.childNodes;
    for (const child of children4) {
      child.nodeType === 3 ? m3.text(child.nodeValue) : _serialize(m3, child);
    }
  }
  return m3.close();
}
var stylesAttr = {
  fill: "fill",
  fillOpacity: "fill-opacity",
  stroke: "stroke",
  strokeOpacity: "stroke-opacity",
  strokeWidth: "stroke-width",
  strokeCap: "stroke-linecap",
  strokeJoin: "stroke-linejoin",
  strokeDash: "stroke-dasharray",
  strokeDashOffset: "stroke-dashoffset",
  strokeMiterLimit: "stroke-miterlimit",
  opacity: "opacity"
};
var stylesCss = {
  blend: "mix-blend-mode"
};
var rootAttributes = {
  "fill": "none",
  "stroke-miterlimit": 4
};
var RootIndex = 0;
var xmlns = "http://www.w3.org/2000/xmlns/";
var svgns = metadata.xmlns;
var SVGRenderer = class extends Renderer {
  constructor(loader2) {
    super(loader2);
    this._dirtyID = 0;
    this._dirty = [];
    this._svg = null;
    this._root = null;
    this._defs = null;
  }
  /**
   * Initialize a new SVGRenderer instance.
   * @param {DOMElement} el - The containing DOM element for the display.
   * @param {number} width - The coordinate width of the display, in pixels.
   * @param {number} height - The coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {SVGRenderer} - This renderer instance.
   */
  initialize(el, width2, height2, origin, scaleFactor) {
    this._defs = {};
    this._clearDefs();
    if (el) {
      this._svg = domChild(el, 0, "svg", svgns);
      this._svg.setAttributeNS(xmlns, "xmlns", svgns);
      this._svg.setAttributeNS(xmlns, "xmlns:xlink", metadata["xmlns:xlink"]);
      this._svg.setAttribute("version", metadata["version"]);
      this._svg.setAttribute("class", "marks");
      this._svg.style.setProperty("vertical-align", "bottom");
      domClear(el, 1);
      this._root = domChild(this._svg, RootIndex, "g", svgns);
      setAttributes(this._root, rootAttributes);
      domClear(this._svg, RootIndex + 1);
    }
    this.background(this._bgcolor);
    return super.initialize(el, width2, height2, origin, scaleFactor);
  }
  /**
   * Get / set the background color.
   */
  background(bgcolor) {
    if (arguments.length && this._svg) {
      this._svg.style.setProperty("background-color", bgcolor);
    }
    return super.background(...arguments);
  }
  /**
   * Resize the display.
   * @param {number} width - The new coordinate width of the display, in pixels.
   * @param {number} height - The new coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The new origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {SVGRenderer} - This renderer instance;
   */
  resize(width2, height2, origin, scaleFactor) {
    super.resize(width2, height2, origin, scaleFactor);
    if (this._svg) {
      setAttributes(this._svg, {
        width: this._width * this._scale,
        height: this._height * this._scale,
        viewBox: `0 0 ${this._width} ${this._height}`
      });
      this._root.setAttribute("transform", `translate(${this._origin})`);
    }
    this._dirty = [];
    return this;
  }
  /**
   * Returns the SVG element of the visualization.
   * @return {DOMElement} - The SVG element.
   */
  canvas() {
    return this._svg;
  }
  /**
   * Returns an SVG text string for the rendered content,
   * or null if this renderer is currently headless.
   */
  svg() {
    const svg = this._svg, bg = this._bgcolor;
    if (!svg) return null;
    const style3 = svg.getAttribute("style");
    svg.removeAttribute("style");
    let node;
    if (bg) {
      node = domChild(svg, RootIndex, "rect", svgns);
      setAttributes(node, {
        width: this._width,
        height: this._height,
        fill: bg
      });
    }
    const text4 = serializeXML(svg);
    if (bg) svg.removeChild(node);
    if (style3) svg.setAttribute("style", style3);
    return text4;
  }
  /**
   * Internal rendering method.
   * @param {object} scene - The root mark of a scenegraph to render.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   */
  _render(scene, markTypes) {
    if (this._dirtyCheck()) {
      if (this._dirtyAll) this._clearDefs();
      this.mark(this._root, scene, void 0, markTypes);
      domClear(this._root, 1);
    }
    this.defs();
    this._dirty = [];
    ++this._dirtyID;
    return this;
  }
  // -- Manage rendering of items marked as dirty --
  /**
   * Flag a mark item as dirty.
   * @param {Item} item - The mark item.
   */
  dirty(item) {
    if (item.dirty !== this._dirtyID) {
      item.dirty = this._dirtyID;
      this._dirty.push(item);
    }
  }
  /**
   * Check if a mark item is considered dirty.
   * @param {Item} item - The mark item.
   */
  isDirty(item) {
    return this._dirtyAll || !item._svg || !item._svg.ownerSVGElement || item.dirty === this._dirtyID;
  }
  /**
   * Internal method to check dirty status and, if possible,
   * make targetted updates without a full rendering pass.
   */
  _dirtyCheck() {
    this._dirtyAll = true;
    const items = this._dirty;
    if (!items.length || !this._dirtyID) return true;
    const id2 = ++this._dirtyID;
    let item, mark, type4, mdef, i, n, o;
    for (i = 0, n = items.length; i < n; ++i) {
      item = items[i];
      mark = item.mark;
      if (mark.marktype !== type4) {
        type4 = mark.marktype;
        mdef = Marks[type4];
      }
      if (mark.zdirty && mark.dirty !== id2) {
        this._dirtyAll = false;
        dirtyParents(item, id2);
        mark.items.forEach((i2) => {
          i2.dirty = id2;
        });
      }
      if (mark.zdirty) continue;
      if (item.exit) {
        if (mdef.nested && mark.items.length) {
          o = mark.items[0];
          if (o._svg) this._update(mdef, o._svg, o);
        } else if (item._svg) {
          o = item._svg.parentNode;
          if (o) o.removeChild(item._svg);
        }
        item._svg = null;
        continue;
      }
      item = mdef.nested ? mark.items[0] : item;
      if (item._update === id2) continue;
      if (!item._svg || !item._svg.ownerSVGElement) {
        this._dirtyAll = false;
        dirtyParents(item, id2);
      } else {
        this._update(mdef, item._svg, item);
      }
      item._update = id2;
    }
    return !this._dirtyAll;
  }
  // -- Construct & maintain scenegraph to SVG mapping ---
  /**
   * Render a set of mark items.
   * @param {SVGElement} el - The parent element in the SVG tree.
   * @param {object} scene - The mark parent to render.
   * @param {SVGElement} prev - The previous sibling in the SVG tree.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   */
  mark(el, scene, prev, markTypes) {
    if (!this.isDirty(scene)) {
      return scene._svg;
    }
    const svg = this._svg, markType2 = scene.marktype, mdef = Marks[markType2], events3 = scene.interactive === false ? "none" : null, isGroup = mdef.tag === "g";
    const parent = bind(scene, el, prev, "g", svg);
    if (markType2 !== "group" && markTypes != null && !markTypes.includes(markType2)) {
      domClear(parent, 0);
      return scene._svg;
    }
    parent.setAttribute("class", cssClass(scene));
    const aria2 = ariaMarkAttributes(scene);
    for (const key2 in aria2) setAttribute(parent, key2, aria2[key2]);
    if (!isGroup) {
      setAttribute(parent, "pointer-events", events3);
    }
    setAttribute(parent, "clip-path", scene.clip ? clip$1(this, scene, scene.group) : null);
    let sibling = null, i = 0;
    const process = (item) => {
      const dirty = this.isDirty(item), node = bind(item, parent, sibling, mdef.tag, svg);
      if (dirty) {
        this._update(mdef, node, item);
        if (isGroup) recurse(this, node, item, markTypes);
      }
      sibling = node;
      ++i;
    };
    if (mdef.nested) {
      if (scene.items.length) process(scene.items[0]);
    } else {
      visit(scene, process);
    }
    domClear(parent, i);
    return parent;
  }
  /**
   * Update the attributes of an SVG element for a mark item.
   * @param {object} mdef - The mark definition object
   * @param {SVGElement} el - The SVG element.
   * @param {Item} item - The mark item.
   */
  _update(mdef, el, item) {
    element = el;
    values = el.__values__;
    ariaItemAttributes(emit, item);
    mdef.attr(emit, item, this);
    const extra = mark_extras[mdef.type];
    if (extra) extra.call(this, mdef, el, item);
    if (element) this.style(element, item);
  }
  /**
   * Update the presentation attributes of an SVG element for a mark item.
   * @param {SVGElement} el - The SVG element.
   * @param {Item} item - The mark item.
   */
  style(el, item) {
    if (item == null) return;
    for (const prop in stylesAttr) {
      let value4 = prop === "font" ? fontFamily(item) : item[prop];
      if (value4 === values[prop]) continue;
      const name = stylesAttr[prop];
      if (value4 == null) {
        el.removeAttribute(name);
      } else {
        if (isGradient(value4)) {
          value4 = gradientRef(value4, this._defs.gradient, href());
        }
        el.setAttribute(name, value4 + "");
      }
      values[prop] = value4;
    }
    for (const prop in stylesCss) {
      setStyle(el, stylesCss[prop], item[prop]);
    }
  }
  /**
   * Render SVG defs, as needed.
   * Must be called *after* marks have been processed to ensure the
   * collected state is current and accurate.
   */
  defs() {
    const svg = this._svg, defs = this._defs;
    let el = defs.el, index4 = 0;
    for (const id2 in defs.gradient) {
      if (!el) defs.el = el = domChild(svg, RootIndex + 1, "defs", svgns);
      index4 = updateGradient(el, defs.gradient[id2], index4);
    }
    for (const id2 in defs.clipping) {
      if (!el) defs.el = el = domChild(svg, RootIndex + 1, "defs", svgns);
      index4 = updateClipping(el, defs.clipping[id2], index4);
    }
    if (el) {
      index4 === 0 ? (svg.removeChild(el), defs.el = null) : domClear(el, index4);
    }
  }
  /**
   * Clear defs caches.
   */
  _clearDefs() {
    const def3 = this._defs;
    def3.gradient = {};
    def3.clipping = {};
  }
};
function dirtyParents(item, id2) {
  for (; item && item.dirty !== id2; item = item.mark.group) {
    item.dirty = id2;
    if (item.mark && item.mark.dirty !== id2) {
      item.mark.dirty = id2;
    } else return;
  }
}
function updateGradient(el, grad, index4) {
  let i, n, stop2;
  if (grad.gradient === "radial") {
    let pt = domChild(el, index4++, "pattern", svgns);
    setAttributes(pt, {
      id: patternPrefix + grad.id,
      viewBox: "0,0,1,1",
      width: "100%",
      height: "100%",
      preserveAspectRatio: "xMidYMid slice"
    });
    pt = domChild(pt, 0, "rect", svgns);
    setAttributes(pt, {
      width: 1,
      height: 1,
      fill: `url(${href()}#${grad.id})`
    });
    el = domChild(el, index4++, "radialGradient", svgns);
    setAttributes(el, {
      id: grad.id,
      fx: grad.x1,
      fy: grad.y1,
      fr: grad.r1,
      cx: grad.x2,
      cy: grad.y2,
      r: grad.r2
    });
  } else {
    el = domChild(el, index4++, "linearGradient", svgns);
    setAttributes(el, {
      id: grad.id,
      x1: grad.x1,
      x2: grad.x2,
      y1: grad.y1,
      y2: grad.y2
    });
  }
  for (i = 0, n = grad.stops.length; i < n; ++i) {
    stop2 = domChild(el, i, "stop", svgns);
    stop2.setAttribute("offset", grad.stops[i].offset);
    stop2.setAttribute("stop-color", grad.stops[i].color);
  }
  domClear(el, i);
  return index4;
}
function updateClipping(el, clip4, index4) {
  let mask;
  el = domChild(el, index4, "clipPath", svgns);
  el.setAttribute("id", clip4.id);
  if (clip4.path) {
    mask = domChild(el, 0, "path", svgns);
    mask.setAttribute("d", clip4.path);
  } else {
    mask = domChild(el, 0, "rect", svgns);
    setAttributes(mask, {
      x: 0,
      y: 0,
      width: clip4.width,
      height: clip4.height
    });
  }
  domClear(el, 1);
  return index4 + 1;
}
function recurse(renderer, el, group3, markTypes) {
  el = el.lastChild.previousSibling;
  let prev, idx = 0;
  visit(group3, (item) => {
    prev = renderer.mark(el, item, prev, markTypes);
    ++idx;
  });
  domClear(el, 1 + idx);
}
function bind(item, el, sibling, tag, svg) {
  let node = item._svg, doc;
  if (!node) {
    doc = el.ownerDocument;
    node = domCreate(doc, tag, svgns);
    item._svg = node;
    if (item.mark) {
      node.__data__ = item;
      node.__values__ = {
        fill: "default"
      };
      if (tag === "g") {
        const bg = domCreate(doc, "path", svgns);
        node.appendChild(bg);
        bg.__data__ = item;
        const cg = domCreate(doc, "g", svgns);
        node.appendChild(cg);
        cg.__data__ = item;
        const fg = domCreate(doc, "path", svgns);
        node.appendChild(fg);
        fg.__data__ = item;
        fg.__values__ = {
          fill: "default"
        };
      }
    }
  }
  if (node.ownerSVGElement !== svg || siblingCheck(node, sibling)) {
    el.insertBefore(node, sibling ? sibling.nextSibling : el.firstChild);
  }
  return node;
}
function siblingCheck(node, sibling) {
  return node.parentNode && node.parentNode.childNodes.length > 1 && node.previousSibling != sibling;
}
var element = null;
var values = null;
var mark_extras = {
  group(mdef, el, item) {
    const fg = element = el.childNodes[2];
    values = fg.__values__;
    mdef.foreground(emit, item, this);
    values = el.__values__;
    element = el.childNodes[1];
    mdef.content(emit, item, this);
    const bg = element = el.childNodes[0];
    mdef.background(emit, item, this);
    const value4 = item.mark.interactive === false ? "none" : null;
    if (value4 !== values.events) {
      setAttribute(fg, "pointer-events", value4);
      setAttribute(bg, "pointer-events", value4);
      values.events = value4;
    }
    if (item.strokeForeground && item.stroke) {
      const fill3 = item.fill;
      setAttribute(fg, "display", null);
      this.style(bg, item);
      setAttribute(bg, "stroke", null);
      if (fill3) item.fill = null;
      values = fg.__values__;
      this.style(fg, item);
      if (fill3) item.fill = fill3;
      element = null;
    } else {
      setAttribute(fg, "display", "none");
    }
  },
  image(mdef, el, item) {
    if (item.smooth === false) {
      setStyle(el, "image-rendering", "optimizeSpeed");
      setStyle(el, "image-rendering", "pixelated");
    } else {
      setStyle(el, "image-rendering", null);
    }
  },
  text(mdef, el, item) {
    const tl3 = textLines(item);
    let key2, value4, doc, lh;
    if (isArray(tl3)) {
      value4 = tl3.map((_) => textValue(item, _));
      key2 = value4.join("\n");
      if (key2 !== values.text) {
        domClear(el, 0);
        doc = el.ownerDocument;
        lh = lineHeight(item);
        value4.forEach((t, i) => {
          const ts3 = domCreate(doc, "tspan", svgns);
          ts3.__data__ = item;
          ts3.textContent = t;
          if (i) {
            ts3.setAttribute("x", 0);
            ts3.setAttribute("dy", lh);
          }
          el.appendChild(ts3);
        });
        values.text = key2;
      }
    } else {
      value4 = textValue(item, tl3);
      if (value4 !== values.text) {
        el.textContent = value4;
        values.text = value4;
      }
    }
    setAttribute(el, "font-family", fontFamily(item));
    setAttribute(el, "font-size", fontSize(item) + "px");
    setAttribute(el, "font-style", item.fontStyle);
    setAttribute(el, "font-variant", item.fontVariant);
    setAttribute(el, "font-weight", item.fontWeight);
  }
};
function emit(name, value4, ns) {
  if (value4 === values[name]) return;
  if (ns) {
    setAttributeNS(element, name, value4, ns);
  } else {
    setAttribute(element, name, value4);
  }
  values[name] = value4;
}
function setStyle(el, name, value4) {
  if (value4 !== values[name]) {
    if (value4 == null) {
      el.style.removeProperty(name);
    } else {
      el.style.setProperty(name, value4 + "");
    }
    values[name] = value4;
  }
}
function setAttributes(el, attrs) {
  for (const key2 in attrs) {
    setAttribute(el, key2, attrs[key2]);
  }
}
function setAttribute(el, name, value4) {
  if (value4 != null) {
    el.setAttribute(name, value4);
  } else {
    el.removeAttribute(name);
  }
}
function setAttributeNS(el, name, value4, ns) {
  if (value4 != null) {
    el.setAttributeNS(ns, name, value4);
  } else {
    el.removeAttributeNS(ns, name);
  }
}
function href() {
  let loc;
  return typeof window === "undefined" ? "" : (loc = window.location).hash ? loc.href.slice(0, -loc.hash.length) : loc.href;
}
var SVGStringRenderer = class extends Renderer {
  constructor(loader2) {
    super(loader2);
    this._text = null;
    this._defs = {
      gradient: {},
      clipping: {}
    };
  }
  /**
   * Returns the rendered SVG text string,
   * or null if rendering has not yet occurred.
   */
  svg() {
    return this._text;
  }
  /**
   * Internal rendering method.
   * @param {object} scene - The root mark of a scenegraph to render.
   */
  _render(scene) {
    const m3 = markup();
    m3.open("svg", extend({}, metadata, {
      class: "marks",
      width: this._width * this._scale,
      height: this._height * this._scale,
      viewBox: `0 0 ${this._width} ${this._height}`
    }));
    const bg = this._bgcolor;
    if (bg && bg !== "transparent" && bg !== "none") {
      m3.open("rect", {
        width: this._width,
        height: this._height,
        fill: bg
      }).close();
    }
    m3.open("g", rootAttributes, {
      transform: "translate(" + this._origin + ")"
    });
    this.mark(m3, scene);
    m3.close();
    this.defs(m3);
    this._text = m3.close() + "";
    return this;
  }
  /**
   * Render a set of mark items.
   * @param {object} m - The markup context.
   * @param {object} scene - The mark parent to render.
   */
  mark(m3, scene) {
    const mdef = Marks[scene.marktype], tag = mdef.tag, attrList = [ariaItemAttributes, mdef.attr];
    m3.open("g", {
      "class": cssClass(scene),
      "clip-path": scene.clip ? clip$1(this, scene, scene.group) : null
    }, ariaMarkAttributes(scene), {
      "pointer-events": tag !== "g" && scene.interactive === false ? "none" : null
    });
    const process = (item) => {
      const href3 = this.href(item);
      if (href3) m3.open("a", href3);
      m3.open(tag, this.attr(scene, item, attrList, tag !== "g" ? tag : null));
      if (tag === "text") {
        const tl3 = textLines(item);
        if (isArray(tl3)) {
          const attrs = {
            x: 0,
            dy: lineHeight(item)
          };
          for (let i = 0; i < tl3.length; ++i) {
            m3.open("tspan", i ? attrs : null).text(textValue(item, tl3[i])).close();
          }
        } else {
          m3.text(textValue(item, tl3));
        }
      } else if (tag === "g") {
        const fore = item.strokeForeground, fill3 = item.fill, stroke3 = item.stroke;
        if (fore && stroke3) {
          item.stroke = null;
        }
        m3.open("path", this.attr(scene, item, mdef.background, "bgrect")).close();
        m3.open("g", this.attr(scene, item, mdef.content));
        visit(item, (scene2) => this.mark(m3, scene2));
        m3.close();
        if (fore && stroke3) {
          if (fill3) item.fill = null;
          item.stroke = stroke3;
          m3.open("path", this.attr(scene, item, mdef.foreground, "bgrect")).close();
          if (fill3) item.fill = fill3;
        } else {
          m3.open("path", this.attr(scene, item, mdef.foreground, "bgfore")).close();
        }
      }
      m3.close();
      if (href3) m3.close();
    };
    if (mdef.nested) {
      if (scene.items && scene.items.length) process(scene.items[0]);
    } else {
      visit(scene, process);
    }
    return m3.close();
  }
  /**
   * Get href attributes for a hyperlinked mark item.
   * @param {Item} item - The mark item.
   */
  href(item) {
    const href3 = item.href;
    let attr3;
    if (href3) {
      if (attr3 = this._hrefs && this._hrefs[href3]) {
        return attr3;
      } else {
        this.sanitizeURL(href3).then((attr4) => {
          attr4["xlink:href"] = attr4.href;
          attr4.href = null;
          (this._hrefs || (this._hrefs = {}))[href3] = attr4;
        });
      }
    }
    return null;
  }
  /**
   * Get an object of SVG attributes for a mark item.
   * @param {object} scene - The mark parent.
   * @param {Item} item - The mark item.
   * @param {array|function} attrs - One or more attribute emitters.
   * @param {string} tag - The tag being rendered.
   */
  attr(scene, item, attrs, tag) {
    const object2 = {}, emit3 = (name, value4, ns, prefixed) => {
      object2[prefixed || name] = value4;
    };
    if (Array.isArray(attrs)) {
      attrs.forEach((fn) => fn(emit3, item, this));
    } else {
      attrs(emit3, item, this);
    }
    if (tag) {
      style(object2, item, scene, tag, this._defs);
    }
    return object2;
  }
  /**
   * Render SVG defs, as needed.
   * Must be called *after* marks have been processed to ensure the
   * collected state is current and accurate.
   * @param {object} m - The markup context.
   */
  defs(m3) {
    const gradient5 = this._defs.gradient, clipping = this._defs.clipping, count2 = Object.keys(gradient5).length + Object.keys(clipping).length;
    if (count2 === 0) return;
    m3.open("defs");
    for (const id2 in gradient5) {
      const def3 = gradient5[id2], stops = def3.stops;
      if (def3.gradient === "radial") {
        m3.open("pattern", {
          id: patternPrefix + id2,
          viewBox: "0,0,1,1",
          width: "100%",
          height: "100%",
          preserveAspectRatio: "xMidYMid slice"
        });
        m3.open("rect", {
          width: "1",
          height: "1",
          fill: "url(#" + id2 + ")"
        }).close();
        m3.close();
        m3.open("radialGradient", {
          id: id2,
          fx: def3.x1,
          fy: def3.y1,
          fr: def3.r1,
          cx: def3.x2,
          cy: def3.y2,
          r: def3.r2
        });
      } else {
        m3.open("linearGradient", {
          id: id2,
          x1: def3.x1,
          x2: def3.x2,
          y1: def3.y1,
          y2: def3.y2
        });
      }
      for (let i = 0; i < stops.length; ++i) {
        m3.open("stop", {
          offset: stops[i].offset,
          "stop-color": stops[i].color
        }).close();
      }
      m3.close();
    }
    for (const id2 in clipping) {
      const def3 = clipping[id2];
      m3.open("clipPath", {
        id: id2
      });
      if (def3.path) {
        m3.open("path", {
          d: def3.path
        }).close();
      } else {
        m3.open("rect", {
          x: 0,
          y: 0,
          width: def3.width,
          height: def3.height
        }).close();
      }
      m3.close();
    }
    m3.close();
  }
};
function style(s, item, scene, tag, defs) {
  let styleList;
  if (item == null) return s;
  if (tag === "bgrect" && scene.interactive === false) {
    s["pointer-events"] = "none";
  }
  if (tag === "bgfore") {
    if (scene.interactive === false) {
      s["pointer-events"] = "none";
    }
    s.display = "none";
    if (item.fill !== null) return s;
  }
  if (tag === "image" && item.smooth === false) {
    styleList = ["image-rendering: optimizeSpeed;", "image-rendering: pixelated;"];
  }
  if (tag === "text") {
    s["font-family"] = fontFamily(item);
    s["font-size"] = fontSize(item) + "px";
    s["font-style"] = item.fontStyle;
    s["font-variant"] = item.fontVariant;
    s["font-weight"] = item.fontWeight;
  }
  for (const prop in stylesAttr) {
    let value4 = item[prop];
    const name = stylesAttr[prop];
    if (value4 === "transparent" && (name === "fill" || name === "stroke")) ;
    else if (value4 != null) {
      if (isGradient(value4)) {
        value4 = gradientRef(value4, defs.gradient, "");
      }
      s[name] = value4;
    }
  }
  for (const prop in stylesCss) {
    const value4 = item[prop];
    if (value4 != null) {
      styleList = styleList || [];
      styleList.push(`${stylesCss[prop]}: ${value4};`);
    }
  }
  if (styleList) {
    s.style = styleList.join(" ");
  }
  return s;
}
var OPTS = {
  svgMarkTypes: ["text"],
  svgOnTop: true,
  debug: false
};
var HybridRenderer = class extends Renderer {
  constructor(loader2) {
    super(loader2);
    this._svgRenderer = new SVGRenderer(loader2);
    this._canvasRenderer = new CanvasRenderer(loader2);
  }
  /**
   * Initialize a new HybridRenderer instance.
   * @param {DOMElement} el - The containing DOM element for the display.
   * @param {number} width - The coordinate width of the display, in pixels.
   * @param {number} height - The coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {HybridRenderer} - This renderer instance.
   */
  initialize(el, width2, height2, origin, scaleFactor) {
    this._root_el = domChild(el, 0, "div");
    const bottomEl = domChild(this._root_el, 0, "div");
    const topEl = domChild(this._root_el, 1, "div");
    this._root_el.style.position = "relative";
    if (!OPTS.debug) {
      bottomEl.style.height = "100%";
      topEl.style.position = "absolute";
      topEl.style.top = "0";
      topEl.style.left = "0";
      topEl.style.height = "100%";
      topEl.style.width = "100%";
    }
    this._svgEl = OPTS.svgOnTop ? topEl : bottomEl;
    this._canvasEl = OPTS.svgOnTop ? bottomEl : topEl;
    this._svgEl.style.pointerEvents = "none";
    this._canvasRenderer.initialize(this._canvasEl, width2, height2, origin, scaleFactor);
    this._svgRenderer.initialize(this._svgEl, width2, height2, origin, scaleFactor);
    return super.initialize(el, width2, height2, origin, scaleFactor);
  }
  /**
   * Flag a mark item as dirty.
   * @param {Item} item - The mark item.
   */
  dirty(item) {
    if (OPTS.svgMarkTypes.includes(item.mark.marktype)) {
      this._svgRenderer.dirty(item);
    } else {
      this._canvasRenderer.dirty(item);
    }
    return this;
  }
  /**
   * Internal rendering method.
   * @param {object} scene - The root mark of a scenegraph to render.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   */
  _render(scene, markTypes) {
    const allMarkTypes = markTypes ?? ["arc", "area", "image", "line", "path", "rect", "rule", "shape", "symbol", "text", "trail"];
    const canvasMarkTypes = allMarkTypes.filter((m3) => !OPTS.svgMarkTypes.includes(m3));
    this._svgRenderer.render(scene, OPTS.svgMarkTypes);
    this._canvasRenderer.render(scene, canvasMarkTypes);
  }
  /**
   * Resize the display.
   * @param {number} width - The new coordinate width of the display, in pixels.
   * @param {number} height - The new coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The new origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {SVGRenderer} - This renderer instance;
   */
  resize(width2, height2, origin, scaleFactor) {
    super.resize(width2, height2, origin, scaleFactor);
    this._svgRenderer.resize(width2, height2, origin, scaleFactor);
    this._canvasRenderer.resize(width2, height2, origin, scaleFactor);
    return this;
  }
  background(bgcolor) {
    if (OPTS.svgOnTop) {
      this._canvasRenderer.background(bgcolor);
    } else {
      this._svgRenderer.background(bgcolor);
    }
    return this;
  }
};
var HybridHandler = class extends CanvasHandler {
  constructor(loader2, tooltip2) {
    super(loader2, tooltip2);
  }
  initialize(el, origin, obj) {
    const canvas = domChild(domChild(el, 0, "div"), OPTS.svgOnTop ? 0 : 1, "div");
    return super.initialize(canvas, origin, obj);
  }
};
var Canvas = "canvas";
var Hybrid = "hybrid";
var PNG = "png";
var SVG = "svg";
var None2 = "none";
var modules = {};
modules[Canvas] = modules[PNG] = {
  renderer: CanvasRenderer,
  headless: CanvasRenderer,
  handler: CanvasHandler
};
modules[SVG] = {
  renderer: SVGRenderer,
  headless: SVGStringRenderer,
  handler: SVGHandler
};
modules[Hybrid] = {
  renderer: HybridRenderer,
  headless: HybridRenderer,
  handler: HybridHandler
};
modules[None2] = {};
function intersect2(scene, bounds3, filter4) {
  const hits = [], box = new Bounds().union(bounds3), type4 = scene.marktype;
  return type4 ? intersectMark(scene, box, filter4, hits) : type4 === "group" ? intersectGroup(scene, box, filter4, hits) : error("Intersect scene must be mark node or group item.");
}
function intersectMark(mark, box, filter4, hits) {
  if (visitMark(mark, box, filter4)) {
    const items = mark.items, type4 = mark.marktype, n = items.length;
    let i = 0;
    if (type4 === "group") {
      for (; i < n; ++i) {
        intersectGroup(items[i], box, filter4, hits);
      }
    } else {
      for (const test2 = Marks[type4].isect; i < n; ++i) {
        const item = items[i];
        if (intersectItem(item, box, test2)) hits.push(item);
      }
    }
  }
  return hits;
}
function visitMark(mark, box, filter4) {
  return mark.bounds && box.intersects(mark.bounds) && (mark.marktype === "group" || mark.interactive !== false && (!filter4 || filter4(mark)));
}
function intersectGroup(group3, box, filter4, hits) {
  if (filter4 && filter4(group3.mark) && intersectItem(group3, box, Marks.group.isect)) {
    hits.push(group3);
  }
  const marks = group3.items, n = marks && marks.length;
  if (n) {
    const x6 = group3.x || 0, y6 = group3.y || 0;
    box.translate(-x6, -y6);
    for (let i = 0; i < n; ++i) {
      intersectMark(marks[i], box, filter4, hits);
    }
    box.translate(x6, y6);
  }
  return hits;
}
function intersectItem(item, box, test2) {
  const bounds3 = item.bounds;
  return box.encloses(bounds3) || box.intersects(bounds3) && test2(item, box);
}
var clipBounds = new Bounds();
function boundClip(mark) {
  const clip4 = mark.clip;
  if (isFunction(clip4)) {
    clip4(boundContext(clipBounds.clear()));
  } else if (clip4) {
    clipBounds.set(0, 0, mark.group.width, mark.group.height);
  } else return;
  mark.bounds.intersect(clipBounds);
}

// ../deepseek-harness/node_modules/.pnpm/vega-view-transforms@5.2.2/node_modules/vega-view-transforms/build/vega-view-transforms.js
var Top = "top";
var Left = "left";
var Right = "right";
var Bottom = "bottom";
var TopLeft = "top-left";
var TopRight = "top-right";
var BottomLeft = "bottom-left";
var BottomRight = "bottom-right";
var Start = "start";
var Middle = "middle";
var End = "end";
var X = "x";
var Y = "y";
var Group = "group";
var Bounds2 = "bounds";
var AxisRole = "axis";
var TitleRole = "title";
var FrameRole = "frame";
var ScopeRole = "scope";
var LegendRole = "legend";
var RowHeader = "row-header";
var RowFooter = "row-footer";
var RowTitle = "row-title";
var ColHeader = "column-header";
var ColFooter = "column-footer";
var ColTitle = "column-title";
var Padding = "padding";
var Symbols = "symbol";
var Fit = "fit";
var FitX = "fit-x";
var FitY = "fit-y";
var Pad = "pad";
var None3 = "none";
var All = "all";
var Each = "each";
var Flush = "flush";
var Column = "column";
var Row = "row";
function Bound(params2) {
  Transform.call(this, null, params2);
}
inherits_default(Bound, Transform, {
  transform(_, pulse2) {
    const view = pulse2.dataflow, mark = _.mark, type4 = mark.marktype, entry2 = Marks[type4], bound3 = entry2.bound;
    let markBounds = mark.bounds, rebound;
    if (entry2.nested) {
      if (mark.items.length) view.dirty(mark.items[0]);
      markBounds = boundItem(mark, bound3);
      mark.items.forEach((item) => {
        item.bounds.clear().union(markBounds);
      });
    } else if (type4 === Group || _.modified()) {
      pulse2.visit(pulse2.MOD, (item) => view.dirty(item));
      markBounds.clear();
      mark.items.forEach((item) => markBounds.union(boundItem(item, bound3)));
      switch (mark.role) {
        case AxisRole:
        case LegendRole:
        case TitleRole:
          pulse2.reflow();
      }
    } else {
      rebound = pulse2.changed(pulse2.REM);
      pulse2.visit(pulse2.ADD, (item) => {
        markBounds.union(boundItem(item, bound3));
      });
      pulse2.visit(pulse2.MOD, (item) => {
        rebound = rebound || markBounds.alignsWith(item.bounds);
        view.dirty(item);
        markBounds.union(boundItem(item, bound3));
      });
      if (rebound) {
        markBounds.clear();
        mark.items.forEach((item) => markBounds.union(item.bounds));
      }
    }
    boundClip(mark);
    return pulse2.modifies("bounds");
  }
});
function boundItem(item, bound3, opt) {
  return bound3(item.bounds.clear(), item, opt);
}
var COUNTER_NAME = ":vega_identifier:";
function Identifier(params2) {
  Transform.call(this, 0, params2);
}
Identifier.Definition = {
  "type": "Identifier",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "as",
    "type": "string",
    "required": true
  }]
};
inherits_default(Identifier, Transform, {
  transform(_, pulse2) {
    const counter = getCounter(pulse2.dataflow), as = _.as;
    let id2 = counter.value;
    pulse2.visit(pulse2.ADD, (t) => t[as] = t[as] || ++id2);
    counter.set(this.value = id2);
    return pulse2;
  }
});
function getCounter(view) {
  return view._signals[COUNTER_NAME] || (view._signals[COUNTER_NAME] = view.add(0));
}
function Mark(params2) {
  Transform.call(this, null, params2);
}
inherits_default(Mark, Transform, {
  transform(_, pulse2) {
    let mark = this.value;
    if (!mark) {
      mark = pulse2.dataflow.scenegraph().mark(_.markdef, lookup$1(_), _.index);
      mark.group.context = _.context;
      if (!_.context.group) _.context.group = mark.group;
      mark.source = this.source;
      mark.clip = _.clip;
      mark.interactive = _.interactive;
      this.value = mark;
    }
    const Init = mark.marktype === Group ? GroupItem : Item;
    pulse2.visit(pulse2.ADD, (item) => Init.call(item, mark));
    if (_.modified("clip") || _.modified("interactive")) {
      mark.clip = _.clip;
      mark.interactive = !!_.interactive;
      mark.zdirty = true;
      pulse2.reflow();
    }
    mark.items = pulse2.source;
    return pulse2;
  }
});
function lookup$1(_) {
  const g = _.groups, p = _.parent;
  return g && g.size === 1 ? g.get(Object.keys(g.object)[0]) : g && p ? g.lookup(p) : null;
}
function Overlap(params2) {
  Transform.call(this, null, params2);
}
var methods = {
  parity: (items) => items.filter((item, i) => i % 2 ? item.opacity = 0 : 1),
  greedy: (items, sep) => {
    let a3;
    return items.filter((b10, i) => !i || !intersect3(a3.bounds, b10.bounds, sep) ? (a3 = b10, 1) : b10.opacity = 0);
  }
};
var intersect3 = (a3, b10, sep) => sep > Math.max(b10.x1 - a3.x2, a3.x1 - b10.x2, b10.y1 - a3.y2, a3.y1 - b10.y2);
var hasOverlap = (items, pad4) => {
  for (var i = 1, n = items.length, a3 = items[0].bounds, b10; i < n; a3 = b10, ++i) {
    if (intersect3(a3, b10 = items[i].bounds, pad4)) return true;
  }
};
var hasBounds = (item) => {
  const b10 = item.bounds;
  return b10.width() > 1 && b10.height() > 1;
};
var boundTest = (scale9, orient2, tolerance) => {
  var range7 = scale9.range(), b10 = new Bounds();
  if (orient2 === Top || orient2 === Bottom) {
    b10.set(range7[0], -Infinity, range7[1], Infinity);
  } else {
    b10.set(-Infinity, range7[0], Infinity, range7[1]);
  }
  b10.expand(tolerance || 1);
  return (item) => b10.encloses(item.bounds);
};
var reset = (source4) => {
  source4.forEach((item) => item.opacity = 1);
  return source4;
};
var reflow = (pulse2, _) => pulse2.reflow(_.modified()).modifies("opacity");
inherits_default(Overlap, Transform, {
  transform(_, pulse2) {
    const reduce2 = methods[_.method] || methods.parity, sep = _.separation || 0;
    let source4 = pulse2.materialize(pulse2.SOURCE).source, items, test2;
    if (!source4 || !source4.length) return;
    if (!_.method) {
      if (_.modified("method")) {
        reset(source4);
        pulse2 = reflow(pulse2, _);
      }
      return pulse2;
    }
    source4 = source4.filter(hasBounds);
    if (!source4.length) return;
    if (_.sort) {
      source4 = source4.slice().sort(_.sort);
    }
    items = reset(source4);
    pulse2 = reflow(pulse2, _);
    if (items.length >= 3 && hasOverlap(items, sep)) {
      do {
        items = reduce2(items, sep);
      } while (items.length >= 3 && hasOverlap(items, sep));
      if (items.length < 3 && !peek(source4).opacity) {
        if (items.length > 1) peek(items).opacity = 0;
        peek(source4).opacity = 1;
      }
    }
    if (_.boundScale && _.boundTolerance >= 0) {
      test2 = boundTest(_.boundScale, _.boundOrient, +_.boundTolerance);
      source4.forEach((item) => {
        if (!test2(item)) item.opacity = 0;
      });
    }
    const bounds3 = items[0].mark.bounds.clear();
    source4.forEach((item) => {
      if (item.opacity) bounds3.union(item.bounds);
    });
    return pulse2;
  }
});
function Render(params2) {
  Transform.call(this, null, params2);
}
inherits_default(Render, Transform, {
  transform(_, pulse2) {
    const view = pulse2.dataflow;
    pulse2.visit(pulse2.ALL, (item) => view.dirty(item));
    if (pulse2.fields && pulse2.fields["zindex"]) {
      const item = pulse2.source && pulse2.source[0];
      if (item) item.mark.zdirty = true;
    }
  }
});
var tempBounds2 = new Bounds();
function set3(item, property2, value4) {
  return item[property2] === value4 ? 0 : (item[property2] = value4, 1);
}
function isYAxis(mark) {
  var orient2 = mark.items[0].orient;
  return orient2 === Left || orient2 === Right;
}
function axisIndices(datum3) {
  let index4 = +datum3.grid;
  return [
    datum3.ticks ? index4++ : -1,
    // ticks index
    datum3.labels ? index4++ : -1,
    // labels index
    index4 + +datum3.domain
    // title index
  ];
}
function axisLayout(view, axis, width2, height2) {
  var item = axis.items[0], datum3 = item.datum, delta = item.translate != null ? item.translate : 0.5, orient2 = item.orient, indices = axisIndices(datum3), range7 = item.range, offset6 = item.offset, position2 = item.position, minExtent = item.minExtent, maxExtent = item.maxExtent, title2 = datum3.title && item.items[indices[2]].items[0], titlePadding = item.titlePadding, bounds3 = item.bounds, dl = title2 && multiLineOffset(title2), x6 = 0, y6 = 0, i, s;
  tempBounds2.clear().union(bounds3);
  bounds3.clear();
  if ((i = indices[0]) > -1) bounds3.union(item.items[i].bounds);
  if ((i = indices[1]) > -1) bounds3.union(item.items[i].bounds);
  switch (orient2) {
    case Top:
      x6 = position2 || 0;
      y6 = -offset6;
      s = Math.max(minExtent, Math.min(maxExtent, -bounds3.y1));
      bounds3.add(0, -s).add(range7, 0);
      if (title2) axisTitleLayout(view, title2, s, titlePadding, dl, 0, -1, bounds3);
      break;
    case Left:
      x6 = -offset6;
      y6 = position2 || 0;
      s = Math.max(minExtent, Math.min(maxExtent, -bounds3.x1));
      bounds3.add(-s, 0).add(0, range7);
      if (title2) axisTitleLayout(view, title2, s, titlePadding, dl, 1, -1, bounds3);
      break;
    case Right:
      x6 = width2 + offset6;
      y6 = position2 || 0;
      s = Math.max(minExtent, Math.min(maxExtent, bounds3.x2));
      bounds3.add(0, 0).add(s, range7);
      if (title2) axisTitleLayout(view, title2, s, titlePadding, dl, 1, 1, bounds3);
      break;
    case Bottom:
      x6 = position2 || 0;
      y6 = height2 + offset6;
      s = Math.max(minExtent, Math.min(maxExtent, bounds3.y2));
      bounds3.add(0, 0).add(range7, s);
      if (title2) axisTitleLayout(view, title2, s, titlePadding, 0, 0, 1, bounds3);
      break;
    default:
      x6 = item.x;
      y6 = item.y;
  }
  boundStroke(bounds3.translate(x6, y6), item);
  if (set3(item, "x", x6 + delta) | set3(item, "y", y6 + delta)) {
    item.bounds = tempBounds2;
    view.dirty(item);
    item.bounds = bounds3;
    view.dirty(item);
  }
  return item.mark.bounds.clear().union(bounds3);
}
function axisTitleLayout(view, title2, offset6, pad4, dl, isYAxis2, sign3, bounds3) {
  const b10 = title2.bounds;
  if (title2.auto) {
    const v2 = sign3 * (offset6 + dl + pad4);
    let dx = 0, dy = 0;
    view.dirty(title2);
    isYAxis2 ? dx = (title2.x || 0) - (title2.x = v2) : dy = (title2.y || 0) - (title2.y = v2);
    title2.mark.bounds.clear().union(b10.translate(-dx, -dy));
    view.dirty(title2);
  }
  bounds3.union(b10);
}
var min3 = (a3, b10) => Math.floor(Math.min(a3, b10));
var max3 = (a3, b10) => Math.ceil(Math.max(a3, b10));
function gridLayoutGroups(group3) {
  var groups = group3.items, n = groups.length, i = 0, mark, items;
  const views = {
    marks: [],
    rowheaders: [],
    rowfooters: [],
    colheaders: [],
    colfooters: [],
    rowtitle: null,
    coltitle: null
  };
  for (; i < n; ++i) {
    mark = groups[i];
    items = mark.items;
    if (mark.marktype === Group) {
      switch (mark.role) {
        case AxisRole:
        case LegendRole:
        case TitleRole:
          break;
        case RowHeader:
          views.rowheaders.push(...items);
          break;
        case RowFooter:
          views.rowfooters.push(...items);
          break;
        case ColHeader:
          views.colheaders.push(...items);
          break;
        case ColFooter:
          views.colfooters.push(...items);
          break;
        case RowTitle:
          views.rowtitle = items[0];
          break;
        case ColTitle:
          views.coltitle = items[0];
          break;
        default:
          views.marks.push(...items);
      }
    }
  }
  return views;
}
function bboxFlush(item) {
  return new Bounds().set(0, 0, item.width || 0, item.height || 0);
}
function bboxFull(item) {
  const b10 = item.bounds.clone();
  return b10.empty() ? b10.set(0, 0, 0, 0) : b10.translate(-(item.x || 0), -(item.y || 0));
}
function get4(opt, key2, d) {
  const v2 = isObject(opt) ? opt[key2] : opt;
  return v2 != null ? v2 : d !== void 0 ? d : 0;
}
function offsetValue(v2) {
  return v2 < 0 ? Math.ceil(-v2) : 0;
}
function gridLayout(view, groups, opt) {
  var dirty = !opt.nodirty, bbox = opt.bounds === Flush ? bboxFlush : bboxFull, bounds3 = tempBounds2.set(0, 0, 0, 0), alignCol = get4(opt.align, Column), alignRow = get4(opt.align, Row), padCol = get4(opt.padding, Column), padRow = get4(opt.padding, Row), ncols = opt.columns || groups.length, nrows = ncols <= 0 ? 1 : Math.ceil(groups.length / ncols), n = groups.length, xOffset = Array(n), xExtent = Array(ncols), xMax = 0, yOffset = Array(n), yExtent = Array(nrows), yMax = 0, dx = Array(n), dy = Array(n), boxes = Array(n), m3, i, c3, r, b10, g, px3, py3, x6, y6, offset6;
  for (i = 0; i < ncols; ++i) xExtent[i] = 0;
  for (i = 0; i < nrows; ++i) yExtent[i] = 0;
  for (i = 0; i < n; ++i) {
    g = groups[i];
    b10 = boxes[i] = bbox(g);
    g.x = g.x || 0;
    dx[i] = 0;
    g.y = g.y || 0;
    dy[i] = 0;
    c3 = i % ncols;
    r = ~~(i / ncols);
    xMax = Math.max(xMax, px3 = Math.ceil(b10.x2));
    yMax = Math.max(yMax, py3 = Math.ceil(b10.y2));
    xExtent[c3] = Math.max(xExtent[c3], px3);
    yExtent[r] = Math.max(yExtent[r], py3);
    xOffset[i] = padCol + offsetValue(b10.x1);
    yOffset[i] = padRow + offsetValue(b10.y1);
    if (dirty) view.dirty(groups[i]);
  }
  for (i = 0; i < n; ++i) {
    if (i % ncols === 0) xOffset[i] = 0;
    if (i < ncols) yOffset[i] = 0;
  }
  if (alignCol === Each) {
    for (c3 = 1; c3 < ncols; ++c3) {
      for (offset6 = 0, i = c3; i < n; i += ncols) {
        if (offset6 < xOffset[i]) offset6 = xOffset[i];
      }
      for (i = c3; i < n; i += ncols) {
        xOffset[i] = offset6 + xExtent[c3 - 1];
      }
    }
  } else if (alignCol === All) {
    for (offset6 = 0, i = 0; i < n; ++i) {
      if (i % ncols && offset6 < xOffset[i]) offset6 = xOffset[i];
    }
    for (i = 0; i < n; ++i) {
      if (i % ncols) xOffset[i] = offset6 + xMax;
    }
  } else {
    for (alignCol = false, c3 = 1; c3 < ncols; ++c3) {
      for (i = c3; i < n; i += ncols) {
        xOffset[i] += xExtent[c3 - 1];
      }
    }
  }
  if (alignRow === Each) {
    for (r = 1; r < nrows; ++r) {
      for (offset6 = 0, i = r * ncols, m3 = i + ncols; i < m3; ++i) {
        if (offset6 < yOffset[i]) offset6 = yOffset[i];
      }
      for (i = r * ncols; i < m3; ++i) {
        yOffset[i] = offset6 + yExtent[r - 1];
      }
    }
  } else if (alignRow === All) {
    for (offset6 = 0, i = ncols; i < n; ++i) {
      if (offset6 < yOffset[i]) offset6 = yOffset[i];
    }
    for (i = ncols; i < n; ++i) {
      yOffset[i] = offset6 + yMax;
    }
  } else {
    for (alignRow = false, r = 1; r < nrows; ++r) {
      for (i = r * ncols, m3 = i + ncols; i < m3; ++i) {
        yOffset[i] += yExtent[r - 1];
      }
    }
  }
  for (x6 = 0, i = 0; i < n; ++i) {
    x6 = xOffset[i] + (i % ncols ? x6 : 0);
    dx[i] += x6 - groups[i].x;
  }
  for (c3 = 0; c3 < ncols; ++c3) {
    for (y6 = 0, i = c3; i < n; i += ncols) {
      y6 += yOffset[i];
      dy[i] += y6 - groups[i].y;
    }
  }
  if (alignCol && get4(opt.center, Column) && nrows > 1) {
    for (i = 0; i < n; ++i) {
      b10 = alignCol === All ? xMax : xExtent[i % ncols];
      x6 = b10 - boxes[i].x2 - groups[i].x - dx[i];
      if (x6 > 0) dx[i] += x6 / 2;
    }
  }
  if (alignRow && get4(opt.center, Row) && ncols !== 1) {
    for (i = 0; i < n; ++i) {
      b10 = alignRow === All ? yMax : yExtent[~~(i / ncols)];
      y6 = b10 - boxes[i].y2 - groups[i].y - dy[i];
      if (y6 > 0) dy[i] += y6 / 2;
    }
  }
  for (i = 0; i < n; ++i) {
    bounds3.union(boxes[i].translate(dx[i], dy[i]));
  }
  x6 = get4(opt.anchor, X);
  y6 = get4(opt.anchor, Y);
  switch (get4(opt.anchor, Column)) {
    case End:
      x6 -= bounds3.width();
      break;
    case Middle:
      x6 -= bounds3.width() / 2;
  }
  switch (get4(opt.anchor, Row)) {
    case End:
      y6 -= bounds3.height();
      break;
    case Middle:
      y6 -= bounds3.height() / 2;
  }
  x6 = Math.round(x6);
  y6 = Math.round(y6);
  bounds3.clear();
  for (i = 0; i < n; ++i) {
    groups[i].mark.bounds.clear();
  }
  for (i = 0; i < n; ++i) {
    g = groups[i];
    g.x += dx[i] += x6;
    g.y += dy[i] += y6;
    bounds3.union(g.mark.bounds.union(g.bounds.translate(dx[i], dy[i])));
    if (dirty) view.dirty(g);
  }
  return bounds3;
}
function trellisLayout(view, group3, opt) {
  var views = gridLayoutGroups(group3), groups = views.marks, bbox = opt.bounds === Flush ? boundFlush : boundFull, off = opt.offset, ncols = opt.columns || groups.length, nrows = ncols <= 0 ? 1 : Math.ceil(groups.length / ncols), cells = nrows * ncols, x6, y6, x22, y22, anchor, band2, offset6;
  const bounds3 = gridLayout(view, groups, opt);
  if (bounds3.empty()) bounds3.set(0, 0, 0, 0);
  if (views.rowheaders) {
    band2 = get4(opt.headerBand, Row, null);
    x6 = layoutHeaders(view, views.rowheaders, groups, ncols, nrows, -get4(off, "rowHeader"), min3, 0, bbox, "x1", 0, ncols, 1, band2);
  }
  if (views.colheaders) {
    band2 = get4(opt.headerBand, Column, null);
    y6 = layoutHeaders(view, views.colheaders, groups, ncols, ncols, -get4(off, "columnHeader"), min3, 1, bbox, "y1", 0, 1, ncols, band2);
  }
  if (views.rowfooters) {
    band2 = get4(opt.footerBand, Row, null);
    x22 = layoutHeaders(view, views.rowfooters, groups, ncols, nrows, get4(off, "rowFooter"), max3, 0, bbox, "x2", ncols - 1, ncols, 1, band2);
  }
  if (views.colfooters) {
    band2 = get4(opt.footerBand, Column, null);
    y22 = layoutHeaders(view, views.colfooters, groups, ncols, ncols, get4(off, "columnFooter"), max3, 1, bbox, "y2", cells - ncols, 1, ncols, band2);
  }
  if (views.rowtitle) {
    anchor = get4(opt.titleAnchor, Row);
    offset6 = get4(off, "rowTitle");
    offset6 = anchor === End ? x22 + offset6 : x6 - offset6;
    band2 = get4(opt.titleBand, Row, 0.5);
    layoutTitle(view, views.rowtitle, offset6, 0, bounds3, band2);
  }
  if (views.coltitle) {
    anchor = get4(opt.titleAnchor, Column);
    offset6 = get4(off, "columnTitle");
    offset6 = anchor === End ? y22 + offset6 : y6 - offset6;
    band2 = get4(opt.titleBand, Column, 0.5);
    layoutTitle(view, views.coltitle, offset6, 1, bounds3, band2);
  }
}
function boundFlush(item, field3) {
  return field3 === "x1" ? item.x || 0 : field3 === "y1" ? item.y || 0 : field3 === "x2" ? (item.x || 0) + (item.width || 0) : field3 === "y2" ? (item.y || 0) + (item.height || 0) : void 0;
}
function boundFull(item, field3) {
  return item.bounds[field3];
}
function layoutHeaders(view, headers, groups, ncols, limit, offset6, agg, isX2, bound3, bf, start, stride, back, band2) {
  var n = groups.length, init2 = 0, edge = 0, i, j, k, m3, b10, h3, g, x6, y6;
  if (!n) return init2;
  for (i = start; i < n; i += stride) {
    if (groups[i]) init2 = agg(init2, bound3(groups[i], bf));
  }
  if (!headers.length) return init2;
  if (headers.length > limit) {
    view.warn("Grid headers exceed limit: " + limit);
    headers = headers.slice(0, limit);
  }
  init2 += offset6;
  for (j = 0, m3 = headers.length; j < m3; ++j) {
    view.dirty(headers[j]);
    headers[j].mark.bounds.clear();
  }
  for (i = start, j = 0, m3 = headers.length; j < m3; ++j, i += stride) {
    h3 = headers[j];
    b10 = h3.mark.bounds;
    for (k = i; k >= 0 && (g = groups[k]) == null; k -= back) ;
    if (isX2) {
      x6 = band2 == null ? g.x : Math.round(g.bounds.x1 + band2 * g.bounds.width());
      y6 = init2;
    } else {
      x6 = init2;
      y6 = band2 == null ? g.y : Math.round(g.bounds.y1 + band2 * g.bounds.height());
    }
    b10.union(h3.bounds.translate(x6 - (h3.x || 0), y6 - (h3.y || 0)));
    h3.x = x6;
    h3.y = y6;
    view.dirty(h3);
    edge = agg(edge, b10[bf]);
  }
  return edge;
}
function layoutTitle(view, g, offset6, isX2, bounds3, band2) {
  if (!g) return;
  view.dirty(g);
  var x6 = offset6, y6 = offset6;
  isX2 ? x6 = Math.round(bounds3.x1 + band2 * bounds3.width()) : y6 = Math.round(bounds3.y1 + band2 * bounds3.height());
  g.bounds.translate(x6 - (g.x || 0), y6 - (g.y || 0));
  g.mark.bounds.clear().union(g.bounds);
  g.x = x6;
  g.y = y6;
  view.dirty(g);
}
function lookup2(config, orient2) {
  const opt = config[orient2] || {};
  return (key2, d) => opt[key2] != null ? opt[key2] : config[key2] != null ? config[key2] : d;
}
function offsets(legends, value4) {
  let max4 = -Infinity;
  legends.forEach((item) => {
    if (item.offset != null) max4 = Math.max(max4, item.offset);
  });
  return max4 > -Infinity ? max4 : value4;
}
function legendParams(g, orient2, config, xb, yb, w3, h3) {
  const _ = lookup2(config, orient2), offset6 = offsets(g, _("offset", 0)), anchor = _("anchor", Start), frame2 = _("frame", Group), mult2 = anchor === End ? 1 : anchor === Middle ? 0.5 : 0, ax = frame2 === Bounds2 ? xb.x1 + mult2 * xb.width() : mult2 * (w3 || yb.width() + 2 * yb.x1), ay = frame2 === Bounds2 ? yb.y1 + mult2 * yb.height() : mult2 * (h3 || xb.height() + 2 * xb.y1);
  const p = {
    align: Each,
    bounds: _("bounds", Flush),
    columns: _("direction") === "vertical" ? 1 : g.length,
    padding: _("margin", 8),
    center: _("center"),
    nodirty: true
  };
  switch (orient2) {
    case Left:
      p.anchor = {
        x: Math.floor(xb.x1) - offset6,
        column: End,
        y: ay,
        row: anchor
      };
      break;
    case Right:
      p.anchor = {
        x: Math.ceil(xb.x2) + offset6,
        y: ay,
        row: anchor
      };
      break;
    case Top:
      p.anchor = {
        y: Math.floor(yb.y1) - offset6,
        row: End,
        x: ax,
        column: anchor
      };
      break;
    case Bottom:
      p.anchor = {
        y: Math.ceil(yb.y2) + offset6,
        x: ax,
        column: anchor
      };
      break;
    case TopLeft:
      p.anchor = {
        x: offset6,
        y: offset6
      };
      break;
    case TopRight:
      p.anchor = {
        x: w3 - offset6,
        y: offset6,
        column: End
      };
      break;
    case BottomLeft:
      p.anchor = {
        x: offset6,
        y: h3 - offset6,
        row: End
      };
      break;
    case BottomRight:
      p.anchor = {
        x: w3 - offset6,
        y: h3 - offset6,
        column: End,
        row: End
      };
      break;
  }
  return p;
}
function legendLayout(view, legend) {
  var item = legend.items[0], datum3 = item.datum, orient2 = item.orient, bounds3 = item.bounds, x6 = item.x, y6 = item.y, w3, h3;
  item._bounds ? item._bounds.clear().union(bounds3) : item._bounds = bounds3.clone();
  bounds3.clear();
  legendGroupLayout(view, item, item.items[0].items[0]);
  bounds3 = legendBounds(item, bounds3);
  w3 = 2 * item.padding;
  h3 = 2 * item.padding;
  if (!bounds3.empty()) {
    w3 = Math.ceil(bounds3.width() + w3);
    h3 = Math.ceil(bounds3.height() + h3);
  }
  if (datum3.type === Symbols) {
    legendEntryLayout(item.items[0].items[0].items[0].items);
  }
  if (orient2 !== None3) {
    item.x = x6 = 0;
    item.y = y6 = 0;
  }
  item.width = w3;
  item.height = h3;
  boundStroke(bounds3.set(x6, y6, x6 + w3, y6 + h3), item);
  item.mark.bounds.clear().union(bounds3);
  return item;
}
function legendBounds(item, b10) {
  item.items.forEach((_) => b10.union(_.bounds));
  if (b10.empty()) {
    b10.x2 = item.padding;
    b10.y2 = item.padding;
  }
  b10.x1 = item.padding;
  b10.y1 = item.padding;
  return b10;
}
function legendGroupLayout(view, item, entry2) {
  var pad4 = item.padding, ex = pad4 - entry2.x, ey = pad4 - entry2.y;
  if (!item.datum.title) {
    if (ex || ey) translate2(view, entry2, ex, ey);
  } else {
    var title2 = item.items[1].items[0], anchor = title2.anchor, tpad = item.titlePadding || 0, tx = pad4 - title2.x, ty = pad4 - title2.y;
    switch (title2.orient) {
      case Left:
        ex += Math.ceil(title2.bounds.width()) + tpad;
        break;
      case Right:
      case Bottom:
        break;
      default:
        ey += title2.bounds.height() + tpad;
    }
    if (ex || ey) translate2(view, entry2, ex, ey);
    switch (title2.orient) {
      case Left:
        ty += legendTitleOffset(item, entry2, title2, anchor, 1, 1);
        break;
      case Right:
        tx += legendTitleOffset(item, entry2, title2, End, 0, 0) + tpad;
        ty += legendTitleOffset(item, entry2, title2, anchor, 1, 1);
        break;
      case Bottom:
        tx += legendTitleOffset(item, entry2, title2, anchor, 0, 0);
        ty += legendTitleOffset(item, entry2, title2, End, -1, 0, 1) + tpad;
        break;
      default:
        tx += legendTitleOffset(item, entry2, title2, anchor, 0, 0);
    }
    if (tx || ty) translate2(view, title2, tx, ty);
    if ((tx = Math.round(title2.bounds.x1 - pad4)) < 0) {
      translate2(view, entry2, -tx, 0);
      translate2(view, title2, -tx, 0);
    }
  }
}
function legendTitleOffset(item, entry2, title2, anchor, y6, lr, noBar) {
  const grad = item.datum.type !== "symbol", vgrad = title2.datum.vgrad, e = grad && (lr || !vgrad) && !noBar ? entry2.items[0] : entry2, s = e.bounds[y6 ? "y2" : "x2"] - item.padding, u4 = vgrad && lr ? s : 0, v2 = vgrad && lr ? 0 : s, o = y6 <= 0 ? 0 : multiLineOffset(title2);
  return Math.round(anchor === Start ? u4 : anchor === End ? v2 - o : 0.5 * (s - o));
}
function translate2(view, item, dx, dy) {
  item.x += dx;
  item.y += dy;
  item.bounds.translate(dx, dy);
  item.mark.bounds.translate(dx, dy);
  view.dirty(item);
}
function legendEntryLayout(entries2) {
  const widths = entries2.reduce((w3, g) => {
    w3[g.column] = Math.max(g.bounds.x2 - g.x, w3[g.column] || 0);
    return w3;
  }, {});
  entries2.forEach((g) => {
    g.width = widths[g.column];
    g.height = g.bounds.y2 - g.y;
  });
}
function titleLayout(view, mark, width2, height2, viewBounds3) {
  var group3 = mark.items[0], frame2 = group3.frame, orient2 = group3.orient, anchor = group3.anchor, offset6 = group3.offset, padding3 = group3.padding, title2 = group3.items[0].items[0], subtitle = group3.items[1] && group3.items[1].items[0], end = orient2 === Left || orient2 === Right ? height2 : width2, start = 0, x6 = 0, y6 = 0, sx = 0, sy = 0, pos;
  if (frame2 !== Group) {
    orient2 === Left ? (start = viewBounds3.y2, end = viewBounds3.y1) : orient2 === Right ? (start = viewBounds3.y1, end = viewBounds3.y2) : (start = viewBounds3.x1, end = viewBounds3.x2);
  } else if (orient2 === Left) {
    start = height2, end = 0;
  }
  pos = anchor === Start ? start : anchor === End ? end : (start + end) / 2;
  if (subtitle && subtitle.text) {
    switch (orient2) {
      case Top:
      case Bottom:
        sy = title2.bounds.height() + padding3;
        break;
      case Left:
        sx = title2.bounds.width() + padding3;
        break;
      case Right:
        sx = -title2.bounds.width() - padding3;
        break;
    }
    tempBounds2.clear().union(subtitle.bounds);
    tempBounds2.translate(sx - (subtitle.x || 0), sy - (subtitle.y || 0));
    if (set3(subtitle, "x", sx) | set3(subtitle, "y", sy)) {
      view.dirty(subtitle);
      subtitle.bounds.clear().union(tempBounds2);
      subtitle.mark.bounds.clear().union(tempBounds2);
      view.dirty(subtitle);
    }
    tempBounds2.clear().union(subtitle.bounds);
  } else {
    tempBounds2.clear();
  }
  tempBounds2.union(title2.bounds);
  switch (orient2) {
    case Top:
      x6 = pos;
      y6 = viewBounds3.y1 - tempBounds2.height() - offset6;
      break;
    case Left:
      x6 = viewBounds3.x1 - tempBounds2.width() - offset6;
      y6 = pos;
      break;
    case Right:
      x6 = viewBounds3.x2 + tempBounds2.width() + offset6;
      y6 = pos;
      break;
    case Bottom:
      x6 = pos;
      y6 = viewBounds3.y2 + offset6;
      break;
    default:
      x6 = group3.x;
      y6 = group3.y;
  }
  if (set3(group3, "x", x6) | set3(group3, "y", y6)) {
    tempBounds2.translate(x6, y6);
    view.dirty(group3);
    group3.bounds.clear().union(tempBounds2);
    mark.bounds.clear().union(tempBounds2);
    view.dirty(group3);
  }
  return group3.bounds;
}
function ViewLayout(params2) {
  Transform.call(this, null, params2);
}
inherits_default(ViewLayout, Transform, {
  transform(_, pulse2) {
    const view = pulse2.dataflow;
    _.mark.items.forEach((group3) => {
      if (_.layout) trellisLayout(view, group3, _.layout);
      layoutGroup(view, group3, _);
    });
    return shouldReflow(_.mark.group) ? pulse2.reflow() : pulse2;
  }
});
function shouldReflow(group3) {
  return group3 && group3.mark.role !== "legend-entry";
}
function layoutGroup(view, group3, _) {
  var items = group3.items, width2 = Math.max(0, group3.width || 0), height2 = Math.max(0, group3.height || 0), viewBounds3 = new Bounds().set(0, 0, width2, height2), xBounds = viewBounds3.clone(), yBounds = viewBounds3.clone(), legends = [], title2, mark, orient2, b10, i, n;
  for (i = 0, n = items.length; i < n; ++i) {
    mark = items[i];
    switch (mark.role) {
      case AxisRole:
        b10 = isYAxis(mark) ? xBounds : yBounds;
        b10.union(axisLayout(view, mark, width2, height2));
        break;
      case TitleRole:
        title2 = mark;
        break;
      case LegendRole:
        legends.push(legendLayout(view, mark));
        break;
      case FrameRole:
      case ScopeRole:
      case RowHeader:
      case RowFooter:
      case RowTitle:
      case ColHeader:
      case ColFooter:
      case ColTitle:
        xBounds.union(mark.bounds);
        yBounds.union(mark.bounds);
        break;
      default:
        viewBounds3.union(mark.bounds);
    }
  }
  if (legends.length) {
    const l = {};
    legends.forEach((item) => {
      orient2 = item.orient || Right;
      if (orient2 !== None3) (l[orient2] || (l[orient2] = [])).push(item);
    });
    for (const orient3 in l) {
      const g = l[orient3];
      gridLayout(view, g, legendParams(g, orient3, _.legends, xBounds, yBounds, width2, height2));
    }
    legends.forEach((item) => {
      const b11 = item.bounds;
      if (!b11.equals(item._bounds)) {
        item.bounds = item._bounds;
        view.dirty(item);
        item.bounds = b11;
        view.dirty(item);
      }
      if (_.autosize && (_.autosize.type === Fit || _.autosize.type === FitX || _.autosize.type === FitY)) {
        switch (item.orient) {
          case Left:
          case Right:
            viewBounds3.add(b11.x1, 0).add(b11.x2, 0);
            break;
          case Top:
          case Bottom:
            viewBounds3.add(0, b11.y1).add(0, b11.y2);
        }
      } else {
        viewBounds3.union(b11);
      }
    });
  }
  viewBounds3.union(xBounds).union(yBounds);
  if (title2) {
    viewBounds3.union(titleLayout(view, title2, width2, height2, viewBounds3));
  }
  if (group3.clip) {
    viewBounds3.set(0, 0, group3.width || 0, group3.height || 0);
  }
  viewSizeLayout(view, group3, viewBounds3, _);
}
function viewSizeLayout(view, group3, viewBounds3, _) {
  const auto = _.autosize || {}, type4 = auto.type;
  if (view._autosize < 1 || !type4) return;
  let viewWidth2 = view._width, viewHeight2 = view._height, width2 = Math.max(0, group3.width || 0), left = Math.max(0, Math.ceil(-viewBounds3.x1)), height2 = Math.max(0, group3.height || 0), top = Math.max(0, Math.ceil(-viewBounds3.y1));
  const right = Math.max(0, Math.ceil(viewBounds3.x2 - width2)), bottom = Math.max(0, Math.ceil(viewBounds3.y2 - height2));
  if (auto.contains === Padding) {
    const padding3 = view.padding();
    viewWidth2 -= padding3.left + padding3.right;
    viewHeight2 -= padding3.top + padding3.bottom;
  }
  if (type4 === None3) {
    left = 0;
    top = 0;
    width2 = viewWidth2;
    height2 = viewHeight2;
  } else if (type4 === Fit) {
    width2 = Math.max(0, viewWidth2 - left - right);
    height2 = Math.max(0, viewHeight2 - top - bottom);
  } else if (type4 === FitX) {
    width2 = Math.max(0, viewWidth2 - left - right);
    viewHeight2 = height2 + top + bottom;
  } else if (type4 === FitY) {
    viewWidth2 = width2 + left + right;
    height2 = Math.max(0, viewHeight2 - top - bottom);
  } else if (type4 === Pad) {
    viewWidth2 = width2 + left + right;
    viewHeight2 = height2 + top + bottom;
  }
  view._resizeView(viewWidth2, viewHeight2, width2, height2, [left, top], auto.resize);
}

// ../deepseek-harness/node_modules/.pnpm/vega-encode@5.2.2/node_modules/vega-encode/build/vega-encode.js
var vega_encode_exports = {};
__export(vega_encode_exports, {
  axisticks: () => AxisTicks,
  datajoin: () => DataJoin,
  encode: () => Encode,
  legendentries: () => LegendEntries,
  linkpath: () => LinkPath,
  pie: () => Pie,
  scale: () => Scale,
  sortitems: () => SortItems,
  stack: () => Stack
});
function AxisTicks(params2) {
  Transform.call(this, null, params2);
}
inherits_default(AxisTicks, Transform, {
  transform(_, pulse2) {
    if (this.value && !_.modified()) {
      return pulse2.StopPropagation;
    }
    var locale4 = pulse2.dataflow.locale(), out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS), ticks2 = this.value, scale9 = _.scale, tally = _.count == null ? _.values ? _.values.length : 10 : _.count, count2 = tickCount(scale9, tally, _.minstep), format6 = _.format || tickFormat2(locale4, scale9, count2, _.formatSpecifier, _.formatType, !!_.values), values4 = _.values ? validTicks(scale9, _.values, count2) : tickValues(scale9, count2);
    if (ticks2) out.rem = ticks2;
    ticks2 = values4.map((value4, i) => ingest$1({
      index: i / (values4.length - 1 || 1),
      tickIndex: i,
      value: value4,
      label: format6(value4)
    }));
    if (_.extra && ticks2.length) {
      ticks2.push(ingest$1({
        index: -1,
        tickIndex: -1,
        extra: {
          value: ticks2[0].value
        },
        label: ""
      }));
    }
    out.source = ticks2;
    out.add = ticks2;
    this.value = ticks2;
    return out;
  }
});
function DataJoin(params2) {
  Transform.call(this, null, params2);
}
function defaultItemCreate() {
  return ingest$1({});
}
function newMap(key2) {
  const map3 = fastmap().test((t) => t.exit);
  map3.lookup = (t) => map3.get(key2(t));
  return map3;
}
inherits_default(DataJoin, Transform, {
  transform(_, pulse2) {
    var df = pulse2.dataflow, out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS), item = _.item || defaultItemCreate, key2 = _.key || tupleid, map3 = this.value;
    if (isArray(out.encode)) {
      out.encode = null;
    }
    if (map3 && (_.modified("key") || pulse2.modified(key2))) {
      error("DataJoin does not support modified key function or fields.");
    }
    if (!map3) {
      pulse2 = pulse2.addAll();
      this.value = map3 = newMap(key2);
    }
    pulse2.visit(pulse2.ADD, (t) => {
      const k = key2(t);
      let x6 = map3.get(k);
      if (x6) {
        if (x6.exit) {
          map3.empty--;
          out.add.push(x6);
        } else {
          out.mod.push(x6);
        }
      } else {
        x6 = item(t);
        map3.set(k, x6);
        out.add.push(x6);
      }
      x6.datum = t;
      x6.exit = false;
    });
    pulse2.visit(pulse2.MOD, (t) => {
      const k = key2(t), x6 = map3.get(k);
      if (x6) {
        x6.datum = t;
        out.mod.push(x6);
      }
    });
    pulse2.visit(pulse2.REM, (t) => {
      const k = key2(t), x6 = map3.get(k);
      if (t === x6.datum && !x6.exit) {
        out.rem.push(x6);
        x6.exit = true;
        ++map3.empty;
      }
    });
    if (pulse2.changed(pulse2.ADD_MOD)) out.modifies("datum");
    if (pulse2.clean() || _.clean && map3.empty > df.cleanThreshold) {
      df.runAfter(map3.clean);
    }
    return out;
  }
});
function Encode(params2) {
  Transform.call(this, null, params2);
}
inherits_default(Encode, Transform, {
  transform(_, pulse2) {
    var out = pulse2.fork(pulse2.ADD_REM), fmod = _.mod || false, encoders = _.encoders, encode3 = pulse2.encode;
    if (isArray(encode3)) {
      if (out.changed() || encode3.every((e) => encoders[e])) {
        encode3 = encode3[0];
        out.encode = null;
      } else {
        return pulse2.StopPropagation;
      }
    }
    var reenter = encode3 === "enter", update3 = encoders.update || falsy, enter = encoders.enter || falsy, exit = encoders.exit || falsy, set7 = (encode3 && !reenter ? encoders[encode3] : update3) || falsy;
    if (pulse2.changed(pulse2.ADD)) {
      pulse2.visit(pulse2.ADD, (t) => {
        enter(t, _);
        update3(t, _);
      });
      out.modifies(enter.output);
      out.modifies(update3.output);
      if (set7 !== falsy && set7 !== update3) {
        pulse2.visit(pulse2.ADD, (t) => {
          set7(t, _);
        });
        out.modifies(set7.output);
      }
    }
    if (pulse2.changed(pulse2.REM) && exit !== falsy) {
      pulse2.visit(pulse2.REM, (t) => {
        exit(t, _);
      });
      out.modifies(exit.output);
    }
    if (reenter || set7 !== falsy) {
      const flag2 = pulse2.MOD | (_.modified() ? pulse2.REFLOW : 0);
      if (reenter) {
        pulse2.visit(flag2, (t) => {
          const mod = enter(t, _) || fmod;
          if (set7(t, _) || mod) out.mod.push(t);
        });
        if (out.mod.length) out.modifies(enter.output);
      } else {
        pulse2.visit(flag2, (t) => {
          if (set7(t, _) || fmod) out.mod.push(t);
        });
      }
      if (out.mod.length) out.modifies(set7.output);
    }
    return out.changed() ? out : pulse2.StopPropagation;
  }
});
function LegendEntries(params2) {
  Transform.call(this, [], params2);
}
inherits_default(LegendEntries, Transform, {
  transform(_, pulse2) {
    if (this.value != null && !_.modified()) {
      return pulse2.StopPropagation;
    }
    var locale4 = pulse2.dataflow.locale(), out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS), items = this.value, type4 = _.type || SymbolLegend, scale9 = _.scale, limit = +_.limit, count2 = tickCount(scale9, _.count == null ? 5 : _.count, _.minstep), lskip = !!_.values || type4 === SymbolLegend, format6 = _.format || labelFormat(locale4, scale9, count2, type4, _.formatSpecifier, _.formatType, lskip), values4 = _.values || labelValues(scale9, count2), domain4, fraction, size, offset6, ellipsis;
    if (items) out.rem = items;
    if (type4 === SymbolLegend) {
      if (limit && values4.length > limit) {
        pulse2.dataflow.warn("Symbol legend count exceeds limit, filtering items.");
        items = values4.slice(0, limit - 1);
        ellipsis = true;
      } else {
        items = values4;
      }
      if (isFunction(size = _.size)) {
        if (!_.values && scale9(items[0]) === 0) {
          items = items.slice(1);
        }
        offset6 = items.reduce((max4, value4) => Math.max(max4, size(value4, _)), 0);
      } else {
        size = constant_default(offset6 = size || 8);
      }
      items = items.map((value4, index4) => ingest$1({
        index: index4,
        label: format6(value4, index4, items),
        value: value4,
        offset: offset6,
        size: size(value4, _)
      }));
      if (ellipsis) {
        ellipsis = values4[items.length];
        items.push(ingest$1({
          index: items.length,
          label: `\u2026${values4.length - items.length} entries`,
          value: ellipsis,
          offset: offset6,
          size: size(ellipsis, _)
        }));
      }
    } else if (type4 === GradientLegend) {
      domain4 = scale9.domain(), fraction = scaleFraction(scale9, domain4[0], peek(domain4));
      if (values4.length < 3 && !_.values && domain4[0] !== peek(domain4)) {
        values4 = [domain4[0], peek(domain4)];
      }
      items = values4.map((value4, index4) => ingest$1({
        index: index4,
        label: format6(value4, index4, values4),
        value: value4,
        perc: fraction(value4)
      }));
    } else {
      size = values4.length - 1;
      fraction = labelFraction(scale9);
      items = values4.map((value4, index4) => ingest$1({
        index: index4,
        label: format6(value4, index4, values4),
        value: value4,
        perc: index4 ? fraction(value4) : 0,
        perc2: index4 === size ? 1 : fraction(values4[index4 + 1])
      }));
    }
    out.source = items;
    out.add = items;
    this.value = items;
    return out;
  }
});
var sourceX = (t) => t.source.x;
var sourceY = (t) => t.source.y;
var targetX = (t) => t.target.x;
var targetY = (t) => t.target.y;
function LinkPath(params2) {
  Transform.call(this, {}, params2);
}
LinkPath.Definition = {
  "type": "LinkPath",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "sourceX",
    "type": "field",
    "default": "source.x"
  }, {
    "name": "sourceY",
    "type": "field",
    "default": "source.y"
  }, {
    "name": "targetX",
    "type": "field",
    "default": "target.x"
  }, {
    "name": "targetY",
    "type": "field",
    "default": "target.y"
  }, {
    "name": "orient",
    "type": "enum",
    "default": "vertical",
    "values": ["horizontal", "vertical", "radial"]
  }, {
    "name": "shape",
    "type": "enum",
    "default": "line",
    "values": ["line", "arc", "curve", "diagonal", "orthogonal"]
  }, {
    "name": "require",
    "type": "signal"
  }, {
    "name": "as",
    "type": "string",
    "default": "path"
  }]
};
inherits_default(LinkPath, Transform, {
  transform(_, pulse2) {
    var sx = _.sourceX || sourceX, sy = _.sourceY || sourceY, tx = _.targetX || targetX, ty = _.targetY || targetY, as = _.as || "path", orient2 = _.orient || "vertical", shape3 = _.shape || "line", path4 = Paths.get(shape3 + "-" + orient2) || Paths.get(shape3);
    if (!path4) {
      error("LinkPath unsupported type: " + _.shape + (_.orient ? "-" + _.orient : ""));
    }
    pulse2.visit(pulse2.SOURCE, (t) => {
      t[as] = path4(sx(t), sy(t), tx(t), ty(t));
    });
    return pulse2.reflow(_.modified()).modifies(as);
  }
});
var line2 = (sx, sy, tx, ty) => "M" + sx + "," + sy + "L" + tx + "," + ty;
var lineR = (sa3, sr, ta, tr3) => line2(sr * Math.cos(sa3), sr * Math.sin(sa3), tr3 * Math.cos(ta), tr3 * Math.sin(ta));
var arc2 = (sx, sy, tx, ty) => {
  var dx = tx - sx, dy = ty - sy, rr = Math.hypot(dx, dy) / 2, ra = 180 * Math.atan2(dy, dx) / Math.PI;
  return "M" + sx + "," + sy + "A" + rr + "," + rr + " " + ra + " 0 1 " + tx + "," + ty;
};
var arcR = (sa3, sr, ta, tr3) => arc2(sr * Math.cos(sa3), sr * Math.sin(sa3), tr3 * Math.cos(ta), tr3 * Math.sin(ta));
var curve = (sx, sy, tx, ty) => {
  const dx = tx - sx, dy = ty - sy, ix = 0.2 * (dx + dy), iy = 0.2 * (dy - dx);
  return "M" + sx + "," + sy + "C" + (sx + ix) + "," + (sy + iy) + " " + (tx + iy) + "," + (ty - ix) + " " + tx + "," + ty;
};
var curveR = (sa3, sr, ta, tr3) => curve(sr * Math.cos(sa3), sr * Math.sin(sa3), tr3 * Math.cos(ta), tr3 * Math.sin(ta));
var orthoX = (sx, sy, tx, ty) => "M" + sx + "," + sy + "V" + ty + "H" + tx;
var orthoY = (sx, sy, tx, ty) => "M" + sx + "," + sy + "H" + tx + "V" + ty;
var orthoR = (sa3, sr, ta, tr3) => {
  const sc = Math.cos(sa3), ss = Math.sin(sa3), tc = Math.cos(ta), ts3 = Math.sin(ta), sf = Math.abs(ta - sa3) > Math.PI ? ta <= sa3 : ta > sa3;
  return "M" + sr * sc + "," + sr * ss + "A" + sr + "," + sr + " 0 0," + (sf ? 1 : 0) + " " + sr * tc + "," + sr * ts3 + "L" + tr3 * tc + "," + tr3 * ts3;
};
var diagonalX = (sx, sy, tx, ty) => {
  const m3 = (sx + tx) / 2;
  return "M" + sx + "," + sy + "C" + m3 + "," + sy + " " + m3 + "," + ty + " " + tx + "," + ty;
};
var diagonalY = (sx, sy, tx, ty) => {
  const m3 = (sy + ty) / 2;
  return "M" + sx + "," + sy + "C" + sx + "," + m3 + " " + tx + "," + m3 + " " + tx + "," + ty;
};
var diagonalR = (sa3, sr, ta, tr3) => {
  const sc = Math.cos(sa3), ss = Math.sin(sa3), tc = Math.cos(ta), ts3 = Math.sin(ta), mr = (sr + tr3) / 2;
  return "M" + sr * sc + "," + sr * ss + "C" + mr * sc + "," + mr * ss + " " + mr * tc + "," + mr * ts3 + " " + tr3 * tc + "," + tr3 * ts3;
};
var Paths = fastmap({
  "line": line2,
  "line-radial": lineR,
  "arc": arc2,
  "arc-radial": arcR,
  "curve": curve,
  "curve-radial": curveR,
  "orthogonal-horizontal": orthoX,
  "orthogonal-vertical": orthoY,
  "orthogonal-radial": orthoR,
  "diagonal-horizontal": diagonalX,
  "diagonal-vertical": diagonalY,
  "diagonal-radial": diagonalR
});
function Pie(params2) {
  Transform.call(this, null, params2);
}
Pie.Definition = {
  "type": "Pie",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "field",
    "type": "field"
  }, {
    "name": "startAngle",
    "type": "number",
    "default": 0
  }, {
    "name": "endAngle",
    "type": "number",
    "default": 6.283185307179586
  }, {
    "name": "sort",
    "type": "boolean",
    "default": false
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": 2,
    "default": ["startAngle", "endAngle"]
  }]
};
inherits_default(Pie, Transform, {
  transform(_, pulse2) {
    var as = _.as || ["startAngle", "endAngle"], startAngle = as[0], endAngle = as[1], field3 = _.field || one, start = _.startAngle || 0, stop2 = _.endAngle != null ? _.endAngle : 2 * Math.PI, data4 = pulse2.source, values4 = data4.map(field3), n = values4.length, a3 = start, k = (stop2 - start) / sum(values4), index4 = range(n), i, t, v2;
    if (_.sort) {
      index4.sort((a4, b10) => values4[a4] - values4[b10]);
    }
    for (i = 0; i < n; ++i) {
      v2 = values4[index4[i]];
      t = data4[index4[i]];
      t[startAngle] = a3;
      t[endAngle] = a3 += v2 * k;
    }
    this.value = values4;
    return pulse2.reflow(_.modified()).modifies(as);
  }
});
var DEFAULT_COUNT = 5;
function includeZero(scale9) {
  const type4 = scale9.type;
  return !scale9.bins && (type4 === Linear2 || type4 === Pow || type4 === Sqrt);
}
function includePad(type4) {
  return isContinuous(type4) && type4 !== Sequential;
}
var SKIP2 = toSet(["set", "modified", "clear", "type", "scheme", "schemeExtent", "schemeCount", "domain", "domainMin", "domainMid", "domainMax", "domainRaw", "domainImplicit", "nice", "zero", "bins", "range", "rangeStep", "round", "reverse", "interpolate", "interpolateGamma"]);
function Scale(params2) {
  Transform.call(this, null, params2);
  this.modified(true);
}
inherits_default(Scale, Transform, {
  transform(_, pulse2) {
    var df = pulse2.dataflow, scale$13 = this.value, key2 = scaleKey(_);
    if (!scale$13 || key2 !== scale$13.type) {
      this.value = scale$13 = scale(key2)();
    }
    for (key2 in _) if (!SKIP2[key2]) {
      if (key2 === "padding" && includePad(scale$13.type)) continue;
      isFunction(scale$13[key2]) ? scale$13[key2](_[key2]) : df.warn("Unsupported scale property: " + key2);
    }
    configureRange(scale$13, _, configureBins(scale$13, _, configureDomain(scale$13, _, df)));
    return pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS);
  }
});
function scaleKey(_) {
  var t = _.type, d = "", n;
  if (t === Sequential) return Sequential + "-" + Linear2;
  if (isContinuousColor(_)) {
    n = _.rawDomain ? _.rawDomain.length : _.domain ? _.domain.length + +(_.domainMid != null) : 0;
    d = n === 2 ? Sequential + "-" : n === 3 ? Diverging + "-" : "";
  }
  return (d + t || Linear2).toLowerCase();
}
function isContinuousColor(_) {
  const t = _.type;
  return isContinuous(t) && t !== Time && t !== UTC && (_.scheme || _.range && _.range.length && _.range.every(isString));
}
function configureDomain(scale9, _, df) {
  const raw = rawDomain(scale9, _.domainRaw, df);
  if (raw > -1) return raw;
  var domain4 = _.domain, type4 = scale9.type, zero6 = _.zero || _.zero === void 0 && includeZero(scale9), n, mid;
  if (!domain4) return 0;
  if (zero6 || _.domainMin != null || _.domainMax != null || _.domainMid != null) {
    n = (domain4 = domain4.slice()).length - 1 || 1;
    if (zero6) {
      if (domain4[0] > 0) domain4[0] = 0;
      if (domain4[n] < 0) domain4[n] = 0;
    }
    if (_.domainMin != null) domain4[0] = _.domainMin;
    if (_.domainMax != null) domain4[n] = _.domainMax;
    if (_.domainMid != null) {
      mid = _.domainMid;
      const i = mid > domain4[n] ? n + 1 : mid < domain4[0] ? 0 : n;
      if (i !== n) df.warn("Scale domainMid exceeds domain min or max.", mid);
      domain4.splice(i, 0, mid);
    }
  }
  if (includePad(type4) && _.padding && domain4[0] !== peek(domain4)) {
    domain4 = padDomain(type4, domain4, _.range, _.padding, _.exponent, _.constant);
  }
  scale9.domain(domainCheck(type4, domain4, df));
  if (type4 === Ordinal) {
    scale9.unknown(_.domainImplicit ? implicit : void 0);
  }
  if (_.nice && scale9.nice) {
    scale9.nice(_.nice !== true && tickCount(scale9, _.nice) || null);
  }
  return domain4.length;
}
function rawDomain(scale9, raw, df) {
  if (raw) {
    scale9.domain(domainCheck(scale9.type, raw, df));
    return raw.length;
  } else {
    return -1;
  }
}
function padDomain(type4, domain4, range7, pad4, exponent2, constant2) {
  var span = Math.abs(peek(range7) - range7[0]), frac = span / (span - 2 * pad4), d = type4 === Log ? zoomLog(domain4, null, frac) : type4 === Sqrt ? zoomPow(domain4, null, frac, 0.5) : type4 === Pow ? zoomPow(domain4, null, frac, exponent2 || 1) : type4 === Symlog ? zoomSymlog(domain4, null, frac, constant2 || 1) : zoomLinear(domain4, null, frac);
  domain4 = domain4.slice();
  domain4[0] = d[0];
  domain4[domain4.length - 1] = d[1];
  return domain4;
}
function domainCheck(type4, domain4, df) {
  if (isLogarithmic(type4)) {
    var s = Math.abs(domain4.reduce((s2, v2) => s2 + (v2 < 0 ? -1 : v2 > 0 ? 1 : 0), 0));
    if (s !== domain4.length) {
      df.warn("Log scale domain includes zero: " + stringValue(domain4));
    }
  }
  return domain4;
}
function configureBins(scale9, _, count2) {
  let bins2 = _.bins;
  if (bins2 && !isArray(bins2)) {
    const domain4 = scale9.domain(), lo = domain4[0], hi = peek(domain4), step = bins2.step;
    let start = bins2.start == null ? lo : bins2.start, stop2 = bins2.stop == null ? hi : bins2.stop;
    if (!step) error("Scale bins parameter missing step property.");
    if (start < lo) start = step * Math.ceil(lo / step);
    if (stop2 > hi) stop2 = step * Math.floor(hi / step);
    bins2 = range(start, stop2 + step / 2, step);
  }
  if (bins2) {
    scale9.bins = bins2;
  } else if (scale9.bins) {
    delete scale9.bins;
  }
  if (scale9.type === BinOrdinal) {
    if (!bins2) {
      scale9.bins = scale9.domain();
    } else if (!_.domain && !_.domainRaw) {
      scale9.domain(bins2);
      count2 = bins2.length;
    }
  }
  return count2;
}
function configureRange(scale9, _, count2) {
  var type4 = scale9.type, round = _.round || false, range7 = _.range;
  if (_.rangeStep != null) {
    range7 = configureRangeStep(type4, _, count2);
  } else if (_.scheme) {
    range7 = configureScheme(type4, _, count2);
    if (isFunction(range7)) {
      if (scale9.interpolator) {
        return scale9.interpolator(range7);
      } else {
        error(`Scale type ${type4} does not support interpolating color schemes.`);
      }
    }
  }
  if (range7 && isInterpolating(type4)) {
    return scale9.interpolator(interpolateColors(flip(range7, _.reverse), _.interpolate, _.interpolateGamma));
  }
  if (range7 && _.interpolate && scale9.interpolate) {
    scale9.interpolate(interpolate(_.interpolate, _.interpolateGamma));
  } else if (isFunction(scale9.round)) {
    scale9.round(round);
  } else if (isFunction(scale9.rangeRound)) {
    scale9.interpolate(round ? round_default : value_default);
  }
  if (range7) scale9.range(flip(range7, _.reverse));
}
function configureRangeStep(type4, _, count2) {
  if (type4 !== Band && type4 !== Point) {
    error("Only band and point scales support rangeStep.");
  }
  var outer = (_.paddingOuter != null ? _.paddingOuter : _.padding) || 0, inner = type4 === Point ? 1 : (_.paddingInner != null ? _.paddingInner : _.padding) || 0;
  return [0, _.rangeStep * bandSpace(count2, inner, outer)];
}
function configureScheme(type4, _, count2) {
  var extent2 = _.schemeExtent, name, scheme$1;
  if (isArray(_.scheme)) {
    scheme$1 = interpolateColors(_.scheme, _.interpolate, _.interpolateGamma);
  } else {
    name = _.scheme.toLowerCase();
    scheme$1 = scheme(name);
    if (!scheme$1) error(`Unrecognized scheme name: ${_.scheme}`);
  }
  count2 = type4 === Threshold ? count2 + 1 : type4 === BinOrdinal ? count2 - 1 : type4 === Ordinal ? +_.schemeCount || count2 || DEFAULT_COUNT : type4 === Quantile2 || type4 === Quantize ? +_.schemeCount || DEFAULT_COUNT : count2;
  return isInterpolating(type4) ? adjustScheme(scheme$1, extent2, _.reverse) : isFunction(scheme$1) ? quantizeInterpolator(adjustScheme(scheme$1, extent2), count2) : type4 === Ordinal ? scheme$1 : scheme$1.slice(0, count2);
}
function adjustScheme(scheme3, extent2, reverse4) {
  return isFunction(scheme3) && (extent2 || reverse4) ? interpolateRange(scheme3, flip(extent2 || [0, 1], reverse4)) : scheme3;
}
function flip(array4, reverse4) {
  return reverse4 ? array4.slice().reverse() : array4;
}
function SortItems(params2) {
  Transform.call(this, null, params2);
}
inherits_default(SortItems, Transform, {
  transform(_, pulse2) {
    const mod = _.modified("sort") || pulse2.changed(pulse2.ADD) || pulse2.modified(_.sort.fields) || pulse2.modified("datum");
    if (mod) pulse2.source.sort(stableCompare(_.sort));
    this.modified(mod);
    return pulse2;
  }
});
var Zero = "zero";
var Center = "center";
var Normalize = "normalize";
var DefOutput = ["y0", "y1"];
function Stack(params2) {
  Transform.call(this, null, params2);
}
Stack.Definition = {
  "type": "Stack",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "field",
    "type": "field"
  }, {
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "sort",
    "type": "compare"
  }, {
    "name": "offset",
    "type": "enum",
    "default": Zero,
    "values": [Zero, Center, Normalize]
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": 2,
    "default": DefOutput
  }]
};
inherits_default(Stack, Transform, {
  transform(_, pulse2) {
    var as = _.as || DefOutput, y06 = as[0], y12 = as[1], sort4 = stableCompare(_.sort), field3 = _.field || one, stack2 = _.offset === Center ? stackCenter : _.offset === Normalize ? stackNormalize : stackZero, groups, i, n, max4;
    groups = partition2(pulse2.source, _.groupby, sort4, field3);
    for (i = 0, n = groups.length, max4 = groups.max; i < n; ++i) {
      stack2(groups[i], max4, field3, y06, y12);
    }
    return pulse2.reflow(_.modified()).modifies(as);
  }
});
function stackCenter(group3, max4, field3, y06, y12) {
  var last = (max4 - group3.sum) / 2, m3 = group3.length, j = 0, t;
  for (; j < m3; ++j) {
    t = group3[j];
    t[y06] = last;
    t[y12] = last += Math.abs(field3(t));
  }
}
function stackNormalize(group3, max4, field3, y06, y12) {
  var scale9 = 1 / group3.sum, last = 0, m3 = group3.length, j = 0, v2 = 0, t;
  for (; j < m3; ++j) {
    t = group3[j];
    t[y06] = last;
    t[y12] = last = scale9 * (v2 += Math.abs(field3(t)));
  }
}
function stackZero(group3, max4, field3, y06, y12) {
  var lastPos = 0, lastNeg = 0, m3 = group3.length, j = 0, v2, t;
  for (; j < m3; ++j) {
    t = group3[j];
    v2 = +field3(t);
    if (v2 < 0) {
      t[y06] = lastNeg;
      t[y12] = lastNeg += v2;
    } else {
      t[y06] = lastPos;
      t[y12] = lastPos += v2;
    }
  }
}
function partition2(data4, groupby, sort4, field3) {
  var groups = [], get7 = (f) => f(t), map3, i, n, m3, t, k, g, s, max4;
  if (groupby == null) {
    groups.push(data4.slice());
  } else {
    for (map3 = {}, i = 0, n = data4.length; i < n; ++i) {
      t = data4[i];
      k = JSON.stringify(groupby.map(get7));
      g = map3[k];
      if (!g) {
        map3[k] = g = [];
        groups.push(g);
      }
      g.push(t);
    }
  }
  for (k = 0, max4 = 0, m3 = groups.length; k < m3; ++k) {
    g = groups[k];
    for (i = 0, s = 0, n = g.length; i < n; ++i) {
      s += Math.abs(field3(g[i]));
    }
    g.sum = s;
    if (s > max4) max4 = s;
    if (sort4) g.sort(sort4);
  }
  groups.max = max4;
  return groups;
}

// ../deepseek-harness/node_modules/.pnpm/vega-geo@5.1.3/node_modules/vega-geo/build/vega-geo.js
var vega_geo_exports = {};
__export(vega_geo_exports, {
  contour: () => Contour,
  geojson: () => GeoJSON,
  geopath: () => GeoPath,
  geopoint: () => GeoPoint,
  geoshape: () => GeoShape,
  graticule: () => Graticule,
  heatmap: () => Heatmap,
  isocontour: () => Isocontour,
  kde2d: () => KDE2D,
  projection: () => Projection
});

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/math.js
var epsilon4 = 1e-6;
var epsilon23 = 1e-12;
var pi3 = Math.PI;
var halfPi2 = pi3 / 2;
var quarterPi = pi3 / 4;
var tau3 = pi3 * 2;
var degrees3 = 180 / pi3;
var radians2 = pi3 / 180;
var abs2 = Math.abs;
var atan = Math.atan;
var atan22 = Math.atan2;
var cos2 = Math.cos;
var ceil = Math.ceil;
var exp3 = Math.exp;
var hypot = Math.hypot;
var log5 = Math.log;
var pow4 = Math.pow;
var sin2 = Math.sin;
var sign2 = Math.sign || function(x6) {
  return x6 > 0 ? 1 : x6 < 0 ? -1 : 0;
};
var sqrt3 = Math.sqrt;
var tan = Math.tan;
function acos2(x6) {
  return x6 > 1 ? 0 : x6 < -1 ? pi3 : Math.acos(x6);
}
function asin2(x6) {
  return x6 > 1 ? halfPi2 : x6 < -1 ? -halfPi2 : Math.asin(x6);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/noop.js
function noop2() {
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/stream.js
function streamGeometry(geometry, stream2) {
  if (geometry && streamGeometryType.hasOwnProperty(geometry.type)) {
    streamGeometryType[geometry.type](geometry, stream2);
  }
}
var streamObjectType = {
  Feature: function(object2, stream2) {
    streamGeometry(object2.geometry, stream2);
  },
  FeatureCollection: function(object2, stream2) {
    var features = object2.features, i = -1, n = features.length;
    while (++i < n) streamGeometry(features[i].geometry, stream2);
  }
};
var streamGeometryType = {
  Sphere: function(object2, stream2) {
    stream2.sphere();
  },
  Point: function(object2, stream2) {
    object2 = object2.coordinates;
    stream2.point(object2[0], object2[1], object2[2]);
  },
  MultiPoint: function(object2, stream2) {
    var coordinates = object2.coordinates, i = -1, n = coordinates.length;
    while (++i < n) object2 = coordinates[i], stream2.point(object2[0], object2[1], object2[2]);
  },
  LineString: function(object2, stream2) {
    streamLine(object2.coordinates, stream2, 0);
  },
  MultiLineString: function(object2, stream2) {
    var coordinates = object2.coordinates, i = -1, n = coordinates.length;
    while (++i < n) streamLine(coordinates[i], stream2, 0);
  },
  Polygon: function(object2, stream2) {
    streamPolygon(object2.coordinates, stream2);
  },
  MultiPolygon: function(object2, stream2) {
    var coordinates = object2.coordinates, i = -1, n = coordinates.length;
    while (++i < n) streamPolygon(coordinates[i], stream2);
  },
  GeometryCollection: function(object2, stream2) {
    var geometries = object2.geometries, i = -1, n = geometries.length;
    while (++i < n) streamGeometry(geometries[i], stream2);
  }
};
function streamLine(coordinates, stream2, closed) {
  var i = -1, n = coordinates.length - closed, coordinate;
  stream2.lineStart();
  while (++i < n) coordinate = coordinates[i], stream2.point(coordinate[0], coordinate[1], coordinate[2]);
  stream2.lineEnd();
}
function streamPolygon(coordinates, stream2) {
  var i = -1, n = coordinates.length;
  stream2.polygonStart();
  while (++i < n) streamLine(coordinates[i], stream2, 1);
  stream2.polygonEnd();
}
function stream_default(object2, stream2) {
  if (object2 && streamObjectType.hasOwnProperty(object2.type)) {
    streamObjectType[object2.type](object2, stream2);
  } else {
    streamGeometry(object2, stream2);
  }
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/area.js
var areaRingSum = new Adder();
var areaSum = new Adder();
var lambda00;
var phi00;
var lambda0;
var cosPhi0;
var sinPhi0;
var areaStream = {
  point: noop2,
  lineStart: noop2,
  lineEnd: noop2,
  polygonStart: function() {
    areaRingSum = new Adder();
    areaStream.lineStart = areaRingStart;
    areaStream.lineEnd = areaRingEnd;
  },
  polygonEnd: function() {
    var areaRing = +areaRingSum;
    areaSum.add(areaRing < 0 ? tau3 + areaRing : areaRing);
    this.lineStart = this.lineEnd = this.point = noop2;
  },
  sphere: function() {
    areaSum.add(tau3);
  }
};
function areaRingStart() {
  areaStream.point = areaPointFirst;
}
function areaRingEnd() {
  areaPoint(lambda00, phi00);
}
function areaPointFirst(lambda, phi2) {
  areaStream.point = areaPoint;
  lambda00 = lambda, phi00 = phi2;
  lambda *= radians2, phi2 *= radians2;
  lambda0 = lambda, cosPhi0 = cos2(phi2 = phi2 / 2 + quarterPi), sinPhi0 = sin2(phi2);
}
function areaPoint(lambda, phi2) {
  lambda *= radians2, phi2 *= radians2;
  phi2 = phi2 / 2 + quarterPi;
  var dLambda = lambda - lambda0, sdLambda = dLambda >= 0 ? 1 : -1, adLambda = sdLambda * dLambda, cosPhi = cos2(phi2), sinPhi = sin2(phi2), k = sinPhi0 * sinPhi, u4 = cosPhi0 * cosPhi + k * cos2(adLambda), v2 = k * sdLambda * sin2(adLambda);
  areaRingSum.add(atan22(v2, u4));
  lambda0 = lambda, cosPhi0 = cosPhi, sinPhi0 = sinPhi;
}
function area_default2(object2) {
  areaSum = new Adder();
  stream_default(object2, areaStream);
  return areaSum * 2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/cartesian.js
function spherical(cartesian2) {
  return [atan22(cartesian2[1], cartesian2[0]), asin2(cartesian2[2])];
}
function cartesian(spherical2) {
  var lambda = spherical2[0], phi2 = spherical2[1], cosPhi = cos2(phi2);
  return [cosPhi * cos2(lambda), cosPhi * sin2(lambda), sin2(phi2)];
}
function cartesianDot(a3, b10) {
  return a3[0] * b10[0] + a3[1] * b10[1] + a3[2] * b10[2];
}
function cartesianCross(a3, b10) {
  return [a3[1] * b10[2] - a3[2] * b10[1], a3[2] * b10[0] - a3[0] * b10[2], a3[0] * b10[1] - a3[1] * b10[0]];
}
function cartesianAddInPlace(a3, b10) {
  a3[0] += b10[0], a3[1] += b10[1], a3[2] += b10[2];
}
function cartesianScale(vector, k) {
  return [vector[0] * k, vector[1] * k, vector[2] * k];
}
function cartesianNormalizeInPlace(d) {
  var l = sqrt3(d[0] * d[0] + d[1] * d[1] + d[2] * d[2]);
  d[0] /= l, d[1] /= l, d[2] /= l;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/bounds.js
var lambda02;
var phi0;
var lambda1;
var phi1;
var lambda2;
var lambda002;
var phi002;
var p0;
var deltaSum;
var ranges;
var range2;
var boundsStream = {
  point: boundsPoint,
  lineStart: boundsLineStart,
  lineEnd: boundsLineEnd,
  polygonStart: function() {
    boundsStream.point = boundsRingPoint;
    boundsStream.lineStart = boundsRingStart;
    boundsStream.lineEnd = boundsRingEnd;
    deltaSum = new Adder();
    areaStream.polygonStart();
  },
  polygonEnd: function() {
    areaStream.polygonEnd();
    boundsStream.point = boundsPoint;
    boundsStream.lineStart = boundsLineStart;
    boundsStream.lineEnd = boundsLineEnd;
    if (areaRingSum < 0) lambda02 = -(lambda1 = 180), phi0 = -(phi1 = 90);
    else if (deltaSum > epsilon4) phi1 = 90;
    else if (deltaSum < -epsilon4) phi0 = -90;
    range2[0] = lambda02, range2[1] = lambda1;
  },
  sphere: function() {
    lambda02 = -(lambda1 = 180), phi0 = -(phi1 = 90);
  }
};
function boundsPoint(lambda, phi2) {
  ranges.push(range2 = [lambda02 = lambda, lambda1 = lambda]);
  if (phi2 < phi0) phi0 = phi2;
  if (phi2 > phi1) phi1 = phi2;
}
function linePoint(lambda, phi2) {
  var p = cartesian([lambda * radians2, phi2 * radians2]);
  if (p0) {
    var normal = cartesianCross(p0, p), equatorial = [normal[1], -normal[0], 0], inflection = cartesianCross(equatorial, normal);
    cartesianNormalizeInPlace(inflection);
    inflection = spherical(inflection);
    var delta = lambda - lambda2, sign3 = delta > 0 ? 1 : -1, lambdai = inflection[0] * degrees3 * sign3, phii, antimeridian = abs2(delta) > 180;
    if (antimeridian ^ (sign3 * lambda2 < lambdai && lambdai < sign3 * lambda)) {
      phii = inflection[1] * degrees3;
      if (phii > phi1) phi1 = phii;
    } else if (lambdai = (lambdai + 360) % 360 - 180, antimeridian ^ (sign3 * lambda2 < lambdai && lambdai < sign3 * lambda)) {
      phii = -inflection[1] * degrees3;
      if (phii < phi0) phi0 = phii;
    } else {
      if (phi2 < phi0) phi0 = phi2;
      if (phi2 > phi1) phi1 = phi2;
    }
    if (antimeridian) {
      if (lambda < lambda2) {
        if (angle(lambda02, lambda) > angle(lambda02, lambda1)) lambda1 = lambda;
      } else {
        if (angle(lambda, lambda1) > angle(lambda02, lambda1)) lambda02 = lambda;
      }
    } else {
      if (lambda1 >= lambda02) {
        if (lambda < lambda02) lambda02 = lambda;
        if (lambda > lambda1) lambda1 = lambda;
      } else {
        if (lambda > lambda2) {
          if (angle(lambda02, lambda) > angle(lambda02, lambda1)) lambda1 = lambda;
        } else {
          if (angle(lambda, lambda1) > angle(lambda02, lambda1)) lambda02 = lambda;
        }
      }
    }
  } else {
    ranges.push(range2 = [lambda02 = lambda, lambda1 = lambda]);
  }
  if (phi2 < phi0) phi0 = phi2;
  if (phi2 > phi1) phi1 = phi2;
  p0 = p, lambda2 = lambda;
}
function boundsLineStart() {
  boundsStream.point = linePoint;
}
function boundsLineEnd() {
  range2[0] = lambda02, range2[1] = lambda1;
  boundsStream.point = boundsPoint;
  p0 = null;
}
function boundsRingPoint(lambda, phi2) {
  if (p0) {
    var delta = lambda - lambda2;
    deltaSum.add(abs2(delta) > 180 ? delta + (delta > 0 ? 360 : -360) : delta);
  } else {
    lambda002 = lambda, phi002 = phi2;
  }
  areaStream.point(lambda, phi2);
  linePoint(lambda, phi2);
}
function boundsRingStart() {
  areaStream.lineStart();
}
function boundsRingEnd() {
  boundsRingPoint(lambda002, phi002);
  areaStream.lineEnd();
  if (abs2(deltaSum) > epsilon4) lambda02 = -(lambda1 = 180);
  range2[0] = lambda02, range2[1] = lambda1;
  p0 = null;
}
function angle(lambda03, lambda12) {
  return (lambda12 -= lambda03) < 0 ? lambda12 + 360 : lambda12;
}
function rangeCompare(a3, b10) {
  return a3[0] - b10[0];
}
function rangeContains(range7, x6) {
  return range7[0] <= range7[1] ? range7[0] <= x6 && x6 <= range7[1] : x6 < range7[0] || range7[1] < x6;
}
function bounds_default(feature2) {
  var i, n, a3, b10, merged, deltaMax, delta;
  phi1 = lambda1 = -(lambda02 = phi0 = Infinity);
  ranges = [];
  stream_default(feature2, boundsStream);
  if (n = ranges.length) {
    ranges.sort(rangeCompare);
    for (i = 1, a3 = ranges[0], merged = [a3]; i < n; ++i) {
      b10 = ranges[i];
      if (rangeContains(a3, b10[0]) || rangeContains(a3, b10[1])) {
        if (angle(a3[0], b10[1]) > angle(a3[0], a3[1])) a3[1] = b10[1];
        if (angle(b10[0], a3[1]) > angle(a3[0], a3[1])) a3[0] = b10[0];
      } else {
        merged.push(a3 = b10);
      }
    }
    for (deltaMax = -Infinity, n = merged.length - 1, i = 0, a3 = merged[n]; i <= n; a3 = b10, ++i) {
      b10 = merged[i];
      if ((delta = angle(a3[1], b10[0])) > deltaMax) deltaMax = delta, lambda02 = b10[0], lambda1 = a3[1];
    }
  }
  ranges = range2 = null;
  return lambda02 === Infinity || phi0 === Infinity ? [[NaN, NaN], [NaN, NaN]] : [[lambda02, phi0], [lambda1, phi1]];
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/centroid.js
var W0;
var W1;
var X0;
var Y0;
var Z0;
var X1;
var Y1;
var Z1;
var X2;
var Y2;
var Z2;
var lambda003;
var phi003;
var x0;
var y0;
var z0;
var centroidStream = {
  sphere: noop2,
  point: centroidPoint,
  lineStart: centroidLineStart,
  lineEnd: centroidLineEnd,
  polygonStart: function() {
    centroidStream.lineStart = centroidRingStart;
    centroidStream.lineEnd = centroidRingEnd;
  },
  polygonEnd: function() {
    centroidStream.lineStart = centroidLineStart;
    centroidStream.lineEnd = centroidLineEnd;
  }
};
function centroidPoint(lambda, phi2) {
  lambda *= radians2, phi2 *= radians2;
  var cosPhi = cos2(phi2);
  centroidPointCartesian(cosPhi * cos2(lambda), cosPhi * sin2(lambda), sin2(phi2));
}
function centroidPointCartesian(x6, y6, z) {
  ++W0;
  X0 += (x6 - X0) / W0;
  Y0 += (y6 - Y0) / W0;
  Z0 += (z - Z0) / W0;
}
function centroidLineStart() {
  centroidStream.point = centroidLinePointFirst;
}
function centroidLinePointFirst(lambda, phi2) {
  lambda *= radians2, phi2 *= radians2;
  var cosPhi = cos2(phi2);
  x0 = cosPhi * cos2(lambda);
  y0 = cosPhi * sin2(lambda);
  z0 = sin2(phi2);
  centroidStream.point = centroidLinePoint;
  centroidPointCartesian(x0, y0, z0);
}
function centroidLinePoint(lambda, phi2) {
  lambda *= radians2, phi2 *= radians2;
  var cosPhi = cos2(phi2), x6 = cosPhi * cos2(lambda), y6 = cosPhi * sin2(lambda), z = sin2(phi2), w3 = atan22(sqrt3((w3 = y0 * z - z0 * y6) * w3 + (w3 = z0 * x6 - x0 * z) * w3 + (w3 = x0 * y6 - y0 * x6) * w3), x0 * x6 + y0 * y6 + z0 * z);
  W1 += w3;
  X1 += w3 * (x0 + (x0 = x6));
  Y1 += w3 * (y0 + (y0 = y6));
  Z1 += w3 * (z0 + (z0 = z));
  centroidPointCartesian(x0, y0, z0);
}
function centroidLineEnd() {
  centroidStream.point = centroidPoint;
}
function centroidRingStart() {
  centroidStream.point = centroidRingPointFirst;
}
function centroidRingEnd() {
  centroidRingPoint(lambda003, phi003);
  centroidStream.point = centroidPoint;
}
function centroidRingPointFirst(lambda, phi2) {
  lambda003 = lambda, phi003 = phi2;
  lambda *= radians2, phi2 *= radians2;
  centroidStream.point = centroidRingPoint;
  var cosPhi = cos2(phi2);
  x0 = cosPhi * cos2(lambda);
  y0 = cosPhi * sin2(lambda);
  z0 = sin2(phi2);
  centroidPointCartesian(x0, y0, z0);
}
function centroidRingPoint(lambda, phi2) {
  lambda *= radians2, phi2 *= radians2;
  var cosPhi = cos2(phi2), x6 = cosPhi * cos2(lambda), y6 = cosPhi * sin2(lambda), z = sin2(phi2), cx = y0 * z - z0 * y6, cy = z0 * x6 - x0 * z, cz = x0 * y6 - y0 * x6, m3 = hypot(cx, cy, cz), w3 = asin2(m3), v2 = m3 && -w3 / m3;
  X2.add(v2 * cx);
  Y2.add(v2 * cy);
  Z2.add(v2 * cz);
  W1 += w3;
  X1 += w3 * (x0 + (x0 = x6));
  Y1 += w3 * (y0 + (y0 = y6));
  Z1 += w3 * (z0 + (z0 = z));
  centroidPointCartesian(x0, y0, z0);
}
function centroid_default(object2) {
  W0 = W1 = X0 = Y0 = Z0 = X1 = Y1 = Z1 = 0;
  X2 = new Adder();
  Y2 = new Adder();
  Z2 = new Adder();
  stream_default(object2, centroidStream);
  var x6 = +X2, y6 = +Y2, z = +Z2, m3 = hypot(x6, y6, z);
  if (m3 < epsilon23) {
    x6 = X1, y6 = Y1, z = Z1;
    if (W1 < epsilon4) x6 = X0, y6 = Y0, z = Z0;
    m3 = hypot(x6, y6, z);
    if (m3 < epsilon23) return [NaN, NaN];
  }
  return [atan22(y6, x6) * degrees3, asin2(z / m3) * degrees3];
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/compose.js
function compose_default(a3, b10) {
  function compose(x6, y6) {
    return x6 = a3(x6, y6), b10(x6[0], x6[1]);
  }
  if (a3.invert && b10.invert) compose.invert = function(x6, y6) {
    return x6 = b10.invert(x6, y6), x6 && a3.invert(x6[0], x6[1]);
  };
  return compose;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/rotation.js
function rotationIdentity(lambda, phi2) {
  if (abs2(lambda) > pi3) lambda -= Math.round(lambda / tau3) * tau3;
  return [lambda, phi2];
}
rotationIdentity.invert = rotationIdentity;
function rotateRadians(deltaLambda, deltaPhi, deltaGamma) {
  return (deltaLambda %= tau3) ? deltaPhi || deltaGamma ? compose_default(rotationLambda(deltaLambda), rotationPhiGamma(deltaPhi, deltaGamma)) : rotationLambda(deltaLambda) : deltaPhi || deltaGamma ? rotationPhiGamma(deltaPhi, deltaGamma) : rotationIdentity;
}
function forwardRotationLambda(deltaLambda) {
  return function(lambda, phi2) {
    lambda += deltaLambda;
    if (abs2(lambda) > pi3) lambda -= Math.round(lambda / tau3) * tau3;
    return [lambda, phi2];
  };
}
function rotationLambda(deltaLambda) {
  var rotation = forwardRotationLambda(deltaLambda);
  rotation.invert = forwardRotationLambda(-deltaLambda);
  return rotation;
}
function rotationPhiGamma(deltaPhi, deltaGamma) {
  var cosDeltaPhi = cos2(deltaPhi), sinDeltaPhi = sin2(deltaPhi), cosDeltaGamma = cos2(deltaGamma), sinDeltaGamma = sin2(deltaGamma);
  function rotation(lambda, phi2) {
    var cosPhi = cos2(phi2), x6 = cos2(lambda) * cosPhi, y6 = sin2(lambda) * cosPhi, z = sin2(phi2), k = z * cosDeltaPhi + x6 * sinDeltaPhi;
    return [
      atan22(y6 * cosDeltaGamma - k * sinDeltaGamma, x6 * cosDeltaPhi - z * sinDeltaPhi),
      asin2(k * cosDeltaGamma + y6 * sinDeltaGamma)
    ];
  }
  rotation.invert = function(lambda, phi2) {
    var cosPhi = cos2(phi2), x6 = cos2(lambda) * cosPhi, y6 = sin2(lambda) * cosPhi, z = sin2(phi2), k = z * cosDeltaGamma - y6 * sinDeltaGamma;
    return [
      atan22(y6 * cosDeltaGamma + z * sinDeltaGamma, x6 * cosDeltaPhi + k * sinDeltaPhi),
      asin2(k * cosDeltaPhi - x6 * sinDeltaPhi)
    ];
  };
  return rotation;
}
function rotation_default(rotate3) {
  rotate3 = rotateRadians(rotate3[0] * radians2, rotate3[1] * radians2, rotate3.length > 2 ? rotate3[2] * radians2 : 0);
  function forward(coordinates) {
    coordinates = rotate3(coordinates[0] * radians2, coordinates[1] * radians2);
    return coordinates[0] *= degrees3, coordinates[1] *= degrees3, coordinates;
  }
  forward.invert = function(coordinates) {
    coordinates = rotate3.invert(coordinates[0] * radians2, coordinates[1] * radians2);
    return coordinates[0] *= degrees3, coordinates[1] *= degrees3, coordinates;
  };
  return forward;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/circle.js
function circleStream(stream2, radius2, delta, direction, t05, t13) {
  if (!delta) return;
  var cosRadius = cos2(radius2), sinRadius = sin2(radius2), step = direction * delta;
  if (t05 == null) {
    t05 = radius2 + direction * tau3;
    t13 = radius2 - step / 2;
  } else {
    t05 = circleRadius(cosRadius, t05);
    t13 = circleRadius(cosRadius, t13);
    if (direction > 0 ? t05 < t13 : t05 > t13) t05 += direction * tau3;
  }
  for (var point9, t = t05; direction > 0 ? t > t13 : t < t13; t -= step) {
    point9 = spherical([cosRadius, -sinRadius * cos2(t), -sinRadius * sin2(t)]);
    stream2.point(point9[0], point9[1]);
  }
}
function circleRadius(cosRadius, point9) {
  point9 = cartesian(point9), point9[0] -= cosRadius;
  cartesianNormalizeInPlace(point9);
  var radius2 = acos2(-point9[1]);
  return ((-point9[2] < 0 ? -radius2 : radius2) + tau3 - epsilon4) % tau3;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/clip/buffer.js
function buffer_default() {
  var lines = [], line5;
  return {
    point: function(x6, y6, m3) {
      line5.push([x6, y6, m3]);
    },
    lineStart: function() {
      lines.push(line5 = []);
    },
    lineEnd: noop2,
    rejoin: function() {
      if (lines.length > 1) lines.push(lines.pop().concat(lines.shift()));
    },
    result: function() {
      var result = lines;
      lines = [];
      line5 = null;
      return result;
    }
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/pointEqual.js
function pointEqual_default(a3, b10) {
  return abs2(a3[0] - b10[0]) < epsilon4 && abs2(a3[1] - b10[1]) < epsilon4;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/clip/rejoin.js
function Intersection(point9, points2, other, entry2) {
  this.x = point9;
  this.z = points2;
  this.o = other;
  this.e = entry2;
  this.v = false;
  this.n = this.p = null;
}
function rejoin_default(segments3, compareIntersection2, startInside, interpolate3, stream2) {
  var subject = [], clip4 = [], i, n;
  segments3.forEach(function(segment) {
    if ((n2 = segment.length - 1) <= 0) return;
    var n2, p02 = segment[0], p1 = segment[n2], x6;
    if (pointEqual_default(p02, p1)) {
      if (!p02[2] && !p1[2]) {
        stream2.lineStart();
        for (i = 0; i < n2; ++i) stream2.point((p02 = segment[i])[0], p02[1]);
        stream2.lineEnd();
        return;
      }
      p1[0] += 2 * epsilon4;
    }
    subject.push(x6 = new Intersection(p02, segment, null, true));
    clip4.push(x6.o = new Intersection(p02, null, x6, false));
    subject.push(x6 = new Intersection(p1, segment, null, false));
    clip4.push(x6.o = new Intersection(p1, null, x6, true));
  });
  if (!subject.length) return;
  clip4.sort(compareIntersection2);
  link(subject);
  link(clip4);
  for (i = 0, n = clip4.length; i < n; ++i) {
    clip4[i].e = startInside = !startInside;
  }
  var start = subject[0], points2, point9;
  while (1) {
    var current2 = start, isSubject = true;
    while (current2.v) if ((current2 = current2.n) === start) return;
    points2 = current2.z;
    stream2.lineStart();
    do {
      current2.v = current2.o.v = true;
      if (current2.e) {
        if (isSubject) {
          for (i = 0, n = points2.length; i < n; ++i) stream2.point((point9 = points2[i])[0], point9[1]);
        } else {
          interpolate3(current2.x, current2.n.x, 1, stream2);
        }
        current2 = current2.n;
      } else {
        if (isSubject) {
          points2 = current2.p.z;
          for (i = points2.length - 1; i >= 0; --i) stream2.point((point9 = points2[i])[0], point9[1]);
        } else {
          interpolate3(current2.x, current2.p.x, -1, stream2);
        }
        current2 = current2.p;
      }
      current2 = current2.o;
      points2 = current2.z;
      isSubject = !isSubject;
    } while (!current2.v);
    stream2.lineEnd();
  }
}
function link(array4) {
  if (!(n = array4.length)) return;
  var n, i = 0, a3 = array4[0], b10;
  while (++i < n) {
    a3.n = b10 = array4[i];
    b10.p = a3;
    a3 = b10;
  }
  a3.n = b10 = array4[0];
  b10.p = a3;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/polygonContains.js
function longitude(point9) {
  return abs2(point9[0]) <= pi3 ? point9[0] : sign2(point9[0]) * ((abs2(point9[0]) + pi3) % tau3 - pi3);
}
function polygonContains_default(polygon, point9) {
  var lambda = longitude(point9), phi2 = point9[1], sinPhi = sin2(phi2), normal = [sin2(lambda), -cos2(lambda), 0], angle2 = 0, winding = 0;
  var sum3 = new Adder();
  if (sinPhi === 1) phi2 = halfPi2 + epsilon4;
  else if (sinPhi === -1) phi2 = -halfPi2 - epsilon4;
  for (var i = 0, n = polygon.length; i < n; ++i) {
    if (!(m3 = (ring = polygon[i]).length)) continue;
    var ring, m3, point0 = ring[m3 - 1], lambda03 = longitude(point0), phi02 = point0[1] / 2 + quarterPi, sinPhi02 = sin2(phi02), cosPhi02 = cos2(phi02);
    for (var j = 0; j < m3; ++j, lambda03 = lambda12, sinPhi02 = sinPhi1, cosPhi02 = cosPhi1, point0 = point1) {
      var point1 = ring[j], lambda12 = longitude(point1), phi12 = point1[1] / 2 + quarterPi, sinPhi1 = sin2(phi12), cosPhi1 = cos2(phi12), delta = lambda12 - lambda03, sign3 = delta >= 0 ? 1 : -1, absDelta = sign3 * delta, antimeridian = absDelta > pi3, k = sinPhi02 * sinPhi1;
      sum3.add(atan22(k * sign3 * sin2(absDelta), cosPhi02 * cosPhi1 + k * cos2(absDelta)));
      angle2 += antimeridian ? delta + sign3 * tau3 : delta;
      if (antimeridian ^ lambda03 >= lambda ^ lambda12 >= lambda) {
        var arc5 = cartesianCross(cartesian(point0), cartesian(point1));
        cartesianNormalizeInPlace(arc5);
        var intersection2 = cartesianCross(normal, arc5);
        cartesianNormalizeInPlace(intersection2);
        var phiArc = (antimeridian ^ delta >= 0 ? -1 : 1) * asin2(intersection2[2]);
        if (phi2 > phiArc || phi2 === phiArc && (arc5[0] || arc5[1])) {
          winding += antimeridian ^ delta >= 0 ? 1 : -1;
        }
      }
    }
  }
  return (angle2 < -epsilon4 || angle2 < epsilon4 && sum3 < -epsilon23) ^ winding & 1;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/clip/index.js
function clip_default(pointVisible, clipLine, interpolate3, start) {
  return function(sink) {
    var line5 = clipLine(sink), ringBuffer = buffer_default(), ringSink = clipLine(ringBuffer), polygonStarted = false, polygon, segments3, ring;
    var clip4 = {
      point: point9,
      lineStart,
      lineEnd,
      polygonStart: function() {
        clip4.point = pointRing;
        clip4.lineStart = ringStart;
        clip4.lineEnd = ringEnd;
        segments3 = [];
        polygon = [];
      },
      polygonEnd: function() {
        clip4.point = point9;
        clip4.lineStart = lineStart;
        clip4.lineEnd = lineEnd;
        segments3 = merge2(segments3);
        var startInside = polygonContains_default(polygon, start);
        if (segments3.length) {
          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
          rejoin_default(segments3, compareIntersection, startInside, interpolate3, sink);
        } else if (startInside) {
          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
          sink.lineStart();
          interpolate3(null, null, 1, sink);
          sink.lineEnd();
        }
        if (polygonStarted) sink.polygonEnd(), polygonStarted = false;
        segments3 = polygon = null;
      },
      sphere: function() {
        sink.polygonStart();
        sink.lineStart();
        interpolate3(null, null, 1, sink);
        sink.lineEnd();
        sink.polygonEnd();
      }
    };
    function point9(lambda, phi2) {
      if (pointVisible(lambda, phi2)) sink.point(lambda, phi2);
    }
    function pointLine(lambda, phi2) {
      line5.point(lambda, phi2);
    }
    function lineStart() {
      clip4.point = pointLine;
      line5.lineStart();
    }
    function lineEnd() {
      clip4.point = point9;
      line5.lineEnd();
    }
    function pointRing(lambda, phi2) {
      ring.push([lambda, phi2]);
      ringSink.point(lambda, phi2);
    }
    function ringStart() {
      ringSink.lineStart();
      ring = [];
    }
    function ringEnd() {
      pointRing(ring[0][0], ring[0][1]);
      ringSink.lineEnd();
      var clean = ringSink.clean(), ringSegments = ringBuffer.result(), i, n = ringSegments.length, m3, segment, point10;
      ring.pop();
      polygon.push(ring);
      ring = null;
      if (!n) return;
      if (clean & 1) {
        segment = ringSegments[0];
        if ((m3 = segment.length - 1) > 0) {
          if (!polygonStarted) sink.polygonStart(), polygonStarted = true;
          sink.lineStart();
          for (i = 0; i < m3; ++i) sink.point((point10 = segment[i])[0], point10[1]);
          sink.lineEnd();
        }
        return;
      }
      if (n > 1 && clean & 2) ringSegments.push(ringSegments.pop().concat(ringSegments.shift()));
      segments3.push(ringSegments.filter(validSegment));
    }
    return clip4;
  };
}
function validSegment(segment) {
  return segment.length > 1;
}
function compareIntersection(a3, b10) {
  return ((a3 = a3.x)[0] < 0 ? a3[1] - halfPi2 - epsilon4 : halfPi2 - a3[1]) - ((b10 = b10.x)[0] < 0 ? b10[1] - halfPi2 - epsilon4 : halfPi2 - b10[1]);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/clip/antimeridian.js
var antimeridian_default = clip_default(
  function() {
    return true;
  },
  clipAntimeridianLine,
  clipAntimeridianInterpolate,
  [-pi3, -halfPi2]
);
function clipAntimeridianLine(stream2) {
  var lambda03 = NaN, phi02 = NaN, sign0 = NaN, clean;
  return {
    lineStart: function() {
      stream2.lineStart();
      clean = 1;
    },
    point: function(lambda12, phi12) {
      var sign1 = lambda12 > 0 ? pi3 : -pi3, delta = abs2(lambda12 - lambda03);
      if (abs2(delta - pi3) < epsilon4) {
        stream2.point(lambda03, phi02 = (phi02 + phi12) / 2 > 0 ? halfPi2 : -halfPi2);
        stream2.point(sign0, phi02);
        stream2.lineEnd();
        stream2.lineStart();
        stream2.point(sign1, phi02);
        stream2.point(lambda12, phi02);
        clean = 0;
      } else if (sign0 !== sign1 && delta >= pi3) {
        if (abs2(lambda03 - sign0) < epsilon4) lambda03 -= sign0 * epsilon4;
        if (abs2(lambda12 - sign1) < epsilon4) lambda12 -= sign1 * epsilon4;
        phi02 = clipAntimeridianIntersect(lambda03, phi02, lambda12, phi12);
        stream2.point(sign0, phi02);
        stream2.lineEnd();
        stream2.lineStart();
        stream2.point(sign1, phi02);
        clean = 0;
      }
      stream2.point(lambda03 = lambda12, phi02 = phi12);
      sign0 = sign1;
    },
    lineEnd: function() {
      stream2.lineEnd();
      lambda03 = phi02 = NaN;
    },
    clean: function() {
      return 2 - clean;
    }
  };
}
function clipAntimeridianIntersect(lambda03, phi02, lambda12, phi12) {
  var cosPhi02, cosPhi1, sinLambda0Lambda1 = sin2(lambda03 - lambda12);
  return abs2(sinLambda0Lambda1) > epsilon4 ? atan((sin2(phi02) * (cosPhi1 = cos2(phi12)) * sin2(lambda12) - sin2(phi12) * (cosPhi02 = cos2(phi02)) * sin2(lambda03)) / (cosPhi02 * cosPhi1 * sinLambda0Lambda1)) : (phi02 + phi12) / 2;
}
function clipAntimeridianInterpolate(from, to, direction, stream2) {
  var phi2;
  if (from == null) {
    phi2 = direction * halfPi2;
    stream2.point(-pi3, phi2);
    stream2.point(0, phi2);
    stream2.point(pi3, phi2);
    stream2.point(pi3, 0);
    stream2.point(pi3, -phi2);
    stream2.point(0, -phi2);
    stream2.point(-pi3, -phi2);
    stream2.point(-pi3, 0);
    stream2.point(-pi3, phi2);
  } else if (abs2(from[0] - to[0]) > epsilon4) {
    var lambda = from[0] < to[0] ? pi3 : -pi3;
    phi2 = direction * lambda / 2;
    stream2.point(-lambda, phi2);
    stream2.point(0, phi2);
    stream2.point(lambda, phi2);
  } else {
    stream2.point(to[0], to[1]);
  }
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/clip/circle.js
function circle_default2(radius2) {
  var cr3 = cos2(radius2), delta = 2 * radians2, smallRadius = cr3 > 0, notHemisphere = abs2(cr3) > epsilon4;
  function interpolate3(from, to, direction, stream2) {
    circleStream(stream2, radius2, delta, direction, from, to);
  }
  function visible(lambda, phi2) {
    return cos2(lambda) * cos2(phi2) > cr3;
  }
  function clipLine(stream2) {
    var point0, c0, v0, v00, clean;
    return {
      lineStart: function() {
        v00 = v0 = false;
        clean = 1;
      },
      point: function(lambda, phi2) {
        var point1 = [lambda, phi2], point22, v2 = visible(lambda, phi2), c3 = smallRadius ? v2 ? 0 : code(lambda, phi2) : v2 ? code(lambda + (lambda < 0 ? pi3 : -pi3), phi2) : 0;
        if (!point0 && (v00 = v0 = v2)) stream2.lineStart();
        if (v2 !== v0) {
          point22 = intersect7(point0, point1);
          if (!point22 || pointEqual_default(point0, point22) || pointEqual_default(point1, point22))
            point1[2] = 1;
        }
        if (v2 !== v0) {
          clean = 0;
          if (v2) {
            stream2.lineStart();
            point22 = intersect7(point1, point0);
            stream2.point(point22[0], point22[1]);
          } else {
            point22 = intersect7(point0, point1);
            stream2.point(point22[0], point22[1], 2);
            stream2.lineEnd();
          }
          point0 = point22;
        } else if (notHemisphere && point0 && smallRadius ^ v2) {
          var t;
          if (!(c3 & c0) && (t = intersect7(point1, point0, true))) {
            clean = 0;
            if (smallRadius) {
              stream2.lineStart();
              stream2.point(t[0][0], t[0][1]);
              stream2.point(t[1][0], t[1][1]);
              stream2.lineEnd();
            } else {
              stream2.point(t[1][0], t[1][1]);
              stream2.lineEnd();
              stream2.lineStart();
              stream2.point(t[0][0], t[0][1], 3);
            }
          }
        }
        if (v2 && (!point0 || !pointEqual_default(point0, point1))) {
          stream2.point(point1[0], point1[1]);
        }
        point0 = point1, v0 = v2, c0 = c3;
      },
      lineEnd: function() {
        if (v0) stream2.lineEnd();
        point0 = null;
      },
      // Rejoin first and last segments if there were intersections and the first
      // and last points were visible.
      clean: function() {
        return clean | (v00 && v0) << 1;
      }
    };
  }
  function intersect7(a3, b10, two) {
    var pa3 = cartesian(a3), pb = cartesian(b10);
    var n1 = [1, 0, 0], n2 = cartesianCross(pa3, pb), n2n2 = cartesianDot(n2, n2), n1n2 = n2[0], determinant = n2n2 - n1n2 * n1n2;
    if (!determinant) return !two && a3;
    var c1 = cr3 * n2n2 / determinant, c22 = -cr3 * n1n2 / determinant, n1xn2 = cartesianCross(n1, n2), A5 = cartesianScale(n1, c1), B3 = cartesianScale(n2, c22);
    cartesianAddInPlace(A5, B3);
    var u4 = n1xn2, w3 = cartesianDot(A5, u4), uu = cartesianDot(u4, u4), t22 = w3 * w3 - uu * (cartesianDot(A5, A5) - 1);
    if (t22 < 0) return;
    var t = sqrt3(t22), q = cartesianScale(u4, (-w3 - t) / uu);
    cartesianAddInPlace(q, A5);
    q = spherical(q);
    if (!two) return q;
    var lambda03 = a3[0], lambda12 = b10[0], phi02 = a3[1], phi12 = b10[1], z;
    if (lambda12 < lambda03) z = lambda03, lambda03 = lambda12, lambda12 = z;
    var delta2 = lambda12 - lambda03, polar = abs2(delta2 - pi3) < epsilon4, meridian = polar || delta2 < epsilon4;
    if (!polar && phi12 < phi02) z = phi02, phi02 = phi12, phi12 = z;
    if (meridian ? polar ? phi02 + phi12 > 0 ^ q[1] < (abs2(q[0] - lambda03) < epsilon4 ? phi02 : phi12) : phi02 <= q[1] && q[1] <= phi12 : delta2 > pi3 ^ (lambda03 <= q[0] && q[0] <= lambda12)) {
      var q1 = cartesianScale(u4, (-w3 + t) / uu);
      cartesianAddInPlace(q1, A5);
      return [q, spherical(q1)];
    }
  }
  function code(lambda, phi2) {
    var r = smallRadius ? radius2 : pi3 - radius2, code2 = 0;
    if (lambda < -r) code2 |= 1;
    else if (lambda > r) code2 |= 2;
    if (phi2 < -r) code2 |= 4;
    else if (phi2 > r) code2 |= 8;
    return code2;
  }
  return clip_default(visible, clipLine, interpolate3, smallRadius ? [0, -radius2] : [-pi3, radius2 - pi3]);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/clip/line.js
function line_default2(a3, b10, x06, y06, x12, y12) {
  var ax = a3[0], ay = a3[1], bx = b10[0], by = b10[1], t05 = 0, t13 = 1, dx = bx - ax, dy = by - ay, r;
  r = x06 - ax;
  if (!dx && r > 0) return;
  r /= dx;
  if (dx < 0) {
    if (r < t05) return;
    if (r < t13) t13 = r;
  } else if (dx > 0) {
    if (r > t13) return;
    if (r > t05) t05 = r;
  }
  r = x12 - ax;
  if (!dx && r < 0) return;
  r /= dx;
  if (dx < 0) {
    if (r > t13) return;
    if (r > t05) t05 = r;
  } else if (dx > 0) {
    if (r < t05) return;
    if (r < t13) t13 = r;
  }
  r = y06 - ay;
  if (!dy && r > 0) return;
  r /= dy;
  if (dy < 0) {
    if (r < t05) return;
    if (r < t13) t13 = r;
  } else if (dy > 0) {
    if (r > t13) return;
    if (r > t05) t05 = r;
  }
  r = y12 - ay;
  if (!dy && r < 0) return;
  r /= dy;
  if (dy < 0) {
    if (r > t13) return;
    if (r > t05) t05 = r;
  } else if (dy > 0) {
    if (r < t05) return;
    if (r < t13) t13 = r;
  }
  if (t05 > 0) a3[0] = ax + t05 * dx, a3[1] = ay + t05 * dy;
  if (t13 < 1) b10[0] = ax + t13 * dx, b10[1] = ay + t13 * dy;
  return true;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/clip/rectangle.js
var clipMax = 1e9;
var clipMin = -clipMax;
function clipRectangle(x06, y06, x12, y12) {
  function visible(x6, y6) {
    return x06 <= x6 && x6 <= x12 && y06 <= y6 && y6 <= y12;
  }
  function interpolate3(from, to, direction, stream2) {
    var a3 = 0, a1 = 0;
    if (from == null || (a3 = corner(from, direction)) !== (a1 = corner(to, direction)) || comparePoint(from, to) < 0 ^ direction > 0) {
      do
        stream2.point(a3 === 0 || a3 === 3 ? x06 : x12, a3 > 1 ? y12 : y06);
      while ((a3 = (a3 + direction + 4) % 4) !== a1);
    } else {
      stream2.point(to[0], to[1]);
    }
  }
  function corner(p, direction) {
    return abs2(p[0] - x06) < epsilon4 ? direction > 0 ? 0 : 3 : abs2(p[0] - x12) < epsilon4 ? direction > 0 ? 2 : 1 : abs2(p[1] - y06) < epsilon4 ? direction > 0 ? 1 : 0 : direction > 0 ? 3 : 2;
  }
  function compareIntersection2(a3, b10) {
    return comparePoint(a3.x, b10.x);
  }
  function comparePoint(a3, b10) {
    var ca3 = corner(a3, 1), cb = corner(b10, 1);
    return ca3 !== cb ? ca3 - cb : ca3 === 0 ? b10[1] - a3[1] : ca3 === 1 ? a3[0] - b10[0] : ca3 === 2 ? a3[1] - b10[1] : b10[0] - a3[0];
  }
  return function(stream2) {
    var activeStream = stream2, bufferStream = buffer_default(), segments3, polygon, ring, x__, y__, v__, x_, y_, v_, first, clean;
    var clipStream = {
      point: point9,
      lineStart,
      lineEnd,
      polygonStart,
      polygonEnd
    };
    function point9(x6, y6) {
      if (visible(x6, y6)) activeStream.point(x6, y6);
    }
    function polygonInside() {
      var winding = 0;
      for (var i = 0, n = polygon.length; i < n; ++i) {
        for (var ring2 = polygon[i], j = 1, m3 = ring2.length, point10 = ring2[0], a0, a1, b02 = point10[0], b12 = point10[1]; j < m3; ++j) {
          a0 = b02, a1 = b12, point10 = ring2[j], b02 = point10[0], b12 = point10[1];
          if (a1 <= y12) {
            if (b12 > y12 && (b02 - a0) * (y12 - a1) > (b12 - a1) * (x06 - a0)) ++winding;
          } else {
            if (b12 <= y12 && (b02 - a0) * (y12 - a1) < (b12 - a1) * (x06 - a0)) --winding;
          }
        }
      }
      return winding;
    }
    function polygonStart() {
      activeStream = bufferStream, segments3 = [], polygon = [], clean = true;
    }
    function polygonEnd() {
      var startInside = polygonInside(), cleanInside = clean && startInside, visible2 = (segments3 = merge2(segments3)).length;
      if (cleanInside || visible2) {
        stream2.polygonStart();
        if (cleanInside) {
          stream2.lineStart();
          interpolate3(null, null, 1, stream2);
          stream2.lineEnd();
        }
        if (visible2) {
          rejoin_default(segments3, compareIntersection2, startInside, interpolate3, stream2);
        }
        stream2.polygonEnd();
      }
      activeStream = stream2, segments3 = polygon = ring = null;
    }
    function lineStart() {
      clipStream.point = linePoint2;
      if (polygon) polygon.push(ring = []);
      first = true;
      v_ = false;
      x_ = y_ = NaN;
    }
    function lineEnd() {
      if (segments3) {
        linePoint2(x__, y__);
        if (v__ && v_) bufferStream.rejoin();
        segments3.push(bufferStream.result());
      }
      clipStream.point = point9;
      if (v_) activeStream.lineEnd();
    }
    function linePoint2(x6, y6) {
      var v2 = visible(x6, y6);
      if (polygon) ring.push([x6, y6]);
      if (first) {
        x__ = x6, y__ = y6, v__ = v2;
        first = false;
        if (v2) {
          activeStream.lineStart();
          activeStream.point(x6, y6);
        }
      } else {
        if (v2 && v_) activeStream.point(x6, y6);
        else {
          var a3 = [x_ = Math.max(clipMin, Math.min(clipMax, x_)), y_ = Math.max(clipMin, Math.min(clipMax, y_))], b10 = [x6 = Math.max(clipMin, Math.min(clipMax, x6)), y6 = Math.max(clipMin, Math.min(clipMax, y6))];
          if (line_default2(a3, b10, x06, y06, x12, y12)) {
            if (!v_) {
              activeStream.lineStart();
              activeStream.point(a3[0], a3[1]);
            }
            activeStream.point(b10[0], b10[1]);
            if (!v2) activeStream.lineEnd();
            clean = false;
          } else if (v2) {
            activeStream.lineStart();
            activeStream.point(x6, y6);
            clean = false;
          }
        }
      }
      x_ = x6, y_ = y6, v_ = v2;
    }
    return clipStream;
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/graticule.js
function graticuleX(y06, y12, dy) {
  var y6 = range(y06, y12 - epsilon4, dy).concat(y12);
  return function(x6) {
    return y6.map(function(y7) {
      return [x6, y7];
    });
  };
}
function graticuleY(x06, x12, dx) {
  var x6 = range(x06, x12 - epsilon4, dx).concat(x12);
  return function(y6) {
    return x6.map(function(x7) {
      return [x7, y6];
    });
  };
}
function graticule() {
  var x12, x06, X13, X03, y12, y06, Y13, Y03, dx = 10, dy = dx, DX = 90, DY = 360, x6, y6, X4, Y4, precision = 2.5;
  function graticule2() {
    return { type: "MultiLineString", coordinates: lines() };
  }
  function lines() {
    return range(ceil(X03 / DX) * DX, X13, DX).map(X4).concat(range(ceil(Y03 / DY) * DY, Y13, DY).map(Y4)).concat(range(ceil(x06 / dx) * dx, x12, dx).filter(function(x7) {
      return abs2(x7 % DX) > epsilon4;
    }).map(x6)).concat(range(ceil(y06 / dy) * dy, y12, dy).filter(function(y7) {
      return abs2(y7 % DY) > epsilon4;
    }).map(y6));
  }
  graticule2.lines = function() {
    return lines().map(function(coordinates) {
      return { type: "LineString", coordinates };
    });
  };
  graticule2.outline = function() {
    return {
      type: "Polygon",
      coordinates: [
        X4(X03).concat(
          Y4(Y13).slice(1),
          X4(X13).reverse().slice(1),
          Y4(Y03).reverse().slice(1)
        )
      ]
    };
  };
  graticule2.extent = function(_) {
    if (!arguments.length) return graticule2.extentMinor();
    return graticule2.extentMajor(_).extentMinor(_);
  };
  graticule2.extentMajor = function(_) {
    if (!arguments.length) return [[X03, Y03], [X13, Y13]];
    X03 = +_[0][0], X13 = +_[1][0];
    Y03 = +_[0][1], Y13 = +_[1][1];
    if (X03 > X13) _ = X03, X03 = X13, X13 = _;
    if (Y03 > Y13) _ = Y03, Y03 = Y13, Y13 = _;
    return graticule2.precision(precision);
  };
  graticule2.extentMinor = function(_) {
    if (!arguments.length) return [[x06, y06], [x12, y12]];
    x06 = +_[0][0], x12 = +_[1][0];
    y06 = +_[0][1], y12 = +_[1][1];
    if (x06 > x12) _ = x06, x06 = x12, x12 = _;
    if (y06 > y12) _ = y06, y06 = y12, y12 = _;
    return graticule2.precision(precision);
  };
  graticule2.step = function(_) {
    if (!arguments.length) return graticule2.stepMinor();
    return graticule2.stepMajor(_).stepMinor(_);
  };
  graticule2.stepMajor = function(_) {
    if (!arguments.length) return [DX, DY];
    DX = +_[0], DY = +_[1];
    return graticule2;
  };
  graticule2.stepMinor = function(_) {
    if (!arguments.length) return [dx, dy];
    dx = +_[0], dy = +_[1];
    return graticule2;
  };
  graticule2.precision = function(_) {
    if (!arguments.length) return precision;
    precision = +_;
    x6 = graticuleX(y06, y12, 90);
    y6 = graticuleY(x06, x12, precision);
    X4 = graticuleX(Y03, Y13, 90);
    Y4 = graticuleY(X03, X13, precision);
    return graticule2;
  };
  return graticule2.extentMajor([[-180, -90 + epsilon4], [180, 90 - epsilon4]]).extentMinor([[-180, -80 - epsilon4], [180, 80 + epsilon4]]);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/identity.js
var identity_default3 = (x6) => x6;

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/path/area.js
var areaSum2 = new Adder();
var areaRingSum2 = new Adder();
var x00;
var y00;
var x02;
var y02;
var areaStream2 = {
  point: noop2,
  lineStart: noop2,
  lineEnd: noop2,
  polygonStart: function() {
    areaStream2.lineStart = areaRingStart2;
    areaStream2.lineEnd = areaRingEnd2;
  },
  polygonEnd: function() {
    areaStream2.lineStart = areaStream2.lineEnd = areaStream2.point = noop2;
    areaSum2.add(abs2(areaRingSum2));
    areaRingSum2 = new Adder();
  },
  result: function() {
    var area5 = areaSum2 / 2;
    areaSum2 = new Adder();
    return area5;
  }
};
function areaRingStart2() {
  areaStream2.point = areaPointFirst2;
}
function areaPointFirst2(x6, y6) {
  areaStream2.point = areaPoint2;
  x00 = x02 = x6, y00 = y02 = y6;
}
function areaPoint2(x6, y6) {
  areaRingSum2.add(y02 * x6 - x02 * y6);
  x02 = x6, y02 = y6;
}
function areaRingEnd2() {
  areaPoint2(x00, y00);
}
var area_default3 = areaStream2;

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/path/bounds.js
var x03 = Infinity;
var y03 = x03;
var x1 = -x03;
var y1 = x1;
var boundsStream2 = {
  point: boundsPoint2,
  lineStart: noop2,
  lineEnd: noop2,
  polygonStart: noop2,
  polygonEnd: noop2,
  result: function() {
    var bounds3 = [[x03, y03], [x1, y1]];
    x1 = y1 = -(y03 = x03 = Infinity);
    return bounds3;
  }
};
function boundsPoint2(x6, y6) {
  if (x6 < x03) x03 = x6;
  if (x6 > x1) x1 = x6;
  if (y6 < y03) y03 = y6;
  if (y6 > y1) y1 = y6;
}
var bounds_default2 = boundsStream2;

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/path/centroid.js
var X02 = 0;
var Y02 = 0;
var Z02 = 0;
var X12 = 0;
var Y12 = 0;
var Z12 = 0;
var X22 = 0;
var Y22 = 0;
var Z22 = 0;
var x002;
var y002;
var x04;
var y04;
var centroidStream2 = {
  point: centroidPoint2,
  lineStart: centroidLineStart2,
  lineEnd: centroidLineEnd2,
  polygonStart: function() {
    centroidStream2.lineStart = centroidRingStart2;
    centroidStream2.lineEnd = centroidRingEnd2;
  },
  polygonEnd: function() {
    centroidStream2.point = centroidPoint2;
    centroidStream2.lineStart = centroidLineStart2;
    centroidStream2.lineEnd = centroidLineEnd2;
  },
  result: function() {
    var centroid = Z22 ? [X22 / Z22, Y22 / Z22] : Z12 ? [X12 / Z12, Y12 / Z12] : Z02 ? [X02 / Z02, Y02 / Z02] : [NaN, NaN];
    X02 = Y02 = Z02 = X12 = Y12 = Z12 = X22 = Y22 = Z22 = 0;
    return centroid;
  }
};
function centroidPoint2(x6, y6) {
  X02 += x6;
  Y02 += y6;
  ++Z02;
}
function centroidLineStart2() {
  centroidStream2.point = centroidPointFirstLine;
}
function centroidPointFirstLine(x6, y6) {
  centroidStream2.point = centroidPointLine;
  centroidPoint2(x04 = x6, y04 = y6);
}
function centroidPointLine(x6, y6) {
  var dx = x6 - x04, dy = y6 - y04, z = sqrt3(dx * dx + dy * dy);
  X12 += z * (x04 + x6) / 2;
  Y12 += z * (y04 + y6) / 2;
  Z12 += z;
  centroidPoint2(x04 = x6, y04 = y6);
}
function centroidLineEnd2() {
  centroidStream2.point = centroidPoint2;
}
function centroidRingStart2() {
  centroidStream2.point = centroidPointFirstRing;
}
function centroidRingEnd2() {
  centroidPointRing(x002, y002);
}
function centroidPointFirstRing(x6, y6) {
  centroidStream2.point = centroidPointRing;
  centroidPoint2(x002 = x04 = x6, y002 = y04 = y6);
}
function centroidPointRing(x6, y6) {
  var dx = x6 - x04, dy = y6 - y04, z = sqrt3(dx * dx + dy * dy);
  X12 += z * (x04 + x6) / 2;
  Y12 += z * (y04 + y6) / 2;
  Z12 += z;
  z = y04 * x6 - x04 * y6;
  X22 += z * (x04 + x6);
  Y22 += z * (y04 + y6);
  Z22 += z * 3;
  centroidPoint2(x04 = x6, y04 = y6);
}
var centroid_default2 = centroidStream2;

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/path/context.js
function PathContext(context4) {
  this._context = context4;
}
PathContext.prototype = {
  _radius: 4.5,
  pointRadius: function(_) {
    return this._radius = _, this;
  },
  polygonStart: function() {
    this._line = 0;
  },
  polygonEnd: function() {
    this._line = NaN;
  },
  lineStart: function() {
    this._point = 0;
  },
  lineEnd: function() {
    if (this._line === 0) this._context.closePath();
    this._point = NaN;
  },
  point: function(x6, y6) {
    switch (this._point) {
      case 0: {
        this._context.moveTo(x6, y6);
        this._point = 1;
        break;
      }
      case 1: {
        this._context.lineTo(x6, y6);
        break;
      }
      default: {
        this._context.moveTo(x6 + this._radius, y6);
        this._context.arc(x6, y6, this._radius, 0, tau3);
        break;
      }
    }
  },
  result: noop2
};

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/path/measure.js
var lengthSum = new Adder();
var lengthRing;
var x003;
var y003;
var x05;
var y05;
var lengthStream = {
  point: noop2,
  lineStart: function() {
    lengthStream.point = lengthPointFirst;
  },
  lineEnd: function() {
    if (lengthRing) lengthPoint(x003, y003);
    lengthStream.point = noop2;
  },
  polygonStart: function() {
    lengthRing = true;
  },
  polygonEnd: function() {
    lengthRing = null;
  },
  result: function() {
    var length3 = +lengthSum;
    lengthSum = new Adder();
    return length3;
  }
};
function lengthPointFirst(x6, y6) {
  lengthStream.point = lengthPoint;
  x003 = x05 = x6, y003 = y05 = y6;
}
function lengthPoint(x6, y6) {
  x05 -= x6, y05 -= y6;
  lengthSum.add(sqrt3(x05 * x05 + y05 * y05));
  x05 = x6, y05 = y6;
}
var measure_default = lengthStream;

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/path/string.js
var cacheDigits;
var cacheAppend;
var cacheRadius;
var cacheCircle;
var PathString = class {
  constructor(digits) {
    this._append = digits == null ? append2 : appendRound2(digits);
    this._radius = 4.5;
    this._ = "";
  }
  pointRadius(_) {
    this._radius = +_;
    return this;
  }
  polygonStart() {
    this._line = 0;
  }
  polygonEnd() {
    this._line = NaN;
  }
  lineStart() {
    this._point = 0;
  }
  lineEnd() {
    if (this._line === 0) this._ += "Z";
    this._point = NaN;
  }
  point(x6, y6) {
    switch (this._point) {
      case 0: {
        this._append`M${x6},${y6}`;
        this._point = 1;
        break;
      }
      case 1: {
        this._append`L${x6},${y6}`;
        break;
      }
      default: {
        this._append`M${x6},${y6}`;
        if (this._radius !== cacheRadius || this._append !== cacheAppend) {
          const r = this._radius;
          const s = this._;
          this._ = "";
          this._append`m0,${r}a${r},${r} 0 1,1 0,${-2 * r}a${r},${r} 0 1,1 0,${2 * r}z`;
          cacheRadius = r;
          cacheAppend = this._append;
          cacheCircle = this._;
          this._ = s;
        }
        this._ += cacheCircle;
        break;
      }
    }
  }
  result() {
    const result = this._;
    this._ = "";
    return result.length ? result : null;
  }
};
function append2(strings) {
  let i = 1;
  this._ += strings[0];
  for (const j = strings.length; i < j; ++i) {
    this._ += arguments[i] + strings[i];
  }
}
function appendRound2(digits) {
  const d = Math.floor(digits);
  if (!(d >= 0)) throw new RangeError(`invalid digits: ${digits}`);
  if (d > 15) return append2;
  if (d !== cacheDigits) {
    const k = 10 ** d;
    cacheDigits = d;
    cacheAppend = function append3(strings) {
      let i = 1;
      this._ += strings[0];
      for (const j = strings.length; i < j; ++i) {
        this._ += Math.round(arguments[i] * k) / k + strings[i];
      }
    };
  }
  return cacheAppend;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/path/index.js
function path_default(projection3, context4) {
  let digits = 3, pointRadius = 4.5, projectionStream, contextStream;
  function path4(object2) {
    if (object2) {
      if (typeof pointRadius === "function") contextStream.pointRadius(+pointRadius.apply(this, arguments));
      stream_default(object2, projectionStream(contextStream));
    }
    return contextStream.result();
  }
  path4.area = function(object2) {
    stream_default(object2, projectionStream(area_default3));
    return area_default3.result();
  };
  path4.measure = function(object2) {
    stream_default(object2, projectionStream(measure_default));
    return measure_default.result();
  };
  path4.bounds = function(object2) {
    stream_default(object2, projectionStream(bounds_default2));
    return bounds_default2.result();
  };
  path4.centroid = function(object2) {
    stream_default(object2, projectionStream(centroid_default2));
    return centroid_default2.result();
  };
  path4.projection = function(_) {
    if (!arguments.length) return projection3;
    projectionStream = _ == null ? (projection3 = null, identity_default3) : (projection3 = _).stream;
    return path4;
  };
  path4.context = function(_) {
    if (!arguments.length) return context4;
    contextStream = _ == null ? (context4 = null, new PathString(digits)) : new PathContext(context4 = _);
    if (typeof pointRadius !== "function") contextStream.pointRadius(pointRadius);
    return path4;
  };
  path4.pointRadius = function(_) {
    if (!arguments.length) return pointRadius;
    pointRadius = typeof _ === "function" ? _ : (contextStream.pointRadius(+_), +_);
    return path4;
  };
  path4.digits = function(_) {
    if (!arguments.length) return digits;
    if (_ == null) digits = null;
    else {
      const d = Math.floor(_);
      if (!(d >= 0)) throw new RangeError(`invalid digits: ${_}`);
      digits = d;
    }
    if (context4 === null) contextStream = new PathString(digits);
    return path4;
  };
  return path4.projection(projection3).digits(digits).context(context4);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/transform.js
function transformer4(methods2) {
  return function(stream2) {
    var s = new TransformStream();
    for (var key2 in methods2) s[key2] = methods2[key2];
    s.stream = stream2;
    return s;
  };
}
function TransformStream() {
}
TransformStream.prototype = {
  constructor: TransformStream,
  point: function(x6, y6) {
    this.stream.point(x6, y6);
  },
  sphere: function() {
    this.stream.sphere();
  },
  lineStart: function() {
    this.stream.lineStart();
  },
  lineEnd: function() {
    this.stream.lineEnd();
  },
  polygonStart: function() {
    this.stream.polygonStart();
  },
  polygonEnd: function() {
    this.stream.polygonEnd();
  }
};

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/fit.js
function fit(projection3, fitBounds, object2) {
  var clip4 = projection3.clipExtent && projection3.clipExtent();
  projection3.scale(150).translate([0, 0]);
  if (clip4 != null) projection3.clipExtent(null);
  stream_default(object2, projection3.stream(bounds_default2));
  fitBounds(bounds_default2.result());
  if (clip4 != null) projection3.clipExtent(clip4);
  return projection3;
}
function fitExtent(projection3, extent2, object2) {
  return fit(projection3, function(b10) {
    var w3 = extent2[1][0] - extent2[0][0], h3 = extent2[1][1] - extent2[0][1], k = Math.min(w3 / (b10[1][0] - b10[0][0]), h3 / (b10[1][1] - b10[0][1])), x6 = +extent2[0][0] + (w3 - k * (b10[1][0] + b10[0][0])) / 2, y6 = +extent2[0][1] + (h3 - k * (b10[1][1] + b10[0][1])) / 2;
    projection3.scale(150 * k).translate([x6, y6]);
  }, object2);
}
function fitSize(projection3, size, object2) {
  return fitExtent(projection3, [[0, 0], size], object2);
}
function fitWidth(projection3, width2, object2) {
  return fit(projection3, function(b10) {
    var w3 = +width2, k = w3 / (b10[1][0] - b10[0][0]), x6 = (w3 - k * (b10[1][0] + b10[0][0])) / 2, y6 = -k * b10[0][1];
    projection3.scale(150 * k).translate([x6, y6]);
  }, object2);
}
function fitHeight(projection3, height2, object2) {
  return fit(projection3, function(b10) {
    var h3 = +height2, k = h3 / (b10[1][1] - b10[0][1]), x6 = -k * b10[0][0], y6 = (h3 - k * (b10[1][1] + b10[0][1])) / 2;
    projection3.scale(150 * k).translate([x6, y6]);
  }, object2);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/resample.js
var maxDepth = 16;
var cosMinDistance = cos2(30 * radians2);
function resample_default(project3, delta2) {
  return +delta2 ? resample(project3, delta2) : resampleNone(project3);
}
function resampleNone(project3) {
  return transformer4({
    point: function(x6, y6) {
      x6 = project3(x6, y6);
      this.stream.point(x6[0], x6[1]);
    }
  });
}
function resample(project3, delta2) {
  function resampleLineTo(x06, y06, lambda03, a0, b02, c0, x12, y12, lambda12, a1, b12, c1, depth, stream2) {
    var dx = x12 - x06, dy = y12 - y06, d2 = dx * dx + dy * dy;
    if (d2 > 4 * delta2 && depth--) {
      var a3 = a0 + a1, b10 = b02 + b12, c3 = c0 + c1, m3 = sqrt3(a3 * a3 + b10 * b10 + c3 * c3), phi2 = asin2(c3 /= m3), lambda22 = abs2(abs2(c3) - 1) < epsilon4 || abs2(lambda03 - lambda12) < epsilon4 ? (lambda03 + lambda12) / 2 : atan22(b10, a3), p = project3(lambda22, phi2), x22 = p[0], y22 = p[1], dx2 = x22 - x06, dy2 = y22 - y06, dz = dy * dx2 - dx * dy2;
      if (dz * dz / d2 > delta2 || abs2((dx * dx2 + dy * dy2) / d2 - 0.5) > 0.3 || a0 * a1 + b02 * b12 + c0 * c1 < cosMinDistance) {
        resampleLineTo(x06, y06, lambda03, a0, b02, c0, x22, y22, lambda22, a3 /= m3, b10 /= m3, c3, depth, stream2);
        stream2.point(x22, y22);
        resampleLineTo(x22, y22, lambda22, a3, b10, c3, x12, y12, lambda12, a1, b12, c1, depth, stream2);
      }
    }
  }
  return function(stream2) {
    var lambda004, x004, y004, a00, b00, c00, lambda03, x06, y06, a0, b02, c0;
    var resampleStream = {
      point: point9,
      lineStart,
      lineEnd,
      polygonStart: function() {
        stream2.polygonStart();
        resampleStream.lineStart = ringStart;
      },
      polygonEnd: function() {
        stream2.polygonEnd();
        resampleStream.lineStart = lineStart;
      }
    };
    function point9(x6, y6) {
      x6 = project3(x6, y6);
      stream2.point(x6[0], x6[1]);
    }
    function lineStart() {
      x06 = NaN;
      resampleStream.point = linePoint2;
      stream2.lineStart();
    }
    function linePoint2(lambda, phi2) {
      var c3 = cartesian([lambda, phi2]), p = project3(lambda, phi2);
      resampleLineTo(x06, y06, lambda03, a0, b02, c0, x06 = p[0], y06 = p[1], lambda03 = lambda, a0 = c3[0], b02 = c3[1], c0 = c3[2], maxDepth, stream2);
      stream2.point(x06, y06);
    }
    function lineEnd() {
      resampleStream.point = point9;
      stream2.lineEnd();
    }
    function ringStart() {
      lineStart();
      resampleStream.point = ringPoint;
      resampleStream.lineEnd = ringEnd;
    }
    function ringPoint(lambda, phi2) {
      linePoint2(lambda004 = lambda, phi2), x004 = x06, y004 = y06, a00 = a0, b00 = b02, c00 = c0;
      resampleStream.point = linePoint2;
    }
    function ringEnd() {
      resampleLineTo(x06, y06, lambda03, a0, b02, c0, x004, y004, lambda004, a00, b00, c00, maxDepth, stream2);
      resampleStream.lineEnd = lineEnd;
      lineEnd();
    }
    return resampleStream;
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/index.js
var transformRadians = transformer4({
  point: function(x6, y6) {
    this.stream.point(x6 * radians2, y6 * radians2);
  }
});
function transformRotate(rotate3) {
  return transformer4({
    point: function(x6, y6) {
      var r = rotate3(x6, y6);
      return this.stream.point(r[0], r[1]);
    }
  });
}
function scaleTranslate(k, dx, dy, sx, sy) {
  function transform4(x6, y6) {
    x6 *= sx;
    y6 *= sy;
    return [dx + k * x6, dy - k * y6];
  }
  transform4.invert = function(x6, y6) {
    return [(x6 - dx) / k * sx, (dy - y6) / k * sy];
  };
  return transform4;
}
function scaleTranslateRotate(k, dx, dy, sx, sy, alpha) {
  if (!alpha) return scaleTranslate(k, dx, dy, sx, sy);
  var cosAlpha = cos2(alpha), sinAlpha = sin2(alpha), a3 = cosAlpha * k, b10 = sinAlpha * k, ai = cosAlpha / k, bi = sinAlpha / k, ci = (sinAlpha * dy - cosAlpha * dx) / k, fi = (sinAlpha * dx + cosAlpha * dy) / k;
  function transform4(x6, y6) {
    x6 *= sx;
    y6 *= sy;
    return [a3 * x6 - b10 * y6 + dx, dy - b10 * x6 - a3 * y6];
  }
  transform4.invert = function(x6, y6) {
    return [sx * (ai * x6 - bi * y6 + ci), sy * (fi - bi * x6 - ai * y6)];
  };
  return transform4;
}
function projection(project3) {
  return projectionMutator(function() {
    return project3;
  })();
}
function projectionMutator(projectAt) {
  var project3, k = 150, x6 = 480, y6 = 250, lambda = 0, phi2 = 0, deltaLambda = 0, deltaPhi = 0, deltaGamma = 0, rotate3, alpha = 0, sx = 1, sy = 1, theta = null, preclip = antimeridian_default, x06 = null, y06, x12, y12, postclip = identity_default3, delta2 = 0.5, projectResample, projectTransform, projectRotateTransform, cache2, cacheStream;
  function projection3(point9) {
    return projectRotateTransform(point9[0] * radians2, point9[1] * radians2);
  }
  function invert3(point9) {
    point9 = projectRotateTransform.invert(point9[0], point9[1]);
    return point9 && [point9[0] * degrees3, point9[1] * degrees3];
  }
  projection3.stream = function(stream2) {
    return cache2 && cacheStream === stream2 ? cache2 : cache2 = transformRadians(transformRotate(rotate3)(preclip(projectResample(postclip(cacheStream = stream2)))));
  };
  projection3.preclip = function(_) {
    return arguments.length ? (preclip = _, theta = void 0, reset3()) : preclip;
  };
  projection3.postclip = function(_) {
    return arguments.length ? (postclip = _, x06 = y06 = x12 = y12 = null, reset3()) : postclip;
  };
  projection3.clipAngle = function(_) {
    return arguments.length ? (preclip = +_ ? circle_default2(theta = _ * radians2) : (theta = null, antimeridian_default), reset3()) : theta * degrees3;
  };
  projection3.clipExtent = function(_) {
    return arguments.length ? (postclip = _ == null ? (x06 = y06 = x12 = y12 = null, identity_default3) : clipRectangle(x06 = +_[0][0], y06 = +_[0][1], x12 = +_[1][0], y12 = +_[1][1]), reset3()) : x06 == null ? null : [[x06, y06], [x12, y12]];
  };
  projection3.scale = function(_) {
    return arguments.length ? (k = +_, recenter()) : k;
  };
  projection3.translate = function(_) {
    return arguments.length ? (x6 = +_[0], y6 = +_[1], recenter()) : [x6, y6];
  };
  projection3.center = function(_) {
    return arguments.length ? (lambda = _[0] % 360 * radians2, phi2 = _[1] % 360 * radians2, recenter()) : [lambda * degrees3, phi2 * degrees3];
  };
  projection3.rotate = function(_) {
    return arguments.length ? (deltaLambda = _[0] % 360 * radians2, deltaPhi = _[1] % 360 * radians2, deltaGamma = _.length > 2 ? _[2] % 360 * radians2 : 0, recenter()) : [deltaLambda * degrees3, deltaPhi * degrees3, deltaGamma * degrees3];
  };
  projection3.angle = function(_) {
    return arguments.length ? (alpha = _ % 360 * radians2, recenter()) : alpha * degrees3;
  };
  projection3.reflectX = function(_) {
    return arguments.length ? (sx = _ ? -1 : 1, recenter()) : sx < 0;
  };
  projection3.reflectY = function(_) {
    return arguments.length ? (sy = _ ? -1 : 1, recenter()) : sy < 0;
  };
  projection3.precision = function(_) {
    return arguments.length ? (projectResample = resample_default(projectTransform, delta2 = _ * _), reset3()) : sqrt3(delta2);
  };
  projection3.fitExtent = function(extent2, object2) {
    return fitExtent(projection3, extent2, object2);
  };
  projection3.fitSize = function(size, object2) {
    return fitSize(projection3, size, object2);
  };
  projection3.fitWidth = function(width2, object2) {
    return fitWidth(projection3, width2, object2);
  };
  projection3.fitHeight = function(height2, object2) {
    return fitHeight(projection3, height2, object2);
  };
  function recenter() {
    var center = scaleTranslateRotate(k, 0, 0, sx, sy, alpha).apply(null, project3(lambda, phi2)), transform4 = scaleTranslateRotate(k, x6 - center[0], y6 - center[1], sx, sy, alpha);
    rotate3 = rotateRadians(deltaLambda, deltaPhi, deltaGamma);
    projectTransform = compose_default(project3, transform4);
    projectRotateTransform = compose_default(rotate3, projectTransform);
    projectResample = resample_default(projectTransform, delta2);
    return reset3();
  }
  function reset3() {
    cache2 = cacheStream = null;
    return projection3;
  }
  return function() {
    project3 = projectAt.apply(this, arguments);
    projection3.invert = project3.invert && invert3;
    return recenter();
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/conic.js
function conicProjection(projectAt) {
  var phi02 = 0, phi12 = pi3 / 3, m3 = projectionMutator(projectAt), p = m3(phi02, phi12);
  p.parallels = function(_) {
    return arguments.length ? m3(phi02 = _[0] * radians2, phi12 = _[1] * radians2) : [phi02 * degrees3, phi12 * degrees3];
  };
  return p;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/cylindricalEqualArea.js
function cylindricalEqualAreaRaw(phi02) {
  var cosPhi02 = cos2(phi02);
  function forward(lambda, phi2) {
    return [lambda * cosPhi02, sin2(phi2) / cosPhi02];
  }
  forward.invert = function(x6, y6) {
    return [x6 / cosPhi02, asin2(y6 * cosPhi02)];
  };
  return forward;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/conicEqualArea.js
function conicEqualAreaRaw(y06, y12) {
  var sy0 = sin2(y06), n = (sy0 + sin2(y12)) / 2;
  if (abs2(n) < epsilon4) return cylindricalEqualAreaRaw(y06);
  var c3 = 1 + sy0 * (2 * n - sy0), r0 = sqrt3(c3) / n;
  function project3(x6, y6) {
    var r = sqrt3(c3 - 2 * n * sin2(y6)) / n;
    return [r * sin2(x6 *= n), r0 - r * cos2(x6)];
  }
  project3.invert = function(x6, y6) {
    var r0y = r0 - y6, l = atan22(x6, abs2(r0y)) * sign2(r0y);
    if (r0y * n < 0)
      l -= pi3 * sign2(x6) * sign2(r0y);
    return [l / n, asin2((c3 - (x6 * x6 + r0y * r0y) * n * n) / (2 * n))];
  };
  return project3;
}
function conicEqualArea_default() {
  return conicProjection(conicEqualAreaRaw).scale(155.424).center([0, 33.6442]);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/albers.js
function albers_default() {
  return conicEqualArea_default().parallels([29.5, 45.5]).scale(1070).translate([480, 250]).rotate([96, 0]).center([-0.6, 38.7]);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/albersUsa.js
function multiplex(streams) {
  var n = streams.length;
  return {
    point: function(x6, y6) {
      var i = -1;
      while (++i < n) streams[i].point(x6, y6);
    },
    sphere: function() {
      var i = -1;
      while (++i < n) streams[i].sphere();
    },
    lineStart: function() {
      var i = -1;
      while (++i < n) streams[i].lineStart();
    },
    lineEnd: function() {
      var i = -1;
      while (++i < n) streams[i].lineEnd();
    },
    polygonStart: function() {
      var i = -1;
      while (++i < n) streams[i].polygonStart();
    },
    polygonEnd: function() {
      var i = -1;
      while (++i < n) streams[i].polygonEnd();
    }
  };
}
function albersUsa_default() {
  var cache2, cacheStream, lower48 = albers_default(), lower48Point, alaska = conicEqualArea_default().rotate([154, 0]).center([-2, 58.5]).parallels([55, 65]), alaskaPoint, hawaii = conicEqualArea_default().rotate([157, 0]).center([-3, 19.9]).parallels([8, 18]), hawaiiPoint, point9, pointStream = { point: function(x6, y6) {
    point9 = [x6, y6];
  } };
  function albersUsa(coordinates) {
    var x6 = coordinates[0], y6 = coordinates[1];
    return point9 = null, (lower48Point.point(x6, y6), point9) || (alaskaPoint.point(x6, y6), point9) || (hawaiiPoint.point(x6, y6), point9);
  }
  albersUsa.invert = function(coordinates) {
    var k = lower48.scale(), t = lower48.translate(), x6 = (coordinates[0] - t[0]) / k, y6 = (coordinates[1] - t[1]) / k;
    return (y6 >= 0.12 && y6 < 0.234 && x6 >= -0.425 && x6 < -0.214 ? alaska : y6 >= 0.166 && y6 < 0.234 && x6 >= -0.214 && x6 < -0.115 ? hawaii : lower48).invert(coordinates);
  };
  albersUsa.stream = function(stream2) {
    return cache2 && cacheStream === stream2 ? cache2 : cache2 = multiplex([lower48.stream(cacheStream = stream2), alaska.stream(stream2), hawaii.stream(stream2)]);
  };
  albersUsa.precision = function(_) {
    if (!arguments.length) return lower48.precision();
    lower48.precision(_), alaska.precision(_), hawaii.precision(_);
    return reset3();
  };
  albersUsa.scale = function(_) {
    if (!arguments.length) return lower48.scale();
    lower48.scale(_), alaska.scale(_ * 0.35), hawaii.scale(_);
    return albersUsa.translate(lower48.translate());
  };
  albersUsa.translate = function(_) {
    if (!arguments.length) return lower48.translate();
    var k = lower48.scale(), x6 = +_[0], y6 = +_[1];
    lower48Point = lower48.translate(_).clipExtent([[x6 - 0.455 * k, y6 - 0.238 * k], [x6 + 0.455 * k, y6 + 0.238 * k]]).stream(pointStream);
    alaskaPoint = alaska.translate([x6 - 0.307 * k, y6 + 0.201 * k]).clipExtent([[x6 - 0.425 * k + epsilon4, y6 + 0.12 * k + epsilon4], [x6 - 0.214 * k - epsilon4, y6 + 0.234 * k - epsilon4]]).stream(pointStream);
    hawaiiPoint = hawaii.translate([x6 - 0.205 * k, y6 + 0.212 * k]).clipExtent([[x6 - 0.214 * k + epsilon4, y6 + 0.166 * k + epsilon4], [x6 - 0.115 * k - epsilon4, y6 + 0.234 * k - epsilon4]]).stream(pointStream);
    return reset3();
  };
  albersUsa.fitExtent = function(extent2, object2) {
    return fitExtent(albersUsa, extent2, object2);
  };
  albersUsa.fitSize = function(size, object2) {
    return fitSize(albersUsa, size, object2);
  };
  albersUsa.fitWidth = function(width2, object2) {
    return fitWidth(albersUsa, width2, object2);
  };
  albersUsa.fitHeight = function(height2, object2) {
    return fitHeight(albersUsa, height2, object2);
  };
  function reset3() {
    cache2 = cacheStream = null;
    return albersUsa;
  }
  return albersUsa.scale(1070);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/azimuthal.js
function azimuthalRaw(scale9) {
  return function(x6, y6) {
    var cx = cos2(x6), cy = cos2(y6), k = scale9(cx * cy);
    if (k === Infinity) return [2, 0];
    return [
      k * cy * sin2(x6),
      k * sin2(y6)
    ];
  };
}
function azimuthalInvert(angle2) {
  return function(x6, y6) {
    var z = sqrt3(x6 * x6 + y6 * y6), c3 = angle2(z), sc = sin2(c3), cc2 = cos2(c3);
    return [
      atan22(x6 * sc, z * cc2),
      asin2(z && y6 * sc / z)
    ];
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/azimuthalEqualArea.js
var azimuthalEqualAreaRaw = azimuthalRaw(function(cxcy) {
  return sqrt3(2 / (1 + cxcy));
});
azimuthalEqualAreaRaw.invert = azimuthalInvert(function(z) {
  return 2 * asin2(z / 2);
});
function azimuthalEqualArea_default() {
  return projection(azimuthalEqualAreaRaw).scale(124.75).clipAngle(180 - 1e-3);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/azimuthalEquidistant.js
var azimuthalEquidistantRaw = azimuthalRaw(function(c3) {
  return (c3 = acos2(c3)) && c3 / sin2(c3);
});
azimuthalEquidistantRaw.invert = azimuthalInvert(function(z) {
  return z;
});
function azimuthalEquidistant_default() {
  return projection(azimuthalEquidistantRaw).scale(79.4188).clipAngle(180 - 1e-3);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/mercator.js
function mercatorRaw(lambda, phi2) {
  return [lambda, log5(tan((halfPi2 + phi2) / 2))];
}
mercatorRaw.invert = function(x6, y6) {
  return [x6, 2 * atan(exp3(y6)) - halfPi2];
};
function mercator_default() {
  return mercatorProjection(mercatorRaw).scale(961 / tau3);
}
function mercatorProjection(project3) {
  var m3 = projection(project3), center = m3.center, scale9 = m3.scale, translate5 = m3.translate, clipExtent = m3.clipExtent, x06 = null, y06, x12, y12;
  m3.scale = function(_) {
    return arguments.length ? (scale9(_), reclip()) : scale9();
  };
  m3.translate = function(_) {
    return arguments.length ? (translate5(_), reclip()) : translate5();
  };
  m3.center = function(_) {
    return arguments.length ? (center(_), reclip()) : center();
  };
  m3.clipExtent = function(_) {
    return arguments.length ? (_ == null ? x06 = y06 = x12 = y12 = null : (x06 = +_[0][0], y06 = +_[0][1], x12 = +_[1][0], y12 = +_[1][1]), reclip()) : x06 == null ? null : [[x06, y06], [x12, y12]];
  };
  function reclip() {
    var k = pi3 * scale9(), t = m3(rotation_default(m3.rotate()).invert([0, 0]));
    return clipExtent(x06 == null ? [[t[0] - k, t[1] - k], [t[0] + k, t[1] + k]] : project3 === mercatorRaw ? [[Math.max(t[0] - k, x06), y06], [Math.min(t[0] + k, x12), y12]] : [[x06, Math.max(t[1] - k, y06)], [x12, Math.min(t[1] + k, y12)]]);
  }
  return reclip();
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/conicConformal.js
function tany(y6) {
  return tan((halfPi2 + y6) / 2);
}
function conicConformalRaw(y06, y12) {
  var cy0 = cos2(y06), n = y06 === y12 ? sin2(y06) : log5(cy0 / cos2(y12)) / log5(tany(y12) / tany(y06)), f = cy0 * pow4(tany(y06), n) / n;
  if (!n) return mercatorRaw;
  function project3(x6, y6) {
    if (f > 0) {
      if (y6 < -halfPi2 + epsilon4) y6 = -halfPi2 + epsilon4;
    } else {
      if (y6 > halfPi2 - epsilon4) y6 = halfPi2 - epsilon4;
    }
    var r = f / pow4(tany(y6), n);
    return [r * sin2(n * x6), f - r * cos2(n * x6)];
  }
  project3.invert = function(x6, y6) {
    var fy = f - y6, r = sign2(n) * sqrt3(x6 * x6 + fy * fy), l = atan22(x6, abs2(fy)) * sign2(fy);
    if (fy * n < 0)
      l -= pi3 * sign2(x6) * sign2(fy);
    return [l / n, 2 * atan(pow4(f / r, 1 / n)) - halfPi2];
  };
  return project3;
}
function conicConformal_default() {
  return conicProjection(conicConformalRaw).scale(109.5).parallels([30, 30]);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/equirectangular.js
function equirectangularRaw(lambda, phi2) {
  return [lambda, phi2];
}
equirectangularRaw.invert = equirectangularRaw;
function equirectangular_default() {
  return projection(equirectangularRaw).scale(152.63);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/conicEquidistant.js
function conicEquidistantRaw(y06, y12) {
  var cy0 = cos2(y06), n = y06 === y12 ? sin2(y06) : (cy0 - cos2(y12)) / (y12 - y06), g = cy0 / n + y06;
  if (abs2(n) < epsilon4) return equirectangularRaw;
  function project3(x6, y6) {
    var gy = g - y6, nx = n * x6;
    return [gy * sin2(nx), g - gy * cos2(nx)];
  }
  project3.invert = function(x6, y6) {
    var gy = g - y6, l = atan22(x6, abs2(gy)) * sign2(gy);
    if (gy * n < 0)
      l -= pi3 * sign2(x6) * sign2(gy);
    return [l / n, g - sign2(n) * sqrt3(x6 * x6 + gy * gy)];
  };
  return project3;
}
function conicEquidistant_default() {
  return conicProjection(conicEquidistantRaw).scale(131.154).center([0, 13.9389]);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/equalEarth.js
var A1 = 1.340264;
var A2 = -0.081106;
var A3 = 893e-6;
var A4 = 3796e-6;
var M = sqrt3(3) / 2;
var iterations = 12;
function equalEarthRaw(lambda, phi2) {
  var l = asin2(M * sin2(phi2)), l2 = l * l, l6 = l2 * l2 * l2;
  return [
    lambda * cos2(l) / (M * (A1 + 3 * A2 * l2 + l6 * (7 * A3 + 9 * A4 * l2))),
    l * (A1 + A2 * l2 + l6 * (A3 + A4 * l2))
  ];
}
equalEarthRaw.invert = function(x6, y6) {
  var l = y6, l2 = l * l, l6 = l2 * l2 * l2;
  for (var i = 0, delta, fy, fpy; i < iterations; ++i) {
    fy = l * (A1 + A2 * l2 + l6 * (A3 + A4 * l2)) - y6;
    fpy = A1 + 3 * A2 * l2 + l6 * (7 * A3 + 9 * A4 * l2);
    l -= delta = fy / fpy, l2 = l * l, l6 = l2 * l2 * l2;
    if (abs2(delta) < epsilon23) break;
  }
  return [
    M * x6 * (A1 + 3 * A2 * l2 + l6 * (7 * A3 + 9 * A4 * l2)) / cos2(l),
    asin2(sin2(l) / M)
  ];
};
function equalEarth_default() {
  return projection(equalEarthRaw).scale(177.158);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/gnomonic.js
function gnomonicRaw(x6, y6) {
  var cy = cos2(y6), k = cos2(x6) * cy;
  return [cy * sin2(x6) / k, sin2(y6) / k];
}
gnomonicRaw.invert = azimuthalInvert(atan);
function gnomonic_default() {
  return projection(gnomonicRaw).scale(144.049).clipAngle(60);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/identity.js
function identity_default4() {
  var k = 1, tx = 0, ty = 0, sx = 1, sy = 1, alpha = 0, ca3, sa3, x06 = null, y06, x12, y12, kx = 1, ky = 1, transform4 = transformer4({
    point: function(x6, y6) {
      var p = projection3([x6, y6]);
      this.stream.point(p[0], p[1]);
    }
  }), postclip = identity_default3, cache2, cacheStream;
  function reset3() {
    kx = k * sx;
    ky = k * sy;
    cache2 = cacheStream = null;
    return projection3;
  }
  function projection3(p) {
    var x6 = p[0] * kx, y6 = p[1] * ky;
    if (alpha) {
      var t = y6 * ca3 - x6 * sa3;
      x6 = x6 * ca3 + y6 * sa3;
      y6 = t;
    }
    return [x6 + tx, y6 + ty];
  }
  projection3.invert = function(p) {
    var x6 = p[0] - tx, y6 = p[1] - ty;
    if (alpha) {
      var t = y6 * ca3 + x6 * sa3;
      x6 = x6 * ca3 - y6 * sa3;
      y6 = t;
    }
    return [x6 / kx, y6 / ky];
  };
  projection3.stream = function(stream2) {
    return cache2 && cacheStream === stream2 ? cache2 : cache2 = transform4(postclip(cacheStream = stream2));
  };
  projection3.postclip = function(_) {
    return arguments.length ? (postclip = _, x06 = y06 = x12 = y12 = null, reset3()) : postclip;
  };
  projection3.clipExtent = function(_) {
    return arguments.length ? (postclip = _ == null ? (x06 = y06 = x12 = y12 = null, identity_default3) : clipRectangle(x06 = +_[0][0], y06 = +_[0][1], x12 = +_[1][0], y12 = +_[1][1]), reset3()) : x06 == null ? null : [[x06, y06], [x12, y12]];
  };
  projection3.scale = function(_) {
    return arguments.length ? (k = +_, reset3()) : k;
  };
  projection3.translate = function(_) {
    return arguments.length ? (tx = +_[0], ty = +_[1], reset3()) : [tx, ty];
  };
  projection3.angle = function(_) {
    return arguments.length ? (alpha = _ % 360 * radians2, sa3 = sin2(alpha), ca3 = cos2(alpha), reset3()) : alpha * degrees3;
  };
  projection3.reflectX = function(_) {
    return arguments.length ? (sx = _ ? -1 : 1, reset3()) : sx < 0;
  };
  projection3.reflectY = function(_) {
    return arguments.length ? (sy = _ ? -1 : 1, reset3()) : sy < 0;
  };
  projection3.fitExtent = function(extent2, object2) {
    return fitExtent(projection3, extent2, object2);
  };
  projection3.fitSize = function(size, object2) {
    return fitSize(projection3, size, object2);
  };
  projection3.fitWidth = function(width2, object2) {
    return fitWidth(projection3, width2, object2);
  };
  projection3.fitHeight = function(height2, object2) {
    return fitHeight(projection3, height2, object2);
  };
  return projection3;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/naturalEarth1.js
function naturalEarth1Raw(lambda, phi2) {
  var phi22 = phi2 * phi2, phi4 = phi22 * phi22;
  return [
    lambda * (0.8707 - 0.131979 * phi22 + phi4 * (-0.013791 + phi4 * (3971e-6 * phi22 - 1529e-6 * phi4))),
    phi2 * (1.007226 + phi22 * (0.015085 + phi4 * (-0.044475 + 0.028874 * phi22 - 5916e-6 * phi4)))
  ];
}
naturalEarth1Raw.invert = function(x6, y6) {
  var phi2 = y6, i = 25, delta;
  do {
    var phi22 = phi2 * phi2, phi4 = phi22 * phi22;
    phi2 -= delta = (phi2 * (1.007226 + phi22 * (0.015085 + phi4 * (-0.044475 + 0.028874 * phi22 - 5916e-6 * phi4))) - y6) / (1.007226 + phi22 * (0.015085 * 3 + phi4 * (-0.044475 * 7 + 0.028874 * 9 * phi22 - 5916e-6 * 11 * phi4)));
  } while (abs2(delta) > epsilon4 && --i > 0);
  return [
    x6 / (0.8707 + (phi22 = phi2 * phi2) * (-0.131979 + phi22 * (-0.013791 + phi22 * phi22 * phi22 * (3971e-6 - 1529e-6 * phi22)))),
    phi2
  ];
};
function naturalEarth1_default() {
  return projection(naturalEarth1Raw).scale(175.295);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/orthographic.js
function orthographicRaw(x6, y6) {
  return [cos2(y6) * sin2(x6), sin2(y6)];
}
orthographicRaw.invert = azimuthalInvert(asin2);
function orthographic_default() {
  return projection(orthographicRaw).scale(249.5).clipAngle(90 + epsilon4);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/stereographic.js
function stereographicRaw(x6, y6) {
  var cy = cos2(y6), k = 1 + cos2(x6) * cy;
  return [cy * sin2(x6) / k, sin2(y6) / k];
}
stereographicRaw.invert = azimuthalInvert(function(z) {
  return 2 * atan(z);
});
function stereographic_default() {
  return projection(stereographicRaw).scale(250).clipAngle(142);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo@3.1.1/node_modules/d3-geo/src/projection/transverseMercator.js
function transverseMercatorRaw(lambda, phi2) {
  return [log5(tan((halfPi2 + phi2) / 2)), -lambda];
}
transverseMercatorRaw.invert = function(x6, y6) {
  return [-y6, 2 * atan(exp3(x6)) - halfPi2];
};
function transverseMercator_default() {
  var m3 = mercatorProjection(transverseMercatorRaw), center = m3.center, rotate3 = m3.rotate;
  m3.center = function(_) {
    return arguments.length ? center([-_[1], _[0]]) : (_ = center(), [_[1], -_[0]]);
  };
  m3.rotate = function(_) {
    return arguments.length ? rotate3([_[0], _[1], _.length > 2 ? _[2] + 90 : 90]) : (_ = rotate3(), [_[0], _[1], _[2] - 90]);
  };
  return rotate3([0, 0, 90]).scale(159.155);
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo-projection@4.0.0/node_modules/d3-geo-projection/src/math.js
var abs3 = Math.abs;
var cos3 = Math.cos;
var sin3 = Math.sin;
var epsilon5 = 1e-6;
var pi4 = Math.PI;
var halfPi3 = pi4 / 2;
var quarterPi2 = pi4 / 4;
var sqrt22 = sqrt4(2);
var sqrtPi = sqrt4(pi4);
var tau4 = pi4 * 2;
var degrees4 = 180 / pi4;
var radians3 = pi4 / 180;
function asin3(x6) {
  return x6 > 1 ? halfPi3 : x6 < -1 ? -halfPi3 : Math.asin(x6);
}
function sqrt4(x6) {
  return x6 > 0 ? Math.sqrt(x6) : 0;
}

// ../deepseek-harness/node_modules/.pnpm/d3-geo-projection@4.0.0/node_modules/d3-geo-projection/src/mollweide.js
function mollweideBromleyTheta(cp, phi2) {
  var cpsinPhi = cp * sin3(phi2), i = 30, delta;
  do
    phi2 -= delta = (phi2 + sin3(phi2) - cpsinPhi) / (1 + cos3(phi2));
  while (abs3(delta) > epsilon5 && --i > 0);
  return phi2 / 2;
}
function mollweideBromleyRaw(cx, cy, cp) {
  function forward(lambda, phi2) {
    return [cx * lambda * cos3(phi2 = mollweideBromleyTheta(cp, phi2)), cy * sin3(phi2)];
  }
  forward.invert = function(x6, y6) {
    return y6 = asin3(y6 / cy), [x6 / (cx * cos3(y6)), asin3((2 * y6 + sin3(2 * y6)) / cp)];
  };
  return forward;
}
var mollweideRaw = mollweideBromleyRaw(sqrt22 / halfPi3, sqrt22, pi4);
function mollweide_default() {
  return projection(mollweideRaw).scale(169.529);
}

// ../deepseek-harness/node_modules/.pnpm/vega-projection@2.1.3/node_modules/vega-projection/build/vega-projection.js
var defaultPath = path_default();
var projectionProperties = [
  // standard properties in d3-geo
  "clipAngle",
  "clipExtent",
  "scale",
  "translate",
  "center",
  "rotate",
  "parallels",
  "precision",
  "reflectX",
  "reflectY",
  // extended properties in d3-geo-projections
  "coefficient",
  "distance",
  "fraction",
  "lobes",
  "parallel",
  "radius",
  "ratio",
  "spacing",
  "tilt"
];
function create2(type4, constructor) {
  return function projection3() {
    const p = constructor();
    p.type = type4;
    p.path = path_default().projection(p);
    p.copy = p.copy || function() {
      const c3 = projection3();
      projectionProperties.forEach((prop) => {
        if (p[prop]) c3[prop](p[prop]());
      });
      c3.path.pointRadius(p.path.pointRadius());
      return c3;
    };
    return registerScale(p);
  };
}
function projection2(type4, proj) {
  if (!type4 || typeof type4 !== "string") {
    throw new Error("Projection type must be a name string.");
  }
  type4 = type4.toLowerCase();
  if (arguments.length > 1) {
    projections[type4] = create2(type4, proj);
    return this;
  } else {
    return projections[type4] || null;
  }
}
function getProjectionPath(proj) {
  return proj && proj.path || defaultPath;
}
var projections = {
  // base d3-geo projection types
  albers: albers_default,
  albersusa: albersUsa_default,
  azimuthalequalarea: azimuthalEqualArea_default,
  azimuthalequidistant: azimuthalEquidistant_default,
  conicconformal: conicConformal_default,
  conicequalarea: conicEqualArea_default,
  conicequidistant: conicEquidistant_default,
  equalEarth: equalEarth_default,
  equirectangular: equirectangular_default,
  gnomonic: gnomonic_default,
  identity: identity_default4,
  mercator: mercator_default,
  mollweide: mollweide_default,
  naturalEarth1: naturalEarth1_default,
  orthographic: orthographic_default,
  stereographic: stereographic_default,
  transversemercator: transverseMercator_default
};
for (const key2 in projections) {
  projection2(key2, projections[key2]);
}

// ../deepseek-harness/node_modules/.pnpm/vega-geo@5.1.3/node_modules/vega-geo/build/vega-geo.js
function noop3() {
}
var cases = [[], [[[1, 1.5], [0.5, 1]]], [[[1.5, 1], [1, 1.5]]], [[[1.5, 1], [0.5, 1]]], [[[1, 0.5], [1.5, 1]]], [[[1, 1.5], [0.5, 1]], [[1, 0.5], [1.5, 1]]], [[[1, 0.5], [1, 1.5]]], [[[1, 0.5], [0.5, 1]]], [[[0.5, 1], [1, 0.5]]], [[[1, 1.5], [1, 0.5]]], [[[0.5, 1], [1, 0.5]], [[1.5, 1], [1, 1.5]]], [[[1.5, 1], [1, 0.5]]], [[[0.5, 1], [1.5, 1]]], [[[1, 1.5], [1.5, 1]]], [[[0.5, 1], [1, 1.5]]], []];
function contours() {
  var dx = 1, dy = 1, smooth = smoothLinear;
  function contours2(values4, tz) {
    return tz.map((value4) => contour(values4, value4));
  }
  function contour(values4, value4) {
    var polygons = [], holes = [];
    isorings(values4, value4, (ring) => {
      smooth(ring, values4, value4);
      if (area2(ring) > 0) polygons.push([ring]);
      else holes.push(ring);
    });
    holes.forEach((hole) => {
      for (var i = 0, n = polygons.length, polygon; i < n; ++i) {
        if (contains((polygon = polygons[i])[0], hole) !== -1) {
          polygon.push(hole);
          return;
        }
      }
    });
    return {
      type: "MultiPolygon",
      value: value4,
      coordinates: polygons
    };
  }
  function isorings(values4, value4, callback) {
    var fragmentByStart = [], fragmentByEnd = [], x6, y6, t05, t13, t22, t32;
    x6 = y6 = -1;
    t13 = values4[0] >= value4;
    cases[t13 << 1].forEach(stitch);
    while (++x6 < dx - 1) {
      t05 = t13, t13 = values4[x6 + 1] >= value4;
      cases[t05 | t13 << 1].forEach(stitch);
    }
    cases[t13 << 0].forEach(stitch);
    while (++y6 < dy - 1) {
      x6 = -1;
      t13 = values4[y6 * dx + dx] >= value4;
      t22 = values4[y6 * dx] >= value4;
      cases[t13 << 1 | t22 << 2].forEach(stitch);
      while (++x6 < dx - 1) {
        t05 = t13, t13 = values4[y6 * dx + dx + x6 + 1] >= value4;
        t32 = t22, t22 = values4[y6 * dx + x6 + 1] >= value4;
        cases[t05 | t13 << 1 | t22 << 2 | t32 << 3].forEach(stitch);
      }
      cases[t13 | t22 << 3].forEach(stitch);
    }
    x6 = -1;
    t22 = values4[y6 * dx] >= value4;
    cases[t22 << 2].forEach(stitch);
    while (++x6 < dx - 1) {
      t32 = t22, t22 = values4[y6 * dx + x6 + 1] >= value4;
      cases[t22 << 2 | t32 << 3].forEach(stitch);
    }
    cases[t22 << 3].forEach(stitch);
    function stitch(line5) {
      var start = [line5[0][0] + x6, line5[0][1] + y6], end = [line5[1][0] + x6, line5[1][1] + y6], startIndex = index4(start), endIndex = index4(end), f, g;
      if (f = fragmentByEnd[startIndex]) {
        if (g = fragmentByStart[endIndex]) {
          delete fragmentByEnd[f.end];
          delete fragmentByStart[g.start];
          if (f === g) {
            f.ring.push(end);
            callback(f.ring);
          } else {
            fragmentByStart[f.start] = fragmentByEnd[g.end] = {
              start: f.start,
              end: g.end,
              ring: f.ring.concat(g.ring)
            };
          }
        } else {
          delete fragmentByEnd[f.end];
          f.ring.push(end);
          fragmentByEnd[f.end = endIndex] = f;
        }
      } else if (f = fragmentByStart[endIndex]) {
        if (g = fragmentByEnd[startIndex]) {
          delete fragmentByStart[f.start];
          delete fragmentByEnd[g.end];
          if (f === g) {
            f.ring.push(end);
            callback(f.ring);
          } else {
            fragmentByStart[g.start] = fragmentByEnd[f.end] = {
              start: g.start,
              end: f.end,
              ring: g.ring.concat(f.ring)
            };
          }
        } else {
          delete fragmentByStart[f.start];
          f.ring.unshift(start);
          fragmentByStart[f.start = startIndex] = f;
        }
      } else {
        fragmentByStart[startIndex] = fragmentByEnd[endIndex] = {
          start: startIndex,
          end: endIndex,
          ring: [start, end]
        };
      }
    }
  }
  function index4(point9) {
    return point9[0] * 2 + point9[1] * (dx + 1) * 4;
  }
  function smoothLinear(ring, values4, value4) {
    ring.forEach((point9) => {
      var x6 = point9[0], y6 = point9[1], xt = x6 | 0, yt = y6 | 0, v0, v1 = values4[yt * dx + xt];
      if (x6 > 0 && x6 < dx && xt === x6) {
        v0 = values4[yt * dx + xt - 1];
        point9[0] = x6 + (value4 - v0) / (v1 - v0) - 0.5;
      }
      if (y6 > 0 && y6 < dy && yt === y6) {
        v0 = values4[(yt - 1) * dx + xt];
        point9[1] = y6 + (value4 - v0) / (v1 - v0) - 0.5;
      }
    });
  }
  contours2.contour = contour;
  contours2.size = function(_) {
    if (!arguments.length) return [dx, dy];
    var _0 = Math.floor(_[0]), _1 = Math.floor(_[1]);
    if (!(_0 >= 0 && _1 >= 0)) error("invalid size");
    return dx = _0, dy = _1, contours2;
  };
  contours2.smooth = function(_) {
    return arguments.length ? (smooth = _ ? smoothLinear : noop3, contours2) : smooth === smoothLinear;
  };
  return contours2;
}
function area2(ring) {
  var i = 0, n = ring.length, area5 = ring[n - 1][1] * ring[0][0] - ring[n - 1][0] * ring[0][1];
  while (++i < n) area5 += ring[i - 1][1] * ring[i][0] - ring[i - 1][0] * ring[i][1];
  return area5;
}
function contains(ring, hole) {
  var i = -1, n = hole.length, c3;
  while (++i < n) if (c3 = ringContains(ring, hole[i])) return c3;
  return 0;
}
function ringContains(ring, point9) {
  var x6 = point9[0], y6 = point9[1], contains3 = -1;
  for (var i = 0, n = ring.length, j = n - 1; i < n; j = i++) {
    var pi6 = ring[i], xi = pi6[0], yi = pi6[1], pj = ring[j], xj = pj[0], yj = pj[1];
    if (segmentContains(pi6, pj, point9)) return 0;
    if (yi > y6 !== yj > y6 && x6 < (xj - xi) * (y6 - yi) / (yj - yi) + xi) contains3 = -contains3;
  }
  return contains3;
}
function segmentContains(a3, b10, c3) {
  var i;
  return collinear(a3, b10, c3) && within(a3[i = +(a3[0] === b10[0])], c3[i], b10[i]);
}
function collinear(a3, b10, c3) {
  return (b10[0] - a3[0]) * (c3[1] - a3[1]) === (c3[0] - a3[0]) * (b10[1] - a3[1]);
}
function within(p, q, r) {
  return p <= q && q <= r || r <= q && q <= p;
}
function quantize2(k, nice3, zero6) {
  return function(values4) {
    var ex = extent(values4), start = zero6 ? Math.min(ex[0], 0) : ex[0], stop2 = ex[1], span = stop2 - start, step = nice3 ? tickStep(start, stop2, k) : span / (k + 1);
    return range(start + step, stop2, step);
  };
}
function Isocontour(params2) {
  Transform.call(this, null, params2);
}
Isocontour.Definition = {
  "type": "Isocontour",
  "metadata": {
    "generates": true
  },
  "params": [{
    "name": "field",
    "type": "field"
  }, {
    "name": "thresholds",
    "type": "number",
    "array": true
  }, {
    "name": "levels",
    "type": "number"
  }, {
    "name": "nice",
    "type": "boolean",
    "default": false
  }, {
    "name": "resolve",
    "type": "enum",
    "values": ["shared", "independent"],
    "default": "independent"
  }, {
    "name": "zero",
    "type": "boolean",
    "default": true
  }, {
    "name": "smooth",
    "type": "boolean",
    "default": true
  }, {
    "name": "scale",
    "type": "number",
    "expr": true
  }, {
    "name": "translate",
    "type": "number",
    "array": true,
    "expr": true
  }, {
    "name": "as",
    "type": "string",
    "null": true,
    "default": "contour"
  }]
};
inherits_default(Isocontour, Transform, {
  transform(_, pulse2) {
    if (this.value && !pulse2.changed() && !_.modified()) {
      return pulse2.StopPropagation;
    }
    var out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS), source4 = pulse2.materialize(pulse2.SOURCE).source, field3 = _.field || identity, contour = contours().smooth(_.smooth !== false), tz = _.thresholds || levels(source4, field3, _), as = _.as === null ? null : _.as || "contour", values4 = [];
    source4.forEach((t) => {
      const grid = field3(t);
      const paths = contour.size([grid.width, grid.height])(grid.values, isArray(tz) ? tz : tz(grid.values));
      transformPaths(paths, grid, t, _);
      paths.forEach((p) => {
        values4.push(rederive(t, ingest$1(as != null ? {
          [as]: p
        } : p)));
      });
    });
    if (this.value) out.rem = this.value;
    this.value = out.source = out.add = values4;
    return out;
  }
});
function levels(values4, f, _) {
  const q = quantize2(_.levels || 10, _.nice, _.zero !== false);
  return _.resolve !== "shared" ? q : q(values4.map((t) => max(f(t).values)));
}
function transformPaths(paths, grid, datum3, _) {
  let s = _.scale || grid.scale, t = _.translate || grid.translate;
  if (isFunction(s)) s = s(datum3, _);
  if (isFunction(t)) t = t(datum3, _);
  if ((s === 1 || s == null) && !t) return;
  const sx = (isNumber(s) ? s : s[0]) || 1, sy = (isNumber(s) ? s : s[1]) || 1, tx = t && t[0] || 0, ty = t && t[1] || 0;
  paths.forEach(transform2(grid, sx, sy, tx, ty));
}
function transform2(grid, sx, sy, tx, ty) {
  const x12 = grid.x1 || 0, y12 = grid.y1 || 0, flip2 = sx * sy < 0;
  function transformPolygon(coordinates) {
    coordinates.forEach(transformRing);
  }
  function transformRing(coordinates) {
    if (flip2) coordinates.reverse();
    coordinates.forEach(transformPoint);
  }
  function transformPoint(coordinates) {
    coordinates[0] = (coordinates[0] - x12) * sx + tx;
    coordinates[1] = (coordinates[1] - y12) * sy + ty;
  }
  return function(geometry) {
    geometry.coordinates.forEach(transformPolygon);
    return geometry;
  };
}
function radius(bw, data4, f) {
  const v2 = bw >= 0 ? bw : estimateBandwidth(data4, f);
  return Math.round((Math.sqrt(4 * v2 * v2 + 1) - 1) / 2);
}
function number5(_) {
  return isFunction(_) ? _ : constant_default(+_);
}
function density2D() {
  var x6 = (d) => d[0], y6 = (d) => d[1], weight = one, bandwidth3 = [-1, -1], dx = 960, dy = 500, k = 2;
  function density(data4, counts) {
    const rx = radius(bandwidth3[0], data4, x6) >> k, ry = radius(bandwidth3[1], data4, y6) >> k, ox = rx ? rx + 2 : 0, oy = ry ? ry + 2 : 0, n = 2 * ox + (dx >> k), m3 = 2 * oy + (dy >> k), values0 = new Float32Array(n * m3), values1 = new Float32Array(n * m3);
    let values4 = values0;
    data4.forEach((d) => {
      const xi = ox + (+x6(d) >> k), yi = oy + (+y6(d) >> k);
      if (xi >= 0 && xi < n && yi >= 0 && yi < m3) {
        values0[xi + yi * n] += +weight(d);
      }
    });
    if (rx > 0 && ry > 0) {
      blurX(n, m3, values0, values1, rx);
      blurY(n, m3, values1, values0, ry);
      blurX(n, m3, values0, values1, rx);
      blurY(n, m3, values1, values0, ry);
      blurX(n, m3, values0, values1, rx);
      blurY(n, m3, values1, values0, ry);
    } else if (rx > 0) {
      blurX(n, m3, values0, values1, rx);
      blurX(n, m3, values1, values0, rx);
      blurX(n, m3, values0, values1, rx);
      values4 = values1;
    } else if (ry > 0) {
      blurY(n, m3, values0, values1, ry);
      blurY(n, m3, values1, values0, ry);
      blurY(n, m3, values0, values1, ry);
      values4 = values1;
    }
    const s = counts ? Math.pow(2, -2 * k) : 1 / sum(values4);
    for (let i = 0, sz3 = n * m3; i < sz3; ++i) values4[i] *= s;
    return {
      values: values4,
      scale: 1 << k,
      width: n,
      height: m3,
      x1: ox,
      y1: oy,
      x2: ox + (dx >> k),
      y2: oy + (dy >> k)
    };
  }
  density.x = function(_) {
    return arguments.length ? (x6 = number5(_), density) : x6;
  };
  density.y = function(_) {
    return arguments.length ? (y6 = number5(_), density) : y6;
  };
  density.weight = function(_) {
    return arguments.length ? (weight = number5(_), density) : weight;
  };
  density.size = function(_) {
    if (!arguments.length) return [dx, dy];
    var _0 = +_[0], _1 = +_[1];
    if (!(_0 >= 0 && _1 >= 0)) error("invalid size");
    return dx = _0, dy = _1, density;
  };
  density.cellSize = function(_) {
    if (!arguments.length) return 1 << k;
    if (!((_ = +_) >= 1)) error("invalid cell size");
    k = Math.floor(Math.log(_) / Math.LN2);
    return density;
  };
  density.bandwidth = function(_) {
    if (!arguments.length) return bandwidth3;
    _ = array_default(_);
    if (_.length === 1) _ = [+_[0], +_[0]];
    if (_.length !== 2) error("invalid bandwidth");
    return bandwidth3 = _, density;
  };
  return density;
}
function blurX(n, m3, source4, target2, r) {
  const w3 = (r << 1) + 1;
  for (let j = 0; j < m3; ++j) {
    for (let i = 0, sr = 0; i < n + r; ++i) {
      if (i < n) {
        sr += source4[i + j * n];
      }
      if (i >= r) {
        if (i >= w3) {
          sr -= source4[i - w3 + j * n];
        }
        target2[i - r + j * n] = sr / Math.min(i + 1, n - 1 + w3 - i, w3);
      }
    }
  }
}
function blurY(n, m3, source4, target2, r) {
  const w3 = (r << 1) + 1;
  for (let i = 0; i < n; ++i) {
    for (let j = 0, sr = 0; j < m3 + r; ++j) {
      if (j < m3) {
        sr += source4[i + j * n];
      }
      if (j >= r) {
        if (j >= w3) {
          sr -= source4[i + (j - w3) * n];
        }
        target2[i + (j - r) * n] = sr / Math.min(j + 1, m3 - 1 + w3 - j, w3);
      }
    }
  }
}
function KDE2D(params2) {
  Transform.call(this, null, params2);
}
KDE2D.Definition = {
  "type": "KDE2D",
  "metadata": {
    "generates": true
  },
  "params": [{
    "name": "size",
    "type": "number",
    "array": true,
    "length": 2,
    "required": true
  }, {
    "name": "x",
    "type": "field",
    "required": true
  }, {
    "name": "y",
    "type": "field",
    "required": true
  }, {
    "name": "weight",
    "type": "field"
  }, {
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "cellSize",
    "type": "number"
  }, {
    "name": "bandwidth",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "counts",
    "type": "boolean",
    "default": false
  }, {
    "name": "as",
    "type": "string",
    "default": "grid"
  }]
};
var PARAMS = ["x", "y", "weight", "size", "cellSize", "bandwidth"];
function params(obj, _) {
  PARAMS.forEach((param2) => _[param2] != null ? obj[param2](_[param2]) : 0);
  return obj;
}
inherits_default(KDE2D, Transform, {
  transform(_, pulse2) {
    if (this.value && !pulse2.changed() && !_.modified()) return pulse2.StopPropagation;
    var out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS), source4 = pulse2.materialize(pulse2.SOURCE).source, groups = partition3(source4, _.groupby), names = (_.groupby || []).map(accessorName), kde2 = params(density2D(), _), as = _.as || "grid", values4 = [];
    function set7(t, vals2) {
      for (let i = 0; i < names.length; ++i) t[names[i]] = vals2[i];
      return t;
    }
    values4 = groups.map((g) => ingest$1(set7({
      [as]: kde2(g, _.counts)
    }, g.dims)));
    if (this.value) out.rem = this.value;
    this.value = out.source = out.add = values4;
    return out;
  }
});
function partition3(data4, groupby) {
  var groups = [], get7 = (f) => f(t), map3, i, n, t, k, g;
  if (groupby == null) {
    groups.push(data4);
  } else {
    for (map3 = {}, i = 0, n = data4.length; i < n; ++i) {
      t = data4[i];
      k = groupby.map(get7);
      g = map3[k];
      if (!g) {
        map3[k] = g = [];
        g.dims = k;
        groups.push(g);
      }
      g.push(t);
    }
  }
  return groups;
}
function Contour(params2) {
  Transform.call(this, null, params2);
}
Contour.Definition = {
  "type": "Contour",
  "metadata": {
    "generates": true
  },
  "params": [{
    "name": "size",
    "type": "number",
    "array": true,
    "length": 2,
    "required": true
  }, {
    "name": "values",
    "type": "number",
    "array": true
  }, {
    "name": "x",
    "type": "field"
  }, {
    "name": "y",
    "type": "field"
  }, {
    "name": "weight",
    "type": "field"
  }, {
    "name": "cellSize",
    "type": "number"
  }, {
    "name": "bandwidth",
    "type": "number"
  }, {
    "name": "count",
    "type": "number"
  }, {
    "name": "nice",
    "type": "boolean",
    "default": false
  }, {
    "name": "thresholds",
    "type": "number",
    "array": true
  }, {
    "name": "smooth",
    "type": "boolean",
    "default": true
  }]
};
inherits_default(Contour, Transform, {
  transform(_, pulse2) {
    if (this.value && !pulse2.changed() && !_.modified()) {
      return pulse2.StopPropagation;
    }
    var out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS), contour = contours().smooth(_.smooth !== false), values4 = _.values, thresh = _.thresholds || quantize2(_.count || 10, _.nice, !!values4), size = _.size, grid, post;
    if (!values4) {
      values4 = pulse2.materialize(pulse2.SOURCE).source;
      grid = params(density2D(), _)(values4, true);
      post = transform2(grid, grid.scale || 1, grid.scale || 1, 0, 0);
      size = [grid.width, grid.height];
      values4 = grid.values;
    }
    thresh = isArray(thresh) ? thresh : thresh(values4);
    values4 = contour.size(size)(values4, thresh);
    if (post) values4.forEach(post);
    if (this.value) out.rem = this.value;
    this.value = out.source = out.add = (values4 || []).map(ingest$1);
    return out;
  }
});
var Feature = "Feature";
var FeatureCollection = "FeatureCollection";
var MultiPoint = "MultiPoint";
function GeoJSON(params2) {
  Transform.call(this, null, params2);
}
GeoJSON.Definition = {
  "type": "GeoJSON",
  "metadata": {},
  "params": [{
    "name": "fields",
    "type": "field",
    "array": true,
    "length": 2
  }, {
    "name": "geojson",
    "type": "field"
  }]
};
inherits_default(GeoJSON, Transform, {
  transform(_, pulse2) {
    var features = this._features, points2 = this._points, fields = _.fields, lon = fields && fields[0], lat = fields && fields[1], geojson = _.geojson || !fields && identity, flag2 = pulse2.ADD, mod;
    mod = _.modified() || pulse2.changed(pulse2.REM) || pulse2.modified(accessorFields(geojson)) || lon && pulse2.modified(accessorFields(lon)) || lat && pulse2.modified(accessorFields(lat));
    if (!this.value || mod) {
      flag2 = pulse2.SOURCE;
      this._features = features = [];
      this._points = points2 = [];
    }
    if (geojson) {
      pulse2.visit(flag2, (t) => features.push(geojson(t)));
    }
    if (lon && lat) {
      pulse2.visit(flag2, (t) => {
        var x6 = lon(t), y6 = lat(t);
        if (x6 != null && y6 != null && (x6 = +x6) === x6 && (y6 = +y6) === y6) {
          points2.push([x6, y6]);
        }
      });
      features = features.concat({
        type: Feature,
        geometry: {
          type: MultiPoint,
          coordinates: points2
        }
      });
    }
    this.value = {
      type: FeatureCollection,
      features
    };
  }
});
function GeoPath(params2) {
  Transform.call(this, null, params2);
}
GeoPath.Definition = {
  "type": "GeoPath",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "projection",
    "type": "projection"
  }, {
    "name": "field",
    "type": "field"
  }, {
    "name": "pointRadius",
    "type": "number",
    "expr": true
  }, {
    "name": "as",
    "type": "string",
    "default": "path"
  }]
};
inherits_default(GeoPath, Transform, {
  transform(_, pulse2) {
    var out = pulse2.fork(pulse2.ALL), path4 = this.value, field3 = _.field || identity, as = _.as || "path", flag2 = out.SOURCE;
    if (!path4 || _.modified()) {
      this.value = path4 = getProjectionPath(_.projection);
      out.materialize().reflow();
    } else {
      flag2 = field3 === identity || pulse2.modified(field3.fields) ? out.ADD_MOD : out.ADD;
    }
    const prev = initPath(path4, _.pointRadius);
    out.visit(flag2, (t) => t[as] = path4(field3(t)));
    path4.pointRadius(prev);
    return out.modifies(as);
  }
});
function initPath(path4, pointRadius) {
  const prev = path4.pointRadius();
  path4.context(null);
  if (pointRadius != null) {
    path4.pointRadius(pointRadius);
  }
  return prev;
}
function GeoPoint(params2) {
  Transform.call(this, null, params2);
}
GeoPoint.Definition = {
  "type": "GeoPoint",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "projection",
    "type": "projection",
    "required": true
  }, {
    "name": "fields",
    "type": "field",
    "array": true,
    "required": true,
    "length": 2
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": 2,
    "default": ["x", "y"]
  }]
};
inherits_default(GeoPoint, Transform, {
  transform(_, pulse2) {
    var proj = _.projection, lon = _.fields[0], lat = _.fields[1], as = _.as || ["x", "y"], x6 = as[0], y6 = as[1], mod;
    function set7(t) {
      const xy = proj([lon(t), lat(t)]);
      if (xy) {
        t[x6] = xy[0];
        t[y6] = xy[1];
      } else {
        t[x6] = void 0;
        t[y6] = void 0;
      }
    }
    if (_.modified()) {
      pulse2 = pulse2.materialize().reflow(true).visit(pulse2.SOURCE, set7);
    } else {
      mod = pulse2.modified(lon.fields) || pulse2.modified(lat.fields);
      pulse2.visit(mod ? pulse2.ADD_MOD : pulse2.ADD, set7);
    }
    return pulse2.modifies(as);
  }
});
function GeoShape(params2) {
  Transform.call(this, null, params2);
}
GeoShape.Definition = {
  "type": "GeoShape",
  "metadata": {
    "modifies": true,
    "nomod": true
  },
  "params": [{
    "name": "projection",
    "type": "projection"
  }, {
    "name": "field",
    "type": "field",
    "default": "datum"
  }, {
    "name": "pointRadius",
    "type": "number",
    "expr": true
  }, {
    "name": "as",
    "type": "string",
    "default": "shape"
  }]
};
inherits_default(GeoShape, Transform, {
  transform(_, pulse2) {
    var out = pulse2.fork(pulse2.ALL), shape3 = this.value, as = _.as || "shape", flag2 = out.ADD;
    if (!shape3 || _.modified()) {
      this.value = shape3 = shapeGenerator(getProjectionPath(_.projection), _.field || field("datum"), _.pointRadius);
      out.materialize().reflow();
      flag2 = out.SOURCE;
    }
    out.visit(flag2, (t) => t[as] = shape3);
    return out.modifies(as);
  }
});
function shapeGenerator(path4, field3, pointRadius) {
  const shape3 = pointRadius == null ? (_) => path4(field3(_)) : (_) => {
    var prev = path4.pointRadius(), value4 = path4.pointRadius(pointRadius)(field3(_));
    path4.pointRadius(prev);
    return value4;
  };
  shape3.context = (_) => {
    path4.context(_);
    return shape3;
  };
  return shape3;
}
function Graticule(params2) {
  Transform.call(this, [], params2);
  this.generator = graticule();
}
Graticule.Definition = {
  "type": "Graticule",
  "metadata": {
    "changes": true,
    "generates": true
  },
  "params": [{
    "name": "extent",
    "type": "array",
    "array": true,
    "length": 2,
    "content": {
      "type": "number",
      "array": true,
      "length": 2
    }
  }, {
    "name": "extentMajor",
    "type": "array",
    "array": true,
    "length": 2,
    "content": {
      "type": "number",
      "array": true,
      "length": 2
    }
  }, {
    "name": "extentMinor",
    "type": "array",
    "array": true,
    "length": 2,
    "content": {
      "type": "number",
      "array": true,
      "length": 2
    }
  }, {
    "name": "step",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "stepMajor",
    "type": "number",
    "array": true,
    "length": 2,
    "default": [90, 360]
  }, {
    "name": "stepMinor",
    "type": "number",
    "array": true,
    "length": 2,
    "default": [10, 10]
  }, {
    "name": "precision",
    "type": "number",
    "default": 2.5
  }]
};
inherits_default(Graticule, Transform, {
  transform(_, pulse2) {
    var src = this.value, gen = this.generator, t;
    if (!src.length || _.modified()) {
      for (const prop in _) {
        if (isFunction(gen[prop])) {
          gen[prop](_[prop]);
        }
      }
    }
    t = gen();
    if (src.length) {
      pulse2.mod.push(replace(src[0], t));
    } else {
      pulse2.add.push(ingest$1(t));
    }
    src[0] = t;
    return pulse2;
  }
});
function Heatmap(params2) {
  Transform.call(this, null, params2);
}
Heatmap.Definition = {
  "type": "heatmap",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "field",
    "type": "field"
  }, {
    "name": "color",
    "type": "string",
    "expr": true
  }, {
    "name": "opacity",
    "type": "number",
    "expr": true
  }, {
    "name": "resolve",
    "type": "enum",
    "values": ["shared", "independent"],
    "default": "independent"
  }, {
    "name": "as",
    "type": "string",
    "default": "image"
  }]
};
inherits_default(Heatmap, Transform, {
  transform(_, pulse2) {
    if (!pulse2.changed() && !_.modified()) {
      return pulse2.StopPropagation;
    }
    var source4 = pulse2.materialize(pulse2.SOURCE).source, shared = _.resolve === "shared", field3 = _.field || identity, opacity2 = opacity_(_.opacity, _), color6 = color_(_.color, _), as = _.as || "image", obj = {
      $x: 0,
      $y: 0,
      $value: 0,
      $max: shared ? max(source4.map((t) => max(field3(t).values))) : 0
    };
    source4.forEach((t) => {
      const v2 = field3(t);
      const o = extend({}, t, obj);
      if (!shared) o.$max = max(v2.values || []);
      t[as] = toCanvas(v2, o, color6.dep ? color6 : constant_default(color6(o)), opacity2.dep ? opacity2 : constant_default(opacity2(o)));
    });
    return pulse2.reflow(true).modifies(as);
  }
});
function color_(color6, _) {
  let f;
  if (isFunction(color6)) {
    f = (obj) => rgb(color6(obj, _));
    f.dep = dependency(color6);
  } else {
    f = constant_default(rgb(color6 || "#888"));
  }
  return f;
}
function opacity_(opacity2, _) {
  let f;
  if (isFunction(opacity2)) {
    f = (obj) => opacity2(obj, _);
    f.dep = dependency(opacity2);
  } else if (opacity2) {
    f = constant_default(opacity2);
  } else {
    f = (obj) => obj.$value / obj.$max || 0;
    f.dep = true;
  }
  return f;
}
function dependency(f) {
  if (!isFunction(f)) return false;
  const set7 = toSet(accessorFields(f));
  return set7.$x || set7.$y || set7.$value || set7.$max;
}
function toCanvas(grid, obj, color6, opacity2) {
  const n = grid.width, m3 = grid.height, x12 = grid.x1 || 0, y12 = grid.y1 || 0, x22 = grid.x2 || n, y22 = grid.y2 || m3, val = grid.values, value4 = val ? (i) => val[i] : zero, can = domCanvas(x22 - x12, y22 - y12), ctx = can.getContext("2d"), img = ctx.getImageData(0, 0, x22 - x12, y22 - y12), pix = img.data;
  for (let j = y12, k = 0; j < y22; ++j) {
    obj.$y = j - y12;
    for (let i = x12, r = j * n; i < x22; ++i, k += 4) {
      obj.$x = i - x12;
      obj.$value = value4(i + r);
      const v2 = color6(obj);
      pix[k + 0] = v2.r;
      pix[k + 1] = v2.g;
      pix[k + 2] = v2.b;
      pix[k + 3] = ~~(255 * opacity2(obj));
    }
  }
  ctx.putImageData(img, 0, 0);
  return can;
}
function Projection(params2) {
  Transform.call(this, null, params2);
  this.modified(true);
}
inherits_default(Projection, Transform, {
  transform(_, pulse2) {
    let proj = this.value;
    if (!proj || _.modified("type")) {
      this.value = proj = create3(_.type);
      projectionProperties.forEach((prop) => {
        if (_[prop] != null) set4(proj, prop, _[prop]);
      });
    } else {
      projectionProperties.forEach((prop) => {
        if (_.modified(prop)) set4(proj, prop, _[prop]);
      });
    }
    if (_.pointRadius != null) proj.path.pointRadius(_.pointRadius);
    if (_.fit) fit2(proj, _);
    return pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS);
  }
});
function fit2(proj, _) {
  const data4 = collectGeoJSON(_.fit);
  _.extent ? proj.fitExtent(_.extent, data4) : _.size ? proj.fitSize(_.size, data4) : 0;
}
function create3(type4) {
  const constructor = projection2((type4 || "mercator").toLowerCase());
  if (!constructor) error("Unrecognized projection type: " + type4);
  return constructor();
}
function set4(proj, key2, value4) {
  if (isFunction(proj[key2])) proj[key2](value4);
}
function collectGeoJSON(data4) {
  data4 = array_default(data4);
  return data4.length === 1 ? data4[0] : {
    type: FeatureCollection,
    features: data4.reduce((a3, f) => a3.concat(featurize(f)), [])
  };
}
function featurize(f) {
  return f.type === FeatureCollection ? f.features : array_default(f).filter((d) => d != null).map((d) => d.type === Feature ? d : {
    type: Feature,
    geometry: d
  });
}

// ../deepseek-harness/node_modules/.pnpm/vega-force@5.1.3/node_modules/vega-force/build/vega-force.js
var vega_force_exports = {};
__export(vega_force_exports, {
  force: () => Force
});

// ../deepseek-harness/node_modules/.pnpm/d3-force@3.0.0/node_modules/d3-force/src/center.js
function center_default(x6, y6) {
  var nodes, strength = 1;
  if (x6 == null) x6 = 0;
  if (y6 == null) y6 = 0;
  function force() {
    var i, n = nodes.length, node, sx = 0, sy = 0;
    for (i = 0; i < n; ++i) {
      node = nodes[i], sx += node.x, sy += node.y;
    }
    for (sx = (sx / n - x6) * strength, sy = (sy / n - y6) * strength, i = 0; i < n; ++i) {
      node = nodes[i], node.x -= sx, node.y -= sy;
    }
  }
  force.initialize = function(_) {
    nodes = _;
  };
  force.x = function(_) {
    return arguments.length ? (x6 = +_, force) : x6;
  };
  force.y = function(_) {
    return arguments.length ? (y6 = +_, force) : y6;
  };
  force.strength = function(_) {
    return arguments.length ? (strength = +_, force) : strength;
  };
  return force;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/add.js
function add_default(d) {
  const x6 = +this._x.call(null, d), y6 = +this._y.call(null, d);
  return add4(this.cover(x6, y6), x6, y6, d);
}
function add4(tree, x6, y6, d) {
  if (isNaN(x6) || isNaN(y6)) return tree;
  var parent, node = tree._root, leaf = { data: d }, x06 = tree._x0, y06 = tree._y0, x12 = tree._x1, y12 = tree._y1, xm, ym, xp, yp, right, bottom, i, j;
  if (!node) return tree._root = leaf, tree;
  while (node.length) {
    if (right = x6 >= (xm = (x06 + x12) / 2)) x06 = xm;
    else x12 = xm;
    if (bottom = y6 >= (ym = (y06 + y12) / 2)) y06 = ym;
    else y12 = ym;
    if (parent = node, !(node = node[i = bottom << 1 | right])) return parent[i] = leaf, tree;
  }
  xp = +tree._x.call(null, node.data);
  yp = +tree._y.call(null, node.data);
  if (x6 === xp && y6 === yp) return leaf.next = node, parent ? parent[i] = leaf : tree._root = leaf, tree;
  do {
    parent = parent ? parent[i] = new Array(4) : tree._root = new Array(4);
    if (right = x6 >= (xm = (x06 + x12) / 2)) x06 = xm;
    else x12 = xm;
    if (bottom = y6 >= (ym = (y06 + y12) / 2)) y06 = ym;
    else y12 = ym;
  } while ((i = bottom << 1 | right) === (j = (yp >= ym) << 1 | xp >= xm));
  return parent[j] = node, parent[i] = leaf, tree;
}
function addAll(data4) {
  var d, i, n = data4.length, x6, y6, xz = new Array(n), yz = new Array(n), x06 = Infinity, y06 = Infinity, x12 = -Infinity, y12 = -Infinity;
  for (i = 0; i < n; ++i) {
    if (isNaN(x6 = +this._x.call(null, d = data4[i])) || isNaN(y6 = +this._y.call(null, d))) continue;
    xz[i] = x6;
    yz[i] = y6;
    if (x6 < x06) x06 = x6;
    if (x6 > x12) x12 = x6;
    if (y6 < y06) y06 = y6;
    if (y6 > y12) y12 = y6;
  }
  if (x06 > x12 || y06 > y12) return this;
  this.cover(x06, y06).cover(x12, y12);
  for (i = 0; i < n; ++i) {
    add4(this, xz[i], yz[i], data4[i]);
  }
  return this;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/cover.js
function cover_default(x6, y6) {
  if (isNaN(x6 = +x6) || isNaN(y6 = +y6)) return this;
  var x06 = this._x0, y06 = this._y0, x12 = this._x1, y12 = this._y1;
  if (isNaN(x06)) {
    x12 = (x06 = Math.floor(x6)) + 1;
    y12 = (y06 = Math.floor(y6)) + 1;
  } else {
    var z = x12 - x06 || 1, node = this._root, parent, i;
    while (x06 > x6 || x6 >= x12 || y06 > y6 || y6 >= y12) {
      i = (y6 < y06) << 1 | x6 < x06;
      parent = new Array(4), parent[i] = node, node = parent, z *= 2;
      switch (i) {
        case 0:
          x12 = x06 + z, y12 = y06 + z;
          break;
        case 1:
          x06 = x12 - z, y12 = y06 + z;
          break;
        case 2:
          x12 = x06 + z, y06 = y12 - z;
          break;
        case 3:
          x06 = x12 - z, y06 = y12 - z;
          break;
      }
    }
    if (this._root && this._root.length) this._root = node;
  }
  this._x0 = x06;
  this._y0 = y06;
  this._x1 = x12;
  this._y1 = y12;
  return this;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/data.js
function data_default() {
  var data4 = [];
  this.visit(function(node) {
    if (!node.length) do
      data4.push(node.data);
    while (node = node.next);
  });
  return data4;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/extent.js
function extent_default(_) {
  return arguments.length ? this.cover(+_[0][0], +_[0][1]).cover(+_[1][0], +_[1][1]) : isNaN(this._x0) ? void 0 : [[this._x0, this._y0], [this._x1, this._y1]];
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/quad.js
function quad_default(node, x06, y06, x12, y12) {
  this.node = node;
  this.x0 = x06;
  this.y0 = y06;
  this.x1 = x12;
  this.y1 = y12;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/find.js
function find_default(x6, y6, radius2) {
  var data4, x06 = this._x0, y06 = this._y0, x12, y12, x22, y22, x32 = this._x1, y32 = this._y1, quads = [], node = this._root, q, i;
  if (node) quads.push(new quad_default(node, x06, y06, x32, y32));
  if (radius2 == null) radius2 = Infinity;
  else {
    x06 = x6 - radius2, y06 = y6 - radius2;
    x32 = x6 + radius2, y32 = y6 + radius2;
    radius2 *= radius2;
  }
  while (q = quads.pop()) {
    if (!(node = q.node) || (x12 = q.x0) > x32 || (y12 = q.y0) > y32 || (x22 = q.x1) < x06 || (y22 = q.y1) < y06) continue;
    if (node.length) {
      var xm = (x12 + x22) / 2, ym = (y12 + y22) / 2;
      quads.push(
        new quad_default(node[3], xm, ym, x22, y22),
        new quad_default(node[2], x12, ym, xm, y22),
        new quad_default(node[1], xm, y12, x22, ym),
        new quad_default(node[0], x12, y12, xm, ym)
      );
      if (i = (y6 >= ym) << 1 | x6 >= xm) {
        q = quads[quads.length - 1];
        quads[quads.length - 1] = quads[quads.length - 1 - i];
        quads[quads.length - 1 - i] = q;
      }
    } else {
      var dx = x6 - +this._x.call(null, node.data), dy = y6 - +this._y.call(null, node.data), d2 = dx * dx + dy * dy;
      if (d2 < radius2) {
        var d = Math.sqrt(radius2 = d2);
        x06 = x6 - d, y06 = y6 - d;
        x32 = x6 + d, y32 = y6 + d;
        data4 = node.data;
      }
    }
  }
  return data4;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/remove.js
function remove_default(d) {
  if (isNaN(x6 = +this._x.call(null, d)) || isNaN(y6 = +this._y.call(null, d))) return this;
  var parent, node = this._root, retainer, previous, next, x06 = this._x0, y06 = this._y0, x12 = this._x1, y12 = this._y1, x6, y6, xm, ym, right, bottom, i, j;
  if (!node) return this;
  if (node.length) while (true) {
    if (right = x6 >= (xm = (x06 + x12) / 2)) x06 = xm;
    else x12 = xm;
    if (bottom = y6 >= (ym = (y06 + y12) / 2)) y06 = ym;
    else y12 = ym;
    if (!(parent = node, node = node[i = bottom << 1 | right])) return this;
    if (!node.length) break;
    if (parent[i + 1 & 3] || parent[i + 2 & 3] || parent[i + 3 & 3]) retainer = parent, j = i;
  }
  while (node.data !== d) if (!(previous = node, node = node.next)) return this;
  if (next = node.next) delete node.next;
  if (previous) return next ? previous.next = next : delete previous.next, this;
  if (!parent) return this._root = next, this;
  next ? parent[i] = next : delete parent[i];
  if ((node = parent[0] || parent[1] || parent[2] || parent[3]) && node === (parent[3] || parent[2] || parent[1] || parent[0]) && !node.length) {
    if (retainer) retainer[j] = node;
    else this._root = node;
  }
  return this;
}
function removeAll(data4) {
  for (var i = 0, n = data4.length; i < n; ++i) this.remove(data4[i]);
  return this;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/root.js
function root_default() {
  return this._root;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/size.js
function size_default() {
  var size = 0;
  this.visit(function(node) {
    if (!node.length) do
      ++size;
    while (node = node.next);
  });
  return size;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/visit.js
function visit_default(callback) {
  var quads = [], q, node = this._root, child, x06, y06, x12, y12;
  if (node) quads.push(new quad_default(node, this._x0, this._y0, this._x1, this._y1));
  while (q = quads.pop()) {
    if (!callback(node = q.node, x06 = q.x0, y06 = q.y0, x12 = q.x1, y12 = q.y1) && node.length) {
      var xm = (x06 + x12) / 2, ym = (y06 + y12) / 2;
      if (child = node[3]) quads.push(new quad_default(child, xm, ym, x12, y12));
      if (child = node[2]) quads.push(new quad_default(child, x06, ym, xm, y12));
      if (child = node[1]) quads.push(new quad_default(child, xm, y06, x12, ym));
      if (child = node[0]) quads.push(new quad_default(child, x06, y06, xm, ym));
    }
  }
  return this;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/visitAfter.js
function visitAfter_default(callback) {
  var quads = [], next = [], q;
  if (this._root) quads.push(new quad_default(this._root, this._x0, this._y0, this._x1, this._y1));
  while (q = quads.pop()) {
    var node = q.node;
    if (node.length) {
      var child, x06 = q.x0, y06 = q.y0, x12 = q.x1, y12 = q.y1, xm = (x06 + x12) / 2, ym = (y06 + y12) / 2;
      if (child = node[0]) quads.push(new quad_default(child, x06, y06, xm, ym));
      if (child = node[1]) quads.push(new quad_default(child, xm, y06, x12, ym));
      if (child = node[2]) quads.push(new quad_default(child, x06, ym, xm, y12));
      if (child = node[3]) quads.push(new quad_default(child, xm, ym, x12, y12));
    }
    next.push(q);
  }
  while (q = next.pop()) {
    callback(q.node, q.x0, q.y0, q.x1, q.y1);
  }
  return this;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/x.js
function defaultX(d) {
  return d[0];
}
function x_default(_) {
  return arguments.length ? (this._x = _, this) : this._x;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/y.js
function defaultY(d) {
  return d[1];
}
function y_default(_) {
  return arguments.length ? (this._y = _, this) : this._y;
}

// ../deepseek-harness/node_modules/.pnpm/d3-quadtree@3.0.1/node_modules/d3-quadtree/src/quadtree.js
function quadtree(nodes, x6, y6) {
  var tree = new Quadtree(x6 == null ? defaultX : x6, y6 == null ? defaultY : y6, NaN, NaN, NaN, NaN);
  return nodes == null ? tree : tree.addAll(nodes);
}
function Quadtree(x6, y6, x06, y06, x12, y12) {
  this._x = x6;
  this._y = y6;
  this._x0 = x06;
  this._y0 = y06;
  this._x1 = x12;
  this._y1 = y12;
  this._root = void 0;
}
function leaf_copy(leaf) {
  var copy5 = { data: leaf.data }, next = copy5;
  while (leaf = leaf.next) next = next.next = { data: leaf.data };
  return copy5;
}
var treeProto = quadtree.prototype = Quadtree.prototype;
treeProto.copy = function() {
  var copy5 = new Quadtree(this._x, this._y, this._x0, this._y0, this._x1, this._y1), node = this._root, nodes, child;
  if (!node) return copy5;
  if (!node.length) return copy5._root = leaf_copy(node), copy5;
  nodes = [{ source: node, target: copy5._root = new Array(4) }];
  while (node = nodes.pop()) {
    for (var i = 0; i < 4; ++i) {
      if (child = node.source[i]) {
        if (child.length) nodes.push({ source: child, target: node.target[i] = new Array(4) });
        else node.target[i] = leaf_copy(child);
      }
    }
  }
  return copy5;
};
treeProto.add = add_default;
treeProto.addAll = addAll;
treeProto.cover = cover_default;
treeProto.data = data_default;
treeProto.extent = extent_default;
treeProto.find = find_default;
treeProto.remove = remove_default;
treeProto.removeAll = removeAll;
treeProto.root = root_default;
treeProto.size = size_default;
treeProto.visit = visit_default;
treeProto.visitAfter = visitAfter_default;
treeProto.x = x_default;
treeProto.y = y_default;

// ../deepseek-harness/node_modules/.pnpm/d3-force@3.0.0/node_modules/d3-force/src/constant.js
function constant_default4(x6) {
  return function() {
    return x6;
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-force@3.0.0/node_modules/d3-force/src/jiggle.js
function jiggle_default(random2) {
  return (random2() - 0.5) * 1e-6;
}

// ../deepseek-harness/node_modules/.pnpm/d3-force@3.0.0/node_modules/d3-force/src/collide.js
function x3(d) {
  return d.x + d.vx;
}
function y3(d) {
  return d.y + d.vy;
}
function collide_default(radius2) {
  var nodes, radii, random2, strength = 1, iterations2 = 1;
  if (typeof radius2 !== "function") radius2 = constant_default4(radius2 == null ? 1 : +radius2);
  function force() {
    var i, n = nodes.length, tree, node, xi, yi, ri, ri2;
    for (var k = 0; k < iterations2; ++k) {
      tree = quadtree(nodes, x3, y3).visitAfter(prepare2);
      for (i = 0; i < n; ++i) {
        node = nodes[i];
        ri = radii[node.index], ri2 = ri * ri;
        xi = node.x + node.vx;
        yi = node.y + node.vy;
        tree.visit(apply3);
      }
    }
    function apply3(quad2, x06, y06, x12, y12) {
      var data4 = quad2.data, rj = quad2.r, r = ri + rj;
      if (data4) {
        if (data4.index > node.index) {
          var x6 = xi - data4.x - data4.vx, y6 = yi - data4.y - data4.vy, l = x6 * x6 + y6 * y6;
          if (l < r * r) {
            if (x6 === 0) x6 = jiggle_default(random2), l += x6 * x6;
            if (y6 === 0) y6 = jiggle_default(random2), l += y6 * y6;
            l = (r - (l = Math.sqrt(l))) / l * strength;
            node.vx += (x6 *= l) * (r = (rj *= rj) / (ri2 + rj));
            node.vy += (y6 *= l) * r;
            data4.vx -= x6 * (r = 1 - r);
            data4.vy -= y6 * r;
          }
        }
        return;
      }
      return x06 > xi + r || x12 < xi - r || y06 > yi + r || y12 < yi - r;
    }
  }
  function prepare2(quad2) {
    if (quad2.data) return quad2.r = radii[quad2.data.index];
    for (var i = quad2.r = 0; i < 4; ++i) {
      if (quad2[i] && quad2[i].r > quad2.r) {
        quad2.r = quad2[i].r;
      }
    }
  }
  function initialize3() {
    if (!nodes) return;
    var i, n = nodes.length, node;
    radii = new Array(n);
    for (i = 0; i < n; ++i) node = nodes[i], radii[node.index] = +radius2(node, i, nodes);
  }
  force.initialize = function(_nodes, _random) {
    nodes = _nodes;
    random2 = _random;
    initialize3();
  };
  force.iterations = function(_) {
    return arguments.length ? (iterations2 = +_, force) : iterations2;
  };
  force.strength = function(_) {
    return arguments.length ? (strength = +_, force) : strength;
  };
  force.radius = function(_) {
    return arguments.length ? (radius2 = typeof _ === "function" ? _ : constant_default4(+_), initialize3(), force) : radius2;
  };
  return force;
}

// ../deepseek-harness/node_modules/.pnpm/d3-force@3.0.0/node_modules/d3-force/src/link.js
function index(d) {
  return d.index;
}
function find2(nodeById, nodeId) {
  var node = nodeById.get(nodeId);
  if (!node) throw new Error("node not found: " + nodeId);
  return node;
}
function link_default(links) {
  var id2 = index, strength = defaultStrength, strengths, distance = constant_default4(30), distances, nodes, count2, bias, random2, iterations2 = 1;
  if (links == null) links = [];
  function defaultStrength(link2) {
    return 1 / Math.min(count2[link2.source.index], count2[link2.target.index]);
  }
  function force(alpha) {
    for (var k = 0, n = links.length; k < iterations2; ++k) {
      for (var i = 0, link2, source4, target2, x6, y6, l, b10; i < n; ++i) {
        link2 = links[i], source4 = link2.source, target2 = link2.target;
        x6 = target2.x + target2.vx - source4.x - source4.vx || jiggle_default(random2);
        y6 = target2.y + target2.vy - source4.y - source4.vy || jiggle_default(random2);
        l = Math.sqrt(x6 * x6 + y6 * y6);
        l = (l - distances[i]) / l * alpha * strengths[i];
        x6 *= l, y6 *= l;
        target2.vx -= x6 * (b10 = bias[i]);
        target2.vy -= y6 * b10;
        source4.vx += x6 * (b10 = 1 - b10);
        source4.vy += y6 * b10;
      }
    }
  }
  function initialize3() {
    if (!nodes) return;
    var i, n = nodes.length, m3 = links.length, nodeById = new Map(nodes.map((d, i2) => [id2(d, i2, nodes), d])), link2;
    for (i = 0, count2 = new Array(n); i < m3; ++i) {
      link2 = links[i], link2.index = i;
      if (typeof link2.source !== "object") link2.source = find2(nodeById, link2.source);
      if (typeof link2.target !== "object") link2.target = find2(nodeById, link2.target);
      count2[link2.source.index] = (count2[link2.source.index] || 0) + 1;
      count2[link2.target.index] = (count2[link2.target.index] || 0) + 1;
    }
    for (i = 0, bias = new Array(m3); i < m3; ++i) {
      link2 = links[i], bias[i] = count2[link2.source.index] / (count2[link2.source.index] + count2[link2.target.index]);
    }
    strengths = new Array(m3), initializeStrength();
    distances = new Array(m3), initializeDistance();
  }
  function initializeStrength() {
    if (!nodes) return;
    for (var i = 0, n = links.length; i < n; ++i) {
      strengths[i] = +strength(links[i], i, links);
    }
  }
  function initializeDistance() {
    if (!nodes) return;
    for (var i = 0, n = links.length; i < n; ++i) {
      distances[i] = +distance(links[i], i, links);
    }
  }
  force.initialize = function(_nodes, _random) {
    nodes = _nodes;
    random2 = _random;
    initialize3();
  };
  force.links = function(_) {
    return arguments.length ? (links = _, initialize3(), force) : links;
  };
  force.id = function(_) {
    return arguments.length ? (id2 = _, force) : id2;
  };
  force.iterations = function(_) {
    return arguments.length ? (iterations2 = +_, force) : iterations2;
  };
  force.strength = function(_) {
    return arguments.length ? (strength = typeof _ === "function" ? _ : constant_default4(+_), initializeStrength(), force) : strength;
  };
  force.distance = function(_) {
    return arguments.length ? (distance = typeof _ === "function" ? _ : constant_default4(+_), initializeDistance(), force) : distance;
  };
  return force;
}

// ../deepseek-harness/node_modules/.pnpm/d3-dispatch@3.0.1/node_modules/d3-dispatch/src/dispatch.js
var noop4 = { value: () => {
} };
function dispatch() {
  for (var i = 0, n = arguments.length, _ = {}, t; i < n; ++i) {
    if (!(t = arguments[i] + "") || t in _ || /[\s.]/.test(t)) throw new Error("illegal type: " + t);
    _[t] = [];
  }
  return new Dispatch(_);
}
function Dispatch(_) {
  this._ = _;
}
function parseTypenames(typenames, types) {
  return typenames.trim().split(/^|\s+/).map(function(t) {
    var name = "", i = t.indexOf(".");
    if (i >= 0) name = t.slice(i + 1), t = t.slice(0, i);
    if (t && !types.hasOwnProperty(t)) throw new Error("unknown type: " + t);
    return { type: t, name };
  });
}
Dispatch.prototype = dispatch.prototype = {
  constructor: Dispatch,
  on: function(typename, callback) {
    var _ = this._, T = parseTypenames(typename + "", _), t, i = -1, n = T.length;
    if (arguments.length < 2) {
      while (++i < n) if ((t = (typename = T[i]).type) && (t = get5(_[t], typename.name))) return t;
      return;
    }
    if (callback != null && typeof callback !== "function") throw new Error("invalid callback: " + callback);
    while (++i < n) {
      if (t = (typename = T[i]).type) _[t] = set5(_[t], typename.name, callback);
      else if (callback == null) for (t in _) _[t] = set5(_[t], typename.name, null);
    }
    return this;
  },
  copy: function() {
    var copy5 = {}, _ = this._;
    for (var t in _) copy5[t] = _[t].slice();
    return new Dispatch(copy5);
  },
  call: function(type4, that) {
    if ((n = arguments.length - 2) > 0) for (var args = new Array(n), i = 0, n, t; i < n; ++i) args[i] = arguments[i + 2];
    if (!this._.hasOwnProperty(type4)) throw new Error("unknown type: " + type4);
    for (t = this._[type4], i = 0, n = t.length; i < n; ++i) t[i].value.apply(that, args);
  },
  apply: function(type4, that, args) {
    if (!this._.hasOwnProperty(type4)) throw new Error("unknown type: " + type4);
    for (var t = this._[type4], i = 0, n = t.length; i < n; ++i) t[i].value.apply(that, args);
  }
};
function get5(type4, name) {
  for (var i = 0, n = type4.length, c3; i < n; ++i) {
    if ((c3 = type4[i]).name === name) {
      return c3.value;
    }
  }
}
function set5(type4, name, callback) {
  for (var i = 0, n = type4.length; i < n; ++i) {
    if (type4[i].name === name) {
      type4[i] = noop4, type4 = type4.slice(0, i).concat(type4.slice(i + 1));
      break;
    }
  }
  if (callback != null) type4.push({ name, value: callback });
  return type4;
}
var dispatch_default = dispatch;

// ../deepseek-harness/node_modules/.pnpm/d3-timer@3.0.1/node_modules/d3-timer/src/timer.js
var frame = 0;
var timeout = 0;
var interval = 0;
var pokeDelay = 1e3;
var taskHead;
var taskTail;
var clockLast = 0;
var clockNow = 0;
var clockSkew = 0;
var clock = typeof performance === "object" && performance.now ? performance : Date;
var setFrame = typeof window === "object" && window.requestAnimationFrame ? window.requestAnimationFrame.bind(window) : function(f) {
  setTimeout(f, 17);
};
function now() {
  return clockNow || (setFrame(clearNow), clockNow = clock.now() + clockSkew);
}
function clearNow() {
  clockNow = 0;
}
function Timer() {
  this._call = this._time = this._next = null;
}
Timer.prototype = timer.prototype = {
  constructor: Timer,
  restart: function(callback, delay, time4) {
    if (typeof callback !== "function") throw new TypeError("callback is not a function");
    time4 = (time4 == null ? now() : +time4) + (delay == null ? 0 : +delay);
    if (!this._next && taskTail !== this) {
      if (taskTail) taskTail._next = this;
      else taskHead = this;
      taskTail = this;
    }
    this._call = callback;
    this._time = time4;
    sleep();
  },
  stop: function() {
    if (this._call) {
      this._call = null;
      this._time = Infinity;
      sleep();
    }
  }
};
function timer(callback, delay, time4) {
  var t = new Timer();
  t.restart(callback, delay, time4);
  return t;
}
function timerFlush() {
  now();
  ++frame;
  var t = taskHead, e;
  while (t) {
    if ((e = clockNow - t._time) >= 0) t._call.call(void 0, e);
    t = t._next;
  }
  --frame;
}
function wake() {
  clockNow = (clockLast = clock.now()) + clockSkew;
  frame = timeout = 0;
  try {
    timerFlush();
  } finally {
    frame = 0;
    nap();
    clockNow = 0;
  }
}
function poke() {
  var now2 = clock.now(), delay = now2 - clockLast;
  if (delay > pokeDelay) clockSkew -= delay, clockLast = now2;
}
function nap() {
  var t05, t13 = taskHead, t22, time4 = Infinity;
  while (t13) {
    if (t13._call) {
      if (time4 > t13._time) time4 = t13._time;
      t05 = t13, t13 = t13._next;
    } else {
      t22 = t13._next, t13._next = null;
      t13 = t05 ? t05._next = t22 : taskHead = t22;
    }
  }
  taskTail = t05;
  sleep(time4);
}
function sleep(time4) {
  if (frame) return;
  if (timeout) timeout = clearTimeout(timeout);
  var delay = time4 - clockNow;
  if (delay > 24) {
    if (time4 < Infinity) timeout = setTimeout(wake, time4 - clock.now() - clockSkew);
    if (interval) interval = clearInterval(interval);
  } else {
    if (!interval) clockLast = clock.now(), interval = setInterval(poke, pokeDelay);
    frame = 1, setFrame(wake);
  }
}

// ../deepseek-harness/node_modules/.pnpm/d3-timer@3.0.1/node_modules/d3-timer/src/interval.js
function interval_default(callback, delay, time4) {
  var t = new Timer(), total = delay;
  if (delay == null) return t.restart(callback, delay, time4), t;
  t._restart = t.restart;
  t.restart = function(callback2, delay2, time5) {
    delay2 = +delay2, time5 = time5 == null ? now() : +time5;
    t._restart(function tick2(elapsed) {
      elapsed += total;
      t._restart(tick2, total += delay2, time5);
      callback2(elapsed);
    }, delay2, time5);
  };
  t.restart(callback, delay, time4);
  return t;
}

// ../deepseek-harness/node_modules/.pnpm/d3-force@3.0.0/node_modules/d3-force/src/lcg.js
var a = 1664525;
var c = 1013904223;
var m = 4294967296;
function lcg_default() {
  let s = 1;
  return () => (s = (a * s + c) % m) / m;
}

// ../deepseek-harness/node_modules/.pnpm/d3-force@3.0.0/node_modules/d3-force/src/simulation.js
function x4(d) {
  return d.x;
}
function y4(d) {
  return d.y;
}
var initialRadius = 10;
var initialAngle = Math.PI * (3 - Math.sqrt(5));
function simulation_default(nodes) {
  var simulation2, alpha = 1, alphaMin = 1e-3, alphaDecay = 1 - Math.pow(alphaMin, 1 / 300), alphaTarget = 0, velocityDecay = 0.6, forces = /* @__PURE__ */ new Map(), stepper = timer(step), event2 = dispatch_default("tick", "end"), random2 = lcg_default();
  if (nodes == null) nodes = [];
  function step() {
    tick2();
    event2.call("tick", simulation2);
    if (alpha < alphaMin) {
      stepper.stop();
      event2.call("end", simulation2);
    }
  }
  function tick2(iterations2) {
    var i, n = nodes.length, node;
    if (iterations2 === void 0) iterations2 = 1;
    for (var k = 0; k < iterations2; ++k) {
      alpha += (alphaTarget - alpha) * alphaDecay;
      forces.forEach(function(force) {
        force(alpha);
      });
      for (i = 0; i < n; ++i) {
        node = nodes[i];
        if (node.fx == null) node.x += node.vx *= velocityDecay;
        else node.x = node.fx, node.vx = 0;
        if (node.fy == null) node.y += node.vy *= velocityDecay;
        else node.y = node.fy, node.vy = 0;
      }
    }
    return simulation2;
  }
  function initializeNodes() {
    for (var i = 0, n = nodes.length, node; i < n; ++i) {
      node = nodes[i], node.index = i;
      if (node.fx != null) node.x = node.fx;
      if (node.fy != null) node.y = node.fy;
      if (isNaN(node.x) || isNaN(node.y)) {
        var radius2 = initialRadius * Math.sqrt(0.5 + i), angle2 = i * initialAngle;
        node.x = radius2 * Math.cos(angle2);
        node.y = radius2 * Math.sin(angle2);
      }
      if (isNaN(node.vx) || isNaN(node.vy)) {
        node.vx = node.vy = 0;
      }
    }
  }
  function initializeForce(force) {
    if (force.initialize) force.initialize(nodes, random2);
    return force;
  }
  initializeNodes();
  return simulation2 = {
    tick: tick2,
    restart: function() {
      return stepper.restart(step), simulation2;
    },
    stop: function() {
      return stepper.stop(), simulation2;
    },
    nodes: function(_) {
      return arguments.length ? (nodes = _, initializeNodes(), forces.forEach(initializeForce), simulation2) : nodes;
    },
    alpha: function(_) {
      return arguments.length ? (alpha = +_, simulation2) : alpha;
    },
    alphaMin: function(_) {
      return arguments.length ? (alphaMin = +_, simulation2) : alphaMin;
    },
    alphaDecay: function(_) {
      return arguments.length ? (alphaDecay = +_, simulation2) : +alphaDecay;
    },
    alphaTarget: function(_) {
      return arguments.length ? (alphaTarget = +_, simulation2) : alphaTarget;
    },
    velocityDecay: function(_) {
      return arguments.length ? (velocityDecay = 1 - _, simulation2) : 1 - velocityDecay;
    },
    randomSource: function(_) {
      return arguments.length ? (random2 = _, forces.forEach(initializeForce), simulation2) : random2;
    },
    force: function(name, _) {
      return arguments.length > 1 ? (_ == null ? forces.delete(name) : forces.set(name, initializeForce(_)), simulation2) : forces.get(name);
    },
    find: function(x6, y6, radius2) {
      var i = 0, n = nodes.length, dx, dy, d2, node, closest;
      if (radius2 == null) radius2 = Infinity;
      else radius2 *= radius2;
      for (i = 0; i < n; ++i) {
        node = nodes[i];
        dx = x6 - node.x;
        dy = y6 - node.y;
        d2 = dx * dx + dy * dy;
        if (d2 < radius2) closest = node, radius2 = d2;
      }
      return closest;
    },
    on: function(name, _) {
      return arguments.length > 1 ? (event2.on(name, _), simulation2) : event2.on(name);
    }
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-force@3.0.0/node_modules/d3-force/src/manyBody.js
function manyBody_default() {
  var nodes, node, random2, alpha, strength = constant_default4(-30), strengths, distanceMin2 = 1, distanceMax2 = Infinity, theta2 = 0.81;
  function force(_) {
    var i, n = nodes.length, tree = quadtree(nodes, x4, y4).visitAfter(accumulate);
    for (alpha = _, i = 0; i < n; ++i) node = nodes[i], tree.visit(apply3);
  }
  function initialize3() {
    if (!nodes) return;
    var i, n = nodes.length, node2;
    strengths = new Array(n);
    for (i = 0; i < n; ++i) node2 = nodes[i], strengths[node2.index] = +strength(node2, i, nodes);
  }
  function accumulate(quad2) {
    var strength2 = 0, q, c3, weight = 0, x6, y6, i;
    if (quad2.length) {
      for (x6 = y6 = i = 0; i < 4; ++i) {
        if ((q = quad2[i]) && (c3 = Math.abs(q.value))) {
          strength2 += q.value, weight += c3, x6 += c3 * q.x, y6 += c3 * q.y;
        }
      }
      quad2.x = x6 / weight;
      quad2.y = y6 / weight;
    } else {
      q = quad2;
      q.x = q.data.x;
      q.y = q.data.y;
      do
        strength2 += strengths[q.data.index];
      while (q = q.next);
    }
    quad2.value = strength2;
  }
  function apply3(quad2, x12, _, x22) {
    if (!quad2.value) return true;
    var x6 = quad2.x - node.x, y6 = quad2.y - node.y, w3 = x22 - x12, l = x6 * x6 + y6 * y6;
    if (w3 * w3 / theta2 < l) {
      if (l < distanceMax2) {
        if (x6 === 0) x6 = jiggle_default(random2), l += x6 * x6;
        if (y6 === 0) y6 = jiggle_default(random2), l += y6 * y6;
        if (l < distanceMin2) l = Math.sqrt(distanceMin2 * l);
        node.vx += x6 * quad2.value * alpha / l;
        node.vy += y6 * quad2.value * alpha / l;
      }
      return true;
    } else if (quad2.length || l >= distanceMax2) return;
    if (quad2.data !== node || quad2.next) {
      if (x6 === 0) x6 = jiggle_default(random2), l += x6 * x6;
      if (y6 === 0) y6 = jiggle_default(random2), l += y6 * y6;
      if (l < distanceMin2) l = Math.sqrt(distanceMin2 * l);
    }
    do
      if (quad2.data !== node) {
        w3 = strengths[quad2.data.index] * alpha / l;
        node.vx += x6 * w3;
        node.vy += y6 * w3;
      }
    while (quad2 = quad2.next);
  }
  force.initialize = function(_nodes, _random) {
    nodes = _nodes;
    random2 = _random;
    initialize3();
  };
  force.strength = function(_) {
    return arguments.length ? (strength = typeof _ === "function" ? _ : constant_default4(+_), initialize3(), force) : strength;
  };
  force.distanceMin = function(_) {
    return arguments.length ? (distanceMin2 = _ * _, force) : Math.sqrt(distanceMin2);
  };
  force.distanceMax = function(_) {
    return arguments.length ? (distanceMax2 = _ * _, force) : Math.sqrt(distanceMax2);
  };
  force.theta = function(_) {
    return arguments.length ? (theta2 = _ * _, force) : Math.sqrt(theta2);
  };
  return force;
}

// ../deepseek-harness/node_modules/.pnpm/d3-force@3.0.0/node_modules/d3-force/src/x.js
function x_default2(x6) {
  var strength = constant_default4(0.1), nodes, strengths, xz;
  if (typeof x6 !== "function") x6 = constant_default4(x6 == null ? 0 : +x6);
  function force(alpha) {
    for (var i = 0, n = nodes.length, node; i < n; ++i) {
      node = nodes[i], node.vx += (xz[i] - node.x) * strengths[i] * alpha;
    }
  }
  function initialize3() {
    if (!nodes) return;
    var i, n = nodes.length;
    strengths = new Array(n);
    xz = new Array(n);
    for (i = 0; i < n; ++i) {
      strengths[i] = isNaN(xz[i] = +x6(nodes[i], i, nodes)) ? 0 : +strength(nodes[i], i, nodes);
    }
  }
  force.initialize = function(_) {
    nodes = _;
    initialize3();
  };
  force.strength = function(_) {
    return arguments.length ? (strength = typeof _ === "function" ? _ : constant_default4(+_), initialize3(), force) : strength;
  };
  force.x = function(_) {
    return arguments.length ? (x6 = typeof _ === "function" ? _ : constant_default4(+_), initialize3(), force) : x6;
  };
  return force;
}

// ../deepseek-harness/node_modules/.pnpm/d3-force@3.0.0/node_modules/d3-force/src/y.js
function y_default2(y6) {
  var strength = constant_default4(0.1), nodes, strengths, yz;
  if (typeof y6 !== "function") y6 = constant_default4(y6 == null ? 0 : +y6);
  function force(alpha) {
    for (var i = 0, n = nodes.length, node; i < n; ++i) {
      node = nodes[i], node.vy += (yz[i] - node.y) * strengths[i] * alpha;
    }
  }
  function initialize3() {
    if (!nodes) return;
    var i, n = nodes.length;
    strengths = new Array(n);
    yz = new Array(n);
    for (i = 0; i < n; ++i) {
      strengths[i] = isNaN(yz[i] = +y6(nodes[i], i, nodes)) ? 0 : +strength(nodes[i], i, nodes);
    }
  }
  force.initialize = function(_) {
    nodes = _;
    initialize3();
  };
  force.strength = function(_) {
    return arguments.length ? (strength = typeof _ === "function" ? _ : constant_default4(+_), initialize3(), force) : strength;
  };
  force.y = function(_) {
    return arguments.length ? (y6 = typeof _ === "function" ? _ : constant_default4(+_), initialize3(), force) : y6;
  };
  return force;
}

// ../deepseek-harness/node_modules/.pnpm/vega-force@5.1.3/node_modules/vega-force/build/vega-force.js
var ForceMap = {
  center: center_default,
  collide: collide_default,
  nbody: manyBody_default,
  link: link_default,
  x: x_default2,
  y: y_default2
};
var Forces = "forces";
var ForceParams = ["alpha", "alphaMin", "alphaTarget", "velocityDecay", "forces"];
var ForceConfig = ["static", "iterations"];
var ForceOutput = ["x", "y", "vx", "vy"];
function Force(params2) {
  Transform.call(this, null, params2);
}
Force.Definition = {
  "type": "Force",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "static",
    "type": "boolean",
    "default": false
  }, {
    "name": "restart",
    "type": "boolean",
    "default": false
  }, {
    "name": "iterations",
    "type": "number",
    "default": 300
  }, {
    "name": "alpha",
    "type": "number",
    "default": 1
  }, {
    "name": "alphaMin",
    "type": "number",
    "default": 1e-3
  }, {
    "name": "alphaTarget",
    "type": "number",
    "default": 0
  }, {
    "name": "velocityDecay",
    "type": "number",
    "default": 0.4
  }, {
    "name": "forces",
    "type": "param",
    "array": true,
    "params": [{
      "key": {
        "force": "center"
      },
      "params": [{
        "name": "x",
        "type": "number",
        "default": 0
      }, {
        "name": "y",
        "type": "number",
        "default": 0
      }]
    }, {
      "key": {
        "force": "collide"
      },
      "params": [{
        "name": "radius",
        "type": "number",
        "expr": true
      }, {
        "name": "strength",
        "type": "number",
        "default": 0.7
      }, {
        "name": "iterations",
        "type": "number",
        "default": 1
      }]
    }, {
      "key": {
        "force": "nbody"
      },
      "params": [{
        "name": "strength",
        "type": "number",
        "default": -30,
        "expr": true
      }, {
        "name": "theta",
        "type": "number",
        "default": 0.9
      }, {
        "name": "distanceMin",
        "type": "number",
        "default": 1
      }, {
        "name": "distanceMax",
        "type": "number"
      }]
    }, {
      "key": {
        "force": "link"
      },
      "params": [{
        "name": "links",
        "type": "data"
      }, {
        "name": "id",
        "type": "field"
      }, {
        "name": "distance",
        "type": "number",
        "default": 30,
        "expr": true
      }, {
        "name": "strength",
        "type": "number",
        "expr": true
      }, {
        "name": "iterations",
        "type": "number",
        "default": 1
      }]
    }, {
      "key": {
        "force": "x"
      },
      "params": [{
        "name": "strength",
        "type": "number",
        "default": 0.1
      }, {
        "name": "x",
        "type": "field"
      }]
    }, {
      "key": {
        "force": "y"
      },
      "params": [{
        "name": "strength",
        "type": "number",
        "default": 0.1
      }, {
        "name": "y",
        "type": "field"
      }]
    }]
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "modify": false,
    "default": ForceOutput
  }]
};
inherits_default(Force, Transform, {
  transform(_, pulse2) {
    var sim = this.value, change2 = pulse2.changed(pulse2.ADD_REM), params2 = _.modified(ForceParams), iters = _.iterations || 300;
    if (!sim) {
      this.value = sim = simulation(pulse2.source, _);
      sim.on("tick", rerun(pulse2.dataflow, this));
      if (!_.static) {
        change2 = true;
        sim.tick();
      }
      pulse2.modifies("index");
    } else {
      if (change2) {
        pulse2.modifies("index");
        sim.nodes(pulse2.source);
      }
      if (params2 || pulse2.changed(pulse2.MOD)) {
        setup(sim, _, 0, pulse2);
      }
    }
    if (params2 || change2 || _.modified(ForceConfig) || pulse2.changed() && _.restart) {
      sim.alpha(Math.max(sim.alpha(), _.alpha || 1)).alphaDecay(1 - Math.pow(sim.alphaMin(), 1 / iters));
      if (_.static) {
        for (sim.stop(); --iters >= 0; ) sim.tick();
      } else {
        if (sim.stopped()) sim.restart();
        if (!change2) return pulse2.StopPropagation;
      }
    }
    return this.finish(_, pulse2);
  },
  finish(_, pulse2) {
    const dataflow = pulse2.dataflow;
    for (let args = this._argops, j = 0, m3 = args.length, arg; j < m3; ++j) {
      arg = args[j];
      if (arg.name !== Forces || arg.op._argval.force !== "link") {
        continue;
      }
      for (var ops2 = arg.op._argops, i = 0, n = ops2.length, op; i < n; ++i) {
        if (ops2[i].name === "links" && (op = ops2[i].op.source)) {
          dataflow.pulse(op, dataflow.changeset().reflow());
          break;
        }
      }
    }
    return pulse2.reflow(_.modified()).modifies(ForceOutput);
  }
});
function rerun(df, op) {
  return () => df.touch(op).run();
}
function simulation(nodes, _) {
  const sim = simulation_default(nodes), stop2 = sim.stop, restart = sim.restart;
  let stopped = false;
  sim.stopped = () => stopped;
  sim.restart = () => (stopped = false, restart());
  sim.stop = () => (stopped = true, stop2());
  return setup(sim, _, true).on("end", () => stopped = true);
}
function setup(sim, _, init2, pulse2) {
  var f = array_default(_.forces), i, n, p, name;
  for (i = 0, n = ForceParams.length; i < n; ++i) {
    p = ForceParams[i];
    if (p !== Forces && _.modified(p)) sim[p](_[p]);
  }
  for (i = 0, n = f.length; i < n; ++i) {
    name = Forces + i;
    p = init2 || _.modified(Forces, i) ? getForce(f[i]) : pulse2 && modified(f[i], pulse2) ? sim.force(name) : null;
    if (p) sim.force(name, p);
  }
  for (n = sim.numForces || 0; i < n; ++i) {
    sim.force(Forces + i, null);
  }
  sim.numForces = f.length;
  return sim;
}
function modified(f, pulse2) {
  var k, v2;
  for (k in f) {
    if (isFunction(v2 = f[k]) && pulse2.modified(accessorFields(v2))) return 1;
  }
  return 0;
}
function getForce(_) {
  var f, p;
  if (!hasOwnProperty(ForceMap, _.force)) {
    error("Unrecognized force: " + _.force);
  }
  f = ForceMap[_.force]();
  for (p in _) {
    if (isFunction(f[p])) setForceParam(f[p], _[p], _);
  }
  return f;
}
function setForceParam(f, v2, _) {
  f(isFunction(v2) ? (d) => v2(d, _) : v2);
}

// ../deepseek-harness/node_modules/.pnpm/vega-hierarchy@5.1.3/node_modules/vega-hierarchy/build/vega-hierarchy.js
var vega_hierarchy_exports = {};
__export(vega_hierarchy_exports, {
  nest: () => Nest,
  pack: () => Pack,
  partition: () => Partition,
  stratify: () => Stratify,
  tree: () => Tree,
  treelinks: () => TreeLinks,
  treemap: () => Treemap
});

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/cluster.js
function defaultSeparation(a3, b10) {
  return a3.parent === b10.parent ? 1 : 2;
}
function meanX(children4) {
  return children4.reduce(meanXReduce, 0) / children4.length;
}
function meanXReduce(x6, c3) {
  return x6 + c3.x;
}
function maxY(children4) {
  return 1 + children4.reduce(maxYReduce, 0);
}
function maxYReduce(y6, c3) {
  return Math.max(y6, c3.y);
}
function leafLeft(node) {
  var children4;
  while (children4 = node.children) node = children4[0];
  return node;
}
function leafRight(node) {
  var children4;
  while (children4 = node.children) node = children4[children4.length - 1];
  return node;
}
function cluster_default() {
  var separation = defaultSeparation, dx = 1, dy = 1, nodeSize = false;
  function cluster(root) {
    var previousNode, x6 = 0;
    root.eachAfter(function(node) {
      var children4 = node.children;
      if (children4) {
        node.x = meanX(children4);
        node.y = maxY(children4);
      } else {
        node.x = previousNode ? x6 += separation(node, previousNode) : 0;
        node.y = 0;
        previousNode = node;
      }
    });
    var left = leafLeft(root), right = leafRight(root), x06 = left.x - separation(left, right) / 2, x12 = right.x + separation(right, left) / 2;
    return root.eachAfter(nodeSize ? function(node) {
      node.x = (node.x - root.x) * dx;
      node.y = (root.y - node.y) * dy;
    } : function(node) {
      node.x = (node.x - x06) / (x12 - x06) * dx;
      node.y = (1 - (root.y ? node.y / root.y : 1)) * dy;
    });
  }
  cluster.separation = function(x6) {
    return arguments.length ? (separation = x6, cluster) : separation;
  };
  cluster.size = function(x6) {
    return arguments.length ? (nodeSize = false, dx = +x6[0], dy = +x6[1], cluster) : nodeSize ? null : [dx, dy];
  };
  cluster.nodeSize = function(x6) {
    return arguments.length ? (nodeSize = true, dx = +x6[0], dy = +x6[1], cluster) : nodeSize ? [dx, dy] : null;
  };
  return cluster;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/count.js
function count(node) {
  var sum3 = 0, children4 = node.children, i = children4 && children4.length;
  if (!i) sum3 = 1;
  else while (--i >= 0) sum3 += children4[i].value;
  node.value = sum3;
}
function count_default() {
  return this.eachAfter(count);
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/each.js
function each_default(callback, that) {
  let index4 = -1;
  for (const node of this) {
    callback.call(that, node, ++index4, this);
  }
  return this;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/eachBefore.js
function eachBefore_default(callback, that) {
  var node = this, nodes = [node], children4, i, index4 = -1;
  while (node = nodes.pop()) {
    callback.call(that, node, ++index4, this);
    if (children4 = node.children) {
      for (i = children4.length - 1; i >= 0; --i) {
        nodes.push(children4[i]);
      }
    }
  }
  return this;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/eachAfter.js
function eachAfter_default(callback, that) {
  var node = this, nodes = [node], next = [], children4, i, n, index4 = -1;
  while (node = nodes.pop()) {
    next.push(node);
    if (children4 = node.children) {
      for (i = 0, n = children4.length; i < n; ++i) {
        nodes.push(children4[i]);
      }
    }
  }
  while (node = next.pop()) {
    callback.call(that, node, ++index4, this);
  }
  return this;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/find.js
function find_default2(callback, that) {
  let index4 = -1;
  for (const node of this) {
    if (callback.call(that, node, ++index4, this)) {
      return node;
    }
  }
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/sum.js
function sum_default(value4) {
  return this.eachAfter(function(node) {
    var sum3 = +value4(node.data) || 0, children4 = node.children, i = children4 && children4.length;
    while (--i >= 0) sum3 += children4[i].value;
    node.value = sum3;
  });
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/sort.js
function sort_default(compare4) {
  return this.eachBefore(function(node) {
    if (node.children) {
      node.children.sort(compare4);
    }
  });
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/path.js
function path_default2(end) {
  var start = this, ancestor = leastCommonAncestor(start, end), nodes = [start];
  while (start !== ancestor) {
    start = start.parent;
    nodes.push(start);
  }
  var k = nodes.length;
  while (end !== ancestor) {
    nodes.splice(k, 0, end);
    end = end.parent;
  }
  return nodes;
}
function leastCommonAncestor(a3, b10) {
  if (a3 === b10) return a3;
  var aNodes = a3.ancestors(), bNodes = b10.ancestors(), c3 = null;
  a3 = aNodes.pop();
  b10 = bNodes.pop();
  while (a3 === b10) {
    c3 = a3;
    a3 = aNodes.pop();
    b10 = bNodes.pop();
  }
  return c3;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/ancestors.js
function ancestors_default() {
  var node = this, nodes = [node];
  while (node = node.parent) {
    nodes.push(node);
  }
  return nodes;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/descendants.js
function descendants_default() {
  return Array.from(this);
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/leaves.js
function leaves_default() {
  var leaves = [];
  this.eachBefore(function(node) {
    if (!node.children) {
      leaves.push(node);
    }
  });
  return leaves;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/links.js
function links_default() {
  var root = this, links = [];
  root.each(function(node) {
    if (node !== root) {
      links.push({ source: node.parent, target: node });
    }
  });
  return links;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/iterator.js
function* iterator_default() {
  var node = this, current2, next = [node], children4, i, n;
  do {
    current2 = next.reverse(), next = [];
    while (node = current2.pop()) {
      yield node;
      if (children4 = node.children) {
        for (i = 0, n = children4.length; i < n; ++i) {
          next.push(children4[i]);
        }
      }
    }
  } while (next.length);
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/hierarchy/index.js
function hierarchy(data4, children4) {
  if (data4 instanceof Map) {
    data4 = [void 0, data4];
    if (children4 === void 0) children4 = mapChildren;
  } else if (children4 === void 0) {
    children4 = objectChildren;
  }
  var root = new Node(data4), node, nodes = [root], child, childs, i, n;
  while (node = nodes.pop()) {
    if ((childs = children4(node.data)) && (n = (childs = Array.from(childs)).length)) {
      node.children = childs;
      for (i = n - 1; i >= 0; --i) {
        nodes.push(child = childs[i] = new Node(childs[i]));
        child.parent = node;
        child.depth = node.depth + 1;
      }
    }
  }
  return root.eachBefore(computeHeight);
}
function node_copy() {
  return hierarchy(this).eachBefore(copyData);
}
function objectChildren(d) {
  return d.children;
}
function mapChildren(d) {
  return Array.isArray(d) ? d[1] : null;
}
function copyData(node) {
  if (node.data.value !== void 0) node.value = node.data.value;
  node.data = node.data.data;
}
function computeHeight(node) {
  var height2 = 0;
  do
    node.height = height2;
  while ((node = node.parent) && node.height < ++height2);
}
function Node(data4) {
  this.data = data4;
  this.depth = this.height = 0;
  this.parent = null;
}
Node.prototype = hierarchy.prototype = {
  constructor: Node,
  count: count_default,
  each: each_default,
  eachAfter: eachAfter_default,
  eachBefore: eachBefore_default,
  find: find_default2,
  sum: sum_default,
  sort: sort_default,
  path: path_default2,
  ancestors: ancestors_default,
  descendants: descendants_default,
  leaves: leaves_default,
  links: links_default,
  copy: node_copy,
  [Symbol.iterator]: iterator_default
};

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/accessors.js
function optional(f) {
  return f == null ? null : required(f);
}
function required(f) {
  if (typeof f !== "function") throw new Error();
  return f;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/constant.js
function constantZero() {
  return 0;
}
function constant_default5(x6) {
  return function() {
    return x6;
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/lcg.js
var a2 = 1664525;
var c2 = 1013904223;
var m2 = 4294967296;
function lcg_default2() {
  let s = 1;
  return () => (s = (a2 * s + c2) % m2) / m2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/array.js
function array_default4(x6) {
  return typeof x6 === "object" && "length" in x6 ? x6 : Array.from(x6);
}
function shuffle(array4, random2) {
  let m3 = array4.length, t, i;
  while (m3) {
    i = random2() * m3-- | 0;
    t = array4[m3];
    array4[m3] = array4[i];
    array4[i] = t;
  }
  return array4;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/pack/enclose.js
function packEncloseRandom(circles, random2) {
  var i = 0, n = (circles = shuffle(Array.from(circles), random2)).length, B3 = [], p, e;
  while (i < n) {
    p = circles[i];
    if (e && enclosesWeak(e, p)) ++i;
    else e = encloseBasis(B3 = extendBasis(B3, p)), i = 0;
  }
  return e;
}
function extendBasis(B3, p) {
  var i, j;
  if (enclosesWeakAll(p, B3)) return [p];
  for (i = 0; i < B3.length; ++i) {
    if (enclosesNot(p, B3[i]) && enclosesWeakAll(encloseBasis2(B3[i], p), B3)) {
      return [B3[i], p];
    }
  }
  for (i = 0; i < B3.length - 1; ++i) {
    for (j = i + 1; j < B3.length; ++j) {
      if (enclosesNot(encloseBasis2(B3[i], B3[j]), p) && enclosesNot(encloseBasis2(B3[i], p), B3[j]) && enclosesNot(encloseBasis2(B3[j], p), B3[i]) && enclosesWeakAll(encloseBasis3(B3[i], B3[j], p), B3)) {
        return [B3[i], B3[j], p];
      }
    }
  }
  throw new Error();
}
function enclosesNot(a3, b10) {
  var dr = a3.r - b10.r, dx = b10.x - a3.x, dy = b10.y - a3.y;
  return dr < 0 || dr * dr < dx * dx + dy * dy;
}
function enclosesWeak(a3, b10) {
  var dr = a3.r - b10.r + Math.max(a3.r, b10.r, 1) * 1e-9, dx = b10.x - a3.x, dy = b10.y - a3.y;
  return dr > 0 && dr * dr > dx * dx + dy * dy;
}
function enclosesWeakAll(a3, B3) {
  for (var i = 0; i < B3.length; ++i) {
    if (!enclosesWeak(a3, B3[i])) {
      return false;
    }
  }
  return true;
}
function encloseBasis(B3) {
  switch (B3.length) {
    case 1:
      return encloseBasis1(B3[0]);
    case 2:
      return encloseBasis2(B3[0], B3[1]);
    case 3:
      return encloseBasis3(B3[0], B3[1], B3[2]);
  }
}
function encloseBasis1(a3) {
  return {
    x: a3.x,
    y: a3.y,
    r: a3.r
  };
}
function encloseBasis2(a3, b10) {
  var x12 = a3.x, y12 = a3.y, r1 = a3.r, x22 = b10.x, y22 = b10.y, r2 = b10.r, x21 = x22 - x12, y21 = y22 - y12, r21 = r2 - r1, l = Math.sqrt(x21 * x21 + y21 * y21);
  return {
    x: (x12 + x22 + x21 / l * r21) / 2,
    y: (y12 + y22 + y21 / l * r21) / 2,
    r: (l + r1 + r2) / 2
  };
}
function encloseBasis3(a3, b10, c3) {
  var x12 = a3.x, y12 = a3.y, r1 = a3.r, x22 = b10.x, y22 = b10.y, r2 = b10.r, x32 = c3.x, y32 = c3.y, r3 = c3.r, a22 = x12 - x22, a32 = x12 - x32, b23 = y12 - y22, b32 = y12 - y32, c22 = r2 - r1, c32 = r3 - r1, d1 = x12 * x12 + y12 * y12 - r1 * r1, d2 = d1 - x22 * x22 - y22 * y22 + r2 * r2, d3 = d1 - x32 * x32 - y32 * y32 + r3 * r3, ab4 = a32 * b23 - a22 * b32, xa = (b23 * d3 - b32 * d2) / (ab4 * 2) - x12, xb = (b32 * c22 - b23 * c32) / ab4, ya = (a32 * d2 - a22 * d3) / (ab4 * 2) - y12, yb = (a22 * c32 - a32 * c22) / ab4, A5 = xb * xb + yb * yb - 1, B3 = 2 * (r1 + xa * xb + ya * yb), C4 = xa * xa + ya * ya - r1 * r1, r = -(Math.abs(A5) > 1e-6 ? (B3 + Math.sqrt(B3 * B3 - 4 * A5 * C4)) / (2 * A5) : C4 / B3);
  return {
    x: x12 + xa + xb * r,
    y: y12 + ya + yb * r,
    r
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/pack/siblings.js
function place(b10, a3, c3) {
  var dx = b10.x - a3.x, x6, a22, dy = b10.y - a3.y, y6, b23, d2 = dx * dx + dy * dy;
  if (d2) {
    a22 = a3.r + c3.r, a22 *= a22;
    b23 = b10.r + c3.r, b23 *= b23;
    if (a22 > b23) {
      x6 = (d2 + b23 - a22) / (2 * d2);
      y6 = Math.sqrt(Math.max(0, b23 / d2 - x6 * x6));
      c3.x = b10.x - x6 * dx - y6 * dy;
      c3.y = b10.y - x6 * dy + y6 * dx;
    } else {
      x6 = (d2 + a22 - b23) / (2 * d2);
      y6 = Math.sqrt(Math.max(0, a22 / d2 - x6 * x6));
      c3.x = a3.x + x6 * dx - y6 * dy;
      c3.y = a3.y + x6 * dy + y6 * dx;
    }
  } else {
    c3.x = a3.x + c3.r;
    c3.y = a3.y;
  }
}
function intersects(a3, b10) {
  var dr = a3.r + b10.r - 1e-6, dx = b10.x - a3.x, dy = b10.y - a3.y;
  return dr > 0 && dr * dr > dx * dx + dy * dy;
}
function score(node) {
  var a3 = node._, b10 = node.next._, ab4 = a3.r + b10.r, dx = (a3.x * b10.r + b10.x * a3.r) / ab4, dy = (a3.y * b10.r + b10.y * a3.r) / ab4;
  return dx * dx + dy * dy;
}
function Node2(circle2) {
  this._ = circle2;
  this.next = null;
  this.previous = null;
}
function packSiblingsRandom(circles, random2) {
  if (!(n = (circles = array_default4(circles)).length)) return 0;
  var a3, b10, c3, n, aa2, ca3, i, j, k, sj, sk;
  a3 = circles[0], a3.x = 0, a3.y = 0;
  if (!(n > 1)) return a3.r;
  b10 = circles[1], a3.x = -b10.r, b10.x = a3.r, b10.y = 0;
  if (!(n > 2)) return a3.r + b10.r;
  place(b10, a3, c3 = circles[2]);
  a3 = new Node2(a3), b10 = new Node2(b10), c3 = new Node2(c3);
  a3.next = c3.previous = b10;
  b10.next = a3.previous = c3;
  c3.next = b10.previous = a3;
  pack: for (i = 3; i < n; ++i) {
    place(a3._, b10._, c3 = circles[i]), c3 = new Node2(c3);
    j = b10.next, k = a3.previous, sj = b10._.r, sk = a3._.r;
    do {
      if (sj <= sk) {
        if (intersects(j._, c3._)) {
          b10 = j, a3.next = b10, b10.previous = a3, --i;
          continue pack;
        }
        sj += j._.r, j = j.next;
      } else {
        if (intersects(k._, c3._)) {
          a3 = k, a3.next = b10, b10.previous = a3, --i;
          continue pack;
        }
        sk += k._.r, k = k.previous;
      }
    } while (j !== k.next);
    c3.previous = a3, c3.next = b10, a3.next = b10.previous = b10 = c3;
    aa2 = score(a3);
    while ((c3 = c3.next) !== b10) {
      if ((ca3 = score(c3)) < aa2) {
        a3 = c3, aa2 = ca3;
      }
    }
    b10 = a3.next;
  }
  a3 = [b10._], c3 = b10;
  while ((c3 = c3.next) !== b10) a3.push(c3._);
  c3 = packEncloseRandom(a3, random2);
  for (i = 0; i < n; ++i) a3 = circles[i], a3.x -= c3.x, a3.y -= c3.y;
  return c3.r;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/pack/index.js
function defaultRadius(d) {
  return Math.sqrt(d.value);
}
function pack_default() {
  var radius2 = null, dx = 1, dy = 1, padding3 = constantZero;
  function pack(root) {
    const random2 = lcg_default2();
    root.x = dx / 2, root.y = dy / 2;
    if (radius2) {
      root.eachBefore(radiusLeaf(radius2)).eachAfter(packChildrenRandom(padding3, 0.5, random2)).eachBefore(translateChild(1));
    } else {
      root.eachBefore(radiusLeaf(defaultRadius)).eachAfter(packChildrenRandom(constantZero, 1, random2)).eachAfter(packChildrenRandom(padding3, root.r / Math.min(dx, dy), random2)).eachBefore(translateChild(Math.min(dx, dy) / (2 * root.r)));
    }
    return root;
  }
  pack.radius = function(x6) {
    return arguments.length ? (radius2 = optional(x6), pack) : radius2;
  };
  pack.size = function(x6) {
    return arguments.length ? (dx = +x6[0], dy = +x6[1], pack) : [dx, dy];
  };
  pack.padding = function(x6) {
    return arguments.length ? (padding3 = typeof x6 === "function" ? x6 : constant_default5(+x6), pack) : padding3;
  };
  return pack;
}
function radiusLeaf(radius2) {
  return function(node) {
    if (!node.children) {
      node.r = Math.max(0, +radius2(node) || 0);
    }
  };
}
function packChildrenRandom(padding3, k, random2) {
  return function(node) {
    if (children4 = node.children) {
      var children4, i, n = children4.length, r = padding3(node) * k || 0, e;
      if (r) for (i = 0; i < n; ++i) children4[i].r += r;
      e = packSiblingsRandom(children4, random2);
      if (r) for (i = 0; i < n; ++i) children4[i].r -= r;
      node.r = e + r;
    }
  };
}
function translateChild(k) {
  return function(node) {
    var parent = node.parent;
    node.r *= k;
    if (parent) {
      node.x = parent.x + k * node.x;
      node.y = parent.y + k * node.y;
    }
  };
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/treemap/round.js
function round_default2(node) {
  node.x0 = Math.round(node.x0);
  node.y0 = Math.round(node.y0);
  node.x1 = Math.round(node.x1);
  node.y1 = Math.round(node.y1);
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/treemap/dice.js
function dice_default(parent, x06, y06, x12, y12) {
  var nodes = parent.children, node, i = -1, n = nodes.length, k = parent.value && (x12 - x06) / parent.value;
  while (++i < n) {
    node = nodes[i], node.y0 = y06, node.y1 = y12;
    node.x0 = x06, node.x1 = x06 += node.value * k;
  }
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/partition.js
function partition_default() {
  var dx = 1, dy = 1, padding3 = 0, round = false;
  function partition5(root) {
    var n = root.height + 1;
    root.x0 = root.y0 = padding3;
    root.x1 = dx;
    root.y1 = dy / n;
    root.eachBefore(positionNode(dy, n));
    if (round) root.eachBefore(round_default2);
    return root;
  }
  function positionNode(dy2, n) {
    return function(node) {
      if (node.children) {
        dice_default(node, node.x0, dy2 * (node.depth + 1) / n, node.x1, dy2 * (node.depth + 2) / n);
      }
      var x06 = node.x0, y06 = node.y0, x12 = node.x1 - padding3, y12 = node.y1 - padding3;
      if (x12 < x06) x06 = x12 = (x06 + x12) / 2;
      if (y12 < y06) y06 = y12 = (y06 + y12) / 2;
      node.x0 = x06;
      node.y0 = y06;
      node.x1 = x12;
      node.y1 = y12;
    };
  }
  partition5.round = function(x6) {
    return arguments.length ? (round = !!x6, partition5) : round;
  };
  partition5.size = function(x6) {
    return arguments.length ? (dx = +x6[0], dy = +x6[1], partition5) : [dx, dy];
  };
  partition5.padding = function(x6) {
    return arguments.length ? (padding3 = +x6, partition5) : padding3;
  };
  return partition5;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/stratify.js
var preroot = { depth: -1 };
var ambiguous = {};
var imputed = {};
function defaultId(d) {
  return d.id;
}
function defaultParentId(d) {
  return d.parentId;
}
function stratify_default() {
  var id2 = defaultId, parentId = defaultParentId, path4;
  function stratify(data4) {
    var nodes = Array.from(data4), currentId = id2, currentParentId = parentId, n, d, i, root, parent, node, nodeId, nodeKey, nodeByKey = /* @__PURE__ */ new Map();
    if (path4 != null) {
      const I = nodes.map((d2, i2) => normalize2(path4(d2, i2, data4)));
      const P = I.map(parentof);
      const S = new Set(I).add("");
      for (const i2 of P) {
        if (!S.has(i2)) {
          S.add(i2);
          I.push(i2);
          P.push(parentof(i2));
          nodes.push(imputed);
        }
      }
      currentId = (_, i2) => I[i2];
      currentParentId = (_, i2) => P[i2];
    }
    for (i = 0, n = nodes.length; i < n; ++i) {
      d = nodes[i], node = nodes[i] = new Node(d);
      if ((nodeId = currentId(d, i, data4)) != null && (nodeId += "")) {
        nodeKey = node.id = nodeId;
        nodeByKey.set(nodeKey, nodeByKey.has(nodeKey) ? ambiguous : node);
      }
      if ((nodeId = currentParentId(d, i, data4)) != null && (nodeId += "")) {
        node.parent = nodeId;
      }
    }
    for (i = 0; i < n; ++i) {
      node = nodes[i];
      if (nodeId = node.parent) {
        parent = nodeByKey.get(nodeId);
        if (!parent) throw new Error("missing: " + nodeId);
        if (parent === ambiguous) throw new Error("ambiguous: " + nodeId);
        if (parent.children) parent.children.push(node);
        else parent.children = [node];
        node.parent = parent;
      } else {
        if (root) throw new Error("multiple roots");
        root = node;
      }
    }
    if (!root) throw new Error("no root");
    if (path4 != null) {
      while (root.data === imputed && root.children.length === 1) {
        root = root.children[0], --n;
      }
      for (let i2 = nodes.length - 1; i2 >= 0; --i2) {
        node = nodes[i2];
        if (node.data !== imputed) break;
        node.data = null;
      }
    }
    root.parent = preroot;
    root.eachBefore(function(node2) {
      node2.depth = node2.parent.depth + 1;
      --n;
    }).eachBefore(computeHeight);
    root.parent = null;
    if (n > 0) throw new Error("cycle");
    return root;
  }
  stratify.id = function(x6) {
    return arguments.length ? (id2 = optional(x6), stratify) : id2;
  };
  stratify.parentId = function(x6) {
    return arguments.length ? (parentId = optional(x6), stratify) : parentId;
  };
  stratify.path = function(x6) {
    return arguments.length ? (path4 = optional(x6), stratify) : path4;
  };
  return stratify;
}
function normalize2(path4) {
  path4 = `${path4}`;
  let i = path4.length;
  if (slash(path4, i - 1) && !slash(path4, i - 2)) path4 = path4.slice(0, -1);
  return path4[0] === "/" ? path4 : `/${path4}`;
}
function parentof(path4) {
  let i = path4.length;
  if (i < 2) return "";
  while (--i > 1) if (slash(path4, i)) break;
  return path4.slice(0, i);
}
function slash(path4, i) {
  if (path4[i] === "/") {
    let k = 0;
    while (i > 0 && path4[--i] === "\\") ++k;
    if ((k & 1) === 0) return true;
  }
  return false;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/tree.js
function defaultSeparation2(a3, b10) {
  return a3.parent === b10.parent ? 1 : 2;
}
function nextLeft(v2) {
  var children4 = v2.children;
  return children4 ? children4[0] : v2.t;
}
function nextRight(v2) {
  var children4 = v2.children;
  return children4 ? children4[children4.length - 1] : v2.t;
}
function moveSubtree(wm, wp, shift) {
  var change2 = shift / (wp.i - wm.i);
  wp.c -= change2;
  wp.s += shift;
  wm.c += change2;
  wp.z += shift;
  wp.m += shift;
}
function executeShifts(v2) {
  var shift = 0, change2 = 0, children4 = v2.children, i = children4.length, w3;
  while (--i >= 0) {
    w3 = children4[i];
    w3.z += shift;
    w3.m += shift;
    shift += w3.s + (change2 += w3.c);
  }
}
function nextAncestor(vim, v2, ancestor) {
  return vim.a.parent === v2.parent ? vim.a : ancestor;
}
function TreeNode(node, i) {
  this._ = node;
  this.parent = null;
  this.children = null;
  this.A = null;
  this.a = this;
  this.z = 0;
  this.m = 0;
  this.c = 0;
  this.s = 0;
  this.t = null;
  this.i = i;
}
TreeNode.prototype = Object.create(Node.prototype);
function treeRoot(root) {
  var tree = new TreeNode(root, 0), node, nodes = [tree], child, children4, i, n;
  while (node = nodes.pop()) {
    if (children4 = node._.children) {
      node.children = new Array(n = children4.length);
      for (i = n - 1; i >= 0; --i) {
        nodes.push(child = node.children[i] = new TreeNode(children4[i], i));
        child.parent = node;
      }
    }
  }
  (tree.parent = new TreeNode(null, 0)).children = [tree];
  return tree;
}
function tree_default() {
  var separation = defaultSeparation2, dx = 1, dy = 1, nodeSize = null;
  function tree(root) {
    var t = treeRoot(root);
    t.eachAfter(firstWalk), t.parent.m = -t.z;
    t.eachBefore(secondWalk);
    if (nodeSize) root.eachBefore(sizeNode);
    else {
      var left = root, right = root, bottom = root;
      root.eachBefore(function(node) {
        if (node.x < left.x) left = node;
        if (node.x > right.x) right = node;
        if (node.depth > bottom.depth) bottom = node;
      });
      var s = left === right ? 1 : separation(left, right) / 2, tx = s - left.x, kx = dx / (right.x + s + tx), ky = dy / (bottom.depth || 1);
      root.eachBefore(function(node) {
        node.x = (node.x + tx) * kx;
        node.y = node.depth * ky;
      });
    }
    return root;
  }
  function firstWalk(v2) {
    var children4 = v2.children, siblings = v2.parent.children, w3 = v2.i ? siblings[v2.i - 1] : null;
    if (children4) {
      executeShifts(v2);
      var midpoint = (children4[0].z + children4[children4.length - 1].z) / 2;
      if (w3) {
        v2.z = w3.z + separation(v2._, w3._);
        v2.m = v2.z - midpoint;
      } else {
        v2.z = midpoint;
      }
    } else if (w3) {
      v2.z = w3.z + separation(v2._, w3._);
    }
    v2.parent.A = apportion(v2, w3, v2.parent.A || siblings[0]);
  }
  function secondWalk(v2) {
    v2._.x = v2.z + v2.parent.m;
    v2.m += v2.parent.m;
  }
  function apportion(v2, w3, ancestor) {
    if (w3) {
      var vip = v2, vop = v2, vim = w3, vom = vip.parent.children[0], sip = vip.m, sop = vop.m, sim = vim.m, som = vom.m, shift;
      while (vim = nextRight(vim), vip = nextLeft(vip), vim && vip) {
        vom = nextLeft(vom);
        vop = nextRight(vop);
        vop.a = v2;
        shift = vim.z + sim - vip.z - sip + separation(vim._, vip._);
        if (shift > 0) {
          moveSubtree(nextAncestor(vim, v2, ancestor), v2, shift);
          sip += shift;
          sop += shift;
        }
        sim += vim.m;
        sip += vip.m;
        som += vom.m;
        sop += vop.m;
      }
      if (vim && !nextRight(vop)) {
        vop.t = vim;
        vop.m += sim - sop;
      }
      if (vip && !nextLeft(vom)) {
        vom.t = vip;
        vom.m += sip - som;
        ancestor = v2;
      }
    }
    return ancestor;
  }
  function sizeNode(node) {
    node.x *= dx;
    node.y = node.depth * dy;
  }
  tree.separation = function(x6) {
    return arguments.length ? (separation = x6, tree) : separation;
  };
  tree.size = function(x6) {
    return arguments.length ? (nodeSize = false, dx = +x6[0], dy = +x6[1], tree) : nodeSize ? null : [dx, dy];
  };
  tree.nodeSize = function(x6) {
    return arguments.length ? (nodeSize = true, dx = +x6[0], dy = +x6[1], tree) : nodeSize ? [dx, dy] : null;
  };
  return tree;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/treemap/slice.js
function slice_default(parent, x06, y06, x12, y12) {
  var nodes = parent.children, node, i = -1, n = nodes.length, k = parent.value && (y12 - y06) / parent.value;
  while (++i < n) {
    node = nodes[i], node.x0 = x06, node.x1 = x12;
    node.y0 = y06, node.y1 = y06 += node.value * k;
  }
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/treemap/squarify.js
var phi = (1 + Math.sqrt(5)) / 2;
function squarifyRatio(ratio, parent, x06, y06, x12, y12) {
  var rows = [], nodes = parent.children, row, nodeValue, i0 = 0, i1 = 0, n = nodes.length, dx, dy, value4 = parent.value, sumValue, minValue, maxValue, newRatio, minRatio, alpha, beta;
  while (i0 < n) {
    dx = x12 - x06, dy = y12 - y06;
    do
      sumValue = nodes[i1++].value;
    while (!sumValue && i1 < n);
    minValue = maxValue = sumValue;
    alpha = Math.max(dy / dx, dx / dy) / (value4 * ratio);
    beta = sumValue * sumValue * alpha;
    minRatio = Math.max(maxValue / beta, beta / minValue);
    for (; i1 < n; ++i1) {
      sumValue += nodeValue = nodes[i1].value;
      if (nodeValue < minValue) minValue = nodeValue;
      if (nodeValue > maxValue) maxValue = nodeValue;
      beta = sumValue * sumValue * alpha;
      newRatio = Math.max(maxValue / beta, beta / minValue);
      if (newRatio > minRatio) {
        sumValue -= nodeValue;
        break;
      }
      minRatio = newRatio;
    }
    rows.push(row = { value: sumValue, dice: dx < dy, children: nodes.slice(i0, i1) });
    if (row.dice) dice_default(row, x06, y06, x12, value4 ? y06 += dy * sumValue / value4 : y12);
    else slice_default(row, x06, y06, value4 ? x06 += dx * sumValue / value4 : x12, y12);
    value4 -= sumValue, i0 = i1;
  }
  return rows;
}
var squarify_default = (function custom9(ratio) {
  function squarify(parent, x06, y06, x12, y12) {
    squarifyRatio(ratio, parent, x06, y06, x12, y12);
  }
  squarify.ratio = function(x6) {
    return custom9((x6 = +x6) > 1 ? x6 : 1);
  };
  return squarify;
})(phi);

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/treemap/index.js
function treemap_default() {
  var tile = squarify_default, round = false, dx = 1, dy = 1, paddingStack = [0], paddingInner2 = constantZero, paddingTop = constantZero, paddingRight = constantZero, paddingBottom = constantZero, paddingLeft = constantZero;
  function treemap(root) {
    root.x0 = root.y0 = 0;
    root.x1 = dx;
    root.y1 = dy;
    root.eachBefore(positionNode);
    paddingStack = [0];
    if (round) root.eachBefore(round_default2);
    return root;
  }
  function positionNode(node) {
    var p = paddingStack[node.depth], x06 = node.x0 + p, y06 = node.y0 + p, x12 = node.x1 - p, y12 = node.y1 - p;
    if (x12 < x06) x06 = x12 = (x06 + x12) / 2;
    if (y12 < y06) y06 = y12 = (y06 + y12) / 2;
    node.x0 = x06;
    node.y0 = y06;
    node.x1 = x12;
    node.y1 = y12;
    if (node.children) {
      p = paddingStack[node.depth + 1] = paddingInner2(node) / 2;
      x06 += paddingLeft(node) - p;
      y06 += paddingTop(node) - p;
      x12 -= paddingRight(node) - p;
      y12 -= paddingBottom(node) - p;
      if (x12 < x06) x06 = x12 = (x06 + x12) / 2;
      if (y12 < y06) y06 = y12 = (y06 + y12) / 2;
      tile(node, x06, y06, x12, y12);
    }
  }
  treemap.round = function(x6) {
    return arguments.length ? (round = !!x6, treemap) : round;
  };
  treemap.size = function(x6) {
    return arguments.length ? (dx = +x6[0], dy = +x6[1], treemap) : [dx, dy];
  };
  treemap.tile = function(x6) {
    return arguments.length ? (tile = required(x6), treemap) : tile;
  };
  treemap.padding = function(x6) {
    return arguments.length ? treemap.paddingInner(x6).paddingOuter(x6) : treemap.paddingInner();
  };
  treemap.paddingInner = function(x6) {
    return arguments.length ? (paddingInner2 = typeof x6 === "function" ? x6 : constant_default5(+x6), treemap) : paddingInner2;
  };
  treemap.paddingOuter = function(x6) {
    return arguments.length ? treemap.paddingTop(x6).paddingRight(x6).paddingBottom(x6).paddingLeft(x6) : treemap.paddingTop();
  };
  treemap.paddingTop = function(x6) {
    return arguments.length ? (paddingTop = typeof x6 === "function" ? x6 : constant_default5(+x6), treemap) : paddingTop;
  };
  treemap.paddingRight = function(x6) {
    return arguments.length ? (paddingRight = typeof x6 === "function" ? x6 : constant_default5(+x6), treemap) : paddingRight;
  };
  treemap.paddingBottom = function(x6) {
    return arguments.length ? (paddingBottom = typeof x6 === "function" ? x6 : constant_default5(+x6), treemap) : paddingBottom;
  };
  treemap.paddingLeft = function(x6) {
    return arguments.length ? (paddingLeft = typeof x6 === "function" ? x6 : constant_default5(+x6), treemap) : paddingLeft;
  };
  return treemap;
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/treemap/binary.js
function binary_default(parent, x06, y06, x12, y12) {
  var nodes = parent.children, i, n = nodes.length, sum3, sums = new Array(n + 1);
  for (sums[0] = sum3 = i = 0; i < n; ++i) {
    sums[i + 1] = sum3 += nodes[i].value;
  }
  partition5(0, n, parent.value, x06, y06, x12, y12);
  function partition5(i2, j, value4, x07, y07, x13, y13) {
    if (i2 >= j - 1) {
      var node = nodes[i2];
      node.x0 = x07, node.y0 = y07;
      node.x1 = x13, node.y1 = y13;
      return;
    }
    var valueOffset = sums[i2], valueTarget = value4 / 2 + valueOffset, k = i2 + 1, hi = j - 1;
    while (k < hi) {
      var mid = k + hi >>> 1;
      if (sums[mid] < valueTarget) k = mid + 1;
      else hi = mid;
    }
    if (valueTarget - sums[k - 1] < sums[k] - valueTarget && i2 + 1 < k) --k;
    var valueLeft = sums[k] - valueOffset, valueRight = value4 - valueLeft;
    if (x13 - x07 > y13 - y07) {
      var xk = value4 ? (x07 * valueRight + x13 * valueLeft) / value4 : x13;
      partition5(i2, k, valueLeft, x07, y07, xk, y13);
      partition5(k, j, valueRight, xk, y07, x13, y13);
    } else {
      var yk = value4 ? (y07 * valueRight + y13 * valueLeft) / value4 : y13;
      partition5(i2, k, valueLeft, x07, y07, x13, yk);
      partition5(k, j, valueRight, x07, yk, x13, y13);
    }
  }
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/treemap/sliceDice.js
function sliceDice_default(parent, x06, y06, x12, y12) {
  (parent.depth & 1 ? slice_default : dice_default)(parent, x06, y06, x12, y12);
}

// ../deepseek-harness/node_modules/.pnpm/d3-hierarchy@3.1.2/node_modules/d3-hierarchy/src/treemap/resquarify.js
var resquarify_default = (function custom10(ratio) {
  function resquarify(parent, x06, y06, x12, y12) {
    if ((rows = parent._squarify) && rows.ratio === ratio) {
      var rows, row, nodes, i, j = -1, n, m3 = rows.length, value4 = parent.value;
      while (++j < m3) {
        row = rows[j], nodes = row.children;
        for (i = row.value = 0, n = nodes.length; i < n; ++i) row.value += nodes[i].value;
        if (row.dice) dice_default(row, x06, y06, x12, value4 ? y06 += (y12 - y06) * row.value / value4 : y12);
        else slice_default(row, x06, y06, value4 ? x06 += (x12 - x06) * row.value / value4 : x12, y12);
        value4 -= row.value;
      }
    } else {
      parent._squarify = rows = squarifyRatio(ratio, parent, x06, y06, x12, y12);
      rows.ratio = ratio;
    }
  }
  resquarify.ratio = function(x6) {
    return custom10((x6 = +x6) > 1 ? x6 : 1);
  };
  return resquarify;
})(phi);

// ../deepseek-harness/node_modules/.pnpm/vega-hierarchy@5.1.3/node_modules/vega-hierarchy/build/vega-hierarchy.js
function lookup3(tree, key2, filter4) {
  const map3 = {};
  tree.each((node) => {
    const t = node.data;
    if (filter4(t)) map3[key2(t)] = node;
  });
  tree.lookup = map3;
  return tree;
}
function Nest(params2) {
  Transform.call(this, null, params2);
}
Nest.Definition = {
  "type": "Nest",
  "metadata": {
    "treesource": true,
    "changes": true
  },
  "params": [{
    "name": "keys",
    "type": "field",
    "array": true
  }, {
    "name": "generate",
    "type": "boolean"
  }]
};
var children = (n) => n.values;
inherits_default(Nest, Transform, {
  transform(_, pulse2) {
    if (!pulse2.source) {
      error("Nest transform requires an upstream data source.");
    }
    var gen = _.generate, mod = _.modified(), out = pulse2.clone(), tree = this.value;
    if (!tree || mod || pulse2.changed()) {
      if (tree) {
        tree.each((node) => {
          if (node.children && isTuple(node.data)) {
            out.rem.push(node.data);
          }
        });
      }
      this.value = tree = hierarchy({
        values: array_default(_.keys).reduce((n, k) => {
          n.key(k);
          return n;
        }, nest()).entries(out.source)
      }, children);
      if (gen) {
        tree.each((node) => {
          if (node.children) {
            node = ingest$1(node.data);
            out.add.push(node);
            out.source.push(node);
          }
        });
      }
      lookup3(tree, tupleid, tupleid);
    }
    out.source.root = tree;
    return out;
  }
});
function nest() {
  const keys4 = [], nest2 = {
    entries: (array4) => entries2(apply3(array4, 0), 0),
    key: (d) => (keys4.push(d), nest2)
  };
  function apply3(array4, depth) {
    if (depth >= keys4.length) {
      return array4;
    }
    const n = array4.length, key2 = keys4[depth++], valuesByKey = {}, result = {};
    let i = -1, keyValue, value4, values4;
    while (++i < n) {
      keyValue = key2(value4 = array4[i]) + "";
      if (values4 = valuesByKey[keyValue]) {
        values4.push(value4);
      } else {
        valuesByKey[keyValue] = [value4];
      }
    }
    for (keyValue in valuesByKey) {
      result[keyValue] = apply3(valuesByKey[keyValue], depth);
    }
    return result;
  }
  function entries2(map3, depth) {
    if (++depth > keys4.length) return map3;
    const array4 = [];
    for (const key2 in map3) {
      array4.push({
        key: key2,
        values: entries2(map3[key2], depth)
      });
    }
    return array4;
  }
  return nest2;
}
function HierarchyLayout(params2) {
  Transform.call(this, null, params2);
}
var defaultSeparation3 = (a3, b10) => a3.parent === b10.parent ? 1 : 2;
inherits_default(HierarchyLayout, Transform, {
  transform(_, pulse2) {
    if (!pulse2.source || !pulse2.source.root) {
      error(this.constructor.name + " transform requires a backing tree data source.");
    }
    const layout = this.layout(_.method), fields = this.fields, root = pulse2.source.root, as = _.as || fields;
    if (_.field) root.sum(_.field);
    else root.count();
    if (_.sort) root.sort(stableCompare(_.sort, (d) => d.data));
    setParams(layout, this.params, _);
    if (layout.separation) {
      layout.separation(_.separation !== false ? defaultSeparation3 : one);
    }
    try {
      this.value = layout(root);
    } catch (err) {
      error(err);
    }
    root.each((node) => setFields(node, fields, as));
    return pulse2.reflow(_.modified()).modifies(as).modifies("leaf");
  }
});
function setParams(layout, params2, _) {
  for (let p, i = 0, n = params2.length; i < n; ++i) {
    p = params2[i];
    if (p in _) layout[p](_[p]);
  }
}
function setFields(node, fields, as) {
  const t = node.data, n = fields.length - 1;
  for (let i = 0; i < n; ++i) {
    t[as[i]] = node[fields[i]];
  }
  t[as[n]] = node.children ? node.children.length : 0;
}
var Output$3 = ["x", "y", "r", "depth", "children"];
function Pack(params2) {
  HierarchyLayout.call(this, params2);
}
Pack.Definition = {
  "type": "Pack",
  "metadata": {
    "tree": true,
    "modifies": true
  },
  "params": [{
    "name": "field",
    "type": "field"
  }, {
    "name": "sort",
    "type": "compare"
  }, {
    "name": "padding",
    "type": "number",
    "default": 0
  }, {
    "name": "radius",
    "type": "field",
    "default": null
  }, {
    "name": "size",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": Output$3.length,
    "default": Output$3
  }]
};
inherits_default(Pack, HierarchyLayout, {
  layout: pack_default,
  params: ["radius", "size", "padding"],
  fields: Output$3
});
var Output$2 = ["x0", "y0", "x1", "y1", "depth", "children"];
function Partition(params2) {
  HierarchyLayout.call(this, params2);
}
Partition.Definition = {
  "type": "Partition",
  "metadata": {
    "tree": true,
    "modifies": true
  },
  "params": [{
    "name": "field",
    "type": "field"
  }, {
    "name": "sort",
    "type": "compare"
  }, {
    "name": "padding",
    "type": "number",
    "default": 0
  }, {
    "name": "round",
    "type": "boolean",
    "default": false
  }, {
    "name": "size",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": Output$2.length,
    "default": Output$2
  }]
};
inherits_default(Partition, HierarchyLayout, {
  layout: partition_default,
  params: ["size", "round", "padding"],
  fields: Output$2
});
function Stratify(params2) {
  Transform.call(this, null, params2);
}
Stratify.Definition = {
  "type": "Stratify",
  "metadata": {
    "treesource": true
  },
  "params": [{
    "name": "key",
    "type": "field",
    "required": true
  }, {
    "name": "parentKey",
    "type": "field",
    "required": true
  }]
};
inherits_default(Stratify, Transform, {
  transform(_, pulse2) {
    if (!pulse2.source) {
      error("Stratify transform requires an upstream data source.");
    }
    let tree = this.value;
    const mod = _.modified(), out = pulse2.fork(pulse2.ALL).materialize(pulse2.SOURCE), run2 = !tree || mod || pulse2.changed(pulse2.ADD_REM) || pulse2.modified(_.key.fields) || pulse2.modified(_.parentKey.fields);
    out.source = out.source.slice();
    if (run2) {
      tree = out.source.length ? lookup3(stratify_default().id(_.key).parentId(_.parentKey)(out.source), _.key, truthy) : lookup3(stratify_default()([{}]), _.key, _.key);
    }
    out.source.root = this.value = tree;
    return out;
  }
});
var Layouts = {
  tidy: tree_default,
  cluster: cluster_default
};
var Output$1 = ["x", "y", "depth", "children"];
function Tree(params2) {
  HierarchyLayout.call(this, params2);
}
Tree.Definition = {
  "type": "Tree",
  "metadata": {
    "tree": true,
    "modifies": true
  },
  "params": [{
    "name": "field",
    "type": "field"
  }, {
    "name": "sort",
    "type": "compare"
  }, {
    "name": "method",
    "type": "enum",
    "default": "tidy",
    "values": ["tidy", "cluster"]
  }, {
    "name": "size",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "nodeSize",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "separation",
    "type": "boolean",
    "default": true
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": Output$1.length,
    "default": Output$1
  }]
};
inherits_default(Tree, HierarchyLayout, {
  /**
   * Tree layout generator. Supports both 'tidy' and 'cluster' layouts.
   */
  layout(method2) {
    const m3 = method2 || "tidy";
    if (hasOwnProperty(Layouts, m3)) return Layouts[m3]();
    else error("Unrecognized Tree layout method: " + m3);
  },
  params: ["size", "nodeSize"],
  fields: Output$1
});
function TreeLinks(params2) {
  Transform.call(this, [], params2);
}
TreeLinks.Definition = {
  "type": "TreeLinks",
  "metadata": {
    "tree": true,
    "generates": true,
    "changes": true
  },
  "params": []
};
inherits_default(TreeLinks, Transform, {
  transform(_, pulse2) {
    const links = this.value, tree = pulse2.source && pulse2.source.root, out = pulse2.fork(pulse2.NO_SOURCE), lut = {};
    if (!tree) error("TreeLinks transform requires a tree data source.");
    if (pulse2.changed(pulse2.ADD_REM)) {
      out.rem = links;
      pulse2.visit(pulse2.SOURCE, (t) => lut[tupleid(t)] = 1);
      tree.each((node) => {
        const t = node.data, p = node.parent && node.parent.data;
        if (p && lut[tupleid(t)] && lut[tupleid(p)]) {
          out.add.push(ingest$1({
            source: p,
            target: t
          }));
        }
      });
      this.value = out.add;
    } else if (pulse2.changed(pulse2.MOD)) {
      pulse2.visit(pulse2.MOD, (t) => lut[tupleid(t)] = 1);
      links.forEach((link2) => {
        if (lut[tupleid(link2.source)] || lut[tupleid(link2.target)]) {
          out.mod.push(link2);
        }
      });
    }
    return out;
  }
});
var Tiles = {
  binary: binary_default,
  dice: dice_default,
  slice: slice_default,
  slicedice: sliceDice_default,
  squarify: squarify_default,
  resquarify: resquarify_default
};
var Output2 = ["x0", "y0", "x1", "y1", "depth", "children"];
function Treemap(params2) {
  HierarchyLayout.call(this, params2);
}
Treemap.Definition = {
  "type": "Treemap",
  "metadata": {
    "tree": true,
    "modifies": true
  },
  "params": [{
    "name": "field",
    "type": "field"
  }, {
    "name": "sort",
    "type": "compare"
  }, {
    "name": "method",
    "type": "enum",
    "default": "squarify",
    "values": ["squarify", "resquarify", "binary", "dice", "slice", "slicedice"]
  }, {
    "name": "padding",
    "type": "number",
    "default": 0
  }, {
    "name": "paddingInner",
    "type": "number",
    "default": 0
  }, {
    "name": "paddingOuter",
    "type": "number",
    "default": 0
  }, {
    "name": "paddingTop",
    "type": "number",
    "default": 0
  }, {
    "name": "paddingRight",
    "type": "number",
    "default": 0
  }, {
    "name": "paddingBottom",
    "type": "number",
    "default": 0
  }, {
    "name": "paddingLeft",
    "type": "number",
    "default": 0
  }, {
    "name": "ratio",
    "type": "number",
    "default": 1.618033988749895
  }, {
    "name": "round",
    "type": "boolean",
    "default": false
  }, {
    "name": "size",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": Output2.length,
    "default": Output2
  }]
};
inherits_default(Treemap, HierarchyLayout, {
  /**
   * Treemap layout generator. Adds 'method' and 'ratio' parameters
   * to configure the underlying tile method.
   */
  layout() {
    const x6 = treemap_default();
    x6.ratio = (_) => {
      const t = x6.tile();
      if (t.ratio) x6.tile(t.ratio(_));
    };
    x6.method = (_) => {
      if (hasOwnProperty(Tiles, _)) x6.tile(Tiles[_]);
      else error("Unrecognized Treemap layout method: " + _);
    };
    return x6;
  },
  params: ["method", "ratio", "size", "round", "padding", "paddingInner", "paddingOuter", "paddingTop", "paddingRight", "paddingBottom", "paddingLeft"],
  fields: Output2
});

// ../deepseek-harness/node_modules/.pnpm/vega-label@2.1.3/node_modules/vega-label/build/vega-label.js
var vega_label_exports = {};
__export(vega_label_exports, {
  label: () => Label
});
var ALPHA_MASK = 4278190080;
function baseBitmaps($, data4) {
  const bitmap = $.bitmap();
  (data4 || []).forEach((d) => bitmap.set($(d.boundary[0]), $(d.boundary[3])));
  return [bitmap, void 0];
}
function markBitmaps($, baseMark, avoidMarks, labelInside, isGroupArea) {
  const width2 = $.width, height2 = $.height, border = labelInside || isGroupArea, context4 = domCanvas(width2, height2).getContext("2d"), baseMarkContext = domCanvas(width2, height2).getContext("2d"), strokeContext = border && domCanvas(width2, height2).getContext("2d");
  avoidMarks.forEach((items) => draw2(context4, items, false));
  draw2(baseMarkContext, baseMark, false);
  if (border) {
    draw2(strokeContext, baseMark, true);
  }
  const buffer = getBuffer(context4, width2, height2), baseMarkBuffer = getBuffer(baseMarkContext, width2, height2), strokeBuffer = border && getBuffer(strokeContext, width2, height2), layer1 = $.bitmap(), layer2 = border && $.bitmap();
  let x6, y6, u4, v2, index4, alpha, strokeAlpha, baseMarkAlpha;
  for (y6 = 0; y6 < height2; ++y6) {
    for (x6 = 0; x6 < width2; ++x6) {
      index4 = y6 * width2 + x6;
      alpha = buffer[index4] & ALPHA_MASK;
      baseMarkAlpha = baseMarkBuffer[index4] & ALPHA_MASK;
      strokeAlpha = border && strokeBuffer[index4] & ALPHA_MASK;
      if (alpha || strokeAlpha || baseMarkAlpha) {
        u4 = $(x6);
        v2 = $(y6);
        if (!isGroupArea && (alpha || baseMarkAlpha)) layer1.set(u4, v2);
        if (border && (alpha || strokeAlpha)) layer2.set(u4, v2);
      }
    }
  }
  return [layer1, layer2];
}
function getBuffer(context4, width2, height2) {
  return new Uint32Array(context4.getImageData(0, 0, width2, height2).data.buffer);
}
function draw2(context4, items, interior) {
  if (!items.length) return;
  const type4 = items[0].mark.marktype;
  if (type4 === "group") {
    items.forEach((group3) => {
      group3.items.forEach((mark) => draw2(context4, mark.items, interior));
    });
  } else {
    Marks[type4].draw(context4, {
      items: interior ? items.map(prepare) : items
    });
  }
}
function prepare(source4) {
  const item = rederive(source4, {});
  if (item.stroke && item.strokeOpacity !== 0 || item.fill && item.fillOpacity !== 0) {
    return {
      ...item,
      strokeOpacity: 1,
      stroke: "#000",
      fillOpacity: 0
    };
  }
  return item;
}
var DIV = 5;
var MOD2 = 31;
var SIZE = 32;
var RIGHT0 = new Uint32Array(SIZE + 1);
var RIGHT1 = new Uint32Array(SIZE + 1);
RIGHT1[0] = 0;
RIGHT0[0] = ~RIGHT1[0];
for (let i = 1; i <= SIZE; ++i) {
  RIGHT1[i] = RIGHT1[i - 1] << 1 | 1;
  RIGHT0[i] = ~RIGHT1[i];
}
function Bitmap(w3, h3) {
  const array4 = new Uint32Array(~~((w3 * h3 + SIZE) / SIZE));
  function _set(index4, mask) {
    array4[index4] |= mask;
  }
  function _clear(index4, mask) {
    array4[index4] &= mask;
  }
  return {
    array: array4,
    get: (x6, y6) => {
      const index4 = y6 * w3 + x6;
      return array4[index4 >>> DIV] & 1 << (index4 & MOD2);
    },
    set: (x6, y6) => {
      const index4 = y6 * w3 + x6;
      _set(index4 >>> DIV, 1 << (index4 & MOD2));
    },
    clear: (x6, y6) => {
      const index4 = y6 * w3 + x6;
      _clear(index4 >>> DIV, ~(1 << (index4 & MOD2)));
    },
    getRange: (x6, y6, x22, y22) => {
      let r = y22, start, end, indexStart, indexEnd;
      for (; r >= y6; --r) {
        start = r * w3 + x6;
        end = r * w3 + x22;
        indexStart = start >>> DIV;
        indexEnd = end >>> DIV;
        if (indexStart === indexEnd) {
          if (array4[indexStart] & RIGHT0[start & MOD2] & RIGHT1[(end & MOD2) + 1]) {
            return true;
          }
        } else {
          if (array4[indexStart] & RIGHT0[start & MOD2]) return true;
          if (array4[indexEnd] & RIGHT1[(end & MOD2) + 1]) return true;
          for (let i = indexStart + 1; i < indexEnd; ++i) {
            if (array4[i]) return true;
          }
        }
      }
      return false;
    },
    setRange: (x6, y6, x22, y22) => {
      let start, end, indexStart, indexEnd, i;
      for (; y6 <= y22; ++y6) {
        start = y6 * w3 + x6;
        end = y6 * w3 + x22;
        indexStart = start >>> DIV;
        indexEnd = end >>> DIV;
        if (indexStart === indexEnd) {
          _set(indexStart, RIGHT0[start & MOD2] & RIGHT1[(end & MOD2) + 1]);
        } else {
          _set(indexStart, RIGHT0[start & MOD2]);
          _set(indexEnd, RIGHT1[(end & MOD2) + 1]);
          for (i = indexStart + 1; i < indexEnd; ++i) _set(i, 4294967295);
        }
      }
    },
    clearRange: (x6, y6, x22, y22) => {
      let start, end, indexStart, indexEnd, i;
      for (; y6 <= y22; ++y6) {
        start = y6 * w3 + x6;
        end = y6 * w3 + x22;
        indexStart = start >>> DIV;
        indexEnd = end >>> DIV;
        if (indexStart === indexEnd) {
          _clear(indexStart, RIGHT1[start & MOD2] | RIGHT0[(end & MOD2) + 1]);
        } else {
          _clear(indexStart, RIGHT1[start & MOD2]);
          _clear(indexEnd, RIGHT0[(end & MOD2) + 1]);
          for (i = indexStart + 1; i < indexEnd; ++i) _clear(i, 0);
        }
      }
    },
    outOfBounds: (x6, y6, x22, y22) => x6 < 0 || y6 < 0 || y22 >= h3 || x22 >= w3
  };
}
function scaler(width2, height2, padding3) {
  const ratio = Math.max(1, Math.sqrt(width2 * height2 / 1e6)), w3 = ~~((width2 + 2 * padding3 + ratio) / ratio), h3 = ~~((height2 + 2 * padding3 + ratio) / ratio), scale9 = (_) => ~~((_ + padding3) / ratio);
  scale9.invert = (_) => _ * ratio - padding3;
  scale9.bitmap = () => Bitmap(w3, h3);
  scale9.ratio = ratio;
  scale9.padding = padding3;
  scale9.width = width2;
  scale9.height = height2;
  return scale9;
}
function placeAreaLabelNaive($, bitmaps, avoidBaseMark, markIndex) {
  const width2 = $.width, height2 = $.height;
  return function(d) {
    const items = d.datum.datum.items[markIndex].items, n = items.length, textHeight = d.datum.fontSize, textWidth = textMetrics.width(d.datum, d.datum.text);
    let maxAreaWidth = 0, x12, x22, y12, y22, x6, y6, areaWidth;
    for (let i = 0; i < n; ++i) {
      x12 = items[i].x;
      y12 = items[i].y;
      x22 = items[i].x2 === void 0 ? x12 : items[i].x2;
      y22 = items[i].y2 === void 0 ? y12 : items[i].y2;
      x6 = (x12 + x22) / 2;
      y6 = (y12 + y22) / 2;
      areaWidth = Math.abs(x22 - x12 + y22 - y12);
      if (areaWidth >= maxAreaWidth) {
        maxAreaWidth = areaWidth;
        d.x = x6;
        d.y = y6;
      }
    }
    x6 = textWidth / 2;
    y6 = textHeight / 2;
    x12 = d.x - x6;
    x22 = d.x + x6;
    y12 = d.y - y6;
    y22 = d.y + y6;
    d.align = "center";
    if (x12 < 0 && x22 <= width2) {
      d.align = "left";
    } else if (0 <= x12 && width2 < x22) {
      d.align = "right";
    }
    d.baseline = "middle";
    if (y12 < 0 && y22 <= height2) {
      d.baseline = "top";
    } else if (0 <= y12 && height2 < y22) {
      d.baseline = "bottom";
    }
    return true;
  };
}
function outOfBounds(x6, y6, textWidth, textHeight, width2, height2) {
  let r = textWidth / 2;
  return x6 - r < 0 || x6 + r > width2 || y6 - (r = textHeight / 2) < 0 || y6 + r > height2;
}
function collision($, x6, y6, textHeight, textWidth, h3, bm0, bm1) {
  const w3 = textWidth * h3 / (textHeight * 2), x12 = $(x6 - w3), x22 = $(x6 + w3), y12 = $(y6 - (h3 = h3 / 2)), y22 = $(y6 + h3);
  return bm0.outOfBounds(x12, y12, x22, y22) || bm0.getRange(x12, y12, x22, y22) || bm1 && bm1.getRange(x12, y12, x22, y22);
}
function placeAreaLabelReducedSearch($, bitmaps, avoidBaseMark, markIndex) {
  const width2 = $.width, height2 = $.height, bm0 = bitmaps[0], bm1 = bitmaps[1];
  function tryLabel(_x3, _y3, maxSize, textWidth, textHeight) {
    const x6 = $.invert(_x3), y6 = $.invert(_y3);
    let lo = maxSize, hi = height2, mid;
    if (!outOfBounds(x6, y6, textWidth, textHeight, width2, height2) && !collision($, x6, y6, textHeight, textWidth, lo, bm0, bm1) && !collision($, x6, y6, textHeight, textWidth, textHeight, bm0, null)) {
      while (hi - lo >= 1) {
        mid = (lo + hi) / 2;
        if (collision($, x6, y6, textHeight, textWidth, mid, bm0, bm1)) {
          hi = mid;
        } else {
          lo = mid;
        }
      }
      if (lo > maxSize) {
        return [x6, y6, lo, true];
      }
    }
  }
  return function(d) {
    const items = d.datum.datum.items[markIndex].items, n = items.length, textHeight = d.datum.fontSize, textWidth = textMetrics.width(d.datum, d.datum.text);
    let maxSize = avoidBaseMark ? textHeight : 0, labelPlaced = false, labelPlaced2 = false, maxAreaWidth = 0, x12, x22, y12, y22, x6, y6, _x3, _y3, _x1, _xMid, _x22, _y1, _yMid, _y22, areaWidth, result, swapTmp;
    for (let i = 0; i < n; ++i) {
      x12 = items[i].x;
      y12 = items[i].y;
      x22 = items[i].x2 === void 0 ? x12 : items[i].x2;
      y22 = items[i].y2 === void 0 ? y12 : items[i].y2;
      if (x12 > x22) {
        swapTmp = x12;
        x12 = x22;
        x22 = swapTmp;
      }
      if (y12 > y22) {
        swapTmp = y12;
        y12 = y22;
        y22 = swapTmp;
      }
      _x1 = $(x12);
      _x22 = $(x22);
      _xMid = ~~((_x1 + _x22) / 2);
      _y1 = $(y12);
      _y22 = $(y22);
      _yMid = ~~((_y1 + _y22) / 2);
      for (_x3 = _xMid; _x3 >= _x1; --_x3) {
        for (_y3 = _yMid; _y3 >= _y1; --_y3) {
          result = tryLabel(_x3, _y3, maxSize, textWidth, textHeight);
          if (result) {
            [d.x, d.y, maxSize, labelPlaced] = result;
          }
        }
      }
      for (_x3 = _xMid; _x3 <= _x22; ++_x3) {
        for (_y3 = _yMid; _y3 <= _y22; ++_y3) {
          result = tryLabel(_x3, _y3, maxSize, textWidth, textHeight);
          if (result) {
            [d.x, d.y, maxSize, labelPlaced] = result;
          }
        }
      }
      if (!labelPlaced && !avoidBaseMark) {
        areaWidth = Math.abs(x22 - x12 + y22 - y12);
        x6 = (x12 + x22) / 2;
        y6 = (y12 + y22) / 2;
        if (areaWidth >= maxAreaWidth && !outOfBounds(x6, y6, textWidth, textHeight, width2, height2) && !collision($, x6, y6, textHeight, textWidth, textHeight, bm0, null)) {
          maxAreaWidth = areaWidth;
          d.x = x6;
          d.y = y6;
          labelPlaced2 = true;
        }
      }
    }
    if (labelPlaced || labelPlaced2) {
      x6 = textWidth / 2;
      y6 = textHeight / 2;
      bm0.setRange($(d.x - x6), $(d.y - y6), $(d.x + x6), $(d.y + y6));
      d.align = "center";
      d.baseline = "middle";
      return true;
    } else {
      return false;
    }
  };
}
var X_DIR = [-1, -1, 1, 1];
var Y_DIR = [-1, 1, -1, 1];
function placeAreaLabelFloodFill($, bitmaps, avoidBaseMark, markIndex) {
  const width2 = $.width, height2 = $.height, bm0 = bitmaps[0], bm1 = bitmaps[1], bm2 = $.bitmap();
  return function(d) {
    const items = d.datum.datum.items[markIndex].items, n = items.length, textHeight = d.datum.fontSize, textWidth = textMetrics.width(d.datum, d.datum.text), stack2 = [];
    let maxSize = avoidBaseMark ? textHeight : 0, labelPlaced = false, labelPlaced2 = false, maxAreaWidth = 0, x12, x22, y12, y22, x6, y6, _x3, _y3, lo, hi, mid, areaWidth;
    for (let i = 0; i < n; ++i) {
      x12 = items[i].x;
      y12 = items[i].y;
      x22 = items[i].x2 === void 0 ? x12 : items[i].x2;
      y22 = items[i].y2 === void 0 ? y12 : items[i].y2;
      stack2.push([$((x12 + x22) / 2), $((y12 + y22) / 2)]);
      while (stack2.length) {
        [_x3, _y3] = stack2.pop();
        if (bm0.get(_x3, _y3) || bm1.get(_x3, _y3) || bm2.get(_x3, _y3)) continue;
        bm2.set(_x3, _y3);
        for (let j = 0; j < 4; ++j) {
          x6 = _x3 + X_DIR[j];
          y6 = _y3 + Y_DIR[j];
          if (!bm2.outOfBounds(x6, y6, x6, y6)) stack2.push([x6, y6]);
        }
        x6 = $.invert(_x3);
        y6 = $.invert(_y3);
        lo = maxSize;
        hi = height2;
        if (!outOfBounds(x6, y6, textWidth, textHeight, width2, height2) && !collision($, x6, y6, textHeight, textWidth, lo, bm0, bm1) && !collision($, x6, y6, textHeight, textWidth, textHeight, bm0, null)) {
          while (hi - lo >= 1) {
            mid = (lo + hi) / 2;
            if (collision($, x6, y6, textHeight, textWidth, mid, bm0, bm1)) {
              hi = mid;
            } else {
              lo = mid;
            }
          }
          if (lo > maxSize) {
            d.x = x6;
            d.y = y6;
            maxSize = lo;
            labelPlaced = true;
          }
        }
      }
      if (!labelPlaced && !avoidBaseMark) {
        areaWidth = Math.abs(x22 - x12 + y22 - y12);
        x6 = (x12 + x22) / 2;
        y6 = (y12 + y22) / 2;
        if (areaWidth >= maxAreaWidth && !outOfBounds(x6, y6, textWidth, textHeight, width2, height2) && !collision($, x6, y6, textHeight, textWidth, textHeight, bm0, null)) {
          maxAreaWidth = areaWidth;
          d.x = x6;
          d.y = y6;
          labelPlaced2 = true;
        }
      }
    }
    if (labelPlaced || labelPlaced2) {
      x6 = textWidth / 2;
      y6 = textHeight / 2;
      bm0.setRange($(d.x - x6), $(d.y - y6), $(d.x + x6), $(d.y + y6));
      d.align = "center";
      d.baseline = "middle";
      return true;
    } else {
      return false;
    }
  };
}
var Aligns = ["right", "center", "left"];
var Baselines = ["bottom", "middle", "top"];
function placeMarkLabel($, bitmaps, anchors, offsets2) {
  const width2 = $.width, height2 = $.height, bm0 = bitmaps[0], bm1 = bitmaps[1], n = offsets2.length;
  return function(d) {
    const boundary = d.boundary, textHeight = d.datum.fontSize;
    if (boundary[2] < 0 || boundary[5] < 0 || boundary[0] > width2 || boundary[3] > height2) {
      return false;
    }
    let textWidth = d.textWidth ?? 0, dx, dy, isInside, sizeFactor, insideFactor, x12, x22, y12, y22, xc, yc, _x1, _x22, _y1, _y22;
    for (let i = 0; i < n; ++i) {
      dx = (anchors[i] & 3) - 1;
      dy = (anchors[i] >>> 2 & 3) - 1;
      isInside = dx === 0 && dy === 0 || offsets2[i] < 0;
      sizeFactor = dx && dy ? Math.SQRT1_2 : 1;
      insideFactor = offsets2[i] < 0 ? -1 : 1;
      x12 = boundary[1 + dx] + offsets2[i] * dx * sizeFactor;
      yc = boundary[4 + dy] + insideFactor * textHeight * dy / 2 + offsets2[i] * dy * sizeFactor;
      y12 = yc - textHeight / 2;
      y22 = yc + textHeight / 2;
      _x1 = $(x12);
      _y1 = $(y12);
      _y22 = $(y22);
      if (!textWidth) {
        if (!test(_x1, _x1, _y1, _y22, bm0, bm1, x12, x12, y12, y22, boundary, isInside)) {
          continue;
        } else {
          textWidth = textMetrics.width(d.datum, d.datum.text);
        }
      }
      xc = x12 + insideFactor * textWidth * dx / 2;
      x12 = xc - textWidth / 2;
      x22 = xc + textWidth / 2;
      _x1 = $(x12);
      _x22 = $(x22);
      if (test(_x1, _x22, _y1, _y22, bm0, bm1, x12, x22, y12, y22, boundary, isInside)) {
        d.x = !dx ? xc : dx * insideFactor < 0 ? x22 : x12;
        d.y = !dy ? yc : dy * insideFactor < 0 ? y22 : y12;
        d.align = Aligns[dx * insideFactor + 1];
        d.baseline = Baselines[dy * insideFactor + 1];
        bm0.setRange(_x1, _y1, _x22, _y22);
        return true;
      }
    }
    return false;
  };
}
function test(_x1, _x22, _y1, _y22, bm0, bm1, x12, x22, y12, y22, boundary, isInside) {
  return !(bm0.outOfBounds(_x1, _y1, _x22, _y22) || (isInside && bm1 || bm0).getRange(_x1, _y1, _x22, _y22));
}
var TOP = 0;
var MIDDLE = 4;
var BOTTOM = 8;
var LEFT = 0;
var CENTER = 1;
var RIGHT = 2;
var anchorCode = {
  "top-left": TOP + LEFT,
  "top": TOP + CENTER,
  "top-right": TOP + RIGHT,
  "left": MIDDLE + LEFT,
  "middle": MIDDLE + CENTER,
  "right": MIDDLE + RIGHT,
  "bottom-left": BOTTOM + LEFT,
  "bottom": BOTTOM + CENTER,
  "bottom-right": BOTTOM + RIGHT
};
var placeAreaLabel = {
  "naive": placeAreaLabelNaive,
  "reduced-search": placeAreaLabelReducedSearch,
  "floodfill": placeAreaLabelFloodFill
};
function labelLayout(texts, size, compare4, offset6, anchor, avoidMarks, avoidBaseMark, lineAnchor, markIndex, padding3, method2) {
  if (!texts.length) return texts;
  const positions = Math.max(offset6.length, anchor.length), offsets2 = getOffsets(offset6, positions), anchors = getAnchors(anchor, positions), marktype = markType(texts[0].datum), grouptype = marktype === "group" && texts[0].datum.items[markIndex].marktype, isGroupArea = grouptype === "area", boundary = markBoundary(marktype, grouptype, lineAnchor, markIndex), infPadding = padding3 === null || padding3 === Infinity, isNaiveGroupArea = isGroupArea && method2 === "naive";
  let maxTextWidth = -1, maxTextHeight = -1;
  const data4 = texts.map((d) => {
    const textWidth = infPadding ? textMetrics.width(d, d.text) : void 0;
    maxTextWidth = Math.max(maxTextWidth, textWidth);
    maxTextHeight = Math.max(maxTextHeight, d.fontSize);
    return {
      datum: d,
      opacity: 0,
      x: void 0,
      y: void 0,
      align: void 0,
      baseline: void 0,
      boundary: boundary(d),
      textWidth
    };
  });
  padding3 = padding3 === null || padding3 === Infinity ? Math.max(maxTextWidth, maxTextHeight) + Math.max(...offset6) : padding3;
  const $ = scaler(size[0], size[1], padding3);
  let bitmaps;
  if (!isNaiveGroupArea) {
    if (compare4) {
      data4.sort((a3, b10) => compare4(a3.datum, b10.datum));
    }
    let labelInside = false;
    for (let i = 0; i < anchors.length && !labelInside; ++i) {
      labelInside = anchors[i] === 5 || offsets2[i] < 0;
    }
    const baseMark = (marktype && avoidBaseMark || isGroupArea) && texts.map((d) => d.datum);
    bitmaps = avoidMarks.length || baseMark ? markBitmaps($, baseMark || [], avoidMarks, labelInside, isGroupArea) : baseBitmaps($, avoidBaseMark && data4);
  }
  const place2 = isGroupArea ? placeAreaLabel[method2]($, bitmaps, avoidBaseMark, markIndex) : placeMarkLabel($, bitmaps, anchors, offsets2);
  data4.forEach((d) => d.opacity = +place2(d));
  return data4;
}
function getOffsets(_, count2) {
  const offsets2 = new Float64Array(count2), n = _.length;
  for (let i = 0; i < n; ++i) offsets2[i] = _[i] || 0;
  for (let i = n; i < count2; ++i) offsets2[i] = offsets2[n - 1];
  return offsets2;
}
function getAnchors(_, count2) {
  const anchors = new Int8Array(count2), n = _.length;
  for (let i = 0; i < n; ++i) anchors[i] |= anchorCode[_[i]];
  for (let i = n; i < count2; ++i) anchors[i] = anchors[n - 1];
  return anchors;
}
function markType(item) {
  return item && item.mark && item.mark.marktype;
}
function markBoundary(marktype, grouptype, lineAnchor, markIndex) {
  const xy = (d) => [d.x, d.x, d.x, d.y, d.y, d.y];
  if (!marktype) {
    return xy;
  } else if (marktype === "line" || marktype === "area") {
    return (d) => xy(d.datum);
  } else if (grouptype === "line") {
    return (d) => {
      const items = d.datum.items[markIndex].items;
      return xy(items.length ? items[lineAnchor === "start" ? 0 : items.length - 1] : {
        x: NaN,
        y: NaN
      });
    };
  } else {
    return (d) => {
      const b10 = d.datum.bounds;
      return [b10.x1, (b10.x1 + b10.x2) / 2, b10.x2, b10.y1, (b10.y1 + b10.y2) / 2, b10.y2];
    };
  }
}
var Output3 = ["x", "y", "opacity", "align", "baseline"];
var Anchors = ["top-left", "left", "bottom-left", "top", "bottom", "top-right", "right", "bottom-right"];
function Label(params2) {
  Transform.call(this, null, params2);
}
Label.Definition = {
  type: "Label",
  metadata: {
    modifies: true
  },
  params: [{
    name: "size",
    type: "number",
    array: true,
    length: 2,
    required: true
  }, {
    name: "sort",
    type: "compare"
  }, {
    name: "anchor",
    type: "string",
    array: true,
    default: Anchors
  }, {
    name: "offset",
    type: "number",
    array: true,
    default: [1]
  }, {
    name: "padding",
    type: "number",
    default: 0,
    null: true
  }, {
    name: "lineAnchor",
    type: "string",
    values: ["start", "end"],
    default: "end"
  }, {
    name: "markIndex",
    type: "number",
    default: 0
  }, {
    name: "avoidBaseMark",
    type: "boolean",
    default: true
  }, {
    name: "avoidMarks",
    type: "data",
    array: true
  }, {
    name: "method",
    type: "string",
    default: "naive"
  }, {
    name: "as",
    type: "string",
    array: true,
    length: Output3.length,
    default: Output3
  }]
};
inherits_default(Label, Transform, {
  transform(_, pulse2) {
    function modp(param2) {
      const p = _[param2];
      return isFunction(p) && pulse2.modified(p.fields);
    }
    const mod = _.modified();
    if (!(mod || pulse2.changed(pulse2.ADD_REM) || modp("sort"))) return;
    if (!_.size || _.size.length !== 2) {
      error("Size parameter should be specified as a [width, height] array.");
    }
    const as = _.as || Output3;
    labelLayout(pulse2.materialize(pulse2.SOURCE).source || [], _.size, _.sort, array_default(_.offset == null ? 1 : _.offset), array_default(_.anchor || Anchors), _.avoidMarks || [], _.avoidBaseMark !== false, _.lineAnchor || "end", _.markIndex || 0, _.padding === void 0 ? 0 : _.padding, _.method || "naive").forEach((l) => {
      const t = l.datum;
      t[as[0]] = l.x;
      t[as[1]] = l.y;
      t[as[2]] = l.opacity;
      t[as[3]] = l.align;
      t[as[4]] = l.baseline;
    });
    return pulse2.reflow(mod).modifies(as);
  }
});

// ../deepseek-harness/node_modules/.pnpm/vega-regression@2.1.3/node_modules/vega-regression/build/vega-regression.js
var vega_regression_exports = {};
__export(vega_regression_exports, {
  loess: () => Loess,
  regression: () => Regression
});
function partition4(data4, groupby) {
  var groups = [], get7 = function(f) {
    return f(t);
  }, map3, i, n, t, k, g;
  if (groupby == null) {
    groups.push(data4);
  } else {
    for (map3 = {}, i = 0, n = data4.length; i < n; ++i) {
      t = data4[i];
      k = groupby.map(get7);
      g = map3[k];
      if (!g) {
        map3[k] = g = [];
        g.dims = k;
        groups.push(g);
      }
      g.push(t);
    }
  }
  return groups;
}
function Loess(params2) {
  Transform.call(this, null, params2);
}
Loess.Definition = {
  "type": "Loess",
  "metadata": {
    "generates": true
  },
  "params": [{
    "name": "x",
    "type": "field",
    "required": true
  }, {
    "name": "y",
    "type": "field",
    "required": true
  }, {
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "bandwidth",
    "type": "number",
    "default": 0.3
  }, {
    "name": "as",
    "type": "string",
    "array": true
  }]
};
inherits_default(Loess, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS);
    if (!this.value || pulse2.changed() || _.modified()) {
      const source4 = pulse2.materialize(pulse2.SOURCE).source, groups = partition4(source4, _.groupby), names = (_.groupby || []).map(accessorName), m3 = names.length, as = _.as || [accessorName(_.x), accessorName(_.y)], values4 = [];
      groups.forEach((g) => {
        loess(g, _.x, _.y, _.bandwidth || 0.3).forEach((p) => {
          const t = {};
          for (let i = 0; i < m3; ++i) {
            t[names[i]] = g.dims[i];
          }
          t[as[0]] = p[0];
          t[as[1]] = p[1];
          values4.push(ingest$1(t));
        });
      });
      if (this.value) out.rem = this.value;
      this.value = out.add = out.source = values4;
    }
    return out;
  }
});
var Methods2 = {
  constant,
  linear,
  log: log3,
  exp: exp2,
  pow: pow2,
  quad,
  poly
};
var degreesOfFreedom = (method2, order) => method2 === "poly" ? order : method2 === "quad" ? 2 : 1;
function Regression(params2) {
  Transform.call(this, null, params2);
}
Regression.Definition = {
  "type": "Regression",
  "metadata": {
    "generates": true
  },
  "params": [{
    "name": "x",
    "type": "field",
    "required": true
  }, {
    "name": "y",
    "type": "field",
    "required": true
  }, {
    "name": "groupby",
    "type": "field",
    "array": true
  }, {
    "name": "method",
    "type": "string",
    "default": "linear",
    "values": Object.keys(Methods2)
  }, {
    "name": "order",
    "type": "number",
    "default": 3
  }, {
    "name": "extent",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "params",
    "type": "boolean",
    "default": false
  }, {
    "name": "as",
    "type": "string",
    "array": true
  }]
};
inherits_default(Regression, Transform, {
  transform(_, pulse2) {
    const out = pulse2.fork(pulse2.NO_SOURCE | pulse2.NO_FIELDS);
    if (!this.value || pulse2.changed() || _.modified()) {
      const source4 = pulse2.materialize(pulse2.SOURCE).source, groups = partition4(source4, _.groupby), names = (_.groupby || []).map(accessorName), method2 = _.method || "linear", order = _.order == null ? 3 : _.order, dof = degreesOfFreedom(method2, order), as = _.as || [accessorName(_.x), accessorName(_.y)], fit3 = Methods2[method2], values4 = [];
      let domain4 = _.extent;
      if (!hasOwnProperty(Methods2, method2)) {
        error("Invalid regression method: " + method2);
      }
      if (domain4 != null) {
        if (method2 === "log" && domain4[0] <= 0) {
          pulse2.dataflow.warn("Ignoring extent with values <= 0 for log regression.");
          domain4 = null;
        }
      }
      groups.forEach((g) => {
        const n = g.length;
        if (n <= dof) {
          pulse2.dataflow.warn("Skipping regression with more parameters than data points.");
          return;
        }
        const model = fit3(g, _.x, _.y, order);
        if (_.params) {
          values4.push(ingest$1({
            keys: g.dims,
            coef: model.coef,
            rSquared: model.rSquared
          }));
          return;
        }
        const dom = domain4 || extent(g, _.x), add7 = (p) => {
          const t = {};
          for (let i = 0; i < names.length; ++i) {
            t[names[i]] = g.dims[i];
          }
          t[as[0]] = p[0];
          t[as[1]] = p[1];
          values4.push(ingest$1(t));
        };
        if (method2 === "linear" || method2 === "constant") {
          dom.forEach((x6) => add7([x6, model.predict(x6)]));
        } else {
          sampleCurve(model.predict, dom, 25, 200).forEach(add7);
        }
      });
      if (this.value) out.rem = this.value;
      this.value = out.add = out.source = values4;
    }
    return out;
  }
});

// ../deepseek-harness/node_modules/.pnpm/vega-voronoi@5.1.3/node_modules/vega-voronoi/build/vega-voronoi.js
var vega_voronoi_exports = {};
__export(vega_voronoi_exports, {
  voronoi: () => Voronoi2
});

// ../deepseek-harness/node_modules/.pnpm/robust-predicates@3.0.3/node_modules/robust-predicates/esm/util.js
var epsilon6 = 11102230246251565e-32;
var splitter = 134217729;
var resulterrbound = (3 + 8 * epsilon6) * epsilon6;
function sum2(elen, e, flen, f, h3) {
  let Q, Qnew, hh, bvirt;
  let enow = e[0];
  let fnow = f[0];
  let eindex = 0;
  let findex = 0;
  if (fnow > enow === fnow > -enow) {
    Q = enow;
    enow = e[++eindex];
  } else {
    Q = fnow;
    fnow = f[++findex];
  }
  let hindex = 0;
  if (eindex < elen && findex < flen) {
    if (fnow > enow === fnow > -enow) {
      Qnew = enow + Q;
      hh = Q - (Qnew - enow);
      enow = e[++eindex];
    } else {
      Qnew = fnow + Q;
      hh = Q - (Qnew - fnow);
      fnow = f[++findex];
    }
    Q = Qnew;
    if (hh !== 0) {
      h3[hindex++] = hh;
    }
    while (eindex < elen && findex < flen) {
      if (fnow > enow === fnow > -enow) {
        Qnew = Q + enow;
        bvirt = Qnew - Q;
        hh = Q - (Qnew - bvirt) + (enow - bvirt);
        enow = e[++eindex];
      } else {
        Qnew = Q + fnow;
        bvirt = Qnew - Q;
        hh = Q - (Qnew - bvirt) + (fnow - bvirt);
        fnow = f[++findex];
      }
      Q = Qnew;
      if (hh !== 0) {
        h3[hindex++] = hh;
      }
    }
  }
  while (eindex < elen) {
    Qnew = Q + enow;
    bvirt = Qnew - Q;
    hh = Q - (Qnew - bvirt) + (enow - bvirt);
    enow = e[++eindex];
    Q = Qnew;
    if (hh !== 0) {
      h3[hindex++] = hh;
    }
  }
  while (findex < flen) {
    Qnew = Q + fnow;
    bvirt = Qnew - Q;
    hh = Q - (Qnew - bvirt) + (fnow - bvirt);
    fnow = f[++findex];
    Q = Qnew;
    if (hh !== 0) {
      h3[hindex++] = hh;
    }
  }
  if (Q !== 0 || hindex === 0) {
    h3[hindex++] = Q;
  }
  return hindex;
}
function estimate(elen, e) {
  let Q = e[0];
  for (let i = 1; i < elen; i++) Q += e[i];
  return Q;
}
function vec(n) {
  return new Float64Array(n);
}

// ../deepseek-harness/node_modules/.pnpm/robust-predicates@3.0.3/node_modules/robust-predicates/esm/orient2d.js
var ccwerrboundA = (3 + 16 * epsilon6) * epsilon6;
var ccwerrboundB = (2 + 12 * epsilon6) * epsilon6;
var ccwerrboundC = (9 + 64 * epsilon6) * epsilon6 * epsilon6;
var B2 = vec(4);
var C1 = vec(8);
var C22 = vec(12);
var D2 = vec(16);
var u = vec(4);
function orient2dadapt(ax, ay, bx, by, cx, cy, detsum) {
  let acxtail, acytail, bcxtail, bcytail;
  let bvirt, c3, ahi, alo, bhi, blo, _i, _j, _0, s1, s0, t13, t05, u32;
  const acx = ax - cx;
  const bcx = bx - cx;
  const acy = ay - cy;
  const bcy = by - cy;
  s1 = acx * bcy;
  c3 = splitter * acx;
  ahi = c3 - (c3 - acx);
  alo = acx - ahi;
  c3 = splitter * bcy;
  bhi = c3 - (c3 - bcy);
  blo = bcy - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t13 = acy * bcx;
  c3 = splitter * acy;
  ahi = c3 - (c3 - acy);
  alo = acy - ahi;
  c3 = splitter * bcx;
  bhi = c3 - (c3 - bcx);
  blo = bcx - bhi;
  t05 = alo * blo - (t13 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t05;
  bvirt = s0 - _i;
  B2[0] = s0 - (_i + bvirt) + (bvirt - t05);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t13;
  bvirt = _0 - _i;
  B2[1] = _0 - (_i + bvirt) + (bvirt - t13);
  u32 = _j + _i;
  bvirt = u32 - _j;
  B2[2] = _j - (u32 - bvirt) + (_i - bvirt);
  B2[3] = u32;
  let det = estimate(4, B2);
  let errbound = ccwerrboundB * detsum;
  if (det >= errbound || -det >= errbound) {
    return det;
  }
  bvirt = ax - acx;
  acxtail = ax - (acx + bvirt) + (bvirt - cx);
  bvirt = bx - bcx;
  bcxtail = bx - (bcx + bvirt) + (bvirt - cx);
  bvirt = ay - acy;
  acytail = ay - (acy + bvirt) + (bvirt - cy);
  bvirt = by - bcy;
  bcytail = by - (bcy + bvirt) + (bvirt - cy);
  if (acxtail === 0 && acytail === 0 && bcxtail === 0 && bcytail === 0) {
    return det;
  }
  errbound = ccwerrboundC * detsum + resulterrbound * Math.abs(det);
  det += acx * bcytail + bcy * acxtail - (acy * bcxtail + bcx * acytail);
  if (det >= errbound || -det >= errbound) return det;
  s1 = acxtail * bcy;
  c3 = splitter * acxtail;
  ahi = c3 - (c3 - acxtail);
  alo = acxtail - ahi;
  c3 = splitter * bcy;
  bhi = c3 - (c3 - bcy);
  blo = bcy - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t13 = acytail * bcx;
  c3 = splitter * acytail;
  ahi = c3 - (c3 - acytail);
  alo = acytail - ahi;
  c3 = splitter * bcx;
  bhi = c3 - (c3 - bcx);
  blo = bcx - bhi;
  t05 = alo * blo - (t13 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t05;
  bvirt = s0 - _i;
  u[0] = s0 - (_i + bvirt) + (bvirt - t05);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t13;
  bvirt = _0 - _i;
  u[1] = _0 - (_i + bvirt) + (bvirt - t13);
  u32 = _j + _i;
  bvirt = u32 - _j;
  u[2] = _j - (u32 - bvirt) + (_i - bvirt);
  u[3] = u32;
  const C1len = sum2(4, B2, 4, u, C1);
  s1 = acx * bcytail;
  c3 = splitter * acx;
  ahi = c3 - (c3 - acx);
  alo = acx - ahi;
  c3 = splitter * bcytail;
  bhi = c3 - (c3 - bcytail);
  blo = bcytail - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t13 = acy * bcxtail;
  c3 = splitter * acy;
  ahi = c3 - (c3 - acy);
  alo = acy - ahi;
  c3 = splitter * bcxtail;
  bhi = c3 - (c3 - bcxtail);
  blo = bcxtail - bhi;
  t05 = alo * blo - (t13 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t05;
  bvirt = s0 - _i;
  u[0] = s0 - (_i + bvirt) + (bvirt - t05);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t13;
  bvirt = _0 - _i;
  u[1] = _0 - (_i + bvirt) + (bvirt - t13);
  u32 = _j + _i;
  bvirt = u32 - _j;
  u[2] = _j - (u32 - bvirt) + (_i - bvirt);
  u[3] = u32;
  const C2len = sum2(C1len, C1, 4, u, C22);
  s1 = acxtail * bcytail;
  c3 = splitter * acxtail;
  ahi = c3 - (c3 - acxtail);
  alo = acxtail - ahi;
  c3 = splitter * bcytail;
  bhi = c3 - (c3 - bcytail);
  blo = bcytail - bhi;
  s0 = alo * blo - (s1 - ahi * bhi - alo * bhi - ahi * blo);
  t13 = acytail * bcxtail;
  c3 = splitter * acytail;
  ahi = c3 - (c3 - acytail);
  alo = acytail - ahi;
  c3 = splitter * bcxtail;
  bhi = c3 - (c3 - bcxtail);
  blo = bcxtail - bhi;
  t05 = alo * blo - (t13 - ahi * bhi - alo * bhi - ahi * blo);
  _i = s0 - t05;
  bvirt = s0 - _i;
  u[0] = s0 - (_i + bvirt) + (bvirt - t05);
  _j = s1 + _i;
  bvirt = _j - s1;
  _0 = s1 - (_j - bvirt) + (_i - bvirt);
  _i = _0 - t13;
  bvirt = _0 - _i;
  u[1] = _0 - (_i + bvirt) + (bvirt - t13);
  u32 = _j + _i;
  bvirt = u32 - _j;
  u[2] = _j - (u32 - bvirt) + (_i - bvirt);
  u[3] = u32;
  const Dlen = sum2(C2len, C22, 4, u, D2);
  return D2[Dlen - 1];
}
function orient2d(ax, ay, bx, by, cx, cy) {
  const detleft = (ay - cy) * (bx - cx);
  const detright = (ax - cx) * (by - cy);
  const det = detleft - detright;
  const detsum = Math.abs(detleft + detright);
  if (Math.abs(det) >= ccwerrboundA * detsum) return det;
  return -orient2dadapt(ax, ay, bx, by, cx, cy, detsum);
}

// ../deepseek-harness/node_modules/.pnpm/robust-predicates@3.0.3/node_modules/robust-predicates/esm/orient3d.js
var o3derrboundA = (7 + 56 * epsilon6) * epsilon6;
var o3derrboundB = (3 + 28 * epsilon6) * epsilon6;
var o3derrboundC = (26 + 288 * epsilon6) * epsilon6 * epsilon6;
var bc = vec(4);
var ca = vec(4);
var ab = vec(4);
var at_b = vec(4);
var at_c = vec(4);
var bt_c = vec(4);
var bt_a = vec(4);
var ct_a = vec(4);
var ct_b = vec(4);
var bct = vec(8);
var cat = vec(8);
var abt = vec(8);
var u2 = vec(4);
var _8 = vec(8);
var _8b = vec(8);
var _16 = vec(16);
var _12 = vec(12);
var fin = vec(192);
var fin2 = vec(192);

// ../deepseek-harness/node_modules/.pnpm/robust-predicates@3.0.3/node_modules/robust-predicates/esm/incircle.js
var iccerrboundA = (10 + 96 * epsilon6) * epsilon6;
var iccerrboundB = (4 + 48 * epsilon6) * epsilon6;
var iccerrboundC = (44 + 576 * epsilon6) * epsilon6 * epsilon6;
var bc2 = vec(4);
var ca2 = vec(4);
var ab2 = vec(4);
var aa = vec(4);
var bb = vec(4);
var cc = vec(4);
var u3 = vec(4);
var v = vec(4);
var axtbc = vec(8);
var aytbc = vec(8);
var bxtca = vec(8);
var bytca = vec(8);
var cxtab = vec(8);
var cytab = vec(8);
var abt2 = vec(8);
var bct2 = vec(8);
var cat2 = vec(8);
var abtt = vec(4);
var bctt = vec(4);
var catt = vec(4);
var _82 = vec(8);
var _162 = vec(16);
var _16b = vec(16);
var _16c = vec(16);
var _32 = vec(32);
var _32b = vec(32);
var _48 = vec(48);
var _64 = vec(64);
var fin3 = vec(1152);
var fin22 = vec(1152);

// ../deepseek-harness/node_modules/.pnpm/robust-predicates@3.0.3/node_modules/robust-predicates/esm/insphere.js
var isperrboundA = (16 + 224 * epsilon6) * epsilon6;
var isperrboundB = (5 + 72 * epsilon6) * epsilon6;
var isperrboundC = (71 + 1408 * epsilon6) * epsilon6 * epsilon6;
var ab3 = vec(4);
var bc3 = vec(4);
var cd = vec(4);
var de = vec(4);
var ea2 = vec(4);
var ac = vec(4);
var bd = vec(4);
var ce = vec(4);
var da = vec(4);
var eb = vec(4);
var abc = vec(24);
var bcd = vec(24);
var cde = vec(24);
var dea = vec(24);
var eab = vec(24);
var abd = vec(24);
var bce = vec(24);
var cda = vec(24);
var deb = vec(24);
var eac = vec(24);
var adet = vec(1152);
var bdet = vec(1152);
var cdet = vec(1152);
var ddet = vec(1152);
var edet = vec(1152);
var abdet = vec(2304);
var cddet = vec(2304);
var cdedet = vec(3456);
var deter = vec(5760);
var _83 = vec(8);
var _8b2 = vec(8);
var _8c = vec(8);
var _163 = vec(16);
var _24 = vec(24);
var _482 = vec(48);
var _48b = vec(48);
var _96 = vec(96);
var _192 = vec(192);
var _384x = vec(384);
var _384y = vec(384);
var _384z = vec(384);
var _768 = vec(768);
var xdet = vec(96);
var ydet = vec(96);
var zdet = vec(96);
var fin4 = vec(1152);

// ../deepseek-harness/node_modules/.pnpm/delaunator@5.1.0/node_modules/delaunator/index.js
var EPSILON2 = Math.pow(2, -52);
var EDGE_STACK = new Uint32Array(512);
var Delaunator = class _Delaunator {
  /**
   * Constructs a delaunay triangulation object given an array of points (`[x, y]` by default).
   * `getX` and `getY` are optional functions of the form `(point) => value` for custom point formats.
   *
   * @template P
   * @param {P[]} points
   * @param {(p: P) => number} [getX]
   * @param {(p: P) => number} [getY]
   */
  // @ts-expect-error TS2322
  static from(points2, getX = defaultGetX, getY = defaultGetY) {
    const n = points2.length;
    const coords = new Float64Array(n * 2);
    for (let i = 0; i < n; i++) {
      const p = points2[i];
      coords[2 * i] = getX(p);
      coords[2 * i + 1] = getY(p);
    }
    return new _Delaunator(coords);
  }
  /**
   * Constructs a delaunay triangulation object given an array of point coordinates of the form:
   * `[x0, y0, x1, y1, ...]` (use a typed array for best performance). Duplicate points are skipped.
   *
   * @param {T} coords
   */
  constructor(coords) {
    const n = coords.length >> 1;
    if (n > 0 && typeof coords[0] !== "number") throw new Error("Expected coords to contain numbers.");
    this.coords = coords;
    const maxTriangles = Math.max(2 * n - 5, 0);
    this._triangles = new Uint32Array(maxTriangles * 3);
    this._halfedges = new Int32Array(maxTriangles * 3);
    this._hashSize = Math.ceil(Math.sqrt(n));
    this._hullPrev = new Uint32Array(n);
    this._hullNext = new Uint32Array(n);
    this._hullTri = new Uint32Array(n);
    this._hullHash = new Int32Array(this._hashSize);
    this._ids = new Uint32Array(n);
    this._dists = new Float64Array(n);
    this.trianglesLen = 0;
    this._cx = 0;
    this._cy = 0;
    this._hullStart = 0;
    this.hull = this._triangles;
    this.triangles = this._triangles;
    this.halfedges = this._halfedges;
    this.update();
  }
  /**
   * Updates the triangulation if you modified `delaunay.coords` values in place, avoiding expensive memory allocations.
   * Useful for iterative relaxation algorithms such as Lloyd's.
   */
  update() {
    const { coords, _hullPrev: hullPrev, _hullNext: hullNext, _hullTri: hullTri, _hullHash: hullHash } = this;
    const n = coords.length >> 1;
    let minX = Infinity;
    let minY = Infinity;
    let maxX = -Infinity;
    let maxY2 = -Infinity;
    for (let i = 0; i < n; i++) {
      const x6 = coords[2 * i];
      const y6 = coords[2 * i + 1];
      if (x6 < minX) minX = x6;
      if (y6 < minY) minY = y6;
      if (x6 > maxX) maxX = x6;
      if (y6 > maxY2) maxY2 = y6;
      this._ids[i] = i;
    }
    const cx = (minX + maxX) / 2;
    const cy = (minY + maxY2) / 2;
    let i0 = 0, i1 = 0, i2 = 0;
    for (let i = 0, minDist = Infinity; i < n; i++) {
      const d = dist(cx, cy, coords[2 * i], coords[2 * i + 1]);
      if (d < minDist) {
        i0 = i;
        minDist = d;
      }
    }
    const i0x = coords[2 * i0];
    const i0y = coords[2 * i0 + 1];
    for (let i = 0, minDist = Infinity; i < n; i++) {
      if (i === i0) continue;
      const d = dist(i0x, i0y, coords[2 * i], coords[2 * i + 1]);
      if (d < minDist && d > 0) {
        i1 = i;
        minDist = d;
      }
    }
    let i1x = coords[2 * i1];
    let i1y = coords[2 * i1 + 1];
    let minRadius = Infinity;
    for (let i = 0; i < n; i++) {
      if (i === i0 || i === i1) continue;
      const r = circumradius(i0x, i0y, i1x, i1y, coords[2 * i], coords[2 * i + 1]);
      if (r < minRadius) {
        i2 = i;
        minRadius = r;
      }
    }
    let i2x = coords[2 * i2];
    let i2y = coords[2 * i2 + 1];
    if (minRadius === Infinity) {
      for (let i = 0; i < n; i++) {
        this._dists[i] = coords[2 * i] - coords[0] || coords[2 * i + 1] - coords[1];
      }
      quicksort(this._ids, this._dists, 0, n - 1);
      const hull = new Uint32Array(n);
      let j = 0;
      for (let i = 0, d0 = -Infinity; i < n; i++) {
        const id2 = this._ids[i];
        const d = this._dists[id2];
        if (d > d0) {
          hull[j++] = id2;
          d0 = d;
        }
      }
      this.hull = hull.subarray(0, j);
      this.triangles = new Uint32Array(0);
      this.halfedges = new Int32Array(0);
      return;
    }
    if (orient2d(i0x, i0y, i1x, i1y, i2x, i2y) < 0) {
      const i = i1;
      const x6 = i1x;
      const y6 = i1y;
      i1 = i2;
      i1x = i2x;
      i1y = i2y;
      i2 = i;
      i2x = x6;
      i2y = y6;
    }
    const center = circumcenter(i0x, i0y, i1x, i1y, i2x, i2y);
    this._cx = center.x;
    this._cy = center.y;
    for (let i = 0; i < n; i++) {
      this._dists[i] = dist(coords[2 * i], coords[2 * i + 1], center.x, center.y);
    }
    quicksort(this._ids, this._dists, 0, n - 1);
    this._hullStart = i0;
    let hullSize = 3;
    hullNext[i0] = hullPrev[i2] = i1;
    hullNext[i1] = hullPrev[i0] = i2;
    hullNext[i2] = hullPrev[i1] = i0;
    hullTri[i0] = 0;
    hullTri[i1] = 1;
    hullTri[i2] = 2;
    hullHash.fill(-1);
    hullHash[this._hashKey(i0x, i0y)] = i0;
    hullHash[this._hashKey(i1x, i1y)] = i1;
    hullHash[this._hashKey(i2x, i2y)] = i2;
    this.trianglesLen = 0;
    this._addTriangle(i0, i1, i2, -1, -1, -1);
    for (let k = 0, xp = 0, yp = 0; k < this._ids.length; k++) {
      const i = this._ids[k];
      const x6 = coords[2 * i];
      const y6 = coords[2 * i + 1];
      if (k > 0 && Math.abs(x6 - xp) <= EPSILON2 && Math.abs(y6 - yp) <= EPSILON2) continue;
      xp = x6;
      yp = y6;
      if (i === i0 || i === i1 || i === i2) continue;
      let start = 0;
      for (let j = 0, key2 = this._hashKey(x6, y6); j < this._hashSize; j++) {
        start = hullHash[(key2 + j) % this._hashSize];
        if (start !== -1 && start !== hullNext[start]) break;
      }
      start = hullPrev[start];
      let e = start, q;
      while (q = hullNext[e], orient2d(x6, y6, coords[2 * e], coords[2 * e + 1], coords[2 * q], coords[2 * q + 1]) >= 0) {
        e = q;
        if (e === start) {
          e = -1;
          break;
        }
      }
      if (e === -1) continue;
      let t = this._addTriangle(e, i, hullNext[e], -1, -1, hullTri[e]);
      hullTri[i] = this._legalize(t + 2);
      hullTri[e] = t;
      hullSize++;
      let n2 = hullNext[e];
      while (q = hullNext[n2], orient2d(x6, y6, coords[2 * n2], coords[2 * n2 + 1], coords[2 * q], coords[2 * q + 1]) < 0) {
        t = this._addTriangle(n2, i, q, hullTri[i], -1, hullTri[n2]);
        hullTri[i] = this._legalize(t + 2);
        hullNext[n2] = n2;
        hullSize--;
        n2 = q;
      }
      if (e === start) {
        while (q = hullPrev[e], orient2d(x6, y6, coords[2 * q], coords[2 * q + 1], coords[2 * e], coords[2 * e + 1]) < 0) {
          t = this._addTriangle(q, i, e, -1, hullTri[e], hullTri[q]);
          this._legalize(t + 2);
          hullTri[q] = t;
          hullNext[e] = e;
          hullSize--;
          e = q;
        }
      }
      this._hullStart = hullPrev[i] = e;
      hullNext[e] = hullPrev[n2] = i;
      hullNext[i] = n2;
      hullHash[this._hashKey(x6, y6)] = i;
      hullHash[this._hashKey(coords[2 * e], coords[2 * e + 1])] = e;
    }
    this.hull = new Uint32Array(hullSize);
    for (let i = 0, e = this._hullStart; i < hullSize; i++) {
      this.hull[i] = e;
      e = hullNext[e];
    }
    this.triangles = this._triangles.subarray(0, this.trianglesLen);
    this.halfedges = this._halfedges.subarray(0, this.trianglesLen);
  }
  /**
   * Calculate an angle-based key for the edge hash used for advancing convex hull.
   *
   * @param {number} x
   * @param {number} y
   * @private
   */
  _hashKey(x6, y6) {
    return Math.floor(pseudoAngle(x6 - this._cx, y6 - this._cy) * this._hashSize) % this._hashSize;
  }
  /**
   * Flip an edge in a pair of triangles if it doesn't satisfy the Delaunay condition.
   *
   * @param {number} a
   * @private
   */
  _legalize(a3) {
    const { _triangles: triangles, _halfedges: halfedges, coords } = this;
    let i = 0;
    let ar = 0;
    while (true) {
      const b10 = halfedges[a3];
      const a0 = a3 - a3 % 3;
      ar = a0 + (a3 + 2) % 3;
      if (b10 === -1) {
        if (i === 0) break;
        a3 = EDGE_STACK[--i];
        continue;
      }
      const b02 = b10 - b10 % 3;
      const al = a0 + (a3 + 1) % 3;
      const bl3 = b02 + (b10 + 2) % 3;
      const p02 = triangles[ar];
      const pr = triangles[a3];
      const pl = triangles[al];
      const p1 = triangles[bl3];
      const illegal = inCircle(
        coords[2 * p02],
        coords[2 * p02 + 1],
        coords[2 * pr],
        coords[2 * pr + 1],
        coords[2 * pl],
        coords[2 * pl + 1],
        coords[2 * p1],
        coords[2 * p1 + 1]
      );
      if (illegal) {
        triangles[a3] = p1;
        triangles[b10] = p02;
        const hbl = halfedges[bl3];
        if (hbl === -1) {
          let e = this._hullStart;
          do {
            if (this._hullTri[e] === bl3) {
              this._hullTri[e] = a3;
              break;
            }
            e = this._hullPrev[e];
          } while (e !== this._hullStart);
        }
        this._link(a3, hbl);
        this._link(b10, halfedges[ar]);
        this._link(ar, bl3);
        const br3 = b02 + (b10 + 1) % 3;
        if (i < EDGE_STACK.length) {
          EDGE_STACK[i++] = br3;
        }
      } else {
        if (i === 0) break;
        a3 = EDGE_STACK[--i];
      }
    }
    return ar;
  }
  /**
   * Link two half-edges to each other.
   * @param {number} a
   * @param {number} b
   * @private
   */
  _link(a3, b10) {
    this._halfedges[a3] = b10;
    if (b10 !== -1) this._halfedges[b10] = a3;
  }
  /**
   * Add a new triangle given vertex indices and adjacent half-edge ids.
   *
   * @param {number} i0
   * @param {number} i1
   * @param {number} i2
   * @param {number} a
   * @param {number} b
   * @param {number} c
   * @private
   */
  _addTriangle(i0, i1, i2, a3, b10, c3) {
    const t = this.trianglesLen;
    this._triangles[t] = i0;
    this._triangles[t + 1] = i1;
    this._triangles[t + 2] = i2;
    this._link(t, a3);
    this._link(t + 1, b10);
    this._link(t + 2, c3);
    this.trianglesLen += 3;
    return t;
  }
};
function pseudoAngle(dx, dy) {
  const p = dx / (Math.abs(dx) + Math.abs(dy));
  return (dy > 0 ? 3 - p : 1 + p) / 4;
}
function dist(ax, ay, bx, by) {
  const dx = ax - bx;
  const dy = ay - by;
  return dx * dx + dy * dy;
}
function inCircle(ax, ay, bx, by, cx, cy, px3, py3) {
  const dx = ax - px3;
  const dy = ay - py3;
  const ex = bx - px3;
  const ey = by - py3;
  const fx = cx - px3;
  const fy = cy - py3;
  const ap = dx * dx + dy * dy;
  const bp = ex * ex + ey * ey;
  const cp = fx * fx + fy * fy;
  return dx * (ey * cp - bp * fy) - dy * (ex * cp - bp * fx) + ap * (ex * fy - ey * fx) < 0;
}
function circumradius(ax, ay, bx, by, cx, cy) {
  const dx = bx - ax;
  const dy = by - ay;
  const ex = cx - ax;
  const ey = cy - ay;
  const bl3 = dx * dx + dy * dy;
  const cl = ex * ex + ey * ey;
  const d = 0.5 / (dx * ey - dy * ex);
  const x6 = (ey * bl3 - dy * cl) * d;
  const y6 = (dx * cl - ex * bl3) * d;
  return x6 * x6 + y6 * y6;
}
function circumcenter(ax, ay, bx, by, cx, cy) {
  const dx = bx - ax;
  const dy = by - ay;
  const ex = cx - ax;
  const ey = cy - ay;
  const bl3 = dx * dx + dy * dy;
  const cl = ex * ex + ey * ey;
  const d = 0.5 / (dx * ey - dy * ex);
  const x6 = ax + (ey * bl3 - dy * cl) * d;
  const y6 = ay + (dx * cl - ex * bl3) * d;
  return { x: x6, y: y6 };
}
function quicksort(ids, dists, left, right) {
  if (right - left <= 20) {
    for (let i = left + 1; i <= right; i++) {
      const temp3 = ids[i];
      const tempDist = dists[temp3];
      let j = i - 1;
      while (j >= left && dists[ids[j]] > tempDist) ids[j + 1] = ids[j--];
      ids[j + 1] = temp3;
    }
  } else {
    const median2 = left + right >> 1;
    let i = left + 1;
    let j = right;
    swap2(ids, median2, i);
    if (dists[ids[left]] > dists[ids[right]]) swap2(ids, left, right);
    if (dists[ids[i]] > dists[ids[right]]) swap2(ids, i, right);
    if (dists[ids[left]] > dists[ids[i]]) swap2(ids, left, i);
    const temp3 = ids[i];
    const tempDist = dists[temp3];
    while (true) {
      do
        i++;
      while (dists[ids[i]] < tempDist);
      do
        j--;
      while (dists[ids[j]] > tempDist);
      if (j < i) break;
      swap2(ids, i, j);
    }
    ids[left + 1] = ids[j];
    ids[j] = temp3;
    if (right - i + 1 >= j - left) {
      quicksort(ids, dists, i, right);
      quicksort(ids, dists, left, j - 1);
    } else {
      quicksort(ids, dists, left, j - 1);
      quicksort(ids, dists, i, right);
    }
  }
}
function swap2(arr, i, j) {
  const tmp = arr[i];
  arr[i] = arr[j];
  arr[j] = tmp;
}
function defaultGetX(p) {
  return p[0];
}
function defaultGetY(p) {
  return p[1];
}

// ../deepseek-harness/node_modules/.pnpm/d3-delaunay@6.0.4/node_modules/d3-delaunay/src/path.js
var epsilon7 = 1e-6;
var Path2 = class {
  constructor() {
    this._x0 = this._y0 = // start of current subpath
    this._x1 = this._y1 = null;
    this._ = "";
  }
  moveTo(x6, y6) {
    this._ += `M${this._x0 = this._x1 = +x6},${this._y0 = this._y1 = +y6}`;
  }
  closePath() {
    if (this._x1 !== null) {
      this._x1 = this._x0, this._y1 = this._y0;
      this._ += "Z";
    }
  }
  lineTo(x6, y6) {
    this._ += `L${this._x1 = +x6},${this._y1 = +y6}`;
  }
  arc(x6, y6, r) {
    x6 = +x6, y6 = +y6, r = +r;
    const x06 = x6 + r;
    const y06 = y6;
    if (r < 0) throw new Error("negative radius");
    if (this._x1 === null) this._ += `M${x06},${y06}`;
    else if (Math.abs(this._x1 - x06) > epsilon7 || Math.abs(this._y1 - y06) > epsilon7) this._ += "L" + x06 + "," + y06;
    if (!r) return;
    this._ += `A${r},${r},0,1,1,${x6 - r},${y6}A${r},${r},0,1,1,${this._x1 = x06},${this._y1 = y06}`;
  }
  rect(x6, y6, w3, h3) {
    this._ += `M${this._x0 = this._x1 = +x6},${this._y0 = this._y1 = +y6}h${+w3}v${+h3}h${-w3}Z`;
  }
  value() {
    return this._ || null;
  }
};

// ../deepseek-harness/node_modules/.pnpm/d3-delaunay@6.0.4/node_modules/d3-delaunay/src/polygon.js
var Polygon = class {
  constructor() {
    this._ = [];
  }
  moveTo(x6, y6) {
    this._.push([x6, y6]);
  }
  closePath() {
    this._.push(this._[0].slice());
  }
  lineTo(x6, y6) {
    this._.push([x6, y6]);
  }
  value() {
    return this._.length ? this._ : null;
  }
};

// ../deepseek-harness/node_modules/.pnpm/d3-delaunay@6.0.4/node_modules/d3-delaunay/src/voronoi.js
var Voronoi = class {
  constructor(delaunay, [xmin, ymin, xmax, ymax] = [0, 0, 960, 500]) {
    if (!((xmax = +xmax) >= (xmin = +xmin)) || !((ymax = +ymax) >= (ymin = +ymin))) throw new Error("invalid bounds");
    this.delaunay = delaunay;
    this._circumcenters = new Float64Array(delaunay.points.length * 2);
    this.vectors = new Float64Array(delaunay.points.length * 2);
    this.xmax = xmax, this.xmin = xmin;
    this.ymax = ymax, this.ymin = ymin;
    this._init();
  }
  update() {
    this.delaunay.update();
    this._init();
    return this;
  }
  _init() {
    const { delaunay: { points: points2, hull, triangles }, vectors } = this;
    let bx, by;
    const circumcenters = this.circumcenters = this._circumcenters.subarray(0, triangles.length / 3 * 2);
    for (let i = 0, j = 0, n = triangles.length, x6, y6; i < n; i += 3, j += 2) {
      const t13 = triangles[i] * 2;
      const t22 = triangles[i + 1] * 2;
      const t32 = triangles[i + 2] * 2;
      const x13 = points2[t13];
      const y13 = points2[t13 + 1];
      const x22 = points2[t22];
      const y22 = points2[t22 + 1];
      const x32 = points2[t32];
      const y32 = points2[t32 + 1];
      const dx = x22 - x13;
      const dy = y22 - y13;
      const ex = x32 - x13;
      const ey = y32 - y13;
      const ab4 = (dx * ey - dy * ex) * 2;
      if (Math.abs(ab4) < 1e-9) {
        if (bx === void 0) {
          bx = by = 0;
          for (const i2 of hull) bx += points2[i2 * 2], by += points2[i2 * 2 + 1];
          bx /= hull.length, by /= hull.length;
        }
        const a3 = 1e9 * Math.sign((bx - x13) * ey - (by - y13) * ex);
        x6 = (x13 + x32) / 2 - a3 * ey;
        y6 = (y13 + y32) / 2 + a3 * ex;
      } else {
        const d = 1 / ab4;
        const bl3 = dx * dx + dy * dy;
        const cl = ex * ex + ey * ey;
        x6 = x13 + (ey * bl3 - dy * cl) * d;
        y6 = y13 + (dx * cl - ex * bl3) * d;
      }
      circumcenters[j] = x6;
      circumcenters[j + 1] = y6;
    }
    let h3 = hull[hull.length - 1];
    let p02, p1 = h3 * 4;
    let x06, x12 = points2[2 * h3];
    let y06, y12 = points2[2 * h3 + 1];
    vectors.fill(0);
    for (let i = 0; i < hull.length; ++i) {
      h3 = hull[i];
      p02 = p1, x06 = x12, y06 = y12;
      p1 = h3 * 4, x12 = points2[2 * h3], y12 = points2[2 * h3 + 1];
      vectors[p02 + 2] = vectors[p1] = y06 - y12;
      vectors[p02 + 3] = vectors[p1 + 1] = x12 - x06;
    }
  }
  render(context4) {
    const buffer = context4 == null ? context4 = new Path2() : void 0;
    const { delaunay: { halfedges, inedges, hull }, circumcenters, vectors } = this;
    if (hull.length <= 1) return null;
    for (let i = 0, n = halfedges.length; i < n; ++i) {
      const j = halfedges[i];
      if (j < i) continue;
      const ti = Math.floor(i / 3) * 2;
      const tj = Math.floor(j / 3) * 2;
      const xi = circumcenters[ti];
      const yi = circumcenters[ti + 1];
      const xj = circumcenters[tj];
      const yj = circumcenters[tj + 1];
      this._renderSegment(xi, yi, xj, yj, context4);
    }
    let h0, h1 = hull[hull.length - 1];
    for (let i = 0; i < hull.length; ++i) {
      h0 = h1, h1 = hull[i];
      const t = Math.floor(inedges[h1] / 3) * 2;
      const x6 = circumcenters[t];
      const y6 = circumcenters[t + 1];
      const v2 = h0 * 4;
      const p = this._project(x6, y6, vectors[v2 + 2], vectors[v2 + 3]);
      if (p) this._renderSegment(x6, y6, p[0], p[1], context4);
    }
    return buffer && buffer.value();
  }
  renderBounds(context4) {
    const buffer = context4 == null ? context4 = new Path2() : void 0;
    context4.rect(this.xmin, this.ymin, this.xmax - this.xmin, this.ymax - this.ymin);
    return buffer && buffer.value();
  }
  renderCell(i, context4) {
    const buffer = context4 == null ? context4 = new Path2() : void 0;
    const points2 = this._clip(i);
    if (points2 === null || !points2.length) return;
    context4.moveTo(points2[0], points2[1]);
    let n = points2.length;
    while (points2[0] === points2[n - 2] && points2[1] === points2[n - 1] && n > 1) n -= 2;
    for (let i2 = 2; i2 < n; i2 += 2) {
      if (points2[i2] !== points2[i2 - 2] || points2[i2 + 1] !== points2[i2 - 1])
        context4.lineTo(points2[i2], points2[i2 + 1]);
    }
    context4.closePath();
    return buffer && buffer.value();
  }
  *cellPolygons() {
    const { delaunay: { points: points2 } } = this;
    for (let i = 0, n = points2.length / 2; i < n; ++i) {
      const cell2 = this.cellPolygon(i);
      if (cell2) cell2.index = i, yield cell2;
    }
  }
  cellPolygon(i) {
    const polygon = new Polygon();
    this.renderCell(i, polygon);
    return polygon.value();
  }
  _renderSegment(x06, y06, x12, y12, context4) {
    let S;
    const c0 = this._regioncode(x06, y06);
    const c1 = this._regioncode(x12, y12);
    if (c0 === 0 && c1 === 0) {
      context4.moveTo(x06, y06);
      context4.lineTo(x12, y12);
    } else if (S = this._clipSegment(x06, y06, x12, y12, c0, c1)) {
      context4.moveTo(S[0], S[1]);
      context4.lineTo(S[2], S[3]);
    }
  }
  contains(i, x6, y6) {
    if ((x6 = +x6, x6 !== x6) || (y6 = +y6, y6 !== y6)) return false;
    return this.delaunay._step(i, x6, y6) === i;
  }
  *neighbors(i) {
    const ci = this._clip(i);
    if (ci) for (const j of this.delaunay.neighbors(i)) {
      const cj = this._clip(j);
      if (cj) loop: for (let ai = 0, li = ci.length; ai < li; ai += 2) {
        for (let aj = 0, lj = cj.length; aj < lj; aj += 2) {
          if (ci[ai] === cj[aj] && ci[ai + 1] === cj[aj + 1] && ci[(ai + 2) % li] === cj[(aj + lj - 2) % lj] && ci[(ai + 3) % li] === cj[(aj + lj - 1) % lj]) {
            yield j;
            break loop;
          }
        }
      }
    }
  }
  _cell(i) {
    const { circumcenters, delaunay: { inedges, halfedges, triangles } } = this;
    const e0 = inedges[i];
    if (e0 === -1) return null;
    const points2 = [];
    let e = e0;
    do {
      const t = Math.floor(e / 3);
      points2.push(circumcenters[t * 2], circumcenters[t * 2 + 1]);
      e = e % 3 === 2 ? e - 2 : e + 1;
      if (triangles[e] !== i) break;
      e = halfedges[e];
    } while (e !== e0 && e !== -1);
    return points2;
  }
  _clip(i) {
    if (i === 0 && this.delaunay.hull.length === 1) {
      return [this.xmax, this.ymin, this.xmax, this.ymax, this.xmin, this.ymax, this.xmin, this.ymin];
    }
    const points2 = this._cell(i);
    if (points2 === null) return null;
    const { vectors: V } = this;
    const v2 = i * 4;
    return this._simplify(V[v2] || V[v2 + 1] ? this._clipInfinite(i, points2, V[v2], V[v2 + 1], V[v2 + 2], V[v2 + 3]) : this._clipFinite(i, points2));
  }
  _clipFinite(i, points2) {
    const n = points2.length;
    let P = null;
    let x06, y06, x12 = points2[n - 2], y12 = points2[n - 1];
    let c0, c1 = this._regioncode(x12, y12);
    let e0, e1 = 0;
    for (let j = 0; j < n; j += 2) {
      x06 = x12, y06 = y12, x12 = points2[j], y12 = points2[j + 1];
      c0 = c1, c1 = this._regioncode(x12, y12);
      if (c0 === 0 && c1 === 0) {
        e0 = e1, e1 = 0;
        if (P) P.push(x12, y12);
        else P = [x12, y12];
      } else {
        let S, sx0, sy0, sx1, sy1;
        if (c0 === 0) {
          if ((S = this._clipSegment(x06, y06, x12, y12, c0, c1)) === null) continue;
          [sx0, sy0, sx1, sy1] = S;
        } else {
          if ((S = this._clipSegment(x12, y12, x06, y06, c1, c0)) === null) continue;
          [sx1, sy1, sx0, sy0] = S;
          e0 = e1, e1 = this._edgecode(sx0, sy0);
          if (e0 && e1) this._edge(i, e0, e1, P, P.length);
          if (P) P.push(sx0, sy0);
          else P = [sx0, sy0];
        }
        e0 = e1, e1 = this._edgecode(sx1, sy1);
        if (e0 && e1) this._edge(i, e0, e1, P, P.length);
        if (P) P.push(sx1, sy1);
        else P = [sx1, sy1];
      }
    }
    if (P) {
      e0 = e1, e1 = this._edgecode(P[0], P[1]);
      if (e0 && e1) this._edge(i, e0, e1, P, P.length);
    } else if (this.contains(i, (this.xmin + this.xmax) / 2, (this.ymin + this.ymax) / 2)) {
      return [this.xmax, this.ymin, this.xmax, this.ymax, this.xmin, this.ymax, this.xmin, this.ymin];
    }
    return P;
  }
  _clipSegment(x06, y06, x12, y12, c0, c1) {
    const flip2 = c0 < c1;
    if (flip2) [x06, y06, x12, y12, c0, c1] = [x12, y12, x06, y06, c1, c0];
    while (true) {
      if (c0 === 0 && c1 === 0) return flip2 ? [x12, y12, x06, y06] : [x06, y06, x12, y12];
      if (c0 & c1) return null;
      let x6, y6, c3 = c0 || c1;
      if (c3 & 8) x6 = x06 + (x12 - x06) * (this.ymax - y06) / (y12 - y06), y6 = this.ymax;
      else if (c3 & 4) x6 = x06 + (x12 - x06) * (this.ymin - y06) / (y12 - y06), y6 = this.ymin;
      else if (c3 & 2) y6 = y06 + (y12 - y06) * (this.xmax - x06) / (x12 - x06), x6 = this.xmax;
      else y6 = y06 + (y12 - y06) * (this.xmin - x06) / (x12 - x06), x6 = this.xmin;
      if (c0) x06 = x6, y06 = y6, c0 = this._regioncode(x06, y06);
      else x12 = x6, y12 = y6, c1 = this._regioncode(x12, y12);
    }
  }
  _clipInfinite(i, points2, vx0, vy0, vxn, vyn) {
    let P = Array.from(points2), p;
    if (p = this._project(P[0], P[1], vx0, vy0)) P.unshift(p[0], p[1]);
    if (p = this._project(P[P.length - 2], P[P.length - 1], vxn, vyn)) P.push(p[0], p[1]);
    if (P = this._clipFinite(i, P)) {
      for (let j = 0, n = P.length, c0, c1 = this._edgecode(P[n - 2], P[n - 1]); j < n; j += 2) {
        c0 = c1, c1 = this._edgecode(P[j], P[j + 1]);
        if (c0 && c1) j = this._edge(i, c0, c1, P, j), n = P.length;
      }
    } else if (this.contains(i, (this.xmin + this.xmax) / 2, (this.ymin + this.ymax) / 2)) {
      P = [this.xmin, this.ymin, this.xmax, this.ymin, this.xmax, this.ymax, this.xmin, this.ymax];
    }
    return P;
  }
  _edge(i, e0, e1, P, j) {
    while (e0 !== e1) {
      let x6, y6;
      switch (e0) {
        case 5:
          e0 = 4;
          continue;
        // top-left
        case 4:
          e0 = 6, x6 = this.xmax, y6 = this.ymin;
          break;
        // top
        case 6:
          e0 = 2;
          continue;
        // top-right
        case 2:
          e0 = 10, x6 = this.xmax, y6 = this.ymax;
          break;
        // right
        case 10:
          e0 = 8;
          continue;
        // bottom-right
        case 8:
          e0 = 9, x6 = this.xmin, y6 = this.ymax;
          break;
        // bottom
        case 9:
          e0 = 1;
          continue;
        // bottom-left
        case 1:
          e0 = 5, x6 = this.xmin, y6 = this.ymin;
          break;
      }
      if ((P[j] !== x6 || P[j + 1] !== y6) && this.contains(i, x6, y6)) {
        P.splice(j, 0, x6, y6), j += 2;
      }
    }
    return j;
  }
  _project(x06, y06, vx, vy) {
    let t = Infinity, c3, x6, y6;
    if (vy < 0) {
      if (y06 <= this.ymin) return null;
      if ((c3 = (this.ymin - y06) / vy) < t) y6 = this.ymin, x6 = x06 + (t = c3) * vx;
    } else if (vy > 0) {
      if (y06 >= this.ymax) return null;
      if ((c3 = (this.ymax - y06) / vy) < t) y6 = this.ymax, x6 = x06 + (t = c3) * vx;
    }
    if (vx > 0) {
      if (x06 >= this.xmax) return null;
      if ((c3 = (this.xmax - x06) / vx) < t) x6 = this.xmax, y6 = y06 + (t = c3) * vy;
    } else if (vx < 0) {
      if (x06 <= this.xmin) return null;
      if ((c3 = (this.xmin - x06) / vx) < t) x6 = this.xmin, y6 = y06 + (t = c3) * vy;
    }
    return [x6, y6];
  }
  _edgecode(x6, y6) {
    return (x6 === this.xmin ? 1 : x6 === this.xmax ? 2 : 0) | (y6 === this.ymin ? 4 : y6 === this.ymax ? 8 : 0);
  }
  _regioncode(x6, y6) {
    return (x6 < this.xmin ? 1 : x6 > this.xmax ? 2 : 0) | (y6 < this.ymin ? 4 : y6 > this.ymax ? 8 : 0);
  }
  _simplify(P) {
    if (P && P.length > 4) {
      for (let i = 0; i < P.length; i += 2) {
        const j = (i + 2) % P.length, k = (i + 4) % P.length;
        if (P[i] === P[j] && P[j] === P[k] || P[i + 1] === P[j + 1] && P[j + 1] === P[k + 1]) {
          P.splice(j, 2), i -= 2;
        }
      }
      if (!P.length) P = null;
    }
    return P;
  }
};

// ../deepseek-harness/node_modules/.pnpm/d3-delaunay@6.0.4/node_modules/d3-delaunay/src/delaunay.js
var tau5 = 2 * Math.PI;
var pow5 = Math.pow;
function pointX(p) {
  return p[0];
}
function pointY(p) {
  return p[1];
}
function collinear2(d) {
  const { triangles, coords } = d;
  for (let i = 0; i < triangles.length; i += 3) {
    const a3 = 2 * triangles[i], b10 = 2 * triangles[i + 1], c3 = 2 * triangles[i + 2], cross2 = (coords[c3] - coords[a3]) * (coords[b10 + 1] - coords[a3 + 1]) - (coords[b10] - coords[a3]) * (coords[c3 + 1] - coords[a3 + 1]);
    if (cross2 > 1e-10) return false;
  }
  return true;
}
function jitter(x6, y6, r) {
  return [x6 + Math.sin(x6 + y6) * r, y6 + Math.cos(x6 - y6) * r];
}
var Delaunay = class _Delaunay {
  static from(points2, fx = pointX, fy = pointY, that) {
    return new _Delaunay("length" in points2 ? flatArray(points2, fx, fy, that) : Float64Array.from(flatIterable(points2, fx, fy, that)));
  }
  constructor(points2) {
    this._delaunator = new Delaunator(points2);
    this.inedges = new Int32Array(points2.length / 2);
    this._hullIndex = new Int32Array(points2.length / 2);
    this.points = this._delaunator.coords;
    this._init();
  }
  update() {
    this._delaunator.update();
    this._init();
    return this;
  }
  _init() {
    const d = this._delaunator, points2 = this.points;
    if (d.hull && d.hull.length > 2 && collinear2(d)) {
      this.collinear = Int32Array.from({ length: points2.length / 2 }, (_, i) => i).sort((i, j) => points2[2 * i] - points2[2 * j] || points2[2 * i + 1] - points2[2 * j + 1]);
      const e = this.collinear[0], f = this.collinear[this.collinear.length - 1], bounds3 = [points2[2 * e], points2[2 * e + 1], points2[2 * f], points2[2 * f + 1]], r = 1e-8 * Math.hypot(bounds3[3] - bounds3[1], bounds3[2] - bounds3[0]);
      for (let i = 0, n = points2.length / 2; i < n; ++i) {
        const p = jitter(points2[2 * i], points2[2 * i + 1], r);
        points2[2 * i] = p[0];
        points2[2 * i + 1] = p[1];
      }
      this._delaunator = new Delaunator(points2);
    } else {
      delete this.collinear;
    }
    const halfedges = this.halfedges = this._delaunator.halfedges;
    const hull = this.hull = this._delaunator.hull;
    const triangles = this.triangles = this._delaunator.triangles;
    const inedges = this.inedges.fill(-1);
    const hullIndex = this._hullIndex.fill(-1);
    for (let e = 0, n = halfedges.length; e < n; ++e) {
      const p = triangles[e % 3 === 2 ? e - 2 : e + 1];
      if (halfedges[e] === -1 || inedges[p] === -1) inedges[p] = e;
    }
    for (let i = 0, n = hull.length; i < n; ++i) {
      hullIndex[hull[i]] = i;
    }
    if (hull.length <= 2 && hull.length > 0) {
      this.triangles = new Int32Array(3).fill(-1);
      this.halfedges = new Int32Array(3).fill(-1);
      this.triangles[0] = hull[0];
      inedges[hull[0]] = 1;
      if (hull.length === 2) {
        inedges[hull[1]] = 0;
        this.triangles[1] = hull[1];
        this.triangles[2] = hull[1];
      }
    }
  }
  voronoi(bounds3) {
    return new Voronoi(this, bounds3);
  }
  *neighbors(i) {
    const { inedges, hull, _hullIndex, halfedges, triangles, collinear: collinear3 } = this;
    if (collinear3) {
      const l = collinear3.indexOf(i);
      if (l > 0) yield collinear3[l - 1];
      if (l < collinear3.length - 1) yield collinear3[l + 1];
      return;
    }
    const e0 = inedges[i];
    if (e0 === -1) return;
    let e = e0, p02 = -1;
    do {
      yield p02 = triangles[e];
      e = e % 3 === 2 ? e - 2 : e + 1;
      if (triangles[e] !== i) return;
      e = halfedges[e];
      if (e === -1) {
        const p = hull[(_hullIndex[i] + 1) % hull.length];
        if (p !== p02) yield p;
        return;
      }
    } while (e !== e0);
  }
  find(x6, y6, i = 0) {
    if ((x6 = +x6, x6 !== x6) || (y6 = +y6, y6 !== y6)) return -1;
    const i0 = i;
    let c3;
    while ((c3 = this._step(i, x6, y6)) >= 0 && c3 !== i && c3 !== i0) i = c3;
    return c3;
  }
  _step(i, x6, y6) {
    const { inedges, hull, _hullIndex, halfedges, triangles, points: points2 } = this;
    if (inedges[i] === -1 || !points2.length) return (i + 1) % (points2.length >> 1);
    let c3 = i;
    let dc = pow5(x6 - points2[i * 2], 2) + pow5(y6 - points2[i * 2 + 1], 2);
    const e0 = inedges[i];
    let e = e0;
    do {
      let t = triangles[e];
      const dt = pow5(x6 - points2[t * 2], 2) + pow5(y6 - points2[t * 2 + 1], 2);
      if (dt < dc) dc = dt, c3 = t;
      e = e % 3 === 2 ? e - 2 : e + 1;
      if (triangles[e] !== i) break;
      e = halfedges[e];
      if (e === -1) {
        e = hull[(_hullIndex[i] + 1) % hull.length];
        if (e !== t) {
          if (pow5(x6 - points2[e * 2], 2) + pow5(y6 - points2[e * 2 + 1], 2) < dc) return e;
        }
        break;
      }
    } while (e !== e0);
    return c3;
  }
  render(context4) {
    const buffer = context4 == null ? context4 = new Path2() : void 0;
    const { points: points2, halfedges, triangles } = this;
    for (let i = 0, n = halfedges.length; i < n; ++i) {
      const j = halfedges[i];
      if (j < i) continue;
      const ti = triangles[i] * 2;
      const tj = triangles[j] * 2;
      context4.moveTo(points2[ti], points2[ti + 1]);
      context4.lineTo(points2[tj], points2[tj + 1]);
    }
    this.renderHull(context4);
    return buffer && buffer.value();
  }
  renderPoints(context4, r) {
    if (r === void 0 && (!context4 || typeof context4.moveTo !== "function")) r = context4, context4 = null;
    r = r == void 0 ? 2 : +r;
    const buffer = context4 == null ? context4 = new Path2() : void 0;
    const { points: points2 } = this;
    for (let i = 0, n = points2.length; i < n; i += 2) {
      const x6 = points2[i], y6 = points2[i + 1];
      context4.moveTo(x6 + r, y6);
      context4.arc(x6, y6, r, 0, tau5);
    }
    return buffer && buffer.value();
  }
  renderHull(context4) {
    const buffer = context4 == null ? context4 = new Path2() : void 0;
    const { hull, points: points2 } = this;
    const h3 = hull[0] * 2, n = hull.length;
    context4.moveTo(points2[h3], points2[h3 + 1]);
    for (let i = 1; i < n; ++i) {
      const h4 = 2 * hull[i];
      context4.lineTo(points2[h4], points2[h4 + 1]);
    }
    context4.closePath();
    return buffer && buffer.value();
  }
  hullPolygon() {
    const polygon = new Polygon();
    this.renderHull(polygon);
    return polygon.value();
  }
  renderTriangle(i, context4) {
    const buffer = context4 == null ? context4 = new Path2() : void 0;
    const { points: points2, triangles } = this;
    const t05 = triangles[i *= 3] * 2;
    const t13 = triangles[i + 1] * 2;
    const t22 = triangles[i + 2] * 2;
    context4.moveTo(points2[t05], points2[t05 + 1]);
    context4.lineTo(points2[t13], points2[t13 + 1]);
    context4.lineTo(points2[t22], points2[t22 + 1]);
    context4.closePath();
    return buffer && buffer.value();
  }
  *trianglePolygons() {
    const { triangles } = this;
    for (let i = 0, n = triangles.length / 3; i < n; ++i) {
      yield this.trianglePolygon(i);
    }
  }
  trianglePolygon(i) {
    const polygon = new Polygon();
    this.renderTriangle(i, polygon);
    return polygon.value();
  }
};
function flatArray(points2, fx, fy, that) {
  const n = points2.length;
  const array4 = new Float64Array(n * 2);
  for (let i = 0; i < n; ++i) {
    const p = points2[i];
    array4[i * 2] = fx.call(that, p, i, points2);
    array4[i * 2 + 1] = fy.call(that, p, i, points2);
  }
  return array4;
}
function* flatIterable(points2, fx, fy, that) {
  let i = 0;
  for (const p of points2) {
    yield fx.call(that, p, i, points2);
    yield fy.call(that, p, i, points2);
    ++i;
  }
}

// ../deepseek-harness/node_modules/.pnpm/vega-voronoi@5.1.3/node_modules/vega-voronoi/build/vega-voronoi.js
function Voronoi2(params2) {
  Transform.call(this, null, params2);
}
Voronoi2.Definition = {
  "type": "Voronoi",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "x",
    "type": "field",
    "required": true
  }, {
    "name": "y",
    "type": "field",
    "required": true
  }, {
    "name": "size",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "extent",
    "type": "array",
    "array": true,
    "length": 2,
    "default": [[-1e5, -1e5], [1e5, 1e5]],
    "content": {
      "type": "number",
      "array": true,
      "length": 2
    }
  }, {
    "name": "as",
    "type": "string",
    "default": "path"
  }]
};
var defaultExtent = [-1e5, -1e5, 1e5, 1e5];
inherits_default(Voronoi2, Transform, {
  transform(_, pulse2) {
    const as = _.as || "path", data4 = pulse2.source;
    if (!data4 || !data4.length) return pulse2;
    let s = _.size;
    s = s ? [0, 0, s[0], s[1]] : (s = _.extent) ? [s[0][0], s[0][1], s[1][0], s[1][1]] : defaultExtent;
    const voronoi = this.value = Delaunay.from(data4, _.x, _.y).voronoi(s);
    for (let i = 0, n = data4.length; i < n; ++i) {
      const polygon = voronoi.cellPolygon(i);
      data4[i][as] = polygon && !isPoint(polygon) ? toPathString(polygon) : null;
    }
    return pulse2.reflow(_.modified()).modifies(as);
  }
});
function toPathString(p) {
  const x6 = p[0][0], y6 = p[0][1];
  let n = p.length - 1;
  for (; p[n][0] === x6 && p[n][1] === y6; --n) ;
  return "M" + p.slice(0, n + 1).join("L") + "Z";
}
function isPoint(p) {
  return p.length === 2 && p[0][0] === p[1][0] && p[0][1] === p[1][1];
}

// ../deepseek-harness/node_modules/.pnpm/vega-wordcloud@5.1.3/node_modules/vega-wordcloud/build/vega-wordcloud.js
var vega_wordcloud_exports = {};
__export(vega_wordcloud_exports, {
  wordcloud: () => Wordcloud
});
var cloudRadians = Math.PI / 180;
var cw = 1 << 11 >> 5;
var ch = 1 << 11;
function cloud() {
  var size = [256, 256], text4, font3, fontSize3, fontStyle, fontWeight, rotate3, padding3, spiral = archimedeanSpiral, words = [], random2 = Math.random, cloud2 = {};
  cloud2.layout = function() {
    var contextAndRatio = getContext2(domCanvas()), board = zeroArray((size[0] >> 5) * size[1]), bounds3 = null, n = words.length, i = -1, tags = [], data4 = words.map((d2) => ({
      text: text4(d2),
      font: font3(d2),
      style: fontStyle(d2),
      weight: fontWeight(d2),
      rotate: rotate3(d2),
      size: ~~(fontSize3(d2) + 1e-14),
      padding: padding3(d2),
      xoff: 0,
      yoff: 0,
      x1: 0,
      y1: 0,
      x0: 0,
      y0: 0,
      hasText: false,
      sprite: null,
      datum: d2
    })).sort((a3, b10) => b10.size - a3.size);
    while (++i < n) {
      var d = data4[i];
      d.x = size[0] * (random2() + 0.5) >> 1;
      d.y = size[1] * (random2() + 0.5) >> 1;
      cloudSprite(contextAndRatio, d, data4, i);
      if (d.hasText && place2(board, d, bounds3)) {
        tags.push(d);
        if (bounds3) cloudBounds(bounds3, d);
        else bounds3 = [{
          x: d.x + d.x0,
          y: d.y + d.y0
        }, {
          x: d.x + d.x1,
          y: d.y + d.y1
        }];
        d.x -= size[0] >> 1;
        d.y -= size[1] >> 1;
      }
    }
    return tags;
  };
  function getContext2(canvas) {
    canvas.width = canvas.height = 1;
    var ratio = Math.sqrt(canvas.getContext("2d").getImageData(0, 0, 1, 1).data.length >> 2);
    canvas.width = (cw << 5) / ratio;
    canvas.height = ch / ratio;
    var context4 = canvas.getContext("2d");
    context4.fillStyle = context4.strokeStyle = "red";
    context4.textAlign = "center";
    return {
      context: context4,
      ratio
    };
  }
  function place2(board, tag, bounds3) {
    var startX = tag.x, startY = tag.y, maxDelta = Math.hypot(size[0], size[1]), s = spiral(size), dt = random2() < 0.5 ? 1 : -1, t = -dt, dxdy, dx, dy;
    while (dxdy = s(t += dt)) {
      dx = ~~dxdy[0];
      dy = ~~dxdy[1];
      if (Math.min(Math.abs(dx), Math.abs(dy)) >= maxDelta) break;
      tag.x = startX + dx;
      tag.y = startY + dy;
      if (tag.x + tag.x0 < 0 || tag.y + tag.y0 < 0 || tag.x + tag.x1 > size[0] || tag.y + tag.y1 > size[1]) continue;
      if (!bounds3 || !cloudCollide(tag, board, size[0])) {
        if (!bounds3 || collideRects(tag, bounds3)) {
          var sprite = tag.sprite, w3 = tag.width >> 5, sw = size[0] >> 5, lx3 = tag.x - (w3 << 4), sx = lx3 & 127, msx = 32 - sx, h3 = tag.y1 - tag.y0, x6 = (tag.y + tag.y0) * sw + (lx3 >> 5), last;
          for (var j = 0; j < h3; j++) {
            last = 0;
            for (var i = 0; i <= w3; i++) {
              board[x6 + i] |= last << msx | (i < w3 ? (last = sprite[j * w3 + i]) >>> sx : 0);
            }
            x6 += sw;
          }
          tag.sprite = null;
          return true;
        }
      }
    }
    return false;
  }
  cloud2.words = function(_) {
    if (arguments.length) {
      words = _;
      return cloud2;
    } else {
      return words;
    }
  };
  cloud2.size = function(_) {
    if (arguments.length) {
      size = [+_[0], +_[1]];
      return cloud2;
    } else {
      return size;
    }
  };
  cloud2.font = function(_) {
    if (arguments.length) {
      font3 = functor(_);
      return cloud2;
    } else {
      return font3;
    }
  };
  cloud2.fontStyle = function(_) {
    if (arguments.length) {
      fontStyle = functor(_);
      return cloud2;
    } else {
      return fontStyle;
    }
  };
  cloud2.fontWeight = function(_) {
    if (arguments.length) {
      fontWeight = functor(_);
      return cloud2;
    } else {
      return fontWeight;
    }
  };
  cloud2.rotate = function(_) {
    if (arguments.length) {
      rotate3 = functor(_);
      return cloud2;
    } else {
      return rotate3;
    }
  };
  cloud2.text = function(_) {
    if (arguments.length) {
      text4 = functor(_);
      return cloud2;
    } else {
      return text4;
    }
  };
  cloud2.spiral = function(_) {
    if (arguments.length) {
      spiral = spirals[_] || _;
      return cloud2;
    } else {
      return spiral;
    }
  };
  cloud2.fontSize = function(_) {
    if (arguments.length) {
      fontSize3 = functor(_);
      return cloud2;
    } else {
      return fontSize3;
    }
  };
  cloud2.padding = function(_) {
    if (arguments.length) {
      padding3 = functor(_);
      return cloud2;
    } else {
      return padding3;
    }
  };
  cloud2.random = function(_) {
    if (arguments.length) {
      random2 = _;
      return cloud2;
    } else {
      return random2;
    }
  };
  return cloud2;
}
function cloudSprite(contextAndRatio, d, data4, di) {
  if (d.sprite) return;
  var c3 = contextAndRatio.context, ratio = contextAndRatio.ratio;
  c3.clearRect(0, 0, (cw << 5) / ratio, ch / ratio);
  var x6 = 0, y6 = 0, maxh = 0, n = data4.length, w3, w32, h3, i, j;
  --di;
  while (++di < n) {
    d = data4[di];
    c3.save();
    c3.font = d.style + " " + d.weight + " " + ~~((d.size + 1) / ratio) + "px " + d.font;
    w3 = c3.measureText(d.text + "m").width * ratio;
    h3 = d.size << 1;
    if (d.rotate) {
      var sr = Math.sin(d.rotate * cloudRadians), cr3 = Math.cos(d.rotate * cloudRadians), wcr = w3 * cr3, wsr = w3 * sr, hcr = h3 * cr3, hsr = h3 * sr;
      w3 = Math.max(Math.abs(wcr + hsr), Math.abs(wcr - hsr)) + 31 >> 5 << 5;
      h3 = ~~Math.max(Math.abs(wsr + hcr), Math.abs(wsr - hcr));
    } else {
      w3 = w3 + 31 >> 5 << 5;
    }
    if (h3 > maxh) maxh = h3;
    if (x6 + w3 >= cw << 5) {
      x6 = 0;
      y6 += maxh;
      maxh = 0;
    }
    if (y6 + h3 >= ch) break;
    c3.translate((x6 + (w3 >> 1)) / ratio, (y6 + (h3 >> 1)) / ratio);
    if (d.rotate) c3.rotate(d.rotate * cloudRadians);
    c3.fillText(d.text, 0, 0);
    if (d.padding) {
      c3.lineWidth = 2 * d.padding;
      c3.strokeText(d.text, 0, 0);
    }
    c3.restore();
    d.width = w3;
    d.height = h3;
    d.xoff = x6;
    d.yoff = y6;
    d.x1 = w3 >> 1;
    d.y1 = h3 >> 1;
    d.x0 = -d.x1;
    d.y0 = -d.y1;
    d.hasText = true;
    x6 += w3;
  }
  var pixels = c3.getImageData(0, 0, (cw << 5) / ratio, ch / ratio).data, sprite = [];
  while (--di >= 0) {
    d = data4[di];
    if (!d.hasText) continue;
    w3 = d.width;
    w32 = w3 >> 5;
    h3 = d.y1 - d.y0;
    for (i = 0; i < h3 * w32; i++) sprite[i] = 0;
    x6 = d.xoff;
    if (x6 == null) return;
    y6 = d.yoff;
    var seen = 0, seenRow = -1;
    for (j = 0; j < h3; j++) {
      for (i = 0; i < w3; i++) {
        var k = w32 * j + (i >> 5), m3 = pixels[(y6 + j) * (cw << 5) + (x6 + i) << 2] ? 1 << 31 - i % 32 : 0;
        sprite[k] |= m3;
        seen |= m3;
      }
      if (seen) seenRow = j;
      else {
        d.y0++;
        h3--;
        j--;
        y6++;
      }
    }
    d.y1 = d.y0 + seenRow;
    d.sprite = sprite.slice(0, (d.y1 - d.y0) * w32);
  }
}
function cloudCollide(tag, board, sw) {
  sw >>= 5;
  var sprite = tag.sprite, w3 = tag.width >> 5, lx3 = tag.x - (w3 << 4), sx = lx3 & 127, msx = 32 - sx, h3 = tag.y1 - tag.y0, x6 = (tag.y + tag.y0) * sw + (lx3 >> 5), last;
  for (var j = 0; j < h3; j++) {
    last = 0;
    for (var i = 0; i <= w3; i++) {
      if ((last << msx | (i < w3 ? (last = sprite[j * w3 + i]) >>> sx : 0)) & board[x6 + i]) return true;
    }
    x6 += sw;
  }
  return false;
}
function cloudBounds(bounds3, d) {
  var b02 = bounds3[0], b12 = bounds3[1];
  if (d.x + d.x0 < b02.x) b02.x = d.x + d.x0;
  if (d.y + d.y0 < b02.y) b02.y = d.y + d.y0;
  if (d.x + d.x1 > b12.x) b12.x = d.x + d.x1;
  if (d.y + d.y1 > b12.y) b12.y = d.y + d.y1;
}
function collideRects(a3, b10) {
  return a3.x + a3.x1 > b10[0].x && a3.x + a3.x0 < b10[1].x && a3.y + a3.y1 > b10[0].y && a3.y + a3.y0 < b10[1].y;
}
function archimedeanSpiral(size) {
  var e = size[0] / size[1];
  return function(t) {
    return [e * (t *= 0.1) * Math.cos(t), t * Math.sin(t)];
  };
}
function rectangularSpiral(size) {
  var dy = 4, dx = dy * size[0] / size[1], x6 = 0, y6 = 0;
  return function(t) {
    var sign3 = t < 0 ? -1 : 1;
    switch (Math.sqrt(1 + 4 * sign3 * t) - sign3 & 3) {
      case 0:
        x6 += dx;
        break;
      case 1:
        y6 += dy;
        break;
      case 2:
        x6 -= dx;
        break;
      default:
        y6 -= dy;
        break;
    }
    return [x6, y6];
  };
}
function zeroArray(n) {
  var a3 = [], i = -1;
  while (++i < n) a3[i] = 0;
  return a3;
}
function functor(d) {
  return typeof d === "function" ? d : function() {
    return d;
  };
}
var spirals = {
  archimedean: archimedeanSpiral,
  rectangular: rectangularSpiral
};
var Output4 = ["x", "y", "font", "fontSize", "fontStyle", "fontWeight", "angle"];
var Params2 = ["text", "font", "rotate", "fontSize", "fontStyle", "fontWeight"];
function Wordcloud(params2) {
  Transform.call(this, cloud(), params2);
}
Wordcloud.Definition = {
  "type": "Wordcloud",
  "metadata": {
    "modifies": true
  },
  "params": [{
    "name": "size",
    "type": "number",
    "array": true,
    "length": 2
  }, {
    "name": "font",
    "type": "string",
    "expr": true,
    "default": "sans-serif"
  }, {
    "name": "fontStyle",
    "type": "string",
    "expr": true,
    "default": "normal"
  }, {
    "name": "fontWeight",
    "type": "string",
    "expr": true,
    "default": "normal"
  }, {
    "name": "fontSize",
    "type": "number",
    "expr": true,
    "default": 14
  }, {
    "name": "fontSizeRange",
    "type": "number",
    "array": "nullable",
    "default": [10, 50]
  }, {
    "name": "rotate",
    "type": "number",
    "expr": true,
    "default": 0
  }, {
    "name": "text",
    "type": "field"
  }, {
    "name": "spiral",
    "type": "string",
    "values": ["archimedean", "rectangular"]
  }, {
    "name": "padding",
    "type": "number",
    "expr": true
  }, {
    "name": "as",
    "type": "string",
    "array": true,
    "length": 7,
    "default": Output4
  }]
};
inherits_default(Wordcloud, Transform, {
  transform(_, pulse2) {
    if (_.size && !(_.size[0] && _.size[1])) {
      error("Wordcloud size dimensions must be non-zero.");
    }
    function modp(param2) {
      const p = _[param2];
      return isFunction(p) && pulse2.modified(p.fields);
    }
    const mod = _.modified();
    if (!(mod || pulse2.changed(pulse2.ADD_REM) || Params2.some(modp))) return;
    const data4 = pulse2.materialize(pulse2.SOURCE).source, layout = this.value, as = _.as || Output4;
    let fontSize3 = _.fontSize || 14, range7;
    isFunction(fontSize3) ? range7 = _.fontSizeRange : fontSize3 = constant_default(fontSize3);
    if (range7) {
      const fsize = fontSize3, sizeScale = scale("sqrt")().domain(extent(data4, fsize)).range(range7);
      fontSize3 = (x6) => sizeScale(fsize(x6));
    }
    data4.forEach((t) => {
      t[as[0]] = NaN;
      t[as[1]] = NaN;
      t[as[3]] = 0;
    });
    const words = layout.words(data4).text(_.text).size(_.size || [500, 500]).padding(_.padding || 1).spiral(_.spiral || "archimedean").rotate(_.rotate || 0).font(_.font || "sans-serif").fontStyle(_.fontStyle || "normal").fontWeight(_.fontWeight || "normal").fontSize(fontSize3).random(random).layout();
    const size = layout.size(), dx = size[0] >> 1, dy = size[1] >> 1, n = words.length;
    for (let i = 0, w3, t; i < n; ++i) {
      w3 = words[i];
      t = w3.datum;
      t[as[0]] = w3.x + dx;
      t[as[1]] = w3.y + dy;
      t[as[2]] = w3.font;
      t[as[3]] = w3.size;
      t[as[4]] = w3.style;
      t[as[5]] = w3.weight;
      t[as[6]] = w3.rotate;
    }
    return pulse2.reflow(mod).modifies(as);
  }
});

// ../deepseek-harness/node_modules/.pnpm/vega-crossfilter@5.1.3/node_modules/vega-crossfilter/build/vega-crossfilter.js
var vega_crossfilter_exports = {};
__export(vega_crossfilter_exports, {
  crossfilter: () => CrossFilter,
  resolvefilter: () => ResolveFilter
});
var array8 = (n) => new Uint8Array(n);
var array16 = (n) => new Uint16Array(n);
var array32 = (n) => new Uint32Array(n);
function Bitmaps() {
  let width2 = 8, data4 = [], seen = array32(0), curr = array(0, width2), prev = array(0, width2);
  return {
    data: () => data4,
    seen: () => seen = lengthen(seen, data4.length),
    add(array4) {
      for (let i = 0, j = data4.length, n = array4.length, t; i < n; ++i) {
        t = array4[i];
        t._index = j++;
        data4.push(t);
      }
    },
    remove(num, map3) {
      const n = data4.length, copy5 = Array(n - num), reindex = data4;
      let t, i, j;
      for (i = 0; !map3[i] && i < n; ++i) {
        copy5[i] = data4[i];
        reindex[i] = i;
      }
      for (j = i; i < n; ++i) {
        t = data4[i];
        if (!map3[i]) {
          reindex[i] = j;
          curr[j] = curr[i];
          prev[j] = prev[i];
          copy5[j] = t;
          t._index = j++;
        } else {
          reindex[i] = -1;
        }
        curr[i] = 0;
      }
      data4 = copy5;
      return reindex;
    },
    size: () => data4.length,
    curr: () => curr,
    prev: () => prev,
    reset: (k) => prev[k] = curr[k],
    all: () => width2 < 257 ? 255 : width2 < 65537 ? 65535 : 4294967295,
    set(k, one4) {
      curr[k] |= one4;
    },
    clear(k, one4) {
      curr[k] &= ~one4;
    },
    resize(n, m3) {
      const k = curr.length;
      if (n > k || m3 > width2) {
        width2 = Math.max(m3, width2);
        curr = array(n, width2, curr);
        prev = array(n, width2);
      }
    }
  };
}
function lengthen(array4, length3, copy5) {
  if (array4.length >= length3) return array4;
  copy5 = copy5 || new array4.constructor(length3);
  copy5.set(array4);
  return copy5;
}
function array(n, m3, array4) {
  const copy5 = (m3 < 257 ? array8 : m3 < 65537 ? array16 : array32)(n);
  if (array4) copy5.set(array4);
  return copy5;
}
function Dimension(index4, i, query) {
  const bit = 1 << i;
  return {
    one: bit,
    zero: ~bit,
    range: query.slice(),
    bisect: index4.bisect,
    index: index4.index,
    size: index4.size,
    onAdd(added, curr) {
      const dim = this, range7 = dim.bisect(dim.range, added.value), idx = added.index, lo = range7[0], hi = range7[1], n1 = idx.length;
      let i2;
      for (i2 = 0; i2 < lo; ++i2) curr[idx[i2]] |= bit;
      for (i2 = hi; i2 < n1; ++i2) curr[idx[i2]] |= bit;
      return dim;
    }
  };
}
function SortedIndex() {
  let index4 = array32(0), value4 = [], size = 0;
  function insert2(key2, data4, base) {
    if (!data4.length) return [];
    const n0 = size, n1 = data4.length, addi = array32(n1);
    let addv = Array(n1), oldv, oldi, i;
    for (i = 0; i < n1; ++i) {
      addv[i] = key2(data4[i]);
      addi[i] = i;
    }
    addv = sort(addv, addi);
    if (n0) {
      oldv = value4;
      oldi = index4;
      value4 = Array(n0 + n1);
      index4 = array32(n0 + n1);
      merge3(base, oldv, oldi, n0, addv, addi, n1, value4, index4);
    } else {
      if (base > 0) for (i = 0; i < n1; ++i) {
        addi[i] += base;
      }
      value4 = addv;
      index4 = addi;
    }
    size = n0 + n1;
    return {
      index: addi,
      value: addv
    };
  }
  function remove2(num, map3) {
    const n = size;
    let idx, i, j;
    for (i = 0; !map3[index4[i]] && i < n; ++i) ;
    for (j = i; i < n; ++i) {
      if (!map3[idx = index4[i]]) {
        index4[j] = idx;
        value4[j] = value4[i];
        ++j;
      }
    }
    size = n - num;
  }
  function reindex(map3) {
    for (let i = 0, n = size; i < n; ++i) {
      index4[i] = map3[index4[i]];
    }
  }
  function bisect2(range7, array4) {
    let n;
    if (array4) {
      n = array4.length;
    } else {
      array4 = value4;
      n = size;
    }
    return [bisectLeft(array4, range7[0], 0, n), bisectLeft(array4, range7[1], 0, n)];
  }
  return {
    insert: insert2,
    remove: remove2,
    bisect: bisect2,
    reindex,
    index: () => index4,
    size: () => size
  };
}
function sort(values4, index4) {
  values4.sort.call(index4, (a3, b10) => {
    const x6 = values4[a3], y6 = values4[b10];
    return x6 < y6 ? -1 : x6 > y6 ? 1 : 0;
  });
  return permute(values4, index4);
}
function merge3(base, value0, index0, n0, value1, index1, n1, value4, index4) {
  let i0 = 0, i1 = 0, i;
  for (i = 0; i0 < n0 && i1 < n1; ++i) {
    if (value0[i0] < value1[i1]) {
      value4[i] = value0[i0];
      index4[i] = index0[i0++];
    } else {
      value4[i] = value1[i1];
      index4[i] = index1[i1++] + base;
    }
  }
  for (; i0 < n0; ++i0, ++i) {
    value4[i] = value0[i0];
    index4[i] = index0[i0];
  }
  for (; i1 < n1; ++i1, ++i) {
    value4[i] = value1[i1];
    index4[i] = index1[i1] + base;
  }
}
function CrossFilter(params2) {
  Transform.call(this, Bitmaps(), params2);
  this._indices = null;
  this._dims = null;
}
CrossFilter.Definition = {
  "type": "CrossFilter",
  "metadata": {},
  "params": [{
    "name": "fields",
    "type": "field",
    "array": true,
    "required": true
  }, {
    "name": "query",
    "type": "array",
    "array": true,
    "required": true,
    "content": {
      "type": "number",
      "array": true,
      "length": 2
    }
  }]
};
inherits_default(CrossFilter, Transform, {
  transform(_, pulse2) {
    if (!this._dims) {
      return this.init(_, pulse2);
    } else {
      var init2 = _.modified("fields") || _.fields.some((f) => pulse2.modified(f.fields));
      return init2 ? this.reinit(_, pulse2) : this.eval(_, pulse2);
    }
  },
  init(_, pulse2) {
    const fields = _.fields, query = _.query, indices = this._indices = {}, dims = this._dims = [], m3 = query.length;
    let i = 0, key2, index4;
    for (; i < m3; ++i) {
      key2 = fields[i].fname;
      index4 = indices[key2] || (indices[key2] = SortedIndex());
      dims.push(Dimension(index4, i, query[i]));
    }
    return this.eval(_, pulse2);
  },
  reinit(_, pulse2) {
    const output3 = pulse2.materialize().fork(), fields = _.fields, query = _.query, indices = this._indices, dims = this._dims, bits = this.value, curr = bits.curr(), prev = bits.prev(), all = bits.all(), out = output3.rem = output3.add, mod = output3.mod, m3 = query.length, adds = {};
    let add7, index4, key2, mods, remMap, modMap, i, n, f;
    prev.set(curr);
    if (pulse2.rem.length) {
      remMap = this.remove(_, pulse2, output3);
    }
    if (pulse2.add.length) {
      bits.add(pulse2.add);
    }
    if (pulse2.mod.length) {
      modMap = {};
      for (mods = pulse2.mod, i = 0, n = mods.length; i < n; ++i) {
        modMap[mods[i]._index] = 1;
      }
    }
    for (i = 0; i < m3; ++i) {
      f = fields[i];
      if (!dims[i] || _.modified("fields", i) || pulse2.modified(f.fields)) {
        key2 = f.fname;
        if (!(add7 = adds[key2])) {
          indices[key2] = index4 = SortedIndex();
          adds[key2] = add7 = index4.insert(f, pulse2.source, 0);
        }
        dims[i] = Dimension(index4, i, query[i]).onAdd(add7, curr);
      }
    }
    for (i = 0, n = bits.data().length; i < n; ++i) {
      if (remMap[i]) {
        continue;
      } else if (prev[i] !== curr[i]) {
        out.push(i);
      } else if (modMap[i] && curr[i] !== all) {
        mod.push(i);
      }
    }
    bits.mask = (1 << m3) - 1;
    return output3;
  },
  eval(_, pulse2) {
    const output3 = pulse2.materialize().fork(), m3 = this._dims.length;
    let mask = 0;
    if (pulse2.rem.length) {
      this.remove(_, pulse2, output3);
      mask |= (1 << m3) - 1;
    }
    if (_.modified("query") && !_.modified("fields")) {
      mask |= this.update(_, pulse2, output3);
    }
    if (pulse2.add.length) {
      this.insert(_, pulse2, output3);
      mask |= (1 << m3) - 1;
    }
    if (pulse2.mod.length) {
      this.modify(pulse2, output3);
      mask |= (1 << m3) - 1;
    }
    this.value.mask = mask;
    return output3;
  },
  insert(_, pulse2, output3) {
    const tuples = pulse2.add, bits = this.value, dims = this._dims, indices = this._indices, fields = _.fields, adds = {}, out = output3.add, n = bits.size() + tuples.length, m3 = dims.length;
    let k = bits.size(), j, key2, add7;
    bits.resize(n, m3);
    bits.add(tuples);
    const curr = bits.curr(), prev = bits.prev(), all = bits.all();
    for (j = 0; j < m3; ++j) {
      key2 = fields[j].fname;
      add7 = adds[key2] || (adds[key2] = indices[key2].insert(fields[j], tuples, k));
      dims[j].onAdd(add7, curr);
    }
    for (; k < n; ++k) {
      prev[k] = all;
      if (curr[k] !== all) out.push(k);
    }
  },
  modify(pulse2, output3) {
    const out = output3.mod, bits = this.value, curr = bits.curr(), all = bits.all(), tuples = pulse2.mod;
    let i, n, k;
    for (i = 0, n = tuples.length; i < n; ++i) {
      k = tuples[i]._index;
      if (curr[k] !== all) out.push(k);
    }
  },
  remove(_, pulse2, output3) {
    const indices = this._indices, bits = this.value, curr = bits.curr(), prev = bits.prev(), all = bits.all(), map3 = {}, out = output3.rem, tuples = pulse2.rem;
    let i, n, k, f;
    for (i = 0, n = tuples.length; i < n; ++i) {
      k = tuples[i]._index;
      map3[k] = 1;
      prev[k] = f = curr[k];
      curr[k] = all;
      if (f !== all) out.push(k);
    }
    for (k in indices) {
      indices[k].remove(n, map3);
    }
    this.reindex(pulse2, n, map3);
    return map3;
  },
  // reindex filters and indices after propagation completes
  reindex(pulse2, num, map3) {
    const indices = this._indices, bits = this.value;
    pulse2.runAfter(() => {
      const indexMap = bits.remove(num, map3);
      for (const key2 in indices) indices[key2].reindex(indexMap);
    });
  },
  update(_, pulse2, output3) {
    const dims = this._dims, query = _.query, stamp = pulse2.stamp, m3 = dims.length;
    let mask = 0, i, q;
    output3.filters = 0;
    for (q = 0; q < m3; ++q) {
      if (_.modified("query", q)) {
        i = q;
        ++mask;
      }
    }
    if (mask === 1) {
      mask = dims[i].one;
      this.incrementOne(dims[i], query[i], output3.add, output3.rem);
    } else {
      for (q = 0, mask = 0; q < m3; ++q) {
        if (!_.modified("query", q)) continue;
        mask |= dims[q].one;
        this.incrementAll(dims[q], query[q], stamp, output3.add);
        output3.rem = output3.add;
      }
    }
    return mask;
  },
  incrementAll(dim, query, stamp, out) {
    const bits = this.value, seen = bits.seen(), curr = bits.curr(), prev = bits.prev(), index4 = dim.index(), old = dim.bisect(dim.range), range7 = dim.bisect(query), lo1 = range7[0], hi1 = range7[1], lo0 = old[0], hi0 = old[1], one4 = dim.one;
    let i, j, k;
    if (lo1 < lo0) {
      for (i = lo1, j = Math.min(lo0, hi1); i < j; ++i) {
        k = index4[i];
        if (seen[k] !== stamp) {
          prev[k] = curr[k];
          seen[k] = stamp;
          out.push(k);
        }
        curr[k] ^= one4;
      }
    } else if (lo1 > lo0) {
      for (i = lo0, j = Math.min(lo1, hi0); i < j; ++i) {
        k = index4[i];
        if (seen[k] !== stamp) {
          prev[k] = curr[k];
          seen[k] = stamp;
          out.push(k);
        }
        curr[k] ^= one4;
      }
    }
    if (hi1 > hi0) {
      for (i = Math.max(lo1, hi0), j = hi1; i < j; ++i) {
        k = index4[i];
        if (seen[k] !== stamp) {
          prev[k] = curr[k];
          seen[k] = stamp;
          out.push(k);
        }
        curr[k] ^= one4;
      }
    } else if (hi1 < hi0) {
      for (i = Math.max(lo0, hi1), j = hi0; i < j; ++i) {
        k = index4[i];
        if (seen[k] !== stamp) {
          prev[k] = curr[k];
          seen[k] = stamp;
          out.push(k);
        }
        curr[k] ^= one4;
      }
    }
    dim.range = query.slice();
  },
  incrementOne(dim, query, add7, rem2) {
    const bits = this.value, curr = bits.curr(), index4 = dim.index(), old = dim.bisect(dim.range), range7 = dim.bisect(query), lo1 = range7[0], hi1 = range7[1], lo0 = old[0], hi0 = old[1], one4 = dim.one;
    let i, j, k;
    if (lo1 < lo0) {
      for (i = lo1, j = Math.min(lo0, hi1); i < j; ++i) {
        k = index4[i];
        curr[k] ^= one4;
        add7.push(k);
      }
    } else if (lo1 > lo0) {
      for (i = lo0, j = Math.min(lo1, hi0); i < j; ++i) {
        k = index4[i];
        curr[k] ^= one4;
        rem2.push(k);
      }
    }
    if (hi1 > hi0) {
      for (i = Math.max(lo1, hi0), j = hi1; i < j; ++i) {
        k = index4[i];
        curr[k] ^= one4;
        add7.push(k);
      }
    } else if (hi1 < hi0) {
      for (i = Math.max(lo0, hi1), j = hi0; i < j; ++i) {
        k = index4[i];
        curr[k] ^= one4;
        rem2.push(k);
      }
    }
    dim.range = query.slice();
  }
});
function ResolveFilter(params2) {
  Transform.call(this, null, params2);
}
ResolveFilter.Definition = {
  "type": "ResolveFilter",
  "metadata": {},
  "params": [{
    "name": "ignore",
    "type": "number",
    "required": true,
    "description": "A bit mask indicating which filters to ignore."
  }, {
    "name": "filter",
    "type": "object",
    "required": true,
    "description": "Per-tuple filter bitmaps from a CrossFilter transform."
  }]
};
inherits_default(ResolveFilter, Transform, {
  transform(_, pulse2) {
    const ignore = ~(_.ignore || 0), bitmap = _.filter, mask = bitmap.mask;
    if ((mask & ignore) === 0) return pulse2.StopPropagation;
    const output3 = pulse2.fork(pulse2.ALL), data4 = bitmap.data(), curr = bitmap.curr(), prev = bitmap.prev(), pass = (k) => !(curr[k] & ignore) ? data4[k] : null;
    output3.filter(output3.MOD, pass);
    if (!(mask & mask - 1)) {
      output3.filter(output3.ADD, pass);
      output3.filter(output3.REM, (k) => (curr[k] & ignore) === mask ? data4[k] : null);
    } else {
      output3.filter(output3.ADD, (k) => {
        const c3 = curr[k] & ignore, f = !c3 && c3 ^ prev[k] & ignore;
        return f ? data4[k] : null;
      });
      output3.filter(output3.REM, (k) => {
        const c3 = curr[k] & ignore, f = c3 && !(c3 ^ (c3 ^ prev[k] & ignore));
        return f ? data4[k] : null;
      });
    }
    return output3.filter(output3.SOURCE, (t) => pass(t._index));
  }
});

// ../deepseek-harness/node_modules/.pnpm/vega-time@3.2.1/node_modules/vega-time/build/vega-time.js
var YEAR2 = "year";
var QUARTER2 = "quarter";
var MONTH2 = "month";
var WEEK2 = "week";
var DATE2 = "date";
var DAY2 = "day";
var DAYOFYEAR2 = "dayofyear";
var HOURS2 = "hours";
var MINUTES2 = "minutes";
var SECONDS2 = "seconds";
var MILLISECONDS2 = "milliseconds";
var TIME_UNITS2 = [YEAR2, QUARTER2, MONTH2, WEEK2, DATE2, DAY2, DAYOFYEAR2, HOURS2, MINUTES2, SECONDS2, MILLISECONDS2];
var UNITS2 = TIME_UNITS2.reduce((o, u4, i) => (o[u4] = 1 + i, o), {});
function timeUnits2(units) {
  const u4 = array_default(units).slice(), m3 = {};
  if (!u4.length) error("Missing time unit.");
  u4.forEach((unit2) => {
    if (hasOwnProperty(UNITS2, unit2)) {
      m3[unit2] = 1;
    } else {
      error(`Invalid time unit: ${unit2}.`);
    }
  });
  const numTypes = (m3[WEEK2] || m3[DAY2] ? 1 : 0) + (m3[QUARTER2] || m3[MONTH2] || m3[DATE2] ? 1 : 0) + (m3[DAYOFYEAR2] ? 1 : 0);
  if (numTypes > 1) {
    error(`Incompatible time units: ${units}`);
  }
  u4.sort((a3, b10) => UNITS2[a3] - UNITS2[b10]);
  return u4;
}
var defaultSpecifiers2 = {
  [YEAR2]: "%Y ",
  [QUARTER2]: "Q%q ",
  [MONTH2]: "%b ",
  [DATE2]: "%d ",
  [WEEK2]: "W%U ",
  [DAY2]: "%a ",
  [DAYOFYEAR2]: "%j ",
  [HOURS2]: "%H:00",
  [MINUTES2]: "00:%M",
  [SECONDS2]: ":%S",
  [MILLISECONDS2]: ".%L",
  [`${YEAR2}-${MONTH2}`]: "%Y-%m ",
  [`${YEAR2}-${MONTH2}-${DATE2}`]: "%Y-%m-%d ",
  [`${HOURS2}-${MINUTES2}`]: "%H:%M"
};
function timeUnitSpecifier2(units, specifiers) {
  const s = extend({}, defaultSpecifiers2, specifiers), u4 = timeUnits2(units), n = u4.length;
  let fmt = "", start = 0, end, key2;
  for (start = 0; start < n; ) {
    for (end = u4.length; end > start; --end) {
      key2 = u4.slice(start, end).join("-");
      if (s[key2] != null) {
        fmt += s[key2];
        start = end;
        break;
      }
    }
  }
  return fmt.trim();
}
var t04 = /* @__PURE__ */ new Date();
function localYear2(y6) {
  t04.setFullYear(y6);
  t04.setMonth(0);
  t04.setDate(1);
  t04.setHours(0, 0, 0, 0);
  return t04;
}
function dayofyear2(d) {
  return localDayOfYear2(new Date(d));
}
function week2(d) {
  return localWeekNum2(new Date(d));
}
function localDayOfYear2(d) {
  return timeDay.count(localYear2(d.getFullYear()) - 1, d);
}
function localWeekNum2(d) {
  return timeSunday.count(localYear2(d.getFullYear()) - 1, d);
}
function localFirst2(y6) {
  return localYear2(y6).getDay();
}
function utcdayofyear2(d) {
  return utcDayOfYear2(new Date(d));
}
function utcweek2(d) {
  return utcWeekNum2(new Date(d));
}
function utcDayOfYear2(d) {
  const y6 = Date.UTC(d.getUTCFullYear(), 0, 1);
  return utcDay.count(y6 - 1, d);
}
function utcWeekNum2(d) {
  const y6 = Date.UTC(d.getUTCFullYear(), 0, 1);
  return utcSunday.count(y6 - 1, d);
}
function utcFirst2(y6) {
  t04.setTime(Date.UTC(y6, 0, 1));
  return t04.getUTCDay();
}
function weekday2(week3, day, firstDay) {
  return day + week3 * 7 - (firstDay + 6) % 7;
}
var localGet2 = {
  [YEAR2]: (d) => d.getFullYear(),
  [QUARTER2]: (d) => Math.floor(d.getMonth() / 3),
  [MONTH2]: (d) => d.getMonth(),
  [DATE2]: (d) => d.getDate(),
  [HOURS2]: (d) => d.getHours(),
  [MINUTES2]: (d) => d.getMinutes(),
  [SECONDS2]: (d) => d.getSeconds(),
  [MILLISECONDS2]: (d) => d.getMilliseconds(),
  [DAYOFYEAR2]: (d) => localDayOfYear2(d),
  [WEEK2]: (d) => localWeekNum2(d),
  [WEEK2 + DAY2]: (d, y6) => weekday2(localWeekNum2(d), d.getDay(), localFirst2(y6)),
  [DAY2]: (d, y6) => weekday2(1, d.getDay(), localFirst2(y6))
};
var localInv2 = {
  [QUARTER2]: (q) => 3 * q,
  [WEEK2]: (w3, y6) => weekday2(w3, 0, localFirst2(y6))
};
var utcGet2 = {
  [YEAR2]: (d) => d.getUTCFullYear(),
  [QUARTER2]: (d) => Math.floor(d.getUTCMonth() / 3),
  [MONTH2]: (d) => d.getUTCMonth(),
  [DATE2]: (d) => d.getUTCDate(),
  [HOURS2]: (d) => d.getUTCHours(),
  [MINUTES2]: (d) => d.getUTCMinutes(),
  [SECONDS2]: (d) => d.getUTCSeconds(),
  [MILLISECONDS2]: (d) => d.getUTCMilliseconds(),
  [DAYOFYEAR2]: (d) => utcDayOfYear2(d),
  [WEEK2]: (d) => utcWeekNum2(d),
  [DAY2]: (d, y6) => weekday2(1, d.getUTCDay(), utcFirst2(y6)),
  [WEEK2 + DAY2]: (d, y6) => weekday2(utcWeekNum2(d), d.getUTCDay(), utcFirst2(y6))
};
var utcInv2 = {
  [QUARTER2]: (q) => 3 * q,
  [WEEK2]: (w3, y6) => weekday2(w3, 0, utcFirst2(y6))
};
var timeIntervals2 = {
  [YEAR2]: timeYear,
  [QUARTER2]: timeMonth.every(3),
  [MONTH2]: timeMonth,
  [WEEK2]: timeSunday,
  [DATE2]: timeDay,
  [DAY2]: timeDay,
  [DAYOFYEAR2]: timeDay,
  [HOURS2]: timeHour,
  [MINUTES2]: timeMinute,
  [SECONDS2]: second,
  [MILLISECONDS2]: millisecond
};
var utcIntervals2 = {
  [YEAR2]: utcYear,
  [QUARTER2]: utcMonth.every(3),
  [MONTH2]: utcMonth,
  [WEEK2]: utcSunday,
  [DATE2]: utcDay,
  [DAY2]: utcDay,
  [DAYOFYEAR2]: utcDay,
  [HOURS2]: utcHour,
  [MINUTES2]: utcMinute,
  [SECONDS2]: second,
  [MILLISECONDS2]: millisecond
};
function timeInterval3(unit2) {
  return timeIntervals2[unit2];
}
function utcInterval2(unit2) {
  return utcIntervals2[unit2];
}
function offset3(ival, date2, step) {
  return ival ? ival.offset(date2, step) : void 0;
}
function timeOffset2(unit2, date2, step) {
  return offset3(timeInterval3(unit2), date2, step);
}
function utcOffset2(unit2, date2, step) {
  return offset3(utcInterval2(unit2), date2, step);
}
function sequence2(ival, start, stop2, step) {
  return ival ? ival.range(start, stop2, step) : void 0;
}
function timeSequence2(unit2, start, stop2, step) {
  return sequence2(timeInterval3(unit2), start, stop2, step);
}
function utcSequence2(unit2, start, stop2, step) {
  return sequence2(utcInterval2(unit2), start, stop2, step);
}
var durationSecond3 = 1e3;
var durationMinute3 = durationSecond3 * 60;
var durationHour3 = durationMinute3 * 60;
var durationDay3 = durationHour3 * 24;
var durationWeek3 = durationDay3 * 7;
var durationMonth3 = durationDay3 * 30;
var durationYear3 = durationDay3 * 365;
var Milli2 = [YEAR2, MONTH2, DATE2, HOURS2, MINUTES2, SECONDS2, MILLISECONDS2];
var Seconds2 = Milli2.slice(0, -1);
var Minutes2 = Seconds2.slice(0, -1);
var Hours2 = Minutes2.slice(0, -1);
var Day2 = Hours2.slice(0, -1);
var Week2 = [YEAR2, WEEK2];
var Month2 = [YEAR2, MONTH2];
var Year2 = [YEAR2];
var intervals2 = [[Seconds2, 1, durationSecond3], [Seconds2, 5, 5 * durationSecond3], [Seconds2, 15, 15 * durationSecond3], [Seconds2, 30, 30 * durationSecond3], [Minutes2, 1, durationMinute3], [Minutes2, 5, 5 * durationMinute3], [Minutes2, 15, 15 * durationMinute3], [Minutes2, 30, 30 * durationMinute3], [Hours2, 1, durationHour3], [Hours2, 3, 3 * durationHour3], [Hours2, 6, 6 * durationHour3], [Hours2, 12, 12 * durationHour3], [Day2, 1, durationDay3], [Week2, 1, durationWeek3], [Month2, 1, durationMonth3], [Month2, 3, 3 * durationMonth3], [Year2, 1, durationYear3]];
var localGetters2 = {
  [YEAR2]: (d) => d.getFullYear(),
  [MONTH2]: (d) => d.getMonth(),
  [DATE2]: (d) => d.getDate(),
  [DAY2]: (d) => d.getDay(),
  [HOURS2]: (d) => d.getHours(),
  [MINUTES2]: (d) => d.getMinutes(),
  [SECONDS2]: (d) => d.getSeconds(),
  [MILLISECONDS2]: (d) => d.getMilliseconds()
};
var utcGetters2 = {
  [YEAR2]: (d) => d.getUTCFullYear(),
  [MONTH2]: (d) => d.getUTCMonth(),
  [DATE2]: (d) => d.getUTCDate(),
  [DAY2]: (d) => d.getUTCDay(),
  [HOURS2]: (d) => d.getUTCHours(),
  [MINUTES2]: (d) => d.getUTCMinutes(),
  [SECONDS2]: (d) => d.getUTCSeconds(),
  [MILLISECONDS2]: (d) => d.getUTCMilliseconds()
};

// ../deepseek-harness/node_modules/.pnpm/vega-scenegraph@5.2.1/node_modules/vega-scenegraph/build/vega-scenegraph.js
var gradient_id2 = 0;
var patternPrefix2 = "p_";
function isGradient2(value4) {
  return value4 && value4.gradient;
}
function gradientRef2(g, defs, base) {
  const type4 = g.gradient;
  let id2 = g.id, prefix = type4 === "radial" ? patternPrefix2 : "";
  if (!id2) {
    id2 = g.id = "gradient_" + gradient_id2++;
    if (type4 === "radial") {
      g.x1 = get6(g.x1, 0.5);
      g.y1 = get6(g.y1, 0.5);
      g.r1 = get6(g.r1, 0);
      g.x2 = get6(g.x2, 0.5);
      g.y2 = get6(g.y2, 0.5);
      g.r2 = get6(g.r2, 0.5);
      prefix = patternPrefix2;
    } else {
      g.x1 = get6(g.x1, 0);
      g.y1 = get6(g.y1, 0);
      g.x2 = get6(g.x2, 1);
      g.y2 = get6(g.y2, 0);
    }
  }
  defs[id2] = g;
  return "url(" + (base || "") + "#" + prefix + id2 + ")";
}
function get6(val, def3) {
  return val != null ? val : def3;
}
function Gradient2(p02, p1) {
  var stops = [], gradient5;
  return gradient5 = {
    gradient: "linear",
    x1: p02 ? p02[0] : 0,
    y1: p02 ? p02[1] : 0,
    x2: p1 ? p1[0] : 1,
    y2: p1 ? p1[1] : 0,
    stops,
    stop: function(offset6, color6) {
      stops.push({
        offset: offset6,
        color: color6
      });
      return gradient5;
    }
  };
}
var lookup4 = {
  "basis": {
    curve: basis_default
  },
  "basis-closed": {
    curve: basisClosed_default
  },
  "basis-open": {
    curve: basisOpen_default
  },
  "bundle": {
    curve: bundle_default,
    tension: "beta",
    value: 0.85
  },
  "cardinal": {
    curve: cardinal_default,
    tension: "tension",
    value: 0
  },
  "cardinal-open": {
    curve: cardinalOpen_default,
    tension: "tension",
    value: 0
  },
  "cardinal-closed": {
    curve: cardinalClosed_default,
    tension: "tension",
    value: 0
  },
  "catmull-rom": {
    curve: catmullRom_default,
    tension: "alpha",
    value: 0.5
  },
  "catmull-rom-closed": {
    curve: catmullRomClosed_default,
    tension: "alpha",
    value: 0.5
  },
  "catmull-rom-open": {
    curve: catmullRomOpen_default,
    tension: "alpha",
    value: 0.5
  },
  "linear": {
    curve: linear_default
  },
  "linear-closed": {
    curve: linearClosed_default
  },
  "monotone": {
    horizontal: monotoneY,
    vertical: monotoneX
  },
  "natural": {
    curve: natural_default
  },
  "step": {
    curve: step_default
  },
  "step-after": {
    curve: stepAfter
  },
  "step-before": {
    curve: stepBefore
  }
};
function curves2(type4, orientation, tension) {
  var entry2 = hasOwnProperty(lookup4, type4) && lookup4[type4], curve2 = null;
  if (entry2) {
    curve2 = entry2.curve || entry2[orientation || "vertical"];
    if (entry2.tension && tension != null) {
      curve2 = curve2[entry2.tension](tension);
    }
  }
  return curve2;
}
var paramCounts2 = {
  m: 2,
  l: 2,
  h: 1,
  v: 1,
  z: 0,
  c: 6,
  s: 4,
  q: 4,
  t: 2,
  a: 7
};
var commandPattern2 = /[mlhvzcsqta]([^mlhvzcsqta]*)/gi;
var numberPattern2 = /^[+-]?(([0-9]*\.[0-9]+)|([0-9]+\.)|([0-9]+))([eE][+-]?[0-9]+)?/;
var spacePattern2 = /^((\s+,?\s*)|(,\s*))/;
var flagPattern2 = /^[01]/;
function parse5(path4) {
  const commands = [];
  const matches = path4.match(commandPattern2) || [];
  matches.forEach((str) => {
    let cmd = str[0];
    const type4 = cmd.toLowerCase();
    const paramCount = paramCounts2[type4];
    const params2 = parseParams2(type4, paramCount, str.slice(1).trim());
    const count2 = params2.length;
    if (count2 < paramCount || count2 && count2 % paramCount !== 0) {
      throw Error("Invalid SVG path, incorrect parameter count");
    }
    commands.push([cmd, ...params2.slice(0, paramCount)]);
    if (count2 === paramCount) {
      return;
    }
    if (type4 === "m") {
      cmd = cmd === "M" ? "L" : "l";
    }
    for (let i = paramCount; i < count2; i += paramCount) {
      commands.push([cmd, ...params2.slice(i, i + paramCount)]);
    }
  });
  return commands;
}
function parseParams2(type4, paramCount, segment) {
  const params2 = [];
  for (let index4 = 0; paramCount && index4 < segment.length; ) {
    for (let i = 0; i < paramCount; ++i) {
      const pattern = type4 === "a" && (i === 3 || i === 4) ? flagPattern2 : numberPattern2;
      const match3 = segment.slice(index4).match(pattern);
      if (match3 === null) {
        throw Error("Invalid SVG path, incorrect parameter type");
      }
      index4 += match3[0].length;
      params2.push(+match3[0]);
      const ws = segment.slice(index4).match(spacePattern2);
      if (ws !== null) {
        index4 += ws[0].length;
      }
    }
  }
  return params2;
}
var DegToRad2 = Math.PI / 180;
var Epsilon2 = 1e-14;
var HalfPi2 = Math.PI / 2;
var Tau2 = Math.PI * 2;
var HalfSqrt32 = Math.sqrt(3) / 2;
var segmentCache2 = {};
var bezierCache2 = {};
var join2 = [].join;
function segments2(x6, y6, rx, ry, large, sweep, rotateX, ox, oy) {
  const key2 = join2.call(arguments);
  if (segmentCache2[key2]) {
    return segmentCache2[key2];
  }
  const th = rotateX * DegToRad2;
  const sin_th = Math.sin(th);
  const cos_th = Math.cos(th);
  rx = Math.abs(rx);
  ry = Math.abs(ry);
  const px3 = cos_th * (ox - x6) * 0.5 + sin_th * (oy - y6) * 0.5;
  const py3 = cos_th * (oy - y6) * 0.5 - sin_th * (ox - x6) * 0.5;
  let pl = px3 * px3 / (rx * rx) + py3 * py3 / (ry * ry);
  if (pl > 1) {
    pl = Math.sqrt(pl);
    rx *= pl;
    ry *= pl;
  }
  const a00 = cos_th / rx;
  const a01 = sin_th / rx;
  const a10 = -sin_th / ry;
  const a11 = cos_th / ry;
  const x06 = a00 * ox + a01 * oy;
  const y06 = a10 * ox + a11 * oy;
  const x12 = a00 * x6 + a01 * y6;
  const y12 = a10 * x6 + a11 * y6;
  const d = (x12 - x06) * (x12 - x06) + (y12 - y06) * (y12 - y06);
  let sfactor_sq = 1 / d - 0.25;
  if (sfactor_sq < 0) sfactor_sq = 0;
  let sfactor = Math.sqrt(sfactor_sq);
  if (sweep == large) sfactor = -sfactor;
  const xc = 0.5 * (x06 + x12) - sfactor * (y12 - y06);
  const yc = 0.5 * (y06 + y12) + sfactor * (x12 - x06);
  const th0 = Math.atan2(y06 - yc, x06 - xc);
  const th1 = Math.atan2(y12 - yc, x12 - xc);
  let th_arc = th1 - th0;
  if (th_arc < 0 && sweep === 1) {
    th_arc += Tau2;
  } else if (th_arc > 0 && sweep === 0) {
    th_arc -= Tau2;
  }
  const segs = Math.ceil(Math.abs(th_arc / (HalfPi2 + 1e-3)));
  const result = [];
  for (let i = 0; i < segs; ++i) {
    const th2 = th0 + i * th_arc / segs;
    const th3 = th0 + (i + 1) * th_arc / segs;
    result[i] = [xc, yc, th2, th3, rx, ry, sin_th, cos_th];
  }
  return segmentCache2[key2] = result;
}
function bezier2(params2) {
  const key2 = join2.call(params2);
  if (bezierCache2[key2]) {
    return bezierCache2[key2];
  }
  var cx = params2[0], cy = params2[1], th0 = params2[2], th1 = params2[3], rx = params2[4], ry = params2[5], sin_th = params2[6], cos_th = params2[7];
  const a00 = cos_th * rx;
  const a01 = -sin_th * ry;
  const a10 = sin_th * rx;
  const a11 = cos_th * ry;
  const cos_th0 = Math.cos(th0);
  const sin_th0 = Math.sin(th0);
  const cos_th1 = Math.cos(th1);
  const sin_th1 = Math.sin(th1);
  const th_half = 0.5 * (th1 - th0);
  const sin_th_h2 = Math.sin(th_half * 0.5);
  const t = 8 / 3 * sin_th_h2 * sin_th_h2 / Math.sin(th_half);
  const x12 = cx + cos_th0 - t * sin_th0;
  const y12 = cy + sin_th0 + t * cos_th0;
  const x32 = cx + cos_th1;
  const y32 = cy + sin_th1;
  const x22 = x32 + t * sin_th1;
  const y22 = y32 - t * cos_th1;
  return bezierCache2[key2] = [a00 * x12 + a01 * y12, a10 * x12 + a11 * y12, a00 * x22 + a01 * y22, a10 * x22 + a11 * y22, a00 * x32 + a01 * y32, a10 * x32 + a11 * y32];
}
var temp2 = ["l", 0, 0, 0, 0, 0, 0, 0];
function scale$12(current2, sX, sY) {
  const c3 = temp2[0] = current2[0];
  if (c3 === "a" || c3 === "A") {
    temp2[1] = sX * current2[1];
    temp2[2] = sY * current2[2];
    temp2[3] = current2[3];
    temp2[4] = current2[4];
    temp2[5] = current2[5];
    temp2[6] = sX * current2[6];
    temp2[7] = sY * current2[7];
  } else if (c3 === "h" || c3 === "H") {
    temp2[1] = sX * current2[1];
  } else if (c3 === "v" || c3 === "V") {
    temp2[1] = sY * current2[1];
  } else {
    for (var i = 1, n = current2.length; i < n; ++i) {
      temp2[i] = (i % 2 == 1 ? sX : sY) * current2[i];
    }
  }
  return temp2;
}
function pathRender2(context4, path4, l, t, sX, sY) {
  var current2, previous = null, x6 = 0, y6 = 0, controlX = 0, controlY = 0, tempX, tempY, tempControlX, tempControlY, anchorX = 0, anchorY = 0;
  if (l == null) l = 0;
  if (t == null) t = 0;
  if (sX == null) sX = 1;
  if (sY == null) sY = sX;
  if (context4.beginPath) context4.beginPath();
  for (var i = 0, len = path4.length; i < len; ++i) {
    current2 = path4[i];
    if (sX !== 1 || sY !== 1) {
      current2 = scale$12(current2, sX, sY);
    }
    switch (current2[0]) {
      // first letter
      case "l":
        x6 += current2[1];
        y6 += current2[2];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "L":
        x6 = current2[1];
        y6 = current2[2];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "h":
        x6 += current2[1];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "H":
        x6 = current2[1];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "v":
        y6 += current2[1];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "V":
        y6 = current2[1];
        context4.lineTo(x6 + l, y6 + t);
        break;
      case "m":
        x6 += current2[1];
        y6 += current2[2];
        anchorX = x6;
        anchorY = y6;
        context4.moveTo(x6 + l, y6 + t);
        break;
      case "M":
        x6 = current2[1];
        y6 = current2[2];
        anchorX = x6;
        anchorY = y6;
        context4.moveTo(x6 + l, y6 + t);
        break;
      case "c":
        tempX = x6 + current2[5];
        tempY = y6 + current2[6];
        controlX = x6 + current2[3];
        controlY = y6 + current2[4];
        context4.bezierCurveTo(
          x6 + current2[1] + l,
          // x1
          y6 + current2[2] + t,
          // y1
          controlX + l,
          // x2
          controlY + t,
          // y2
          tempX + l,
          tempY + t
        );
        x6 = tempX;
        y6 = tempY;
        break;
      case "C":
        x6 = current2[5];
        y6 = current2[6];
        controlX = current2[3];
        controlY = current2[4];
        context4.bezierCurveTo(current2[1] + l, current2[2] + t, controlX + l, controlY + t, x6 + l, y6 + t);
        break;
      case "s":
        tempX = x6 + current2[3];
        tempY = y6 + current2[4];
        controlX = 2 * x6 - controlX;
        controlY = 2 * y6 - controlY;
        context4.bezierCurveTo(controlX + l, controlY + t, x6 + current2[1] + l, y6 + current2[2] + t, tempX + l, tempY + t);
        controlX = x6 + current2[1];
        controlY = y6 + current2[2];
        x6 = tempX;
        y6 = tempY;
        break;
      case "S":
        tempX = current2[3];
        tempY = current2[4];
        controlX = 2 * x6 - controlX;
        controlY = 2 * y6 - controlY;
        context4.bezierCurveTo(controlX + l, controlY + t, current2[1] + l, current2[2] + t, tempX + l, tempY + t);
        x6 = tempX;
        y6 = tempY;
        controlX = current2[1];
        controlY = current2[2];
        break;
      case "q":
        tempX = x6 + current2[3];
        tempY = y6 + current2[4];
        controlX = x6 + current2[1];
        controlY = y6 + current2[2];
        context4.quadraticCurveTo(controlX + l, controlY + t, tempX + l, tempY + t);
        x6 = tempX;
        y6 = tempY;
        break;
      case "Q":
        tempX = current2[3];
        tempY = current2[4];
        context4.quadraticCurveTo(current2[1] + l, current2[2] + t, tempX + l, tempY + t);
        x6 = tempX;
        y6 = tempY;
        controlX = current2[1];
        controlY = current2[2];
        break;
      case "t":
        tempX = x6 + current2[1];
        tempY = y6 + current2[2];
        if (previous[0].match(/[QqTt]/) === null) {
          controlX = x6;
          controlY = y6;
        } else if (previous[0] === "t") {
          controlX = 2 * x6 - tempControlX;
          controlY = 2 * y6 - tempControlY;
        } else if (previous[0] === "q") {
          controlX = 2 * x6 - controlX;
          controlY = 2 * y6 - controlY;
        }
        tempControlX = controlX;
        tempControlY = controlY;
        context4.quadraticCurveTo(controlX + l, controlY + t, tempX + l, tempY + t);
        x6 = tempX;
        y6 = tempY;
        controlX = x6 + current2[1];
        controlY = y6 + current2[2];
        break;
      case "T":
        tempX = current2[1];
        tempY = current2[2];
        controlX = 2 * x6 - controlX;
        controlY = 2 * y6 - controlY;
        context4.quadraticCurveTo(controlX + l, controlY + t, tempX + l, tempY + t);
        x6 = tempX;
        y6 = tempY;
        break;
      case "a":
        drawArc2(context4, x6 + l, y6 + t, [current2[1], current2[2], current2[3], current2[4], current2[5], current2[6] + x6 + l, current2[7] + y6 + t]);
        x6 += current2[6];
        y6 += current2[7];
        break;
      case "A":
        drawArc2(context4, x6 + l, y6 + t, [current2[1], current2[2], current2[3], current2[4], current2[5], current2[6] + l, current2[7] + t]);
        x6 = current2[6];
        y6 = current2[7];
        break;
      case "z":
      case "Z":
        x6 = anchorX;
        y6 = anchorY;
        context4.closePath();
        break;
    }
    previous = current2;
  }
}
function drawArc2(context4, x6, y6, coords) {
  const seg = segments2(
    coords[5],
    // end x
    coords[6],
    // end y
    coords[0],
    // radius x
    coords[1],
    // radius y
    coords[3],
    // large flag
    coords[4],
    // sweep flag
    coords[2],
    // rotation
    x6,
    y6
  );
  for (let i = 0; i < seg.length; ++i) {
    const bez = bezier2(seg[i]);
    context4.bezierCurveTo(bez[0], bez[1], bez[2], bez[3], bez[4], bez[5]);
  }
}
var Tan302 = 0.5773502691896257;
var builtins2 = {
  "circle": {
    draw: function(context4, size) {
      const r = Math.sqrt(size) / 2;
      context4.moveTo(r, 0);
      context4.arc(0, 0, r, 0, Tau2);
    }
  },
  "cross": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, s = r / 2.5;
      context4.moveTo(-r, -s);
      context4.lineTo(-r, s);
      context4.lineTo(-s, s);
      context4.lineTo(-s, r);
      context4.lineTo(s, r);
      context4.lineTo(s, s);
      context4.lineTo(r, s);
      context4.lineTo(r, -s);
      context4.lineTo(s, -s);
      context4.lineTo(s, -r);
      context4.lineTo(-s, -r);
      context4.lineTo(-s, -s);
      context4.closePath();
    }
  },
  "diamond": {
    draw: function(context4, size) {
      const r = Math.sqrt(size) / 2;
      context4.moveTo(-r, 0);
      context4.lineTo(0, -r);
      context4.lineTo(r, 0);
      context4.lineTo(0, r);
      context4.closePath();
    }
  },
  "square": {
    draw: function(context4, size) {
      var w3 = Math.sqrt(size), x6 = -w3 / 2;
      context4.rect(x6, x6, w3, w3);
    }
  },
  "arrow": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, s = r / 7, t = r / 2.5, v2 = r / 8;
      context4.moveTo(-s, r);
      context4.lineTo(s, r);
      context4.lineTo(s, -v2);
      context4.lineTo(t, -v2);
      context4.lineTo(0, -r);
      context4.lineTo(-t, -v2);
      context4.lineTo(-s, -v2);
      context4.closePath();
    }
  },
  "wedge": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt32 * r, o = h3 - r * Tan302, b10 = r / 4;
      context4.moveTo(0, -h3 - o);
      context4.lineTo(-b10, h3 - o);
      context4.lineTo(b10, h3 - o);
      context4.closePath();
    }
  },
  "triangle": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt32 * r, o = h3 - r * Tan302;
      context4.moveTo(0, -h3 - o);
      context4.lineTo(-r, h3 - o);
      context4.lineTo(r, h3 - o);
      context4.closePath();
    }
  },
  "triangle-up": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt32 * r;
      context4.moveTo(0, -h3);
      context4.lineTo(-r, h3);
      context4.lineTo(r, h3);
      context4.closePath();
    }
  },
  "triangle-down": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt32 * r;
      context4.moveTo(0, h3);
      context4.lineTo(-r, -h3);
      context4.lineTo(r, -h3);
      context4.closePath();
    }
  },
  "triangle-right": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt32 * r;
      context4.moveTo(h3, 0);
      context4.lineTo(-h3, -r);
      context4.lineTo(-h3, r);
      context4.closePath();
    }
  },
  "triangle-left": {
    draw: function(context4, size) {
      var r = Math.sqrt(size) / 2, h3 = HalfSqrt32 * r;
      context4.moveTo(-h3, 0);
      context4.lineTo(h3, -r);
      context4.lineTo(h3, r);
      context4.closePath();
    }
  },
  "stroke": {
    draw: function(context4, size) {
      const r = Math.sqrt(size) / 2;
      context4.moveTo(-r, 0);
      context4.lineTo(r, 0);
    }
  }
};
function symbols3(_) {
  return hasOwnProperty(builtins2, _) ? builtins2[_] : customSymbol2(_);
}
var custom11 = {};
function customSymbol2(path4) {
  if (!hasOwnProperty(custom11, path4)) {
    const parsed = parse5(path4);
    custom11[path4] = {
      draw: function(context4, size) {
        pathRender2(context4, parsed, 0, 0, Math.sqrt(size) / 2);
      }
    };
  }
  return custom11[path4];
}
var C3 = 0.448084975506;
function rectangleX2(d) {
  return d.x;
}
function rectangleY2(d) {
  return d.y;
}
function rectangleWidth2(d) {
  return d.width;
}
function rectangleHeight2(d) {
  return d.height;
}
function number6(_) {
  return typeof _ === "function" ? _ : () => +_;
}
function clamp3(value4, min4, max4) {
  return Math.max(min4, Math.min(value4, max4));
}
function vg_rect2() {
  var x6 = rectangleX2, y6 = rectangleY2, width2 = rectangleWidth2, height2 = rectangleHeight2, crTL = number6(0), crTR = crTL, crBL = crTL, crBR = crTL, context4 = null;
  function rectangle3(_, x06, y06) {
    var buffer, x12 = x06 != null ? x06 : +x6.call(this, _), y12 = y06 != null ? y06 : +y6.call(this, _), w3 = +width2.call(this, _), h3 = +height2.call(this, _), s = Math.min(w3, h3) / 2, tl3 = clamp3(+crTL.call(this, _), 0, s), tr3 = clamp3(+crTR.call(this, _), 0, s), bl3 = clamp3(+crBL.call(this, _), 0, s), br3 = clamp3(+crBR.call(this, _), 0, s);
    if (!context4) context4 = buffer = path();
    if (tl3 <= 0 && tr3 <= 0 && bl3 <= 0 && br3 <= 0) {
      context4.rect(x12, y12, w3, h3);
    } else {
      var x22 = x12 + w3, y22 = y12 + h3;
      context4.moveTo(x12 + tl3, y12);
      context4.lineTo(x22 - tr3, y12);
      context4.bezierCurveTo(x22 - C3 * tr3, y12, x22, y12 + C3 * tr3, x22, y12 + tr3);
      context4.lineTo(x22, y22 - br3);
      context4.bezierCurveTo(x22, y22 - C3 * br3, x22 - C3 * br3, y22, x22 - br3, y22);
      context4.lineTo(x12 + bl3, y22);
      context4.bezierCurveTo(x12 + C3 * bl3, y22, x12, y22 - C3 * bl3, x12, y22 - bl3);
      context4.lineTo(x12, y12 + tl3);
      context4.bezierCurveTo(x12, y12 + C3 * tl3, x12 + C3 * tl3, y12, x12 + tl3, y12);
      context4.closePath();
    }
    if (buffer) {
      context4 = null;
      return buffer + "" || null;
    }
  }
  rectangle3.x = function(_) {
    if (arguments.length) {
      x6 = number6(_);
      return rectangle3;
    } else {
      return x6;
    }
  };
  rectangle3.y = function(_) {
    if (arguments.length) {
      y6 = number6(_);
      return rectangle3;
    } else {
      return y6;
    }
  };
  rectangle3.width = function(_) {
    if (arguments.length) {
      width2 = number6(_);
      return rectangle3;
    } else {
      return width2;
    }
  };
  rectangle3.height = function(_) {
    if (arguments.length) {
      height2 = number6(_);
      return rectangle3;
    } else {
      return height2;
    }
  };
  rectangle3.cornerRadius = function(tl3, tr3, br3, bl3) {
    if (arguments.length) {
      crTL = number6(tl3);
      crTR = tr3 != null ? number6(tr3) : crTL;
      crBR = br3 != null ? number6(br3) : crTL;
      crBL = bl3 != null ? number6(bl3) : crTR;
      return rectangle3;
    } else {
      return crTL;
    }
  };
  rectangle3.context = function(_) {
    if (arguments.length) {
      context4 = _ == null ? null : _;
      return rectangle3;
    } else {
      return context4;
    }
  };
  return rectangle3;
}
function vg_trail2() {
  var x6, y6, size, defined2, context4 = null, ready, x12, y12, r1;
  function point9(x22, y22, w22) {
    const r2 = w22 / 2;
    if (ready) {
      var ux = y12 - y22, uy = x22 - x12;
      if (ux || uy) {
        var ud = Math.hypot(ux, uy), rx = (ux /= ud) * r1, ry = (uy /= ud) * r1, t = Math.atan2(uy, ux);
        context4.moveTo(x12 - rx, y12 - ry);
        context4.lineTo(x22 - ux * r2, y22 - uy * r2);
        context4.arc(x22, y22, r2, t - Math.PI, t);
        context4.lineTo(x12 + rx, y12 + ry);
        context4.arc(x12, y12, r1, t, t + Math.PI);
      } else {
        context4.arc(x22, y22, r2, 0, Tau2);
      }
      context4.closePath();
    } else {
      ready = 1;
    }
    x12 = x22;
    y12 = y22;
    r1 = r2;
  }
  function trail4(data4) {
    var i, n = data4.length, d, defined0 = false, buffer;
    if (context4 == null) context4 = buffer = path();
    for (i = 0; i <= n; ++i) {
      if (!(i < n && defined2(d = data4[i], i, data4)) === defined0) {
        if (defined0 = !defined0) ready = 0;
      }
      if (defined0) point9(+x6(d, i, data4), +y6(d, i, data4), +size(d, i, data4));
    }
    if (buffer) {
      context4 = null;
      return buffer + "" || null;
    }
  }
  trail4.x = function(_) {
    if (arguments.length) {
      x6 = _;
      return trail4;
    } else {
      return x6;
    }
  };
  trail4.y = function(_) {
    if (arguments.length) {
      y6 = _;
      return trail4;
    } else {
      return y6;
    }
  };
  trail4.size = function(_) {
    if (arguments.length) {
      size = _;
      return trail4;
    } else {
      return size;
    }
  };
  trail4.defined = function(_) {
    if (arguments.length) {
      defined2 = _;
      return trail4;
    } else {
      return defined2;
    }
  };
  trail4.context = function(_) {
    if (arguments.length) {
      if (_ == null) {
        context4 = null;
      } else {
        context4 = _;
      }
      return trail4;
    } else {
      return context4;
    }
  };
  return trail4;
}
function value$12(a3, b10) {
  return a3 != null ? a3 : b10;
}
var x5 = (item) => item.x || 0;
var y5 = (item) => item.y || 0;
var w2 = (item) => item.width || 0;
var h2 = (item) => item.height || 0;
var xw2 = (item) => (item.x || 0) + (item.width || 0);
var yh2 = (item) => (item.y || 0) + (item.height || 0);
var sa2 = (item) => item.startAngle || 0;
var ea3 = (item) => item.endAngle || 0;
var pa2 = (item) => item.padAngle || 0;
var ir2 = (item) => item.innerRadius || 0;
var or2 = (item) => item.outerRadius || 0;
var cr2 = (item) => item.cornerRadius || 0;
var tl2 = (item) => value$12(item.cornerRadiusTopLeft, item.cornerRadius) || 0;
var tr2 = (item) => value$12(item.cornerRadiusTopRight, item.cornerRadius) || 0;
var br2 = (item) => value$12(item.cornerRadiusBottomRight, item.cornerRadius) || 0;
var bl2 = (item) => value$12(item.cornerRadiusBottomLeft, item.cornerRadius) || 0;
var sz2 = (item) => value$12(item.size, 64);
var ts2 = (item) => item.size || 1;
var def2 = (item) => !(item.defined === false);
var type2 = (item) => symbols3(item.shape || "circle");
var arcShape2 = arc_default().startAngle(sa2).endAngle(ea3).padAngle(pa2).innerRadius(ir2).outerRadius(or2).cornerRadius(cr2);
var areavShape2 = area_default().x(x5).y1(y5).y0(yh2).defined(def2);
var areahShape2 = area_default().y(y5).x1(x5).x0(xw2).defined(def2);
var lineShape2 = line_default().x(x5).y(y5).defined(def2);
var rectShape2 = vg_rect2().x(x5).y(y5).width(w2).height(h2).cornerRadius(tl2, tr2, br2, bl2);
var symbolShape2 = Symbol2().type(type2).size(sz2);
var trailShape2 = vg_trail2().x(x5).y(y5).defined(def2).size(ts2);
function hasCornerRadius2(item) {
  return item.cornerRadius || item.cornerRadiusTopLeft || item.cornerRadiusTopRight || item.cornerRadiusBottomRight || item.cornerRadiusBottomLeft;
}
function arc$12(context4, item) {
  return arcShape2.context(context4)(item);
}
function area$12(context4, items) {
  const item = items[0], interp = item.interpolate || "linear";
  return (item.orient === "horizontal" ? areahShape2 : areavShape2).curve(curves2(interp, item.orient, item.tension)).context(context4)(items);
}
function line$12(context4, items) {
  const item = items[0], interp = item.interpolate || "linear";
  return lineShape2.curve(curves2(interp, item.orient, item.tension)).context(context4)(items);
}
function rectangle2(context4, item, x6, y6) {
  return rectShape2.context(context4)(item, x6, y6);
}
function shape$12(context4, item) {
  return (item.mark.shape || item.shape).context(context4)(item);
}
function symbol$12(context4, item) {
  return symbolShape2.context(context4)(item);
}
function trail$12(context4, items) {
  return trailShape2.context(context4)(items);
}
var clip_id2 = 1;
function clip$12(renderer, item, size) {
  var clip4 = item.clip, defs = renderer._defs, id2 = item.clip_id || (item.clip_id = "clip" + clip_id2++), c3 = defs.clipping[id2] || (defs.clipping[id2] = {
    id: id2
  });
  if (isFunction(clip4)) {
    c3.path = clip4(null);
  } else if (hasCornerRadius2(size)) {
    c3.path = rectangle2(null, size, 0, 0);
  } else {
    c3.width = size.width || 0;
    c3.height = size.height || 0;
  }
  return "url(#" + id2 + ")";
}
function Bounds3(b10) {
  this.clear();
  if (b10) this.union(b10);
}
Bounds3.prototype = {
  clone() {
    return new Bounds3(this);
  },
  clear() {
    this.x1 = +Number.MAX_VALUE;
    this.y1 = +Number.MAX_VALUE;
    this.x2 = -Number.MAX_VALUE;
    this.y2 = -Number.MAX_VALUE;
    return this;
  },
  empty() {
    return this.x1 === +Number.MAX_VALUE && this.y1 === +Number.MAX_VALUE && this.x2 === -Number.MAX_VALUE && this.y2 === -Number.MAX_VALUE;
  },
  equals(b10) {
    return this.x1 === b10.x1 && this.y1 === b10.y1 && this.x2 === b10.x2 && this.y2 === b10.y2;
  },
  set(x12, y12, x22, y22) {
    if (x22 < x12) {
      this.x2 = x12;
      this.x1 = x22;
    } else {
      this.x1 = x12;
      this.x2 = x22;
    }
    if (y22 < y12) {
      this.y2 = y12;
      this.y1 = y22;
    } else {
      this.y1 = y12;
      this.y2 = y22;
    }
    return this;
  },
  add(x6, y6) {
    if (x6 < this.x1) this.x1 = x6;
    if (y6 < this.y1) this.y1 = y6;
    if (x6 > this.x2) this.x2 = x6;
    if (y6 > this.y2) this.y2 = y6;
    return this;
  },
  expand(d) {
    this.x1 -= d;
    this.y1 -= d;
    this.x2 += d;
    this.y2 += d;
    return this;
  },
  round() {
    this.x1 = Math.floor(this.x1);
    this.y1 = Math.floor(this.y1);
    this.x2 = Math.ceil(this.x2);
    this.y2 = Math.ceil(this.y2);
    return this;
  },
  scale(s) {
    this.x1 *= s;
    this.y1 *= s;
    this.x2 *= s;
    this.y2 *= s;
    return this;
  },
  translate(dx, dy) {
    this.x1 += dx;
    this.x2 += dx;
    this.y1 += dy;
    this.y2 += dy;
    return this;
  },
  rotate(angle2, x6, y6) {
    const p = this.rotatedPoints(angle2, x6, y6);
    return this.clear().add(p[0], p[1]).add(p[2], p[3]).add(p[4], p[5]).add(p[6], p[7]);
  },
  rotatedPoints(angle2, x6, y6) {
    var {
      x1: x12,
      y1: y12,
      x2: x22,
      y2: y22
    } = this, cos4 = Math.cos(angle2), sin4 = Math.sin(angle2), cx = x6 - x6 * cos4 + y6 * sin4, cy = y6 - x6 * sin4 - y6 * cos4;
    return [cos4 * x12 - sin4 * y12 + cx, sin4 * x12 + cos4 * y12 + cy, cos4 * x12 - sin4 * y22 + cx, sin4 * x12 + cos4 * y22 + cy, cos4 * x22 - sin4 * y12 + cx, sin4 * x22 + cos4 * y12 + cy, cos4 * x22 - sin4 * y22 + cx, sin4 * x22 + cos4 * y22 + cy];
  },
  union(b10) {
    if (b10.x1 < this.x1) this.x1 = b10.x1;
    if (b10.y1 < this.y1) this.y1 = b10.y1;
    if (b10.x2 > this.x2) this.x2 = b10.x2;
    if (b10.y2 > this.y2) this.y2 = b10.y2;
    return this;
  },
  intersect(b10) {
    if (b10.x1 > this.x1) this.x1 = b10.x1;
    if (b10.y1 > this.y1) this.y1 = b10.y1;
    if (b10.x2 < this.x2) this.x2 = b10.x2;
    if (b10.y2 < this.y2) this.y2 = b10.y2;
    return this;
  },
  encloses(b10) {
    return b10 && this.x1 <= b10.x1 && this.x2 >= b10.x2 && this.y1 <= b10.y1 && this.y2 >= b10.y2;
  },
  alignsWith(b10) {
    return b10 && (this.x1 == b10.x1 || this.x2 == b10.x2 || this.y1 == b10.y1 || this.y2 == b10.y2);
  },
  intersects(b10) {
    return b10 && !(this.x2 < b10.x1 || this.x1 > b10.x2 || this.y2 < b10.y1 || this.y1 > b10.y2);
  },
  contains(x6, y6) {
    return !(x6 < this.x1 || x6 > this.x2 || y6 < this.y1 || y6 > this.y2);
  },
  width() {
    return this.x2 - this.x1;
  },
  height() {
    return this.y2 - this.y1;
  }
};
function Item2(mark) {
  this.mark = mark;
  this.bounds = this.bounds || new Bounds3();
}
function GroupItem2(mark) {
  Item2.call(this, mark);
  this.items = this.items || [];
}
inherits_default(GroupItem2, Item2);
var ResourceLoader2 = class {
  constructor(customLoader) {
    this._pending = 0;
    this._loader = customLoader || loader();
  }
  pending() {
    return this._pending;
  }
  sanitizeURL(uri) {
    const loader2 = this;
    increment2(loader2);
    return loader2._loader.sanitize(uri, {
      context: "href"
    }).then((opt) => {
      decrement2(loader2);
      return opt;
    }).catch(() => {
      decrement2(loader2);
      return null;
    });
  }
  loadImage(uri) {
    const loader2 = this, Image2 = domImage();
    increment2(loader2);
    return loader2._loader.sanitize(uri, {
      context: "image"
    }).then((opt) => {
      const url = opt.href;
      if (!url || !Image2) throw {
        url
      };
      const img = new Image2();
      const cors = hasOwnProperty(opt, "crossOrigin") ? opt.crossOrigin : "anonymous";
      if (cors != null) img.crossOrigin = cors;
      img.onload = () => decrement2(loader2);
      img.onerror = () => decrement2(loader2);
      img.src = url;
      return img;
    }).catch((e) => {
      decrement2(loader2);
      return {
        complete: false,
        width: 0,
        height: 0,
        src: e && e.url || ""
      };
    });
  }
  ready() {
    const loader2 = this;
    return new Promise((accept) => {
      function poll(value4) {
        if (!loader2.pending()) accept(value4);
        else setTimeout(() => {
          poll(true);
        }, 10);
      }
      poll(false);
    });
  }
};
function increment2(loader2) {
  loader2._pending += 1;
}
function decrement2(loader2) {
  loader2._pending -= 1;
}
function boundStroke2(bounds3, item, miter) {
  if (item.stroke && item.opacity !== 0 && item.strokeOpacity !== 0) {
    const sw = item.strokeWidth != null ? +item.strokeWidth : 1;
    let e = (item.strokeCap === "square" ? Math.SQRT2 : 1) * sw / 2;
    if (miter && (!item.strokeJoin || item.strokeJoin === "miter")) {
      e = Math.max(e, (item.strokeMiterLimit != null ? +item.strokeMiterLimit : 4) * sw / 2);
    }
    bounds3.expand(e);
  }
  return bounds3;
}
var circleThreshold2 = Tau2 - 1e-8;
var bounds2;
var lx2;
var ly2;
var rot2;
var ma2;
var mb2;
var mc2;
var md2;
var add5 = (x6, y6) => bounds2.add(x6, y6);
var addL2 = (x6, y6) => add5(lx2 = x6, ly2 = y6);
var addX2 = (x6) => add5(x6, bounds2.y1);
var addY2 = (y6) => add5(bounds2.x1, y6);
var px2 = (x6, y6) => ma2 * x6 + mc2 * y6;
var py2 = (x6, y6) => mb2 * x6 + md2 * y6;
var addp2 = (x6, y6) => add5(px2(x6, y6), py2(x6, y6));
var addpL2 = (x6, y6) => addL2(px2(x6, y6), py2(x6, y6));
function boundContext2(_, deg) {
  bounds2 = _;
  if (deg) {
    rot2 = deg * DegToRad2;
    ma2 = md2 = Math.cos(rot2);
    mb2 = Math.sin(rot2);
    mc2 = -mb2;
  } else {
    ma2 = md2 = 1;
    rot2 = mb2 = mc2 = 0;
  }
  return context$12;
}
var context$12 = {
  beginPath() {
  },
  closePath() {
  },
  moveTo: addpL2,
  lineTo: addpL2,
  rect(x6, y6, w3, h3) {
    if (rot2) {
      addp2(x6 + w3, y6);
      addp2(x6 + w3, y6 + h3);
      addp2(x6, y6 + h3);
      addpL2(x6, y6);
    } else {
      add5(x6 + w3, y6 + h3);
      addL2(x6, y6);
    }
  },
  quadraticCurveTo(x12, y12, x22, y22) {
    const px1 = px2(x12, y12), py1 = py2(x12, y12), px22 = px2(x22, y22), py22 = py2(x22, y22);
    quadExtrema2(lx2, px1, px22, addX2);
    quadExtrema2(ly2, py1, py22, addY2);
    addL2(px22, py22);
  },
  bezierCurveTo(x12, y12, x22, y22, x32, y32) {
    const px1 = px2(x12, y12), py1 = py2(x12, y12), px22 = px2(x22, y22), py22 = py2(x22, y22), px3 = px2(x32, y32), py3 = py2(x32, y32);
    cubicExtrema2(lx2, px1, px22, px3, addX2);
    cubicExtrema2(ly2, py1, py22, py3, addY2);
    addL2(px3, py3);
  },
  arc(cx, cy, r, sa3, ea4, ccw) {
    sa3 += rot2;
    ea4 += rot2;
    lx2 = r * Math.cos(ea4) + cx;
    ly2 = r * Math.sin(ea4) + cy;
    if (Math.abs(ea4 - sa3) > circleThreshold2) {
      add5(cx - r, cy - r);
      add5(cx + r, cy + r);
    } else {
      const update3 = (a3) => add5(r * Math.cos(a3) + cx, r * Math.sin(a3) + cy);
      let s, i;
      update3(sa3);
      update3(ea4);
      if (ea4 !== sa3) {
        sa3 = sa3 % Tau2;
        if (sa3 < 0) sa3 += Tau2;
        ea4 = ea4 % Tau2;
        if (ea4 < 0) ea4 += Tau2;
        if (ea4 < sa3) {
          ccw = !ccw;
          s = sa3;
          sa3 = ea4;
          ea4 = s;
        }
        if (ccw) {
          ea4 -= Tau2;
          s = sa3 - sa3 % HalfPi2;
          for (i = 0; i < 4 && s > ea4; ++i, s -= HalfPi2) update3(s);
        } else {
          s = sa3 - sa3 % HalfPi2 + HalfPi2;
          for (i = 0; i < 4 && s < ea4; ++i, s = s + HalfPi2) update3(s);
        }
      }
    }
  }
};
function quadExtrema2(x06, x12, x22, cb) {
  const t = (x06 - x12) / (x06 + x22 - 2 * x12);
  if (0 < t && t < 1) cb(x06 + (x12 - x06) * t);
}
function cubicExtrema2(x06, x12, x22, x32, cb) {
  const a3 = x32 - x06 + 3 * x12 - 3 * x22, b10 = x06 + x22 - 2 * x12, c3 = x06 - x12;
  let t05 = 0, t13 = 0, r;
  if (Math.abs(a3) > Epsilon2) {
    r = b10 * b10 + c3 * a3;
    if (r >= 0) {
      r = Math.sqrt(r);
      t05 = (-b10 + r) / a3;
      t13 = (-b10 - r) / a3;
    }
  } else {
    t05 = 0.5 * c3 / b10;
  }
  if (0 < t05 && t05 < 1) cb(cubic2(t05, x06, x12, x22, x32));
  if (0 < t13 && t13 < 1) cb(cubic2(t13, x06, x12, x22, x32));
}
function cubic2(t, x06, x12, x22, x32) {
  const s = 1 - t, s2 = s * s, t22 = t * t;
  return s2 * s * x06 + 3 * s2 * t * x12 + 3 * s * t22 * x22 + t22 * t * x32;
}
var context2 = (context2 = domCanvas(1, 1)) ? context2.getContext("2d") : null;
var b2 = new Bounds3();
function intersectPath2(draw4) {
  return function(item, brush) {
    if (!context2) return true;
    draw4(context2, item);
    b2.clear().union(item.bounds).intersect(brush).round();
    const {
      x1: x12,
      y1: y12,
      x2: x22,
      y2: y22
    } = b2;
    for (let y6 = y12; y6 <= y22; ++y6) {
      for (let x6 = x12; x6 <= x22; ++x6) {
        if (context2.isPointInPath(x6, y6)) {
          return true;
        }
      }
    }
    return false;
  };
}
function intersectPoint2(item, box) {
  return box.contains(item.x || 0, item.y || 0);
}
function intersectRect2(item, box) {
  const x6 = item.x || 0, y6 = item.y || 0, w3 = item.width || 0, h3 = item.height || 0;
  return box.intersects(b2.set(x6, y6, x6 + w3, y6 + h3));
}
function intersectRule2(item, box) {
  const x6 = item.x || 0, y6 = item.y || 0, x22 = item.x2 != null ? item.x2 : x6, y22 = item.y2 != null ? item.y2 : y6;
  return intersectBoxLine2(box, x6, y6, x22, y22);
}
function intersectBoxLine2(box, x6, y6, u4, v2) {
  const {
    x1: x12,
    y1: y12,
    x2: x22,
    y2: y22
  } = box, dx = u4 - x6, dy = v2 - y6;
  let t05 = 0, t13 = 1, p, q, r, e;
  for (e = 0; e < 4; ++e) {
    if (e === 0) {
      p = -dx;
      q = -(x12 - x6);
    }
    if (e === 1) {
      p = dx;
      q = x22 - x6;
    }
    if (e === 2) {
      p = -dy;
      q = -(y12 - y6);
    }
    if (e === 3) {
      p = dy;
      q = y22 - y6;
    }
    if (Math.abs(p) < 1e-10 && q < 0) return false;
    r = q / p;
    if (p < 0) {
      if (r > t13) return false;
      else if (r > t05) t05 = r;
    } else if (p > 0) {
      if (r < t05) return false;
      else if (r < t13) t13 = r;
    }
  }
  return true;
}
function blend2(context4, item) {
  context4.globalCompositeOperation = item.blend || "source-over";
}
function value2(value4, dflt) {
  return value4 == null ? dflt : value4;
}
function addStops2(gradient5, stops) {
  const n = stops.length;
  for (let i = 0; i < n; ++i) {
    gradient5.addColorStop(stops[i].offset, stops[i].color);
  }
  return gradient5;
}
function gradient2(context4, spec, bounds3) {
  const w3 = bounds3.width(), h3 = bounds3.height();
  let gradient5;
  if (spec.gradient === "radial") {
    gradient5 = context4.createRadialGradient(bounds3.x1 + value2(spec.x1, 0.5) * w3, bounds3.y1 + value2(spec.y1, 0.5) * h3, Math.max(w3, h3) * value2(spec.r1, 0), bounds3.x1 + value2(spec.x2, 0.5) * w3, bounds3.y1 + value2(spec.y2, 0.5) * h3, Math.max(w3, h3) * value2(spec.r2, 0.5));
  } else {
    const x12 = value2(spec.x1, 0), y12 = value2(spec.y1, 0), x22 = value2(spec.x2, 1), y22 = value2(spec.y2, 0);
    if (x12 === x22 || y12 === y22 || w3 === h3) {
      gradient5 = context4.createLinearGradient(bounds3.x1 + x12 * w3, bounds3.y1 + y12 * h3, bounds3.x1 + x22 * w3, bounds3.y1 + y22 * h3);
    } else {
      const image4 = domCanvas(Math.ceil(w3), Math.ceil(h3)), ictx = image4.getContext("2d");
      ictx.scale(w3, h3);
      ictx.fillStyle = addStops2(ictx.createLinearGradient(x12, y12, x22, y22), spec.stops);
      ictx.fillRect(0, 0, w3, h3);
      return context4.createPattern(image4, "no-repeat");
    }
  }
  return addStops2(gradient5, spec.stops);
}
function color3(context4, item, value4) {
  return isGradient2(value4) ? gradient2(context4, value4, item.bounds) : value4;
}
function fill2(context4, item, opacity2) {
  opacity2 *= item.fillOpacity == null ? 1 : item.fillOpacity;
  if (opacity2 > 0) {
    context4.globalAlpha = opacity2;
    context4.fillStyle = color3(context4, item, item.fill);
    return true;
  } else {
    return false;
  }
}
var Empty3 = [];
function stroke2(context4, item, opacity2) {
  var lw = (lw = item.strokeWidth) != null ? lw : 1;
  if (lw <= 0) return false;
  opacity2 *= item.strokeOpacity == null ? 1 : item.strokeOpacity;
  if (opacity2 > 0) {
    context4.globalAlpha = opacity2;
    context4.strokeStyle = color3(context4, item, item.stroke);
    context4.lineWidth = lw;
    context4.lineCap = item.strokeCap || "butt";
    context4.lineJoin = item.strokeJoin || "miter";
    context4.miterLimit = item.strokeMiterLimit != null ? item.strokeMiterLimit : 4;
    if (context4.setLineDash) {
      context4.setLineDash(item.strokeDash || Empty3);
      context4.lineDashOffset = item.strokeDashOffset || 0;
    }
    return true;
  } else {
    return false;
  }
}
function compare3(a3, b10) {
  return a3.zindex - b10.zindex || a3.index - b10.index;
}
function zorder2(scene) {
  if (!scene.zdirty) return scene.zitems;
  var items = scene.items, output3 = [], item, i, n;
  for (i = 0, n = items.length; i < n; ++i) {
    item = items[i];
    item.index = i;
    if (item.zindex) output3.push(item);
  }
  scene.zdirty = false;
  return scene.zitems = output3.sort(compare3);
}
function visit2(scene, visitor) {
  var items = scene.items, i, n;
  if (!items || !items.length) return;
  const zitems = zorder2(scene);
  if (zitems && zitems.length) {
    for (i = 0, n = items.length; i < n; ++i) {
      if (!items[i].zindex) visitor(items[i]);
    }
    items = zitems;
  }
  for (i = 0, n = items.length; i < n; ++i) {
    visitor(items[i]);
  }
}
function pickVisit2(scene, visitor) {
  var items = scene.items, hit3, i;
  if (!items || !items.length) return null;
  const zitems = zorder2(scene);
  if (zitems && zitems.length) items = zitems;
  for (i = items.length; --i >= 0; ) {
    if (hit3 = visitor(items[i])) return hit3;
  }
  if (items === zitems) {
    for (items = scene.items, i = items.length; --i >= 0; ) {
      if (!items[i].zindex) {
        if (hit3 = visitor(items[i])) return hit3;
      }
    }
  }
  return null;
}
function drawAll2(path4) {
  return function(context4, scene, bounds3) {
    visit2(scene, (item) => {
      if (!bounds3 || bounds3.intersects(item.bounds)) {
        drawPath2(path4, context4, item, item);
      }
    });
  };
}
function drawOne2(path4) {
  return function(context4, scene, bounds3) {
    if (scene.items.length && (!bounds3 || bounds3.intersects(scene.bounds))) {
      drawPath2(path4, context4, scene.items[0], scene.items);
    }
  };
}
function drawPath2(path4, context4, item, items) {
  var opacity2 = item.opacity == null ? 1 : item.opacity;
  if (opacity2 === 0) return;
  if (path4(context4, items)) return;
  blend2(context4, item);
  if (item.fill && fill2(context4, item, opacity2)) {
    context4.fill();
  }
  if (item.stroke && stroke2(context4, item, opacity2)) {
    context4.stroke();
  }
}
function pick$12(test2) {
  test2 = test2 || truthy;
  return function(context4, scene, x6, y6, gx, gy) {
    x6 *= context4.pixelRatio;
    y6 *= context4.pixelRatio;
    return pickVisit2(scene, (item) => {
      const b10 = item.bounds;
      if (b10 && !b10.contains(gx, gy) || !b10) return;
      if (test2(context4, item, x6, y6, gx, gy)) return item;
    });
  };
}
function hitPath2(path4, filled) {
  return function(context4, o, x6, y6) {
    var item = Array.isArray(o) ? o[0] : o, fill3 = filled == null ? item.fill : filled, stroke3 = item.stroke && context4.isPointInStroke, lw, lc;
    if (stroke3) {
      lw = item.strokeWidth;
      lc = item.strokeCap;
      context4.lineWidth = lw != null ? lw : 1;
      context4.lineCap = lc != null ? lc : "butt";
    }
    return path4(context4, o) ? false : fill3 && context4.isPointInPath(x6, y6) || stroke3 && context4.isPointInStroke(x6, y6);
  };
}
function pickPath2(path4) {
  return pick$12(hitPath2(path4));
}
function translate3(x6, y6) {
  return "translate(" + x6 + "," + y6 + ")";
}
function rotate2(a3) {
  return "rotate(" + a3 + ")";
}
function scale4(scaleX, scaleY2) {
  return "scale(" + scaleX + "," + scaleY2 + ")";
}
function translateItem2(item) {
  return translate3(item.x || 0, item.y || 0);
}
function rotateItem2(item) {
  return translate3(item.x || 0, item.y || 0) + (item.angle ? " " + rotate2(item.angle) : "");
}
function transformItem2(item) {
  return translate3(item.x || 0, item.y || 0) + (item.angle ? " " + rotate2(item.angle) : "") + (item.scaleX || item.scaleY ? " " + scale4(item.scaleX || 1, item.scaleY || 1) : "");
}
function markItemPath2(type4, shape3, isect) {
  function attr3(emit3, item) {
    emit3("transform", rotateItem2(item));
    emit3("d", shape3(null, item));
  }
  function bound3(bounds3, item) {
    shape3(boundContext2(bounds3, item.angle), item);
    return boundStroke2(bounds3, item, true).translate(item.x || 0, item.y || 0);
  }
  function draw4(context4, item) {
    var x6 = item.x || 0, y6 = item.y || 0, a3 = item.angle || 0;
    context4.translate(x6, y6);
    if (a3) context4.rotate(a3 *= DegToRad2);
    context4.beginPath();
    shape3(context4, item);
    if (a3) context4.rotate(-a3);
    context4.translate(-x6, -y6);
  }
  return {
    type: type4,
    tag: "path",
    nested: false,
    attr: attr3,
    bound: bound3,
    draw: drawAll2(draw4),
    pick: pickPath2(draw4),
    isect: isect || intersectPath2(draw4)
  };
}
var arc3 = markItemPath2("arc", arc$12);
function pickArea2(a3, p) {
  var v2 = a3[0].orient === "horizontal" ? p[1] : p[0], z = a3[0].orient === "horizontal" ? "y" : "x", i = a3.length, min4 = Infinity, hit3, d;
  while (--i >= 0) {
    if (a3[i].defined === false) continue;
    d = Math.abs(a3[i][z] - v2);
    if (d < min4) {
      min4 = d;
      hit3 = a3[i];
    }
  }
  return hit3;
}
function pickLine2(a3, p) {
  var t = Math.pow(a3[0].strokeWidth || 1, 2), i = a3.length, dx, dy, dd;
  while (--i >= 0) {
    if (a3[i].defined === false) continue;
    dx = a3[i].x - p[0];
    dy = a3[i].y - p[1];
    dd = dx * dx + dy * dy;
    if (dd < t) return a3[i];
  }
  return null;
}
function pickTrail2(a3, p) {
  var i = a3.length, dx, dy, dd;
  while (--i >= 0) {
    if (a3[i].defined === false) continue;
    dx = a3[i].x - p[0];
    dy = a3[i].y - p[1];
    dd = dx * dx + dy * dy;
    dx = a3[i].size || 1;
    if (dd < dx * dx) return a3[i];
  }
  return null;
}
function markMultiItemPath2(type4, shape3, tip) {
  function attr3(emit3, item) {
    var items = item.mark.items;
    if (items.length) emit3("d", shape3(null, items));
  }
  function bound3(bounds3, mark) {
    var items = mark.items;
    if (items.length === 0) {
      return bounds3;
    } else {
      shape3(boundContext2(bounds3), items);
      return boundStroke2(bounds3, items[0], true);
    }
  }
  function draw4(context4, items) {
    context4.beginPath();
    shape3(context4, items);
  }
  const hit3 = hitPath2(draw4);
  function pick4(context4, scene, x6, y6, gx, gy) {
    var items = scene.items, b10 = scene.bounds;
    if (!items || !items.length || b10 && !b10.contains(gx, gy)) {
      return null;
    }
    x6 *= context4.pixelRatio;
    y6 *= context4.pixelRatio;
    return hit3(context4, items, x6, y6) ? items[0] : null;
  }
  return {
    type: type4,
    tag: "path",
    nested: true,
    attr: attr3,
    bound: bound3,
    draw: drawOne2(draw4),
    pick: pick4,
    isect: intersectPoint2,
    tip
  };
}
var area3 = markMultiItemPath2("area", area$12, pickArea2);
function clip2(context4, scene) {
  var clip4 = scene.clip;
  context4.save();
  if (isFunction(clip4)) {
    context4.beginPath();
    clip4(context4);
    context4.clip();
  } else {
    clipGroup2(context4, scene.group);
  }
}
function clipGroup2(context4, group3) {
  context4.beginPath();
  hasCornerRadius2(group3) ? rectangle2(context4, group3, 0, 0) : context4.rect(0, 0, group3.width || 0, group3.height || 0);
  context4.clip();
}
function offset$12(item) {
  const sw = value2(item.strokeWidth, 1);
  return item.strokeOffset != null ? item.strokeOffset : item.stroke && sw > 0.5 && sw < 1.5 ? 0.5 - Math.abs(sw - 1) : 0;
}
function attr$52(emit3, item) {
  emit3("transform", translateItem2(item));
}
function emitRectangle2(emit3, item) {
  const off = offset$12(item);
  emit3("d", rectangle2(null, item, off, off));
}
function background2(emit3, item) {
  emit3("class", "background");
  emit3("aria-hidden", true);
  emitRectangle2(emit3, item);
}
function foreground2(emit3, item) {
  emit3("class", "foreground");
  emit3("aria-hidden", true);
  if (item.strokeForeground) {
    emitRectangle2(emit3, item);
  } else {
    emit3("d", "");
  }
}
function content2(emit3, item, renderer) {
  const url = item.clip ? clip$12(renderer, item, item) : null;
  emit3("clip-path", url);
}
function bound$52(bounds3, group3) {
  if (!group3.clip && group3.items) {
    const items = group3.items, m3 = items.length;
    for (let j = 0; j < m3; ++j) {
      bounds3.union(items[j].bounds);
    }
  }
  if ((group3.clip || group3.width || group3.height) && !group3.noBound) {
    bounds3.add(0, 0).add(group3.width || 0, group3.height || 0);
  }
  boundStroke2(bounds3, group3);
  return bounds3.translate(group3.x || 0, group3.y || 0);
}
function rectanglePath2(context4, group3, x6, y6) {
  const off = offset$12(group3);
  context4.beginPath();
  rectangle2(context4, group3, (x6 || 0) + off, (y6 || 0) + off);
}
var hitBackground2 = hitPath2(rectanglePath2);
var hitForeground2 = hitPath2(rectanglePath2, false);
var hitCorner2 = hitPath2(rectanglePath2, true);
function draw$42(context4, scene, bounds3, markTypes) {
  visit2(scene, (group3) => {
    const gx = group3.x || 0, gy = group3.y || 0, fore = group3.strokeForeground, opacity2 = group3.opacity == null ? 1 : group3.opacity;
    if ((group3.stroke || group3.fill) && opacity2) {
      rectanglePath2(context4, group3, gx, gy);
      blend2(context4, group3);
      if (group3.fill && fill2(context4, group3, opacity2)) {
        context4.fill();
      }
      if (group3.stroke && !fore && stroke2(context4, group3, opacity2)) {
        context4.stroke();
      }
    }
    context4.save();
    context4.translate(gx, gy);
    if (group3.clip) clipGroup2(context4, group3);
    if (bounds3) bounds3.translate(-gx, -gy);
    visit2(group3, (item) => {
      if (item.marktype === "group" || markTypes == null || markTypes.includes(item.marktype)) {
        this.draw(context4, item, bounds3, markTypes);
      }
    });
    if (bounds3) bounds3.translate(gx, gy);
    context4.restore();
    if (fore && group3.stroke && opacity2) {
      rectanglePath2(context4, group3, gx, gy);
      blend2(context4, group3);
      if (stroke2(context4, group3, opacity2)) {
        context4.stroke();
      }
    }
  });
}
function pick2(context4, scene, x6, y6, gx, gy) {
  if (scene.bounds && !scene.bounds.contains(gx, gy) || !scene.items) {
    return null;
  }
  const cx = x6 * context4.pixelRatio, cy = y6 * context4.pixelRatio;
  return pickVisit2(scene, (group3) => {
    let hit3, dx, dy;
    const b10 = group3.bounds;
    if (b10 && !b10.contains(gx, gy)) return;
    dx = group3.x || 0;
    dy = group3.y || 0;
    const dw = dx + (group3.width || 0), dh = dy + (group3.height || 0), c3 = group3.clip;
    if (c3 && (gx < dx || gx > dw || gy < dy || gy > dh)) return;
    context4.save();
    context4.translate(dx, dy);
    dx = gx - dx;
    dy = gy - dy;
    if (c3 && hasCornerRadius2(group3) && !hitCorner2(context4, group3, cx, cy)) {
      context4.restore();
      return null;
    }
    const fore = group3.strokeForeground, ix = scene.interactive !== false;
    if (ix && fore && group3.stroke && hitForeground2(context4, group3, cx, cy)) {
      context4.restore();
      return group3;
    }
    hit3 = pickVisit2(group3, (mark) => pickMark2(mark, dx, dy) ? this.pick(mark, x6, y6, dx, dy) : null);
    if (!hit3 && ix && (group3.fill || !fore && group3.stroke) && hitBackground2(context4, group3, cx, cy)) {
      hit3 = group3;
    }
    context4.restore();
    return hit3 || null;
  });
}
function pickMark2(mark, x6, y6) {
  return (mark.interactive !== false || mark.marktype === "group") && mark.bounds && mark.bounds.contains(x6, y6);
}
var group2 = {
  type: "group",
  tag: "g",
  nested: false,
  attr: attr$52,
  bound: bound$52,
  draw: draw$42,
  pick: pick2,
  isect: intersectRect2,
  content: content2,
  background: background2,
  foreground: foreground2
};
var metadata2 = {
  "xmlns": "http://www.w3.org/2000/svg",
  "xmlns:xlink": "http://www.w3.org/1999/xlink",
  "version": "1.1"
};
function getImage2(item, renderer) {
  var image4 = item.image;
  if (!image4 || item.url && item.url !== image4.url) {
    image4 = {
      complete: false,
      width: 0,
      height: 0
    };
    renderer.loadImage(item.url).then((image5) => {
      item.image = image5;
      item.image.url = item.url;
    });
  }
  return image4;
}
function imageWidth2(item, image4) {
  return item.width != null ? item.width : !image4 || !image4.width ? 0 : item.aspect !== false && item.height ? item.height * image4.width / image4.height : image4.width;
}
function imageHeight2(item, image4) {
  return item.height != null ? item.height : !image4 || !image4.height ? 0 : item.aspect !== false && item.width ? item.width * image4.height / image4.width : image4.height;
}
function imageXOffset2(align2, w3) {
  return align2 === "center" ? w3 / 2 : align2 === "right" ? w3 : 0;
}
function imageYOffset2(baseline3, h3) {
  return baseline3 === "middle" ? h3 / 2 : baseline3 === "bottom" ? h3 : 0;
}
function attr$42(emit3, item, renderer) {
  const img = getImage2(item, renderer), w3 = imageWidth2(item, img), h3 = imageHeight2(item, img), x6 = (item.x || 0) - imageXOffset2(item.align, w3), y6 = (item.y || 0) - imageYOffset2(item.baseline, h3), i = !img.src && img.toDataURL ? img.toDataURL() : img.src || "";
  emit3("href", i, metadata2["xmlns:xlink"], "xlink:href");
  emit3("transform", translate3(x6, y6));
  emit3("width", w3);
  emit3("height", h3);
  emit3("preserveAspectRatio", item.aspect === false ? "none" : "xMidYMid");
}
function bound$42(bounds3, item) {
  const img = item.image, w3 = imageWidth2(item, img), h3 = imageHeight2(item, img), x6 = (item.x || 0) - imageXOffset2(item.align, w3), y6 = (item.y || 0) - imageYOffset2(item.baseline, h3);
  return bounds3.set(x6, y6, x6 + w3, y6 + h3);
}
function draw$32(context4, scene, bounds3) {
  visit2(scene, (item) => {
    if (bounds3 && !bounds3.intersects(item.bounds)) return;
    const img = getImage2(item, this);
    let w3 = imageWidth2(item, img);
    let h3 = imageHeight2(item, img);
    if (w3 === 0 || h3 === 0) return;
    let x6 = (item.x || 0) - imageXOffset2(item.align, w3), y6 = (item.y || 0) - imageYOffset2(item.baseline, h3), opacity2, ar0, ar1, t;
    if (item.aspect !== false) {
      ar0 = img.width / img.height;
      ar1 = item.width / item.height;
      if (ar0 === ar0 && ar1 === ar1 && ar0 !== ar1) {
        if (ar1 < ar0) {
          t = w3 / ar0;
          y6 += (h3 - t) / 2;
          h3 = t;
        } else {
          t = h3 * ar0;
          x6 += (w3 - t) / 2;
          w3 = t;
        }
      }
    }
    if (img.complete || img.toDataURL) {
      blend2(context4, item);
      context4.globalAlpha = (opacity2 = item.opacity) != null ? opacity2 : 1;
      context4.imageSmoothingEnabled = item.smooth !== false;
      context4.drawImage(img, x6, y6, w3, h3);
    }
  });
}
var image2 = {
  type: "image",
  tag: "image",
  nested: false,
  attr: attr$42,
  bound: bound$42,
  draw: draw$32,
  pick: pick$12(),
  isect: truthy,
  // bounds check is sufficient
  get: getImage2,
  xOffset: imageXOffset2,
  yOffset: imageYOffset2
};
var line3 = markMultiItemPath2("line", line$12, pickLine2);
function attr$32(emit3, item) {
  var sx = item.scaleX || 1, sy = item.scaleY || 1;
  if (sx !== 1 || sy !== 1) {
    emit3("vector-effect", "non-scaling-stroke");
  }
  emit3("transform", transformItem2(item));
  emit3("d", item.path);
}
function path$12(context4, item) {
  var path4 = item.path;
  if (path4 == null) return true;
  var x6 = item.x || 0, y6 = item.y || 0, sx = item.scaleX || 1, sy = item.scaleY || 1, a3 = (item.angle || 0) * DegToRad2, cache2 = item.pathCache;
  if (!cache2 || cache2.path !== path4) {
    (item.pathCache = cache2 = parse5(path4)).path = path4;
  }
  if (a3 && context4.rotate && context4.translate) {
    context4.translate(x6, y6);
    context4.rotate(a3);
    pathRender2(context4, cache2, 0, 0, sx, sy);
    context4.rotate(-a3);
    context4.translate(-x6, -y6);
  } else {
    pathRender2(context4, cache2, x6, y6, sx, sy);
  }
}
function bound$32(bounds3, item) {
  return path$12(boundContext2(bounds3, item.angle), item) ? bounds3.set(0, 0, 0, 0) : boundStroke2(bounds3, item, true);
}
var path$22 = {
  type: "path",
  tag: "path",
  nested: false,
  attr: attr$32,
  bound: bound$32,
  draw: drawAll2(path$12),
  pick: pickPath2(path$12),
  isect: intersectPath2(path$12)
};
function attr$22(emit3, item) {
  emit3("d", rectangle2(null, item));
}
function bound$22(bounds3, item) {
  var x6, y6;
  return boundStroke2(bounds3.set(x6 = item.x || 0, y6 = item.y || 0, x6 + item.width || 0, y6 + item.height || 0), item);
}
function draw$22(context4, item) {
  context4.beginPath();
  rectangle2(context4, item);
}
var rect2 = {
  type: "rect",
  tag: "path",
  nested: false,
  attr: attr$22,
  bound: bound$22,
  draw: drawAll2(draw$22),
  pick: pickPath2(draw$22),
  isect: intersectRect2
};
function attr$12(emit3, item) {
  emit3("transform", translateItem2(item));
  emit3("x2", item.x2 != null ? item.x2 - (item.x || 0) : 0);
  emit3("y2", item.y2 != null ? item.y2 - (item.y || 0) : 0);
}
function bound$12(bounds3, item) {
  var x12, y12;
  return boundStroke2(bounds3.set(x12 = item.x || 0, y12 = item.y || 0, item.x2 != null ? item.x2 : x12, item.y2 != null ? item.y2 : y12), item);
}
function path3(context4, item, opacity2) {
  var x12, y12, x22, y22;
  if (item.stroke && stroke2(context4, item, opacity2)) {
    x12 = item.x || 0;
    y12 = item.y || 0;
    x22 = item.x2 != null ? item.x2 : x12;
    y22 = item.y2 != null ? item.y2 : y12;
    context4.beginPath();
    context4.moveTo(x12, y12);
    context4.lineTo(x22, y22);
    return true;
  }
  return false;
}
function draw$12(context4, scene, bounds3) {
  visit2(scene, (item) => {
    if (bounds3 && !bounds3.intersects(item.bounds)) return;
    var opacity2 = item.opacity == null ? 1 : item.opacity;
    if (opacity2 && path3(context4, item, opacity2)) {
      blend2(context4, item);
      context4.stroke();
    }
  });
}
function hit$12(context4, item, x6, y6) {
  if (!context4.isPointInStroke) return false;
  return path3(context4, item, 1) && context4.isPointInStroke(x6, y6);
}
var rule2 = {
  type: "rule",
  tag: "line",
  nested: false,
  attr: attr$12,
  bound: bound$12,
  draw: draw$12,
  pick: pick$12(hit$12),
  isect: intersectRule2
};
var shape2 = markItemPath2("shape", shape$12);
var symbol2 = markItemPath2("symbol", symbol$12, intersectPoint2);
var widthCache2 = lruCache_default();
var textMetrics2 = {
  height: fontSize2,
  measureWidth: measureWidth2,
  estimateWidth: estimateWidth2,
  width: estimateWidth2,
  canvas: useCanvas2
};
useCanvas2(true);
function useCanvas2(use) {
  textMetrics2.width = use && context2 ? measureWidth2 : estimateWidth2;
}
function estimateWidth2(item, text4) {
  return _estimateWidth2(textValue2(item, text4), fontSize2(item));
}
function _estimateWidth2(text4, currentFontHeight) {
  return ~~(0.8 * text4.length * currentFontHeight);
}
function measureWidth2(item, text4) {
  return fontSize2(item) <= 0 || !(text4 = textValue2(item, text4)) ? 0 : _measureWidth2(text4, font2(item));
}
function _measureWidth2(text4, currentFont) {
  const key2 = `(${currentFont}) ${text4}`;
  let width2 = widthCache2.get(key2);
  if (width2 === void 0) {
    context2.font = currentFont;
    width2 = context2.measureText(text4).width;
    widthCache2.set(key2, width2);
  }
  return width2;
}
function fontSize2(item) {
  return item.fontSize != null ? +item.fontSize || 0 : 11;
}
function lineHeight2(item) {
  return item.lineHeight != null ? item.lineHeight : fontSize2(item) + 2;
}
function lineArray2(_) {
  return isArray(_) ? _.length > 1 ? _ : _[0] : _;
}
function textLines2(item) {
  return lineArray2(item.lineBreak && item.text && !isArray(item.text) ? item.text.split(item.lineBreak) : item.text);
}
function textValue2(item, line5) {
  const text4 = line5 == null ? "" : (line5 + "").trim();
  return item.limit > 0 && text4.length ? truncate3(item, text4) : text4;
}
function widthGetter2(item) {
  if (textMetrics2.width === measureWidth2) {
    const currentFont = font2(item);
    return (text4) => _measureWidth2(text4, currentFont);
  } else if (textMetrics2.width === estimateWidth2) {
    const currentFontHeight = fontSize2(item);
    return (text4) => _estimateWidth2(text4, currentFontHeight);
  } else {
    return (text4) => textMetrics2.width(item, text4);
  }
}
function truncate3(item, text4) {
  var limit = +item.limit, width2 = widthGetter2(item);
  if (width2(text4) < limit) return text4;
  var ellipsis = item.ellipsis || "\u2026", rtl = item.dir === "rtl", lo = 0, hi = text4.length, mid;
  limit -= width2(ellipsis);
  if (rtl) {
    while (lo < hi) {
      mid = lo + hi >>> 1;
      if (width2(text4.slice(mid)) > limit) lo = mid + 1;
      else hi = mid;
    }
    return ellipsis + text4.slice(lo);
  } else {
    while (lo < hi) {
      mid = 1 + (lo + hi >>> 1);
      if (width2(text4.slice(0, mid)) < limit) lo = mid;
      else hi = mid - 1;
    }
    return text4.slice(0, lo) + ellipsis;
  }
}
function fontFamily2(item, quote) {
  var font3 = item.font;
  return (quote && font3 ? String(font3).replace(/"/g, "'") : font3) || "sans-serif";
}
function font2(item, quote) {
  return (item.fontStyle ? item.fontStyle + " " : "") + (item.fontVariant ? item.fontVariant + " " : "") + (item.fontWeight ? item.fontWeight + " " : "") + fontSize2(item) + "px " + fontFamily2(item, quote);
}
function offset4(item) {
  var baseline3 = item.baseline, h3 = fontSize2(item);
  return Math.round(baseline3 === "top" ? 0.79 * h3 : baseline3 === "middle" ? 0.3 * h3 : baseline3 === "bottom" ? -0.21 * h3 : baseline3 === "line-top" ? 0.29 * h3 + 0.5 * lineHeight2(item) : baseline3 === "line-bottom" ? 0.29 * h3 - 0.5 * lineHeight2(item) : 0);
}
var textAlign2 = {
  "left": "start",
  "center": "middle",
  "right": "end"
};
var tempBounds3 = new Bounds3();
function anchorPoint2(item) {
  var x6 = item.x || 0, y6 = item.y || 0, r = item.radius || 0, t;
  if (r) {
    t = (item.theta || 0) - HalfPi2;
    x6 += r * Math.cos(t);
    y6 += r * Math.sin(t);
  }
  tempBounds3.x1 = x6;
  tempBounds3.y1 = y6;
  return tempBounds3;
}
function attr2(emit3, item) {
  var dx = item.dx || 0, dy = (item.dy || 0) + offset4(item), p = anchorPoint2(item), x6 = p.x1, y6 = p.y1, a3 = item.angle || 0, t;
  emit3("text-anchor", textAlign2[item.align] || "start");
  if (a3) {
    t = translate3(x6, y6) + " " + rotate2(a3);
    if (dx || dy) t += " " + translate3(dx, dy);
  } else {
    t = translate3(x6 + dx, y6 + dy);
  }
  emit3("transform", t);
}
function bound2(bounds3, item, mode) {
  var h3 = textMetrics2.height(item), a3 = item.align, p = anchorPoint2(item), x6 = p.x1, y6 = p.y1, dx = item.dx || 0, dy = (item.dy || 0) + offset4(item) - Math.round(0.8 * h3), tl3 = textLines2(item), w3;
  if (isArray(tl3)) {
    h3 += lineHeight2(item) * (tl3.length - 1);
    w3 = tl3.reduce((w4, t) => Math.max(w4, textMetrics2.width(item, t)), 0);
  } else {
    w3 = textMetrics2.width(item, tl3);
  }
  if (a3 === "center") {
    dx -= w3 / 2;
  } else if (a3 === "right") {
    dx -= w3;
  } else ;
  bounds3.set(dx += x6, dy += y6, dx + w3, dy + h3);
  if (item.angle && !mode) {
    bounds3.rotate(item.angle * DegToRad2, x6, y6);
  } else if (mode === 2) {
    return bounds3.rotatedPoints(item.angle * DegToRad2, x6, y6);
  }
  return bounds3;
}
function draw3(context4, scene, bounds3) {
  visit2(scene, (item) => {
    var opacity2 = item.opacity == null ? 1 : item.opacity, p, x6, y6, i, lh, tl3, str;
    if (bounds3 && !bounds3.intersects(item.bounds) || // bounds check
    opacity2 === 0 || item.fontSize <= 0 || item.text == null || item.text.length === 0) return;
    context4.font = font2(item);
    context4.textAlign = item.align || "left";
    p = anchorPoint2(item);
    x6 = p.x1, y6 = p.y1;
    if (item.angle) {
      context4.save();
      context4.translate(x6, y6);
      context4.rotate(item.angle * DegToRad2);
      x6 = y6 = 0;
    }
    x6 += item.dx || 0;
    y6 += (item.dy || 0) + offset4(item);
    tl3 = textLines2(item);
    blend2(context4, item);
    if (isArray(tl3)) {
      lh = lineHeight2(item);
      for (i = 0; i < tl3.length; ++i) {
        str = textValue2(item, tl3[i]);
        if (item.fill && fill2(context4, item, opacity2)) {
          context4.fillText(str, x6, y6);
        }
        if (item.stroke && stroke2(context4, item, opacity2)) {
          context4.strokeText(str, x6, y6);
        }
        y6 += lh;
      }
    } else {
      str = textValue2(item, tl3);
      if (item.fill && fill2(context4, item, opacity2)) {
        context4.fillText(str, x6, y6);
      }
      if (item.stroke && stroke2(context4, item, opacity2)) {
        context4.strokeText(str, x6, y6);
      }
    }
    if (item.angle) context4.restore();
  });
}
function hit2(context4, item, x6, y6, gx, gy) {
  if (item.fontSize <= 0) return false;
  if (!item.angle) return true;
  var p = anchorPoint2(item), ax = p.x1, ay = p.y1, b10 = bound2(tempBounds3, item, 1), a3 = -item.angle * DegToRad2, cos4 = Math.cos(a3), sin4 = Math.sin(a3), px3 = cos4 * gx - sin4 * gy + (ax - cos4 * ax + sin4 * ay), py3 = sin4 * gx + cos4 * gy + (ay - sin4 * ax - cos4 * ay);
  return b10.contains(px3, py3);
}
function intersectText2(item, box) {
  const p = bound2(tempBounds3, item, 2);
  return intersectBoxLine2(box, p[0], p[1], p[2], p[3]) || intersectBoxLine2(box, p[0], p[1], p[4], p[5]) || intersectBoxLine2(box, p[4], p[5], p[6], p[7]) || intersectBoxLine2(box, p[2], p[3], p[6], p[7]);
}
var text2 = {
  type: "text",
  tag: "text",
  nested: false,
  attr: attr2,
  bound: bound2,
  draw: draw3,
  pick: pick$12(hit2),
  isect: intersectText2
};
var trail2 = markMultiItemPath2("trail", trail$12, pickTrail2);
var Marks2 = {
  arc: arc3,
  area: area3,
  group: group2,
  image: image2,
  line: line3,
  path: path$22,
  rect: rect2,
  rule: rule2,
  shape: shape2,
  symbol: symbol2,
  text: text2,
  trail: trail2
};
function boundItem2(item, func, opt) {
  var type4 = Marks2[item.mark.marktype], bound3 = func || type4.bound;
  if (type4.nested) item = item.mark;
  return bound3(item.bounds || (item.bounds = new Bounds3()), item, opt);
}
var DUMMY = {
  mark: null
};
function boundMark(mark, bounds3, opt) {
  var type4 = Marks2[mark.marktype], bound3 = type4.bound, items = mark.items, hasItems = items && items.length, i, n, item, b10;
  if (type4.nested) {
    if (hasItems) {
      item = items[0];
    } else {
      DUMMY.mark = mark;
      item = DUMMY;
    }
    b10 = boundItem2(item, bound3, opt);
    bounds3 = bounds3 && bounds3.union(b10) || b10;
    return bounds3;
  }
  bounds3 = bounds3 || mark.bounds && mark.bounds.clear() || new Bounds3();
  if (hasItems) {
    for (i = 0, n = items.length; i < n; ++i) {
      bounds3.union(boundItem2(items[i], bound3, opt));
    }
  }
  return mark.bounds = bounds3;
}
var keys = [
  "marktype",
  "name",
  "role",
  "interactive",
  "clip",
  "items",
  "zindex",
  "x",
  "y",
  "width",
  "height",
  "align",
  "baseline",
  // layout
  "fill",
  "fillOpacity",
  "opacity",
  "blend",
  // fill
  "stroke",
  "strokeOpacity",
  "strokeWidth",
  "strokeCap",
  // stroke
  "strokeDash",
  "strokeDashOffset",
  // stroke dash
  "strokeForeground",
  "strokeOffset",
  // group
  "startAngle",
  "endAngle",
  "innerRadius",
  "outerRadius",
  // arc
  "cornerRadius",
  "padAngle",
  // arc, rect
  "cornerRadiusTopLeft",
  "cornerRadiusTopRight",
  // rect, group
  "cornerRadiusBottomLeft",
  "cornerRadiusBottomRight",
  "interpolate",
  "tension",
  "orient",
  "defined",
  // area, line
  "url",
  "aspect",
  "smooth",
  // image
  "path",
  "scaleX",
  "scaleY",
  // path
  "x2",
  "y2",
  // rule
  "size",
  "shape",
  // symbol
  "text",
  "angle",
  "theta",
  "radius",
  "dir",
  "dx",
  "dy",
  // text
  "ellipsis",
  "limit",
  "lineBreak",
  "lineHeight",
  "font",
  "fontSize",
  "fontWeight",
  "fontStyle",
  "fontVariant",
  // font
  "description",
  "aria",
  "ariaRole",
  "ariaRoleDescription"
  // aria
];
function sceneToJSON(scene, indent) {
  return JSON.stringify(scene, keys, indent);
}
function sceneFromJSON(json2) {
  const scene = typeof json2 === "string" ? JSON.parse(json2) : json2;
  return initialize(scene);
}
function initialize(scene) {
  var type4 = scene.marktype, items = scene.items, parent, i, n;
  if (items) {
    for (i = 0, n = items.length; i < n; ++i) {
      parent = type4 ? "mark" : "group";
      items[i][parent] = scene;
      if (items[i].zindex) items[i][parent].zdirty = true;
      if ("group" === (type4 || parent)) initialize(items[i]);
    }
  }
  if (type4) boundMark(scene);
  return scene;
}
var Scenegraph = class {
  constructor(scene) {
    if (arguments.length) {
      this.root = sceneFromJSON(scene);
    } else {
      this.root = createMark({
        marktype: "group",
        name: "root",
        role: "frame"
      });
      this.root.items = [new GroupItem2(this.root)];
    }
  }
  toJSON(indent) {
    return sceneToJSON(this.root, indent || 0);
  }
  mark(markdef, group3, index4) {
    group3 = group3 || this.root.items[0];
    const mark = createMark(markdef, group3);
    group3.items[index4] = mark;
    if (mark.zindex) mark.group.zdirty = true;
    return mark;
  }
};
function createMark(def3, group3) {
  const mark = {
    bounds: new Bounds3(),
    clip: !!def3.clip,
    group: group3,
    interactive: def3.interactive === false ? false : true,
    items: [],
    marktype: def3.marktype,
    name: def3.name || void 0,
    role: def3.role || void 0,
    zindex: def3.zindex || 0
  };
  if (def3.aria != null) {
    mark.aria = def3.aria;
  }
  if (def3.description) {
    mark.description = def3.description;
  }
  return mark;
}
function domCreate2(doc, tag, ns) {
  if (!doc && typeof document !== "undefined" && document.createElement) {
    doc = document;
  }
  return doc ? ns ? doc.createElementNS(ns, tag) : doc.createElement(tag) : null;
}
function domFind2(el, tag) {
  tag = tag.toLowerCase();
  var nodes = el.childNodes, i = 0, n = nodes.length;
  for (; i < n; ++i) if (nodes[i].tagName.toLowerCase() === tag) {
    return nodes[i];
  }
}
function domChild2(el, index4, tag, ns) {
  var a3 = el.childNodes[index4], b10;
  if (!a3 || a3.tagName.toLowerCase() !== tag.toLowerCase()) {
    b10 = a3 || null;
    a3 = domCreate2(el.ownerDocument, tag, ns);
    el.insertBefore(a3, b10);
  }
  return a3;
}
function domClear2(el, index4) {
  var nodes = el.childNodes, curr = nodes.length;
  while (curr > index4) el.removeChild(nodes[--curr]);
  return el;
}
function cssClass2(mark) {
  return "mark-" + mark.marktype + (mark.role ? " role-" + mark.role : "") + (mark.name ? " " + mark.name : "");
}
function point7(event2, el) {
  if (el.getScreenCTM) {
    const p = ctmPoint2(event2, el.getScreenCTM());
    if (p) return p;
  }
  return rectPoint2(event2, el, el.getBoundingClientRect());
}
function ctmPoint2(event2, ctm) {
  if (!ctm) return null;
  const det = ctm.a * ctm.d - ctm.b * ctm.c;
  if (!det) return null;
  const x6 = event2.clientX - ctm.e, y6 = event2.clientY - ctm.f;
  return [(ctm.d * x6 - ctm.c * y6) / det, (ctm.a * y6 - ctm.b * x6) / det];
}
function rectPoint2(event2, el, rect4) {
  const sx = el.offsetWidth ? rect4.width / el.offsetWidth : 1, sy = el.offsetHeight ? rect4.height / el.offsetHeight : 1;
  return [(event2.clientX - rect4.left) / sx - (el.clientLeft || 0), (event2.clientY - rect4.top) / sy - (el.clientTop || 0)];
}
function resolveItem2(item, event2, el, origin) {
  var mark = item && item.mark, mdef, p;
  if (mark && (mdef = Marks2[mark.marktype]).tip) {
    p = point7(event2, el);
    p[0] -= origin[0];
    p[1] -= origin[1];
    while (item = item.mark.group) {
      p[0] -= item.x || 0;
      p[1] -= item.y || 0;
    }
    item = mdef.tip(mark.items, p);
  }
  return item;
}
var Handler2 = class {
  /**
   * Create a new Handler instance.
   * @param {object} [customLoader] - Optional loader instance for
   *   href URL sanitization. If not specified, a standard loader
   *   instance will be generated.
   * @param {function} [customTooltip] - Optional tooltip handler
   *   function for custom tooltip display.
   * @constructor
   */
  constructor(customLoader, customTooltip) {
    this._active = null;
    this._handlers = {};
    this._loader = customLoader || loader();
    this._tooltip = customTooltip || defaultTooltip2;
  }
  /**
   * Initialize a new Handler instance.
   * @param {DOMElement} el - The containing DOM element for the display.
   * @param {Array<number>} origin - The origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {object} [obj] - Optional context object that should serve as
   *   the "this" context for event callbacks.
   * @return {Handler} - This handler instance.
   */
  initialize(el, origin, obj) {
    this._el = el;
    this._obj = obj || null;
    return this.origin(origin);
  }
  /**
   * Returns the parent container element for a visualization.
   * @return {DOMElement} - The containing DOM element.
   */
  element() {
    return this._el;
  }
  /**
   * Returns the scene element (e.g., canvas or SVG) of the visualization
   * Subclasses must override if the first child is not the scene element.
   * @return {DOMElement} - The scene (e.g., canvas or SVG) element.
   */
  canvas() {
    return this._el && this._el.firstChild;
  }
  /**
   * Get / set the origin coordinates of the visualization.
   */
  origin(origin) {
    if (arguments.length) {
      this._origin = origin || [0, 0];
      return this;
    } else {
      return this._origin.slice();
    }
  }
  /**
   * Get / set the scenegraph root.
   */
  scene(scene) {
    if (!arguments.length) return this._scene;
    this._scene = scene;
    return this;
  }
  /**
   * Add an event handler. Subclasses should override this method.
   */
  on() {
  }
  /**
   * Remove an event handler. Subclasses should override this method.
   */
  off() {
  }
  /**
   * Utility method for finding the array index of an event handler.
   * @param {Array} h - An array of registered event handlers.
   * @param {string} type - The event type.
   * @param {function} handler - The event handler instance to find.
   * @return {number} - The handler's array index or -1 if not registered.
   */
  _handlerIndex(h3, type4, handler) {
    for (let i = h3 ? h3.length : 0; --i >= 0; ) {
      if (h3[i].type === type4 && (!handler || h3[i].handler === handler)) {
        return i;
      }
    }
    return -1;
  }
  /**
   * Returns an array with registered event handlers.
   * @param {string} [type] - The event type to query. Any annotations
   *   are ignored; for example, for the argument "click.foo", ".foo" will
   *   be ignored and the method returns all "click" handlers. If type is
   *   null or unspecified, this method returns handlers for all types.
   * @return {Array} - A new array containing all registered event handlers.
   */
  handlers(type4) {
    const h3 = this._handlers, a3 = [];
    if (type4) {
      a3.push(...h3[this.eventName(type4)]);
    } else {
      for (const k in h3) {
        a3.push(...h3[k]);
      }
    }
    return a3;
  }
  /**
   * Parses an event name string to return the specific event type.
   * For example, given "click.foo" returns "click"
   * @param {string} name - The input event type string.
   * @return {string} - A string with the event type only.
   */
  eventName(name) {
    const i = name.indexOf(".");
    return i < 0 ? name : name.slice(0, i);
  }
  /**
   * Handle hyperlink navigation in response to an item.href value.
   * @param {Event} event - The event triggering hyperlink navigation.
   * @param {Item} item - The scenegraph item.
   * @param {string} href - The URL to navigate to.
   */
  handleHref(event2, item, href3) {
    this._loader.sanitize(href3, {
      context: "href"
    }).then((opt) => {
      const e = new MouseEvent(event2.type, event2), a3 = domCreate2(null, "a");
      for (const name in opt) a3.setAttribute(name, opt[name]);
      a3.dispatchEvent(e);
    }).catch(() => {
    });
  }
  /**
   * Handle tooltip display in response to an item.tooltip value.
   * @param {Event} event - The event triggering tooltip display.
   * @param {Item} item - The scenegraph item.
   * @param {boolean} show - A boolean flag indicating whether
   *   to show or hide a tooltip for the given item.
   */
  handleTooltip(event2, item, show) {
    if (item && item.tooltip != null) {
      item = resolveItem2(item, event2, this.canvas(), this._origin);
      const value4 = show && item && item.tooltip || null;
      this._tooltip.call(this._obj, this, event2, item, value4);
    }
  }
  /**
   * Returns the size of a scenegraph item and its position relative
   * to the viewport.
   * @param {Item} item - The scenegraph item.
   * @return {object} - A bounding box object (compatible with the
   *   DOMRect type) consisting of x, y, width, heigh, top, left,
   *   right, and bottom properties.
   */
  getItemBoundingClientRect(item) {
    const el = this.canvas();
    if (!el) return;
    const rect4 = el.getBoundingClientRect(), origin = this._origin, bounds3 = item.bounds, width2 = bounds3.width(), height2 = bounds3.height();
    let x6 = bounds3.x1 + origin[0] + rect4.left, y6 = bounds3.y1 + origin[1] + rect4.top;
    while (item.mark && (item = item.mark.group)) {
      x6 += item.x || 0;
      y6 += item.y || 0;
    }
    return {
      x: x6,
      y: y6,
      width: width2,
      height: height2,
      left: x6,
      top: y6,
      right: x6 + width2,
      bottom: y6 + height2
    };
  }
};
function defaultTooltip2(handler, event2, item, value4) {
  handler.element().setAttribute("title", value4 || "");
}
var Renderer2 = class {
  /**
   * Create a new Renderer instance.
   * @param {object} [loader] - Optional loader instance for
   *   image and href URL sanitization. If not specified, a
   *   standard loader instance will be generated.
   * @constructor
   */
  constructor(loader2) {
    this._el = null;
    this._bgcolor = null;
    this._loader = new ResourceLoader2(loader2);
  }
  /**
   * Initialize a new Renderer instance.
   * @param {DOMElement} el - The containing DOM element for the display.
   * @param {number} width - The coordinate width of the display, in pixels.
   * @param {number} height - The coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {Renderer} - This renderer instance.
   */
  initialize(el, width2, height2, origin, scaleFactor) {
    this._el = el;
    return this.resize(width2, height2, origin, scaleFactor);
  }
  /**
   * Returns the parent container element for a visualization.
   * @return {DOMElement} - The containing DOM element.
   */
  element() {
    return this._el;
  }
  /**
   * Returns the scene element (e.g., canvas or SVG) of the visualization
   * Subclasses must override if the first child is not the scene element.
   * @return {DOMElement} - The scene (e.g., canvas or SVG) element.
   */
  canvas() {
    return this._el && this._el.firstChild;
  }
  /**
   * Get / set the background color.
   */
  background(bgcolor) {
    if (arguments.length === 0) return this._bgcolor;
    this._bgcolor = bgcolor;
    return this;
  }
  /**
   * Resize the display.
   * @param {number} width - The new coordinate width of the display, in pixels.
   * @param {number} height - The new coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The new origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {Renderer} - This renderer instance;
   */
  resize(width2, height2, origin, scaleFactor) {
    this._width = width2;
    this._height = height2;
    this._origin = origin || [0, 0];
    this._scale = scaleFactor || 1;
    return this;
  }
  /**
   * Report a dirty item whose bounds should be redrawn.
   * This base class method does nothing. Subclasses that perform
   * incremental should implement this method.
   * @param {Item} item - The dirty item whose bounds should be redrawn.
   */
  dirty() {
  }
  /**
   * Render an input scenegraph, potentially with a set of dirty items.
   * This method will perform an immediate rendering with available resources.
   * The renderer may also need to perform image loading to perform a complete
   * render. This process can lead to asynchronous re-rendering of the scene
   * after this method returns. To receive notification when rendering is
   * complete, use the renderAsync method instead.
   * @param {object} scene - The root mark of a scenegraph to render.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   * @return {Renderer} - This renderer instance.
   */
  render(scene, markTypes) {
    const r = this;
    r._call = function() {
      r._render(scene, markTypes);
    };
    r._call();
    r._call = null;
    return r;
  }
  /**
   * Internal rendering method. Renderer subclasses should override this
   * method to actually perform rendering.
   * @param {object} scene - The root mark of a scenegraph to render.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   */
  _render() {
  }
  /**
   * Asynchronous rendering method. Similar to render, but returns a Promise
   * that resolves when all rendering is completed. Sometimes a renderer must
   * perform image loading to get a complete rendering. The returned
   * Promise will not resolve until this process completes.
   * @param {object} scene - The root mark of a scenegraph to render.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   * @return {Promise} - A Promise that resolves when rendering is complete.
   */
  renderAsync(scene, markTypes) {
    const r = this.render(scene, markTypes);
    return this._ready ? this._ready.then(() => r) : Promise.resolve(r);
  }
  /**
   * Internal method for asynchronous resource loading.
   * Proxies method calls to the ImageLoader, and tracks loading
   * progress to invoke a re-render once complete.
   * @param {string} method - The method name to invoke on the ImageLoader.
   * @param {string} uri - The URI for the requested resource.
   * @return {Promise} - A Promise that resolves to the requested resource.
   */
  _load(method2, uri) {
    var r = this, p = r._loader[method2](uri);
    if (!r._ready) {
      const call = r._call;
      r._ready = r._loader.ready().then((redraw) => {
        if (redraw) call();
        r._ready = null;
      });
    }
    return p;
  }
  /**
   * Sanitize a URL to include as a hyperlink in the rendered scene.
   * This method proxies a call to ImageLoader.sanitizeURL, but also tracks
   * image loading progress and invokes a re-render once complete.
   * @param {string} uri - The URI string to sanitize.
   * @return {Promise} - A Promise that resolves to the sanitized URL.
   */
  sanitizeURL(uri) {
    return this._load("sanitizeURL", uri);
  }
  /**
   * Requests an image to include in the rendered scene.
   * This method proxies a call to ImageLoader.loadImage, but also tracks
   * image loading progress and invokes a re-render once complete.
   * @param {string} uri - The URI string of the image.
   * @return {Promise} - A Promise that resolves to the loaded Image.
   */
  loadImage(uri) {
    return this._load("loadImage", uri);
  }
};
var KeyDownEvent2 = "keydown";
var KeyPressEvent2 = "keypress";
var KeyUpEvent2 = "keyup";
var DragEnterEvent2 = "dragenter";
var DragLeaveEvent2 = "dragleave";
var DragOverEvent2 = "dragover";
var PointerDownEvent2 = "pointerdown";
var PointerUpEvent2 = "pointerup";
var PointerMoveEvent2 = "pointermove";
var PointerOutEvent2 = "pointerout";
var PointerOverEvent2 = "pointerover";
var MouseDownEvent2 = "mousedown";
var MouseUpEvent2 = "mouseup";
var MouseMoveEvent2 = "mousemove";
var MouseOutEvent2 = "mouseout";
var MouseOverEvent2 = "mouseover";
var ClickEvent2 = "click";
var DoubleClickEvent2 = "dblclick";
var WheelEvent2 = "wheel";
var MouseWheelEvent2 = "mousewheel";
var TouchStartEvent2 = "touchstart";
var TouchMoveEvent2 = "touchmove";
var TouchEndEvent2 = "touchend";
var Events2 = [KeyDownEvent2, KeyPressEvent2, KeyUpEvent2, DragEnterEvent2, DragLeaveEvent2, DragOverEvent2, PointerDownEvent2, PointerUpEvent2, PointerMoveEvent2, PointerOutEvent2, PointerOverEvent2, MouseDownEvent2, MouseUpEvent2, MouseMoveEvent2, MouseOutEvent2, MouseOverEvent2, ClickEvent2, DoubleClickEvent2, WheelEvent2, MouseWheelEvent2, TouchStartEvent2, TouchMoveEvent2, TouchEndEvent2];
var TooltipShowEvent2 = PointerMoveEvent2;
var TooltipHideEvent2 = MouseOutEvent2;
var HrefEvent2 = ClickEvent2;
var CanvasHandler2 = class extends Handler2 {
  constructor(loader2, tooltip2) {
    super(loader2, tooltip2);
    this._down = null;
    this._touch = null;
    this._first = true;
    this._events = {};
    this.events = Events2;
    this.pointermove = move2([PointerMoveEvent2, MouseMoveEvent2], [PointerOverEvent2, MouseOverEvent2], [PointerOutEvent2, MouseOutEvent2]);
    this.dragover = move2([DragOverEvent2], [DragEnterEvent2], [DragLeaveEvent2]), this.pointerout = inactive2([PointerOutEvent2, MouseOutEvent2]);
    this.dragleave = inactive2([DragLeaveEvent2]);
  }
  initialize(el, origin, obj) {
    this._canvas = el && domFind2(el, "canvas");
    [ClickEvent2, MouseDownEvent2, PointerDownEvent2, PointerMoveEvent2, PointerOutEvent2, DragLeaveEvent2].forEach((type4) => eventListenerCheck2(this, type4));
    return super.initialize(el, origin, obj);
  }
  // return the backing canvas instance
  canvas() {
    return this._canvas;
  }
  // retrieve the current canvas context
  context() {
    return this._canvas.getContext("2d");
  }
  // to keep old versions of firefox happy
  DOMMouseScroll(evt) {
    this.fire(MouseWheelEvent2, evt);
  }
  pointerdown(evt) {
    this._down = this._active;
    this.fire(PointerDownEvent2, evt);
  }
  mousedown(evt) {
    this._down = this._active;
    this.fire(MouseDownEvent2, evt);
  }
  click(evt) {
    if (this._down === this._active) {
      this.fire(ClickEvent2, evt);
      this._down = null;
    }
  }
  touchstart(evt) {
    this._touch = this.pickEvent(evt.changedTouches[0]);
    if (this._first) {
      this._active = this._touch;
      this._first = false;
    }
    this.fire(TouchStartEvent2, evt, true);
  }
  touchmove(evt) {
    this.fire(TouchMoveEvent2, evt, true);
  }
  touchend(evt) {
    this.fire(TouchEndEvent2, evt, true);
    this._touch = null;
  }
  // fire an event
  fire(type4, evt, touch2) {
    const a3 = touch2 ? this._touch : this._active, h3 = this._handlers[type4];
    evt.vegaType = type4;
    if (type4 === HrefEvent2 && a3 && a3.href) {
      this.handleHref(evt, a3, a3.href);
    } else if (type4 === TooltipShowEvent2 || type4 === TooltipHideEvent2) {
      this.handleTooltip(evt, a3, type4 !== TooltipHideEvent2);
    }
    if (h3) {
      for (let i = 0, len = h3.length; i < len; ++i) {
        h3[i].handler.call(this._obj, evt, a3);
      }
    }
  }
  // add an event handler
  on(type4, handler) {
    const name = this.eventName(type4), h3 = this._handlers, i = this._handlerIndex(h3[name], type4, handler);
    if (i < 0) {
      eventListenerCheck2(this, type4);
      (h3[name] || (h3[name] = [])).push({
        type: type4,
        handler
      });
    }
    return this;
  }
  // remove an event handler
  off(type4, handler) {
    const name = this.eventName(type4), h3 = this._handlers[name], i = this._handlerIndex(h3, type4, handler);
    if (i >= 0) {
      h3.splice(i, 1);
    }
    return this;
  }
  pickEvent(evt) {
    const p = point7(evt, this._canvas), o = this._origin;
    return this.pick(this._scene, p[0], p[1], p[0] - o[0], p[1] - o[1]);
  }
  // find the scenegraph item at the current pointer position
  // x, y -- the absolute x, y pointer coordinates on the canvas element
  // gx, gy -- the relative coordinates within the current group
  pick(scene, x6, y6, gx, gy) {
    const g = this.context(), mark = Marks2[scene.marktype];
    return mark.pick.call(this, g, scene, x6, y6, gx, gy);
  }
};
var eventBundle2 = (type4) => type4 === TouchStartEvent2 || type4 === TouchMoveEvent2 || type4 === TouchEndEvent2 ? [TouchStartEvent2, TouchMoveEvent2, TouchEndEvent2] : [type4];
function eventListenerCheck2(handler, type4) {
  eventBundle2(type4).forEach((_) => addEventListener2(handler, _));
}
function addEventListener2(handler, type4) {
  const canvas = handler.canvas();
  if (canvas && !handler._events[type4]) {
    handler._events[type4] = 1;
    canvas.addEventListener(type4, handler[type4] ? (evt) => handler[type4](evt) : (evt) => handler.fire(type4, evt));
  }
}
function fireAll2(handler, types, event2) {
  types.forEach((type4) => handler.fire(type4, event2));
}
function move2(moveEvents, overEvents, outEvents) {
  return function(evt) {
    const a3 = this._active, p = this.pickEvent(evt);
    if (p === a3) {
      fireAll2(this, moveEvents, evt);
    } else {
      if (!a3 || !a3.exit) {
        fireAll2(this, outEvents, evt);
      }
      this._active = p;
      fireAll2(this, overEvents, evt);
      fireAll2(this, moveEvents, evt);
    }
  };
}
function inactive2(types) {
  return function(evt) {
    fireAll2(this, types, evt);
    this._active = null;
  };
}
function devicePixelRatio2() {
  return typeof window !== "undefined" ? window.devicePixelRatio || 1 : 1;
}
function resize2(canvas, width2, height2, origin, scaleFactor, opt) {
  const inDOM = typeof HTMLElement !== "undefined" && canvas instanceof HTMLElement && canvas.parentNode != null, context4 = canvas.getContext("2d"), ratio = inDOM ? devicePixelRatio2() : scaleFactor;
  canvas.width = width2 * ratio;
  canvas.height = height2 * ratio;
  for (const key2 in opt) {
    context4[key2] = opt[key2];
  }
  if (inDOM && ratio !== 1) {
    canvas.style.width = width2 + "px";
    canvas.style.height = height2 + "px";
  }
  context4.pixelRatio = ratio;
  context4.setTransform(ratio, 0, 0, ratio, ratio * origin[0], ratio * origin[1]);
  return canvas;
}
var CanvasRenderer2 = class extends Renderer2 {
  constructor(loader2) {
    super(loader2);
    this._options = {};
    this._redraw = false;
    this._dirty = new Bounds3();
    this._tempb = new Bounds3();
  }
  initialize(el, width2, height2, origin, scaleFactor, options) {
    this._options = options || {};
    this._canvas = this._options.externalContext ? null : domCanvas(1, 1, this._options.type);
    if (el && this._canvas) {
      domClear2(el, 0).appendChild(this._canvas);
      this._canvas.setAttribute("class", "marks");
    }
    return super.initialize(el, width2, height2, origin, scaleFactor);
  }
  resize(width2, height2, origin, scaleFactor) {
    super.resize(width2, height2, origin, scaleFactor);
    if (this._canvas) {
      resize2(this._canvas, this._width, this._height, this._origin, this._scale, this._options.context);
    } else {
      const ctx = this._options.externalContext;
      if (!ctx) error("CanvasRenderer is missing a valid canvas or context");
      ctx.scale(this._scale, this._scale);
      ctx.translate(this._origin[0], this._origin[1]);
    }
    this._redraw = true;
    return this;
  }
  canvas() {
    return this._canvas;
  }
  context() {
    return this._options.externalContext || (this._canvas ? this._canvas.getContext("2d") : null);
  }
  dirty(item) {
    const b10 = this._tempb.clear().union(item.bounds);
    let g = item.mark.group;
    while (g) {
      b10.translate(g.x || 0, g.y || 0);
      g = g.mark.group;
    }
    this._dirty.union(b10);
  }
  _render(scene, markTypes) {
    const g = this.context(), o = this._origin, w3 = this._width, h3 = this._height, db = this._dirty, vb = viewBounds2(o, w3, h3);
    g.save();
    const b10 = this._redraw || db.empty() ? (this._redraw = false, vb.expand(1)) : clipToBounds2(g, vb.intersect(db), o);
    this.clear(-o[0], -o[1], w3, h3);
    this.draw(g, scene, b10, markTypes);
    g.restore();
    db.clear();
    return this;
  }
  draw(ctx, scene, bounds3, markTypes) {
    if (scene.marktype !== "group" && markTypes != null && !markTypes.includes(scene.marktype)) {
      return;
    }
    const mark = Marks2[scene.marktype];
    if (scene.clip) clip2(ctx, scene);
    mark.draw.call(this, ctx, scene, bounds3, markTypes);
    if (scene.clip) ctx.restore();
  }
  clear(x6, y6, w3, h3) {
    const opt = this._options, g = this.context();
    if (opt.type !== "pdf" && !opt.externalContext) {
      g.clearRect(x6, y6, w3, h3);
    }
    if (this._bgcolor != null) {
      g.fillStyle = this._bgcolor;
      g.fillRect(x6, y6, w3, h3);
    }
  }
};
var viewBounds2 = (origin, width2, height2) => new Bounds3().set(0, 0, width2, height2).translate(-origin[0], -origin[1]);
function clipToBounds2(g, b10, origin) {
  b10.expand(1).round();
  if (g.pixelRatio % 1) {
    b10.scale(g.pixelRatio).round().scale(1 / g.pixelRatio);
  }
  b10.translate(-(origin[0] % 1), -(origin[1] % 1));
  g.beginPath();
  g.rect(b10.x1, b10.y1, b10.width(), b10.height());
  g.clip();
  return b10;
}
var SVGHandler2 = class extends Handler2 {
  constructor(loader2, tooltip2) {
    super(loader2, tooltip2);
    const h3 = this;
    h3._hrefHandler = listener2(h3, (evt, item) => {
      if (item && item.href) h3.handleHref(evt, item, item.href);
    });
    h3._tooltipHandler = listener2(h3, (evt, item) => {
      h3.handleTooltip(evt, item, evt.type !== TooltipHideEvent2);
    });
  }
  initialize(el, origin, obj) {
    let svg = this._svg;
    if (svg) {
      svg.removeEventListener(HrefEvent2, this._hrefHandler);
      svg.removeEventListener(TooltipShowEvent2, this._tooltipHandler);
      svg.removeEventListener(TooltipHideEvent2, this._tooltipHandler);
    }
    this._svg = svg = el && domFind2(el, "svg");
    if (svg) {
      svg.addEventListener(HrefEvent2, this._hrefHandler);
      svg.addEventListener(TooltipShowEvent2, this._tooltipHandler);
      svg.addEventListener(TooltipHideEvent2, this._tooltipHandler);
    }
    return super.initialize(el, origin, obj);
  }
  canvas() {
    return this._svg;
  }
  // add an event handler
  on(type4, handler) {
    const name = this.eventName(type4), h3 = this._handlers, i = this._handlerIndex(h3[name], type4, handler);
    if (i < 0) {
      const x6 = {
        type: type4,
        handler,
        listener: listener2(this, handler)
      };
      (h3[name] || (h3[name] = [])).push(x6);
      if (this._svg) {
        this._svg.addEventListener(name, x6.listener);
      }
    }
    return this;
  }
  // remove an event handler
  off(type4, handler) {
    const name = this.eventName(type4), h3 = this._handlers[name], i = this._handlerIndex(h3, type4, handler);
    if (i >= 0) {
      if (this._svg) {
        this._svg.removeEventListener(name, h3[i].listener);
      }
      h3.splice(i, 1);
    }
    return this;
  }
};
var listener2 = (context4, handler) => (evt) => {
  let item = evt.target.__data__;
  item = Array.isArray(item) ? item[0] : item;
  evt.vegaType = evt.type;
  handler.call(context4._obj, evt, item);
};
var ARIA_HIDDEN2 = "aria-hidden";
var ARIA_LABEL2 = "aria-label";
var ARIA_ROLE2 = "role";
var ARIA_ROLEDESCRIPTION2 = "aria-roledescription";
var GRAPHICS_OBJECT2 = "graphics-object";
var GRAPHICS_SYMBOL2 = "graphics-symbol";
var bundle2 = (role, roledesc, label) => ({
  [ARIA_ROLE2]: role,
  [ARIA_ROLEDESCRIPTION2]: roledesc,
  [ARIA_LABEL2]: label || void 0
});
var AriaIgnore2 = toSet(["axis-domain", "axis-grid", "axis-label", "axis-tick", "axis-title", "legend-band", "legend-entry", "legend-gradient", "legend-label", "legend-title", "legend-symbol", "title"]);
var AriaGuides2 = {
  "axis": {
    desc: "axis",
    caption: axisCaption2
  },
  "legend": {
    desc: "legend",
    caption: legendCaption2
  },
  "title-text": {
    desc: "title",
    caption: (item) => `Title text '${titleCaption2(item)}'`
  },
  "title-subtitle": {
    desc: "subtitle",
    caption: (item) => `Subtitle text '${titleCaption2(item)}'`
  }
};
var AriaEncode2 = {
  ariaRole: ARIA_ROLE2,
  ariaRoleDescription: ARIA_ROLEDESCRIPTION2,
  description: ARIA_LABEL2
};
function ariaItemAttributes2(emit3, item) {
  const hide = item.aria === false;
  emit3(ARIA_HIDDEN2, hide || void 0);
  if (hide || item.description == null) {
    for (const prop in AriaEncode2) {
      emit3(AriaEncode2[prop], void 0);
    }
  } else {
    const type4 = item.mark.marktype;
    emit3(ARIA_LABEL2, item.description);
    emit3(ARIA_ROLE2, item.ariaRole || (type4 === "group" ? GRAPHICS_OBJECT2 : GRAPHICS_SYMBOL2));
    emit3(ARIA_ROLEDESCRIPTION2, item.ariaRoleDescription || `${type4} mark`);
  }
}
function ariaMarkAttributes2(mark) {
  return mark.aria === false ? {
    [ARIA_HIDDEN2]: true
  } : AriaIgnore2[mark.role] ? null : AriaGuides2[mark.role] ? ariaGuide2(mark, AriaGuides2[mark.role]) : ariaMark2(mark);
}
function ariaMark2(mark) {
  const type4 = mark.marktype;
  const recurse3 = type4 === "group" || type4 === "text" || mark.items.some((_) => _.description != null && _.aria !== false);
  return bundle2(recurse3 ? GRAPHICS_OBJECT2 : GRAPHICS_SYMBOL2, `${type4} mark container`, mark.description);
}
function ariaGuide2(mark, opt) {
  try {
    const item = mark.items[0], caption = opt.caption || (() => "");
    return bundle2(opt.role || GRAPHICS_SYMBOL2, opt.desc, mark.description || item.description || caption(item));
  } catch (err) {
    return null;
  }
}
function titleCaption2(item) {
  return array_default(item.text).join(" ");
}
function axisCaption2(item) {
  const datum3 = item.datum, orient2 = item.orient, title2 = datum3.title ? extractTitle2(item) : null, ctx = item.context, scale9 = ctx.scales[datum3.scale].value, locale4 = ctx.dataflow.locale(), type4 = scale9.type, xy = orient2 === "left" || orient2 === "right" ? "Y" : "X";
  return `${xy}-axis` + (title2 ? ` titled '${title2}'` : "") + ` for a ${isDiscrete(type4) ? "discrete" : type4} scale with ${domainCaption(locale4, scale9, item)}`;
}
function legendCaption2(item) {
  const datum3 = item.datum, title2 = datum3.title ? extractTitle2(item) : null, type4 = `${datum3.type || ""} legend`.trim(), scales2 = datum3.scales, props = Object.keys(scales2), ctx = item.context, scale9 = ctx.scales[scales2[props[0]]].value, locale4 = ctx.dataflow.locale();
  return capitalize2(type4) + (title2 ? ` titled '${title2}'` : "") + ` for ${channelCaption2(props)} with ${domainCaption(locale4, scale9, item)}`;
}
function extractTitle2(item) {
  try {
    return array_default(peek(item.items).items[0].text).join(" ");
  } catch (err) {
    return null;
  }
}
function channelCaption2(props) {
  props = props.map((p) => p + (p === "fill" || p === "stroke" ? " color" : ""));
  return props.length < 2 ? props[0] : props.slice(0, -1).join(", ") + " and " + peek(props);
}
function capitalize2(s) {
  return s.length ? s[0].toUpperCase() + s.slice(1) : s;
}
var innerText2 = (val) => (val + "").replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
var attrText2 = (val) => innerText2(val).replace(/"/g, "&quot;").replace(/\t/g, "&#x9;").replace(/\n/g, "&#xA;").replace(/\r/g, "&#xD;");
function markup2() {
  let buf = "", outer = "", inner = "";
  const stack2 = [], clear2 = () => outer = inner = "", push = (tag) => {
    if (outer) {
      buf += `${outer}>${inner}`;
      clear2();
    }
    stack2.push(tag);
  }, attr3 = (name, value4) => {
    if (value4 != null) outer += ` ${name}="${attrText2(value4)}"`;
    return m3;
  }, m3 = {
    open(tag, ...attrs) {
      push(tag);
      outer = "<" + tag;
      for (const set7 of attrs) {
        for (const key2 in set7) attr3(key2, set7[key2]);
      }
      return m3;
    },
    close() {
      const tag = stack2.pop();
      if (outer) {
        buf += outer + (inner ? `>${inner}</${tag}>` : "/>");
      } else {
        buf += `</${tag}>`;
      }
      clear2();
      return m3;
    },
    attr: attr3,
    text: (t) => (inner += innerText2(t), m3),
    toString: () => buf
  };
  return m3;
}
var serializeXML2 = (node) => _serialize2(markup2(), node) + "";
function _serialize2(m3, node) {
  m3.open(node.tagName);
  if (node.hasAttributes()) {
    const attrs = node.attributes, n = attrs.length;
    for (let i = 0; i < n; ++i) {
      m3.attr(attrs[i].name, attrs[i].value);
    }
  }
  if (node.hasChildNodes()) {
    const children4 = node.childNodes;
    for (const child of children4) {
      child.nodeType === 3 ? m3.text(child.nodeValue) : _serialize2(m3, child);
    }
  }
  return m3.close();
}
var stylesAttr2 = {
  fill: "fill",
  fillOpacity: "fill-opacity",
  stroke: "stroke",
  strokeOpacity: "stroke-opacity",
  strokeWidth: "stroke-width",
  strokeCap: "stroke-linecap",
  strokeJoin: "stroke-linejoin",
  strokeDash: "stroke-dasharray",
  strokeDashOffset: "stroke-dashoffset",
  strokeMiterLimit: "stroke-miterlimit",
  opacity: "opacity"
};
var stylesCss2 = {
  blend: "mix-blend-mode"
};
var rootAttributes2 = {
  "fill": "none",
  "stroke-miterlimit": 4
};
var RootIndex2 = 0;
var xmlns2 = "http://www.w3.org/2000/xmlns/";
var svgns2 = metadata2.xmlns;
var SVGRenderer2 = class extends Renderer2 {
  constructor(loader2) {
    super(loader2);
    this._dirtyID = 0;
    this._dirty = [];
    this._svg = null;
    this._root = null;
    this._defs = null;
  }
  /**
   * Initialize a new SVGRenderer instance.
   * @param {DOMElement} el - The containing DOM element for the display.
   * @param {number} width - The coordinate width of the display, in pixels.
   * @param {number} height - The coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {SVGRenderer} - This renderer instance.
   */
  initialize(el, width2, height2, origin, scaleFactor) {
    this._defs = {};
    this._clearDefs();
    if (el) {
      this._svg = domChild2(el, 0, "svg", svgns2);
      this._svg.setAttributeNS(xmlns2, "xmlns", svgns2);
      this._svg.setAttributeNS(xmlns2, "xmlns:xlink", metadata2["xmlns:xlink"]);
      this._svg.setAttribute("version", metadata2["version"]);
      this._svg.setAttribute("class", "marks");
      domClear2(el, 1);
      this._root = domChild2(this._svg, RootIndex2, "g", svgns2);
      setAttributes2(this._root, rootAttributes2);
      domClear2(this._svg, RootIndex2 + 1);
    }
    this.background(this._bgcolor);
    return super.initialize(el, width2, height2, origin, scaleFactor);
  }
  /**
   * Get / set the background color.
   */
  background(bgcolor) {
    if (arguments.length && this._svg) {
      this._svg.style.setProperty("background-color", bgcolor);
    }
    return super.background(...arguments);
  }
  /**
   * Resize the display.
   * @param {number} width - The new coordinate width of the display, in pixels.
   * @param {number} height - The new coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The new origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {SVGRenderer} - This renderer instance;
   */
  resize(width2, height2, origin, scaleFactor) {
    super.resize(width2, height2, origin, scaleFactor);
    if (this._svg) {
      setAttributes2(this._svg, {
        width: this._width * this._scale,
        height: this._height * this._scale,
        viewBox: `0 0 ${this._width} ${this._height}`
      });
      this._root.setAttribute("transform", `translate(${this._origin})`);
    }
    this._dirty = [];
    return this;
  }
  /**
   * Returns the SVG element of the visualization.
   * @return {DOMElement} - The SVG element.
   */
  canvas() {
    return this._svg;
  }
  /**
   * Returns an SVG text string for the rendered content,
   * or null if this renderer is currently headless.
   */
  svg() {
    const svg = this._svg, bg = this._bgcolor;
    if (!svg) return null;
    let node;
    if (bg) {
      svg.removeAttribute("style");
      node = domChild2(svg, RootIndex2, "rect", svgns2);
      setAttributes2(node, {
        width: this._width,
        height: this._height,
        fill: bg
      });
    }
    const text4 = serializeXML2(svg);
    if (bg) {
      svg.removeChild(node);
      this._svg.style.setProperty("background-color", bg);
    }
    return text4;
  }
  /**
   * Internal rendering method.
   * @param {object} scene - The root mark of a scenegraph to render.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   */
  _render(scene, markTypes) {
    if (this._dirtyCheck()) {
      if (this._dirtyAll) this._clearDefs();
      this.mark(this._root, scene, void 0, markTypes);
      domClear2(this._root, 1);
    }
    this.defs();
    this._dirty = [];
    ++this._dirtyID;
    return this;
  }
  // -- Manage rendering of items marked as dirty --
  /**
   * Flag a mark item as dirty.
   * @param {Item} item - The mark item.
   */
  dirty(item) {
    if (item.dirty !== this._dirtyID) {
      item.dirty = this._dirtyID;
      this._dirty.push(item);
    }
  }
  /**
   * Check if a mark item is considered dirty.
   * @param {Item} item - The mark item.
   */
  isDirty(item) {
    return this._dirtyAll || !item._svg || !item._svg.ownerSVGElement || item.dirty === this._dirtyID;
  }
  /**
   * Internal method to check dirty status and, if possible,
   * make targetted updates without a full rendering pass.
   */
  _dirtyCheck() {
    this._dirtyAll = true;
    const items = this._dirty;
    if (!items.length || !this._dirtyID) return true;
    const id2 = ++this._dirtyID;
    let item, mark, type4, mdef, i, n, o;
    for (i = 0, n = items.length; i < n; ++i) {
      item = items[i];
      mark = item.mark;
      if (mark.marktype !== type4) {
        type4 = mark.marktype;
        mdef = Marks2[type4];
      }
      if (mark.zdirty && mark.dirty !== id2) {
        this._dirtyAll = false;
        dirtyParents2(item, id2);
        mark.items.forEach((i2) => {
          i2.dirty = id2;
        });
      }
      if (mark.zdirty) continue;
      if (item.exit) {
        if (mdef.nested && mark.items.length) {
          o = mark.items[0];
          if (o._svg) this._update(mdef, o._svg, o);
        } else if (item._svg) {
          o = item._svg.parentNode;
          if (o) o.removeChild(item._svg);
        }
        item._svg = null;
        continue;
      }
      item = mdef.nested ? mark.items[0] : item;
      if (item._update === id2) continue;
      if (!item._svg || !item._svg.ownerSVGElement) {
        this._dirtyAll = false;
        dirtyParents2(item, id2);
      } else {
        this._update(mdef, item._svg, item);
      }
      item._update = id2;
    }
    return !this._dirtyAll;
  }
  // -- Construct & maintain scenegraph to SVG mapping ---
  /**
   * Render a set of mark items.
   * @param {SVGElement} el - The parent element in the SVG tree.
   * @param {object} scene - The mark parent to render.
   * @param {SVGElement} prev - The previous sibling in the SVG tree.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   */
  mark(el, scene, prev, markTypes) {
    if (!this.isDirty(scene)) {
      return scene._svg;
    }
    const svg = this._svg, markType2 = scene.marktype, mdef = Marks2[markType2], events3 = scene.interactive === false ? "none" : null, isGroup = mdef.tag === "g";
    const parent = bind2(scene, el, prev, "g", svg);
    if (markType2 !== "group" && markTypes != null && !markTypes.includes(markType2)) {
      domClear2(parent, 0);
      return scene._svg;
    }
    parent.setAttribute("class", cssClass2(scene));
    const aria2 = ariaMarkAttributes2(scene);
    for (const key2 in aria2) setAttribute2(parent, key2, aria2[key2]);
    if (!isGroup) {
      setAttribute2(parent, "pointer-events", events3);
    }
    setAttribute2(parent, "clip-path", scene.clip ? clip$12(this, scene, scene.group) : null);
    let sibling = null, i = 0;
    const process = (item) => {
      const dirty = this.isDirty(item), node = bind2(item, parent, sibling, mdef.tag, svg);
      if (dirty) {
        this._update(mdef, node, item);
        if (isGroup) recurse2(this, node, item, markTypes);
      }
      sibling = node;
      ++i;
    };
    if (mdef.nested) {
      if (scene.items.length) process(scene.items[0]);
    } else {
      visit2(scene, process);
    }
    domClear2(parent, i);
    return parent;
  }
  /**
   * Update the attributes of an SVG element for a mark item.
   * @param {object} mdef - The mark definition object
   * @param {SVGElement} el - The SVG element.
   * @param {Item} item - The mark item.
   */
  _update(mdef, el, item) {
    element2 = el;
    values2 = el.__values__;
    ariaItemAttributes2(emit2, item);
    mdef.attr(emit2, item, this);
    const extra = mark_extras2[mdef.type];
    if (extra) extra.call(this, mdef, el, item);
    if (element2) this.style(element2, item);
  }
  /**
   * Update the presentation attributes of an SVG element for a mark item.
   * @param {SVGElement} el - The SVG element.
   * @param {Item} item - The mark item.
   */
  style(el, item) {
    if (item == null) return;
    for (const prop in stylesAttr2) {
      let value4 = prop === "font" ? fontFamily2(item) : item[prop];
      if (value4 === values2[prop]) continue;
      const name = stylesAttr2[prop];
      if (value4 == null) {
        el.removeAttribute(name);
      } else {
        if (isGradient2(value4)) {
          value4 = gradientRef2(value4, this._defs.gradient, href2());
        }
        el.setAttribute(name, value4 + "");
      }
      values2[prop] = value4;
    }
    for (const prop in stylesCss2) {
      setStyle2(el, stylesCss2[prop], item[prop]);
    }
  }
  /**
   * Render SVG defs, as needed.
   * Must be called *after* marks have been processed to ensure the
   * collected state is current and accurate.
   */
  defs() {
    const svg = this._svg, defs = this._defs;
    let el = defs.el, index4 = 0;
    for (const id2 in defs.gradient) {
      if (!el) defs.el = el = domChild2(svg, RootIndex2 + 1, "defs", svgns2);
      index4 = updateGradient2(el, defs.gradient[id2], index4);
    }
    for (const id2 in defs.clipping) {
      if (!el) defs.el = el = domChild2(svg, RootIndex2 + 1, "defs", svgns2);
      index4 = updateClipping2(el, defs.clipping[id2], index4);
    }
    if (el) {
      index4 === 0 ? (svg.removeChild(el), defs.el = null) : domClear2(el, index4);
    }
  }
  /**
   * Clear defs caches.
   */
  _clearDefs() {
    const def3 = this._defs;
    def3.gradient = {};
    def3.clipping = {};
  }
};
function dirtyParents2(item, id2) {
  for (; item && item.dirty !== id2; item = item.mark.group) {
    item.dirty = id2;
    if (item.mark && item.mark.dirty !== id2) {
      item.mark.dirty = id2;
    } else return;
  }
}
function updateGradient2(el, grad, index4) {
  let i, n, stop2;
  if (grad.gradient === "radial") {
    let pt = domChild2(el, index4++, "pattern", svgns2);
    setAttributes2(pt, {
      id: patternPrefix2 + grad.id,
      viewBox: "0,0,1,1",
      width: "100%",
      height: "100%",
      preserveAspectRatio: "xMidYMid slice"
    });
    pt = domChild2(pt, 0, "rect", svgns2);
    setAttributes2(pt, {
      width: 1,
      height: 1,
      fill: `url(${href2()}#${grad.id})`
    });
    el = domChild2(el, index4++, "radialGradient", svgns2);
    setAttributes2(el, {
      id: grad.id,
      fx: grad.x1,
      fy: grad.y1,
      fr: grad.r1,
      cx: grad.x2,
      cy: grad.y2,
      r: grad.r2
    });
  } else {
    el = domChild2(el, index4++, "linearGradient", svgns2);
    setAttributes2(el, {
      id: grad.id,
      x1: grad.x1,
      x2: grad.x2,
      y1: grad.y1,
      y2: grad.y2
    });
  }
  for (i = 0, n = grad.stops.length; i < n; ++i) {
    stop2 = domChild2(el, i, "stop", svgns2);
    stop2.setAttribute("offset", grad.stops[i].offset);
    stop2.setAttribute("stop-color", grad.stops[i].color);
  }
  domClear2(el, i);
  return index4;
}
function updateClipping2(el, clip4, index4) {
  let mask;
  el = domChild2(el, index4, "clipPath", svgns2);
  el.setAttribute("id", clip4.id);
  if (clip4.path) {
    mask = domChild2(el, 0, "path", svgns2);
    mask.setAttribute("d", clip4.path);
  } else {
    mask = domChild2(el, 0, "rect", svgns2);
    setAttributes2(mask, {
      x: 0,
      y: 0,
      width: clip4.width,
      height: clip4.height
    });
  }
  domClear2(el, 1);
  return index4 + 1;
}
function recurse2(renderer, el, group3, markTypes) {
  el = el.lastChild.previousSibling;
  let prev, idx = 0;
  visit2(group3, (item) => {
    prev = renderer.mark(el, item, prev, markTypes);
    ++idx;
  });
  domClear2(el, 1 + idx);
}
function bind2(item, el, sibling, tag, svg) {
  let node = item._svg, doc;
  if (!node) {
    doc = el.ownerDocument;
    node = domCreate2(doc, tag, svgns2);
    item._svg = node;
    if (item.mark) {
      node.__data__ = item;
      node.__values__ = {
        fill: "default"
      };
      if (tag === "g") {
        const bg = domCreate2(doc, "path", svgns2);
        node.appendChild(bg);
        bg.__data__ = item;
        const cg = domCreate2(doc, "g", svgns2);
        node.appendChild(cg);
        cg.__data__ = item;
        const fg = domCreate2(doc, "path", svgns2);
        node.appendChild(fg);
        fg.__data__ = item;
        fg.__values__ = {
          fill: "default"
        };
      }
    }
  }
  if (node.ownerSVGElement !== svg || siblingCheck2(node, sibling)) {
    el.insertBefore(node, sibling ? sibling.nextSibling : el.firstChild);
  }
  return node;
}
function siblingCheck2(node, sibling) {
  return node.parentNode && node.parentNode.childNodes.length > 1 && node.previousSibling != sibling;
}
var element2 = null;
var values2 = null;
var mark_extras2 = {
  group(mdef, el, item) {
    const fg = element2 = el.childNodes[2];
    values2 = fg.__values__;
    mdef.foreground(emit2, item, this);
    values2 = el.__values__;
    element2 = el.childNodes[1];
    mdef.content(emit2, item, this);
    const bg = element2 = el.childNodes[0];
    mdef.background(emit2, item, this);
    const value4 = item.mark.interactive === false ? "none" : null;
    if (value4 !== values2.events) {
      setAttribute2(fg, "pointer-events", value4);
      setAttribute2(bg, "pointer-events", value4);
      values2.events = value4;
    }
    if (item.strokeForeground && item.stroke) {
      const fill3 = item.fill;
      setAttribute2(fg, "display", null);
      this.style(bg, item);
      setAttribute2(bg, "stroke", null);
      if (fill3) item.fill = null;
      values2 = fg.__values__;
      this.style(fg, item);
      if (fill3) item.fill = fill3;
      element2 = null;
    } else {
      setAttribute2(fg, "display", "none");
    }
  },
  image(mdef, el, item) {
    if (item.smooth === false) {
      setStyle2(el, "image-rendering", "optimizeSpeed");
      setStyle2(el, "image-rendering", "pixelated");
    } else {
      setStyle2(el, "image-rendering", null);
    }
  },
  text(mdef, el, item) {
    const tl3 = textLines2(item);
    let key2, value4, doc, lh;
    if (isArray(tl3)) {
      value4 = tl3.map((_) => textValue2(item, _));
      key2 = value4.join("\n");
      if (key2 !== values2.text) {
        domClear2(el, 0);
        doc = el.ownerDocument;
        lh = lineHeight2(item);
        value4.forEach((t, i) => {
          const ts3 = domCreate2(doc, "tspan", svgns2);
          ts3.__data__ = item;
          ts3.textContent = t;
          if (i) {
            ts3.setAttribute("x", 0);
            ts3.setAttribute("dy", lh);
          }
          el.appendChild(ts3);
        });
        values2.text = key2;
      }
    } else {
      value4 = textValue2(item, tl3);
      if (value4 !== values2.text) {
        el.textContent = value4;
        values2.text = value4;
      }
    }
    setAttribute2(el, "font-family", fontFamily2(item));
    setAttribute2(el, "font-size", fontSize2(item) + "px");
    setAttribute2(el, "font-style", item.fontStyle);
    setAttribute2(el, "font-variant", item.fontVariant);
    setAttribute2(el, "font-weight", item.fontWeight);
  }
};
function emit2(name, value4, ns) {
  if (value4 === values2[name]) return;
  if (ns) {
    setAttributeNS2(element2, name, value4, ns);
  } else {
    setAttribute2(element2, name, value4);
  }
  values2[name] = value4;
}
function setStyle2(el, name, value4) {
  if (value4 !== values2[name]) {
    if (value4 == null) {
      el.style.removeProperty(name);
    } else {
      el.style.setProperty(name, value4 + "");
    }
    values2[name] = value4;
  }
}
function setAttributes2(el, attrs) {
  for (const key2 in attrs) {
    setAttribute2(el, key2, attrs[key2]);
  }
}
function setAttribute2(el, name, value4) {
  if (value4 != null) {
    el.setAttribute(name, value4);
  } else {
    el.removeAttribute(name);
  }
}
function setAttributeNS2(el, name, value4, ns) {
  if (value4 != null) {
    el.setAttributeNS(ns, name, value4);
  } else {
    el.removeAttributeNS(ns, name);
  }
}
function href2() {
  let loc;
  return typeof window === "undefined" ? "" : (loc = window.location).hash ? loc.href.slice(0, -loc.hash.length) : loc.href;
}
var SVGStringRenderer2 = class extends Renderer2 {
  constructor(loader2) {
    super(loader2);
    this._text = null;
    this._defs = {
      gradient: {},
      clipping: {}
    };
  }
  /**
   * Returns the rendered SVG text string,
   * or null if rendering has not yet occurred.
   */
  svg() {
    return this._text;
  }
  /**
   * Internal rendering method.
   * @param {object} scene - The root mark of a scenegraph to render.
   */
  _render(scene) {
    const m3 = markup2();
    m3.open("svg", extend({}, metadata2, {
      class: "marks",
      width: this._width * this._scale,
      height: this._height * this._scale,
      viewBox: `0 0 ${this._width} ${this._height}`
    }));
    const bg = this._bgcolor;
    if (bg && bg !== "transparent" && bg !== "none") {
      m3.open("rect", {
        width: this._width,
        height: this._height,
        fill: bg
      }).close();
    }
    m3.open("g", rootAttributes2, {
      transform: "translate(" + this._origin + ")"
    });
    this.mark(m3, scene);
    m3.close();
    this.defs(m3);
    this._text = m3.close() + "";
    return this;
  }
  /**
   * Render a set of mark items.
   * @param {object} m - The markup context.
   * @param {object} scene - The mark parent to render.
   */
  mark(m3, scene) {
    const mdef = Marks2[scene.marktype], tag = mdef.tag, attrList = [ariaItemAttributes2, mdef.attr];
    m3.open("g", {
      "class": cssClass2(scene),
      "clip-path": scene.clip ? clip$12(this, scene, scene.group) : null
    }, ariaMarkAttributes2(scene), {
      "pointer-events": tag !== "g" && scene.interactive === false ? "none" : null
    });
    const process = (item) => {
      const href3 = this.href(item);
      if (href3) m3.open("a", href3);
      m3.open(tag, this.attr(scene, item, attrList, tag !== "g" ? tag : null));
      if (tag === "text") {
        const tl3 = textLines2(item);
        if (isArray(tl3)) {
          const attrs = {
            x: 0,
            dy: lineHeight2(item)
          };
          for (let i = 0; i < tl3.length; ++i) {
            m3.open("tspan", i ? attrs : null).text(textValue2(item, tl3[i])).close();
          }
        } else {
          m3.text(textValue2(item, tl3));
        }
      } else if (tag === "g") {
        const fore = item.strokeForeground, fill3 = item.fill, stroke3 = item.stroke;
        if (fore && stroke3) {
          item.stroke = null;
        }
        m3.open("path", this.attr(scene, item, mdef.background, "bgrect")).close();
        m3.open("g", this.attr(scene, item, mdef.content));
        visit2(item, (scene2) => this.mark(m3, scene2));
        m3.close();
        if (fore && stroke3) {
          if (fill3) item.fill = null;
          item.stroke = stroke3;
          m3.open("path", this.attr(scene, item, mdef.foreground, "bgrect")).close();
          if (fill3) item.fill = fill3;
        } else {
          m3.open("path", this.attr(scene, item, mdef.foreground, "bgfore")).close();
        }
      }
      m3.close();
      if (href3) m3.close();
    };
    if (mdef.nested) {
      if (scene.items && scene.items.length) process(scene.items[0]);
    } else {
      visit2(scene, process);
    }
    return m3.close();
  }
  /**
   * Get href attributes for a hyperlinked mark item.
   * @param {Item} item - The mark item.
   */
  href(item) {
    const href3 = item.href;
    let attr3;
    if (href3) {
      if (attr3 = this._hrefs && this._hrefs[href3]) {
        return attr3;
      } else {
        this.sanitizeURL(href3).then((attr4) => {
          attr4["xlink:href"] = attr4.href;
          attr4.href = null;
          (this._hrefs || (this._hrefs = {}))[href3] = attr4;
        });
      }
    }
    return null;
  }
  /**
   * Get an object of SVG attributes for a mark item.
   * @param {object} scene - The mark parent.
   * @param {Item} item - The mark item.
   * @param {array|function} attrs - One or more attribute emitters.
   * @param {string} tag - The tag being rendered.
   */
  attr(scene, item, attrs, tag) {
    const object2 = {}, emit3 = (name, value4, ns, prefixed) => {
      object2[prefixed || name] = value4;
    };
    if (Array.isArray(attrs)) {
      attrs.forEach((fn) => fn(emit3, item, this));
    } else {
      attrs(emit3, item, this);
    }
    if (tag) {
      style2(object2, item, scene, tag, this._defs);
    }
    return object2;
  }
  /**
   * Render SVG defs, as needed.
   * Must be called *after* marks have been processed to ensure the
   * collected state is current and accurate.
   * @param {object} m - The markup context.
   */
  defs(m3) {
    const gradient5 = this._defs.gradient, clipping = this._defs.clipping, count2 = Object.keys(gradient5).length + Object.keys(clipping).length;
    if (count2 === 0) return;
    m3.open("defs");
    for (const id2 in gradient5) {
      const def3 = gradient5[id2], stops = def3.stops;
      if (def3.gradient === "radial") {
        m3.open("pattern", {
          id: patternPrefix2 + id2,
          viewBox: "0,0,1,1",
          width: "100%",
          height: "100%",
          preserveAspectRatio: "xMidYMid slice"
        });
        m3.open("rect", {
          width: "1",
          height: "1",
          fill: "url(#" + id2 + ")"
        }).close();
        m3.close();
        m3.open("radialGradient", {
          id: id2,
          fx: def3.x1,
          fy: def3.y1,
          fr: def3.r1,
          cx: def3.x2,
          cy: def3.y2,
          r: def3.r2
        });
      } else {
        m3.open("linearGradient", {
          id: id2,
          x1: def3.x1,
          x2: def3.x2,
          y1: def3.y1,
          y2: def3.y2
        });
      }
      for (let i = 0; i < stops.length; ++i) {
        m3.open("stop", {
          offset: stops[i].offset,
          "stop-color": stops[i].color
        }).close();
      }
      m3.close();
    }
    for (const id2 in clipping) {
      const def3 = clipping[id2];
      m3.open("clipPath", {
        id: id2
      });
      if (def3.path) {
        m3.open("path", {
          d: def3.path
        }).close();
      } else {
        m3.open("rect", {
          x: 0,
          y: 0,
          width: def3.width,
          height: def3.height
        }).close();
      }
      m3.close();
    }
    m3.close();
  }
};
function style2(s, item, scene, tag, defs) {
  let styleList;
  if (item == null) return s;
  if (tag === "bgrect" && scene.interactive === false) {
    s["pointer-events"] = "none";
  }
  if (tag === "bgfore") {
    if (scene.interactive === false) {
      s["pointer-events"] = "none";
    }
    s.display = "none";
    if (item.fill !== null) return s;
  }
  if (tag === "image" && item.smooth === false) {
    styleList = ["image-rendering: optimizeSpeed;", "image-rendering: pixelated;"];
  }
  if (tag === "text") {
    s["font-family"] = fontFamily2(item);
    s["font-size"] = fontSize2(item) + "px";
    s["font-style"] = item.fontStyle;
    s["font-variant"] = item.fontVariant;
    s["font-weight"] = item.fontWeight;
  }
  for (const prop in stylesAttr2) {
    let value4 = item[prop];
    const name = stylesAttr2[prop];
    if (value4 === "transparent" && (name === "fill" || name === "stroke")) ;
    else if (value4 != null) {
      if (isGradient2(value4)) {
        value4 = gradientRef2(value4, defs.gradient, "");
      }
      s[name] = value4;
    }
  }
  for (const prop in stylesCss2) {
    const value4 = item[prop];
    if (value4 != null) {
      styleList = styleList || [];
      styleList.push(`${stylesCss2[prop]}: ${value4};`);
    }
  }
  if (styleList) {
    s.style = styleList.join(" ");
  }
  return s;
}
var OPTS2 = {
  svgMarkTypes: ["text"],
  svgOnTop: true,
  debug: false
};
var HybridRenderer2 = class extends Renderer2 {
  constructor(loader2) {
    super(loader2);
    this._svgRenderer = new SVGRenderer2(loader2);
    this._canvasRenderer = new CanvasRenderer2(loader2);
  }
  /**
   * Initialize a new HybridRenderer instance.
   * @param {DOMElement} el - The containing DOM element for the display.
   * @param {number} width - The coordinate width of the display, in pixels.
   * @param {number} height - The coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {HybridRenderer} - This renderer instance.
   */
  initialize(el, width2, height2, origin, scaleFactor) {
    this._root_el = domChild2(el, 0, "div");
    const bottomEl = domChild2(this._root_el, 0, "div");
    const topEl = domChild2(this._root_el, 1, "div");
    this._root_el.style.position = "relative";
    if (!OPTS2.debug) {
      bottomEl.style.height = "100%";
      topEl.style.position = "absolute";
      topEl.style.top = "0";
      topEl.style.left = "0";
      topEl.style.height = "100%";
      topEl.style.width = "100%";
    }
    this._svgEl = OPTS2.svgOnTop ? topEl : bottomEl;
    this._canvasEl = OPTS2.svgOnTop ? bottomEl : topEl;
    this._svgEl.style.pointerEvents = "none";
    this._canvasRenderer.initialize(this._canvasEl, width2, height2, origin, scaleFactor);
    this._svgRenderer.initialize(this._svgEl, width2, height2, origin, scaleFactor);
    return super.initialize(el, width2, height2, origin, scaleFactor);
  }
  /**
   * Flag a mark item as dirty.
   * @param {Item} item - The mark item.
   */
  dirty(item) {
    if (OPTS2.svgMarkTypes.includes(item.mark.marktype)) {
      this._svgRenderer.dirty(item);
    } else {
      this._canvasRenderer.dirty(item);
    }
    return this;
  }
  /**
   * Internal rendering method.
   * @param {object} scene - The root mark of a scenegraph to render.
   * @param {Array} markTypes - Array of the mark types to render.
   *                            If undefined, render all mark types
   */
  _render(scene, markTypes) {
    const allMarkTypes = markTypes ?? ["arc", "area", "image", "line", "path", "rect", "rule", "shape", "symbol", "text", "trail"];
    const canvasMarkTypes = allMarkTypes.filter((m3) => !OPTS2.svgMarkTypes.includes(m3));
    this._svgRenderer.render(scene, OPTS2.svgMarkTypes);
    this._canvasRenderer.render(scene, canvasMarkTypes);
  }
  /**
   * Resize the display.
   * @param {number} width - The new coordinate width of the display, in pixels.
   * @param {number} height - The new coordinate height of the display, in pixels.
   * @param {Array<number>} origin - The new origin of the display, in pixels.
   *   The coordinate system will be translated to this point.
   * @param {number} [scaleFactor=1] - Optional scaleFactor by which to multiply
   *   the width and height to determine the final pixel size.
   * @return {SVGRenderer} - This renderer instance;
   */
  resize(width2, height2, origin, scaleFactor) {
    super.resize(width2, height2, origin, scaleFactor);
    this._svgRenderer.resize(width2, height2, origin, scaleFactor);
    this._canvasRenderer.resize(width2, height2, origin, scaleFactor);
    return this;
  }
  background(bgcolor) {
    if (OPTS2.svgOnTop) {
      this._canvasRenderer.background(bgcolor);
    } else {
      this._svgRenderer.background(bgcolor);
    }
    return this;
  }
};
var HybridHandler2 = class extends CanvasHandler2 {
  constructor(loader2, tooltip2) {
    super(loader2, tooltip2);
  }
  initialize(el, origin, obj) {
    const canvas = domChild2(domChild2(el, 0, "div"), OPTS2.svgOnTop ? 0 : 1, "div");
    return super.initialize(canvas, origin, obj);
  }
};
var Canvas2 = "canvas";
var Hybrid2 = "hybrid";
var PNG2 = "png";
var SVG2 = "svg";
var None4 = "none";
var RenderType = {
  Canvas: Canvas2,
  PNG: PNG2,
  SVG: SVG2,
  Hybrid: Hybrid2,
  None: None4
};
var modules2 = {};
modules2[Canvas2] = modules2[PNG2] = {
  renderer: CanvasRenderer2,
  headless: CanvasRenderer2,
  handler: CanvasHandler2
};
modules2[SVG2] = {
  renderer: SVGRenderer2,
  headless: SVGStringRenderer2,
  handler: SVGHandler2
};
modules2[Hybrid2] = {
  renderer: HybridRenderer2,
  headless: HybridRenderer2,
  handler: HybridHandler2
};
modules2[None4] = {};
function renderModule(name, _) {
  name = String(name || "").toLowerCase();
  if (arguments.length > 1) {
    modules2[name] = _;
    return this;
  } else {
    return modules2[name];
  }
}
function intersect4(scene, bounds3, filter4) {
  const hits = [], box = new Bounds3().union(bounds3), type4 = scene.marktype;
  return type4 ? intersectMark2(scene, box, filter4, hits) : type4 === "group" ? intersectGroup2(scene, box, filter4, hits) : error("Intersect scene must be mark node or group item.");
}
function intersectMark2(mark, box, filter4, hits) {
  if (visitMark2(mark, box, filter4)) {
    const items = mark.items, type4 = mark.marktype, n = items.length;
    let i = 0;
    if (type4 === "group") {
      for (; i < n; ++i) {
        intersectGroup2(items[i], box, filter4, hits);
      }
    } else {
      for (const test2 = Marks2[type4].isect; i < n; ++i) {
        const item = items[i];
        if (intersectItem2(item, box, test2)) hits.push(item);
      }
    }
  }
  return hits;
}
function visitMark2(mark, box, filter4) {
  return mark.bounds && box.intersects(mark.bounds) && (mark.marktype === "group" || mark.interactive !== false && (!filter4 || filter4(mark)));
}
function intersectGroup2(group3, box, filter4, hits) {
  if (filter4 && filter4(group3.mark) && intersectItem2(group3, box, Marks2.group.isect)) {
    hits.push(group3);
  }
  const marks = group3.items, n = marks && marks.length;
  if (n) {
    const x6 = group3.x || 0, y6 = group3.y || 0;
    box.translate(-x6, -y6);
    for (let i = 0; i < n; ++i) {
      intersectMark2(marks[i], box, filter4, hits);
    }
    box.translate(x6, y6);
  }
  return hits;
}
function intersectItem2(item, box, test2) {
  const bounds3 = item.bounds;
  return box.encloses(bounds3) || box.intersects(bounds3) && test2(item, box);
}
var clipBounds2 = new Bounds3();

// ../deepseek-harness/node_modules/.pnpm/vega-expression@6.2.2/node_modules/vega-expression/build/vega-expression.js
var RawCode = "RawCode";
var Literal = "Literal";
var Property = "Property";
var Identifier2 = "Identifier";
var ArrayExpression = "ArrayExpression";
var BinaryExpression = "BinaryExpression";
var CallExpression = "CallExpression";
var ConditionalExpression = "ConditionalExpression";
var LogicalExpression = "LogicalExpression";
var MemberExpression = "MemberExpression";
var ObjectExpression = "ObjectExpression";
var UnaryExpression = "UnaryExpression";
function ASTNode(type4) {
  this.type = type4;
}
ASTNode.prototype.visit = function(visitor) {
  let c3, i, n;
  if (visitor(this)) return 1;
  for (c3 = children2(this), i = 0, n = c3.length; i < n; ++i) {
    if (c3[i].visit(visitor)) return 1;
  }
};
function children2(node) {
  switch (node.type) {
    case ArrayExpression:
      return node.elements;
    case BinaryExpression:
    case LogicalExpression:
      return [node.left, node.right];
    case CallExpression:
      return [node.callee].concat(node.arguments);
    case ConditionalExpression:
      return [node.test, node.consequent, node.alternate];
    case MemberExpression:
      return [node.object, node.property];
    case ObjectExpression:
      return node.properties;
    case Property:
      return [node.key, node.value];
    case UnaryExpression:
      return [node.argument];
    case Identifier2:
    case Literal:
    case RawCode:
    default:
      return [];
  }
}
var TokenName;
var source2;
var index2;
var length;
var lookahead;
var TokenBooleanLiteral = 1;
var TokenEOF = 2;
var TokenIdentifier = 3;
var TokenKeyword = 4;
var TokenNullLiteral = 5;
var TokenNumericLiteral = 6;
var TokenPunctuator = 7;
var TokenStringLiteral = 8;
var TokenRegularExpression = 9;
TokenName = {};
TokenName[TokenBooleanLiteral] = "Boolean";
TokenName[TokenEOF] = "<end>";
TokenName[TokenIdentifier] = "Identifier";
TokenName[TokenKeyword] = "Keyword";
TokenName[TokenNullLiteral] = "Null";
TokenName[TokenNumericLiteral] = "Numeric";
TokenName[TokenPunctuator] = "Punctuator";
TokenName[TokenStringLiteral] = "String";
TokenName[TokenRegularExpression] = "RegularExpression";
var SyntaxArrayExpression = "ArrayExpression";
var SyntaxBinaryExpression = "BinaryExpression";
var SyntaxCallExpression = "CallExpression";
var SyntaxConditionalExpression = "ConditionalExpression";
var SyntaxIdentifier = "Identifier";
var SyntaxLiteral = "Literal";
var SyntaxLogicalExpression = "LogicalExpression";
var SyntaxMemberExpression = "MemberExpression";
var SyntaxObjectExpression = "ObjectExpression";
var SyntaxProperty = "Property";
var SyntaxUnaryExpression = "UnaryExpression";
var MessageUnexpectedToken = "Unexpected token %0";
var MessageUnexpectedNumber = "Unexpected number";
var MessageUnexpectedString = "Unexpected string";
var MessageUnexpectedIdentifier = "Unexpected identifier";
var MessageUnexpectedReserved = "Unexpected reserved word";
var MessageUnexpectedEOS = "Unexpected end of input";
var MessageInvalidRegExp = "Invalid regular expression";
var MessageUnterminatedRegExp = "Invalid regular expression: missing /";
var MessageStrictOctalLiteral = "Octal literals are not allowed in strict mode.";
var MessageStrictDuplicateProperty = "Duplicate data property in object literal not allowed in strict mode";
var ILLEGAL = "ILLEGAL";
var DISABLED = "Disabled.";
var RegexNonAsciiIdentifierStart = new RegExp("[\\xAA\\xB5\\xBA\\xC0-\\xD6\\xD8-\\xF6\\xF8-\\u02C1\\u02C6-\\u02D1\\u02E0-\\u02E4\\u02EC\\u02EE\\u0370-\\u0374\\u0376\\u0377\\u037A-\\u037D\\u037F\\u0386\\u0388-\\u038A\\u038C\\u038E-\\u03A1\\u03A3-\\u03F5\\u03F7-\\u0481\\u048A-\\u052F\\u0531-\\u0556\\u0559\\u0561-\\u0587\\u05D0-\\u05EA\\u05F0-\\u05F2\\u0620-\\u064A\\u066E\\u066F\\u0671-\\u06D3\\u06D5\\u06E5\\u06E6\\u06EE\\u06EF\\u06FA-\\u06FC\\u06FF\\u0710\\u0712-\\u072F\\u074D-\\u07A5\\u07B1\\u07CA-\\u07EA\\u07F4\\u07F5\\u07FA\\u0800-\\u0815\\u081A\\u0824\\u0828\\u0840-\\u0858\\u08A0-\\u08B2\\u0904-\\u0939\\u093D\\u0950\\u0958-\\u0961\\u0971-\\u0980\\u0985-\\u098C\\u098F\\u0990\\u0993-\\u09A8\\u09AA-\\u09B0\\u09B2\\u09B6-\\u09B9\\u09BD\\u09CE\\u09DC\\u09DD\\u09DF-\\u09E1\\u09F0\\u09F1\\u0A05-\\u0A0A\\u0A0F\\u0A10\\u0A13-\\u0A28\\u0A2A-\\u0A30\\u0A32\\u0A33\\u0A35\\u0A36\\u0A38\\u0A39\\u0A59-\\u0A5C\\u0A5E\\u0A72-\\u0A74\\u0A85-\\u0A8D\\u0A8F-\\u0A91\\u0A93-\\u0AA8\\u0AAA-\\u0AB0\\u0AB2\\u0AB3\\u0AB5-\\u0AB9\\u0ABD\\u0AD0\\u0AE0\\u0AE1\\u0B05-\\u0B0C\\u0B0F\\u0B10\\u0B13-\\u0B28\\u0B2A-\\u0B30\\u0B32\\u0B33\\u0B35-\\u0B39\\u0B3D\\u0B5C\\u0B5D\\u0B5F-\\u0B61\\u0B71\\u0B83\\u0B85-\\u0B8A\\u0B8E-\\u0B90\\u0B92-\\u0B95\\u0B99\\u0B9A\\u0B9C\\u0B9E\\u0B9F\\u0BA3\\u0BA4\\u0BA8-\\u0BAA\\u0BAE-\\u0BB9\\u0BD0\\u0C05-\\u0C0C\\u0C0E-\\u0C10\\u0C12-\\u0C28\\u0C2A-\\u0C39\\u0C3D\\u0C58\\u0C59\\u0C60\\u0C61\\u0C85-\\u0C8C\\u0C8E-\\u0C90\\u0C92-\\u0CA8\\u0CAA-\\u0CB3\\u0CB5-\\u0CB9\\u0CBD\\u0CDE\\u0CE0\\u0CE1\\u0CF1\\u0CF2\\u0D05-\\u0D0C\\u0D0E-\\u0D10\\u0D12-\\u0D3A\\u0D3D\\u0D4E\\u0D60\\u0D61\\u0D7A-\\u0D7F\\u0D85-\\u0D96\\u0D9A-\\u0DB1\\u0DB3-\\u0DBB\\u0DBD\\u0DC0-\\u0DC6\\u0E01-\\u0E30\\u0E32\\u0E33\\u0E40-\\u0E46\\u0E81\\u0E82\\u0E84\\u0E87\\u0E88\\u0E8A\\u0E8D\\u0E94-\\u0E97\\u0E99-\\u0E9F\\u0EA1-\\u0EA3\\u0EA5\\u0EA7\\u0EAA\\u0EAB\\u0EAD-\\u0EB0\\u0EB2\\u0EB3\\u0EBD\\u0EC0-\\u0EC4\\u0EC6\\u0EDC-\\u0EDF\\u0F00\\u0F40-\\u0F47\\u0F49-\\u0F6C\\u0F88-\\u0F8C\\u1000-\\u102A\\u103F\\u1050-\\u1055\\u105A-\\u105D\\u1061\\u1065\\u1066\\u106E-\\u1070\\u1075-\\u1081\\u108E\\u10A0-\\u10C5\\u10C7\\u10CD\\u10D0-\\u10FA\\u10FC-\\u1248\\u124A-\\u124D\\u1250-\\u1256\\u1258\\u125A-\\u125D\\u1260-\\u1288\\u128A-\\u128D\\u1290-\\u12B0\\u12B2-\\u12B5\\u12B8-\\u12BE\\u12C0\\u12C2-\\u12C5\\u12C8-\\u12D6\\u12D8-\\u1310\\u1312-\\u1315\\u1318-\\u135A\\u1380-\\u138F\\u13A0-\\u13F4\\u1401-\\u166C\\u166F-\\u167F\\u1681-\\u169A\\u16A0-\\u16EA\\u16EE-\\u16F8\\u1700-\\u170C\\u170E-\\u1711\\u1720-\\u1731\\u1740-\\u1751\\u1760-\\u176C\\u176E-\\u1770\\u1780-\\u17B3\\u17D7\\u17DC\\u1820-\\u1877\\u1880-\\u18A8\\u18AA\\u18B0-\\u18F5\\u1900-\\u191E\\u1950-\\u196D\\u1970-\\u1974\\u1980-\\u19AB\\u19C1-\\u19C7\\u1A00-\\u1A16\\u1A20-\\u1A54\\u1AA7\\u1B05-\\u1B33\\u1B45-\\u1B4B\\u1B83-\\u1BA0\\u1BAE\\u1BAF\\u1BBA-\\u1BE5\\u1C00-\\u1C23\\u1C4D-\\u1C4F\\u1C5A-\\u1C7D\\u1CE9-\\u1CEC\\u1CEE-\\u1CF1\\u1CF5\\u1CF6\\u1D00-\\u1DBF\\u1E00-\\u1F15\\u1F18-\\u1F1D\\u1F20-\\u1F45\\u1F48-\\u1F4D\\u1F50-\\u1F57\\u1F59\\u1F5B\\u1F5D\\u1F5F-\\u1F7D\\u1F80-\\u1FB4\\u1FB6-\\u1FBC\\u1FBE\\u1FC2-\\u1FC4\\u1FC6-\\u1FCC\\u1FD0-\\u1FD3\\u1FD6-\\u1FDB\\u1FE0-\\u1FEC\\u1FF2-\\u1FF4\\u1FF6-\\u1FFC\\u2071\\u207F\\u2090-\\u209C\\u2102\\u2107\\u210A-\\u2113\\u2115\\u2119-\\u211D\\u2124\\u2126\\u2128\\u212A-\\u212D\\u212F-\\u2139\\u213C-\\u213F\\u2145-\\u2149\\u214E\\u2160-\\u2188\\u2C00-\\u2C2E\\u2C30-\\u2C5E\\u2C60-\\u2CE4\\u2CEB-\\u2CEE\\u2CF2\\u2CF3\\u2D00-\\u2D25\\u2D27\\u2D2D\\u2D30-\\u2D67\\u2D6F\\u2D80-\\u2D96\\u2DA0-\\u2DA6\\u2DA8-\\u2DAE\\u2DB0-\\u2DB6\\u2DB8-\\u2DBE\\u2DC0-\\u2DC6\\u2DC8-\\u2DCE\\u2DD0-\\u2DD6\\u2DD8-\\u2DDE\\u2E2F\\u3005-\\u3007\\u3021-\\u3029\\u3031-\\u3035\\u3038-\\u303C\\u3041-\\u3096\\u309D-\\u309F\\u30A1-\\u30FA\\u30FC-\\u30FF\\u3105-\\u312D\\u3131-\\u318E\\u31A0-\\u31BA\\u31F0-\\u31FF\\u3400-\\u4DB5\\u4E00-\\u9FCC\\uA000-\\uA48C\\uA4D0-\\uA4FD\\uA500-\\uA60C\\uA610-\\uA61F\\uA62A\\uA62B\\uA640-\\uA66E\\uA67F-\\uA69D\\uA6A0-\\uA6EF\\uA717-\\uA71F\\uA722-\\uA788\\uA78B-\\uA78E\\uA790-\\uA7AD\\uA7B0\\uA7B1\\uA7F7-\\uA801\\uA803-\\uA805\\uA807-\\uA80A\\uA80C-\\uA822\\uA840-\\uA873\\uA882-\\uA8B3\\uA8F2-\\uA8F7\\uA8FB\\uA90A-\\uA925\\uA930-\\uA946\\uA960-\\uA97C\\uA984-\\uA9B2\\uA9CF\\uA9E0-\\uA9E4\\uA9E6-\\uA9EF\\uA9FA-\\uA9FE\\uAA00-\\uAA28\\uAA40-\\uAA42\\uAA44-\\uAA4B\\uAA60-\\uAA76\\uAA7A\\uAA7E-\\uAAAF\\uAAB1\\uAAB5\\uAAB6\\uAAB9-\\uAABD\\uAAC0\\uAAC2\\uAADB-\\uAADD\\uAAE0-\\uAAEA\\uAAF2-\\uAAF4\\uAB01-\\uAB06\\uAB09-\\uAB0E\\uAB11-\\uAB16\\uAB20-\\uAB26\\uAB28-\\uAB2E\\uAB30-\\uAB5A\\uAB5C-\\uAB5F\\uAB64\\uAB65\\uABC0-\\uABE2\\uAC00-\\uD7A3\\uD7B0-\\uD7C6\\uD7CB-\\uD7FB\\uF900-\\uFA6D\\uFA70-\\uFAD9\\uFB00-\\uFB06\\uFB13-\\uFB17\\uFB1D\\uFB1F-\\uFB28\\uFB2A-\\uFB36\\uFB38-\\uFB3C\\uFB3E\\uFB40\\uFB41\\uFB43\\uFB44\\uFB46-\\uFBB1\\uFBD3-\\uFD3D\\uFD50-\\uFD8F\\uFD92-\\uFDC7\\uFDF0-\\uFDFB\\uFE70-\\uFE74\\uFE76-\\uFEFC\\uFF21-\\uFF3A\\uFF41-\\uFF5A\\uFF66-\\uFFBE\\uFFC2-\\uFFC7\\uFFCA-\\uFFCF\\uFFD2-\\uFFD7\\uFFDA-\\uFFDC]");
var RegexNonAsciiIdentifierPart = new RegExp("[\\xAA\\xB5\\xBA\\xC0-\\xD6\\xD8-\\xF6\\xF8-\\u02C1\\u02C6-\\u02D1\\u02E0-\\u02E4\\u02EC\\u02EE\\u0300-\\u0374\\u0376\\u0377\\u037A-\\u037D\\u037F\\u0386\\u0388-\\u038A\\u038C\\u038E-\\u03A1\\u03A3-\\u03F5\\u03F7-\\u0481\\u0483-\\u0487\\u048A-\\u052F\\u0531-\\u0556\\u0559\\u0561-\\u0587\\u0591-\\u05BD\\u05BF\\u05C1\\u05C2\\u05C4\\u05C5\\u05C7\\u05D0-\\u05EA\\u05F0-\\u05F2\\u0610-\\u061A\\u0620-\\u0669\\u066E-\\u06D3\\u06D5-\\u06DC\\u06DF-\\u06E8\\u06EA-\\u06FC\\u06FF\\u0710-\\u074A\\u074D-\\u07B1\\u07C0-\\u07F5\\u07FA\\u0800-\\u082D\\u0840-\\u085B\\u08A0-\\u08B2\\u08E4-\\u0963\\u0966-\\u096F\\u0971-\\u0983\\u0985-\\u098C\\u098F\\u0990\\u0993-\\u09A8\\u09AA-\\u09B0\\u09B2\\u09B6-\\u09B9\\u09BC-\\u09C4\\u09C7\\u09C8\\u09CB-\\u09CE\\u09D7\\u09DC\\u09DD\\u09DF-\\u09E3\\u09E6-\\u09F1\\u0A01-\\u0A03\\u0A05-\\u0A0A\\u0A0F\\u0A10\\u0A13-\\u0A28\\u0A2A-\\u0A30\\u0A32\\u0A33\\u0A35\\u0A36\\u0A38\\u0A39\\u0A3C\\u0A3E-\\u0A42\\u0A47\\u0A48\\u0A4B-\\u0A4D\\u0A51\\u0A59-\\u0A5C\\u0A5E\\u0A66-\\u0A75\\u0A81-\\u0A83\\u0A85-\\u0A8D\\u0A8F-\\u0A91\\u0A93-\\u0AA8\\u0AAA-\\u0AB0\\u0AB2\\u0AB3\\u0AB5-\\u0AB9\\u0ABC-\\u0AC5\\u0AC7-\\u0AC9\\u0ACB-\\u0ACD\\u0AD0\\u0AE0-\\u0AE3\\u0AE6-\\u0AEF\\u0B01-\\u0B03\\u0B05-\\u0B0C\\u0B0F\\u0B10\\u0B13-\\u0B28\\u0B2A-\\u0B30\\u0B32\\u0B33\\u0B35-\\u0B39\\u0B3C-\\u0B44\\u0B47\\u0B48\\u0B4B-\\u0B4D\\u0B56\\u0B57\\u0B5C\\u0B5D\\u0B5F-\\u0B63\\u0B66-\\u0B6F\\u0B71\\u0B82\\u0B83\\u0B85-\\u0B8A\\u0B8E-\\u0B90\\u0B92-\\u0B95\\u0B99\\u0B9A\\u0B9C\\u0B9E\\u0B9F\\u0BA3\\u0BA4\\u0BA8-\\u0BAA\\u0BAE-\\u0BB9\\u0BBE-\\u0BC2\\u0BC6-\\u0BC8\\u0BCA-\\u0BCD\\u0BD0\\u0BD7\\u0BE6-\\u0BEF\\u0C00-\\u0C03\\u0C05-\\u0C0C\\u0C0E-\\u0C10\\u0C12-\\u0C28\\u0C2A-\\u0C39\\u0C3D-\\u0C44\\u0C46-\\u0C48\\u0C4A-\\u0C4D\\u0C55\\u0C56\\u0C58\\u0C59\\u0C60-\\u0C63\\u0C66-\\u0C6F\\u0C81-\\u0C83\\u0C85-\\u0C8C\\u0C8E-\\u0C90\\u0C92-\\u0CA8\\u0CAA-\\u0CB3\\u0CB5-\\u0CB9\\u0CBC-\\u0CC4\\u0CC6-\\u0CC8\\u0CCA-\\u0CCD\\u0CD5\\u0CD6\\u0CDE\\u0CE0-\\u0CE3\\u0CE6-\\u0CEF\\u0CF1\\u0CF2\\u0D01-\\u0D03\\u0D05-\\u0D0C\\u0D0E-\\u0D10\\u0D12-\\u0D3A\\u0D3D-\\u0D44\\u0D46-\\u0D48\\u0D4A-\\u0D4E\\u0D57\\u0D60-\\u0D63\\u0D66-\\u0D6F\\u0D7A-\\u0D7F\\u0D82\\u0D83\\u0D85-\\u0D96\\u0D9A-\\u0DB1\\u0DB3-\\u0DBB\\u0DBD\\u0DC0-\\u0DC6\\u0DCA\\u0DCF-\\u0DD4\\u0DD6\\u0DD8-\\u0DDF\\u0DE6-\\u0DEF\\u0DF2\\u0DF3\\u0E01-\\u0E3A\\u0E40-\\u0E4E\\u0E50-\\u0E59\\u0E81\\u0E82\\u0E84\\u0E87\\u0E88\\u0E8A\\u0E8D\\u0E94-\\u0E97\\u0E99-\\u0E9F\\u0EA1-\\u0EA3\\u0EA5\\u0EA7\\u0EAA\\u0EAB\\u0EAD-\\u0EB9\\u0EBB-\\u0EBD\\u0EC0-\\u0EC4\\u0EC6\\u0EC8-\\u0ECD\\u0ED0-\\u0ED9\\u0EDC-\\u0EDF\\u0F00\\u0F18\\u0F19\\u0F20-\\u0F29\\u0F35\\u0F37\\u0F39\\u0F3E-\\u0F47\\u0F49-\\u0F6C\\u0F71-\\u0F84\\u0F86-\\u0F97\\u0F99-\\u0FBC\\u0FC6\\u1000-\\u1049\\u1050-\\u109D\\u10A0-\\u10C5\\u10C7\\u10CD\\u10D0-\\u10FA\\u10FC-\\u1248\\u124A-\\u124D\\u1250-\\u1256\\u1258\\u125A-\\u125D\\u1260-\\u1288\\u128A-\\u128D\\u1290-\\u12B0\\u12B2-\\u12B5\\u12B8-\\u12BE\\u12C0\\u12C2-\\u12C5\\u12C8-\\u12D6\\u12D8-\\u1310\\u1312-\\u1315\\u1318-\\u135A\\u135D-\\u135F\\u1380-\\u138F\\u13A0-\\u13F4\\u1401-\\u166C\\u166F-\\u167F\\u1681-\\u169A\\u16A0-\\u16EA\\u16EE-\\u16F8\\u1700-\\u170C\\u170E-\\u1714\\u1720-\\u1734\\u1740-\\u1753\\u1760-\\u176C\\u176E-\\u1770\\u1772\\u1773\\u1780-\\u17D3\\u17D7\\u17DC\\u17DD\\u17E0-\\u17E9\\u180B-\\u180D\\u1810-\\u1819\\u1820-\\u1877\\u1880-\\u18AA\\u18B0-\\u18F5\\u1900-\\u191E\\u1920-\\u192B\\u1930-\\u193B\\u1946-\\u196D\\u1970-\\u1974\\u1980-\\u19AB\\u19B0-\\u19C9\\u19D0-\\u19D9\\u1A00-\\u1A1B\\u1A20-\\u1A5E\\u1A60-\\u1A7C\\u1A7F-\\u1A89\\u1A90-\\u1A99\\u1AA7\\u1AB0-\\u1ABD\\u1B00-\\u1B4B\\u1B50-\\u1B59\\u1B6B-\\u1B73\\u1B80-\\u1BF3\\u1C00-\\u1C37\\u1C40-\\u1C49\\u1C4D-\\u1C7D\\u1CD0-\\u1CD2\\u1CD4-\\u1CF6\\u1CF8\\u1CF9\\u1D00-\\u1DF5\\u1DFC-\\u1F15\\u1F18-\\u1F1D\\u1F20-\\u1F45\\u1F48-\\u1F4D\\u1F50-\\u1F57\\u1F59\\u1F5B\\u1F5D\\u1F5F-\\u1F7D\\u1F80-\\u1FB4\\u1FB6-\\u1FBC\\u1FBE\\u1FC2-\\u1FC4\\u1FC6-\\u1FCC\\u1FD0-\\u1FD3\\u1FD6-\\u1FDB\\u1FE0-\\u1FEC\\u1FF2-\\u1FF4\\u1FF6-\\u1FFC\\u200C\\u200D\\u203F\\u2040\\u2054\\u2071\\u207F\\u2090-\\u209C\\u20D0-\\u20DC\\u20E1\\u20E5-\\u20F0\\u2102\\u2107\\u210A-\\u2113\\u2115\\u2119-\\u211D\\u2124\\u2126\\u2128\\u212A-\\u212D\\u212F-\\u2139\\u213C-\\u213F\\u2145-\\u2149\\u214E\\u2160-\\u2188\\u2C00-\\u2C2E\\u2C30-\\u2C5E\\u2C60-\\u2CE4\\u2CEB-\\u2CF3\\u2D00-\\u2D25\\u2D27\\u2D2D\\u2D30-\\u2D67\\u2D6F\\u2D7F-\\u2D96\\u2DA0-\\u2DA6\\u2DA8-\\u2DAE\\u2DB0-\\u2DB6\\u2DB8-\\u2DBE\\u2DC0-\\u2DC6\\u2DC8-\\u2DCE\\u2DD0-\\u2DD6\\u2DD8-\\u2DDE\\u2DE0-\\u2DFF\\u2E2F\\u3005-\\u3007\\u3021-\\u302F\\u3031-\\u3035\\u3038-\\u303C\\u3041-\\u3096\\u3099\\u309A\\u309D-\\u309F\\u30A1-\\u30FA\\u30FC-\\u30FF\\u3105-\\u312D\\u3131-\\u318E\\u31A0-\\u31BA\\u31F0-\\u31FF\\u3400-\\u4DB5\\u4E00-\\u9FCC\\uA000-\\uA48C\\uA4D0-\\uA4FD\\uA500-\\uA60C\\uA610-\\uA62B\\uA640-\\uA66F\\uA674-\\uA67D\\uA67F-\\uA69D\\uA69F-\\uA6F1\\uA717-\\uA71F\\uA722-\\uA788\\uA78B-\\uA78E\\uA790-\\uA7AD\\uA7B0\\uA7B1\\uA7F7-\\uA827\\uA840-\\uA873\\uA880-\\uA8C4\\uA8D0-\\uA8D9\\uA8E0-\\uA8F7\\uA8FB\\uA900-\\uA92D\\uA930-\\uA953\\uA960-\\uA97C\\uA980-\\uA9C0\\uA9CF-\\uA9D9\\uA9E0-\\uA9FE\\uAA00-\\uAA36\\uAA40-\\uAA4D\\uAA50-\\uAA59\\uAA60-\\uAA76\\uAA7A-\\uAAC2\\uAADB-\\uAADD\\uAAE0-\\uAAEF\\uAAF2-\\uAAF6\\uAB01-\\uAB06\\uAB09-\\uAB0E\\uAB11-\\uAB16\\uAB20-\\uAB26\\uAB28-\\uAB2E\\uAB30-\\uAB5A\\uAB5C-\\uAB5F\\uAB64\\uAB65\\uABC0-\\uABEA\\uABEC\\uABED\\uABF0-\\uABF9\\uAC00-\\uD7A3\\uD7B0-\\uD7C6\\uD7CB-\\uD7FB\\uF900-\\uFA6D\\uFA70-\\uFAD9\\uFB00-\\uFB06\\uFB13-\\uFB17\\uFB1D-\\uFB28\\uFB2A-\\uFB36\\uFB38-\\uFB3C\\uFB3E\\uFB40\\uFB41\\uFB43\\uFB44\\uFB46-\\uFBB1\\uFBD3-\\uFD3D\\uFD50-\\uFD8F\\uFD92-\\uFDC7\\uFDF0-\\uFDFB\\uFE00-\\uFE0F\\uFE20-\\uFE2D\\uFE33\\uFE34\\uFE4D-\\uFE4F\\uFE70-\\uFE74\\uFE76-\\uFEFC\\uFF10-\\uFF19\\uFF21-\\uFF3A\\uFF3F\\uFF41-\\uFF5A\\uFF66-\\uFFBE\\uFFC2-\\uFFC7\\uFFCA-\\uFFCF\\uFFD2-\\uFFD7\\uFFDA-\\uFFDC]");
function assert(condition, message) {
  if (!condition) {
    throw new Error("ASSERT: " + message);
  }
}
function isDecimalDigit(ch2) {
  return ch2 >= 48 && ch2 <= 57;
}
function isHexDigit(ch2) {
  return "0123456789abcdefABCDEF".includes(ch2);
}
function isOctalDigit(ch2) {
  return "01234567".includes(ch2);
}
function isWhiteSpace(ch2) {
  return ch2 === 32 || ch2 === 9 || ch2 === 11 || ch2 === 12 || ch2 === 160 || ch2 >= 5760 && [5760, 6158, 8192, 8193, 8194, 8195, 8196, 8197, 8198, 8199, 8200, 8201, 8202, 8239, 8287, 12288, 65279].includes(ch2);
}
function isLineTerminator(ch2) {
  return ch2 === 10 || ch2 === 13 || ch2 === 8232 || ch2 === 8233;
}
function isIdentifierStart(ch2) {
  return ch2 === 36 || ch2 === 95 || // $ (dollar) and _ (underscore)
  ch2 >= 65 && ch2 <= 90 || // A..Z
  ch2 >= 97 && ch2 <= 122 || // a..z
  ch2 === 92 || // \ (backslash)
  ch2 >= 128 && RegexNonAsciiIdentifierStart.test(String.fromCharCode(ch2));
}
function isIdentifierPart(ch2) {
  return ch2 === 36 || ch2 === 95 || // $ (dollar) and _ (underscore)
  ch2 >= 65 && ch2 <= 90 || // A..Z
  ch2 >= 97 && ch2 <= 122 || // a..z
  ch2 >= 48 && ch2 <= 57 || // 0..9
  ch2 === 92 || // \ (backslash)
  ch2 >= 128 && RegexNonAsciiIdentifierPart.test(String.fromCharCode(ch2));
}
var keywords = {
  "if": 1,
  "in": 1,
  "do": 1,
  "var": 1,
  "for": 1,
  "new": 1,
  "try": 1,
  "let": 1,
  "this": 1,
  "else": 1,
  "case": 1,
  "void": 1,
  "with": 1,
  "enum": 1,
  "while": 1,
  "break": 1,
  "catch": 1,
  "throw": 1,
  "const": 1,
  "yield": 1,
  "class": 1,
  "super": 1,
  "return": 1,
  "typeof": 1,
  "delete": 1,
  "switch": 1,
  "export": 1,
  "import": 1,
  "public": 1,
  "static": 1,
  "default": 1,
  "finally": 1,
  "extends": 1,
  "package": 1,
  "private": 1,
  "function": 1,
  "continue": 1,
  "debugger": 1,
  "interface": 1,
  "protected": 1,
  "instanceof": 1,
  "implements": 1
};
function skipComment() {
  while (index2 < length) {
    const ch2 = source2.charCodeAt(index2);
    if (isWhiteSpace(ch2) || isLineTerminator(ch2)) {
      ++index2;
    } else {
      break;
    }
  }
}
function scanHexEscape(prefix) {
  var i, len, ch2, code = 0;
  len = prefix === "u" ? 4 : 2;
  for (i = 0; i < len; ++i) {
    if (index2 < length && isHexDigit(source2[index2])) {
      ch2 = source2[index2++];
      code = code * 16 + "0123456789abcdef".indexOf(ch2.toLowerCase());
    } else {
      throwError({}, MessageUnexpectedToken, ILLEGAL);
    }
  }
  return String.fromCharCode(code);
}
function scanUnicodeCodePointEscape() {
  var ch2, code, cu1, cu2;
  ch2 = source2[index2];
  code = 0;
  if (ch2 === "}") {
    throwError({}, MessageUnexpectedToken, ILLEGAL);
  }
  while (index2 < length) {
    ch2 = source2[index2++];
    if (!isHexDigit(ch2)) {
      break;
    }
    code = code * 16 + "0123456789abcdef".indexOf(ch2.toLowerCase());
  }
  if (code > 1114111 || ch2 !== "}") {
    throwError({}, MessageUnexpectedToken, ILLEGAL);
  }
  if (code <= 65535) {
    return String.fromCharCode(code);
  }
  cu1 = (code - 65536 >> 10) + 55296;
  cu2 = (code - 65536 & 1023) + 56320;
  return String.fromCharCode(cu1, cu2);
}
function getEscapedIdentifier() {
  var ch2, id2;
  ch2 = source2.charCodeAt(index2++);
  id2 = String.fromCharCode(ch2);
  if (ch2 === 92) {
    if (source2.charCodeAt(index2) !== 117) {
      throwError({}, MessageUnexpectedToken, ILLEGAL);
    }
    ++index2;
    ch2 = scanHexEscape("u");
    if (!ch2 || ch2 === "\\" || !isIdentifierStart(ch2.charCodeAt(0))) {
      throwError({}, MessageUnexpectedToken, ILLEGAL);
    }
    id2 = ch2;
  }
  while (index2 < length) {
    ch2 = source2.charCodeAt(index2);
    if (!isIdentifierPart(ch2)) {
      break;
    }
    ++index2;
    id2 += String.fromCharCode(ch2);
    if (ch2 === 92) {
      id2 = id2.substr(0, id2.length - 1);
      if (source2.charCodeAt(index2) !== 117) {
        throwError({}, MessageUnexpectedToken, ILLEGAL);
      }
      ++index2;
      ch2 = scanHexEscape("u");
      if (!ch2 || ch2 === "\\" || !isIdentifierPart(ch2.charCodeAt(0))) {
        throwError({}, MessageUnexpectedToken, ILLEGAL);
      }
      id2 += ch2;
    }
  }
  return id2;
}
function getIdentifier() {
  var start, ch2;
  start = index2++;
  while (index2 < length) {
    ch2 = source2.charCodeAt(index2);
    if (ch2 === 92) {
      index2 = start;
      return getEscapedIdentifier();
    }
    if (isIdentifierPart(ch2)) {
      ++index2;
    } else {
      break;
    }
  }
  return source2.slice(start, index2);
}
function scanIdentifier() {
  var start, id2, type4;
  start = index2;
  id2 = source2.charCodeAt(index2) === 92 ? getEscapedIdentifier() : getIdentifier();
  if (id2.length === 1) {
    type4 = TokenIdentifier;
  } else if (keywords.hasOwnProperty(id2)) {
    type4 = TokenKeyword;
  } else if (id2 === "null") {
    type4 = TokenNullLiteral;
  } else if (id2 === "true" || id2 === "false") {
    type4 = TokenBooleanLiteral;
  } else {
    type4 = TokenIdentifier;
  }
  return {
    type: type4,
    value: id2,
    start,
    end: index2
  };
}
function scanPunctuator() {
  var start = index2, code = source2.charCodeAt(index2), code2, ch1 = source2[index2], ch2, ch3, ch4;
  switch (code) {
    // Check for most common single-character punctuators.
    case 46:
    // . dot
    case 40:
    // ( open bracket
    case 41:
    // ) close bracket
    case 59:
    // ; semicolon
    case 44:
    // , comma
    case 123:
    // { open curly brace
    case 125:
    // } close curly brace
    case 91:
    // [
    case 93:
    // ]
    case 58:
    // :
    case 63:
    // ?
    case 126:
      ++index2;
      return {
        type: TokenPunctuator,
        value: String.fromCharCode(code),
        start,
        end: index2
      };
    default:
      code2 = source2.charCodeAt(index2 + 1);
      if (code2 === 61) {
        switch (code) {
          case 43:
          // +
          case 45:
          // -
          case 47:
          // /
          case 60:
          // <
          case 62:
          // >
          case 94:
          // ^
          case 124:
          // |
          case 37:
          // %
          case 38:
          // &
          case 42:
            index2 += 2;
            return {
              type: TokenPunctuator,
              value: String.fromCharCode(code) + String.fromCharCode(code2),
              start,
              end: index2
            };
          case 33:
          // !
          case 61:
            index2 += 2;
            if (source2.charCodeAt(index2) === 61) {
              ++index2;
            }
            return {
              type: TokenPunctuator,
              value: source2.slice(start, index2),
              start,
              end: index2
            };
        }
      }
  }
  ch4 = source2.substr(index2, 4);
  if (ch4 === ">>>=") {
    index2 += 4;
    return {
      type: TokenPunctuator,
      value: ch4,
      start,
      end: index2
    };
  }
  ch3 = ch4.substr(0, 3);
  if (ch3 === ">>>" || ch3 === "<<=" || ch3 === ">>=") {
    index2 += 3;
    return {
      type: TokenPunctuator,
      value: ch3,
      start,
      end: index2
    };
  }
  ch2 = ch3.substr(0, 2);
  if (ch1 === ch2[1] && "+-<>&|".includes(ch1) || ch2 === "=>") {
    index2 += 2;
    return {
      type: TokenPunctuator,
      value: ch2,
      start,
      end: index2
    };
  }
  if (ch2 === "//") {
    throwError({}, MessageUnexpectedToken, ILLEGAL);
  }
  if ("<>=!+-*%&|^/".includes(ch1)) {
    ++index2;
    return {
      type: TokenPunctuator,
      value: ch1,
      start,
      end: index2
    };
  }
  throwError({}, MessageUnexpectedToken, ILLEGAL);
}
function scanHexLiteral(start) {
  let number9 = "";
  while (index2 < length) {
    if (!isHexDigit(source2[index2])) {
      break;
    }
    number9 += source2[index2++];
  }
  if (number9.length === 0) {
    throwError({}, MessageUnexpectedToken, ILLEGAL);
  }
  if (isIdentifierStart(source2.charCodeAt(index2))) {
    throwError({}, MessageUnexpectedToken, ILLEGAL);
  }
  return {
    type: TokenNumericLiteral,
    value: parseInt("0x" + number9, 16),
    start,
    end: index2
  };
}
function scanOctalLiteral(start) {
  let number9 = "0" + source2[index2++];
  while (index2 < length) {
    if (!isOctalDigit(source2[index2])) {
      break;
    }
    number9 += source2[index2++];
  }
  if (isIdentifierStart(source2.charCodeAt(index2)) || isDecimalDigit(source2.charCodeAt(index2))) {
    throwError({}, MessageUnexpectedToken, ILLEGAL);
  }
  return {
    type: TokenNumericLiteral,
    value: parseInt(number9, 8),
    octal: true,
    start,
    end: index2
  };
}
function scanNumericLiteral() {
  var number9, start, ch2;
  ch2 = source2[index2];
  assert(isDecimalDigit(ch2.charCodeAt(0)) || ch2 === ".", "Numeric literal must start with a decimal digit or a decimal point");
  start = index2;
  number9 = "";
  if (ch2 !== ".") {
    number9 = source2[index2++];
    ch2 = source2[index2];
    if (number9 === "0") {
      if (ch2 === "x" || ch2 === "X") {
        ++index2;
        return scanHexLiteral(start);
      }
      if (isOctalDigit(ch2)) {
        return scanOctalLiteral(start);
      }
      if (ch2 && isDecimalDigit(ch2.charCodeAt(0))) {
        throwError({}, MessageUnexpectedToken, ILLEGAL);
      }
    }
    while (isDecimalDigit(source2.charCodeAt(index2))) {
      number9 += source2[index2++];
    }
    ch2 = source2[index2];
  }
  if (ch2 === ".") {
    number9 += source2[index2++];
    while (isDecimalDigit(source2.charCodeAt(index2))) {
      number9 += source2[index2++];
    }
    ch2 = source2[index2];
  }
  if (ch2 === "e" || ch2 === "E") {
    number9 += source2[index2++];
    ch2 = source2[index2];
    if (ch2 === "+" || ch2 === "-") {
      number9 += source2[index2++];
    }
    if (isDecimalDigit(source2.charCodeAt(index2))) {
      while (isDecimalDigit(source2.charCodeAt(index2))) {
        number9 += source2[index2++];
      }
    } else {
      throwError({}, MessageUnexpectedToken, ILLEGAL);
    }
  }
  if (isIdentifierStart(source2.charCodeAt(index2))) {
    throwError({}, MessageUnexpectedToken, ILLEGAL);
  }
  return {
    type: TokenNumericLiteral,
    value: parseFloat(number9),
    start,
    end: index2
  };
}
function scanStringLiteral() {
  var str = "", quote, start, ch2, code, octal = false;
  quote = source2[index2];
  assert(quote === "'" || quote === '"', "String literal must starts with a quote");
  start = index2;
  ++index2;
  while (index2 < length) {
    ch2 = source2[index2++];
    if (ch2 === quote) {
      quote = "";
      break;
    } else if (ch2 === "\\") {
      ch2 = source2[index2++];
      if (!ch2 || !isLineTerminator(ch2.charCodeAt(0))) {
        switch (ch2) {
          case "u":
          case "x":
            if (source2[index2] === "{") {
              ++index2;
              str += scanUnicodeCodePointEscape();
            } else {
              str += scanHexEscape(ch2);
            }
            break;
          case "n":
            str += "\n";
            break;
          case "r":
            str += "\r";
            break;
          case "t":
            str += "	";
            break;
          case "b":
            str += "\b";
            break;
          case "f":
            str += "\f";
            break;
          case "v":
            str += "\v";
            break;
          default:
            if (isOctalDigit(ch2)) {
              code = "01234567".indexOf(ch2);
              if (code !== 0) {
                octal = true;
              }
              if (index2 < length && isOctalDigit(source2[index2])) {
                octal = true;
                code = code * 8 + "01234567".indexOf(source2[index2++]);
                if ("0123".includes(ch2) && index2 < length && isOctalDigit(source2[index2])) {
                  code = code * 8 + "01234567".indexOf(source2[index2++]);
                }
              }
              str += String.fromCharCode(code);
            } else {
              str += ch2;
            }
            break;
        }
      } else {
        if (ch2 === "\r" && source2[index2] === "\n") {
          ++index2;
        }
      }
    } else if (isLineTerminator(ch2.charCodeAt(0))) {
      break;
    } else {
      str += ch2;
    }
  }
  if (quote !== "") {
    throwError({}, MessageUnexpectedToken, ILLEGAL);
  }
  return {
    type: TokenStringLiteral,
    value: str,
    octal,
    start,
    end: index2
  };
}
function testRegExp(pattern, flags) {
  let tmp = pattern;
  if (flags.includes("u")) {
    tmp = tmp.replace(/\\u\{([0-9a-fA-F]+)\}/g, ($0, $1) => {
      if (parseInt($1, 16) <= 1114111) {
        return "x";
      }
      throwError({}, MessageInvalidRegExp);
    }).replace(/[\uD800-\uDBFF][\uDC00-\uDFFF]/g, "x");
  }
  try {
    new RegExp(tmp);
  } catch (e) {
    throwError({}, MessageInvalidRegExp);
  }
  try {
    return new RegExp(pattern, flags);
  } catch (exception) {
    return null;
  }
}
function scanRegExpBody() {
  var ch2, str, classMarker, terminated, body;
  ch2 = source2[index2];
  assert(ch2 === "/", "Regular expression literal must start with a slash");
  str = source2[index2++];
  classMarker = false;
  terminated = false;
  while (index2 < length) {
    ch2 = source2[index2++];
    str += ch2;
    if (ch2 === "\\") {
      ch2 = source2[index2++];
      if (isLineTerminator(ch2.charCodeAt(0))) {
        throwError({}, MessageUnterminatedRegExp);
      }
      str += ch2;
    } else if (isLineTerminator(ch2.charCodeAt(0))) {
      throwError({}, MessageUnterminatedRegExp);
    } else if (classMarker) {
      if (ch2 === "]") {
        classMarker = false;
      }
    } else {
      if (ch2 === "/") {
        terminated = true;
        break;
      } else if (ch2 === "[") {
        classMarker = true;
      }
    }
  }
  if (!terminated) {
    throwError({}, MessageUnterminatedRegExp);
  }
  body = str.substr(1, str.length - 2);
  return {
    value: body,
    literal: str
  };
}
function scanRegExpFlags() {
  var ch2, str, flags;
  str = "";
  flags = "";
  while (index2 < length) {
    ch2 = source2[index2];
    if (!isIdentifierPart(ch2.charCodeAt(0))) {
      break;
    }
    ++index2;
    if (ch2 === "\\" && index2 < length) {
      throwError({}, MessageUnexpectedToken, ILLEGAL);
    } else {
      flags += ch2;
      str += ch2;
    }
  }
  if (flags.search(/[^gimuy]/g) >= 0) {
    throwError({}, MessageInvalidRegExp, flags);
  }
  return {
    value: flags,
    literal: str
  };
}
function scanRegExp() {
  var start, body, flags, value4;
  lookahead = null;
  skipComment();
  start = index2;
  body = scanRegExpBody();
  flags = scanRegExpFlags();
  value4 = testRegExp(body.value, flags.value);
  return {
    literal: body.literal + flags.literal,
    value: value4,
    regex: {
      pattern: body.value,
      flags: flags.value
    },
    start,
    end: index2
  };
}
function isIdentifierName(token) {
  return token.type === TokenIdentifier || token.type === TokenKeyword || token.type === TokenBooleanLiteral || token.type === TokenNullLiteral;
}
function advance() {
  skipComment();
  if (index2 >= length) {
    return {
      type: TokenEOF,
      start: index2,
      end: index2
    };
  }
  const ch2 = source2.charCodeAt(index2);
  if (isIdentifierStart(ch2)) {
    return scanIdentifier();
  }
  if (ch2 === 40 || ch2 === 41 || ch2 === 59) {
    return scanPunctuator();
  }
  if (ch2 === 39 || ch2 === 34) {
    return scanStringLiteral();
  }
  if (ch2 === 46) {
    if (isDecimalDigit(source2.charCodeAt(index2 + 1))) {
      return scanNumericLiteral();
    }
    return scanPunctuator();
  }
  if (isDecimalDigit(ch2)) {
    return scanNumericLiteral();
  }
  return scanPunctuator();
}
function lex() {
  const token = lookahead;
  index2 = token.end;
  lookahead = advance();
  index2 = token.end;
  return token;
}
function peek2() {
  const pos = index2;
  lookahead = advance();
  index2 = pos;
}
function finishArrayExpression(elements) {
  const node = new ASTNode(SyntaxArrayExpression);
  node.elements = elements;
  return node;
}
function finishBinaryExpression(operator2, left, right) {
  const node = new ASTNode(operator2 === "||" || operator2 === "&&" ? SyntaxLogicalExpression : SyntaxBinaryExpression);
  node.operator = operator2;
  node.left = left;
  node.right = right;
  return node;
}
function finishCallExpression(callee, args) {
  const node = new ASTNode(SyntaxCallExpression);
  node.callee = callee;
  node.arguments = args;
  return node;
}
function finishConditionalExpression(test2, consequent, alternate) {
  const node = new ASTNode(SyntaxConditionalExpression);
  node.test = test2;
  node.consequent = consequent;
  node.alternate = alternate;
  return node;
}
function finishIdentifier(name) {
  const node = new ASTNode(SyntaxIdentifier);
  node.name = name;
  return node;
}
function finishLiteral(token) {
  const node = new ASTNode(SyntaxLiteral);
  node.value = token.value;
  node.raw = source2.slice(token.start, token.end);
  if (token.regex) {
    if (node.raw === "//") {
      node.raw = "/(?:)/";
    }
    node.regex = token.regex;
  }
  return node;
}
function finishMemberExpression(accessor2, object2, property2) {
  const node = new ASTNode(SyntaxMemberExpression);
  node.computed = accessor2 === "[";
  node.object = object2;
  node.property = property2;
  if (!node.computed) property2.member = true;
  return node;
}
function finishObjectExpression(properties) {
  const node = new ASTNode(SyntaxObjectExpression);
  node.properties = properties;
  return node;
}
function finishProperty(kind, key2, value4) {
  const node = new ASTNode(SyntaxProperty);
  node.key = key2;
  node.value = value4;
  node.kind = kind;
  return node;
}
function finishUnaryExpression(operator2, argument) {
  const node = new ASTNode(SyntaxUnaryExpression);
  node.operator = operator2;
  node.argument = argument;
  node.prefix = true;
  return node;
}
function throwError(token, messageFormat) {
  var error3, args = Array.prototype.slice.call(arguments, 2), msg = messageFormat.replace(/%(\d)/g, (whole, index4) => {
    assert(index4 < args.length, "Message reference must be in range");
    return args[index4];
  });
  error3 = new Error(msg);
  error3.index = index2;
  error3.description = msg;
  throw error3;
}
function throwUnexpected(token) {
  if (token.type === TokenEOF) {
    throwError(token, MessageUnexpectedEOS);
  }
  if (token.type === TokenNumericLiteral) {
    throwError(token, MessageUnexpectedNumber);
  }
  if (token.type === TokenStringLiteral) {
    throwError(token, MessageUnexpectedString);
  }
  if (token.type === TokenIdentifier) {
    throwError(token, MessageUnexpectedIdentifier);
  }
  if (token.type === TokenKeyword) {
    throwError(token, MessageUnexpectedReserved);
  }
  throwError(token, MessageUnexpectedToken, token.value);
}
function expect(value4) {
  const token = lex();
  if (token.type !== TokenPunctuator || token.value !== value4) {
    throwUnexpected(token);
  }
}
function match(value4) {
  return lookahead.type === TokenPunctuator && lookahead.value === value4;
}
function matchKeyword(keyword) {
  return lookahead.type === TokenKeyword && lookahead.value === keyword;
}
function parseArrayInitialiser() {
  const elements = [];
  index2 = lookahead.start;
  expect("[");
  while (!match("]")) {
    if (match(",")) {
      lex();
      elements.push(null);
    } else {
      elements.push(parseConditionalExpression());
      if (!match("]")) {
        expect(",");
      }
    }
  }
  lex();
  return finishArrayExpression(elements);
}
function parseObjectPropertyKey() {
  index2 = lookahead.start;
  const token = lex();
  if (token.type === TokenStringLiteral || token.type === TokenNumericLiteral) {
    if (token.octal) {
      throwError(token, MessageStrictOctalLiteral);
    }
    return finishLiteral(token);
  }
  return finishIdentifier(token.value);
}
function parseObjectProperty() {
  var token, key2, id2, value4;
  index2 = lookahead.start;
  token = lookahead;
  if (token.type === TokenIdentifier) {
    id2 = parseObjectPropertyKey();
    expect(":");
    value4 = parseConditionalExpression();
    return finishProperty("init", id2, value4);
  }
  if (token.type === TokenEOF || token.type === TokenPunctuator) {
    throwUnexpected(token);
  } else {
    key2 = parseObjectPropertyKey();
    expect(":");
    value4 = parseConditionalExpression();
    return finishProperty("init", key2, value4);
  }
}
function parseObjectInitialiser() {
  var properties = [], property2, name, key2, map3 = {}, toString2 = String;
  index2 = lookahead.start;
  expect("{");
  while (!match("}")) {
    property2 = parseObjectProperty();
    if (property2.key.type === SyntaxIdentifier) {
      name = property2.key.name;
    } else {
      name = toString2(property2.key.value);
    }
    key2 = "$" + name;
    if (Object.prototype.hasOwnProperty.call(map3, key2)) {
      throwError({}, MessageStrictDuplicateProperty);
    } else {
      map3[key2] = true;
    }
    properties.push(property2);
    if (!match("}")) {
      expect(",");
    }
  }
  expect("}");
  return finishObjectExpression(properties);
}
function parseGroupExpression() {
  expect("(");
  const expr2 = parseExpression();
  expect(")");
  return expr2;
}
var legalKeywords = {
  "if": 1
};
function parsePrimaryExpression() {
  var type4, token, expr2;
  if (match("(")) {
    return parseGroupExpression();
  }
  if (match("[")) {
    return parseArrayInitialiser();
  }
  if (match("{")) {
    return parseObjectInitialiser();
  }
  type4 = lookahead.type;
  index2 = lookahead.start;
  if (type4 === TokenIdentifier || legalKeywords[lookahead.value]) {
    expr2 = finishIdentifier(lex().value);
  } else if (type4 === TokenStringLiteral || type4 === TokenNumericLiteral) {
    if (lookahead.octal) {
      throwError(lookahead, MessageStrictOctalLiteral);
    }
    expr2 = finishLiteral(lex());
  } else if (type4 === TokenKeyword) {
    throw new Error(DISABLED);
  } else if (type4 === TokenBooleanLiteral) {
    token = lex();
    token.value = token.value === "true";
    expr2 = finishLiteral(token);
  } else if (type4 === TokenNullLiteral) {
    token = lex();
    token.value = null;
    expr2 = finishLiteral(token);
  } else if (match("/") || match("/=")) {
    expr2 = finishLiteral(scanRegExp());
    peek2();
  } else {
    throwUnexpected(lex());
  }
  return expr2;
}
function parseArguments() {
  const args = [];
  expect("(");
  if (!match(")")) {
    while (index2 < length) {
      args.push(parseConditionalExpression());
      if (match(")")) {
        break;
      }
      expect(",");
    }
  }
  expect(")");
  return args;
}
function parseNonComputedProperty() {
  index2 = lookahead.start;
  const token = lex();
  if (!isIdentifierName(token)) {
    throwUnexpected(token);
  }
  return finishIdentifier(token.value);
}
function parseNonComputedMember() {
  expect(".");
  return parseNonComputedProperty();
}
function parseComputedMember() {
  expect("[");
  const expr2 = parseExpression();
  expect("]");
  return expr2;
}
function parseLeftHandSideExpressionAllowCall() {
  var expr2, args, property2;
  expr2 = parsePrimaryExpression();
  for (; ; ) {
    if (match(".")) {
      property2 = parseNonComputedMember();
      expr2 = finishMemberExpression(".", expr2, property2);
    } else if (match("(")) {
      args = parseArguments();
      expr2 = finishCallExpression(expr2, args);
    } else if (match("[")) {
      property2 = parseComputedMember();
      expr2 = finishMemberExpression("[", expr2, property2);
    } else {
      break;
    }
  }
  return expr2;
}
function parsePostfixExpression() {
  const expr2 = parseLeftHandSideExpressionAllowCall();
  if (lookahead.type === TokenPunctuator) {
    if (match("++") || match("--")) {
      throw new Error(DISABLED);
    }
  }
  return expr2;
}
function parseUnaryExpression() {
  var token, expr2;
  if (lookahead.type !== TokenPunctuator && lookahead.type !== TokenKeyword) {
    expr2 = parsePostfixExpression();
  } else if (match("++") || match("--")) {
    throw new Error(DISABLED);
  } else if (match("+") || match("-") || match("~") || match("!")) {
    token = lex();
    expr2 = parseUnaryExpression();
    expr2 = finishUnaryExpression(token.value, expr2);
  } else if (matchKeyword("delete") || matchKeyword("void") || matchKeyword("typeof")) {
    throw new Error(DISABLED);
  } else {
    expr2 = parsePostfixExpression();
  }
  return expr2;
}
function binaryPrecedence(token) {
  let prec = 0;
  if (token.type !== TokenPunctuator && token.type !== TokenKeyword) {
    return 0;
  }
  switch (token.value) {
    case "||":
      prec = 1;
      break;
    case "&&":
      prec = 2;
      break;
    case "|":
      prec = 3;
      break;
    case "^":
      prec = 4;
      break;
    case "&":
      prec = 5;
      break;
    case "==":
    case "!=":
    case "===":
    case "!==":
      prec = 6;
      break;
    case "<":
    case ">":
    case "<=":
    case ">=":
    case "instanceof":
    case "in":
      prec = 7;
      break;
    case "<<":
    case ">>":
    case ">>>":
      prec = 8;
      break;
    case "+":
    case "-":
      prec = 9;
      break;
    case "*":
    case "/":
    case "%":
      prec = 11;
      break;
  }
  return prec;
}
function parseBinaryExpression() {
  var marker, markers, expr2, token, prec, stack2, right, operator2, left, i;
  marker = lookahead;
  left = parseUnaryExpression();
  token = lookahead;
  prec = binaryPrecedence(token);
  if (prec === 0) {
    return left;
  }
  token.prec = prec;
  lex();
  markers = [marker, lookahead];
  right = parseUnaryExpression();
  stack2 = [left, token, right];
  while ((prec = binaryPrecedence(lookahead)) > 0) {
    while (stack2.length > 2 && prec <= stack2[stack2.length - 2].prec) {
      right = stack2.pop();
      operator2 = stack2.pop().value;
      left = stack2.pop();
      markers.pop();
      expr2 = finishBinaryExpression(operator2, left, right);
      stack2.push(expr2);
    }
    token = lex();
    token.prec = prec;
    stack2.push(token);
    markers.push(lookahead);
    expr2 = parseUnaryExpression();
    stack2.push(expr2);
  }
  i = stack2.length - 1;
  expr2 = stack2[i];
  markers.pop();
  while (i > 1) {
    markers.pop();
    expr2 = finishBinaryExpression(stack2[i - 1].value, stack2[i - 2], expr2);
    i -= 2;
  }
  return expr2;
}
function parseConditionalExpression() {
  var expr2, consequent, alternate;
  expr2 = parseBinaryExpression();
  if (match("?")) {
    lex();
    consequent = parseConditionalExpression();
    expect(":");
    alternate = parseConditionalExpression();
    expr2 = finishConditionalExpression(expr2, consequent, alternate);
  }
  return expr2;
}
function parseExpression() {
  const expr2 = parseConditionalExpression();
  if (match(",")) {
    throw new Error(DISABLED);
  }
  return expr2;
}
function parser(code) {
  source2 = code;
  index2 = 0;
  length = source2.length;
  lookahead = null;
  peek2();
  const expr2 = parseExpression();
  if (lookahead.type !== TokenEOF) {
    throw new Error("Unexpect token after expression.");
  }
  return expr2;
}
var Constants = {
  NaN: "NaN",
  E: "Math.E",
  LN2: "Math.LN2",
  LN10: "Math.LN10",
  LOG2E: "Math.LOG2E",
  LOG10E: "Math.LOG10E",
  PI: "Math.PI",
  SQRT1_2: "Math.SQRT1_2",
  SQRT2: "Math.SQRT2",
  MIN_VALUE: "Number.MIN_VALUE",
  MAX_VALUE: "Number.MAX_VALUE"
};
function Functions(codegen2) {
  function fncall(name, args, cast, type4) {
    let obj = codegen2(args[0]);
    if (cast) {
      obj = cast + "(" + obj + ")";
      if (cast.lastIndexOf("new ", 0) === 0) obj = "(" + obj + ")";
    }
    return obj + "." + name + (type4 < 0 ? "" : type4 === 0 ? "()" : "(" + args.slice(1).map(codegen2).join(",") + ")");
  }
  function fn(name, cast, type4) {
    return (args) => fncall(name, args, cast, type4);
  }
  const DATE3 = "new Date", STRING = "String", REGEXP = "RegExp";
  return {
    // MATH functions
    isNaN: "Number.isNaN",
    isFinite: "Number.isFinite",
    abs: "Math.abs",
    acos: "Math.acos",
    asin: "Math.asin",
    atan: "Math.atan",
    atan2: "Math.atan2",
    ceil: "Math.ceil",
    cos: "Math.cos",
    exp: "Math.exp",
    floor: "Math.floor",
    hypot: "Math.hypot",
    log: "Math.log",
    max: "Math.max",
    min: "Math.min",
    pow: "Math.pow",
    random: "Math.random",
    round: "Math.round",
    sin: "Math.sin",
    sqrt: "Math.sqrt",
    tan: "Math.tan",
    clamp: function(args) {
      if (args.length < 3) error("Missing arguments to clamp function.");
      if (args.length > 3) error("Too many arguments to clamp function.");
      const a3 = args.map(codegen2);
      return "Math.max(" + a3[1] + ", Math.min(" + a3[2] + "," + a3[0] + "))";
    },
    // DATE functions
    now: "Date.now",
    utc: "Date.UTC",
    datetime: DATE3,
    date: fn("getDate", DATE3, 0),
    day: fn("getDay", DATE3, 0),
    year: fn("getFullYear", DATE3, 0),
    month: fn("getMonth", DATE3, 0),
    hours: fn("getHours", DATE3, 0),
    minutes: fn("getMinutes", DATE3, 0),
    seconds: fn("getSeconds", DATE3, 0),
    milliseconds: fn("getMilliseconds", DATE3, 0),
    time: fn("getTime", DATE3, 0),
    timezoneoffset: fn("getTimezoneOffset", DATE3, 0),
    utcdate: fn("getUTCDate", DATE3, 0),
    utcday: fn("getUTCDay", DATE3, 0),
    utcyear: fn("getUTCFullYear", DATE3, 0),
    utcmonth: fn("getUTCMonth", DATE3, 0),
    utchours: fn("getUTCHours", DATE3, 0),
    utcminutes: fn("getUTCMinutes", DATE3, 0),
    utcseconds: fn("getUTCSeconds", DATE3, 0),
    utcmilliseconds: fn("getUTCMilliseconds", DATE3, 0),
    // sequence functions
    length: fn("length", null, -1),
    // STRING functions
    parseFloat: "parseFloat",
    parseInt: "parseInt",
    upper: fn("toUpperCase", STRING, 0),
    lower: fn("toLowerCase", STRING, 0),
    substring: fn("substring", STRING),
    split: fn("split", STRING),
    trim: fn("trim", STRING, 0),
    // base64 encode/decode
    btoa: "btoa",
    atob: "atob",
    // URI encoding
    encodeURIComponent: "encodeURIComponent",
    // REGEXP functions
    regexp: REGEXP,
    test: fn("test", REGEXP),
    // Control Flow functions
    if: function(args) {
      if (args.length < 3) error("Missing arguments to if function.");
      if (args.length > 3) error("Too many arguments to if function.");
      const a3 = args.map(codegen2);
      return "(" + a3[0] + "?" + a3[1] + ":" + a3[2] + ")";
    }
  };
}
function stripQuotes(s) {
  const n = s && s.length - 1;
  return n && (s[0] === '"' && s[n] === '"' || s[0] === "'" && s[n] === "'") ? s.slice(1, -1) : s;
}
function codegen(opt) {
  opt = opt || {};
  const allowed = opt.allowed ? toSet(opt.allowed) : {}, forbidden = opt.forbidden ? toSet(opt.forbidden) : {}, constants2 = opt.constants || Constants, functions = (opt.functions || Functions)(visit3), globalvar = opt.globalvar, fieldvar = opt.fieldvar, outputGlobal = isFunction(globalvar) ? globalvar : (id2) => `${globalvar}["${id2}"]`;
  let globals = {}, fields = {}, memberDepth = 0;
  function visit3(ast) {
    if (isString(ast)) return ast;
    const generator = Generators[ast.type];
    if (generator == null) error("Unsupported type: " + ast.type);
    return generator(ast);
  }
  const Generators = {
    Literal: (n) => n.raw,
    Identifier: (n) => {
      const id2 = n.name;
      if (memberDepth > 0) {
        return id2;
      } else if (hasOwnProperty(forbidden, id2)) {
        return error("Illegal identifier: " + id2);
      } else if (hasOwnProperty(constants2, id2)) {
        return constants2[id2];
      } else if (hasOwnProperty(allowed, id2)) {
        return id2;
      } else {
        globals[id2] = 1;
        return outputGlobal(id2);
      }
    },
    MemberExpression: (n) => {
      const d = !n.computed, o = visit3(n.object);
      if (d) memberDepth += 1;
      const p = visit3(n.property);
      if (o === fieldvar) {
        fields[stripQuotes(p)] = 1;
      }
      if (d) memberDepth -= 1;
      return o + (d ? "." + p : "[" + p + "]");
    },
    CallExpression: (n) => {
      if (n.callee.type !== "Identifier") {
        error("Illegal callee type: " + n.callee.type);
      }
      const callee = n.callee.name, args = n.arguments, fn = hasOwnProperty(functions, callee) && functions[callee];
      if (!fn) error("Unrecognized function: " + callee);
      return isFunction(fn) ? fn(args) : fn + "(" + args.map(visit3).join(",") + ")";
    },
    ArrayExpression: (n) => "[" + n.elements.map(visit3).join(",") + "]",
    BinaryExpression: (n) => "(" + visit3(n.left) + " " + n.operator + " " + visit3(n.right) + ")",
    UnaryExpression: (n) => "(" + n.operator + visit3(n.argument) + ")",
    ConditionalExpression: (n) => "(" + visit3(n.test) + "?" + visit3(n.consequent) + ":" + visit3(n.alternate) + ")",
    LogicalExpression: (n) => "(" + visit3(n.left) + n.operator + visit3(n.right) + ")",
    ObjectExpression: (n) => {
      for (const prop of n.properties) {
        const keyName = prop.key.type === "Literal" ? String(prop.key.value) : prop.key.name;
        if (DisallowedObjectProperties.has(keyName)) {
          error("Illegal property: " + keyName);
        }
      }
      return "{" + n.properties.map(visit3).join(",") + "}";
    },
    Property: (n) => {
      memberDepth += 1;
      const k = visit3(n.key);
      memberDepth -= 1;
      return k + ":" + visit3(n.value);
    }
  };
  function codegen2(ast) {
    const result = {
      code: visit3(ast),
      globals: Object.keys(globals),
      fields: Object.keys(fields)
    };
    globals = {};
    fields = {};
    return result;
  }
  codegen2.functions = functions;
  codegen2.constants = constants2;
  return codegen2;
}

// ../deepseek-harness/node_modules/.pnpm/vega-selections@6.1.5/node_modules/vega-selections/build/vega-selection.js
var SELECTION_GETTER = Symbol("vega_selection_getter");
function getter2(f) {
  if (!f.getter || !f.getter[SELECTION_GETTER]) {
    f.getter = field(f.field);
    f.getter[SELECTION_GETTER] = true;
  }
  return f.getter;
}
var Intersect = "intersect";
var Union = "union";
var VlMulti = "vlMulti";
var VlPoint = "vlPoint";
var Or = "or";
var And = "and";
var SelectionId = "_vgsid_";
var $selectionId = field(SelectionId);
var TYPE_ENUM = "E";
var TYPE_RANGE_INC = "R";
var TYPE_RANGE_EXC = "R-E";
var TYPE_RANGE_LE = "R-LE";
var TYPE_RANGE_RE = "R-RE";
var TYPE_PRED_LT = "E-LT";
var TYPE_PRED_LTE = "E-LTE";
var TYPE_PRED_GT = "E-GT";
var TYPE_PRED_GTE = "E-GTE";
var TYPE_PRED_VALID = "E-VALID";
var TYPE_PRED_ONE_OF = "E-ONE";
var UNIT_INDEX = "index:unit";
function testPoint(datum3, entry2) {
  var fields = entry2.fields, values4 = entry2.values, n = fields.length, i = 0, dval, f;
  for (; i < n; ++i) {
    f = fields[i];
    dval = getter2(f)(datum3);
    if (isDate(dval)) dval = toNumber(dval);
    if (isDate(values4[i])) values4[i] = toNumber(values4[i]);
    if (isArray(values4[i]) && isDate(values4[i][0])) values4[i] = values4[i].map(toNumber);
    if (f.type === TYPE_ENUM) {
      if (isArray(values4[i]) ? !values4[i].includes(dval) : dval !== values4[i]) {
        return false;
      }
    } else {
      if (f.type === TYPE_RANGE_INC) {
        if (!inrange(dval, values4[i])) return false;
      } else if (f.type === TYPE_RANGE_RE) {
        if (!inrange(dval, values4[i], true, false)) return false;
      } else if (f.type === TYPE_RANGE_EXC) {
        if (!inrange(dval, values4[i], false, false)) return false;
      } else if (f.type === TYPE_RANGE_LE) {
        if (!inrange(dval, values4[i], false, true)) return false;
      } else if (f.type === TYPE_PRED_LT) {
        if (dval >= values4[i]) return false;
      } else if (f.type === TYPE_PRED_LTE) {
        if (dval > values4[i]) return false;
      } else if (f.type === TYPE_PRED_GT) {
        if (dval <= values4[i]) return false;
      } else if (f.type === TYPE_PRED_GTE) {
        if (dval < values4[i]) return false;
      } else if (f.type === TYPE_PRED_VALID) {
        if (dval === null || isNaN(dval)) return false;
      } else if (f.type === TYPE_PRED_ONE_OF) {
        if (values4[i].indexOf(dval) === -1) return false;
      }
    }
  }
  return true;
}
function selectionTest(name, datum3, op) {
  var data4 = this.context.data[name], entries2 = data4 ? data4.values.value : [], unitIdx = data4 ? data4[UNIT_INDEX] && data4[UNIT_INDEX].value : void 0, intersect7 = op === Intersect, n = entries2.length, i = 0, entry2, miss, count2, unit2, b10;
  for (; i < n; ++i) {
    entry2 = entries2[i];
    if (unitIdx && intersect7) {
      miss = miss || {};
      count2 = miss[unit2 = entry2.unit] || 0;
      if (count2 === -1) continue;
      b10 = testPoint(datum3, entry2);
      miss[unit2] = b10 ? -1 : ++count2;
      if (b10 && unitIdx.size === 1) return true;
      if (!b10 && count2 === unitIdx.get(unit2).count) return false;
    } else {
      b10 = testPoint(datum3, entry2);
      if (intersect7 ^ b10) return b10;
    }
  }
  return n && intersect7;
}
var bisect = bisector($selectionId);
var bisectLeft2 = bisect.left;
var bisectRight2 = bisect.right;
function selectionIdTest(name, datum3, op) {
  const data4 = this.context.data[name], entries2 = data4 ? data4.values.value : [], unitIdx = data4 ? data4[UNIT_INDEX] && data4[UNIT_INDEX].value : void 0, intersect7 = op === Intersect, value4 = $selectionId(datum3), index4 = bisectLeft2(entries2, value4);
  if (index4 === entries2.length) return false;
  if ($selectionId(entries2[index4]) !== value4) return false;
  if (unitIdx && intersect7) {
    if (unitIdx.size === 1) return true;
    if (bisectRight2(entries2, value4) - index4 < unitIdx.size) return false;
  }
  return true;
}
function selectionTuples(array4, base) {
  if (!isArray(array4)) {
    error("First argument to selectionTuples must be an array.");
  }
  if (!isObject(base)) {
    error("Second argument to selectionTuples must be an object.");
  }
  return array4.map((x6) => extend(base.fields ? {
    values: base.fields.map((f) => getter2(f)(x6.datum))
  } : {
    [SelectionId]: $selectionId(x6.datum)
  }, base));
}
function selectionResolve(name, op, isMulti, vl5) {
  var data4 = this.context.data[name], entries2 = data4 ? data4.values.value : [], resolved = {}, multiRes = {}, types = {}, entry2, fields, values4, unit2, field3, value4, res, resUnit, type4, union2, n = entries2.length, i = 0, j, m3;
  for (; i < n; ++i) {
    entry2 = entries2[i];
    unit2 = entry2.unit;
    fields = entry2.fields;
    values4 = entry2.values;
    if (fields && values4) {
      for (j = 0, m3 = fields.length; j < m3; ++j) {
        field3 = fields[j];
        res = resolved[field3.field] || (resolved[field3.field] = {});
        resUnit = res[unit2] || (res[unit2] = []);
        types[field3.field] = type4 = field3.type.charAt(0);
        union2 = ops[`${type4}_union`];
        res[unit2] = union2(resUnit, array_default(values4[j]));
      }
      if (isMulti) {
        resUnit = multiRes[unit2] || (multiRes[unit2] = []);
        resUnit.push(array_default(values4).reduce((obj, curr, j2) => (obj[fields[j2].field] = curr, obj), {}));
      }
    } else {
      field3 = SelectionId;
      value4 = $selectionId(entry2);
      res = resolved[field3] || (resolved[field3] = {});
      resUnit = res[unit2] || (res[unit2] = []);
      resUnit.push(value4);
      if (isMulti) {
        resUnit = multiRes[unit2] || (multiRes[unit2] = []);
        resUnit.push({
          [SelectionId]: value4
        });
      }
    }
  }
  op = op || Union;
  if (resolved[SelectionId]) {
    resolved[SelectionId] = ops[`${SelectionId}_${op}`](...Object.values(resolved[SelectionId]));
  } else {
    Object.keys(resolved).forEach((field4) => {
      resolved[field4] = Object.keys(resolved[field4]).map((unit3) => resolved[field4][unit3]).reduce((acc, curr) => acc === void 0 ? curr : ops[`${types[field4]}_${op}`](acc, curr));
    });
  }
  entries2 = Object.keys(multiRes);
  if (isMulti && entries2.length) {
    const key2 = vl5 ? VlPoint : VlMulti;
    resolved[key2] = op === Union ? {
      [Or]: entries2.reduce((acc, k) => (acc.push(...multiRes[k]), acc), [])
    } : {
      [And]: entries2.map((k) => ({
        [Or]: multiRes[k]
      }))
    };
  }
  return resolved;
}
var ops = {
  [`${SelectionId}_union`]: union,
  [`${SelectionId}_intersect`]: intersection,
  E_union: function(base, value4) {
    if (!base.length) return value4;
    var i = 0, n = value4.length;
    for (; i < n; ++i) if (!base.includes(value4[i])) base.push(value4[i]);
    return base;
  },
  E_intersect: function(base, value4) {
    return !base.length ? value4 : base.filter((v2) => value4.includes(v2));
  },
  R_union: function(base, value4) {
    var lo = toNumber(value4[0]), hi = toNumber(value4[1]);
    if (lo > hi) {
      lo = value4[1];
      hi = value4[0];
    }
    if (!base.length) return [lo, hi];
    if (base[0] > lo) base[0] = lo;
    if (base[1] < hi) base[1] = hi;
    return base;
  },
  R_intersect: function(base, value4) {
    var lo = toNumber(value4[0]), hi = toNumber(value4[1]);
    if (lo > hi) {
      lo = value4[1];
      hi = value4[0];
    }
    if (!base.length) return [lo, hi];
    if (hi < base[0] || base[1] < lo) {
      return [];
    } else {
      if (base[0] < lo) base[0] = lo;
      if (base[1] > hi) base[1] = hi;
    }
    return base;
  }
};
var DataPrefix = ":";
var IndexPrefix = "@";
function selectionVisitor(name, args, scope2, params2) {
  if (args[0].type !== Literal) error("First argument to selection functions must be a string literal.");
  const data4 = args[0].value, op = args.length >= 2 && peek(args).value, field3 = "unit", indexName = IndexPrefix + field3, dataName = DataPrefix + data4;
  if (op === Intersect && !hasOwnProperty(params2, indexName)) {
    params2[indexName] = scope2.getData(data4).indataRef(scope2, field3);
  }
  if (!hasOwnProperty(params2, dataName)) {
    params2[dataName] = scope2.getData(data4).tuplesRef();
  }
}

// ../deepseek-harness/node_modules/.pnpm/vega-functions@6.1.3/node_modules/vega-functions/build/vega-functions.js
function data(name) {
  const data4 = this.context.data[name];
  return data4 ? data4.values.value : [];
}
function indata(name, field3, value4) {
  const index4 = this.context.data[name]["index:" + field3], entry2 = index4 ? index4.value.get(value4) : void 0;
  return entry2 ? entry2.count : entry2;
}
function setdata(name, tuples) {
  const df = this.context.dataflow, data4 = this.context.data[name], input = data4.input;
  df.pulse(input, df.changeset().remove(truthy).insert(tuples));
  return 1;
}
function encode(item, name, retval) {
  if (item) {
    const df = this.context.dataflow, target2 = item.mark.source;
    df.pulse(target2, df.changeset().encode(item, name));
  }
  return retval !== void 0 ? retval : item;
}
var wrap = (method2) => function(value4, spec) {
  const locale4 = this.context.dataflow.locale();
  return value4 === null ? "null" : locale4[method2](spec)(value4);
};
var format4 = wrap("format");
var timeFormat2 = wrap("timeFormat");
var utcFormat2 = wrap("utcFormat");
var timeParse2 = wrap("timeParse");
var utcParse2 = wrap("utcParse");
var dateObj = new Date(2e3, 0, 1);
function time2(month, day, specifier) {
  if (!Number.isInteger(month) || !Number.isInteger(day)) return "";
  dateObj.setYear(2e3);
  dateObj.setMonth(month);
  dateObj.setDate(day);
  return timeFormat2.call(this, dateObj, specifier);
}
function monthFormat(month) {
  return time2.call(this, month, 1, "%B");
}
function monthAbbrevFormat(month) {
  return time2.call(this, month, 1, "%b");
}
function dayFormat(day) {
  return time2.call(this, 0, 2 + day, "%A");
}
function dayAbbrevFormat(day) {
  return time2.call(this, 0, 2 + day, "%a");
}
var DataPrefix2 = ":";
var IndexPrefix2 = "@";
var ScalePrefix = "%";
var SignalPrefix = "$";
function dataVisitor(name, args, scope2, params2) {
  if (args[0].type !== Literal) {
    error("First argument to data functions must be a string literal.");
  }
  const data4 = args[0].value, dataName = DataPrefix2 + data4;
  if (!hasOwnProperty(dataName, params2)) {
    try {
      params2[dataName] = scope2.getData(data4).tuplesRef();
    } catch (err) {
    }
  }
}
function indataVisitor(name, args, scope2, params2) {
  if (args[0].type !== Literal) error("First argument to indata must be a string literal.");
  if (args[1].type !== Literal) error("Second argument to indata must be a string literal.");
  const data4 = args[0].value, field3 = args[1].value, indexName = IndexPrefix2 + field3;
  if (!hasOwnProperty(indexName, params2)) {
    params2[indexName] = scope2.getData(data4).indataRef(scope2, field3);
  }
}
function scaleVisitor(name, args, scope2, params2) {
  if (args[0].type === Literal) {
    addScaleDependency(scope2, params2, args[0].value);
  } else {
    for (name in scope2.scales) {
      addScaleDependency(scope2, params2, name);
    }
  }
}
function addScaleDependency(scope2, params2, name) {
  const scaleName = ScalePrefix + name;
  if (!hasOwnProperty(params2, scaleName)) {
    try {
      params2[scaleName] = scope2.scaleRef(name);
    } catch (err) {
    }
  }
}
function getScale(nameOrFunction, ctx) {
  if (isString(nameOrFunction)) {
    const maybeScale = ctx.scales[nameOrFunction];
    return maybeScale && isRegisteredScale(maybeScale.value) ? maybeScale.value : void 0;
  } else if (isFunction(nameOrFunction)) {
    return isRegisteredScale(nameOrFunction) ? nameOrFunction : void 0;
  }
  return void 0;
}
function internalScaleFunctions(codegen2, fnctx, visitors) {
  fnctx.__bandwidth = (s) => s && s.bandwidth ? s.bandwidth() : 0;
  visitors._bandwidth = scaleVisitor;
  visitors._range = scaleVisitor;
  visitors._scale = scaleVisitor;
  const ref2 = (arg) => "_[" + (arg.type === Literal ? stringValue(ScalePrefix + arg.value) : stringValue(ScalePrefix) + "+" + codegen2(arg)) + "]";
  return {
    _bandwidth: (args) => `this.__bandwidth(${ref2(args[0])})`,
    _range: (args) => `${ref2(args[0])}.range()`,
    _scale: (args) => `${ref2(args[0])}(${codegen2(args[1])})`
  };
}
function geoMethod(methodName, globalMethod) {
  return function(projection3, geojson, group3) {
    if (projection3) {
      const p = getScale(projection3, (group3 || this).context);
      return p && p.path[methodName](geojson);
    } else {
      return globalMethod(geojson);
    }
  };
}
var geoArea = geoMethod("area", area_default2);
var geoBounds = geoMethod("bounds", bounds_default);
var geoCentroid = geoMethod("centroid", centroid_default);
function geoScale(projection3, group3) {
  const p = getScale(projection3, (group3 || this).context);
  return p && p.scale();
}
function inScope(item) {
  const group3 = this.context.group;
  let value4 = false;
  if (group3) while (item) {
    if (item === group3) {
      value4 = true;
      break;
    }
    item = item.mark.group;
  }
  return value4;
}
function log6(df, method2, args) {
  try {
    df[method2].apply(df, ["EXPRESSION"].concat([].slice.call(args)));
  } catch (err) {
    df.warn(err);
  }
  return args[args.length - 1];
}
function warn() {
  return log6(this.context.dataflow, "warn", arguments);
}
function info() {
  return log6(this.context.dataflow, "info", arguments);
}
function debug() {
  return log6(this.context.dataflow, "debug", arguments);
}
function channel_luminance_value(channelValue) {
  const val = channelValue / 255;
  if (val <= 0.03928) {
    return val / 12.92;
  }
  return Math.pow((val + 0.055) / 1.055, 2.4);
}
function luminance(color6) {
  const c3 = rgb(color6), r = channel_luminance_value(c3.r), g = channel_luminance_value(c3.g), b10 = channel_luminance_value(c3.b);
  return 0.2126 * r + 0.7152 * g + 0.0722 * b10;
}
function contrast(color1, color22) {
  const lum1 = luminance(color1), lum2 = luminance(color22), lumL = Math.max(lum1, lum2), lumD = Math.min(lum1, lum2);
  return (lumL + 0.05) / (lumD + 0.05);
}
function merge4() {
  const args = [].slice.call(arguments);
  args.unshift({});
  return extend(...args);
}
function equal(a3, b10) {
  return a3 === b10 || a3 !== a3 && b10 !== b10 ? true : isArray(a3) ? isArray(b10) && a3.length === b10.length ? equalArray(a3, b10) : false : isObject(a3) && isObject(b10) ? equalObject(a3, b10) : false;
}
function equalArray(a3, b10) {
  for (let i = 0, n = a3.length; i < n; ++i) {
    if (!equal(a3[i], b10[i])) return false;
  }
  return true;
}
function equalObject(a3, b10) {
  for (const key2 in a3) {
    if (!equal(a3[key2], b10[key2])) return false;
  }
  return true;
}
function removePredicate(props) {
  return (_) => equalObject(props, _);
}
function modify(name, insert2, remove2, toggle2, modify3, values4) {
  const df = this.context.dataflow, data4 = this.context.data[name], input = data4.input, stamp = df.stamp();
  let changes = data4.changes, predicate, key2;
  if (df._trigger === false || !(input.value.length || insert2 || toggle2)) {
    return 0;
  }
  if (!changes || changes.stamp < stamp) {
    data4.changes = changes = df.changeset();
    changes.stamp = stamp;
    df.runAfter(() => {
      data4.modified = true;
      df.pulse(input, changes).run();
    }, true, 1);
  }
  if (remove2) {
    predicate = remove2 === true ? truthy : isArray(remove2) || isTuple(remove2) ? remove2 : removePredicate(remove2);
    changes.remove(predicate);
  }
  if (insert2) {
    changes.insert(insert2);
  }
  if (toggle2) {
    predicate = removePredicate(toggle2);
    if (input.value.some(predicate)) {
      changes.remove(predicate);
    } else {
      changes.insert(toggle2);
    }
  }
  if (modify3) {
    if (isFunction(modify3)) {
      throw Error("modify parameter must be a data tuple, not a function");
    }
    for (key2 in values4) {
      changes.modify(modify3, key2, values4[key2]);
    }
  }
  return 1;
}
function pinchDistance(event2) {
  const t = event2.touches, dx = t[0].clientX - t[1].clientX, dy = t[0].clientY - t[1].clientY;
  return Math.hypot(dx, dy);
}
function pinchAngle(event2) {
  const t = event2.touches;
  return Math.atan2(t[0].clientY - t[1].clientY, t[0].clientX - t[1].clientX);
}
var accessors = {};
function pluck(data4, name) {
  const accessor2 = accessors[name] || (accessors[name] = field(name));
  return isArray(data4) ? data4.map(accessor2) : accessor2(data4);
}
function array2(seq) {
  return isArray(seq) || ArrayBuffer.isView(seq) ? seq : null;
}
function sequence3(seq) {
  return array2(seq) || (isString(seq) ? seq : null);
}
function join3(seq, ...args) {
  return array2(seq).join(...args);
}
function indexof(seq, ...args) {
  return sequence3(seq).indexOf(...args);
}
function lastindexof(seq, ...args) {
  return sequence3(seq).lastIndexOf(...args);
}
function slice3(seq, ...args) {
  return sequence3(seq).slice(...args);
}
function replace2(str, pattern, repl) {
  if (isFunction(repl)) error("Function argument passed to replace.");
  if (!isString(pattern) && !isRegExp(pattern)) error("Please pass a string or RegExp argument to replace.");
  return String(str).replace(pattern, repl);
}
function reverse(seq) {
  return array2(seq).slice().reverse();
}
function sort2(seq) {
  return array2(seq).slice().sort(ascending);
}
function bandspace(count2, paddingInner2, paddingOuter2) {
  return bandSpace(count2 || 0, paddingInner2 || 0, paddingOuter2 || 0);
}
function bandwidth(name, group3) {
  const s = getScale(name, (group3 || this).context);
  return s && s.bandwidth ? s.bandwidth() : 0;
}
function copy3(name, group3) {
  const s = getScale(name, (group3 || this).context);
  return s ? s.copy() : void 0;
}
function domain(name, group3) {
  const s = getScale(name, (group3 || this).context);
  return s ? s.domain() : [];
}
function invert(name, range7, group3) {
  const s = getScale(name, (group3 || this).context);
  return !s ? void 0 : isArray(range7) ? (s.invertRange || s.invert)(range7) : (s.invert || s.invertExtent)(range7);
}
function range3(name, group3) {
  const s = getScale(name, (group3 || this).context);
  return s && s.range ? s.range() : [];
}
function scale5(name, value4, group3) {
  const s = getScale(name, (group3 || this).context);
  return s ? s(value4) : void 0;
}
function scaleGradient(scale9, p02, p1, count2, group3) {
  scale9 = getScale(scale9, (group3 || this).context);
  const gradient5 = Gradient2(p02, p1);
  let stops = scale9.domain(), min4 = stops[0], max4 = peek(stops), fraction = identity;
  if (!(max4 - min4)) {
    scale9 = (scale9.interpolator ? scale("sequential")().interpolator(scale9.interpolator()) : scale("linear")().interpolate(scale9.interpolate()).range(scale9.range())).domain([min4 = 0, max4 = 1]);
  } else {
    fraction = scaleFraction(scale9, min4, max4);
  }
  if (scale9.ticks) {
    stops = scale9.ticks(+count2 || 15);
    if (min4 !== stops[0]) stops.unshift(min4);
    if (max4 !== peek(stops)) stops.push(max4);
  }
  stops.forEach((_) => gradient5.stop(fraction(_), scale9(_)));
  return gradient5;
}
function geoShape(projection3, geojson, group3) {
  const p = getScale(projection3, (group3 || this).context);
  return function(context4) {
    return p ? p.path.context(context4)(geojson) : "";
  };
}
function pathShape(path4) {
  let p = null;
  return function(context4) {
    return context4 ? pathRender2(context4, p = p || parse5(path4)) : path4;
  };
}
var datum = (d) => d.data;
function treeNodes(name, context4) {
  const tree = data.call(context4, name);
  return tree.root && tree.root.lookup || {};
}
function treePath(name, source4, target2) {
  const nodes = treeNodes(name, this), s = nodes[source4], t = nodes[target2];
  return s && t ? s.path(t).map(datum) : void 0;
}
function treeAncestors(name, node) {
  const n = treeNodes(name, this)[node];
  return n ? n.ancestors().map(datum) : void 0;
}
var _window = () => typeof window !== "undefined" && window || null;
function screen() {
  const w3 = _window();
  return w3 ? w3.screen : {};
}
function windowSize() {
  const w3 = _window();
  return w3 ? [w3.innerWidth, w3.innerHeight] : [void 0, void 0];
}
function containerSize() {
  const view = this.context.dataflow, el = view.container && view.container();
  return el ? [el.clientWidth, el.clientHeight] : [void 0, void 0];
}
function intersect5(b10, opt, group3) {
  if (!b10) return [];
  const [u4, v2] = b10, box = new Bounds3().set(u4[0], u4[1], v2[0], v2[1]), scene = group3 || this.context.dataflow.scenegraph().root;
  return intersect4(scene, box, filter2(opt));
}
function filter2(opt) {
  let p = null;
  if (opt) {
    const types = array_default(opt.marktype), names = array_default(opt.markname);
    p = (_) => (!types.length || types.some((t) => _.marktype === t)) && (!names.length || names.some((s) => _.name === s));
  }
  return p;
}
function lassoAppend(lasso, x6, y6, minDist = 5) {
  lasso = array_default(lasso);
  const last = lasso[lasso.length - 1];
  return last === void 0 || Math.hypot(last[0] - x6, last[1] - y6) > minDist ? [...lasso, [x6, y6]] : lasso;
}
function lassoPath(lasso) {
  return array_default(lasso).reduce((svg, [x6, y6], i) => {
    return svg += i == 0 ? `M ${x6},${y6} ` : i === lasso.length - 1 ? " Z" : `L ${x6},${y6} `;
  }, "");
}
function intersectLasso(markname, pixelLasso, unit2) {
  const {
    x: x6,
    y: y6,
    mark
  } = unit2;
  const bb2 = new Bounds3().set(Number.MAX_SAFE_INTEGER, Number.MAX_SAFE_INTEGER, Number.MIN_SAFE_INTEGER, Number.MIN_SAFE_INTEGER);
  for (const [px3, py3] of pixelLasso) {
    if (px3 < bb2.x1) bb2.x1 = px3;
    if (px3 > bb2.x2) bb2.x2 = px3;
    if (py3 < bb2.y1) bb2.y1 = py3;
    if (py3 > bb2.y2) bb2.y2 = py3;
  }
  bb2.translate(x6, y6);
  const intersection2 = intersect5([[bb2.x1, bb2.y1], [bb2.x2, bb2.y2]], markname, mark);
  return intersection2.filter((tuple) => pointInPolygon(tuple.x, tuple.y, pixelLasso));
}
function pointInPolygon(testx, testy, polygon) {
  let intersections = 0;
  for (let i = 0, j = polygon.length - 1; i < polygon.length; j = i++) {
    const [prevX, prevY] = polygon[j];
    const [x6, y6] = polygon[i];
    if (y6 > testy != prevY > testy && testx < (prevX - x6) * (testy - y6) / (prevY - y6) + x6) {
      intersections++;
    }
  }
  return intersections & 1;
}
var functionContext = {
  random() {
    return random();
  },
  // override default
  cumulativeNormal,
  cumulativeLogNormal,
  cumulativeUniform,
  densityNormal,
  densityLogNormal,
  densityUniform,
  quantileNormal,
  quantileLogNormal,
  quantileUniform,
  sampleNormal,
  sampleLogNormal,
  sampleUniform,
  isArray,
  isBoolean,
  isDate,
  isDefined(_) {
    return _ !== void 0;
  },
  isNumber,
  isObject,
  isRegExp,
  isString,
  isTuple,
  isValid(_) {
    return _ != null && _ === _;
  },
  toBoolean,
  toDate(_) {
    return toDate(_);
  },
  // suppress extra arguments
  toNumber,
  toString,
  indexof,
  join: join3,
  lastindexof,
  replace: replace2,
  reverse,
  sort: sort2,
  slice: slice3,
  flush: flush_default,
  lerp: lerp_default,
  merge: merge4,
  pad,
  peek,
  pluck,
  span: span_default,
  inrange,
  truncate,
  rgb,
  lab,
  hcl,
  hsl,
  luminance,
  contrast,
  sequence: range,
  format: format4,
  utcFormat: utcFormat2,
  utcParse: utcParse2,
  utcOffset: utcOffset2,
  utcSequence: utcSequence2,
  timeFormat: timeFormat2,
  timeParse: timeParse2,
  timeOffset: timeOffset2,
  timeSequence: timeSequence2,
  timeUnitSpecifier: timeUnitSpecifier2,
  monthFormat,
  monthAbbrevFormat,
  dayFormat,
  dayAbbrevFormat,
  quarter,
  utcquarter,
  week: week2,
  utcweek: utcweek2,
  dayofyear: dayofyear2,
  utcdayofyear: utcdayofyear2,
  warn,
  info,
  debug,
  extent(_) {
    return extent(_);
  },
  // suppress extra arguments
  inScope,
  intersect: intersect5,
  clampRange,
  pinchDistance,
  pinchAngle,
  screen,
  containerSize,
  windowSize,
  bandspace,
  setdata,
  pathShape,
  panLinear,
  panLog,
  panPow,
  panSymlog,
  zoomLinear,
  zoomLog,
  zoomPow,
  zoomSymlog,
  encode,
  modify,
  lassoAppend,
  lassoPath,
  intersectLasso
};
var eventFunctions = ["view", "item", "group", "xy", "x", "y"];
var eventPrefix = "event.vega.";
var thisPrefix = "this.";
var astVisitors = {};
var codegenParams = {
  forbidden: ["_"],
  allowed: ["datum", "event", "item"],
  fieldvar: "datum",
  globalvar: (id2) => `_[${stringValue(SignalPrefix + id2)}]`,
  functions: buildFunctions,
  constants: Constants,
  visitors: astVisitors
};
var codeGenerator = codegen(codegenParams);
function buildFunctions(codegen2) {
  const fn = Functions(codegen2);
  eventFunctions.forEach((name) => fn[name] = eventPrefix + name);
  for (const name in functionContext) {
    fn[name] = thisPrefix + name;
  }
  extend(fn, internalScaleFunctions(codegen2, functionContext, astVisitors));
  return fn;
}
function expressionFunction(name, fn, visitor) {
  if (arguments.length === 1) {
    return functionContext[name];
  }
  functionContext[name] = fn;
  if (visitor) astVisitors[name] = visitor;
  if (codeGenerator) codeGenerator.functions[name] = thisPrefix + name;
  return this;
}
expressionFunction("bandwidth", bandwidth, scaleVisitor);
expressionFunction("copy", copy3, scaleVisitor);
expressionFunction("domain", domain, scaleVisitor);
expressionFunction("range", range3, scaleVisitor);
expressionFunction("invert", invert, scaleVisitor);
expressionFunction("scale", scale5, scaleVisitor);
expressionFunction("gradient", scaleGradient, scaleVisitor);
expressionFunction("geoArea", geoArea, scaleVisitor);
expressionFunction("geoBounds", geoBounds, scaleVisitor);
expressionFunction("geoCentroid", geoCentroid, scaleVisitor);
expressionFunction("geoShape", geoShape, scaleVisitor);
expressionFunction("geoScale", geoScale, scaleVisitor);
expressionFunction("indata", indata, indataVisitor);
expressionFunction("data", data, dataVisitor);
expressionFunction("treePath", treePath, dataVisitor);
expressionFunction("treeAncestors", treeAncestors, dataVisitor);
expressionFunction("vlSelectionTest", selectionTest, selectionVisitor);
expressionFunction("vlSelectionIdTest", selectionIdTest, selectionVisitor);
expressionFunction("vlSelectionResolve", selectionResolve, selectionVisitor);
expressionFunction("vlSelectionTuples", selectionTuples);

// ../deepseek-harness/node_modules/.pnpm/vega-runtime@7.1.3/node_modules/vega-runtime/build/vega-runtime.js
function parse6(spec) {
  const ctx = this, operators = spec.operators || [];
  if (spec.background) {
    ctx.background = spec.background;
  }
  if (spec.eventConfig) {
    ctx.eventConfig = spec.eventConfig;
  }
  if (spec.locale) {
    ctx.locale = spec.locale;
  }
  operators.forEach((entry2) => ctx.parseOperator(entry2));
  operators.forEach((entry2) => ctx.parseOperatorParameters(entry2));
  (spec.streams || []).forEach((entry2) => ctx.parseStream(entry2));
  (spec.updates || []).forEach((entry2) => ctx.parseUpdate(entry2));
  return ctx.resolve();
}
var Skip = toSet(["rule"]);
var Swap = toSet(["group", "image", "rect"]);
function adjustSpatial(encode3, marktype) {
  let code = "";
  if (Skip[marktype]) return code;
  if (encode3.x2) {
    if (encode3.x) {
      if (Swap[marktype]) {
        code += "if(o.x>o.x2)$=o.x,o.x=o.x2,o.x2=$;";
      }
      code += "o.width=o.x2-o.x;";
    } else {
      code += "o.x=o.x2-(o.width||0);";
    }
  }
  if (encode3.xc) {
    code += "o.x=o.xc-(o.width||0)/2;";
  }
  if (encode3.y2) {
    if (encode3.y) {
      if (Swap[marktype]) {
        code += "if(o.y>o.y2)$=o.y,o.y=o.y2,o.y2=$;";
      }
      code += "o.height=o.y2-o.y;";
    } else {
      code += "o.y=o.y2-(o.height||0);";
    }
  }
  if (encode3.yc) {
    code += "o.y=o.yc-(o.height||0)/2;";
  }
  return code;
}
function canonicalType(type4) {
  return (type4 + "").toLowerCase();
}
function isOperator(type4) {
  return canonicalType(type4) === "operator";
}
function isCollect(type4) {
  return canonicalType(type4) === "collect";
}
function expression(ctx, args, code) {
  if (!code.endsWith(";")) {
    code = "return(" + code + ");";
  }
  const fn = Function(...args.concat(code));
  return ctx && ctx.functions ? fn.bind(ctx.functions) : fn;
}
function _compare(u4, v2, lt, gt) {
  return `((u = ${u4}) < (v = ${v2}) || u == null) && v != null ? ${lt}
  : (u > v || v == null) && u != null ? ${gt}
  : ((v = v instanceof Date ? +v : v), (u = u instanceof Date ? +u : u)) !== u && v === v ? ${lt}
  : v !== v && u === u ? ${gt} : `;
}
var expressionCodegen = {
  /**
   * Parse an expression used to update an operator value.
   */
  operator: (ctx, expr2) => expression(ctx, ["_"], expr2.code),
  /**
   * Parse an expression provided as an operator parameter value.
   */
  parameter: (ctx, expr2) => expression(ctx, ["datum", "_"], expr2.code),
  /**
   * Parse an expression applied to an event stream.
   */
  event: (ctx, expr2) => expression(ctx, ["event"], expr2.code),
  /**
   * Parse an expression used to handle an event-driven operator update.
   */
  handler: (ctx, expr2) => {
    const code = `var datum=event.item&&event.item.datum;return ${expr2.code};`;
    return expression(ctx, ["_", "event"], code);
  },
  /**
   * Parse an expression that performs visual encoding.
   */
  encode: (ctx, encode3) => {
    const {
      marktype,
      channels
    } = encode3;
    let code = "var o=item,datum=o.datum,m=0,$;";
    for (const name in channels) {
      const o = "o[" + stringValue(name) + "]";
      code += `$=${channels[name].code};if(${o}!==$)${o}=$,m=1;`;
    }
    code += adjustSpatial(channels, marktype);
    code += "return m;";
    return expression(ctx, ["item", "_"], code);
  },
  /**
   * Optimized code generators for access and comparison.
   */
  codegen: {
    get(path4) {
      const ref2 = `[${path4.map(stringValue).join("][")}]`;
      const get7 = Function("_", `return _${ref2};`);
      get7.path = ref2;
      return get7;
    },
    comparator(fields, orders) {
      let t;
      const map3 = (f, i) => {
        const o = orders[i];
        let u4, v2;
        if (f.path) {
          u4 = `a${f.path}`;
          v2 = `b${f.path}`;
        } else {
          (t = t || {})["f" + i] = f;
          u4 = `this.f${i}(a)`;
          v2 = `this.f${i}(b)`;
        }
        return _compare(u4, v2, -o, o);
      };
      const fn = Function("a", "b", "var u, v; return " + fields.map(map3).join("") + "0;");
      return t ? fn.bind(t) : fn;
    }
  }
};
function parseOperator(spec) {
  const ctx = this;
  if (isOperator(spec.type) || !spec.type) {
    ctx.operator(spec, spec.update ? ctx.operatorExpression(spec.update) : null);
  } else {
    ctx.transform(spec, spec.type);
  }
}
function parseOperatorParameters(spec) {
  const ctx = this;
  if (spec.params) {
    const op = ctx.get(spec.id);
    if (!op) error("Invalid operator id: " + spec.id);
    ctx.dataflow.connect(op, op.parameters(ctx.parseParameters(spec.params), spec.react, spec.initonly));
  }
}
function parseParameters(spec, params2) {
  params2 = params2 || {};
  const ctx = this;
  for (const key2 in spec) {
    const value4 = spec[key2];
    params2[key2] = isArray(value4) ? value4.map((v2) => parseParameter(v2, ctx, params2)) : parseParameter(value4, ctx, params2);
  }
  return params2;
}
function parseParameter(spec, ctx, params2) {
  if (!spec || !isObject(spec)) return spec;
  for (let i = 0, n = PARSERS.length, p; i < n; ++i) {
    p = PARSERS[i];
    if (hasOwnProperty(spec, p.key)) {
      return p.parse(spec, ctx, params2);
    }
  }
  return spec;
}
var PARSERS = [{
  key: "$ref",
  parse: getOperator
}, {
  key: "$key",
  parse: getKey
}, {
  key: "$expr",
  parse: getExpression
}, {
  key: "$field",
  parse: getField2
}, {
  key: "$encode",
  parse: getEncode
}, {
  key: "$compare",
  parse: getCompare
}, {
  key: "$context",
  parse: getContext
}, {
  key: "$subflow",
  parse: getSubflow
}, {
  key: "$tupleid",
  parse: getTupleId
}];
function getOperator(_, ctx) {
  return ctx.get(_.$ref) || error("Operator not defined: " + _.$ref);
}
function getExpression(_, ctx, params2) {
  if (_.$params) {
    ctx.parseParameters(_.$params, params2);
  }
  const k = "e:" + _.$expr.code;
  return ctx.fn[k] || (ctx.fn[k] = accessor(ctx.parameterExpression(_.$expr), _.$fields));
}
function getKey(_, ctx) {
  const k = "k:" + _.$key + "_" + !!_.$flat;
  return ctx.fn[k] || (ctx.fn[k] = key(_.$key, _.$flat, ctx.expr.codegen));
}
function getField2(_, ctx) {
  if (!_.$field) return null;
  const k = "f:" + _.$field + "_" + _.$name;
  return ctx.fn[k] || (ctx.fn[k] = field(_.$field, _.$name, ctx.expr.codegen));
}
function getCompare(_, ctx) {
  const k = "c:" + _.$compare + "_" + _.$order, c3 = array_default(_.$compare).map((_2) => _2 && _2.$tupleid ? tupleid : _2);
  return ctx.fn[k] || (ctx.fn[k] = compare(c3, _.$order, ctx.expr.codegen));
}
function getEncode(_, ctx) {
  const spec = _.$encode, encode3 = {};
  for (const name in spec) {
    const enc = spec[name];
    encode3[name] = accessor(ctx.encodeExpression(enc.$expr), enc.$fields);
    encode3[name].output = enc.$output;
  }
  return encode3;
}
function getContext(_, ctx) {
  return ctx;
}
function getSubflow(_, ctx) {
  const spec = _.$subflow;
  return function(dataflow, key2, parent) {
    const subctx = ctx.fork().parse(spec), op = subctx.get(spec.operators[0].id), p = subctx.signals.parent;
    if (p) p.set(parent);
    op.detachSubflow = () => ctx.detach(subctx);
    return op;
  };
}
function getTupleId() {
  return tupleid;
}
function parseStream(spec) {
  var ctx = this, filter4 = spec.filter != null ? ctx.eventExpression(spec.filter) : void 0, stream2 = spec.stream != null ? ctx.get(spec.stream) : void 0, args;
  if (spec.source) {
    stream2 = ctx.events(spec.source, spec.type, filter4);
  } else if (spec.merge) {
    args = spec.merge.map((_) => ctx.get(_));
    stream2 = args[0].merge.apply(args[0], args.slice(1));
  }
  if (spec.between) {
    args = spec.between.map((_) => ctx.get(_));
    stream2 = stream2.between(args[0], args[1]);
  }
  if (spec.filter) {
    stream2 = stream2.filter(filter4);
  }
  if (spec.throttle != null) {
    stream2 = stream2.throttle(+spec.throttle);
  }
  if (spec.debounce != null) {
    stream2 = stream2.debounce(+spec.debounce);
  }
  if (stream2 == null) {
    error("Invalid stream definition: " + JSON.stringify(spec));
  }
  if (spec.consume) stream2.consume(true);
  ctx.stream(spec, stream2);
}
function parseUpdate(spec) {
  var ctx = this, srcid = isObject(srcid = spec.source) ? srcid.$ref : srcid, source4 = ctx.get(srcid), target2 = null, update3 = spec.update, params2 = void 0;
  if (!source4) error("Source not defined: " + spec.source);
  target2 = spec.target && spec.target.$expr ? ctx.eventExpression(spec.target.$expr) : ctx.get(spec.target);
  if (update3 && update3.$expr) {
    if (update3.$params) {
      params2 = ctx.parseParameters(update3.$params);
    }
    update3 = ctx.handlerExpression(update3.$expr);
  }
  ctx.update(spec, source4, target2, update3, params2);
}
var SKIP3 = {
  skip: true
};
function getState(options) {
  var ctx = this, state = {};
  if (options.signals) {
    var signals = state.signals = {};
    Object.keys(ctx.signals).forEach((key2) => {
      const op = ctx.signals[key2];
      if (options.signals(key2, op)) {
        signals[key2] = op.value;
      }
    });
  }
  if (options.data) {
    var data4 = state.data = {};
    Object.keys(ctx.data).forEach((key2) => {
      const dataset = ctx.data[key2];
      if (options.data(key2, dataset)) {
        data4[key2] = dataset.input.value;
      }
    });
  }
  if (ctx.subcontext && options.recurse !== false) {
    state.subcontext = ctx.subcontext.map((ctx2) => ctx2.getState(options));
  }
  return state;
}
function setState(state) {
  var ctx = this, df = ctx.dataflow, data4 = state.data, signals = state.signals;
  Object.keys(signals || {}).forEach((key2) => {
    df.update(ctx.signals[key2], signals[key2], SKIP3);
  });
  Object.keys(data4 || {}).forEach((key2) => {
    df.pulse(ctx.data[key2].input, df.changeset().remove(truthy).insert(data4[key2]));
  });
  (state.subcontext || []).forEach((substate, i) => {
    const subctx = ctx.subcontext[i];
    if (subctx) subctx.setState(substate);
  });
}
function context3(df, transforms2, functions, expr2) {
  return new Context(df, transforms2, functions, expr2);
}
function Context(df, transforms2, functions, expr2) {
  this.dataflow = df;
  this.transforms = transforms2;
  this.events = df.events.bind(df);
  this.expr = expr2 || expressionCodegen, this.signals = {};
  this.scales = {};
  this.nodes = {};
  this.data = {};
  this.fn = {};
  if (functions) {
    this.functions = Object.create(functions);
    this.functions.context = this;
  }
}
function Subcontext(ctx) {
  this.dataflow = ctx.dataflow;
  this.transforms = ctx.transforms;
  this.events = ctx.events;
  this.expr = ctx.expr;
  this.signals = Object.create(ctx.signals);
  this.scales = Object.create(ctx.scales);
  this.nodes = Object.create(ctx.nodes);
  this.data = Object.create(ctx.data);
  this.fn = Object.create(ctx.fn);
  if (ctx.functions) {
    this.functions = Object.create(ctx.functions);
    this.functions.context = this;
  }
}
Context.prototype = Subcontext.prototype = {
  fork() {
    const ctx = new Subcontext(this);
    (this.subcontext || (this.subcontext = [])).push(ctx);
    return ctx;
  },
  detach(ctx) {
    this.subcontext = this.subcontext.filter((c3) => c3 !== ctx);
    const keys4 = Object.keys(ctx.nodes);
    for (const key2 of keys4) ctx.nodes[key2]._targets = null;
    for (const key2 of keys4) ctx.nodes[key2].detach();
    ctx.nodes = null;
  },
  get(id2) {
    return this.nodes[id2];
  },
  set(id2, node) {
    return this.nodes[id2] = node;
  },
  add(spec, op) {
    const ctx = this, df = ctx.dataflow, data4 = spec.value;
    ctx.set(spec.id, op);
    if (isCollect(spec.type) && data4) {
      if (data4.$ingest) {
        df.ingest(op, data4.$ingest, data4.$format);
      } else if (data4.$request) {
        df.preload(op, data4.$request, data4.$format);
      } else {
        df.pulse(op, df.changeset().insert(data4));
      }
    }
    if (spec.root) {
      ctx.root = op;
    }
    if (spec.parent) {
      let p = ctx.get(spec.parent.$ref);
      if (p) {
        df.connect(p, [op]);
        op.targets().add(p);
      } else {
        (ctx.unresolved = ctx.unresolved || []).push(() => {
          p = ctx.get(spec.parent.$ref);
          df.connect(p, [op]);
          op.targets().add(p);
        });
      }
    }
    if (spec.signal) {
      ctx.signals[spec.signal] = op;
    }
    if (spec.scale) {
      ctx.scales[spec.scale] = op;
    }
    if (spec.data) {
      for (const name in spec.data) {
        const data5 = ctx.data[name] || (ctx.data[name] = {});
        spec.data[name].forEach((role) => data5[role] = op);
      }
    }
  },
  resolve() {
    (this.unresolved || []).forEach((fn) => fn());
    delete this.unresolved;
    return this;
  },
  operator(spec, update3) {
    this.add(spec, this.dataflow.add(spec.value, update3));
  },
  transform(spec, type4) {
    this.add(spec, this.dataflow.add(this.transforms[canonicalType(type4)]));
  },
  stream(spec, stream2) {
    this.set(spec.id, stream2);
  },
  update(spec, stream2, target2, update3, params2) {
    this.dataflow.on(stream2, target2, update3, params2, spec.options);
  },
  // expression parsing
  operatorExpression(expr2) {
    return this.expr.operator(this, expr2);
  },
  parameterExpression(expr2) {
    return this.expr.parameter(this, expr2);
  },
  eventExpression(expr2) {
    return this.expr.event(this, expr2);
  },
  handlerExpression(expr2) {
    return this.expr.handler(this, expr2);
  },
  encodeExpression(encode3) {
    return this.expr.encode(this, encode3);
  },
  // parse methods
  parse: parse6,
  parseOperator,
  parseOperatorParameters,
  parseParameters,
  parseStream,
  parseUpdate,
  // state methods
  getState,
  setState
};

// ../deepseek-harness/node_modules/.pnpm/vega-view@6.1.2/node_modules/vega-view/build/vega-view.js
function initializeAria(view) {
  const el = view.container();
  if (el) {
    el.setAttribute("role", "graphics-document");
    el.setAttribute("aria-roleDescription", "visualization");
    ariaLabel(el, view.description());
  }
}
function ariaLabel(el, desc) {
  if (el) desc == null ? el.removeAttribute("aria-label") : el.setAttribute("aria-label", desc);
}
function background3(view) {
  view.add(null, (_) => {
    view._background = _.bg;
    view._resize = 1;
    return _.bg;
  }, {
    bg: view._signals.background
  });
}
var Default = "default";
function cursor(view) {
  const cursor3 = view._signals.cursor || (view._signals.cursor = view.add({
    user: Default,
    item: null
  }));
  view.on(view.events("view", "pointermove"), cursor3, (_, event2) => {
    const value4 = cursor3.value, user = value4 ? isString(value4) ? value4 : value4.user : Default, item = event2.item && event2.item.cursor || null;
    return value4 && user === value4.user && item == value4.item ? value4 : {
      user,
      item
    };
  });
  view.add(null, function(_) {
    let user = _.cursor, item = this.value;
    if (!isString(user)) {
      item = user.item;
      user = user.user;
    }
    setCursor(view, user && user !== Default ? user : item || user);
    return item;
  }, {
    cursor: cursor3
  });
}
function setCursor(view, cursor3) {
  const el = view.globalCursor() ? typeof document !== "undefined" && document.body : view.container();
  if (el) {
    return cursor3 == null ? el.style.removeProperty("cursor") : el.style.cursor = cursor3;
  }
}
function dataref(view, name) {
  var data4 = view._runtime.data;
  if (!hasOwnProperty(data4, name)) {
    error("Unrecognized data set: " + name);
  }
  return data4[name];
}
function data2(name, values4) {
  return arguments.length < 2 ? dataref(this, name).values.value : change.call(this, name, changeset().remove(truthy).insert(values4));
}
function change(name, changes) {
  if (!isChangeSet(changes)) {
    error("Second argument to changes must be a changeset.");
  }
  const dataset = dataref(this, name);
  dataset.modified = true;
  return this.pulse(dataset.input, changes);
}
function insert(name, _) {
  return change.call(this, name, changeset().insert(_));
}
function remove(name, _) {
  return change.call(this, name, changeset().remove(_));
}
function width(view) {
  var padding3 = view.padding();
  return Math.max(0, view._viewWidth + padding3.left + padding3.right);
}
function height(view) {
  var padding3 = view.padding();
  return Math.max(0, view._viewHeight + padding3.top + padding3.bottom);
}
function offset5(view) {
  var padding3 = view.padding(), origin = view._origin;
  return [padding3.left + origin[0], padding3.top + origin[1]];
}
function resizeRenderer(view) {
  var origin = offset5(view), w3 = width(view), h3 = height(view);
  view._renderer.background(view.background());
  view._renderer.resize(w3, h3, origin);
  view._handler.origin(origin);
  view._resizeListeners.forEach((handler) => {
    try {
      handler(w3, h3);
    } catch (error3) {
      view.error(error3);
    }
  });
}
function eventExtend(view, event2, item) {
  var r = view._renderer, el = r && r.canvas(), p, e, translate5;
  if (el) {
    translate5 = offset5(view);
    e = event2.changedTouches ? event2.changedTouches[0] : event2;
    p = point7(e, el);
    p[0] -= translate5[0];
    p[1] -= translate5[1];
  }
  event2.dataflow = view;
  event2.item = item;
  event2.vega = extension(view, item, p);
  return event2;
}
function extension(view, item, point9) {
  const itemGroup = item ? item.mark.marktype === "group" ? item : item.mark.group : null;
  function group3(name) {
    var g = itemGroup, i;
    if (name) for (i = item; i; i = i.mark.group) {
      if (i.mark.name === name) {
        g = i;
        break;
      }
    }
    return g && g.mark && g.mark.interactive ? g : {};
  }
  function xy(item2) {
    if (!item2) return point9;
    if (isString(item2)) item2 = group3(item2);
    const p = point9.slice();
    while (item2) {
      p[0] -= item2.x || 0;
      p[1] -= item2.y || 0;
      item2 = item2.mark && item2.mark.group;
    }
    return p;
  }
  return {
    view: constant_default(view),
    item: constant_default(item || {}),
    group: group3,
    xy,
    x: (item2) => xy(item2)[0],
    y: (item2) => xy(item2)[1]
  };
}
var VIEW = "view";
var TIMER = "timer";
var WINDOW = "window";
var NO_TRAP = {
  trap: false
};
function initializeEventConfig(config) {
  const events3 = extend({
    defaults: {}
  }, config);
  const unpack = (obj, keys4) => {
    keys4.forEach((k) => {
      if (isArray(obj[k])) obj[k] = toSet(obj[k]);
    });
  };
  unpack(events3.defaults, ["prevent", "allow"]);
  unpack(events3, ["view", "window", "selector"]);
  return events3;
}
function trackEventListener(view, sources, type4, handler) {
  view._eventListeners.push({
    type: type4,
    sources: array_default(sources),
    handler
  });
}
function prevent(view, type4) {
  var def3 = view._eventConfig.defaults, prevent2 = def3.prevent, allow = def3.allow;
  return prevent2 === false || allow === true ? false : prevent2 === true || allow === false ? true : prevent2 ? prevent2[type4] : allow ? !allow[type4] : view.preventDefault();
}
function permit(view, key2, type4) {
  const rule5 = view._eventConfig && view._eventConfig[key2];
  if (rule5 === false || isObject(rule5) && !rule5[type4]) {
    view.warn(`Blocked ${key2} ${type4} event listener.`);
    return false;
  }
  return true;
}
function events2(source4, type4, filter4) {
  var view = this, s = new EventStream(filter4), send = function(e, item) {
    view.runAsync(null, () => {
      if (source4 === VIEW && prevent(view, type4)) {
        e.preventDefault();
      }
      s.receive(eventExtend(view, e, item));
    });
  }, sources;
  if (source4 === TIMER) {
    if (permit(view, "timer", type4)) {
      view.timer(send, type4);
    }
  } else if (source4 === VIEW) {
    if (permit(view, "view", type4)) {
      view.addEventListener(type4, send, NO_TRAP);
    }
  } else {
    if (source4 === WINDOW) {
      if (permit(view, "window", type4) && typeof window !== "undefined") {
        sources = [window];
      }
    } else if (typeof document !== "undefined") {
      if (permit(view, "selector", type4)) {
        sources = Array.from(document.querySelectorAll(source4));
      }
    }
    if (!sources) {
      view.warn("Can not resolve event source: " + source4);
    } else {
      for (var i = 0, n = sources.length; i < n; ++i) {
        sources[i].addEventListener(type4, send);
      }
      trackEventListener(view, sources, type4, send);
    }
  }
  return s;
}
function itemFilter(event2) {
  return event2.item;
}
function markTarget(event2) {
  return event2.item.mark.source;
}
function invoke(name) {
  return function(_, event2) {
    return event2.vega.view().changeset().encode(event2.item, name);
  };
}
function hover(hoverSet, leaveSet) {
  hoverSet = [hoverSet || "hover"];
  leaveSet = [leaveSet || "update", hoverSet[0]];
  this.on(this.events("view", "pointerover", itemFilter), markTarget, invoke(hoverSet));
  this.on(this.events("view", "pointerout", itemFilter), markTarget, invoke(leaveSet));
  return this;
}
function finalize() {
  var tooltip2 = this._tooltip, timers = this._timers, handlers = this._handler.handlers(), listeners = this._eventListeners, n, m3, e, h3, t;
  n = timers.length;
  while (--n >= 0) {
    timers[n].stop();
  }
  n = listeners.length;
  while (--n >= 0) {
    e = listeners[n];
    m3 = e.sources.length;
    while (--m3 >= 0) {
      e.sources[m3].removeEventListener(e.type, e.handler);
    }
  }
  if (tooltip2) {
    tooltip2.call(this, this._handler, null, null, null);
  }
  n = handlers.length;
  while (--n >= 0) {
    t = handlers[n].type;
    h3 = handlers[n].handler;
    this._handler.off(t, h3);
  }
  return this;
}
function element3(tag, attr3, text4) {
  const el = document.createElement(tag);
  for (const key2 in attr3) el.setAttribute(key2, attr3[key2]);
  if (text4 != null) el.textContent = text4;
  return el;
}
var BindClass = "vega-bind";
var NameClass = "vega-bind-name";
var RadioClass = "vega-bind-radio";
function bind3(view, el, binding) {
  if (!el) return;
  const param2 = binding.param;
  let bind4 = binding.state;
  if (!bind4) {
    bind4 = binding.state = {
      elements: null,
      active: false,
      set: null,
      update: (value4) => {
        if (value4 != view.signal(param2.signal)) {
          view.runAsync(null, () => {
            bind4.source = true;
            view.signal(param2.signal, value4);
          });
        }
      }
    };
    if (param2.debounce) {
      bind4.update = debounce(param2.debounce, bind4.update);
    }
  }
  const create4 = param2.input == null && param2.element ? target : generate;
  create4(bind4, el, param2, view);
  if (!bind4.active) {
    view.on(view._signals[param2.signal], null, () => {
      bind4.source ? bind4.source = false : bind4.set(view.signal(param2.signal));
    });
    bind4.active = true;
  }
  return bind4;
}
function target(bind4, node, param2, view) {
  const type4 = param2.event || "input";
  const handler = () => bind4.update(node.value);
  view.signal(param2.signal, node.value);
  node.addEventListener(type4, handler);
  trackEventListener(view, node, type4, handler);
  bind4.set = (value4) => {
    node.value = value4;
    node.dispatchEvent(event(type4));
  };
}
function event(type4) {
  return typeof Event !== "undefined" ? new Event(type4) : {
    type: type4
  };
}
function generate(bind4, el, param2, view) {
  const value4 = view.signal(param2.signal);
  const div = element3("div", {
    "class": BindClass
  });
  const wrapper = param2.input === "radio" ? div : div.appendChild(element3("label"));
  wrapper.appendChild(element3("span", {
    "class": NameClass
  }, param2.name || param2.signal));
  el.appendChild(div);
  let input = form;
  switch (param2.input) {
    case "checkbox":
      input = checkbox;
      break;
    case "select":
      input = select;
      break;
    case "radio":
      input = radio;
      break;
    case "range":
      input = range4;
      break;
  }
  input(bind4, wrapper, param2, value4);
}
function form(bind4, el, param2, value4) {
  const node = element3("input");
  for (const key2 in param2) {
    if (key2 !== "signal" && key2 !== "element") {
      node.setAttribute(key2 === "input" ? "type" : key2, param2[key2]);
    }
  }
  node.setAttribute("name", param2.signal);
  node.value = value4;
  el.appendChild(node);
  node.addEventListener("input", () => bind4.update(node.value));
  bind4.elements = [node];
  bind4.set = (value5) => node.value = value5;
}
function checkbox(bind4, el, param2, value4) {
  const attr3 = {
    type: "checkbox",
    name: param2.signal
  };
  if (value4) attr3.checked = true;
  const node = element3("input", attr3);
  el.appendChild(node);
  node.addEventListener("change", () => bind4.update(node.checked));
  bind4.elements = [node];
  bind4.set = (value5) => node.checked = !!value5 || null;
}
function select(bind4, el, param2, value4) {
  const node = element3("select", {
    name: param2.signal
  }), labels2 = param2.labels || [];
  param2.options.forEach((option, i) => {
    const attr3 = {
      value: option
    };
    if (valuesEqual(option, value4)) attr3.selected = true;
    node.appendChild(element3("option", attr3, (labels2[i] || option) + ""));
  });
  el.appendChild(node);
  node.addEventListener("change", () => {
    bind4.update(param2.options[node.selectedIndex]);
  });
  bind4.elements = [node];
  bind4.set = (value5) => {
    for (let i = 0, n = param2.options.length; i < n; ++i) {
      if (valuesEqual(param2.options[i], value5)) {
        node.selectedIndex = i;
        return;
      }
    }
  };
}
function radio(bind4, el, param2, value4) {
  const group3 = element3("span", {
    "class": RadioClass
  }), labels2 = param2.labels || [];
  el.appendChild(group3);
  bind4.elements = param2.options.map((option, i) => {
    const attr3 = {
      type: "radio",
      name: param2.signal,
      value: option
    };
    if (valuesEqual(option, value4)) attr3.checked = true;
    const input = element3("input", attr3);
    input.addEventListener("change", () => bind4.update(option));
    const label = element3("label", {}, (labels2[i] || option) + "");
    label.prepend(input);
    group3.appendChild(label);
    return input;
  });
  bind4.set = (value5) => {
    const nodes = bind4.elements, n = nodes.length;
    for (let i = 0; i < n; ++i) {
      if (valuesEqual(nodes[i].value, value5)) nodes[i].checked = true;
    }
  };
}
function range4(bind4, el, param2, value4) {
  value4 = value4 !== void 0 ? value4 : (+param2.max + +param2.min) / 2;
  const max4 = param2.max != null ? param2.max : Math.max(100, +value4) || 100, min4 = param2.min || Math.min(0, max4, +value4) || 0, step = param2.step || tickStep(min4, max4, 100);
  const node = element3("input", {
    type: "range",
    name: param2.signal,
    min: min4,
    max: max4,
    step
  });
  node.value = value4;
  const span = element3("span", {}, +value4);
  el.appendChild(node);
  el.appendChild(span);
  const update3 = () => {
    span.textContent = node.value;
    bind4.update(+node.value);
  };
  node.addEventListener("input", update3);
  node.addEventListener("change", update3);
  bind4.elements = [node];
  bind4.set = (value5) => {
    node.value = value5;
    span.textContent = value5;
  };
}
function valuesEqual(a3, b10) {
  return a3 === b10 || a3 + "" === b10 + "";
}
function initializeRenderer(view, r, el, constructor, scaleFactor, opt) {
  r = r || new constructor(view.loader());
  return r.initialize(el, width(view), height(view), offset5(view), scaleFactor, opt).background(view.background());
}
function trap(view, fn) {
  return !fn ? null : function() {
    try {
      fn.apply(this, arguments);
    } catch (error3) {
      view.error(error3);
    }
  };
}
function initializeHandler(view, prevHandler, el, constructor) {
  const handler = new constructor(view.loader(), trap(view, view.tooltip())).scene(view.scenegraph().root).initialize(el, offset5(view), view);
  if (prevHandler) {
    prevHandler.handlers().forEach((h3) => {
      handler.on(h3.type, h3.handler);
    });
  }
  return handler;
}
function initialize2(el, elBind) {
  const view = this, type4 = view._renderType, config = view._eventConfig.bind, module = renderModule(type4);
  el = view._el = el ? lookup5(view, el, true) : null;
  initializeAria(view);
  if (!module) view.error("Unrecognized renderer type: " + type4);
  const Handler3 = module.handler || CanvasHandler2, Renderer3 = el ? module.renderer : module.headless;
  view._renderer = !Renderer3 ? null : initializeRenderer(view, view._renderer, el, Renderer3);
  view._handler = initializeHandler(view, view._handler, el, Handler3);
  view._redraw = true;
  if (el && config !== "none") {
    elBind = elBind ? view._elBind = lookup5(view, elBind, true) : el.appendChild(element3("form", {
      "class": "vega-bindings"
    }));
    view._bind.forEach((_) => {
      if (_.param.element && config !== "container") {
        _.element = lookup5(view, _.param.element, !!_.param.input);
      }
    });
    view._bind.forEach((_) => {
      bind3(view, _.element || elBind, _);
    });
  }
  return view;
}
function lookup5(view, el, clear2) {
  if (typeof el === "string") {
    if (typeof document !== "undefined") {
      el = document.querySelector(el);
      if (!el) {
        view.error("Signal bind element not found: " + el);
        return null;
      }
    } else {
      view.error("DOM document instance not found.");
      return null;
    }
  }
  if (el && clear2) {
    try {
      el.textContent = "";
    } catch (e) {
      el = null;
      view.error(e);
    }
  }
  return el;
}
var number7 = (_) => +_ || 0;
var paddingObject = (_) => ({
  top: _,
  bottom: _,
  left: _,
  right: _
});
function padding(_) {
  return isObject(_) ? {
    top: number7(_.top),
    bottom: number7(_.bottom),
    left: number7(_.left),
    right: number7(_.right)
  } : paddingObject(number7(_));
}
async function renderHeadless(view, type4, scaleFactor, opt) {
  const module = renderModule(type4), ctr = module && module.headless;
  if (!ctr) error("Unrecognized renderer type: " + type4);
  await view.runAsync();
  return initializeRenderer(view, null, null, ctr, scaleFactor, opt).renderAsync(view._scenegraph.root);
}
async function renderToImageURL(type4, scaleFactor) {
  if (type4 !== RenderType.Canvas && type4 !== RenderType.SVG && type4 !== RenderType.PNG) {
    error("Unrecognized image type: " + type4);
  }
  const r = await renderHeadless(this, type4, scaleFactor);
  return type4 === RenderType.SVG ? toBlobURL(r.svg(), "image/svg+xml") : r.canvas().toDataURL("image/png");
}
function toBlobURL(data4, mime) {
  const blob = new Blob([data4], {
    type: mime
  });
  return window.URL.createObjectURL(blob);
}
async function renderToCanvas(scaleFactor, opt) {
  const r = await renderHeadless(this, RenderType.Canvas, scaleFactor, opt);
  return r.canvas();
}
async function renderToSVG(scaleFactor) {
  const r = await renderHeadless(this, RenderType.SVG, scaleFactor);
  return r.svg();
}
function runtime(view, spec, expr2) {
  return context3(view, transforms, functionContext, expr2).parse(spec);
}
function scale6(name) {
  var scales2 = this._runtime.scales;
  if (!hasOwnProperty(scales2, name)) {
    error("Unrecognized scale or projection: " + name);
  }
  return scales2[name].value;
}
var Width = "width";
var Height = "height";
var Padding2 = "padding";
var Skip2 = {
  skip: true
};
function viewWidth(view, width2) {
  var a3 = view.autosize(), p = view.padding();
  return width2 - (a3 && a3.contains === Padding2 ? p.left + p.right : 0);
}
function viewHeight(view, height2) {
  var a3 = view.autosize(), p = view.padding();
  return height2 - (a3 && a3.contains === Padding2 ? p.top + p.bottom : 0);
}
function initializeResize(view) {
  var s = view._signals, w3 = s[Width], h3 = s[Height], p = s[Padding2];
  function resetSize() {
    view._autosize = view._resize = 1;
  }
  view._resizeWidth = view.add(null, (_) => {
    view._width = _.size;
    view._viewWidth = viewWidth(view, _.size);
    resetSize();
  }, {
    size: w3
  });
  view._resizeHeight = view.add(null, (_) => {
    view._height = _.size;
    view._viewHeight = viewHeight(view, _.size);
    resetSize();
  }, {
    size: h3
  });
  const resizePadding = view.add(null, resetSize, {
    pad: p
  });
  view._resizeWidth.rank = w3.rank + 1;
  view._resizeHeight.rank = h3.rank + 1;
  resizePadding.rank = p.rank + 1;
}
function resizeView(viewWidth2, viewHeight2, width2, height2, origin, auto) {
  this.runAfter((view) => {
    let rerun2 = 0;
    view._autosize = 0;
    if (view.width() !== width2) {
      rerun2 = 1;
      view.signal(Width, width2, Skip2);
      view._resizeWidth.skip(true);
    }
    if (view.height() !== height2) {
      rerun2 = 1;
      view.signal(Height, height2, Skip2);
      view._resizeHeight.skip(true);
    }
    if (view._viewWidth !== viewWidth2) {
      view._resize = 1;
      view._viewWidth = viewWidth2;
    }
    if (view._viewHeight !== viewHeight2) {
      view._resize = 1;
      view._viewHeight = viewHeight2;
    }
    if (view._origin[0] !== origin[0] || view._origin[1] !== origin[1]) {
      view._resize = 1;
      view._origin = origin;
    }
    if (rerun2) view.run("enter");
    if (auto) view.runAfter((v2) => v2.resize());
  }, false, 1);
}
function getState2(options) {
  return this._runtime.getState(options || {
    data: dataTest,
    signals: signalTest,
    recurse: true
  });
}
function dataTest(name, data4) {
  return data4.modified && isArray(data4.input.value) && !name.startsWith("_:vega:_");
}
function signalTest(name, op) {
  return !(name === "parent" || op instanceof transforms.proxy);
}
function setState2(state) {
  this.runAsync(null, (v2) => {
    v2._trigger = false;
    v2._runtime.setState(state);
  }, (v2) => {
    v2._trigger = true;
  });
  return this;
}
function timer2(callback, delay) {
  function tick2(elapsed) {
    callback({
      timestamp: Date.now(),
      elapsed
    });
  }
  this._timers.push(interval_default(tick2, delay));
}
function defaultTooltip3(handler, event2, item, value4) {
  const el = handler.element();
  if (el) el.setAttribute("title", formatTooltip(value4));
}
function formatTooltip(value4) {
  return value4 == null ? "" : isArray(value4) ? formatArray(value4) : isObject(value4) && !isDate(value4) ? formatObject(value4) : value4 + "";
}
function formatObject(obj) {
  return Object.keys(obj).map((key2) => {
    const v2 = obj[key2];
    return key2 + ": " + (isArray(v2) ? formatArray(v2) : formatValue2(v2));
  }).join("\n");
}
function formatArray(value4) {
  return "[" + value4.map(formatValue2).join(", ") + "]";
}
function formatValue2(value4) {
  return isArray(value4) ? "[\u2026]" : isObject(value4) && !isDate(value4) ? "{\u2026}" : value4;
}
function watchPixelRatio() {
  if (this.renderer() === "canvas" && this._renderer._canvas) {
    let remove2 = null;
    const updatePixelRatio = () => {
      if (remove2 != null) {
        remove2();
      }
      const media = matchMedia(`(resolution: ${window.devicePixelRatio}dppx)`);
      media.addEventListener("change", updatePixelRatio);
      remove2 = () => {
        media.removeEventListener("change", updatePixelRatio);
      };
      this._renderer._canvas.getContext("2d").pixelRatio = window.devicePixelRatio || 1;
      this._redraw = true;
      this._resize = 1;
      this.resize().runAsync();
    };
    updatePixelRatio();
  }
}
function View(spec, options) {
  const view = this;
  options = options || {};
  Dataflow.call(view);
  if (options.loader) view.loader(options.loader);
  if (options.logger) view.logger(options.logger);
  if (options.logLevel != null) view.logLevel(options.logLevel);
  if (options.locale || spec.locale) {
    const loc = extend({}, spec.locale, options.locale);
    view.locale(locale3(loc.number, loc.time));
  }
  view._el = null;
  view._elBind = null;
  view._renderType = options.renderer || RenderType.Canvas;
  view._scenegraph = new Scenegraph();
  const root = view._scenegraph.root;
  view._renderer = null;
  view._tooltip = options.tooltip || defaultTooltip3, view._redraw = true;
  view._handler = new CanvasHandler2().scene(root);
  view._globalCursor = false;
  view._preventDefault = false;
  view._timers = [];
  view._eventListeners = [];
  view._resizeListeners = [];
  view._eventConfig = initializeEventConfig(spec.eventConfig);
  view.globalCursor(view._eventConfig.globalCursor);
  const ctx = runtime(view, spec, options.expr);
  view._runtime = ctx;
  view._signals = ctx.signals;
  view._bind = (spec.bindings || []).map((_) => ({
    state: null,
    param: extend({}, _)
  }));
  if (ctx.root) ctx.root.set(root);
  root.source = ctx.data.root.input;
  view.pulse(ctx.data.root.input, view.changeset().insert(root.items));
  view._width = view.width();
  view._height = view.height();
  view._viewWidth = viewWidth(view, view._width);
  view._viewHeight = viewHeight(view, view._height);
  view._origin = [0, 0];
  view._resize = 0;
  view._autosize = 1;
  initializeResize(view);
  background3(view);
  cursor(view);
  view.description(spec.description);
  if (options.hover) view.hover();
  if (options.container) view.initialize(options.container, options.bind);
  if (options.watchPixelRatio) view._watchPixelRatio();
}
function lookupSignal(view, name) {
  return hasOwnProperty(view._signals, name) ? view._signals[name] : error("Unrecognized signal name: " + stringValue(name));
}
function findOperatorHandler(op, handler) {
  const h3 = (op._targets || []).filter((op2) => op2._update && op2._update.handler === handler);
  return h3.length ? h3[0] : null;
}
function addOperatorListener(view, name, op, handler) {
  let h3 = findOperatorHandler(op, handler);
  if (!h3) {
    h3 = trap(view, () => handler(name, op.value));
    h3.handler = handler;
    view.on(op, null, h3);
  }
  return view;
}
function removeOperatorListener(view, op, handler) {
  const h3 = findOperatorHandler(op, handler);
  if (h3) op._targets.remove(h3);
  return view;
}
inherits_default(View, Dataflow, {
  // -- DATAFLOW / RENDERING ----
  async evaluate(encode3, prerun, postrun) {
    await Dataflow.prototype.evaluate.call(this, encode3, prerun);
    if (this._redraw || this._resize) {
      try {
        if (this._renderer) {
          if (this._resize) {
            this._resize = 0;
            resizeRenderer(this);
          }
          await this._renderer.renderAsync(this._scenegraph.root);
        }
        this._redraw = false;
      } catch (e) {
        this.error(e);
      }
    }
    if (postrun) asyncCallback(this, postrun);
    return this;
  },
  dirty(item) {
    this._redraw = true;
    this._renderer && this._renderer.dirty(item);
  },
  // -- GET / SET ----
  description(text4) {
    if (arguments.length) {
      const desc = text4 != null ? text4 + "" : null;
      if (desc !== this._desc) ariaLabel(this._el, this._desc = desc);
      return this;
    }
    return this._desc;
  },
  container() {
    return this._el;
  },
  scenegraph() {
    return this._scenegraph;
  },
  origin() {
    return this._origin.slice();
  },
  signal(name, value4, options) {
    const op = lookupSignal(this, name);
    return arguments.length === 1 ? op.value : this.update(op, value4, options);
  },
  width(_) {
    return arguments.length ? this.signal("width", _) : this.signal("width");
  },
  height(_) {
    return arguments.length ? this.signal("height", _) : this.signal("height");
  },
  padding(_) {
    return arguments.length ? this.signal("padding", padding(_)) : padding(this.signal("padding"));
  },
  autosize(_) {
    return arguments.length ? this.signal("autosize", _) : this.signal("autosize");
  },
  background(_) {
    return arguments.length ? this.signal("background", _) : this.signal("background");
  },
  renderer(type4) {
    if (!arguments.length) return this._renderType;
    if (!renderModule(type4)) error("Unrecognized renderer type: " + type4);
    if (type4 !== this._renderType) {
      this._renderType = type4;
      this._resetRenderer();
    }
    return this;
  },
  tooltip(handler) {
    if (!arguments.length) return this._tooltip;
    if (handler !== this._tooltip) {
      this._tooltip = handler;
      this._resetRenderer();
    }
    return this;
  },
  loader(loader2) {
    if (!arguments.length) return this._loader;
    if (loader2 !== this._loader) {
      Dataflow.prototype.loader.call(this, loader2);
      this._resetRenderer();
    }
    return this;
  },
  resize() {
    this._autosize = 1;
    return this.touch(lookupSignal(this, "autosize"));
  },
  _resetRenderer() {
    if (this._renderer) {
      this._renderer = null;
      this.initialize(this._el, this._elBind);
    }
  },
  // -- SIZING ----
  _resizeView: resizeView,
  // -- EVENT HANDLING ----
  addEventListener(type4, handler, options) {
    let callback = handler;
    if (!(options && options.trap === false)) {
      callback = trap(this, handler);
      callback.raw = handler;
    }
    this._handler.on(type4, callback);
    return this;
  },
  removeEventListener(type4, handler) {
    var handlers = this._handler.handlers(type4), i = handlers.length, h3, t;
    while (--i >= 0) {
      t = handlers[i].type;
      h3 = handlers[i].handler;
      if (type4 === t && (handler === h3 || handler === h3.raw)) {
        this._handler.off(t, h3);
        break;
      }
    }
    return this;
  },
  addResizeListener(handler) {
    const l = this._resizeListeners;
    if (!l.includes(handler)) {
      l.push(handler);
    }
    return this;
  },
  removeResizeListener(handler) {
    var l = this._resizeListeners, i = l.indexOf(handler);
    if (i >= 0) {
      l.splice(i, 1);
    }
    return this;
  },
  addSignalListener(name, handler) {
    return addOperatorListener(this, name, lookupSignal(this, name), handler);
  },
  removeSignalListener(name, handler) {
    return removeOperatorListener(this, lookupSignal(this, name), handler);
  },
  addDataListener(name, handler) {
    return addOperatorListener(this, name, dataref(this, name).values, handler);
  },
  removeDataListener(name, handler) {
    return removeOperatorListener(this, dataref(this, name).values, handler);
  },
  globalCursor(_) {
    if (arguments.length) {
      if (this._globalCursor !== !!_) {
        const prev = setCursor(this, null);
        this._globalCursor = !!_;
        if (prev) setCursor(this, prev);
      }
      return this;
    } else {
      return this._globalCursor;
    }
  },
  preventDefault(_) {
    if (arguments.length) {
      this._preventDefault = _;
      return this;
    } else {
      return this._preventDefault;
    }
  },
  timer: timer2,
  events: events2,
  finalize,
  hover,
  // -- DATA ----
  data: data2,
  change,
  insert,
  remove,
  // -- SCALES --
  scale: scale6,
  // -- INITIALIZATION ----
  initialize: initialize2,
  // -- HEADLESS RENDERING ----
  toImageURL: renderToImageURL,
  toCanvas: renderToCanvas,
  toSVG: renderToSVG,
  // -- SAVE / RESTORE STATE ----
  getState: getState2,
  setState: setState2,
  // RE-RENDER ON ZOOM
  _watchPixelRatio: watchPixelRatio
});

// ../deepseek-harness/node_modules/.pnpm/d3-ease@3.0.1/node_modules/d3-ease/src/linear.js
var linear4 = (t) => +t;

// ../deepseek-harness/node_modules/.pnpm/d3-ease@3.0.1/node_modules/d3-ease/src/quad.js
function quadIn(t) {
  return t * t;
}
function quadOut(t) {
  return t * (2 - t);
}
function quadInOut(t) {
  return ((t *= 2) <= 1 ? t * t : --t * (2 - t) + 1) / 2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-ease@3.0.1/node_modules/d3-ease/src/cubic.js
function cubicIn(t) {
  return t * t * t;
}
function cubicOut(t) {
  return --t * t * t + 1;
}
function cubicInOut(t) {
  return ((t *= 2) <= 1 ? t * t * t : (t -= 2) * t * t + 2) / 2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-ease@3.0.1/node_modules/d3-ease/src/poly.js
var exponent = 3;
var polyIn = (function custom12(e) {
  e = +e;
  function polyIn2(t) {
    return Math.pow(t, e);
  }
  polyIn2.exponent = custom12;
  return polyIn2;
})(exponent);
var polyOut = (function custom13(e) {
  e = +e;
  function polyOut2(t) {
    return 1 - Math.pow(1 - t, e);
  }
  polyOut2.exponent = custom13;
  return polyOut2;
})(exponent);
var polyInOut = (function custom14(e) {
  e = +e;
  function polyInOut2(t) {
    return ((t *= 2) <= 1 ? Math.pow(t, e) : 2 - Math.pow(2 - t, e)) / 2;
  }
  polyInOut2.exponent = custom14;
  return polyInOut2;
})(exponent);

// ../deepseek-harness/node_modules/.pnpm/d3-ease@3.0.1/node_modules/d3-ease/src/sin.js
var pi5 = Math.PI;
var halfPi4 = pi5 / 2;
function sinIn(t) {
  return +t === 1 ? 1 : 1 - Math.cos(t * halfPi4);
}
function sinOut(t) {
  return Math.sin(t * halfPi4);
}
function sinInOut(t) {
  return (1 - Math.cos(pi5 * t)) / 2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-ease@3.0.1/node_modules/d3-ease/src/math.js
function tpmt(x6) {
  return (Math.pow(2, -10 * x6) - 9765625e-10) * 1.0009775171065494;
}

// ../deepseek-harness/node_modules/.pnpm/d3-ease@3.0.1/node_modules/d3-ease/src/exp.js
function expIn(t) {
  return tpmt(1 - +t);
}
function expOut(t) {
  return 1 - tpmt(t);
}
function expInOut(t) {
  return ((t *= 2) <= 1 ? tpmt(1 - t) : 2 - tpmt(t - 1)) / 2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-ease@3.0.1/node_modules/d3-ease/src/circle.js
function circleIn(t) {
  return 1 - Math.sqrt(1 - t * t);
}
function circleOut(t) {
  return Math.sqrt(1 - --t * t);
}
function circleInOut(t) {
  return ((t *= 2) <= 1 ? 1 - Math.sqrt(1 - t * t) : Math.sqrt(1 - (t -= 2) * t) + 1) / 2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-ease@3.0.1/node_modules/d3-ease/src/bounce.js
var b1 = 4 / 11;
var b22 = 6 / 11;
var b3 = 8 / 11;
var b4 = 3 / 4;
var b5 = 9 / 11;
var b6 = 10 / 11;
var b7 = 15 / 16;
var b8 = 21 / 22;
var b9 = 63 / 64;
var b0 = 1 / b1 / b1;
function bounceIn(t) {
  return 1 - bounceOut(1 - t);
}
function bounceOut(t) {
  return (t = +t) < b1 ? b0 * t * t : t < b3 ? b0 * (t -= b22) * t + b4 : t < b6 ? b0 * (t -= b5) * t + b7 : b0 * (t -= b8) * t + b9;
}
function bounceInOut(t) {
  return ((t *= 2) <= 1 ? 1 - bounceOut(1 - t) : bounceOut(t - 1) + 1) / 2;
}

// ../deepseek-harness/node_modules/.pnpm/d3-ease@3.0.1/node_modules/d3-ease/src/back.js
var overshoot = 1.70158;
var backIn = (function custom15(s) {
  s = +s;
  function backIn2(t) {
    return (t = +t) * t * (s * (t - 1) + t);
  }
  backIn2.overshoot = custom15;
  return backIn2;
})(overshoot);
var backOut = (function custom16(s) {
  s = +s;
  function backOut2(t) {
    return --t * t * ((t + 1) * s + t) + 1;
  }
  backOut2.overshoot = custom16;
  return backOut2;
})(overshoot);
var backInOut = (function custom17(s) {
  s = +s;
  function backInOut2(t) {
    return ((t *= 2) < 1 ? t * t * ((s + 1) * t - s) : (t -= 2) * t * ((s + 1) * t + s) + 2) / 2;
  }
  backInOut2.overshoot = custom17;
  return backInOut2;
})(overshoot);

// ../deepseek-harness/node_modules/.pnpm/d3-ease@3.0.1/node_modules/d3-ease/src/elastic.js
var tau6 = 2 * Math.PI;
var amplitude = 1;
var period = 0.3;
var elasticIn = (function custom18(a3, p) {
  var s = Math.asin(1 / (a3 = Math.max(1, a3))) * (p /= tau6);
  function elasticIn2(t) {
    return a3 * tpmt(- --t) * Math.sin((s - t) / p);
  }
  elasticIn2.amplitude = function(a4) {
    return custom18(a4, p * tau6);
  };
  elasticIn2.period = function(p2) {
    return custom18(a3, p2);
  };
  return elasticIn2;
})(amplitude, period);
var elasticOut = (function custom19(a3, p) {
  var s = Math.asin(1 / (a3 = Math.max(1, a3))) * (p /= tau6);
  function elasticOut2(t) {
    return 1 - a3 * tpmt(t = +t) * Math.sin((t + s) / p);
  }
  elasticOut2.amplitude = function(a4) {
    return custom19(a4, p * tau6);
  };
  elasticOut2.period = function(p2) {
    return custom19(a3, p2);
  };
  return elasticOut2;
})(amplitude, period);
var elasticInOut = (function custom20(a3, p) {
  var s = Math.asin(1 / (a3 = Math.max(1, a3))) * (p /= tau6);
  function elasticInOut2(t) {
    return ((t = t * 2 - 1) < 0 ? a3 * tpmt(-t) * Math.sin((s - t) / p) : 2 - a3 * tpmt(t) * Math.sin((s + t) / p)) / 2;
  }
  elasticInOut2.amplitude = function(a4) {
    return custom20(a4, p * tau6);
  };
  elasticInOut2.period = function(p2) {
    return custom20(a3, p2);
  };
  return elasticInOut2;
})(amplitude, period);

// ../deepseek-harness/node_modules/.pnpm/vega-functions@6.2.0/node_modules/vega-functions/build/vega-functions.js
function data3(name) {
  const data4 = this.context.data[name];
  return data4 ? data4.values.value : [];
}
function indata2(name, field3, value4) {
  const index4 = this.context.data[name]["index:" + field3], entry2 = index4 ? index4.value.get(value4) : void 0;
  return entry2 ? entry2.count : entry2;
}
function setdata2(name, tuples) {
  const df = this.context.dataflow, data4 = this.context.data[name], input = data4.input;
  df.pulse(input, df.changeset().remove(truthy).insert(tuples));
  return 1;
}
var easeFunctions = {
  easeLinear: linear4,
  easeQuad: quadInOut,
  easeQuadIn: quadIn,
  easeQuadOut: quadOut,
  easeQuadInOut: quadInOut,
  easeCubic: cubicInOut,
  easeCubicIn: cubicIn,
  easeCubicOut: cubicOut,
  easeCubicInOut: cubicInOut,
  easePoly: polyInOut,
  easePolyIn: polyIn,
  easePolyOut: polyOut,
  easePolyInOut: polyInOut,
  easeSin: sinInOut,
  easeSinIn: sinIn,
  easeSinOut: sinOut,
  easeSinInOut: sinInOut,
  easeExp: expInOut,
  easeExpIn: expIn,
  easeExpOut: expOut,
  easeExpInOut: expInOut,
  easeCircle: circleInOut,
  easeCircleIn: circleIn,
  easeCircleOut: circleOut,
  easeCircleInOut: circleInOut,
  easeBounce: bounceOut,
  easeBounceIn: bounceIn,
  easeBounceOut: bounceOut,
  easeBounceInOut: bounceInOut,
  easeBack: backInOut,
  easeBackIn: backIn,
  easeBackOut: backOut,
  easeBackInOut: backInOut,
  easeElastic: elasticOut,
  easeElasticIn: elasticIn,
  easeElasticOut: elasticOut,
  easeElasticInOut: elasticInOut
};
function encode2(item, name, retval) {
  if (item) {
    const df = this.context.dataflow, target2 = item.mark.source;
    df.pulse(target2, df.changeset().encode(item, name));
  }
  return retval !== void 0 ? retval : item;
}
function interpolateLinear(values4, frac) {
  if (!isArray(values4) || !values4.length) return void 0;
  const n = values4.length, lo = values4[0], f = +frac;
  if (n === 1 || !(f > 0)) return lo;
  if (f >= 1) return peek(values4);
  const pos = f * (n - 1), i = Math.floor(pos), t = pos - i;
  return t ? values4[i] + t * (values4[i + 1] - values4[i]) : values4[i];
}
var wrap2 = (method2) => function(value4, spec) {
  const locale4 = this.context.dataflow.locale();
  return value4 === null ? "null" : locale4[method2](spec)(value4);
};
var format5 = wrap2("format");
var timeFormat3 = wrap2("timeFormat");
var utcFormat3 = wrap2("utcFormat");
var timeParse3 = wrap2("timeParse");
var utcParse3 = wrap2("utcParse");
var dateObj2 = new Date(2e3, 0, 1);
function time3(month, day, specifier) {
  if (!Number.isInteger(month) || !Number.isInteger(day)) return "";
  dateObj2.setYear(2e3);
  dateObj2.setMonth(month);
  dateObj2.setDate(day);
  return timeFormat3.call(this, dateObj2, specifier);
}
function monthFormat2(month) {
  return time3.call(this, month, 1, "%B");
}
function monthAbbrevFormat2(month) {
  return time3.call(this, month, 1, "%b");
}
function dayFormat2(day) {
  return time3.call(this, 0, 2 + day, "%A");
}
function dayAbbrevFormat2(day) {
  return time3.call(this, 0, 2 + day, "%a");
}
var DataPrefix3 = ":";
var IndexPrefix3 = "@";
var ScalePrefix2 = "%";
var SignalPrefix2 = "$";
function dataVisitor2(name, args, scope2, params2) {
  if (args[0].type !== Literal) {
    error("First argument to data functions must be a string literal.");
  }
  const data4 = args[0].value, dataName = DataPrefix3 + data4;
  if (!hasOwnProperty(dataName, params2)) {
    try {
      params2[dataName] = scope2.getData(data4).tuplesRef();
    } catch (err) {
    }
  }
}
function indataVisitor2(name, args, scope2, params2) {
  if (args[0].type !== Literal) error("First argument to indata must be a string literal.");
  if (args[1].type !== Literal) error("Second argument to indata must be a string literal.");
  const data4 = args[0].value, field3 = args[1].value, indexName = IndexPrefix3 + field3;
  if (!hasOwnProperty(indexName, params2)) {
    params2[indexName] = scope2.getData(data4).indataRef(scope2, field3);
  }
}
function scaleVisitor2(name, args, scope2, params2) {
  if (args[0].type === Literal) {
    addScaleDependency2(scope2, params2, args[0].value);
  } else {
    for (name in scope2.scales) {
      addScaleDependency2(scope2, params2, name);
    }
  }
}
function addScaleDependency2(scope2, params2, name) {
  const scaleName = ScalePrefix2 + name;
  if (!hasOwnProperty(params2, scaleName)) {
    try {
      params2[scaleName] = scope2.scaleRef(name);
    } catch (err) {
    }
  }
}
function getScale2(nameOrFunction, ctx) {
  if (isString(nameOrFunction)) {
    const maybeScale = ctx.scales[nameOrFunction];
    return maybeScale && isRegisteredScale(maybeScale.value) ? maybeScale.value : void 0;
  } else if (isFunction(nameOrFunction)) {
    return isRegisteredScale(nameOrFunction) ? nameOrFunction : void 0;
  }
  return void 0;
}
function internalScaleFunctions2(codegen2, fnctx, visitors) {
  fnctx.__bandwidth = (s) => s && s.bandwidth ? s.bandwidth() : 0;
  visitors._bandwidth = scaleVisitor2;
  visitors._range = scaleVisitor2;
  visitors._scale = scaleVisitor2;
  const ref2 = (arg) => "_[" + (arg.type === Literal ? stringValue(ScalePrefix2 + arg.value) : stringValue(ScalePrefix2) + "+" + codegen2(arg)) + "]";
  return {
    _bandwidth: (args) => `this.__bandwidth(${ref2(args[0])})`,
    _range: (args) => `${ref2(args[0])}.range()`,
    _scale: (args) => `${ref2(args[0])}(${codegen2(args[1])})`
  };
}
function geoMethod2(methodName, globalMethod) {
  return function(projection3, geojson, group3) {
    if (projection3) {
      const p = getScale2(projection3, (group3 || this).context);
      return p && p.path[methodName](geojson);
    } else {
      return globalMethod(geojson);
    }
  };
}
var geoArea2 = geoMethod2("area", area_default2);
var geoBounds2 = geoMethod2("bounds", bounds_default);
var geoCentroid2 = geoMethod2("centroid", centroid_default);
function geoScale2(projection3, group3) {
  const p = getScale2(projection3, (group3 || this).context);
  return p && p.scale();
}
function geoTranslate(projection3, group3) {
  const p = getScale2(projection3, (group3 || this).context);
  return p && p.translate();
}
function inScope2(item) {
  const group3 = this.context.group;
  let value4 = false;
  if (group3) while (item) {
    if (item === group3) {
      value4 = true;
      break;
    }
    item = item.mark.group;
  }
  return value4;
}
function log7(df, method2, args) {
  try {
    df[method2].apply(df, ["EXPRESSION"].concat([].slice.call(args)));
  } catch (err) {
    df.warn(err);
  }
  return args[args.length - 1];
}
function warn2() {
  return log7(this.context.dataflow, "warn", arguments);
}
function info2() {
  return log7(this.context.dataflow, "info", arguments);
}
function debug2() {
  return log7(this.context.dataflow, "debug", arguments);
}
function channel_luminance_value2(channelValue) {
  const val = channelValue / 255;
  if (val <= 0.03928) {
    return val / 12.92;
  }
  return Math.pow((val + 0.055) / 1.055, 2.4);
}
function luminance2(color6) {
  const c3 = rgb(color6), r = channel_luminance_value2(c3.r), g = channel_luminance_value2(c3.g), b10 = channel_luminance_value2(c3.b);
  return 0.2126 * r + 0.7152 * g + 0.0722 * b10;
}
function contrast2(color1, color22) {
  const lum1 = luminance2(color1), lum2 = luminance2(color22), lumL = Math.max(lum1, lum2), lumD = Math.min(lum1, lum2);
  return (lumL + 0.05) / (lumD + 0.05);
}
function merge5() {
  const args = [].slice.call(arguments);
  args.unshift({});
  return extend(...args);
}
function equal2(a3, b10) {
  return a3 === b10 || a3 !== a3 && b10 !== b10 ? true : isArray(a3) ? isArray(b10) && a3.length === b10.length ? equalArray2(a3, b10) : false : isObject(a3) && isObject(b10) ? equalObject2(a3, b10) : false;
}
function equalArray2(a3, b10) {
  for (let i = 0, n = a3.length; i < n; ++i) {
    if (!equal2(a3[i], b10[i])) return false;
  }
  return true;
}
function equalObject2(a3, b10) {
  for (const key2 in a3) {
    if (!equal2(a3[key2], b10[key2])) return false;
  }
  return true;
}
function removePredicate2(props) {
  return (_) => equalObject2(props, _);
}
function modify2(name, insert2, remove2, toggle2, modify3, values4) {
  const df = this.context.dataflow, data4 = this.context.data[name], input = data4.input, stamp = df.stamp();
  let changes = data4.changes, predicate, key2;
  if (df._trigger === false || !(input.value.length || insert2 || toggle2)) {
    return 0;
  }
  if (!changes || changes.stamp < stamp) {
    data4.changes = changes = df.changeset();
    changes.stamp = stamp;
    df.runAfter(() => {
      data4.modified = true;
      df.pulse(input, changes).run();
    }, true, 1);
  }
  if (remove2) {
    predicate = remove2 === true ? truthy : isArray(remove2) || isTuple(remove2) ? remove2 : removePredicate2(remove2);
    changes.remove(predicate);
  }
  if (insert2) {
    changes.insert(insert2);
  }
  if (toggle2) {
    predicate = removePredicate2(toggle2);
    if (input.value.some(predicate)) {
      changes.remove(predicate);
    } else {
      changes.insert(toggle2);
    }
  }
  if (modify3) {
    if (isFunction(modify3)) {
      throw Error("modify parameter must be a data tuple, not a function");
    }
    for (key2 in values4) {
      changes.modify(modify3, key2, values4[key2]);
    }
  }
  return 1;
}
function pinchDistance2(event2) {
  const t = event2.touches, dx = t[0].clientX - t[1].clientX, dy = t[0].clientY - t[1].clientY;
  return Math.hypot(dx, dy);
}
function pinchAngle2(event2) {
  const t = event2.touches;
  return Math.atan2(t[0].clientY - t[1].clientY, t[0].clientX - t[1].clientX);
}
var accessors2 = {};
function pluck2(data4, name) {
  const accessor2 = accessors2[name] || (accessors2[name] = field(name));
  return isArray(data4) ? data4.map(accessor2) : accessor2(data4);
}
function array3(seq) {
  return isArray(seq) || ArrayBuffer.isView(seq) ? seq : null;
}
function sequence4(seq) {
  return array3(seq) || (isString(seq) ? seq : null);
}
function join4(seq, ...args) {
  return array3(seq).join(...args);
}
function indexof2(seq, ...args) {
  return sequence4(seq).indexOf(...args);
}
function lastindexof2(seq, ...args) {
  return sequence4(seq).lastIndexOf(...args);
}
function slice4(seq, ...args) {
  return sequence4(seq).slice(...args);
}
function replace3(str, pattern, repl) {
  if (isFunction(repl)) error("Function argument passed to replace.");
  if (!isString(pattern) && !isRegExp(pattern)) error("Please pass a string or RegExp argument to replace.");
  return String(str).replace(pattern, repl);
}
function reverse2(seq) {
  return array3(seq).slice().reverse();
}
function sort3(seq) {
  return array3(seq).slice().sort(ascending);
}
function bandspace2(count2, paddingInner2, paddingOuter2) {
  return bandSpace(count2 || 0, paddingInner2 || 0, paddingOuter2 || 0);
}
function bandwidth2(name, group3) {
  const s = getScale2(name, (group3 || this).context);
  return s && s.bandwidth ? s.bandwidth() : 0;
}
function copy4(name, group3) {
  const s = getScale2(name, (group3 || this).context);
  return s ? s.copy() : void 0;
}
function domain2(name, group3) {
  const s = getScale2(name, (group3 || this).context);
  return s ? s.domain() : [];
}
function invert2(name, range7, group3) {
  const s = getScale2(name, (group3 || this).context);
  return !s ? void 0 : isArray(range7) ? (s.invertRange || s.invert)(range7) : (s.invert || s.invertExtent)(range7);
}
function range5(name, group3) {
  const s = getScale2(name, (group3 || this).context);
  return s && s.range ? s.range() : [];
}
function scale7(name, value4, group3) {
  const s = getScale2(name, (group3 || this).context);
  return s ? s(value4) : void 0;
}
function scaleGradient2(scale9, p02, p1, count2, group3) {
  scale9 = getScale2(scale9, (group3 || this).context);
  const gradient5 = Gradient(p02, p1);
  let stops = scale9.domain(), min4 = stops[0], max4 = peek(stops), fraction = identity;
  if (!(max4 - min4)) {
    scale9 = (scale9.interpolator ? scale("sequential")().interpolator(scale9.interpolator()) : scale("linear")().interpolate(scale9.interpolate()).range(scale9.range())).domain([min4 = 0, max4 = 1]);
  } else {
    fraction = scaleFraction(scale9, min4, max4);
  }
  if (scale9.ticks) {
    stops = scale9.ticks(+count2 || 15);
    if (min4 !== stops[0]) stops.unshift(min4);
    if (max4 !== peek(stops)) stops.push(max4);
  }
  stops.forEach((_) => gradient5.stop(fraction(_), scale9(_)));
  return gradient5;
}
function geoShape2(projection3, geojson, group3) {
  const p = getScale2(projection3, (group3 || this).context);
  return function(context4) {
    return p ? p.path.context(context4)(geojson) : "";
  };
}
function pathShape2(path4) {
  let p = null;
  return function(context4) {
    return context4 ? pathRender(context4, p = p || parse4(path4)) : path4;
  };
}
var datum2 = (d) => d.data;
function treeNodes2(name, context4) {
  const tree = data3.call(context4, name);
  return tree.root && tree.root.lookup || {};
}
function treePath2(name, source4, target2) {
  const nodes = treeNodes2(name, this), s = nodes[source4], t = nodes[target2];
  return s && t ? s.path(t).map(datum2) : void 0;
}
function treeAncestors2(name, node) {
  const n = treeNodes2(name, this)[node];
  return n ? n.ancestors().map(datum2) : void 0;
}
var _window2 = () => typeof window !== "undefined" && window || null;
function screen2() {
  const w3 = _window2();
  return w3 ? w3.screen : {};
}
function windowSize2() {
  const w3 = _window2();
  return w3 ? [w3.innerWidth, w3.innerHeight] : [void 0, void 0];
}
function containerSize2() {
  const view = this.context.dataflow, el = view.container && view.container();
  return el ? [el.clientWidth, el.clientHeight] : [void 0, void 0];
}
function intersect6(b10, opt, group3) {
  if (!b10) return [];
  const [u4, v2] = b10, box = new Bounds().set(u4[0], u4[1], v2[0], v2[1]), scene = group3 || this.context.dataflow.scenegraph().root;
  return intersect2(scene, box, filter3(opt));
}
function filter3(opt) {
  let p = null;
  if (opt) {
    const types = array_default(opt.marktype), names = array_default(opt.markname);
    p = (_) => (!types.length || types.some((t) => _.marktype === t)) && (!names.length || names.some((s) => _.name === s));
  }
  return p;
}
function lassoAppend2(lasso, x6, y6, minDist = 5) {
  lasso = array_default(lasso);
  const last = lasso[lasso.length - 1];
  return last === void 0 || Math.hypot(last[0] - x6, last[1] - y6) > minDist ? [...lasso, [x6, y6]] : lasso;
}
function lassoPath2(lasso) {
  return array_default(lasso).reduce((svg, [x6, y6], i) => {
    return svg += i == 0 ? `M ${x6},${y6} ` : i === lasso.length - 1 ? " Z" : `L ${x6},${y6} `;
  }, "");
}
function intersectLasso2(markname, pixelLasso, unit2) {
  const {
    x: x6,
    y: y6,
    mark
  } = unit2;
  const bb2 = new Bounds().set(Number.MAX_SAFE_INTEGER, Number.MAX_SAFE_INTEGER, Number.MIN_SAFE_INTEGER, Number.MIN_SAFE_INTEGER);
  for (const [px3, py3] of pixelLasso) {
    if (px3 < bb2.x1) bb2.x1 = px3;
    if (px3 > bb2.x2) bb2.x2 = px3;
    if (py3 < bb2.y1) bb2.y1 = py3;
    if (py3 > bb2.y2) bb2.y2 = py3;
  }
  bb2.translate(x6, y6);
  const intersection2 = intersect6([[bb2.x1, bb2.y1], [bb2.x2, bb2.y2]], markname, mark);
  return intersection2.filter((tuple) => pointInPolygon2(tuple.x, tuple.y, pixelLasso));
}
function pointInPolygon2(testx, testy, polygon) {
  let intersections = 0;
  for (let i = 0, j = polygon.length - 1; i < polygon.length; j = i++) {
    const [prevX, prevY] = polygon[j];
    const [x6, y6] = polygon[i];
    if (y6 > testy != prevY > testy && testx < (prevX - x6) * (testy - y6) / (prevY - y6) + x6) {
      intersections++;
    }
  }
  return intersections & 1;
}
var functionContext2 = {
  random() {
    return random();
  },
  // override default
  cumulativeNormal,
  cumulativeLogNormal,
  cumulativeUniform,
  densityNormal,
  densityLogNormal,
  densityUniform,
  quantileNormal,
  quantileLogNormal,
  quantileUniform,
  sampleNormal,
  sampleLogNormal,
  sampleUniform,
  isArray,
  isBoolean,
  isDate,
  isDefined(_) {
    return _ !== void 0;
  },
  isNumber,
  isObject,
  isRegExp,
  isString,
  isTuple,
  isValid(_) {
    return _ != null && _ === _;
  },
  toBoolean,
  toDate(_) {
    return toDate(_);
  },
  // suppress extra arguments
  toNumber,
  toString,
  indexof: indexof2,
  join: join4,
  lastindexof: lastindexof2,
  replace: replace3,
  reverse: reverse2,
  sort: sort3,
  slice: slice4,
  flush: flush_default,
  lerp: lerp_default,
  interpolateLinear,
  merge: merge5,
  pad,
  peek,
  pluck: pluck2,
  span: span_default,
  inrange,
  truncate,
  rgb,
  lab,
  hcl,
  hsl,
  luminance: luminance2,
  contrast: contrast2,
  sequence: range,
  format: format5,
  utcFormat: utcFormat3,
  utcParse: utcParse3,
  utcOffset,
  utcSequence,
  timeFormat: timeFormat3,
  timeParse: timeParse3,
  timeOffset,
  timeSequence,
  timeUnitSpecifier,
  monthFormat: monthFormat2,
  monthAbbrevFormat: monthAbbrevFormat2,
  dayFormat: dayFormat2,
  dayAbbrevFormat: dayAbbrevFormat2,
  quarter,
  utcquarter,
  week,
  utcweek,
  isoweek,
  utcisoweek,
  dayofyear,
  utcdayofyear,
  warn: warn2,
  info: info2,
  debug: debug2,
  extent(_) {
    return extent(_);
  },
  // suppress extra arguments
  inScope: inScope2,
  intersect: intersect6,
  clampRange,
  pinchDistance: pinchDistance2,
  pinchAngle: pinchAngle2,
  screen: screen2,
  containerSize: containerSize2,
  windowSize: windowSize2,
  bandspace: bandspace2,
  setdata: setdata2,
  pathShape: pathShape2,
  panLinear,
  panLog,
  panPow,
  panSymlog,
  zoomLinear,
  zoomLog,
  zoomPow,
  zoomSymlog,
  encode: encode2,
  modify: modify2,
  lassoAppend: lassoAppend2,
  lassoPath: lassoPath2,
  intersectLasso: intersectLasso2,
  ...easeFunctions
};
var eventFunctions2 = ["view", "item", "group", "xy", "x", "y"];
var eventPrefix2 = "event.vega.";
var thisPrefix2 = "this.";
var astVisitors2 = {};
var codegenParams2 = {
  forbidden: ["_"],
  allowed: ["datum", "event", "item"],
  fieldvar: "datum",
  globalvar: (id2) => `_[${stringValue(SignalPrefix2 + id2)}]`,
  functions: buildFunctions2,
  constants: Constants,
  visitors: astVisitors2
};
var codeGenerator2 = codegen(codegenParams2);
function buildFunctions2(codegen2) {
  const fn = Functions(codegen2);
  eventFunctions2.forEach((name) => fn[name] = eventPrefix2 + name);
  for (const name in functionContext2) {
    fn[name] = thisPrefix2 + name;
  }
  extend(fn, internalScaleFunctions2(codegen2, functionContext2, astVisitors2));
  return fn;
}
function expressionFunction2(name, fn, visitor) {
  if (arguments.length === 1) {
    return functionContext2[name];
  }
  functionContext2[name] = fn;
  if (visitor) astVisitors2[name] = visitor;
  if (codeGenerator2) codeGenerator2.functions[name] = thisPrefix2 + name;
  return this;
}
expressionFunction2("bandwidth", bandwidth2, scaleVisitor2);
expressionFunction2("copy", copy4, scaleVisitor2);
expressionFunction2("domain", domain2, scaleVisitor2);
expressionFunction2("range", range5, scaleVisitor2);
expressionFunction2("invert", invert2, scaleVisitor2);
expressionFunction2("scale", scale7, scaleVisitor2);
expressionFunction2("gradient", scaleGradient2, scaleVisitor2);
expressionFunction2("geoArea", geoArea2, scaleVisitor2);
expressionFunction2("geoBounds", geoBounds2, scaleVisitor2);
expressionFunction2("geoCentroid", geoCentroid2, scaleVisitor2);
expressionFunction2("geoShape", geoShape2, scaleVisitor2);
expressionFunction2("geoScale", geoScale2, scaleVisitor2);
expressionFunction2("geoTranslate", geoTranslate, scaleVisitor2);
expressionFunction2("indata", indata2, indataVisitor2);
expressionFunction2("data", data3, dataVisitor2);
expressionFunction2("treePath", treePath2, dataVisitor2);
expressionFunction2("treeAncestors", treeAncestors2, dataVisitor2);
expressionFunction2("vlSelectionTest", selectionTest, selectionVisitor);
expressionFunction2("vlSelectionIdTest", selectionIdTest, selectionVisitor);
expressionFunction2("vlSelectionResolve", selectionResolve, selectionVisitor);
expressionFunction2("vlSelectionTuples", selectionTuples);
function parser2(expr2, scope2) {
  const params2 = {};
  let ast;
  try {
    expr2 = isString(expr2) ? expr2 : stringValue(expr2) + "";
    ast = parser(expr2);
  } catch (err) {
    error("Expression parse error: " + expr2);
  }
  ast.visit((node) => {
    if (node.type !== CallExpression) return;
    const name = node.callee.name, visit3 = codegenParams2.visitors[name];
    if (visit3) visit3(name, node.arguments, scope2, params2);
  });
  const gen = codeGenerator2(ast);
  gen.globals.forEach((name) => {
    const signalName = SignalPrefix2 + name;
    if (!hasOwnProperty(params2, signalName) && scope2.getSignal(name)) {
      params2[signalName] = scope2.signalRef(name);
    }
  });
  return {
    $expr: extend({
      code: gen.code
    }, scope2.options.ast ? {
      ast
    } : null),
    $fields: gen.fields,
    $params: params2
  };
}

// ../deepseek-harness/node_modules/.pnpm/vega-event-selector@4.0.0/node_modules/vega-event-selector/build/vega-event-selector.js
var VIEW2 = "view";
var LBRACK = "[";
var RBRACK = "]";
var LBRACE = "{";
var RBRACE = "}";
var COLON = ":";
var COMMA = ",";
var NAME = "@";
var GT = ">";
var ILLEGAL2 = /[[\]{}]/;
var DEFAULT_MARKS = {
  "*": 1,
  arc: 1,
  area: 1,
  group: 1,
  image: 1,
  line: 1,
  path: 1,
  rect: 1,
  rule: 1,
  shape: 1,
  symbol: 1,
  text: 1,
  trail: 1
};
var DEFAULT_SOURCE;
var MARKS;
function eventSelector(selector, source4, marks) {
  DEFAULT_SOURCE = source4 || VIEW2;
  MARKS = marks || DEFAULT_MARKS;
  return parseMerge(selector.trim()).map(parseSelector);
}
function isMarkType(type4) {
  return MARKS[type4];
}
function find3(s, i, endChar, pushChar, popChar) {
  const n = s.length;
  let count2 = 0, c3;
  for (; i < n; ++i) {
    c3 = s[i];
    if (!count2 && c3 === endChar) return i;
    else if (popChar && popChar.includes(c3)) --count2;
    else if (pushChar && pushChar.includes(c3)) ++count2;
  }
  return i;
}
function parseMerge(s) {
  const output3 = [], n = s.length;
  let start = 0, i = 0;
  while (i < n) {
    i = find3(s, i, COMMA, LBRACK + LBRACE, RBRACK + RBRACE);
    output3.push(s.substring(start, i).trim());
    start = ++i;
  }
  if (output3.length === 0) {
    throw "Empty event selector: " + s;
  }
  return output3;
}
function parseSelector(s) {
  return s[0] === "[" ? parseBetween(s) : parseStream2(s);
}
function parseBetween(s) {
  const n = s.length;
  let i = 1, b10;
  i = find3(s, i, RBRACK, LBRACK, RBRACK);
  if (i === n) {
    throw "Empty between selector: " + s;
  }
  b10 = parseMerge(s.substring(1, i));
  if (b10.length !== 2) {
    throw "Between selector must have two elements: " + s;
  }
  s = s.slice(i + 1).trim();
  if (s[0] !== GT) {
    throw "Expected '>' after between selector: " + s;
  }
  b10 = b10.map(parseSelector);
  const stream2 = parseSelector(s.slice(1).trim());
  if (stream2.between) {
    return {
      between: b10,
      stream: stream2
    };
  } else {
    stream2.between = b10;
  }
  return stream2;
}
function parseStream2(s) {
  const stream2 = {
    source: DEFAULT_SOURCE
  }, source4 = [];
  let throttle = [0, 0], markname = 0, start = 0, n = s.length, i = 0, j, filter4;
  if (s[n - 1] === RBRACE) {
    i = s.lastIndexOf(LBRACE);
    if (i >= 0) {
      try {
        throttle = parseThrottle(s.substring(i + 1, n - 1));
      } catch (e) {
        throw "Invalid throttle specification: " + s;
      }
      s = s.slice(0, i).trim();
      n = s.length;
    } else throw "Unmatched right brace: " + s;
    i = 0;
  }
  if (!n) throw s;
  if (s[0] === NAME) markname = ++i;
  j = find3(s, i, COLON);
  if (j < n) {
    source4.push(s.substring(start, j).trim());
    start = i = ++j;
  }
  i = find3(s, i, LBRACK);
  if (i === n) {
    source4.push(s.substring(start, n).trim());
  } else {
    source4.push(s.substring(start, i).trim());
    filter4 = [];
    start = ++i;
    if (start === n) throw "Unmatched left bracket: " + s;
  }
  while (i < n) {
    i = find3(s, i, RBRACK);
    if (i === n) throw "Unmatched left bracket: " + s;
    filter4.push(s.substring(start, i).trim());
    if (i < n - 1 && s[++i] !== LBRACK) throw "Expected left bracket: " + s;
    start = ++i;
  }
  if (!(n = source4.length) || ILLEGAL2.test(source4[n - 1])) {
    throw "Invalid event selector: " + s;
  }
  if (n > 1) {
    stream2.type = source4[1];
    if (markname) {
      stream2.markname = source4[0].slice(1);
    } else if (isMarkType(source4[0])) {
      stream2.marktype = source4[0];
    } else {
      stream2.source = source4[0];
    }
  } else {
    stream2.type = source4[0];
  }
  if (stream2.type.slice(-1) === "!") {
    stream2.consume = true;
    stream2.type = stream2.type.slice(0, -1);
  }
  if (filter4 != null) stream2.filter = filter4;
  if (throttle[0]) stream2.throttle = throttle[0];
  if (throttle[1]) stream2.debounce = throttle[1];
  return stream2;
}
function parseThrottle(s) {
  const a3 = s.split(COMMA);
  if (!s.length || a3.length > 2) throw s;
  return a3.map((_) => {
    const x6 = +_;
    if (x6 !== x6) throw s;
    return x6;
  });
}

// ../deepseek-harness/node_modules/.pnpm/vega-parser@7.1.3/node_modules/vega-parser/build/vega-parser.js
function parseAutosize(spec) {
  return isObject(spec) ? spec : {
    type: spec || "pad"
  };
}
var number8 = (_) => +_ || 0;
var paddingObject2 = (_) => ({
  top: _,
  bottom: _,
  left: _,
  right: _
});
function parsePadding(spec) {
  return !isObject(spec) ? paddingObject2(number8(spec)) : spec.signal ? spec : {
    top: number8(spec.top),
    bottom: number8(spec.bottom),
    left: number8(spec.left),
    right: number8(spec.right)
  };
}
var encoder = (_) => isObject(_) && !isArray(_) ? extend({}, _) : {
  value: _
};
function addEncode(object2, name, value4, set7) {
  if (value4 != null) {
    const isEncoder = isObject(value4) && !isArray(value4) || isArray(value4) && value4.length && isObject(value4[0]);
    if (isEncoder) {
      object2.update[name] = value4;
    } else {
      object2[set7 || "enter"][name] = {
        value: value4
      };
    }
    return 1;
  } else {
    return 0;
  }
}
function addEncoders(object2, enter, update3) {
  for (const name in enter) {
    addEncode(object2, name, enter[name]);
  }
  for (const name in update3) {
    addEncode(object2, name, update3[name], "update");
  }
}
function extendEncode(encode3, extra, skip) {
  for (const name in extra) {
    if (skip && hasOwnProperty(skip, name)) continue;
    encode3[name] = extend(encode3[name] || {}, extra[name]);
  }
  return encode3;
}
function has(key2, encode3) {
  return encode3 && (encode3.enter && encode3.enter[key2] || encode3.update && encode3.update[key2]);
}
var MarkRole = "mark";
var FrameRole2 = "frame";
var ScopeRole2 = "scope";
var AxisRole2 = "axis";
var AxisDomainRole = "axis-domain";
var AxisGridRole = "axis-grid";
var AxisLabelRole = "axis-label";
var AxisTickRole = "axis-tick";
var AxisTitleRole = "axis-title";
var LegendRole2 = "legend";
var LegendBandRole = "legend-band";
var LegendEntryRole = "legend-entry";
var LegendGradientRole = "legend-gradient";
var LegendLabelRole = "legend-label";
var LegendSymbolRole = "legend-symbol";
var LegendTitleRole = "legend-title";
var TitleRole2 = "title";
var TitleTextRole = "title-text";
var TitleSubtitleRole = "title-subtitle";
function applyDefaults(encode3, type4, role, style3, config) {
  const defaults2 = {}, enter = {};
  let update3, key2, skip, props;
  key2 = "lineBreak";
  if (type4 === "text" && config[key2] != null && !has(key2, encode3)) {
    applyDefault(defaults2, key2, config[key2]);
  }
  if (role == "legend" || String(role).startsWith("axis")) {
    role = null;
  }
  props = role === FrameRole2 ? config.group : role === MarkRole ? extend({}, config.mark, config[type4]) : null;
  for (key2 in props) {
    skip = has(key2, encode3) || (key2 === "fill" || key2 === "stroke") && (has("fill", encode3) || has("stroke", encode3));
    if (!skip) applyDefault(defaults2, key2, props[key2]);
  }
  array_default(style3).forEach((name) => {
    const props2 = config.style && config.style[name];
    for (const key3 in props2) {
      if (!has(key3, encode3)) {
        applyDefault(defaults2, key3, props2[key3]);
      }
    }
  });
  encode3 = extend({}, encode3);
  for (key2 in defaults2) {
    props = defaults2[key2];
    if (props.signal) {
      (update3 = update3 || {})[key2] = props;
    } else {
      enter[key2] = props;
    }
  }
  encode3.enter = extend(enter, encode3.enter);
  if (update3) encode3.update = extend(update3, encode3.update);
  return encode3;
}
function applyDefault(defaults2, key2, value4) {
  defaults2[key2] = value4 && value4.signal ? {
    signal: value4.signal
  } : {
    value: value4
  };
}
var scaleRef = (scale9) => isString(scale9) ? stringValue(scale9) : scale9.signal ? `(${scale9.signal})` : field2(scale9);
function entry$1(enc) {
  if (enc.gradient != null) {
    return gradient3(enc);
  }
  let value4 = enc.signal ? `(${enc.signal})` : enc.color ? color4(enc.color) : enc.field != null ? field2(enc.field) : enc.value !== void 0 ? stringValue(enc.value) : void 0;
  if (enc.scale != null) {
    value4 = scale8(enc, value4);
  }
  if (value4 === void 0) {
    value4 = null;
  }
  if (enc.exponent != null) {
    value4 = `pow(${value4},${property(enc.exponent)})`;
  }
  if (enc.mult != null) {
    value4 += `*${property(enc.mult)}`;
  }
  if (enc.offset != null) {
    value4 += `+${property(enc.offset)}`;
  }
  if (enc.round) {
    value4 = `round(${value4})`;
  }
  return value4;
}
var _color = (type4, x6, y6, z) => `(${type4}(${[x6, y6, z].map(entry$1).join(",")})+'')`;
function color4(enc) {
  return enc.c ? _color("hcl", enc.h, enc.c, enc.l) : enc.h || enc.s ? _color("hsl", enc.h, enc.s, enc.l) : enc.l || enc.a ? _color("lab", enc.l, enc.a, enc.b) : enc.r || enc.g || enc.b ? _color("rgb", enc.r, enc.g, enc.b) : null;
}
function gradient3(enc) {
  const args = [enc.start, enc.stop, enc.count].map((_) => _ == null ? null : stringValue(_));
  while (args.length && peek(args) == null) args.pop();
  args.unshift(scaleRef(enc.gradient));
  return `gradient(${args.join(",")})`;
}
function property(property2) {
  return isObject(property2) ? "(" + entry$1(property2) + ")" : property2;
}
function field2(ref2) {
  return resolveField(isObject(ref2) ? ref2 : {
    datum: ref2
  });
}
function resolveField(ref2) {
  let object2, level, field3;
  if (ref2.signal) {
    object2 = "datum";
    field3 = ref2.signal;
  } else if (ref2.group || ref2.parent) {
    level = Math.max(1, ref2.level || 1);
    object2 = "item";
    while (level-- > 0) {
      object2 += ".mark.group";
    }
    if (ref2.parent) {
      field3 = ref2.parent;
      object2 += ".datum";
    } else {
      field3 = ref2.group;
    }
  } else if (ref2.datum) {
    object2 = "datum";
    field3 = ref2.datum;
  } else {
    error("Invalid field reference: " + stringValue(ref2));
  }
  if (!ref2.signal) {
    field3 = isString(field3) ? splitAccessPath(field3).map(stringValue).join("][") : resolveField(field3);
  }
  return object2 + "[" + field3 + "]";
}
function scale8(enc, value4) {
  const scale9 = scaleRef(enc.scale);
  if (enc.range != null) {
    value4 = `lerp(_range(${scale9}), ${+enc.range})`;
  } else {
    if (value4 !== void 0) value4 = `_scale(${scale9}, ${value4})`;
    if (enc.band) {
      value4 = (value4 ? value4 + "+" : "") + `_bandwidth(${scale9})` + (+enc.band === 1 ? "" : "*" + property(enc.band));
      if (enc.extra) {
        value4 = `(datum.extra ? _scale(${scale9}, datum.extra.value) : ${value4})`;
      }
    }
    if (value4 == null) value4 = "0";
  }
  return value4;
}
function rule3(enc) {
  let code = "";
  enc.forEach((rule5) => {
    const value4 = entry$1(rule5);
    code += rule5.test ? `(${rule5.test})?${value4}:` : value4;
  });
  if (peek(code) === ":") {
    code += "null";
  }
  return code;
}
function parseEncode(encode3, type4, role, style3, scope2, params2) {
  const enc = {};
  params2 = params2 || {};
  params2.encoders = {
    $encode: enc
  };
  encode3 = applyDefaults(encode3, type4, role, style3, scope2.config);
  for (const key2 in encode3) {
    enc[key2] = parseBlock(encode3[key2], type4, params2, scope2);
  }
  return params2;
}
function parseBlock(block, marktype, params2, scope2) {
  const channels = {}, fields = {};
  for (const name in block) {
    if (block[name] != null) {
      channels[name] = parse$1(expr(block[name]), scope2, params2, fields);
    }
  }
  return {
    $expr: {
      marktype,
      channels
    },
    $fields: Object.keys(fields),
    $output: Object.keys(block)
  };
}
function expr(enc) {
  return isArray(enc) ? rule3(enc) : entry$1(enc);
}
function parse$1(code, scope2, params2, fields) {
  const expr2 = parser2(code, scope2);
  expr2.$fields.forEach((name) => fields[name] = 1);
  extend(params2, expr2.$params);
  return expr2.$expr;
}
var OUTER = "outer";
var OUTER_INVALID = ["value", "update", "init", "react", "bind"];
function outerError(prefix, name) {
  error(prefix + ' for "outer" push: ' + stringValue(name));
}
function parseSignal(signal, scope2) {
  const name = signal.name;
  if (signal.push === OUTER) {
    if (!scope2.signals[name]) outerError("No prior signal definition", name);
    OUTER_INVALID.forEach((prop) => {
      if (signal[prop] !== void 0) outerError("Invalid property ", prop);
    });
  } else {
    const op = scope2.addSignal(name, signal.value);
    if (signal.react === false) op.react = false;
    if (signal.bind) scope2.addBinding(name, signal.bind);
  }
}
function Entry(type4, value4, params2, parent) {
  this.id = -1;
  this.type = type4;
  this.value = value4;
  this.params = params2;
  if (parent) this.parent = parent;
}
function entry(type4, value4, params2, parent) {
  return new Entry(type4, value4, params2, parent);
}
function operator(value4, params2) {
  return entry("operator", value4, params2);
}
function ref(op) {
  const ref2 = {
    $ref: op.id
  };
  if (op.id < 0) (op.refs = op.refs || []).push(ref2);
  return ref2;
}
function fieldRef$1(field3, name) {
  return name ? {
    $field: field3,
    $name: name
  } : {
    $field: field3
  };
}
var keyFieldRef = fieldRef$1("key");
function compareRef(fields, orders) {
  return {
    $compare: fields,
    $order: orders
  };
}
function keyRef(fields, flat) {
  const ref2 = {
    $key: fields
  };
  if (flat) ref2.$flat = true;
  return ref2;
}
var Ascending = "ascending";
var Descending = "descending";
function sortKey(sort4) {
  return !isObject(sort4) ? "" : (sort4.order === Descending ? "-" : "+") + aggrField(sort4.op, sort4.field);
}
function aggrField(op, field3) {
  return (op && op.signal ? "$" + op.signal : op || "") + (op && field3 ? "_" : "") + (field3 && field3.signal ? "$" + field3.signal : field3?.replace(/\W+/g, "_").replace(/^_+|_+$/g, "") || "");
}
var Scope$1 = "scope";
var View2 = "view";
function isSignal(_) {
  return _ && _.signal;
}
function isExpr$1(_) {
  return _ && _.expr;
}
function hasSignal(_) {
  if (isSignal(_)) return true;
  if (isObject(_)) for (const key2 in _) {
    if (hasSignal(_[key2])) return true;
  }
  return false;
}
function value3(specValue, defaultValue) {
  return specValue != null ? specValue : defaultValue;
}
function deref(v2) {
  return v2 && v2.signal || v2;
}
var Timer2 = "timer";
function parseStream3(stream2, scope2) {
  const method2 = stream2.merge ? mergeStream : stream2.stream ? nestedStream : stream2.type ? eventStream : error("Invalid stream specification: " + stringValue(stream2));
  return method2(stream2, scope2);
}
function eventSource(source4) {
  return source4 === Scope$1 ? View2 : source4 || View2;
}
function mergeStream(stream2, scope2) {
  const list = stream2.merge.map((s) => parseStream3(s, scope2)), entry2 = streamParameters({
    merge: list
  }, stream2, scope2);
  return scope2.addStream(entry2).id;
}
function nestedStream(stream2, scope2) {
  const id2 = parseStream3(stream2.stream, scope2), entry2 = streamParameters({
    stream: id2
  }, stream2, scope2);
  return scope2.addStream(entry2).id;
}
function eventStream(stream2, scope2) {
  let id2;
  if (stream2.type === Timer2) {
    id2 = scope2.event(Timer2, stream2.throttle);
    stream2 = {
      between: stream2.between,
      filter: stream2.filter
    };
  } else {
    id2 = scope2.event(eventSource(stream2.source), stream2.type);
  }
  const entry2 = streamParameters({
    stream: id2
  }, stream2, scope2);
  return Object.keys(entry2).length === 1 ? id2 : scope2.addStream(entry2).id;
}
function streamParameters(entry2, stream2, scope2) {
  let param2 = stream2.between;
  if (param2) {
    if (param2.length !== 2) {
      error('Stream "between" parameter must have 2 entries: ' + stringValue(stream2));
    }
    entry2.between = [parseStream3(param2[0], scope2), parseStream3(param2[1], scope2)];
  }
  param2 = stream2.filter ? [].concat(stream2.filter) : [];
  if (stream2.marktype || stream2.markname || stream2.markrole) {
    param2.push(filterMark(stream2.marktype, stream2.markname, stream2.markrole));
  }
  if (stream2.source === Scope$1) {
    param2.push("inScope(event.item)");
  }
  if (param2.length) {
    entry2.filter = parser2("(" + param2.join(")&&(") + ")", scope2).$expr;
  }
  if ((param2 = stream2.throttle) != null) {
    entry2.throttle = +param2;
  }
  if ((param2 = stream2.debounce) != null) {
    entry2.debounce = +param2;
  }
  if (stream2.consume) {
    entry2.consume = true;
  }
  return entry2;
}
function filterMark(type4, name, role) {
  const item = "event.item";
  return item + (type4 && type4 !== "*" ? "&&" + item + ".mark.marktype==='" + type4 + "'" : "") + (role ? "&&" + item + ".mark.role==='" + role + "'" : "") + (name ? "&&" + item + ".mark.name==='" + name + "'" : "");
}
var OP_VALUE_EXPR = {
  code: "_.$value",
  ast: {
    type: "Identifier",
    value: "value"
  }
};
function parseUpdate2(spec, scope2, target2) {
  const encode3 = spec.encode, entry2 = {
    target: target2
  };
  let events3 = spec.events, update3 = spec.update, sources = [];
  if (!events3) {
    error("Signal update missing events specification.");
  }
  if (isString(events3)) {
    events3 = eventSelector(events3, scope2.isSubscope() ? Scope$1 : View2);
  }
  events3 = array_default(events3).filter((s) => s.signal || s.scale ? (sources.push(s), 0) : 1);
  if (sources.length > 1) {
    sources = [mergeSources(sources)];
  }
  if (events3.length) {
    sources.push(events3.length > 1 ? {
      merge: events3
    } : events3[0]);
  }
  if (encode3 != null) {
    if (update3) error("Signal encode and update are mutually exclusive.");
    update3 = "encode(item()," + stringValue(encode3) + ")";
  }
  entry2.update = isString(update3) ? parser2(update3, scope2) : update3.expr != null ? parser2(update3.expr, scope2) : update3.value != null ? update3.value : update3.signal != null ? {
    $expr: OP_VALUE_EXPR,
    $params: {
      $value: scope2.signalRef(update3.signal)
    }
  } : error("Invalid signal update specification.");
  if (spec.force) {
    entry2.options = {
      force: true
    };
  }
  sources.forEach((source4) => scope2.addUpdate(extend(streamSource(source4, scope2), entry2)));
}
function streamSource(stream2, scope2) {
  return {
    source: stream2.signal ? scope2.signalRef(stream2.signal) : stream2.scale ? scope2.scaleRef(stream2.scale) : parseStream3(stream2, scope2)
  };
}
function mergeSources(sources) {
  return {
    signal: "[" + sources.map((s) => s.scale ? 'scale("' + s.scale + '")' : s.signal) + "]"
  };
}
function parseSignalUpdates(signal, scope2) {
  const op = scope2.getSignal(signal.name);
  let expr2 = signal.update;
  if (signal.init) {
    if (expr2) {
      error("Signals can not include both init and update expressions.");
    } else {
      expr2 = signal.init;
      op.initonly = true;
    }
  }
  if (expr2) {
    expr2 = parser2(expr2, scope2);
    op.update = expr2.$expr;
    op.params = expr2.$params;
  }
  if (signal.on) {
    signal.on.forEach((_) => parseUpdate2(_, scope2, op.id));
  }
}
var transform3 = (name) => (params2, value4, parent) => entry(name, value4, params2 || void 0, parent);
var Aggregate2 = transform3("aggregate");
var AxisTicks2 = transform3("axisticks");
var Bound2 = transform3("bound");
var Collect2 = transform3("collect");
var Compare2 = transform3("compare");
var DataJoin2 = transform3("datajoin");
var Encode2 = transform3("encode");
var Expression2 = transform3("expression");
var Facet2 = transform3("facet");
var Field2 = transform3("field");
var Key2 = transform3("key");
var LegendEntries2 = transform3("legendentries");
var Load2 = transform3("load");
var Mark2 = transform3("mark");
var MultiExtent2 = transform3("multiextent");
var MultiValues2 = transform3("multivalues");
var Overlap2 = transform3("overlap");
var Params3 = transform3("params");
var PreFacet2 = transform3("prefacet");
var Projection2 = transform3("projection");
var Proxy3 = transform3("proxy");
var Relay2 = transform3("relay");
var Render2 = transform3("render");
var Scale2 = transform3("scale");
var Sieve2 = transform3("sieve");
var SortItems2 = transform3("sortitems");
var ViewLayout2 = transform3("viewlayout");
var Values2 = transform3("values");
var FIELD_REF_ID = 0;
var MULTIDOMAIN_SORT_OPS = {
  min: "min",
  max: "max",
  count: "sum"
};
function initScale(spec, scope2) {
  const type4 = spec.type || "linear";
  if (!isValidScaleType(type4)) {
    error("Unrecognized scale type: " + stringValue(type4));
  }
  scope2.addScale(spec.name, {
    type: type4,
    domain: void 0
  });
}
function parseScale(spec, scope2) {
  const params2 = scope2.getScale(spec.name).params;
  let key2;
  params2.domain = parseScaleDomain(spec.domain, spec, scope2);
  if (spec.range != null) {
    params2.range = parseScaleRange(spec, scope2, params2);
  }
  if (spec.interpolate != null) {
    parseScaleInterpolate(spec.interpolate, params2);
  }
  if (spec.nice != null) {
    params2.nice = parseScaleNice(spec.nice, scope2);
  }
  if (spec.bins != null) {
    params2.bins = parseScaleBins(spec.bins, scope2);
  }
  for (key2 in spec) {
    if (hasOwnProperty(params2, key2) || key2 === "name") continue;
    params2[key2] = parseLiteral(spec[key2], scope2);
  }
}
function parseLiteral(v2, scope2) {
  return !isObject(v2) ? v2 : v2.signal ? scope2.signalRef(v2.signal) : error("Unsupported object: " + stringValue(v2));
}
function parseArray(v2, scope2) {
  return v2.signal ? scope2.signalRef(v2.signal) : v2.map((v3) => parseLiteral(v3, scope2));
}
function dataLookupError(name) {
  error("Can not find data set: " + stringValue(name));
}
function parseScaleDomain(domain4, spec, scope2) {
  if (!domain4) {
    if (spec.domainMin != null || spec.domainMax != null) {
      error("No scale domain defined for domainMin/domainMax to override.");
    }
    return;
  }
  return domain4.signal ? scope2.signalRef(domain4.signal) : (isArray(domain4) ? explicitDomain : domain4.fields ? multipleDomain : singularDomain)(domain4, spec, scope2);
}
function explicitDomain(domain4, spec, scope2) {
  return domain4.map((v2) => parseLiteral(v2, scope2));
}
function singularDomain(domain4, spec, scope2) {
  const data4 = scope2.getData(domain4.data);
  if (!data4) dataLookupError(domain4.data);
  return isDiscrete(spec.type) ? data4.valuesRef(scope2, domain4.field, parseSort(domain4.sort, false)) : isQuantile(spec.type) ? data4.domainRef(scope2, domain4.field) : data4.extentRef(scope2, domain4.field);
}
function multipleDomain(domain4, spec, scope2) {
  const data4 = domain4.data, fields = domain4.fields.reduce((dom, d) => {
    d = isString(d) ? {
      data: data4,
      field: d
    } : isArray(d) || d.signal ? fieldRef(d, scope2) : d;
    dom.push(d);
    return dom;
  }, []);
  return (isDiscrete(spec.type) ? ordinalMultipleDomain : isQuantile(spec.type) ? quantileMultipleDomain : numericMultipleDomain)(domain4, scope2, fields);
}
function fieldRef(data4, scope2) {
  const name = "_:vega:_" + FIELD_REF_ID++, coll = Collect2({});
  if (isArray(data4) && !data4.some(isSignal)) {
    coll.value = {
      $ingest: data4
    };
  } else {
    const values4 = isArray(data4) ? "[" + data4.map((v2) => isSignal(v2) ? v2.signal : stringValue(v2)).join(",") + "]" : data4.signal;
    const code = "setdata(" + stringValue(name) + "," + values4 + ")";
    coll.params.input = scope2.signalRef(code);
  }
  scope2.addDataPipeline(name, [coll, Sieve2({})]);
  return {
    data: name,
    field: "data"
  };
}
function ordinalMultipleDomain(domain4, scope2, fields) {
  const sort4 = parseSort(domain4.sort, true);
  let a3, v2;
  const counts = fields.map((f) => {
    const data4 = scope2.getData(f.data);
    if (!data4) dataLookupError(f.data);
    return data4.countsRef(scope2, f.field, sort4);
  });
  const p = {
    groupby: keyFieldRef,
    pulse: counts
  };
  if (sort4) {
    a3 = sort4.op || "count";
    v2 = sort4.field ? aggrField(a3, sort4.field) : "count";
    p.ops = [MULTIDOMAIN_SORT_OPS[a3]];
    p.fields = [scope2.fieldRef(v2)];
    p.as = [v2];
  }
  a3 = scope2.add(Aggregate2(p));
  const c3 = scope2.add(Collect2({
    pulse: ref(a3)
  }));
  v2 = scope2.add(Values2({
    field: keyFieldRef,
    sort: scope2.sortRef(sort4),
    pulse: ref(c3)
  }));
  return ref(v2);
}
function parseSort(sort4, multidomain) {
  if (sort4) {
    if (!sort4.field && !sort4.op) {
      if (isObject(sort4)) sort4.field = "key";
      else sort4 = {
        field: "key"
      };
    } else if (!sort4.field && sort4.op !== "count") {
      error("No field provided for sort aggregate op: " + sort4.op);
    } else if (multidomain && sort4.field) {
      if (sort4.op && !MULTIDOMAIN_SORT_OPS[sort4.op]) {
        error("Multiple domain scales can not be sorted using " + sort4.op);
      }
    }
  }
  return sort4;
}
function quantileMultipleDomain(domain4, scope2, fields) {
  const values4 = fields.map((f) => {
    const data4 = scope2.getData(f.data);
    if (!data4) dataLookupError(f.data);
    return data4.domainRef(scope2, f.field);
  });
  return ref(scope2.add(MultiValues2({
    values: values4
  })));
}
function numericMultipleDomain(domain4, scope2, fields) {
  const extents = fields.map((f) => {
    const data4 = scope2.getData(f.data);
    if (!data4) dataLookupError(f.data);
    return data4.extentRef(scope2, f.field);
  });
  return ref(scope2.add(MultiExtent2({
    extents
  })));
}
function parseScaleBins(v2, scope2) {
  return v2.signal || isArray(v2) ? parseArray(v2, scope2) : scope2.objectProperty(v2);
}
function parseScaleNice(nice3, scope2) {
  return nice3.signal ? scope2.signalRef(nice3.signal) : isObject(nice3) ? {
    interval: parseLiteral(nice3.interval),
    step: parseLiteral(nice3.step)
  } : parseLiteral(nice3);
}
function parseScaleInterpolate(interpolate3, params2) {
  params2.interpolate = parseLiteral(interpolate3.type || interpolate3);
  if (interpolate3.gamma != null) {
    params2.interpolateGamma = parseLiteral(interpolate3.gamma);
  }
}
function parseScaleRange(spec, scope2, params2) {
  const config = scope2.config.range;
  let range7 = spec.range;
  if (range7.signal) {
    return scope2.signalRef(range7.signal);
  } else if (isString(range7)) {
    if (config && hasOwnProperty(config, range7)) {
      spec = extend({}, spec, {
        range: config[range7]
      });
      return parseScaleRange(spec, scope2, params2);
    } else if (range7 === "width") {
      range7 = [0, {
        signal: "width"
      }];
    } else if (range7 === "height") {
      range7 = isDiscrete(spec.type) ? [0, {
        signal: "height"
      }] : [{
        signal: "height"
      }, 0];
    } else {
      error("Unrecognized scale range value: " + stringValue(range7));
    }
  } else if (range7.scheme) {
    params2.scheme = isArray(range7.scheme) ? parseArray(range7.scheme, scope2) : parseLiteral(range7.scheme, scope2);
    if (range7.extent) params2.schemeExtent = parseArray(range7.extent, scope2);
    if (range7.count) params2.schemeCount = parseLiteral(range7.count, scope2);
    return;
  } else if (range7.step) {
    params2.rangeStep = parseLiteral(range7.step, scope2);
    return;
  } else if (isDiscrete(spec.type) && !isArray(range7)) {
    return parseScaleDomain(range7, spec, scope2);
  } else if (!isArray(range7)) {
    error("Unsupported range type: " + stringValue(range7));
  }
  return range7.map((v2) => (isArray(v2) ? parseArray : parseLiteral)(v2, scope2));
}
function parseProjection(proj, scope2) {
  const config = scope2.config.projection || {}, params2 = {};
  for (const name in proj) {
    if (name === "name") continue;
    params2[name] = parseParameter$1(proj[name], name, scope2);
  }
  for (const name in config) {
    if (params2[name] == null) {
      params2[name] = parseParameter$1(config[name], name, scope2);
    }
  }
  scope2.addProjection(proj.name, params2);
}
function parseParameter$1(_, name, scope2) {
  return isArray(_) ? _.map((_2) => parseParameter$1(_2, name, scope2)) : !isObject(_) ? _ : _.signal ? scope2.signalRef(_.signal) : name === "fit" ? _ : error("Unsupported parameter object: " + stringValue(_));
}
var Top2 = "top";
var Left2 = "left";
var Right2 = "right";
var Bottom2 = "bottom";
var Center2 = "center";
var Vertical = "vertical";
var Start2 = "start";
var Middle2 = "middle";
var End2 = "end";
var Index = "index";
var Label2 = "label";
var Offset = "offset";
var Perc = "perc";
var Perc2 = "perc2";
var Value = "value";
var GuideLabelStyle = "guide-label";
var GuideTitleStyle = "guide-title";
var GroupTitleStyle = "group-title";
var GroupSubtitleStyle = "group-subtitle";
var Symbols2 = "symbol";
var Gradient3 = "gradient";
var Discrete2 = "discrete";
var Size = "size";
var Shape = "shape";
var Fill = "fill";
var Stroke = "stroke";
var StrokeWidth = "strokeWidth";
var StrokeDash = "strokeDash";
var Opacity = "opacity";
var LegendScales = [Size, Shape, Fill, Stroke, StrokeWidth, StrokeDash, Opacity];
var Skip3 = {
  name: 1,
  style: 1,
  interactive: 1
};
var zero4 = {
  value: 0
};
var one3 = {
  value: 1
};
var GroupMark = "group";
var RectMark = "rect";
var RuleMark = "rule";
var SymbolMark = "symbol";
var TextMark = "text";
function guideGroup(mark) {
  mark.type = GroupMark;
  mark.interactive = mark.interactive || false;
  return mark;
}
function lookup6(spec, config) {
  const _ = (name, dflt) => value3(spec[name], value3(config[name], dflt));
  _.isVertical = (s) => Vertical === value3(spec.direction, config.direction || (s ? config.symbolDirection : config.gradientDirection));
  _.gradientLength = () => value3(spec.gradientLength, config.gradientLength || config.gradientWidth);
  _.gradientThickness = () => value3(spec.gradientThickness, config.gradientThickness || config.gradientHeight);
  _.entryColumns = () => value3(spec.columns, value3(config.columns, +_.isVertical(true)));
  return _;
}
function getEncoding(name, encode3) {
  const v2 = encode3 && (encode3.update && encode3.update[name] || encode3.enter && encode3.enter[name]);
  return v2 && v2.signal ? v2 : v2 ? v2.value : null;
}
function getStyle(name, scope2, style3) {
  const s = scope2.config.style[style3];
  return s && s[name];
}
function anchorExpr(s, e, m3) {
  return `item.anchor === '${Start2}' ? ${s} : item.anchor === '${End2}' ? ${e} : ${m3}`;
}
var alignExpr$1 = anchorExpr(stringValue(Left2), stringValue(Right2), stringValue(Center2));
function tickBand(_) {
  const v2 = _("tickBand");
  let offset6 = _("tickOffset"), band2, extra;
  if (!v2) {
    band2 = _("bandPosition");
    extra = _("tickExtra");
  } else if (v2.signal) {
    band2 = {
      signal: `(${v2.signal}) === 'extent' ? 1 : 0.5`
    };
    extra = {
      signal: `(${v2.signal}) === 'extent'`
    };
    if (!isObject(offset6)) {
      offset6 = {
        signal: `(${v2.signal}) === 'extent' ? 0 : ${offset6}`
      };
    }
  } else if (v2 === "extent") {
    band2 = 1;
    extra = true;
    offset6 = 0;
  } else {
    band2 = 0.5;
    extra = false;
  }
  return {
    extra,
    band: band2,
    offset: offset6
  };
}
function extendOffset(value4, offset6) {
  return !offset6 ? value4 : !value4 ? offset6 : !isObject(value4) ? {
    value: value4,
    offset: offset6
  } : Object.assign({}, value4, {
    offset: extendOffset(value4.offset, offset6)
  });
}
function guideMark(mark, extras) {
  if (extras) {
    mark.name = extras.name;
    mark.style = extras.style || mark.style;
    mark.interactive = !!extras.interactive;
    mark.encode = extendEncode(mark.encode, extras, Skip3);
  } else {
    mark.interactive = false;
  }
  return mark;
}
function legendGradient(spec, scale9, config, userEncode) {
  const _ = lookup6(spec, config), vertical = _.isVertical(), thickness = _.gradientThickness(), length3 = _.gradientLength();
  let enter, start, stop2, width2, height2;
  if (vertical) {
    start = [0, 1];
    stop2 = [0, 0];
    width2 = thickness;
    height2 = length3;
  } else {
    start = [0, 0];
    stop2 = [1, 0];
    width2 = length3;
    height2 = thickness;
  }
  const encode3 = {
    enter: enter = {
      opacity: zero4,
      x: zero4,
      y: zero4,
      width: encoder(width2),
      height: encoder(height2)
    },
    update: extend({}, enter, {
      opacity: one3,
      fill: {
        gradient: scale9,
        start,
        stop: stop2
      }
    }),
    exit: {
      opacity: zero4
    }
  };
  addEncoders(encode3, {
    stroke: _("gradientStrokeColor"),
    strokeWidth: _("gradientStrokeWidth")
  }, {
    // update
    opacity: _("gradientOpacity")
  });
  return guideMark({
    type: RectMark,
    role: LegendGradientRole,
    encode: encode3
  }, userEncode);
}
function legendGradientDiscrete(spec, scale9, config, userEncode, dataRef) {
  const _ = lookup6(spec, config), vertical = _.isVertical(), thickness = _.gradientThickness(), length3 = _.gradientLength();
  let u4, v2, uu, vv, adjust = "";
  vertical ? (u4 = "y", uu = "y2", v2 = "x", vv = "width", adjust = "1-") : (u4 = "x", uu = "x2", v2 = "y", vv = "height");
  const enter = {
    opacity: zero4,
    fill: {
      scale: scale9,
      field: Value
    }
  };
  enter[u4] = {
    signal: adjust + "datum." + Perc,
    mult: length3
  };
  enter[v2] = zero4;
  enter[uu] = {
    signal: adjust + "datum." + Perc2,
    mult: length3
  };
  enter[vv] = encoder(thickness);
  const encode3 = {
    enter,
    update: extend({}, enter, {
      opacity: one3
    }),
    exit: {
      opacity: zero4
    }
  };
  addEncoders(encode3, {
    stroke: _("gradientStrokeColor"),
    strokeWidth: _("gradientStrokeWidth")
  }, {
    // update
    opacity: _("gradientOpacity")
  });
  return guideMark({
    type: RectMark,
    role: LegendBandRole,
    key: Value,
    from: dataRef,
    encode: encode3
  }, userEncode);
}
var alignExpr = `datum.${Perc}<=0?"${Left2}":datum.${Perc}>=1?"${Right2}":"${Center2}"`;
var baselineExpr = `datum.${Perc}<=0?"${Bottom2}":datum.${Perc}>=1?"${Top2}":"${Middle2}"`;
function legendGradientLabels(spec, config, userEncode, dataRef) {
  const _ = lookup6(spec, config), vertical = _.isVertical(), thickness = encoder(_.gradientThickness()), length3 = _.gradientLength();
  let overlap = _("labelOverlap"), enter, update3, u4, v2, adjust = "";
  const encode3 = {
    enter: enter = {
      opacity: zero4
    },
    update: update3 = {
      opacity: one3,
      text: {
        field: Label2
      }
    },
    exit: {
      opacity: zero4
    }
  };
  addEncoders(encode3, {
    fill: _("labelColor"),
    fillOpacity: _("labelOpacity"),
    font: _("labelFont"),
    fontSize: _("labelFontSize"),
    fontStyle: _("labelFontStyle"),
    fontWeight: _("labelFontWeight"),
    limit: value3(spec.labelLimit, config.gradientLabelLimit)
  });
  if (vertical) {
    enter.align = {
      value: "left"
    };
    enter.baseline = update3.baseline = {
      signal: baselineExpr
    };
    u4 = "y";
    v2 = "x";
    adjust = "1-";
  } else {
    enter.align = update3.align = {
      signal: alignExpr
    };
    enter.baseline = {
      value: "top"
    };
    u4 = "x";
    v2 = "y";
  }
  enter[u4] = update3[u4] = {
    signal: adjust + "datum." + Perc,
    mult: length3
  };
  enter[v2] = update3[v2] = thickness;
  thickness.offset = value3(spec.labelOffset, config.gradientLabelOffset) || 0;
  overlap = overlap ? {
    separation: _("labelSeparation"),
    method: overlap,
    order: "datum." + Index
  } : void 0;
  return guideMark({
    type: TextMark,
    role: LegendLabelRole,
    style: GuideLabelStyle,
    key: Value,
    from: dataRef,
    encode: encode3,
    overlap
  }, userEncode);
}
function legendSymbolGroups(spec, config, userEncode, dataRef, columns) {
  const _ = lookup6(spec, config), entries2 = userEncode.entries, interactive2 = !!(entries2 && entries2.interactive), name = entries2 ? entries2.name : void 0, height2 = _("clipHeight"), symbolOffset = _("symbolOffset"), valueRef = {
    data: "value"
  }, xSignal = `(${columns}) ? datum.${Offset} : datum.${Size}`, yEncode = height2 ? encoder(height2) : {
    field: Size
  }, index4 = `datum.${Index}`, ncols = `max(1, ${columns})`;
  let encode3, enter, update3, nrows, sort4;
  yEncode.mult = 0.5;
  encode3 = {
    enter: enter = {
      opacity: zero4,
      x: {
        signal: xSignal,
        mult: 0.5,
        offset: symbolOffset
      },
      y: yEncode
    },
    update: update3 = {
      opacity: one3,
      x: enter.x,
      y: enter.y
    },
    exit: {
      opacity: zero4
    }
  };
  let baseFill = null, baseStroke = null;
  if (!spec.fill) {
    baseFill = config.symbolBaseFillColor;
    baseStroke = config.symbolBaseStrokeColor;
  }
  addEncoders(encode3, {
    fill: _("symbolFillColor", baseFill),
    shape: _("symbolType"),
    size: _("symbolSize"),
    stroke: _("symbolStrokeColor", baseStroke),
    strokeDash: _("symbolDash"),
    strokeDashOffset: _("symbolDashOffset"),
    strokeWidth: _("symbolStrokeWidth")
  }, {
    // update
    opacity: _("symbolOpacity")
  });
  LegendScales.forEach((scale9) => {
    if (spec[scale9]) {
      update3[scale9] = enter[scale9] = {
        scale: spec[scale9],
        field: Value
      };
    }
  });
  const symbols5 = guideMark({
    type: SymbolMark,
    role: LegendSymbolRole,
    key: Value,
    from: valueRef,
    clip: height2 ? true : void 0,
    encode: encode3
  }, userEncode.symbols);
  const labelOffset = encoder(symbolOffset);
  labelOffset.offset = _("labelOffset");
  encode3 = {
    enter: enter = {
      opacity: zero4,
      x: {
        signal: xSignal,
        offset: labelOffset
      },
      y: yEncode
    },
    update: update3 = {
      opacity: one3,
      text: {
        field: Label2
      },
      x: enter.x,
      y: enter.y
    },
    exit: {
      opacity: zero4
    }
  };
  addEncoders(encode3, {
    align: _("labelAlign"),
    baseline: _("labelBaseline"),
    fill: _("labelColor"),
    fillOpacity: _("labelOpacity"),
    font: _("labelFont"),
    fontSize: _("labelFontSize"),
    fontStyle: _("labelFontStyle"),
    fontWeight: _("labelFontWeight"),
    limit: _("labelLimit")
  });
  const labels2 = guideMark({
    type: TextMark,
    role: LegendLabelRole,
    style: GuideLabelStyle,
    key: Value,
    from: valueRef,
    encode: encode3
  }, userEncode.labels);
  encode3 = {
    enter: {
      noBound: {
        value: !height2
      },
      // ignore width/height in bounds calc
      width: zero4,
      height: height2 ? encoder(height2) : zero4,
      opacity: zero4
    },
    exit: {
      opacity: zero4
    },
    update: update3 = {
      opacity: one3,
      row: {
        signal: null
      },
      column: {
        signal: null
      }
    }
  };
  if (_.isVertical(true)) {
    nrows = `ceil(item.mark.items.length / ${ncols})`;
    update3.row.signal = `${index4}%${nrows}`;
    update3.column.signal = `floor(${index4} / ${nrows})`;
    sort4 = {
      field: ["row", index4]
    };
  } else {
    update3.row.signal = `floor(${index4} / ${ncols})`;
    update3.column.signal = `${index4} % ${ncols}`;
    sort4 = {
      field: index4
    };
  }
  update3.column.signal = `(${columns})?${update3.column.signal}:${index4}`;
  dataRef = {
    facet: {
      data: dataRef,
      name: "value",
      groupby: Index
    }
  };
  return guideGroup({
    role: ScopeRole2,
    from: dataRef,
    encode: extendEncode(encode3, entries2, Skip3),
    marks: [symbols5, labels2],
    name,
    interactive: interactive2,
    sort: sort4
  });
}
function legendSymbolLayout(spec, config) {
  const _ = lookup6(spec, config);
  return {
    align: _("gridAlign"),
    columns: _.entryColumns(),
    center: {
      row: true,
      column: false
    },
    padding: {
      row: _("rowPadding"),
      column: _("columnPadding")
    }
  };
}
var isL = 'item.orient === "left"';
var isR = 'item.orient === "right"';
var isLR = `(${isL} || ${isR})`;
var isVG = `datum.vgrad && ${isLR}`;
var baseline = anchorExpr('"top"', '"bottom"', '"middle"');
var alignFlip = anchorExpr('"right"', '"left"', '"center"');
var exprAlign = `datum.vgrad && ${isR} ? (${alignFlip}) : (${isLR} && !(datum.vgrad && ${isL})) ? "left" : ${alignExpr$1}`;
var exprAnchor = `item._anchor || (${isLR} ? "middle" : "start")`;
var exprAngle = `${isVG} ? (${isL} ? -90 : 90) : 0`;
var exprBaseline = `${isLR} ? (datum.vgrad ? (${isR} ? "bottom" : "top") : ${baseline}) : "top"`;
function legendTitle(spec, config, userEncode, dataRef) {
  const _ = lookup6(spec, config);
  const encode3 = {
    enter: {
      opacity: zero4
    },
    update: {
      opacity: one3,
      x: {
        field: {
          group: "padding"
        }
      },
      y: {
        field: {
          group: "padding"
        }
      }
    },
    exit: {
      opacity: zero4
    }
  };
  addEncoders(encode3, {
    orient: _("titleOrient"),
    _anchor: _("titleAnchor"),
    anchor: {
      signal: exprAnchor
    },
    angle: {
      signal: exprAngle
    },
    align: {
      signal: exprAlign
    },
    baseline: {
      signal: exprBaseline
    },
    text: spec.title,
    fill: _("titleColor"),
    fillOpacity: _("titleOpacity"),
    font: _("titleFont"),
    fontSize: _("titleFontSize"),
    fontStyle: _("titleFontStyle"),
    fontWeight: _("titleFontWeight"),
    limit: _("titleLimit"),
    lineHeight: _("titleLineHeight")
  }, {
    // require update
    align: _("titleAlign"),
    baseline: _("titleBaseline")
  });
  return guideMark({
    type: TextMark,
    role: LegendTitleRole,
    style: GuideTitleStyle,
    from: dataRef,
    encode: encode3
  }, userEncode);
}
function clip3(clip4, scope2) {
  let expr2;
  if (isObject(clip4)) {
    if (clip4.signal) {
      expr2 = clip4.signal;
    } else if (clip4.path) {
      expr2 = "pathShape(" + param(clip4.path) + ")";
    } else if (clip4.sphere) {
      expr2 = "geoShape(" + param(clip4.sphere) + ', {type: "Sphere"})';
    }
  }
  return expr2 ? scope2.signalRef(expr2) : !!clip4;
}
function param(value4) {
  return isObject(value4) && value4.signal ? value4.signal : stringValue(value4);
}
function getRole(spec) {
  const role = spec.role || "";
  return role.startsWith("axis") || role.startsWith("legend") || role.startsWith("title") ? role : spec.type === GroupMark ? ScopeRole2 : role || MarkRole;
}
function definition2(spec) {
  return {
    marktype: spec.type,
    name: spec.name || void 0,
    role: spec.role || getRole(spec),
    zindex: +spec.zindex || void 0,
    aria: spec.aria,
    description: spec.description
  };
}
function interactive(spec, scope2) {
  return spec && spec.signal ? scope2.signalRef(spec.signal) : spec === false ? false : true;
}
function parseTransform(spec, scope2) {
  const def3 = definition(spec.type);
  if (!def3) error("Unrecognized transform type: " + stringValue(spec.type));
  const t = entry(def3.type.toLowerCase(), null, parseParameters2(def3, spec, scope2));
  if (spec.signal) scope2.addSignal(spec.signal, scope2.proxy(t));
  t.metadata = def3.metadata || {};
  return t;
}
function parseParameters2(def3, spec, scope2) {
  const params2 = {}, n = def3.params.length;
  for (let i = 0; i < n; ++i) {
    const pdef = def3.params[i];
    params2[pdef.name] = parseParameter2(pdef, spec, scope2);
  }
  return params2;
}
function parseParameter2(def3, spec, scope2) {
  const type4 = def3.type, value4 = spec[def3.name];
  if (type4 === "index") {
    return parseIndexParameter(def3, spec, scope2);
  } else if (value4 === void 0) {
    if (def3.required) {
      error("Missing required " + stringValue(spec.type) + " parameter: " + stringValue(def3.name));
    }
    return;
  } else if (type4 === "param") {
    return parseSubParameters(def3, spec, scope2);
  } else if (type4 === "projection") {
    return scope2.projectionRef(spec[def3.name]);
  }
  return def3.array && !isSignal(value4) ? value4.map((v2) => parameterValue(def3, v2, scope2)) : parameterValue(def3, value4, scope2);
}
function parameterValue(def3, value4, scope2) {
  const type4 = def3.type;
  if (isSignal(value4)) {
    return isExpr(type4) ? error("Expression references can not be signals.") : isField(type4) ? scope2.fieldRef(value4) : isCompare(type4) ? scope2.compareRef(value4) : scope2.signalRef(value4.signal);
  } else {
    const expr2 = def3.expr || isField(type4);
    return expr2 && outerExpr(value4) ? scope2.exprRef(value4.expr, value4.as) : expr2 && outerField(value4) ? fieldRef$1(value4.field, value4.as) : isExpr(type4) ? parser2(value4, scope2) : isData(type4) ? ref(scope2.getData(value4).values) : isField(type4) ? fieldRef$1(value4) : isCompare(type4) ? scope2.compareRef(value4) : value4;
  }
}
function parseIndexParameter(def3, spec, scope2) {
  if (!isString(spec.from)) {
    error('Lookup "from" parameter must be a string literal.');
  }
  return scope2.getData(spec.from).lookupRef(scope2, spec.key);
}
function parseSubParameters(def3, spec, scope2) {
  const value4 = spec[def3.name];
  if (def3.array) {
    if (!isArray(value4)) {
      error("Expected an array of sub-parameters. Instead: " + stringValue(value4));
    }
    return value4.map((v2) => parseSubParameter(def3, v2, scope2));
  } else {
    return parseSubParameter(def3, value4, scope2);
  }
}
function parseSubParameter(def3, value4, scope2) {
  const n = def3.params.length;
  let pdef;
  for (let i = 0; i < n; ++i) {
    pdef = def3.params[i];
    for (const k in pdef.key) {
      if (pdef.key[k] !== value4[k]) {
        pdef = null;
        break;
      }
    }
    if (pdef) break;
  }
  if (!pdef) error("Unsupported parameter: " + stringValue(value4));
  const params2 = extend(parseParameters2(pdef, value4, scope2), pdef.key);
  return ref(scope2.add(Params3(params2)));
}
var outerExpr = (_) => _ && _.expr;
var outerField = (_) => _ && _.field;
var isData = (_) => _ === "data";
var isExpr = (_) => _ === "expr";
var isField = (_) => _ === "field";
var isCompare = (_) => _ === "compare";
function parseData$1(from, group3, scope2) {
  let facet, key2, op, dataRef, parent;
  if (!from) {
    dataRef = ref(scope2.add(Collect2(null, [{}])));
  } else if (facet = from.facet) {
    if (!group3) error("Only group marks can be faceted.");
    if (facet.field != null) {
      dataRef = parent = getDataRef(facet, scope2);
    } else {
      if (!from.data) {
        op = parseTransform(extend({
          type: "aggregate",
          groupby: array_default(facet.groupby)
        }, facet.aggregate), scope2);
        op.params.key = scope2.keyRef(facet.groupby);
        op.params.pulse = getDataRef(facet, scope2);
        dataRef = parent = ref(scope2.add(op));
      } else {
        parent = ref(scope2.getData(from.data).aggregate);
      }
      key2 = scope2.keyRef(facet.groupby, true);
    }
  }
  if (!dataRef) {
    dataRef = getDataRef(from, scope2);
  }
  return {
    key: key2,
    pulse: dataRef,
    parent
  };
}
function getDataRef(from, scope2) {
  return from.$ref ? from : from.data && from.data.$ref ? from.data : ref(scope2.getData(from.data).output);
}
function DataScope(scope2, input, output3, values4, aggr) {
  this.scope = scope2;
  this.input = input;
  this.output = output3;
  this.values = values4;
  this.aggregate = aggr;
  this.index = {};
}
DataScope.fromEntries = function(scope2, entries2) {
  const n = entries2.length, values4 = entries2[n - 1], output3 = entries2[n - 2];
  let input = entries2[0], aggr = null, i = 1;
  if (input && input.type === "load") {
    input = entries2[1];
  }
  scope2.add(entries2[0]);
  for (; i < n; ++i) {
    entries2[i].params.pulse = ref(entries2[i - 1]);
    scope2.add(entries2[i]);
    if (entries2[i].type === "aggregate") aggr = entries2[i];
  }
  return new DataScope(scope2, input, output3, values4, aggr);
};
function fieldKey(field3) {
  return isString(field3) ? field3 : null;
}
function addSortField(scope2, p, sort4) {
  const as = aggrField(sort4.op, sort4.field);
  let s;
  if (p.ops) {
    for (let i = 0, n = p.as.length; i < n; ++i) {
      if (p.as[i] === as) return;
    }
  } else {
    p.ops = ["count"];
    p.fields = [null];
    p.as = ["count"];
  }
  if (sort4.op) {
    p.ops.push((s = sort4.op.signal) ? scope2.signalRef(s) : sort4.op);
    p.fields.push(scope2.fieldRef(sort4.field));
    p.as.push(as);
  }
}
function cache(scope2, ds, name, optype, field3, counts, index4) {
  const cache2 = ds[name] || (ds[name] = {}), sort4 = sortKey(counts);
  let k = fieldKey(field3), v2, op;
  if (k != null) {
    scope2 = ds.scope;
    k = k + (sort4 ? "|" + sort4 : "");
    v2 = cache2[k];
  }
  if (!v2) {
    const params2 = counts ? {
      field: keyFieldRef,
      pulse: ds.countsRef(scope2, field3, counts)
    } : {
      field: scope2.fieldRef(field3),
      pulse: ref(ds.output)
    };
    if (sort4) params2.sort = scope2.sortRef(counts);
    op = scope2.add(entry(optype, void 0, params2));
    if (index4) ds.index[field3] = op;
    v2 = ref(op);
    if (k != null) cache2[k] = v2;
  }
  return v2;
}
DataScope.prototype = {
  countsRef(scope2, field3, sort4) {
    const ds = this, cache2 = ds.counts || (ds.counts = {}), k = fieldKey(field3);
    let v2, a3, p;
    if (k != null) {
      scope2 = ds.scope;
      v2 = cache2[k];
    }
    if (!v2) {
      p = {
        groupby: scope2.fieldRef(field3, "key"),
        pulse: ref(ds.output)
      };
      if (sort4 && sort4.field) addSortField(scope2, p, sort4);
      a3 = scope2.add(Aggregate2(p));
      v2 = scope2.add(Collect2({
        pulse: ref(a3)
      }));
      v2 = {
        agg: a3,
        ref: ref(v2)
      };
      if (k != null) cache2[k] = v2;
    } else if (sort4 && sort4.field) {
      addSortField(scope2, v2.agg.params, sort4);
    }
    return v2.ref;
  },
  tuplesRef() {
    return ref(this.values);
  },
  extentRef(scope2, field3) {
    return cache(scope2, this, "extent", "extent", field3, false);
  },
  domainRef(scope2, field3) {
    return cache(scope2, this, "domain", "values", field3, false);
  },
  valuesRef(scope2, field3, sort4) {
    return cache(scope2, this, "vals", "values", field3, sort4 || true);
  },
  lookupRef(scope2, field3) {
    return cache(scope2, this, "lookup", "tupleindex", field3, false);
  },
  indataRef(scope2, field3) {
    return cache(scope2, this, "indata", "tupleindex", field3, true, true);
  }
};
function parseFacet(spec, scope2, group3) {
  const facet = spec.from.facet, name = facet.name, data4 = getDataRef(facet, scope2);
  let op;
  if (!facet.name) {
    error("Facet must have a name: " + stringValue(facet));
  }
  if (!facet.data) {
    error("Facet must reference a data set: " + stringValue(facet));
  }
  if (facet.field) {
    op = scope2.add(PreFacet2({
      field: scope2.fieldRef(facet.field),
      pulse: data4
    }));
  } else if (facet.groupby) {
    op = scope2.add(Facet2({
      key: scope2.keyRef(facet.groupby),
      group: ref(scope2.proxy(group3.parent)),
      pulse: data4
    }));
  } else {
    error("Facet must specify groupby or field: " + stringValue(facet));
  }
  const subscope = scope2.fork(), source4 = subscope.add(Collect2()), values4 = subscope.add(Sieve2({
    pulse: ref(source4)
  }));
  subscope.addData(name, new DataScope(subscope, source4, source4, values4));
  subscope.addSignal("parent", null);
  op.params.subflow = {
    $subflow: subscope.parse(spec).toRuntime()
  };
}
function parseSubflow(spec, scope2, input) {
  const op = scope2.add(PreFacet2({
    pulse: input.pulse
  })), subscope = scope2.fork();
  subscope.add(Sieve2());
  subscope.addSignal("parent", null);
  op.params.subflow = {
    $subflow: subscope.parse(spec).toRuntime()
  };
}
function parseTrigger(spec, scope2, name) {
  const remove2 = spec.remove, insert2 = spec.insert, toggle2 = spec.toggle, modify3 = spec.modify, values4 = spec.values, op = scope2.add(operator());
  const update3 = "if(" + spec.trigger + ',modify("' + name + '",' + [insert2, remove2, toggle2, modify3, values4].map((_) => _ == null ? "null" : _).join(",") + "),0)";
  const expr2 = parser2(update3, scope2);
  op.update = expr2.$expr;
  op.params = expr2.$params;
}
function parseMark(spec, scope2) {
  const role = getRole(spec), group3 = spec.type === GroupMark, facet = spec.from && spec.from.facet, overlap = spec.overlap;
  let layout = spec.layout || role === ScopeRole2 || role === FrameRole2, ops2, op, store, enc, name, layoutRef, boundRef;
  const nested = role === MarkRole || layout || facet;
  const input = parseData$1(spec.from, group3, scope2);
  op = scope2.add(DataJoin2({
    key: input.key || (spec.key ? fieldRef$1(spec.key) : void 0),
    pulse: input.pulse,
    clean: !group3
  }));
  const joinRef = ref(op);
  op = store = scope2.add(Collect2({
    pulse: joinRef
  }));
  op = scope2.add(Mark2({
    markdef: definition2(spec),
    interactive: interactive(spec.interactive, scope2),
    clip: clip3(spec.clip, scope2),
    context: {
      $context: true
    },
    groups: scope2.lookup(),
    parent: scope2.signals.parent ? scope2.signalRef("parent") : null,
    index: scope2.markpath(),
    pulse: ref(op)
  }));
  const markRef = ref(op);
  op = enc = scope2.add(Encode2(parseEncode(spec.encode, spec.type, role, spec.style, scope2, {
    mod: false,
    pulse: markRef
  })));
  op.params.parent = scope2.encode();
  if (spec.transform) {
    spec.transform.forEach((_) => {
      const tx = parseTransform(_, scope2), md3 = tx.metadata;
      if (md3.generates || md3.changes) {
        error("Mark transforms should not generate new data.");
      }
      if (!md3.nomod) enc.params.mod = true;
      tx.params.pulse = ref(op);
      scope2.add(op = tx);
    });
  }
  if (spec.sort) {
    op = scope2.add(SortItems2({
      sort: scope2.compareRef(spec.sort),
      pulse: ref(op)
    }));
  }
  const encodeRef = ref(op);
  if (facet || layout) {
    layout = scope2.add(ViewLayout2({
      layout: scope2.objectProperty(spec.layout),
      legends: scope2.legends,
      mark: markRef,
      pulse: encodeRef
    }));
    layoutRef = ref(layout);
  }
  const bound3 = scope2.add(Bound2({
    mark: markRef,
    pulse: layoutRef || encodeRef
  }));
  boundRef = ref(bound3);
  if (group3) {
    if (nested) {
      ops2 = scope2.operators;
      ops2.pop();
      if (layout) ops2.pop();
    }
    scope2.pushState(encodeRef, layoutRef || boundRef, joinRef);
    facet ? parseFacet(spec, scope2, input) : nested ? parseSubflow(spec, scope2, input) : scope2.parse(spec);
    scope2.popState();
    if (nested) {
      if (layout) ops2.push(layout);
      ops2.push(bound3);
    }
  }
  if (overlap) {
    boundRef = parseOverlap(overlap, boundRef, scope2);
  }
  const render2 = scope2.add(Render2({
    pulse: boundRef
  })), sieve = scope2.add(Sieve2({
    pulse: ref(render2)
  }, void 0, scope2.parent()));
  if (spec.name != null) {
    name = spec.name;
    scope2.addData(name, new DataScope(scope2, store, render2, sieve));
    if (spec.on) spec.on.forEach((on2) => {
      if (on2.insert || on2.remove || on2.toggle) {
        error("Marks only support modify triggers.");
      }
      parseTrigger(on2, scope2, name);
    });
  }
}
function parseOverlap(overlap, source4, scope2) {
  const method2 = overlap.method, bound3 = overlap.bound, sep = overlap.separation;
  const params2 = {
    separation: isSignal(sep) ? scope2.signalRef(sep.signal) : sep,
    method: isSignal(method2) ? scope2.signalRef(method2.signal) : method2,
    pulse: source4
  };
  if (overlap.order) {
    params2.sort = scope2.compareRef({
      field: overlap.order
    });
  }
  if (bound3) {
    const tol = bound3.tolerance;
    params2.boundTolerance = isSignal(tol) ? scope2.signalRef(tol.signal) : +tol;
    params2.boundScale = scope2.scaleRef(bound3.scale);
    params2.boundOrient = bound3.orient;
  }
  return ref(scope2.add(Overlap2(params2)));
}
function parseLegend(spec, scope2) {
  const config = scope2.config.legend, encode3 = spec.encode || {}, _ = lookup6(spec, config), legendEncode = encode3.legend || {}, name = legendEncode.name || void 0, interactive2 = legendEncode.interactive, style3 = legendEncode.style, scales2 = {};
  let scale9 = 0, entryLayout, params2, children4;
  LegendScales.forEach((s) => spec[s] ? (scales2[s] = spec[s], scale9 = scale9 || spec[s]) : 0);
  if (!scale9) error("Missing valid scale for legend.");
  const type4 = legendType(spec, scope2.scaleType(scale9));
  const datum3 = {
    title: spec.title != null,
    scales: scales2,
    type: type4,
    vgrad: type4 !== "symbol" && _.isVertical()
  };
  const dataRef = ref(scope2.add(Collect2(null, [datum3])));
  const entryEncode = {
    enter: {
      x: {
        value: 0
      },
      y: {
        value: 0
      }
    }
  };
  const entryRef = ref(scope2.add(LegendEntries2(params2 = {
    type: type4,
    scale: scope2.scaleRef(scale9),
    count: scope2.objectProperty(_("tickCount")),
    limit: scope2.property(_("symbolLimit")),
    values: scope2.objectProperty(spec.values),
    minstep: scope2.property(spec.tickMinStep),
    formatType: scope2.property(spec.formatType),
    formatSpecifier: scope2.property(spec.format)
  })));
  if (type4 === Gradient3) {
    children4 = [legendGradient(spec, scale9, config, encode3.gradient), legendGradientLabels(spec, config, encode3.labels, entryRef)];
    params2.count = params2.count || scope2.signalRef(`max(2,2*floor((${deref(_.gradientLength())})/100))`);
  } else if (type4 === Discrete2) {
    children4 = [legendGradientDiscrete(spec, scale9, config, encode3.gradient, entryRef), legendGradientLabels(spec, config, encode3.labels, entryRef)];
  } else {
    entryLayout = legendSymbolLayout(spec, config);
    children4 = [legendSymbolGroups(spec, config, encode3, entryRef, deref(entryLayout.columns))];
    params2.size = sizeExpression(spec, scope2, children4[0].marks);
  }
  children4 = [guideGroup({
    role: LegendEntryRole,
    from: dataRef,
    encode: entryEncode,
    marks: children4,
    layout: entryLayout,
    interactive: interactive2
  })];
  if (datum3.title) {
    children4.push(legendTitle(spec, config, encode3.title, dataRef));
  }
  return parseMark(guideGroup({
    role: LegendRole2,
    from: dataRef,
    encode: extendEncode(buildLegendEncode(_, spec, config), legendEncode, Skip3),
    marks: children4,
    aria: _("aria"),
    description: _("description"),
    zindex: _("zindex"),
    name,
    interactive: interactive2,
    style: style3
  }), scope2);
}
function legendType(spec, scaleType2) {
  let type4 = spec.type || Symbols2;
  if (!spec.type && scaleCount(spec) === 1 && (spec.fill || spec.stroke)) {
    type4 = isContinuous(scaleType2) ? Gradient3 : isDiscretizing(scaleType2) ? Discrete2 : Symbols2;
  }
  return type4 !== Gradient3 ? type4 : isDiscretizing(scaleType2) ? Discrete2 : Gradient3;
}
function scaleCount(spec) {
  return LegendScales.reduce((count2, type4) => count2 + (spec[type4] ? 1 : 0), 0);
}
function buildLegendEncode(_, spec, config) {
  const encode3 = {
    enter: {},
    update: {}
  };
  addEncoders(encode3, {
    orient: _("orient"),
    offset: _("offset"),
    padding: _("padding"),
    titlePadding: _("titlePadding"),
    cornerRadius: _("cornerRadius"),
    fill: _("fillColor"),
    stroke: _("strokeColor"),
    strokeWidth: config.strokeWidth,
    strokeDash: config.strokeDash,
    x: _("legendX"),
    y: _("legendY"),
    // accessibility support
    format: spec.format,
    formatType: spec.formatType
  });
  return encode3;
}
function sizeExpression(spec, scope2, marks) {
  const size = deref(getChannel("size", spec, marks)), strokeWidth = deref(getChannel("strokeWidth", spec, marks)), fontSize3 = deref(getFontSize(marks[1].encode, scope2, GuideLabelStyle));
  return parser2(`max(ceil(sqrt(${size})+${strokeWidth}),${fontSize3})`, scope2);
}
function getChannel(name, spec, marks) {
  return spec[name] ? `scale("${spec[name]}",datum)` : getEncoding(name, marks[0].encode);
}
function getFontSize(encode3, scope2, style3) {
  return getEncoding("fontSize", encode3) || getStyle("fontSize", scope2, style3);
}
var angleExpr = `item.orient==="${Left2}"?-90:item.orient==="${Right2}"?90:0`;
function parseTitle(spec, scope2) {
  spec = isString(spec) ? {
    text: spec
  } : spec;
  const _ = lookup6(spec, scope2.config.title), encode3 = spec.encode || {}, userEncode = encode3.group || {}, name = userEncode.name || void 0, interactive2 = userEncode.interactive, style3 = userEncode.style, children4 = [];
  const datum3 = {}, dataRef = ref(scope2.add(Collect2(null, [datum3])));
  children4.push(buildTitle(spec, _, titleEncode(spec), dataRef));
  if (spec.subtitle) {
    children4.push(buildSubTitle(spec, _, encode3.subtitle, dataRef));
  }
  return parseMark(guideGroup({
    role: TitleRole2,
    from: dataRef,
    encode: groupEncode(_, userEncode),
    marks: children4,
    aria: _("aria"),
    description: _("description"),
    zindex: _("zindex"),
    name,
    interactive: interactive2,
    style: style3
  }), scope2);
}
function titleEncode(spec) {
  const encode3 = spec.encode;
  return encode3 && encode3.title || extend({
    name: spec.name,
    interactive: spec.interactive,
    style: spec.style
  }, encode3);
}
function groupEncode(_, userEncode) {
  const encode3 = {
    enter: {},
    update: {}
  };
  addEncoders(encode3, {
    orient: _("orient"),
    anchor: _("anchor"),
    align: {
      signal: alignExpr$1
    },
    angle: {
      signal: angleExpr
    },
    limit: _("limit"),
    frame: _("frame"),
    offset: _("offset") || 0,
    padding: _("subtitlePadding")
  });
  return extendEncode(encode3, userEncode, Skip3);
}
function buildTitle(spec, _, userEncode, dataRef) {
  const zero6 = {
    value: 0
  }, text4 = spec.text, encode3 = {
    enter: {
      opacity: zero6
    },
    update: {
      opacity: {
        value: 1
      }
    },
    exit: {
      opacity: zero6
    }
  };
  addEncoders(encode3, {
    text: text4,
    align: {
      signal: "item.mark.group.align"
    },
    angle: {
      signal: "item.mark.group.angle"
    },
    limit: {
      signal: "item.mark.group.limit"
    },
    baseline: "top",
    dx: _("dx"),
    dy: _("dy"),
    fill: _("color"),
    font: _("font"),
    fontSize: _("fontSize"),
    fontStyle: _("fontStyle"),
    fontWeight: _("fontWeight"),
    lineHeight: _("lineHeight")
  }, {
    // update
    align: _("align"),
    angle: _("angle"),
    baseline: _("baseline")
  });
  return guideMark({
    type: TextMark,
    role: TitleTextRole,
    style: GroupTitleStyle,
    from: dataRef,
    encode: encode3
  }, userEncode);
}
function buildSubTitle(spec, _, userEncode, dataRef) {
  const zero6 = {
    value: 0
  }, text4 = spec.subtitle, encode3 = {
    enter: {
      opacity: zero6
    },
    update: {
      opacity: {
        value: 1
      }
    },
    exit: {
      opacity: zero6
    }
  };
  addEncoders(encode3, {
    text: text4,
    align: {
      signal: "item.mark.group.align"
    },
    angle: {
      signal: "item.mark.group.angle"
    },
    limit: {
      signal: "item.mark.group.limit"
    },
    baseline: "top",
    dx: _("dx"),
    dy: _("dy"),
    fill: _("subtitleColor"),
    font: _("subtitleFont"),
    fontSize: _("subtitleFontSize"),
    fontStyle: _("subtitleFontStyle"),
    fontWeight: _("subtitleFontWeight"),
    lineHeight: _("subtitleLineHeight")
  }, {
    // update
    align: _("align"),
    angle: _("angle"),
    baseline: _("baseline")
  });
  return guideMark({
    type: TextMark,
    role: TitleSubtitleRole,
    style: GroupSubtitleStyle,
    from: dataRef,
    encode: encode3
  }, userEncode);
}
function parseData(data4, scope2) {
  const transforms2 = [];
  if (data4.transform) {
    data4.transform.forEach((tx) => {
      transforms2.push(parseTransform(tx, scope2));
    });
  }
  if (data4.on) {
    data4.on.forEach((on2) => {
      parseTrigger(on2, scope2, data4.name);
    });
  }
  scope2.addDataPipeline(data4.name, analyze(data4, scope2, transforms2));
}
function analyze(data4, scope2, ops2) {
  const output3 = [];
  let source4 = null, modify3 = false, generate2 = false, upstream, i, n, t, m3;
  if (data4.values) {
    if (isSignal(data4.values) || hasSignal(data4.format)) {
      output3.push(load2(scope2, data4));
      output3.push(source4 = collect());
    } else {
      output3.push(source4 = collect({
        $ingest: data4.values,
        $format: data4.format
      }));
    }
  } else if (data4.url) {
    if (hasSignal(data4.url) || hasSignal(data4.format)) {
      output3.push(load2(scope2, data4));
      output3.push(source4 = collect());
    } else {
      output3.push(source4 = collect({
        $request: data4.url,
        $format: data4.format
      }));
    }
  } else if (data4.source) {
    source4 = upstream = array_default(data4.source).map((d) => ref(scope2.getData(d).output));
    output3.push(null);
  }
  for (i = 0, n = ops2.length; i < n; ++i) {
    t = ops2[i];
    m3 = t.metadata;
    if (!source4 && !m3.source) {
      output3.push(source4 = collect());
    }
    output3.push(t);
    if (m3.generates) generate2 = true;
    if (m3.modifies && !generate2) modify3 = true;
    if (m3.source) source4 = t;
    else if (m3.changes) source4 = null;
  }
  if (upstream) {
    n = upstream.length - 1;
    output3[0] = Relay2({
      derive: modify3,
      pulse: n ? upstream : upstream[0]
    });
    if (modify3 || n) {
      output3.splice(1, 0, collect());
    }
  }
  if (!source4) output3.push(collect());
  output3.push(Sieve2({}));
  return output3;
}
function collect(values4) {
  const s = Collect2({}, values4);
  s.metadata = {
    source: true
  };
  return s;
}
function load2(scope2, data4) {
  return Load2({
    url: data4.url ? scope2.property(data4.url) : void 0,
    async: data4.async ? scope2.property(data4.async) : void 0,
    values: data4.values ? scope2.property(data4.values) : void 0,
    format: scope2.objectProperty(data4.format)
  });
}
var isX = (orient2) => orient2 === Bottom2 || orient2 === Top2;
var getSign = (orient2, a3, b10) => isSignal(orient2) ? ifLeftTopExpr(orient2.signal, a3, b10) : orient2 === Left2 || orient2 === Top2 ? a3 : b10;
var ifX = (orient2, a3, b10) => isSignal(orient2) ? ifXEnc(orient2.signal, a3, b10) : isX(orient2) ? a3 : b10;
var ifY = (orient2, a3, b10) => isSignal(orient2) ? ifYEnc(orient2.signal, a3, b10) : isX(orient2) ? b10 : a3;
var ifTop = (orient2, a3, b10) => isSignal(orient2) ? ifTopExpr(orient2.signal, a3, b10) : orient2 === Top2 ? {
  value: a3
} : {
  value: b10
};
var ifRight = (orient2, a3, b10) => isSignal(orient2) ? ifRightExpr(orient2.signal, a3, b10) : orient2 === Right2 ? {
  value: a3
} : {
  value: b10
};
var ifXEnc = ($orient, a3, b10) => ifEnc(`${$orient} === '${Top2}' || ${$orient} === '${Bottom2}'`, a3, b10);
var ifYEnc = ($orient, a3, b10) => ifEnc(`${$orient} !== '${Top2}' && ${$orient} !== '${Bottom2}'`, a3, b10);
var ifLeftTopExpr = ($orient, a3, b10) => ifExpr(`${$orient} === '${Left2}' || ${$orient} === '${Top2}'`, a3, b10);
var ifTopExpr = ($orient, a3, b10) => ifExpr(`${$orient} === '${Top2}'`, a3, b10);
var ifRightExpr = ($orient, a3, b10) => ifExpr(`${$orient} === '${Right2}'`, a3, b10);
var ifEnc = (test2, a3, b10) => {
  a3 = a3 != null ? encoder(a3) : a3;
  b10 = b10 != null ? encoder(b10) : b10;
  if (isSimple(a3) && isSimple(b10)) {
    a3 = a3 ? a3.signal || stringValue(a3.value) : null;
    b10 = b10 ? b10.signal || stringValue(b10.value) : null;
    return {
      signal: `${test2} ? (${a3}) : (${b10})`
    };
  } else {
    return [extend({
      test: test2
    }, a3)].concat(b10 || []);
  }
};
var isSimple = (enc) => enc == null || Object.keys(enc).length === 1;
var ifExpr = (test2, a3, b10) => ({
  signal: `${test2} ? (${toExpr(a3)}) : (${toExpr(b10)})`
});
var ifOrient = ($orient, t, b10, l, r) => ({
  signal: (l != null ? `${$orient} === '${Left2}' ? (${toExpr(l)}) : ` : "") + (b10 != null ? `${$orient} === '${Bottom2}' ? (${toExpr(b10)}) : ` : "") + (r != null ? `${$orient} === '${Right2}' ? (${toExpr(r)}) : ` : "") + (t != null ? `${$orient} === '${Top2}' ? (${toExpr(t)}) : ` : "") + "(null)"
});
var toExpr = (v2) => isSignal(v2) ? v2.signal : v2 == null ? null : stringValue(v2);
var mult = (sign3, value4) => value4 === 0 ? 0 : isSignal(sign3) ? {
  signal: `(${sign3.signal}) * ${value4}`
} : {
  value: sign3 * value4
};
var patch = (value4, base) => {
  const s = value4.signal;
  return s && s.endsWith("(null)") ? {
    signal: s.slice(0, -6) + base.signal
  } : value4;
};
function fallback(prop, config, axisConfig2, style3) {
  let styleProp;
  if (config && hasOwnProperty(config, prop)) {
    return config[prop];
  } else if (hasOwnProperty(axisConfig2, prop)) {
    return axisConfig2[prop];
  } else if (prop.startsWith("title")) {
    switch (prop) {
      case "titleColor":
        styleProp = "fill";
        break;
      case "titleFont":
      case "titleFontSize":
      case "titleFontWeight":
        styleProp = prop[5].toLowerCase() + prop.slice(6);
    }
    return style3[GuideTitleStyle][styleProp];
  } else if (prop.startsWith("label")) {
    switch (prop) {
      case "labelColor":
        styleProp = "fill";
        break;
      case "labelFont":
      case "labelFontSize":
        styleProp = prop[5].toLowerCase() + prop.slice(6);
    }
    return style3[GuideLabelStyle][styleProp];
  }
  return null;
}
function keys2(objects) {
  const map3 = {};
  for (const obj of objects) {
    if (!obj) continue;
    for (const key2 in obj) map3[key2] = 1;
  }
  return Object.keys(map3);
}
function axisConfig(spec, scope2) {
  var config = scope2.config, style3 = config.style, axis = config.axis, band2 = scope2.scaleType(spec.scale) === "band" && config.axisBand, orient2 = spec.orient, xy, or3, key2;
  if (isSignal(orient2)) {
    const xyKeys = keys2([config.axisX, config.axisY]), orientKeys = keys2([config.axisTop, config.axisBottom, config.axisLeft, config.axisRight]);
    xy = {};
    for (key2 of xyKeys) {
      xy[key2] = ifX(orient2, fallback(key2, config.axisX, axis, style3), fallback(key2, config.axisY, axis, style3));
    }
    or3 = {};
    for (key2 of orientKeys) {
      or3[key2] = ifOrient(orient2.signal, fallback(key2, config.axisTop, axis, style3), fallback(key2, config.axisBottom, axis, style3), fallback(key2, config.axisLeft, axis, style3), fallback(key2, config.axisRight, axis, style3));
    }
  } else {
    xy = orient2 === Top2 || orient2 === Bottom2 ? config.axisX : config.axisY;
    or3 = config["axis" + orient2[0].toUpperCase() + orient2.slice(1)];
  }
  const result = xy || or3 || band2 ? extend({}, axis, xy, or3, band2) : axis;
  return result;
}
function axisDomain(spec, config, userEncode, dataRef) {
  const _ = lookup6(spec, config), orient2 = spec.orient;
  let enter, update3;
  const encode3 = {
    enter: enter = {
      opacity: zero4
    },
    update: update3 = {
      opacity: one3
    },
    exit: {
      opacity: zero4
    }
  };
  addEncoders(encode3, {
    stroke: _("domainColor"),
    strokeCap: _("domainCap"),
    strokeDash: _("domainDash"),
    strokeDashOffset: _("domainDashOffset"),
    strokeWidth: _("domainWidth"),
    strokeOpacity: _("domainOpacity")
  });
  const pos0 = position(spec, 0);
  const pos1 = position(spec, 1);
  enter.x = update3.x = ifX(orient2, pos0, zero4);
  enter.x2 = update3.x2 = ifX(orient2, pos1);
  enter.y = update3.y = ifY(orient2, pos0, zero4);
  enter.y2 = update3.y2 = ifY(orient2, pos1);
  return guideMark({
    type: RuleMark,
    role: AxisDomainRole,
    from: dataRef,
    encode: encode3
  }, userEncode);
}
function position(spec, pos) {
  return {
    scale: spec.scale,
    range: pos
  };
}
function axisGrid(spec, config, userEncode, dataRef, band2) {
  const _ = lookup6(spec, config), orient2 = spec.orient, vscale = spec.gridScale, sign3 = getSign(orient2, 1, -1), offset6 = offsetValue2(spec.offset, sign3);
  let enter, exit, update3;
  const encode3 = {
    enter: enter = {
      opacity: zero4
    },
    update: update3 = {
      opacity: one3
    },
    exit: exit = {
      opacity: zero4
    }
  };
  addEncoders(encode3, {
    stroke: _("gridColor"),
    strokeCap: _("gridCap"),
    strokeDash: _("gridDash"),
    strokeDashOffset: _("gridDashOffset"),
    strokeOpacity: _("gridOpacity"),
    strokeWidth: _("gridWidth")
  });
  const tickPos = {
    scale: spec.scale,
    field: Value,
    band: band2.band,
    extra: band2.extra,
    offset: band2.offset,
    round: _("tickRound")
  };
  const sz3 = ifX(orient2, {
    signal: "height"
  }, {
    signal: "width"
  });
  const gridStart = vscale ? {
    scale: vscale,
    range: 0,
    mult: sign3,
    offset: offset6
  } : {
    value: 0,
    offset: offset6
  };
  const gridEnd = vscale ? {
    scale: vscale,
    range: 1,
    mult: sign3,
    offset: offset6
  } : extend(sz3, {
    mult: sign3,
    offset: offset6
  });
  enter.x = update3.x = ifX(orient2, tickPos, gridStart);
  enter.y = update3.y = ifY(orient2, tickPos, gridStart);
  enter.x2 = update3.x2 = ifY(orient2, gridEnd);
  enter.y2 = update3.y2 = ifX(orient2, gridEnd);
  exit.x = ifX(orient2, tickPos);
  exit.y = ifY(orient2, tickPos);
  return guideMark({
    type: RuleMark,
    role: AxisGridRole,
    key: Value,
    from: dataRef,
    encode: encode3
  }, userEncode);
}
function offsetValue2(offset6, sign3) {
  if (sign3 === 1) ;
  else if (!isObject(offset6)) {
    offset6 = isSignal(sign3) ? {
      signal: `(${sign3.signal}) * (${offset6 || 0})`
    } : sign3 * (offset6 || 0);
  } else {
    let entry2 = offset6 = extend({}, offset6);
    while (entry2.mult != null) {
      if (!isObject(entry2.mult)) {
        entry2.mult = isSignal(sign3) ? {
          signal: `(${entry2.mult}) * (${sign3.signal})`
        } : entry2.mult * sign3;
        return offset6;
      } else {
        entry2 = entry2.mult = extend({}, entry2.mult);
      }
    }
    entry2.mult = sign3;
  }
  return offset6;
}
function axisTicks(spec, config, userEncode, dataRef, size, band2) {
  const _ = lookup6(spec, config), orient2 = spec.orient, sign3 = getSign(orient2, -1, 1);
  let enter, exit, update3;
  const encode3 = {
    enter: enter = {
      opacity: zero4
    },
    update: update3 = {
      opacity: one3
    },
    exit: exit = {
      opacity: zero4
    }
  };
  addEncoders(encode3, {
    stroke: _("tickColor"),
    strokeCap: _("tickCap"),
    strokeDash: _("tickDash"),
    strokeDashOffset: _("tickDashOffset"),
    strokeOpacity: _("tickOpacity"),
    strokeWidth: _("tickWidth")
  });
  const tickSize = encoder(size);
  tickSize.mult = sign3;
  const tickPos = {
    scale: spec.scale,
    field: Value,
    band: band2.band,
    extra: band2.extra,
    offset: band2.offset,
    round: _("tickRound")
  };
  update3.y = enter.y = ifX(orient2, zero4, tickPos);
  update3.y2 = enter.y2 = ifX(orient2, tickSize);
  exit.x = ifX(orient2, tickPos);
  update3.x = enter.x = ifY(orient2, zero4, tickPos);
  update3.x2 = enter.x2 = ifY(orient2, tickSize);
  exit.y = ifY(orient2, tickPos);
  return guideMark({
    type: RuleMark,
    role: AxisTickRole,
    key: Value,
    from: dataRef,
    encode: encode3
  }, userEncode);
}
function flushExpr(scale9, threshold2, a3, b10, c3) {
  return {
    signal: 'flush(range("' + scale9 + '"), scale("' + scale9 + '", datum.value), ' + threshold2 + "," + a3 + "," + b10 + "," + c3 + ")"
  };
}
function axisLabels(spec, config, userEncode, dataRef, size, band2) {
  const _ = lookup6(spec, config), orient2 = spec.orient, scale9 = spec.scale, sign3 = getSign(orient2, -1, 1), flush = deref(_("labelFlush")), flushOffset = deref(_("labelFlushOffset")), labelAlign = _("labelAlign"), labelBaseline = _("labelBaseline");
  let flushOn = flush === 0 || !!flush, update3;
  const tickSize = encoder(size);
  tickSize.mult = sign3;
  tickSize.offset = encoder(_("labelPadding") || 0);
  tickSize.offset.mult = sign3;
  const tickPos = {
    scale: scale9,
    field: Value,
    band: 0.5,
    offset: extendOffset(band2.offset, _("labelOffset"))
  };
  const align2 = ifX(orient2, flushOn ? flushExpr(scale9, flush, '"left"', '"right"', '"center"') : {
    value: "center"
  }, ifRight(orient2, "left", "right"));
  const baseline3 = ifX(orient2, ifTop(orient2, "bottom", "top"), flushOn ? flushExpr(scale9, flush, '"top"', '"bottom"', '"middle"') : {
    value: "middle"
  });
  const offsetExpr2 = flushExpr(scale9, flush, `-(${flushOffset})`, flushOffset, 0);
  flushOn = flushOn && flushOffset;
  const enter = {
    opacity: zero4,
    x: ifX(orient2, tickPos, tickSize),
    y: ifY(orient2, tickPos, tickSize)
  };
  const encode3 = {
    enter,
    update: update3 = {
      opacity: one3,
      text: {
        field: Label2
      },
      x: enter.x,
      y: enter.y,
      align: align2,
      baseline: baseline3
    },
    exit: {
      opacity: zero4,
      x: enter.x,
      y: enter.y
    }
  };
  addEncoders(encode3, {
    dx: !labelAlign && flushOn ? ifX(orient2, offsetExpr2) : null,
    dy: !labelBaseline && flushOn ? ifY(orient2, offsetExpr2) : null
  });
  addEncoders(encode3, {
    angle: _("labelAngle"),
    fill: _("labelColor"),
    fillOpacity: _("labelOpacity"),
    font: _("labelFont"),
    fontSize: _("labelFontSize"),
    fontWeight: _("labelFontWeight"),
    fontStyle: _("labelFontStyle"),
    limit: _("labelLimit"),
    lineHeight: _("labelLineHeight")
  }, {
    align: labelAlign,
    baseline: labelBaseline
  });
  const bound3 = _("labelBound");
  let overlap = _("labelOverlap");
  overlap = overlap || bound3 ? {
    separation: _("labelSeparation"),
    method: overlap,
    order: "datum.index",
    bound: bound3 ? {
      scale: scale9,
      orient: orient2,
      tolerance: bound3
    } : null
  } : void 0;
  if (update3.align !== align2) {
    update3.align = patch(update3.align, align2);
  }
  if (update3.baseline !== baseline3) {
    update3.baseline = patch(update3.baseline, baseline3);
  }
  return guideMark({
    type: TextMark,
    role: AxisLabelRole,
    style: GuideLabelStyle,
    key: Value,
    from: dataRef,
    encode: encode3,
    overlap
  }, userEncode);
}
function axisTitle(spec, config, userEncode, dataRef) {
  const _ = lookup6(spec, config), orient2 = spec.orient, sign3 = getSign(orient2, -1, 1);
  let enter, update3;
  const encode3 = {
    enter: enter = {
      opacity: zero4,
      anchor: encoder(_("titleAnchor", null)),
      align: {
        signal: alignExpr$1
      }
    },
    update: update3 = extend({}, enter, {
      opacity: one3,
      text: encoder(spec.title)
    }),
    exit: {
      opacity: zero4
    }
  };
  const titlePos = {
    signal: `lerp(range("${spec.scale}"), ${anchorExpr(0, 1, 0.5)})`
  };
  update3.x = ifX(orient2, titlePos);
  update3.y = ifY(orient2, titlePos);
  enter.angle = ifX(orient2, zero4, mult(sign3, 90));
  enter.baseline = ifX(orient2, ifTop(orient2, Bottom2, Top2), {
    value: Bottom2
  });
  update3.angle = enter.angle;
  update3.baseline = enter.baseline;
  addEncoders(encode3, {
    fill: _("titleColor"),
    fillOpacity: _("titleOpacity"),
    font: _("titleFont"),
    fontSize: _("titleFontSize"),
    fontStyle: _("titleFontStyle"),
    fontWeight: _("titleFontWeight"),
    limit: _("titleLimit"),
    lineHeight: _("titleLineHeight")
  }, {
    // require update
    align: _("titleAlign"),
    angle: _("titleAngle"),
    baseline: _("titleBaseline")
  });
  autoLayout(_, orient2, encode3, userEncode);
  encode3.update.align = patch(encode3.update.align, enter.align);
  encode3.update.angle = patch(encode3.update.angle, enter.angle);
  encode3.update.baseline = patch(encode3.update.baseline, enter.baseline);
  return guideMark({
    type: TextMark,
    role: AxisTitleRole,
    style: GuideTitleStyle,
    from: dataRef,
    encode: encode3
  }, userEncode);
}
function autoLayout(_, orient2, encode3, userEncode) {
  const auto = (value4, dim) => value4 != null ? (encode3.update[dim] = patch(encoder(value4), encode3.update[dim]), false) : !has(dim, userEncode) ? true : false;
  const autoY = auto(_("titleX"), "x"), autoX = auto(_("titleY"), "y");
  encode3.enter.auto = autoX === autoY ? encoder(autoX) : ifX(orient2, encoder(autoX), encoder(autoY));
}
function parseAxis(spec, scope2) {
  const config = axisConfig(spec, scope2), encode3 = spec.encode || {}, axisEncode = encode3.axis || {}, name = axisEncode.name || void 0, interactive2 = axisEncode.interactive, style3 = axisEncode.style, _ = lookup6(spec, config), band2 = tickBand(_);
  const datum3 = {
    scale: spec.scale,
    ticks: !!_("ticks"),
    labels: !!_("labels"),
    grid: !!_("grid"),
    domain: !!_("domain"),
    title: spec.title != null
  };
  const dataRef = ref(scope2.add(Collect2({}, [datum3])));
  const ticksRef = ref(scope2.add(AxisTicks2({
    scale: scope2.scaleRef(spec.scale),
    extra: scope2.property(band2.extra),
    count: scope2.objectProperty(spec.tickCount),
    values: scope2.objectProperty(spec.values),
    minstep: scope2.property(spec.tickMinStep),
    formatType: scope2.property(spec.formatType),
    formatSpecifier: scope2.property(spec.format)
  })));
  const children4 = [];
  let size;
  if (datum3.grid) {
    children4.push(axisGrid(spec, config, encode3.grid, ticksRef, band2));
  }
  if (datum3.ticks) {
    size = _("tickSize");
    children4.push(axisTicks(spec, config, encode3.ticks, ticksRef, size, band2));
  }
  if (datum3.labels) {
    size = datum3.ticks ? size : 0;
    children4.push(axisLabels(spec, config, encode3.labels, ticksRef, size, band2));
  }
  if (datum3.domain) {
    children4.push(axisDomain(spec, config, encode3.domain, dataRef));
  }
  if (datum3.title) {
    children4.push(axisTitle(spec, config, encode3.title, dataRef));
  }
  return parseMark(guideGroup({
    role: AxisRole2,
    from: dataRef,
    encode: extendEncode(buildAxisEncode(_, spec), axisEncode, Skip3),
    marks: children4,
    aria: _("aria"),
    description: _("description"),
    zindex: _("zindex"),
    name,
    interactive: interactive2,
    style: style3
  }), scope2);
}
function buildAxisEncode(_, spec) {
  const encode3 = {
    enter: {},
    update: {}
  };
  addEncoders(encode3, {
    orient: _("orient"),
    offset: _("offset") || 0,
    position: value3(spec.position, 0),
    titlePadding: _("titlePadding"),
    minExtent: _("minExtent"),
    maxExtent: _("maxExtent"),
    range: {
      signal: `abs(span(range("${spec.scale}")))`
    },
    translate: _("translate"),
    // accessibility support
    format: spec.format,
    formatType: spec.formatType
  });
  return encode3;
}
function parseScope(spec, scope2, preprocessed) {
  const signals = array_default(spec.signals), scales2 = array_default(spec.scales);
  if (!preprocessed) signals.forEach((_) => parseSignal(_, scope2));
  array_default(spec.projections).forEach((_) => parseProjection(_, scope2));
  scales2.forEach((_) => initScale(_, scope2));
  array_default(spec.data).forEach((_) => parseData(_, scope2));
  scales2.forEach((_) => parseScale(_, scope2));
  (preprocessed || signals).forEach((_) => parseSignalUpdates(_, scope2));
  array_default(spec.axes).forEach((_) => parseAxis(_, scope2));
  array_default(spec.marks).forEach((_) => parseMark(_, scope2));
  array_default(spec.legends).forEach((_) => parseLegend(_, scope2));
  if (spec.title) parseTitle(spec.title, scope2);
  scope2.parseLambdas();
  return scope2;
}
var rootEncode = (spec) => extendEncode({
  enter: {
    x: {
      value: 0
    },
    y: {
      value: 0
    }
  },
  update: {
    width: {
      signal: "width"
    },
    height: {
      signal: "height"
    }
  }
}, spec);
function parseView(spec, scope2) {
  const config = scope2.config;
  const root = ref(scope2.root = scope2.add(operator()));
  const signals = collectSignals(spec, config);
  signals.forEach((_) => parseSignal(_, scope2));
  scope2.description = spec.description || config.description;
  scope2.eventConfig = config.events;
  scope2.legends = scope2.objectProperty(config.legend && config.legend.layout);
  scope2.locale = config.locale;
  const input = scope2.add(Collect2());
  const encode3 = scope2.add(Encode2(parseEncode(rootEncode(spec.encode), GroupMark, FrameRole2, spec.style, scope2, {
    pulse: ref(input)
  })));
  const parent = scope2.add(ViewLayout2({
    layout: scope2.objectProperty(spec.layout),
    legends: scope2.legends,
    autosize: scope2.signalRef("autosize"),
    mark: root,
    pulse: ref(encode3)
  }));
  scope2.operators.pop();
  scope2.pushState(ref(encode3), ref(parent), null);
  parseScope(spec, scope2, signals);
  scope2.operators.push(parent);
  let op = scope2.add(Bound2({
    mark: root,
    pulse: ref(parent)
  }));
  op = scope2.add(Render2({
    pulse: ref(op)
  }));
  op = scope2.add(Sieve2({
    pulse: ref(op)
  }));
  scope2.addData("root", new DataScope(scope2, input, input, op));
  return scope2;
}
function signalObject(name, value4) {
  return value4 && value4.signal ? {
    name,
    update: value4.signal
  } : {
    name,
    value: value4
  };
}
function collectSignals(spec, config) {
  const _ = (name) => value3(spec[name], config[name]), signals = [signalObject("background", _("background")), signalObject("autosize", parseAutosize(_("autosize"))), signalObject("padding", parsePadding(_("padding"))), signalObject("width", _("width") || 0), signalObject("height", _("height") || 0)], pre = signals.reduce((p, s) => (p[s.name] = s, p), {}), map3 = {};
  array_default(spec.signals).forEach((s) => {
    if (hasOwnProperty(pre, s.name)) {
      s = extend(pre[s.name], s);
    } else {
      signals.push(s);
    }
    map3[s.name] = s;
  });
  array_default(config.signals).forEach((s) => {
    if (!hasOwnProperty(map3, s.name) && !hasOwnProperty(pre, s.name)) {
      signals.push(s);
    }
  });
  return signals;
}
function Scope(config, options) {
  this.config = config || {};
  this.options = options || {};
  this.bindings = [];
  this.field = {};
  this.signals = {};
  this.lambdas = {};
  this.scales = {};
  this.events = {};
  this.data = {};
  this.streams = [];
  this.updates = [];
  this.operators = [];
  this.eventConfig = null;
  this.locale = null;
  this._id = 0;
  this._subid = 0;
  this._nextsub = [0];
  this._parent = [];
  this._encode = [];
  this._lookup = [];
  this._markpath = [];
}
function Subscope(scope2) {
  this.config = scope2.config;
  this.options = scope2.options;
  this.legends = scope2.legends;
  this.field = Object.create(scope2.field);
  this.signals = Object.create(scope2.signals);
  this.lambdas = Object.create(scope2.lambdas);
  this.scales = Object.create(scope2.scales);
  this.events = Object.create(scope2.events);
  this.data = Object.create(scope2.data);
  this.streams = [];
  this.updates = [];
  this.operators = [];
  this._id = 0;
  this._subid = ++scope2._nextsub[0];
  this._nextsub = scope2._nextsub;
  this._parent = scope2._parent.slice();
  this._encode = scope2._encode.slice();
  this._lookup = scope2._lookup.slice();
  this._markpath = scope2._markpath;
}
Scope.prototype = Subscope.prototype = {
  parse(spec) {
    return parseScope(spec, this);
  },
  fork() {
    return new Subscope(this);
  },
  isSubscope() {
    return this._subid > 0;
  },
  toRuntime() {
    this.finish();
    return {
      description: this.description,
      operators: this.operators,
      streams: this.streams,
      updates: this.updates,
      bindings: this.bindings,
      eventConfig: this.eventConfig,
      locale: this.locale
    };
  },
  id() {
    return (this._subid ? this._subid + ":" : 0) + this._id++;
  },
  add(op) {
    this.operators.push(op);
    op.id = this.id();
    if (op.refs) {
      op.refs.forEach((ref2) => {
        ref2.$ref = op.id;
      });
      op.refs = null;
    }
    return op;
  },
  proxy(op) {
    const vref = op instanceof Entry ? ref(op) : op;
    return this.add(Proxy3({
      value: vref
    }));
  },
  addStream(stream2) {
    this.streams.push(stream2);
    stream2.id = this.id();
    return stream2;
  },
  addUpdate(update3) {
    this.updates.push(update3);
    return update3;
  },
  // Apply metadata
  finish() {
    let name, ds;
    if (this.root) this.root.root = true;
    for (name in this.signals) {
      this.signals[name].signal = name;
    }
    for (name in this.scales) {
      this.scales[name].scale = name;
    }
    function annotate(op, name2, type4) {
      let data4, list;
      if (op) {
        data4 = op.data || (op.data = {});
        list = data4[name2] || (data4[name2] = []);
        list.push(type4);
      }
    }
    for (name in this.data) {
      ds = this.data[name];
      annotate(ds.input, name, "input");
      annotate(ds.output, name, "output");
      annotate(ds.values, name, "values");
      for (const field3 in ds.index) {
        annotate(ds.index[field3], name, "index:" + field3);
      }
    }
    return this;
  },
  // ----
  pushState(encode3, parent, lookup7) {
    this._encode.push(ref(this.add(Sieve2({
      pulse: encode3
    }))));
    this._parent.push(parent);
    this._lookup.push(lookup7 ? ref(this.proxy(lookup7)) : null);
    this._markpath.push(-1);
  },
  popState() {
    this._encode.pop();
    this._parent.pop();
    this._lookup.pop();
    this._markpath.pop();
  },
  parent() {
    return peek(this._parent);
  },
  encode() {
    return peek(this._encode);
  },
  lookup() {
    return peek(this._lookup);
  },
  markpath() {
    const p = this._markpath;
    return ++p[p.length - 1];
  },
  // ----
  fieldRef(field3, name) {
    if (isString(field3)) return fieldRef$1(field3, name);
    if (!field3.signal) {
      error("Unsupported field reference: " + stringValue(field3));
    }
    const s = field3.signal;
    let f = this.field[s];
    if (!f) {
      const params2 = {
        name: this.signalRef(s)
      };
      if (name) params2.as = name;
      this.field[s] = f = ref(this.add(Field2(params2)));
    }
    return f;
  },
  compareRef(cmp) {
    let signal = false;
    const check = (_) => isSignal(_) ? (signal = true, this.signalRef(_.signal)) : isExpr$1(_) ? (signal = true, this.exprRef(_.expr)) : _;
    const fields = array_default(cmp.field).map(check), orders = array_default(cmp.order).map(check);
    return signal ? ref(this.add(Compare2({
      fields,
      orders
    }))) : compareRef(fields, orders);
  },
  keyRef(fields, flat) {
    let signal = false;
    const check = (_) => isSignal(_) ? (signal = true, ref(sig[_.signal])) : _;
    const sig = this.signals;
    fields = array_default(fields).map(check);
    return signal ? ref(this.add(Key2({
      fields,
      flat
    }))) : keyRef(fields, flat);
  },
  sortRef(sort4) {
    if (!sort4) return sort4;
    const a3 = aggrField(sort4.op, sort4.field), o = sort4.order || Ascending;
    return o.signal ? ref(this.add(Compare2({
      fields: a3,
      orders: this.signalRef(o.signal)
    }))) : compareRef(a3, o);
  },
  // ----
  event(source4, type4) {
    const key2 = source4 + ":" + type4;
    if (!this.events[key2]) {
      const id2 = this.id();
      this.streams.push({
        id: id2,
        source: source4,
        type: type4
      });
      this.events[key2] = id2;
    }
    return this.events[key2];
  },
  // ----
  hasOwnSignal(name) {
    return hasOwnProperty(this.signals, name);
  },
  addSignal(name, value4) {
    if (this.hasOwnSignal(name)) {
      error("Duplicate signal name: " + stringValue(name));
    }
    const op = value4 instanceof Entry ? value4 : this.add(operator(value4));
    return this.signals[name] = op;
  },
  getSignal(name) {
    if (!this.signals[name]) {
      error("Unrecognized signal name: " + stringValue(name));
    }
    return this.signals[name];
  },
  signalRef(s) {
    if (this.signals[s]) {
      return ref(this.signals[s]);
    } else if (!hasOwnProperty(this.lambdas, s)) {
      this.lambdas[s] = this.add(operator(null));
    }
    return ref(this.lambdas[s]);
  },
  parseLambdas() {
    const code = Object.keys(this.lambdas);
    for (let i = 0, n = code.length; i < n; ++i) {
      const s = code[i], e = parser2(s, this), op = this.lambdas[s];
      op.params = e.$params;
      op.update = e.$expr;
    }
  },
  property(spec) {
    return spec && spec.signal ? this.signalRef(spec.signal) : spec;
  },
  objectProperty(spec) {
    return !spec || !isObject(spec) ? spec : this.signalRef(spec.signal || propertyLambda(spec));
  },
  exprRef(code, name) {
    const params2 = {
      expr: parser2(code, this)
    };
    if (name) params2.expr.$name = name;
    return ref(this.add(Expression2(params2)));
  },
  addBinding(name, bind4) {
    if (!this.bindings) {
      error("Nested signals do not support binding: " + stringValue(name));
    }
    this.bindings.push(extend({
      signal: name
    }, bind4));
  },
  // ----
  addScaleProj(name, transform4) {
    if (hasOwnProperty(this.scales, name)) {
      error("Duplicate scale or projection name: " + stringValue(name));
    }
    this.scales[name] = this.add(transform4);
  },
  addScale(name, params2) {
    this.addScaleProj(name, Scale2(params2));
  },
  addProjection(name, params2) {
    this.addScaleProj(name, Projection2(params2));
  },
  getScale(name) {
    if (!this.scales[name]) {
      error("Unrecognized scale name: " + stringValue(name));
    }
    return this.scales[name];
  },
  scaleRef(name) {
    return ref(this.getScale(name));
  },
  scaleType(name) {
    return this.getScale(name).params.type;
  },
  projectionRef(name) {
    return this.scaleRef(name);
  },
  projectionType(name) {
    return this.scaleType(name);
  },
  // ----
  addData(name, dataScope) {
    if (hasOwnProperty(this.data, name)) {
      error("Duplicate data set name: " + stringValue(name));
    }
    return this.data[name] = dataScope;
  },
  getData(name) {
    if (!this.data[name]) {
      error("Undefined data set name: " + stringValue(name));
    }
    return this.data[name];
  },
  addDataPipeline(name, entries2) {
    if (hasOwnProperty(this.data, name)) {
      error("Duplicate data set name: " + stringValue(name));
    }
    return this.addData(name, DataScope.fromEntries(this, entries2));
  }
};
function propertyLambda(spec) {
  return (isArray(spec) ? arrayLambda : objectLambda)(spec);
}
function arrayLambda(array4) {
  const n = array4.length;
  let code = "[";
  for (let i = 0; i < n; ++i) {
    const value4 = array4[i];
    code += (i > 0 ? "," : "") + (isObject(value4) ? value4.signal || propertyLambda(value4) : stringValue(value4));
  }
  return code + "]";
}
function objectLambda(obj) {
  let code = "{", i = 0, key2, value4;
  for (key2 in obj) {
    value4 = obj[key2];
    code += (++i > 1 ? "," : "") + stringValue(key2) + ":" + (isObject(value4) ? value4.signal || propertyLambda(value4) : stringValue(value4));
  }
  return code + "}";
}
function defaults() {
  const defaultFont = "sans-serif", defaultSymbolSize = 30, defaultStrokeWidth = 2, defaultColor = "#4c78a8", black = "#000", gray2 = "#888", lightGray = "#ddd";
  return {
    // default visualization description
    description: "Vega visualization",
    // default padding around visualization
    padding: 0,
    // default for automatic sizing; options: 'none', 'pad', 'fit'
    // or provide an object (e.g., {'type': 'pad', 'resize': true})
    autosize: "pad",
    // default view background color
    // covers the entire view component
    background: null,
    // default event handling configuration
    // preventDefault for view-sourced event types except 'wheel'
    events: {
      defaults: {
        allow: ["wheel"]
      }
    },
    // defaults for top-level group marks
    // accepts mark properties (fill, stroke, etc)
    // covers the data rectangle within group width/height
    group: null,
    // defaults for basic mark types
    // each subset accepts mark properties (fill, stroke, etc)
    mark: null,
    arc: {
      fill: defaultColor
    },
    area: {
      fill: defaultColor
    },
    image: null,
    line: {
      stroke: defaultColor,
      strokeWidth: defaultStrokeWidth
    },
    path: {
      stroke: defaultColor
    },
    rect: {
      fill: defaultColor
    },
    rule: {
      stroke: black
    },
    shape: {
      stroke: defaultColor
    },
    symbol: {
      fill: defaultColor,
      size: 64
    },
    text: {
      fill: black,
      font: defaultFont,
      fontSize: 11
    },
    trail: {
      fill: defaultColor,
      size: defaultStrokeWidth
    },
    // style definitions
    style: {
      // axis & legend labels
      "guide-label": {
        fill: black,
        font: defaultFont,
        fontSize: 10
      },
      // axis & legend titles
      "guide-title": {
        fill: black,
        font: defaultFont,
        fontSize: 11,
        fontWeight: "bold"
      },
      // headers, including chart title
      "group-title": {
        fill: black,
        font: defaultFont,
        fontSize: 13,
        fontWeight: "bold"
      },
      // chart subtitle
      "group-subtitle": {
        fill: black,
        font: defaultFont,
        fontSize: 12
      },
      // defaults for styled point marks in Vega-Lite
      point: {
        size: defaultSymbolSize,
        strokeWidth: defaultStrokeWidth,
        shape: "circle"
      },
      circle: {
        size: defaultSymbolSize,
        strokeWidth: defaultStrokeWidth
      },
      square: {
        size: defaultSymbolSize,
        strokeWidth: defaultStrokeWidth,
        shape: "square"
      },
      // defaults for styled group marks in Vega-Lite
      cell: {
        fill: "transparent",
        stroke: lightGray
      },
      view: {
        fill: "transparent"
      }
    },
    // defaults for title
    title: {
      orient: "top",
      anchor: "middle",
      offset: 4,
      subtitlePadding: 3
    },
    // defaults for axes
    axis: {
      minExtent: 0,
      maxExtent: 200,
      bandPosition: 0.5,
      domain: true,
      domainWidth: 1,
      domainColor: gray2,
      grid: false,
      gridWidth: 1,
      gridColor: lightGray,
      labels: true,
      labelAngle: 0,
      labelLimit: 180,
      labelOffset: 0,
      labelPadding: 2,
      ticks: true,
      tickColor: gray2,
      tickOffset: 0,
      tickRound: true,
      tickSize: 5,
      tickWidth: 1,
      titlePadding: 4
    },
    // correction for centering bias
    axisBand: {
      tickOffset: -0.5
    },
    // defaults for cartographic projection
    projection: {
      type: "mercator"
    },
    // defaults for legends
    legend: {
      orient: "right",
      padding: 0,
      gridAlign: "each",
      columnPadding: 10,
      rowPadding: 2,
      symbolDirection: "vertical",
      gradientDirection: "vertical",
      gradientLength: 200,
      gradientThickness: 16,
      gradientStrokeColor: lightGray,
      gradientStrokeWidth: 0,
      gradientLabelOffset: 2,
      labelAlign: "left",
      labelBaseline: "middle",
      labelLimit: 160,
      labelOffset: 4,
      labelOverlap: true,
      symbolLimit: 30,
      symbolType: "circle",
      symbolSize: 100,
      symbolOffset: 0,
      symbolStrokeWidth: 1.5,
      symbolBaseFillColor: "transparent",
      symbolBaseStrokeColor: gray2,
      titleLimit: 180,
      titleOrient: "top",
      titlePadding: 5,
      layout: {
        offset: 18,
        direction: "horizontal",
        left: {
          direction: "vertical"
        },
        right: {
          direction: "vertical"
        }
      }
    },
    // defaults for scale ranges
    range: {
      category: {
        scheme: "tableau10"
      },
      ordinal: {
        scheme: "blues"
      },
      heatmap: {
        scheme: "yellowgreenblue"
      },
      ramp: {
        scheme: "blues"
      },
      diverging: {
        scheme: "blueorange",
        extent: [1, 0]
      },
      symbol: ["circle", "square", "triangle-up", "cross", "diamond", "triangle-right", "triangle-down", "triangle-left"]
    }
  };
}
function parse7(spec, config, options) {
  if (!isObject(spec)) {
    error("Input Vega specification must be an object.");
  }
  config = mergeConfig(defaults(), config, spec.config);
  return parseView(spec, new Scope(config, options)).toRuntime();
}

// ../deepseek-harness/node_modules/.pnpm/vega@6.3.1/node_modules/vega/build/vega.module.js
extend(transforms, vega_transforms_exports, vega_view_transforms_exports, vega_encode_exports, vega_geo_exports, vega_force_exports, vega_label_exports, vega_hierarchy_exports, vega_regression_exports, vega_voronoi_exports, vega_wordcloud_exports, vega_crossfilter_exports);

// ../deepseek-harness/node_modules/.pnpm/vega-expression@6.1.0/node_modules/vega-expression/build/vega-expression.js
var RawCode2 = "RawCode";
var Literal2 = "Literal";
var Property2 = "Property";
var Identifier3 = "Identifier";
var ArrayExpression2 = "ArrayExpression";
var BinaryExpression2 = "BinaryExpression";
var CallExpression2 = "CallExpression";
var ConditionalExpression2 = "ConditionalExpression";
var LogicalExpression2 = "LogicalExpression";
var MemberExpression2 = "MemberExpression";
var ObjectExpression2 = "ObjectExpression";
var UnaryExpression2 = "UnaryExpression";
function ASTNode2(type4) {
  this.type = type4;
}
ASTNode2.prototype.visit = function(visitor) {
  let c3, i, n;
  if (visitor(this)) return 1;
  for (c3 = children3(this), i = 0, n = c3.length; i < n; ++i) {
    if (c3[i].visit(visitor)) return 1;
  }
};
function children3(node) {
  switch (node.type) {
    case ArrayExpression2:
      return node.elements;
    case BinaryExpression2:
    case LogicalExpression2:
      return [node.left, node.right];
    case CallExpression2:
      return [node.callee].concat(node.arguments);
    case ConditionalExpression2:
      return [node.test, node.consequent, node.alternate];
    case MemberExpression2:
      return [node.object, node.property];
    case ObjectExpression2:
      return node.properties;
    case Property2:
      return [node.key, node.value];
    case UnaryExpression2:
      return [node.argument];
    case Identifier3:
    case Literal2:
    case RawCode2:
    default:
      return [];
  }
}
var TokenName2;
var source3;
var index3;
var length2;
var lookahead2;
var TokenBooleanLiteral2 = 1;
var TokenEOF2 = 2;
var TokenIdentifier2 = 3;
var TokenKeyword2 = 4;
var TokenNullLiteral2 = 5;
var TokenNumericLiteral2 = 6;
var TokenPunctuator2 = 7;
var TokenStringLiteral2 = 8;
var TokenRegularExpression2 = 9;
TokenName2 = {};
TokenName2[TokenBooleanLiteral2] = "Boolean";
TokenName2[TokenEOF2] = "<end>";
TokenName2[TokenIdentifier2] = "Identifier";
TokenName2[TokenKeyword2] = "Keyword";
TokenName2[TokenNullLiteral2] = "Null";
TokenName2[TokenNumericLiteral2] = "Numeric";
TokenName2[TokenPunctuator2] = "Punctuator";
TokenName2[TokenStringLiteral2] = "String";
TokenName2[TokenRegularExpression2] = "RegularExpression";
var SyntaxArrayExpression2 = "ArrayExpression";
var SyntaxBinaryExpression2 = "BinaryExpression";
var SyntaxCallExpression2 = "CallExpression";
var SyntaxConditionalExpression2 = "ConditionalExpression";
var SyntaxIdentifier2 = "Identifier";
var SyntaxLiteral2 = "Literal";
var SyntaxLogicalExpression2 = "LogicalExpression";
var SyntaxMemberExpression2 = "MemberExpression";
var SyntaxObjectExpression2 = "ObjectExpression";
var SyntaxProperty2 = "Property";
var SyntaxUnaryExpression2 = "UnaryExpression";
var MessageUnexpectedToken2 = "Unexpected token %0";
var MessageUnexpectedNumber2 = "Unexpected number";
var MessageUnexpectedString2 = "Unexpected string";
var MessageUnexpectedIdentifier2 = "Unexpected identifier";
var MessageUnexpectedReserved2 = "Unexpected reserved word";
var MessageUnexpectedEOS2 = "Unexpected end of input";
var MessageInvalidRegExp2 = "Invalid regular expression";
var MessageUnterminatedRegExp2 = "Invalid regular expression: missing /";
var MessageStrictOctalLiteral2 = "Octal literals are not allowed in strict mode.";
var MessageStrictDuplicateProperty2 = "Duplicate data property in object literal not allowed in strict mode";
var ILLEGAL3 = "ILLEGAL";
var DISABLED2 = "Disabled.";
var RegexNonAsciiIdentifierStart2 = new RegExp("[\\xAA\\xB5\\xBA\\xC0-\\xD6\\xD8-\\xF6\\xF8-\\u02C1\\u02C6-\\u02D1\\u02E0-\\u02E4\\u02EC\\u02EE\\u0370-\\u0374\\u0376\\u0377\\u037A-\\u037D\\u037F\\u0386\\u0388-\\u038A\\u038C\\u038E-\\u03A1\\u03A3-\\u03F5\\u03F7-\\u0481\\u048A-\\u052F\\u0531-\\u0556\\u0559\\u0561-\\u0587\\u05D0-\\u05EA\\u05F0-\\u05F2\\u0620-\\u064A\\u066E\\u066F\\u0671-\\u06D3\\u06D5\\u06E5\\u06E6\\u06EE\\u06EF\\u06FA-\\u06FC\\u06FF\\u0710\\u0712-\\u072F\\u074D-\\u07A5\\u07B1\\u07CA-\\u07EA\\u07F4\\u07F5\\u07FA\\u0800-\\u0815\\u081A\\u0824\\u0828\\u0840-\\u0858\\u08A0-\\u08B2\\u0904-\\u0939\\u093D\\u0950\\u0958-\\u0961\\u0971-\\u0980\\u0985-\\u098C\\u098F\\u0990\\u0993-\\u09A8\\u09AA-\\u09B0\\u09B2\\u09B6-\\u09B9\\u09BD\\u09CE\\u09DC\\u09DD\\u09DF-\\u09E1\\u09F0\\u09F1\\u0A05-\\u0A0A\\u0A0F\\u0A10\\u0A13-\\u0A28\\u0A2A-\\u0A30\\u0A32\\u0A33\\u0A35\\u0A36\\u0A38\\u0A39\\u0A59-\\u0A5C\\u0A5E\\u0A72-\\u0A74\\u0A85-\\u0A8D\\u0A8F-\\u0A91\\u0A93-\\u0AA8\\u0AAA-\\u0AB0\\u0AB2\\u0AB3\\u0AB5-\\u0AB9\\u0ABD\\u0AD0\\u0AE0\\u0AE1\\u0B05-\\u0B0C\\u0B0F\\u0B10\\u0B13-\\u0B28\\u0B2A-\\u0B30\\u0B32\\u0B33\\u0B35-\\u0B39\\u0B3D\\u0B5C\\u0B5D\\u0B5F-\\u0B61\\u0B71\\u0B83\\u0B85-\\u0B8A\\u0B8E-\\u0B90\\u0B92-\\u0B95\\u0B99\\u0B9A\\u0B9C\\u0B9E\\u0B9F\\u0BA3\\u0BA4\\u0BA8-\\u0BAA\\u0BAE-\\u0BB9\\u0BD0\\u0C05-\\u0C0C\\u0C0E-\\u0C10\\u0C12-\\u0C28\\u0C2A-\\u0C39\\u0C3D\\u0C58\\u0C59\\u0C60\\u0C61\\u0C85-\\u0C8C\\u0C8E-\\u0C90\\u0C92-\\u0CA8\\u0CAA-\\u0CB3\\u0CB5-\\u0CB9\\u0CBD\\u0CDE\\u0CE0\\u0CE1\\u0CF1\\u0CF2\\u0D05-\\u0D0C\\u0D0E-\\u0D10\\u0D12-\\u0D3A\\u0D3D\\u0D4E\\u0D60\\u0D61\\u0D7A-\\u0D7F\\u0D85-\\u0D96\\u0D9A-\\u0DB1\\u0DB3-\\u0DBB\\u0DBD\\u0DC0-\\u0DC6\\u0E01-\\u0E30\\u0E32\\u0E33\\u0E40-\\u0E46\\u0E81\\u0E82\\u0E84\\u0E87\\u0E88\\u0E8A\\u0E8D\\u0E94-\\u0E97\\u0E99-\\u0E9F\\u0EA1-\\u0EA3\\u0EA5\\u0EA7\\u0EAA\\u0EAB\\u0EAD-\\u0EB0\\u0EB2\\u0EB3\\u0EBD\\u0EC0-\\u0EC4\\u0EC6\\u0EDC-\\u0EDF\\u0F00\\u0F40-\\u0F47\\u0F49-\\u0F6C\\u0F88-\\u0F8C\\u1000-\\u102A\\u103F\\u1050-\\u1055\\u105A-\\u105D\\u1061\\u1065\\u1066\\u106E-\\u1070\\u1075-\\u1081\\u108E\\u10A0-\\u10C5\\u10C7\\u10CD\\u10D0-\\u10FA\\u10FC-\\u1248\\u124A-\\u124D\\u1250-\\u1256\\u1258\\u125A-\\u125D\\u1260-\\u1288\\u128A-\\u128D\\u1290-\\u12B0\\u12B2-\\u12B5\\u12B8-\\u12BE\\u12C0\\u12C2-\\u12C5\\u12C8-\\u12D6\\u12D8-\\u1310\\u1312-\\u1315\\u1318-\\u135A\\u1380-\\u138F\\u13A0-\\u13F4\\u1401-\\u166C\\u166F-\\u167F\\u1681-\\u169A\\u16A0-\\u16EA\\u16EE-\\u16F8\\u1700-\\u170C\\u170E-\\u1711\\u1720-\\u1731\\u1740-\\u1751\\u1760-\\u176C\\u176E-\\u1770\\u1780-\\u17B3\\u17D7\\u17DC\\u1820-\\u1877\\u1880-\\u18A8\\u18AA\\u18B0-\\u18F5\\u1900-\\u191E\\u1950-\\u196D\\u1970-\\u1974\\u1980-\\u19AB\\u19C1-\\u19C7\\u1A00-\\u1A16\\u1A20-\\u1A54\\u1AA7\\u1B05-\\u1B33\\u1B45-\\u1B4B\\u1B83-\\u1BA0\\u1BAE\\u1BAF\\u1BBA-\\u1BE5\\u1C00-\\u1C23\\u1C4D-\\u1C4F\\u1C5A-\\u1C7D\\u1CE9-\\u1CEC\\u1CEE-\\u1CF1\\u1CF5\\u1CF6\\u1D00-\\u1DBF\\u1E00-\\u1F15\\u1F18-\\u1F1D\\u1F20-\\u1F45\\u1F48-\\u1F4D\\u1F50-\\u1F57\\u1F59\\u1F5B\\u1F5D\\u1F5F-\\u1F7D\\u1F80-\\u1FB4\\u1FB6-\\u1FBC\\u1FBE\\u1FC2-\\u1FC4\\u1FC6-\\u1FCC\\u1FD0-\\u1FD3\\u1FD6-\\u1FDB\\u1FE0-\\u1FEC\\u1FF2-\\u1FF4\\u1FF6-\\u1FFC\\u2071\\u207F\\u2090-\\u209C\\u2102\\u2107\\u210A-\\u2113\\u2115\\u2119-\\u211D\\u2124\\u2126\\u2128\\u212A-\\u212D\\u212F-\\u2139\\u213C-\\u213F\\u2145-\\u2149\\u214E\\u2160-\\u2188\\u2C00-\\u2C2E\\u2C30-\\u2C5E\\u2C60-\\u2CE4\\u2CEB-\\u2CEE\\u2CF2\\u2CF3\\u2D00-\\u2D25\\u2D27\\u2D2D\\u2D30-\\u2D67\\u2D6F\\u2D80-\\u2D96\\u2DA0-\\u2DA6\\u2DA8-\\u2DAE\\u2DB0-\\u2DB6\\u2DB8-\\u2DBE\\u2DC0-\\u2DC6\\u2DC8-\\u2DCE\\u2DD0-\\u2DD6\\u2DD8-\\u2DDE\\u2E2F\\u3005-\\u3007\\u3021-\\u3029\\u3031-\\u3035\\u3038-\\u303C\\u3041-\\u3096\\u309D-\\u309F\\u30A1-\\u30FA\\u30FC-\\u30FF\\u3105-\\u312D\\u3131-\\u318E\\u31A0-\\u31BA\\u31F0-\\u31FF\\u3400-\\u4DB5\\u4E00-\\u9FCC\\uA000-\\uA48C\\uA4D0-\\uA4FD\\uA500-\\uA60C\\uA610-\\uA61F\\uA62A\\uA62B\\uA640-\\uA66E\\uA67F-\\uA69D\\uA6A0-\\uA6EF\\uA717-\\uA71F\\uA722-\\uA788\\uA78B-\\uA78E\\uA790-\\uA7AD\\uA7B0\\uA7B1\\uA7F7-\\uA801\\uA803-\\uA805\\uA807-\\uA80A\\uA80C-\\uA822\\uA840-\\uA873\\uA882-\\uA8B3\\uA8F2-\\uA8F7\\uA8FB\\uA90A-\\uA925\\uA930-\\uA946\\uA960-\\uA97C\\uA984-\\uA9B2\\uA9CF\\uA9E0-\\uA9E4\\uA9E6-\\uA9EF\\uA9FA-\\uA9FE\\uAA00-\\uAA28\\uAA40-\\uAA42\\uAA44-\\uAA4B\\uAA60-\\uAA76\\uAA7A\\uAA7E-\\uAAAF\\uAAB1\\uAAB5\\uAAB6\\uAAB9-\\uAABD\\uAAC0\\uAAC2\\uAADB-\\uAADD\\uAAE0-\\uAAEA\\uAAF2-\\uAAF4\\uAB01-\\uAB06\\uAB09-\\uAB0E\\uAB11-\\uAB16\\uAB20-\\uAB26\\uAB28-\\uAB2E\\uAB30-\\uAB5A\\uAB5C-\\uAB5F\\uAB64\\uAB65\\uABC0-\\uABE2\\uAC00-\\uD7A3\\uD7B0-\\uD7C6\\uD7CB-\\uD7FB\\uF900-\\uFA6D\\uFA70-\\uFAD9\\uFB00-\\uFB06\\uFB13-\\uFB17\\uFB1D\\uFB1F-\\uFB28\\uFB2A-\\uFB36\\uFB38-\\uFB3C\\uFB3E\\uFB40\\uFB41\\uFB43\\uFB44\\uFB46-\\uFBB1\\uFBD3-\\uFD3D\\uFD50-\\uFD8F\\uFD92-\\uFDC7\\uFDF0-\\uFDFB\\uFE70-\\uFE74\\uFE76-\\uFEFC\\uFF21-\\uFF3A\\uFF41-\\uFF5A\\uFF66-\\uFFBE\\uFFC2-\\uFFC7\\uFFCA-\\uFFCF\\uFFD2-\\uFFD7\\uFFDA-\\uFFDC]");
var RegexNonAsciiIdentifierPart2 = new RegExp("[\\xAA\\xB5\\xBA\\xC0-\\xD6\\xD8-\\xF6\\xF8-\\u02C1\\u02C6-\\u02D1\\u02E0-\\u02E4\\u02EC\\u02EE\\u0300-\\u0374\\u0376\\u0377\\u037A-\\u037D\\u037F\\u0386\\u0388-\\u038A\\u038C\\u038E-\\u03A1\\u03A3-\\u03F5\\u03F7-\\u0481\\u0483-\\u0487\\u048A-\\u052F\\u0531-\\u0556\\u0559\\u0561-\\u0587\\u0591-\\u05BD\\u05BF\\u05C1\\u05C2\\u05C4\\u05C5\\u05C7\\u05D0-\\u05EA\\u05F0-\\u05F2\\u0610-\\u061A\\u0620-\\u0669\\u066E-\\u06D3\\u06D5-\\u06DC\\u06DF-\\u06E8\\u06EA-\\u06FC\\u06FF\\u0710-\\u074A\\u074D-\\u07B1\\u07C0-\\u07F5\\u07FA\\u0800-\\u082D\\u0840-\\u085B\\u08A0-\\u08B2\\u08E4-\\u0963\\u0966-\\u096F\\u0971-\\u0983\\u0985-\\u098C\\u098F\\u0990\\u0993-\\u09A8\\u09AA-\\u09B0\\u09B2\\u09B6-\\u09B9\\u09BC-\\u09C4\\u09C7\\u09C8\\u09CB-\\u09CE\\u09D7\\u09DC\\u09DD\\u09DF-\\u09E3\\u09E6-\\u09F1\\u0A01-\\u0A03\\u0A05-\\u0A0A\\u0A0F\\u0A10\\u0A13-\\u0A28\\u0A2A-\\u0A30\\u0A32\\u0A33\\u0A35\\u0A36\\u0A38\\u0A39\\u0A3C\\u0A3E-\\u0A42\\u0A47\\u0A48\\u0A4B-\\u0A4D\\u0A51\\u0A59-\\u0A5C\\u0A5E\\u0A66-\\u0A75\\u0A81-\\u0A83\\u0A85-\\u0A8D\\u0A8F-\\u0A91\\u0A93-\\u0AA8\\u0AAA-\\u0AB0\\u0AB2\\u0AB3\\u0AB5-\\u0AB9\\u0ABC-\\u0AC5\\u0AC7-\\u0AC9\\u0ACB-\\u0ACD\\u0AD0\\u0AE0-\\u0AE3\\u0AE6-\\u0AEF\\u0B01-\\u0B03\\u0B05-\\u0B0C\\u0B0F\\u0B10\\u0B13-\\u0B28\\u0B2A-\\u0B30\\u0B32\\u0B33\\u0B35-\\u0B39\\u0B3C-\\u0B44\\u0B47\\u0B48\\u0B4B-\\u0B4D\\u0B56\\u0B57\\u0B5C\\u0B5D\\u0B5F-\\u0B63\\u0B66-\\u0B6F\\u0B71\\u0B82\\u0B83\\u0B85-\\u0B8A\\u0B8E-\\u0B90\\u0B92-\\u0B95\\u0B99\\u0B9A\\u0B9C\\u0B9E\\u0B9F\\u0BA3\\u0BA4\\u0BA8-\\u0BAA\\u0BAE-\\u0BB9\\u0BBE-\\u0BC2\\u0BC6-\\u0BC8\\u0BCA-\\u0BCD\\u0BD0\\u0BD7\\u0BE6-\\u0BEF\\u0C00-\\u0C03\\u0C05-\\u0C0C\\u0C0E-\\u0C10\\u0C12-\\u0C28\\u0C2A-\\u0C39\\u0C3D-\\u0C44\\u0C46-\\u0C48\\u0C4A-\\u0C4D\\u0C55\\u0C56\\u0C58\\u0C59\\u0C60-\\u0C63\\u0C66-\\u0C6F\\u0C81-\\u0C83\\u0C85-\\u0C8C\\u0C8E-\\u0C90\\u0C92-\\u0CA8\\u0CAA-\\u0CB3\\u0CB5-\\u0CB9\\u0CBC-\\u0CC4\\u0CC6-\\u0CC8\\u0CCA-\\u0CCD\\u0CD5\\u0CD6\\u0CDE\\u0CE0-\\u0CE3\\u0CE6-\\u0CEF\\u0CF1\\u0CF2\\u0D01-\\u0D03\\u0D05-\\u0D0C\\u0D0E-\\u0D10\\u0D12-\\u0D3A\\u0D3D-\\u0D44\\u0D46-\\u0D48\\u0D4A-\\u0D4E\\u0D57\\u0D60-\\u0D63\\u0D66-\\u0D6F\\u0D7A-\\u0D7F\\u0D82\\u0D83\\u0D85-\\u0D96\\u0D9A-\\u0DB1\\u0DB3-\\u0DBB\\u0DBD\\u0DC0-\\u0DC6\\u0DCA\\u0DCF-\\u0DD4\\u0DD6\\u0DD8-\\u0DDF\\u0DE6-\\u0DEF\\u0DF2\\u0DF3\\u0E01-\\u0E3A\\u0E40-\\u0E4E\\u0E50-\\u0E59\\u0E81\\u0E82\\u0E84\\u0E87\\u0E88\\u0E8A\\u0E8D\\u0E94-\\u0E97\\u0E99-\\u0E9F\\u0EA1-\\u0EA3\\u0EA5\\u0EA7\\u0EAA\\u0EAB\\u0EAD-\\u0EB9\\u0EBB-\\u0EBD\\u0EC0-\\u0EC4\\u0EC6\\u0EC8-\\u0ECD\\u0ED0-\\u0ED9\\u0EDC-\\u0EDF\\u0F00\\u0F18\\u0F19\\u0F20-\\u0F29\\u0F35\\u0F37\\u0F39\\u0F3E-\\u0F47\\u0F49-\\u0F6C\\u0F71-\\u0F84\\u0F86-\\u0F97\\u0F99-\\u0FBC\\u0FC6\\u1000-\\u1049\\u1050-\\u109D\\u10A0-\\u10C5\\u10C7\\u10CD\\u10D0-\\u10FA\\u10FC-\\u1248\\u124A-\\u124D\\u1250-\\u1256\\u1258\\u125A-\\u125D\\u1260-\\u1288\\u128A-\\u128D\\u1290-\\u12B0\\u12B2-\\u12B5\\u12B8-\\u12BE\\u12C0\\u12C2-\\u12C5\\u12C8-\\u12D6\\u12D8-\\u1310\\u1312-\\u1315\\u1318-\\u135A\\u135D-\\u135F\\u1380-\\u138F\\u13A0-\\u13F4\\u1401-\\u166C\\u166F-\\u167F\\u1681-\\u169A\\u16A0-\\u16EA\\u16EE-\\u16F8\\u1700-\\u170C\\u170E-\\u1714\\u1720-\\u1734\\u1740-\\u1753\\u1760-\\u176C\\u176E-\\u1770\\u1772\\u1773\\u1780-\\u17D3\\u17D7\\u17DC\\u17DD\\u17E0-\\u17E9\\u180B-\\u180D\\u1810-\\u1819\\u1820-\\u1877\\u1880-\\u18AA\\u18B0-\\u18F5\\u1900-\\u191E\\u1920-\\u192B\\u1930-\\u193B\\u1946-\\u196D\\u1970-\\u1974\\u1980-\\u19AB\\u19B0-\\u19C9\\u19D0-\\u19D9\\u1A00-\\u1A1B\\u1A20-\\u1A5E\\u1A60-\\u1A7C\\u1A7F-\\u1A89\\u1A90-\\u1A99\\u1AA7\\u1AB0-\\u1ABD\\u1B00-\\u1B4B\\u1B50-\\u1B59\\u1B6B-\\u1B73\\u1B80-\\u1BF3\\u1C00-\\u1C37\\u1C40-\\u1C49\\u1C4D-\\u1C7D\\u1CD0-\\u1CD2\\u1CD4-\\u1CF6\\u1CF8\\u1CF9\\u1D00-\\u1DF5\\u1DFC-\\u1F15\\u1F18-\\u1F1D\\u1F20-\\u1F45\\u1F48-\\u1F4D\\u1F50-\\u1F57\\u1F59\\u1F5B\\u1F5D\\u1F5F-\\u1F7D\\u1F80-\\u1FB4\\u1FB6-\\u1FBC\\u1FBE\\u1FC2-\\u1FC4\\u1FC6-\\u1FCC\\u1FD0-\\u1FD3\\u1FD6-\\u1FDB\\u1FE0-\\u1FEC\\u1FF2-\\u1FF4\\u1FF6-\\u1FFC\\u200C\\u200D\\u203F\\u2040\\u2054\\u2071\\u207F\\u2090-\\u209C\\u20D0-\\u20DC\\u20E1\\u20E5-\\u20F0\\u2102\\u2107\\u210A-\\u2113\\u2115\\u2119-\\u211D\\u2124\\u2126\\u2128\\u212A-\\u212D\\u212F-\\u2139\\u213C-\\u213F\\u2145-\\u2149\\u214E\\u2160-\\u2188\\u2C00-\\u2C2E\\u2C30-\\u2C5E\\u2C60-\\u2CE4\\u2CEB-\\u2CF3\\u2D00-\\u2D25\\u2D27\\u2D2D\\u2D30-\\u2D67\\u2D6F\\u2D7F-\\u2D96\\u2DA0-\\u2DA6\\u2DA8-\\u2DAE\\u2DB0-\\u2DB6\\u2DB8-\\u2DBE\\u2DC0-\\u2DC6\\u2DC8-\\u2DCE\\u2DD0-\\u2DD6\\u2DD8-\\u2DDE\\u2DE0-\\u2DFF\\u2E2F\\u3005-\\u3007\\u3021-\\u302F\\u3031-\\u3035\\u3038-\\u303C\\u3041-\\u3096\\u3099\\u309A\\u309D-\\u309F\\u30A1-\\u30FA\\u30FC-\\u30FF\\u3105-\\u312D\\u3131-\\u318E\\u31A0-\\u31BA\\u31F0-\\u31FF\\u3400-\\u4DB5\\u4E00-\\u9FCC\\uA000-\\uA48C\\uA4D0-\\uA4FD\\uA500-\\uA60C\\uA610-\\uA62B\\uA640-\\uA66F\\uA674-\\uA67D\\uA67F-\\uA69D\\uA69F-\\uA6F1\\uA717-\\uA71F\\uA722-\\uA788\\uA78B-\\uA78E\\uA790-\\uA7AD\\uA7B0\\uA7B1\\uA7F7-\\uA827\\uA840-\\uA873\\uA880-\\uA8C4\\uA8D0-\\uA8D9\\uA8E0-\\uA8F7\\uA8FB\\uA900-\\uA92D\\uA930-\\uA953\\uA960-\\uA97C\\uA980-\\uA9C0\\uA9CF-\\uA9D9\\uA9E0-\\uA9FE\\uAA00-\\uAA36\\uAA40-\\uAA4D\\uAA50-\\uAA59\\uAA60-\\uAA76\\uAA7A-\\uAAC2\\uAADB-\\uAADD\\uAAE0-\\uAAEF\\uAAF2-\\uAAF6\\uAB01-\\uAB06\\uAB09-\\uAB0E\\uAB11-\\uAB16\\uAB20-\\uAB26\\uAB28-\\uAB2E\\uAB30-\\uAB5A\\uAB5C-\\uAB5F\\uAB64\\uAB65\\uABC0-\\uABEA\\uABEC\\uABED\\uABF0-\\uABF9\\uAC00-\\uD7A3\\uD7B0-\\uD7C6\\uD7CB-\\uD7FB\\uF900-\\uFA6D\\uFA70-\\uFAD9\\uFB00-\\uFB06\\uFB13-\\uFB17\\uFB1D-\\uFB28\\uFB2A-\\uFB36\\uFB38-\\uFB3C\\uFB3E\\uFB40\\uFB41\\uFB43\\uFB44\\uFB46-\\uFBB1\\uFBD3-\\uFD3D\\uFD50-\\uFD8F\\uFD92-\\uFDC7\\uFDF0-\\uFDFB\\uFE00-\\uFE0F\\uFE20-\\uFE2D\\uFE33\\uFE34\\uFE4D-\\uFE4F\\uFE70-\\uFE74\\uFE76-\\uFEFC\\uFF10-\\uFF19\\uFF21-\\uFF3A\\uFF3F\\uFF41-\\uFF5A\\uFF66-\\uFFBE\\uFFC2-\\uFFC7\\uFFCA-\\uFFCF\\uFFD2-\\uFFD7\\uFFDA-\\uFFDC]");
function assert2(condition, message) {
  if (!condition) {
    throw new Error("ASSERT: " + message);
  }
}
function isDecimalDigit2(ch2) {
  return ch2 >= 48 && ch2 <= 57;
}
function isHexDigit2(ch2) {
  return "0123456789abcdefABCDEF".includes(ch2);
}
function isOctalDigit2(ch2) {
  return "01234567".includes(ch2);
}
function isWhiteSpace2(ch2) {
  return ch2 === 32 || ch2 === 9 || ch2 === 11 || ch2 === 12 || ch2 === 160 || ch2 >= 5760 && [5760, 6158, 8192, 8193, 8194, 8195, 8196, 8197, 8198, 8199, 8200, 8201, 8202, 8239, 8287, 12288, 65279].includes(ch2);
}
function isLineTerminator2(ch2) {
  return ch2 === 10 || ch2 === 13 || ch2 === 8232 || ch2 === 8233;
}
function isIdentifierStart2(ch2) {
  return ch2 === 36 || ch2 === 95 || // $ (dollar) and _ (underscore)
  ch2 >= 65 && ch2 <= 90 || // A..Z
  ch2 >= 97 && ch2 <= 122 || // a..z
  ch2 === 92 || // \ (backslash)
  ch2 >= 128 && RegexNonAsciiIdentifierStart2.test(String.fromCharCode(ch2));
}
function isIdentifierPart2(ch2) {
  return ch2 === 36 || ch2 === 95 || // $ (dollar) and _ (underscore)
  ch2 >= 65 && ch2 <= 90 || // A..Z
  ch2 >= 97 && ch2 <= 122 || // a..z
  ch2 >= 48 && ch2 <= 57 || // 0..9
  ch2 === 92 || // \ (backslash)
  ch2 >= 128 && RegexNonAsciiIdentifierPart2.test(String.fromCharCode(ch2));
}
var keywords2 = {
  "if": 1,
  "in": 1,
  "do": 1,
  "var": 1,
  "for": 1,
  "new": 1,
  "try": 1,
  "let": 1,
  "this": 1,
  "else": 1,
  "case": 1,
  "void": 1,
  "with": 1,
  "enum": 1,
  "while": 1,
  "break": 1,
  "catch": 1,
  "throw": 1,
  "const": 1,
  "yield": 1,
  "class": 1,
  "super": 1,
  "return": 1,
  "typeof": 1,
  "delete": 1,
  "switch": 1,
  "export": 1,
  "import": 1,
  "public": 1,
  "static": 1,
  "default": 1,
  "finally": 1,
  "extends": 1,
  "package": 1,
  "private": 1,
  "function": 1,
  "continue": 1,
  "debugger": 1,
  "interface": 1,
  "protected": 1,
  "instanceof": 1,
  "implements": 1
};
function skipComment2() {
  while (index3 < length2) {
    const ch2 = source3.charCodeAt(index3);
    if (isWhiteSpace2(ch2) || isLineTerminator2(ch2)) {
      ++index3;
    } else {
      break;
    }
  }
}
function scanHexEscape2(prefix) {
  var i, len, ch2, code = 0;
  len = prefix === "u" ? 4 : 2;
  for (i = 0; i < len; ++i) {
    if (index3 < length2 && isHexDigit2(source3[index3])) {
      ch2 = source3[index3++];
      code = code * 16 + "0123456789abcdef".indexOf(ch2.toLowerCase());
    } else {
      throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
    }
  }
  return String.fromCharCode(code);
}
function scanUnicodeCodePointEscape2() {
  var ch2, code, cu1, cu2;
  ch2 = source3[index3];
  code = 0;
  if (ch2 === "}") {
    throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
  }
  while (index3 < length2) {
    ch2 = source3[index3++];
    if (!isHexDigit2(ch2)) {
      break;
    }
    code = code * 16 + "0123456789abcdef".indexOf(ch2.toLowerCase());
  }
  if (code > 1114111 || ch2 !== "}") {
    throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
  }
  if (code <= 65535) {
    return String.fromCharCode(code);
  }
  cu1 = (code - 65536 >> 10) + 55296;
  cu2 = (code - 65536 & 1023) + 56320;
  return String.fromCharCode(cu1, cu2);
}
function getEscapedIdentifier2() {
  var ch2, id2;
  ch2 = source3.charCodeAt(index3++);
  id2 = String.fromCharCode(ch2);
  if (ch2 === 92) {
    if (source3.charCodeAt(index3) !== 117) {
      throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
    }
    ++index3;
    ch2 = scanHexEscape2("u");
    if (!ch2 || ch2 === "\\" || !isIdentifierStart2(ch2.charCodeAt(0))) {
      throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
    }
    id2 = ch2;
  }
  while (index3 < length2) {
    ch2 = source3.charCodeAt(index3);
    if (!isIdentifierPart2(ch2)) {
      break;
    }
    ++index3;
    id2 += String.fromCharCode(ch2);
    if (ch2 === 92) {
      id2 = id2.substr(0, id2.length - 1);
      if (source3.charCodeAt(index3) !== 117) {
        throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
      }
      ++index3;
      ch2 = scanHexEscape2("u");
      if (!ch2 || ch2 === "\\" || !isIdentifierPart2(ch2.charCodeAt(0))) {
        throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
      }
      id2 += ch2;
    }
  }
  return id2;
}
function getIdentifier2() {
  var start, ch2;
  start = index3++;
  while (index3 < length2) {
    ch2 = source3.charCodeAt(index3);
    if (ch2 === 92) {
      index3 = start;
      return getEscapedIdentifier2();
    }
    if (isIdentifierPart2(ch2)) {
      ++index3;
    } else {
      break;
    }
  }
  return source3.slice(start, index3);
}
function scanIdentifier2() {
  var start, id2, type4;
  start = index3;
  id2 = source3.charCodeAt(index3) === 92 ? getEscapedIdentifier2() : getIdentifier2();
  if (id2.length === 1) {
    type4 = TokenIdentifier2;
  } else if (keywords2.hasOwnProperty(id2)) {
    type4 = TokenKeyword2;
  } else if (id2 === "null") {
    type4 = TokenNullLiteral2;
  } else if (id2 === "true" || id2 === "false") {
    type4 = TokenBooleanLiteral2;
  } else {
    type4 = TokenIdentifier2;
  }
  return {
    type: type4,
    value: id2,
    start,
    end: index3
  };
}
function scanPunctuator2() {
  var start = index3, code = source3.charCodeAt(index3), code2, ch1 = source3[index3], ch2, ch3, ch4;
  switch (code) {
    // Check for most common single-character punctuators.
    case 46:
    // . dot
    case 40:
    // ( open bracket
    case 41:
    // ) close bracket
    case 59:
    // ; semicolon
    case 44:
    // , comma
    case 123:
    // { open curly brace
    case 125:
    // } close curly brace
    case 91:
    // [
    case 93:
    // ]
    case 58:
    // :
    case 63:
    // ?
    case 126:
      ++index3;
      return {
        type: TokenPunctuator2,
        value: String.fromCharCode(code),
        start,
        end: index3
      };
    default:
      code2 = source3.charCodeAt(index3 + 1);
      if (code2 === 61) {
        switch (code) {
          case 43:
          // +
          case 45:
          // -
          case 47:
          // /
          case 60:
          // <
          case 62:
          // >
          case 94:
          // ^
          case 124:
          // |
          case 37:
          // %
          case 38:
          // &
          case 42:
            index3 += 2;
            return {
              type: TokenPunctuator2,
              value: String.fromCharCode(code) + String.fromCharCode(code2),
              start,
              end: index3
            };
          case 33:
          // !
          case 61:
            index3 += 2;
            if (source3.charCodeAt(index3) === 61) {
              ++index3;
            }
            return {
              type: TokenPunctuator2,
              value: source3.slice(start, index3),
              start,
              end: index3
            };
        }
      }
  }
  ch4 = source3.substr(index3, 4);
  if (ch4 === ">>>=") {
    index3 += 4;
    return {
      type: TokenPunctuator2,
      value: ch4,
      start,
      end: index3
    };
  }
  ch3 = ch4.substr(0, 3);
  if (ch3 === ">>>" || ch3 === "<<=" || ch3 === ">>=") {
    index3 += 3;
    return {
      type: TokenPunctuator2,
      value: ch3,
      start,
      end: index3
    };
  }
  ch2 = ch3.substr(0, 2);
  if (ch1 === ch2[1] && "+-<>&|".includes(ch1) || ch2 === "=>") {
    index3 += 2;
    return {
      type: TokenPunctuator2,
      value: ch2,
      start,
      end: index3
    };
  }
  if (ch2 === "//") {
    throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
  }
  if ("<>=!+-*%&|^/".includes(ch1)) {
    ++index3;
    return {
      type: TokenPunctuator2,
      value: ch1,
      start,
      end: index3
    };
  }
  throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
}
function scanHexLiteral2(start) {
  let number9 = "";
  while (index3 < length2) {
    if (!isHexDigit2(source3[index3])) {
      break;
    }
    number9 += source3[index3++];
  }
  if (number9.length === 0) {
    throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
  }
  if (isIdentifierStart2(source3.charCodeAt(index3))) {
    throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
  }
  return {
    type: TokenNumericLiteral2,
    value: parseInt("0x" + number9, 16),
    start,
    end: index3
  };
}
function scanOctalLiteral2(start) {
  let number9 = "0" + source3[index3++];
  while (index3 < length2) {
    if (!isOctalDigit2(source3[index3])) {
      break;
    }
    number9 += source3[index3++];
  }
  if (isIdentifierStart2(source3.charCodeAt(index3)) || isDecimalDigit2(source3.charCodeAt(index3))) {
    throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
  }
  return {
    type: TokenNumericLiteral2,
    value: parseInt(number9, 8),
    octal: true,
    start,
    end: index3
  };
}
function scanNumericLiteral2() {
  var number9, start, ch2;
  ch2 = source3[index3];
  assert2(isDecimalDigit2(ch2.charCodeAt(0)) || ch2 === ".", "Numeric literal must start with a decimal digit or a decimal point");
  start = index3;
  number9 = "";
  if (ch2 !== ".") {
    number9 = source3[index3++];
    ch2 = source3[index3];
    if (number9 === "0") {
      if (ch2 === "x" || ch2 === "X") {
        ++index3;
        return scanHexLiteral2(start);
      }
      if (isOctalDigit2(ch2)) {
        return scanOctalLiteral2(start);
      }
      if (ch2 && isDecimalDigit2(ch2.charCodeAt(0))) {
        throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
      }
    }
    while (isDecimalDigit2(source3.charCodeAt(index3))) {
      number9 += source3[index3++];
    }
    ch2 = source3[index3];
  }
  if (ch2 === ".") {
    number9 += source3[index3++];
    while (isDecimalDigit2(source3.charCodeAt(index3))) {
      number9 += source3[index3++];
    }
    ch2 = source3[index3];
  }
  if (ch2 === "e" || ch2 === "E") {
    number9 += source3[index3++];
    ch2 = source3[index3];
    if (ch2 === "+" || ch2 === "-") {
      number9 += source3[index3++];
    }
    if (isDecimalDigit2(source3.charCodeAt(index3))) {
      while (isDecimalDigit2(source3.charCodeAt(index3))) {
        number9 += source3[index3++];
      }
    } else {
      throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
    }
  }
  if (isIdentifierStart2(source3.charCodeAt(index3))) {
    throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
  }
  return {
    type: TokenNumericLiteral2,
    value: parseFloat(number9),
    start,
    end: index3
  };
}
function scanStringLiteral2() {
  var str = "", quote, start, ch2, code, octal = false;
  quote = source3[index3];
  assert2(quote === "'" || quote === '"', "String literal must starts with a quote");
  start = index3;
  ++index3;
  while (index3 < length2) {
    ch2 = source3[index3++];
    if (ch2 === quote) {
      quote = "";
      break;
    } else if (ch2 === "\\") {
      ch2 = source3[index3++];
      if (!ch2 || !isLineTerminator2(ch2.charCodeAt(0))) {
        switch (ch2) {
          case "u":
          case "x":
            if (source3[index3] === "{") {
              ++index3;
              str += scanUnicodeCodePointEscape2();
            } else {
              str += scanHexEscape2(ch2);
            }
            break;
          case "n":
            str += "\n";
            break;
          case "r":
            str += "\r";
            break;
          case "t":
            str += "	";
            break;
          case "b":
            str += "\b";
            break;
          case "f":
            str += "\f";
            break;
          case "v":
            str += "\v";
            break;
          default:
            if (isOctalDigit2(ch2)) {
              code = "01234567".indexOf(ch2);
              if (code !== 0) {
                octal = true;
              }
              if (index3 < length2 && isOctalDigit2(source3[index3])) {
                octal = true;
                code = code * 8 + "01234567".indexOf(source3[index3++]);
                if ("0123".includes(ch2) && index3 < length2 && isOctalDigit2(source3[index3])) {
                  code = code * 8 + "01234567".indexOf(source3[index3++]);
                }
              }
              str += String.fromCharCode(code);
            } else {
              str += ch2;
            }
            break;
        }
      } else {
        if (ch2 === "\r" && source3[index3] === "\n") {
          ++index3;
        }
      }
    } else if (isLineTerminator2(ch2.charCodeAt(0))) {
      break;
    } else {
      str += ch2;
    }
  }
  if (quote !== "") {
    throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
  }
  return {
    type: TokenStringLiteral2,
    value: str,
    octal,
    start,
    end: index3
  };
}
function testRegExp2(pattern, flags) {
  let tmp = pattern;
  if (flags.includes("u")) {
    tmp = tmp.replace(/\\u\{([0-9a-fA-F]+)\}/g, ($0, $1) => {
      if (parseInt($1, 16) <= 1114111) {
        return "x";
      }
      throwError2({}, MessageInvalidRegExp2);
    }).replace(/[\uD800-\uDBFF][\uDC00-\uDFFF]/g, "x");
  }
  try {
    new RegExp(tmp);
  } catch (e) {
    throwError2({}, MessageInvalidRegExp2);
  }
  try {
    return new RegExp(pattern, flags);
  } catch (exception) {
    return null;
  }
}
function scanRegExpBody2() {
  var ch2, str, classMarker, terminated, body;
  ch2 = source3[index3];
  assert2(ch2 === "/", "Regular expression literal must start with a slash");
  str = source3[index3++];
  classMarker = false;
  terminated = false;
  while (index3 < length2) {
    ch2 = source3[index3++];
    str += ch2;
    if (ch2 === "\\") {
      ch2 = source3[index3++];
      if (isLineTerminator2(ch2.charCodeAt(0))) {
        throwError2({}, MessageUnterminatedRegExp2);
      }
      str += ch2;
    } else if (isLineTerminator2(ch2.charCodeAt(0))) {
      throwError2({}, MessageUnterminatedRegExp2);
    } else if (classMarker) {
      if (ch2 === "]") {
        classMarker = false;
      }
    } else {
      if (ch2 === "/") {
        terminated = true;
        break;
      } else if (ch2 === "[") {
        classMarker = true;
      }
    }
  }
  if (!terminated) {
    throwError2({}, MessageUnterminatedRegExp2);
  }
  body = str.substr(1, str.length - 2);
  return {
    value: body,
    literal: str
  };
}
function scanRegExpFlags2() {
  var ch2, str, flags;
  str = "";
  flags = "";
  while (index3 < length2) {
    ch2 = source3[index3];
    if (!isIdentifierPart2(ch2.charCodeAt(0))) {
      break;
    }
    ++index3;
    if (ch2 === "\\" && index3 < length2) {
      throwError2({}, MessageUnexpectedToken2, ILLEGAL3);
    } else {
      flags += ch2;
      str += ch2;
    }
  }
  if (flags.search(/[^gimuy]/g) >= 0) {
    throwError2({}, MessageInvalidRegExp2, flags);
  }
  return {
    value: flags,
    literal: str
  };
}
function scanRegExp2() {
  var start, body, flags, value4;
  lookahead2 = null;
  skipComment2();
  start = index3;
  body = scanRegExpBody2();
  flags = scanRegExpFlags2();
  value4 = testRegExp2(body.value, flags.value);
  return {
    literal: body.literal + flags.literal,
    value: value4,
    regex: {
      pattern: body.value,
      flags: flags.value
    },
    start,
    end: index3
  };
}
function isIdentifierName2(token) {
  return token.type === TokenIdentifier2 || token.type === TokenKeyword2 || token.type === TokenBooleanLiteral2 || token.type === TokenNullLiteral2;
}
function advance2() {
  skipComment2();
  if (index3 >= length2) {
    return {
      type: TokenEOF2,
      start: index3,
      end: index3
    };
  }
  const ch2 = source3.charCodeAt(index3);
  if (isIdentifierStart2(ch2)) {
    return scanIdentifier2();
  }
  if (ch2 === 40 || ch2 === 41 || ch2 === 59) {
    return scanPunctuator2();
  }
  if (ch2 === 39 || ch2 === 34) {
    return scanStringLiteral2();
  }
  if (ch2 === 46) {
    if (isDecimalDigit2(source3.charCodeAt(index3 + 1))) {
      return scanNumericLiteral2();
    }
    return scanPunctuator2();
  }
  if (isDecimalDigit2(ch2)) {
    return scanNumericLiteral2();
  }
  return scanPunctuator2();
}
function lex2() {
  const token = lookahead2;
  index3 = token.end;
  lookahead2 = advance2();
  index3 = token.end;
  return token;
}
function peek3() {
  const pos = index3;
  lookahead2 = advance2();
  index3 = pos;
}
function finishArrayExpression2(elements) {
  const node = new ASTNode2(SyntaxArrayExpression2);
  node.elements = elements;
  return node;
}
function finishBinaryExpression2(operator2, left, right) {
  const node = new ASTNode2(operator2 === "||" || operator2 === "&&" ? SyntaxLogicalExpression2 : SyntaxBinaryExpression2);
  node.operator = operator2;
  node.left = left;
  node.right = right;
  return node;
}
function finishCallExpression2(callee, args) {
  const node = new ASTNode2(SyntaxCallExpression2);
  node.callee = callee;
  node.arguments = args;
  return node;
}
function finishConditionalExpression2(test2, consequent, alternate) {
  const node = new ASTNode2(SyntaxConditionalExpression2);
  node.test = test2;
  node.consequent = consequent;
  node.alternate = alternate;
  return node;
}
function finishIdentifier2(name) {
  const node = new ASTNode2(SyntaxIdentifier2);
  node.name = name;
  return node;
}
function finishLiteral2(token) {
  const node = new ASTNode2(SyntaxLiteral2);
  node.value = token.value;
  node.raw = source3.slice(token.start, token.end);
  if (token.regex) {
    if (node.raw === "//") {
      node.raw = "/(?:)/";
    }
    node.regex = token.regex;
  }
  return node;
}
function finishMemberExpression2(accessor2, object2, property2) {
  const node = new ASTNode2(SyntaxMemberExpression2);
  node.computed = accessor2 === "[";
  node.object = object2;
  node.property = property2;
  if (!node.computed) property2.member = true;
  return node;
}
function finishObjectExpression2(properties) {
  const node = new ASTNode2(SyntaxObjectExpression2);
  node.properties = properties;
  return node;
}
function finishProperty2(kind, key2, value4) {
  const node = new ASTNode2(SyntaxProperty2);
  node.key = key2;
  node.value = value4;
  node.kind = kind;
  return node;
}
function finishUnaryExpression2(operator2, argument) {
  const node = new ASTNode2(SyntaxUnaryExpression2);
  node.operator = operator2;
  node.argument = argument;
  node.prefix = true;
  return node;
}
function throwError2(token, messageFormat) {
  var error3, args = Array.prototype.slice.call(arguments, 2), msg = messageFormat.replace(/%(\d)/g, (whole, index4) => {
    assert2(index4 < args.length, "Message reference must be in range");
    return args[index4];
  });
  error3 = new Error(msg);
  error3.index = index3;
  error3.description = msg;
  throw error3;
}
function throwUnexpected2(token) {
  if (token.type === TokenEOF2) {
    throwError2(token, MessageUnexpectedEOS2);
  }
  if (token.type === TokenNumericLiteral2) {
    throwError2(token, MessageUnexpectedNumber2);
  }
  if (token.type === TokenStringLiteral2) {
    throwError2(token, MessageUnexpectedString2);
  }
  if (token.type === TokenIdentifier2) {
    throwError2(token, MessageUnexpectedIdentifier2);
  }
  if (token.type === TokenKeyword2) {
    throwError2(token, MessageUnexpectedReserved2);
  }
  throwError2(token, MessageUnexpectedToken2, token.value);
}
function expect2(value4) {
  const token = lex2();
  if (token.type !== TokenPunctuator2 || token.value !== value4) {
    throwUnexpected2(token);
  }
}
function match2(value4) {
  return lookahead2.type === TokenPunctuator2 && lookahead2.value === value4;
}
function matchKeyword2(keyword) {
  return lookahead2.type === TokenKeyword2 && lookahead2.value === keyword;
}
function parseArrayInitialiser2() {
  const elements = [];
  index3 = lookahead2.start;
  expect2("[");
  while (!match2("]")) {
    if (match2(",")) {
      lex2();
      elements.push(null);
    } else {
      elements.push(parseConditionalExpression2());
      if (!match2("]")) {
        expect2(",");
      }
    }
  }
  lex2();
  return finishArrayExpression2(elements);
}
function parseObjectPropertyKey2() {
  index3 = lookahead2.start;
  const token = lex2();
  if (token.type === TokenStringLiteral2 || token.type === TokenNumericLiteral2) {
    if (token.octal) {
      throwError2(token, MessageStrictOctalLiteral2);
    }
    return finishLiteral2(token);
  }
  return finishIdentifier2(token.value);
}
function parseObjectProperty2() {
  var token, key2, id2, value4;
  index3 = lookahead2.start;
  token = lookahead2;
  if (token.type === TokenIdentifier2) {
    id2 = parseObjectPropertyKey2();
    expect2(":");
    value4 = parseConditionalExpression2();
    return finishProperty2("init", id2, value4);
  }
  if (token.type === TokenEOF2 || token.type === TokenPunctuator2) {
    throwUnexpected2(token);
  } else {
    key2 = parseObjectPropertyKey2();
    expect2(":");
    value4 = parseConditionalExpression2();
    return finishProperty2("init", key2, value4);
  }
}
function parseObjectInitialiser2() {
  var properties = [], property2, name, key2, map3 = {}, toString2 = String;
  index3 = lookahead2.start;
  expect2("{");
  while (!match2("}")) {
    property2 = parseObjectProperty2();
    if (property2.key.type === SyntaxIdentifier2) {
      name = property2.key.name;
    } else {
      name = toString2(property2.key.value);
    }
    key2 = "$" + name;
    if (Object.prototype.hasOwnProperty.call(map3, key2)) {
      throwError2({}, MessageStrictDuplicateProperty2);
    } else {
      map3[key2] = true;
    }
    properties.push(property2);
    if (!match2("}")) {
      expect2(",");
    }
  }
  expect2("}");
  return finishObjectExpression2(properties);
}
function parseGroupExpression2() {
  expect2("(");
  const expr2 = parseExpression2();
  expect2(")");
  return expr2;
}
var legalKeywords2 = {
  "if": 1
};
function parsePrimaryExpression2() {
  var type4, token, expr2;
  if (match2("(")) {
    return parseGroupExpression2();
  }
  if (match2("[")) {
    return parseArrayInitialiser2();
  }
  if (match2("{")) {
    return parseObjectInitialiser2();
  }
  type4 = lookahead2.type;
  index3 = lookahead2.start;
  if (type4 === TokenIdentifier2 || legalKeywords2[lookahead2.value]) {
    expr2 = finishIdentifier2(lex2().value);
  } else if (type4 === TokenStringLiteral2 || type4 === TokenNumericLiteral2) {
    if (lookahead2.octal) {
      throwError2(lookahead2, MessageStrictOctalLiteral2);
    }
    expr2 = finishLiteral2(lex2());
  } else if (type4 === TokenKeyword2) {
    throw new Error(DISABLED2);
  } else if (type4 === TokenBooleanLiteral2) {
    token = lex2();
    token.value = token.value === "true";
    expr2 = finishLiteral2(token);
  } else if (type4 === TokenNullLiteral2) {
    token = lex2();
    token.value = null;
    expr2 = finishLiteral2(token);
  } else if (match2("/") || match2("/=")) {
    expr2 = finishLiteral2(scanRegExp2());
    peek3();
  } else {
    throwUnexpected2(lex2());
  }
  return expr2;
}
function parseArguments2() {
  const args = [];
  expect2("(");
  if (!match2(")")) {
    while (index3 < length2) {
      args.push(parseConditionalExpression2());
      if (match2(")")) {
        break;
      }
      expect2(",");
    }
  }
  expect2(")");
  return args;
}
function parseNonComputedProperty2() {
  index3 = lookahead2.start;
  const token = lex2();
  if (!isIdentifierName2(token)) {
    throwUnexpected2(token);
  }
  return finishIdentifier2(token.value);
}
function parseNonComputedMember2() {
  expect2(".");
  return parseNonComputedProperty2();
}
function parseComputedMember2() {
  expect2("[");
  const expr2 = parseExpression2();
  expect2("]");
  return expr2;
}
function parseLeftHandSideExpressionAllowCall2() {
  var expr2, args, property2;
  expr2 = parsePrimaryExpression2();
  for (; ; ) {
    if (match2(".")) {
      property2 = parseNonComputedMember2();
      expr2 = finishMemberExpression2(".", expr2, property2);
    } else if (match2("(")) {
      args = parseArguments2();
      expr2 = finishCallExpression2(expr2, args);
    } else if (match2("[")) {
      property2 = parseComputedMember2();
      expr2 = finishMemberExpression2("[", expr2, property2);
    } else {
      break;
    }
  }
  return expr2;
}
function parsePostfixExpression2() {
  const expr2 = parseLeftHandSideExpressionAllowCall2();
  if (lookahead2.type === TokenPunctuator2) {
    if (match2("++") || match2("--")) {
      throw new Error(DISABLED2);
    }
  }
  return expr2;
}
function parseUnaryExpression2() {
  var token, expr2;
  if (lookahead2.type !== TokenPunctuator2 && lookahead2.type !== TokenKeyword2) {
    expr2 = parsePostfixExpression2();
  } else if (match2("++") || match2("--")) {
    throw new Error(DISABLED2);
  } else if (match2("+") || match2("-") || match2("~") || match2("!")) {
    token = lex2();
    expr2 = parseUnaryExpression2();
    expr2 = finishUnaryExpression2(token.value, expr2);
  } else if (matchKeyword2("delete") || matchKeyword2("void") || matchKeyword2("typeof")) {
    throw new Error(DISABLED2);
  } else {
    expr2 = parsePostfixExpression2();
  }
  return expr2;
}
function binaryPrecedence2(token) {
  let prec = 0;
  if (token.type !== TokenPunctuator2 && token.type !== TokenKeyword2) {
    return 0;
  }
  switch (token.value) {
    case "||":
      prec = 1;
      break;
    case "&&":
      prec = 2;
      break;
    case "|":
      prec = 3;
      break;
    case "^":
      prec = 4;
      break;
    case "&":
      prec = 5;
      break;
    case "==":
    case "!=":
    case "===":
    case "!==":
      prec = 6;
      break;
    case "<":
    case ">":
    case "<=":
    case ">=":
    case "instanceof":
    case "in":
      prec = 7;
      break;
    case "<<":
    case ">>":
    case ">>>":
      prec = 8;
      break;
    case "+":
    case "-":
      prec = 9;
      break;
    case "*":
    case "/":
    case "%":
      prec = 11;
      break;
  }
  return prec;
}
function parseBinaryExpression2() {
  var marker, markers, expr2, token, prec, stack2, right, operator2, left, i;
  marker = lookahead2;
  left = parseUnaryExpression2();
  token = lookahead2;
  prec = binaryPrecedence2(token);
  if (prec === 0) {
    return left;
  }
  token.prec = prec;
  lex2();
  markers = [marker, lookahead2];
  right = parseUnaryExpression2();
  stack2 = [left, token, right];
  while ((prec = binaryPrecedence2(lookahead2)) > 0) {
    while (stack2.length > 2 && prec <= stack2[stack2.length - 2].prec) {
      right = stack2.pop();
      operator2 = stack2.pop().value;
      left = stack2.pop();
      markers.pop();
      expr2 = finishBinaryExpression2(operator2, left, right);
      stack2.push(expr2);
    }
    token = lex2();
    token.prec = prec;
    stack2.push(token);
    markers.push(lookahead2);
    expr2 = parseUnaryExpression2();
    stack2.push(expr2);
  }
  i = stack2.length - 1;
  expr2 = stack2[i];
  markers.pop();
  while (i > 1) {
    markers.pop();
    expr2 = finishBinaryExpression2(stack2[i - 1].value, stack2[i - 2], expr2);
    i -= 2;
  }
  return expr2;
}
function parseConditionalExpression2() {
  var expr2, consequent, alternate;
  expr2 = parseBinaryExpression2();
  if (match2("?")) {
    lex2();
    consequent = parseConditionalExpression2();
    expect2(":");
    alternate = parseConditionalExpression2();
    expr2 = finishConditionalExpression2(expr2, consequent, alternate);
  }
  return expr2;
}
function parseExpression2() {
  const expr2 = parseConditionalExpression2();
  if (match2(",")) {
    throw new Error(DISABLED2);
  }
  return expr2;
}
function parser3(code) {
  source3 = code;
  index3 = 0;
  length2 = source3.length;
  lookahead2 = null;
  peek3();
  const expr2 = parseExpression2();
  if (lookahead2.type !== TokenEOF2) {
    throw new Error("Unexpect token after expression.");
  }
  return expr2;
}

// ../deepseek-harness/node_modules/.pnpm/vega-lite@6.4.3_vega@6.3.1/node_modules/vega-lite/build/index.js
var version$1 = "6.4.3";
var pkg = {
  version: version$1
};
function isLogicalOr(op) {
  return hasProperty(op, "or");
}
function isLogicalAnd(op) {
  return hasProperty(op, "and");
}
function isLogicalNot(op) {
  return hasProperty(op, "not");
}
function forEachLeaf(op, fn) {
  if (isLogicalNot(op)) {
    forEachLeaf(op.not, fn);
  } else if (isLogicalAnd(op)) {
    for (const subop of op.and) {
      forEachLeaf(subop, fn);
    }
  } else if (isLogicalOr(op)) {
    for (const subop of op.or) {
      forEachLeaf(subop, fn);
    }
  } else {
    fn(op);
  }
}
function normalizeLogicalComposition(op, normalizer) {
  if (isLogicalNot(op)) {
    return { not: normalizeLogicalComposition(op.not, normalizer) };
  } else if (isLogicalAnd(op)) {
    return { and: op.and.map((o) => normalizeLogicalComposition(o, normalizer)) };
  } else if (isLogicalOr(op)) {
    return { or: op.or.map((o) => normalizeLogicalComposition(o, normalizer)) };
  } else {
    return normalizer(op);
  }
}
var duplicate = structuredClone;
function never(message) {
  throw new Error(message);
}
function pick3(obj, props) {
  const copy5 = {};
  for (const prop of props) {
    if (hasOwnProperty(obj, prop)) {
      copy5[prop] = obj[prop];
    }
  }
  return copy5;
}
function omit(obj, props) {
  const copy5 = { ...obj };
  for (const prop of props) {
    delete copy5[prop];
  }
  return copy5;
}
Set.prototype["toJSON"] = function() {
  return `Set(${[...this].map((x6) => stringify(x6)).join(",")})`;
};
function hash(a3) {
  if (isNumber(a3)) {
    return a3;
  }
  const str = isString(a3) ? a3 : stringify(a3);
  if (str.length < 250) {
    return str;
  }
  let h3 = 0;
  for (let i = 0; i < str.length; i++) {
    const char = str.charCodeAt(i);
    h3 = (h3 << 5) - h3 + char;
    h3 = h3 & h3;
  }
  return h3;
}
function isNullOrFalse(x6) {
  return x6 === false || x6 === null;
}
function contains2(array4, item) {
  return array4.includes(item);
}
function some(arr, f) {
  let i = 0;
  for (const [k, a3] of arr.entries()) {
    if (f(a3, k, i++)) {
      return true;
    }
  }
  return false;
}
function every(arr, f) {
  let i = 0;
  for (const [k, a3] of arr.entries()) {
    if (!f(a3, k, i++)) {
      return false;
    }
  }
  return true;
}
function mergeDeep(dest, ...src) {
  for (const s of src) {
    deepMerge_(dest, s ?? {});
  }
  return dest;
}
function deepMerge_(dest, src) {
  for (const property2 of keys3(src)) {
    writeConfig(dest, property2, src[property2], true);
  }
}
function unique(values4, f) {
  const results = [];
  const u4 = {};
  let v2;
  for (const val of values4) {
    v2 = f(val);
    if (v2 in u4) {
      continue;
    }
    u4[v2] = 1;
    results.push(val);
  }
  return results;
}
function setEqual(a3, b10) {
  if (a3.size !== b10.size) {
    return false;
  }
  for (const e of a3) {
    if (!b10.has(e)) {
      return false;
    }
  }
  return true;
}
function hasIntersection(a3, b10) {
  for (const key2 of a3) {
    if (b10.has(key2)) {
      return true;
    }
  }
  return false;
}
function prefixGenerator(a3) {
  const prefixes2 = /* @__PURE__ */ new Set();
  for (const x6 of a3) {
    const splitField = splitAccessPath(x6);
    const wrappedWithAccessors = splitField.map((y6, i) => i === 0 ? y6 : `[${y6}]`);
    const computedPrefixes = wrappedWithAccessors.map((_, i) => wrappedWithAccessors.slice(0, i + 1).join(""));
    for (const y6 of computedPrefixes) {
      prefixes2.add(y6);
    }
  }
  return prefixes2;
}
function fieldIntersection(a3, b10) {
  if (a3 === void 0 || b10 === void 0) {
    return true;
  }
  return hasIntersection(prefixGenerator(a3), prefixGenerator(b10));
}
function isEmpty(obj) {
  return keys3(obj).length === 0;
}
var keys3 = Object.keys;
var vals = Object.values;
var entries$1 = Object.entries;
function isBoolean3(b10) {
  return b10 === true || b10 === false;
}
function isPrimitive(v2) {
  return isString(v2) || isNumber(v2) || isBoolean3(v2);
}
function varName(s) {
  const alphanumericS = s.replace(/\W/g, "_");
  return (s.match(/^\d+/) ? "_" : "") + alphanumericS;
}
function logicalExpr(op, cb) {
  if (isLogicalNot(op)) {
    return `!(${logicalExpr(op.not, cb)})`;
  } else if (isLogicalAnd(op)) {
    return `(${op.and.map((and) => logicalExpr(and, cb)).join(") && (")})`;
  } else if (isLogicalOr(op)) {
    return `(${op.or.map((or3) => logicalExpr(or3, cb)).join(") || (")})`;
  } else {
    return cb(op);
  }
}
function deleteNestedProperty(obj, orderedProps) {
  if (orderedProps.length === 0) {
    return true;
  }
  const prop = orderedProps.shift();
  if (prop in obj && deleteNestedProperty(obj[prop], orderedProps)) {
    delete obj[prop];
  }
  return isEmpty(obj);
}
function titleCase(s) {
  return s.charAt(0).toUpperCase() + s.substr(1);
}
function accessPathWithDatum(path4, datum3 = "datum") {
  const pieces = splitAccessPath(path4);
  const prefixes2 = [];
  for (let i = 1; i <= pieces.length; i++) {
    const prefix = `[${pieces.slice(0, i).map(stringValue).join("][")}]`;
    prefixes2.push(`${datum3}${prefix}`);
  }
  return prefixes2.join(" && ");
}
function flatAccessWithDatum(path4, datum3 = "datum") {
  return `${datum3}[${stringValue(splitAccessPath(path4).join("."))}]`;
}
function accessWithDatumToUnescapedPath(unescapedPath) {
  const singleQuoteEscapedPath = unescapedPath.replaceAll("'", "\\'");
  return `datum['${singleQuoteEscapedPath}']`;
}
function unescapeSingleQuoteAndPathDot(escapedPath) {
  return escapedPath.replaceAll("\\'", "'").replaceAll("\\.", ".");
}
function escapePathAccess(string) {
  return string.replace(/(\[|\]|\.|'|")/g, "\\$1");
}
function replacePathInField(path4) {
  return `${splitAccessPath(path4).map(escapePathAccess).join("\\.")}`;
}
function replaceAll(string, find4, replacement) {
  return string.replace(new RegExp(find4.replace(/[-/\\^$*+?.()|[\]{}]/g, "\\$&"), "g"), replacement);
}
function removePathFromField(path4) {
  return `${splitAccessPath(path4).join(".")}`;
}
function accessPathDepth(path4) {
  if (!path4) {
    return 0;
  }
  return splitAccessPath(path4).length;
}
function getFirstDefined(...args) {
  return args.find((a3) => a3 !== void 0);
}
var idCounter = 42;
function uniqueId(prefix) {
  const id2 = ++idCounter;
  return prefix ? String(prefix) + id2 : id2;
}
function internalField(name) {
  return isInternalField(name) ? name : `__${name}`;
}
function isInternalField(name) {
  return name.startsWith("__");
}
function normalizeAngle(angle2) {
  if (angle2 === void 0) {
    return void 0;
  }
  return (angle2 % 360 + 360) % 360;
}
function isNumeric(value4) {
  if (isNumber(value4)) {
    return true;
  }
  return !isNaN(value4) && !isNaN(parseFloat(value4));
}
var clonedProto = Object.getPrototypeOf(structuredClone({}));
function deepEqual(a3, b10) {
  if (a3 === b10)
    return true;
  if (a3 && b10 && typeof a3 == "object" && typeof b10 == "object") {
    if (a3.constructor.name !== b10.constructor.name)
      return false;
    let length3;
    let i;
    if (Array.isArray(a3)) {
      length3 = a3.length;
      if (length3 != b10.length)
        return false;
      for (i = length3; i-- !== 0; )
        if (!deepEqual(a3[i], b10[i]))
          return false;
      return true;
    }
    if (a3 instanceof Map && b10 instanceof Map) {
      if (a3.size !== b10.size)
        return false;
      for (const e of a3.entries())
        if (!b10.has(e[0]))
          return false;
      for (const e of a3.entries())
        if (!deepEqual(e[1], b10.get(e[0])))
          return false;
      return true;
    }
    if (a3 instanceof Set && b10 instanceof Set) {
      if (a3.size !== b10.size)
        return false;
      for (const e of a3.entries())
        if (!b10.has(e[0]))
          return false;
      return true;
    }
    if (ArrayBuffer.isView(a3) && ArrayBuffer.isView(b10)) {
      length3 = a3.length;
      if (length3 != b10.length)
        return false;
      for (i = length3; i-- !== 0; )
        if (a3[i] !== b10[i])
          return false;
      return true;
    }
    if (a3.constructor === RegExp)
      return a3.source === b10.source && a3.flags === b10.flags;
    if (a3.valueOf !== Object.prototype.valueOf && a3.valueOf !== clonedProto.valueOf)
      return a3.valueOf() === b10.valueOf();
    if (a3.toString !== Object.prototype.toString && a3.toString !== clonedProto.toString)
      return a3.toString() === b10.toString();
    const ks = Object.keys(a3);
    length3 = ks.length;
    if (length3 !== Object.keys(b10).length)
      return false;
    for (i = length3; i-- !== 0; )
      if (!Object.prototype.hasOwnProperty.call(b10, ks[i]))
        return false;
    for (i = length3; i-- !== 0; ) {
      const key2 = ks[i];
      if (!deepEqual(a3[key2], b10[key2]))
        return false;
    }
    return true;
  }
  return a3 !== a3 && b10 !== b10;
}
function stringify(data4) {
  const seen = [];
  return (function _stringify(node) {
    if (node?.toJSON && typeof node.toJSON === "function") {
      node = node.toJSON();
    }
    if (node === void 0)
      return void 0;
    if (typeof node == "number")
      return isFinite(node) ? `${node}` : "null";
    if (typeof node !== "object")
      return JSON.stringify(node);
    let i;
    let out;
    if (Array.isArray(node)) {
      out = "[";
      for (i = 0; i < node.length; i++) {
        if (i)
          out += ",";
        out += _stringify(node[i]) || "null";
      }
      return `${out}]`;
    }
    if (node === null)
      return "null";
    if (seen.includes(node)) {
      throw new TypeError("Converting circular structure to JSON");
    }
    const seenIndex = seen.push(node) - 1;
    const ks = Object.keys(node).sort();
    out = "";
    for (i = 0; i < ks.length; i++) {
      const key2 = ks[i];
      const value4 = _stringify(node[key2]);
      if (!value4)
        continue;
      if (out)
        out += ",";
      out += `${JSON.stringify(key2)}:${value4}`;
    }
    seen.splice(seenIndex, 1);
    return `{${out}}`;
  })(data4);
}
function hasProperty(obj, key2) {
  return isObject(obj) && hasOwnProperty(obj, key2) && obj[key2] !== void 0;
}
var ROW = "row";
var COLUMN = "column";
var FACET = "facet";
var X3 = "x";
var Y3 = "y";
var X23 = "x2";
var Y23 = "y2";
var XOFFSET = "xOffset";
var YOFFSET = "yOffset";
var RADIUS = "radius";
var RADIUS2 = "radius2";
var THETA = "theta";
var THETA2 = "theta2";
var LATITUDE = "latitude";
var LONGITUDE = "longitude";
var LATITUDE2 = "latitude2";
var LONGITUDE2 = "longitude2";
var TIME = "time";
var COLOR = "color";
var FILL = "fill";
var STROKE = "stroke";
var SHAPE = "shape";
var SIZE2 = "size";
var ANGLE = "angle";
var OPACITY = "opacity";
var FILLOPACITY = "fillOpacity";
var STROKEOPACITY = "strokeOpacity";
var STROKEWIDTH = "strokeWidth";
var STROKEDASH = "strokeDash";
var TEXT$1 = "text";
var ORDER = "order";
var DETAIL = "detail";
var KEY = "key";
var TOOLTIP = "tooltip";
var HREF = "href";
var URL = "url";
var DESCRIPTION = "description";
var POSITION_CHANNEL_INDEX = {
  x: 1,
  y: 1,
  x2: 1,
  y2: 1
};
var POLAR_POSITION_CHANNEL_INDEX = {
  theta: 1,
  theta2: 1,
  radius: 1,
  radius2: 1
};
function isPolarPositionChannel(c3) {
  return hasOwnProperty(POLAR_POSITION_CHANNEL_INDEX, c3);
}
var GEO_POSIITON_CHANNEL_INDEX = {
  longitude: 1,
  longitude2: 1,
  latitude: 1,
  latitude2: 1
};
function getPositionChannelFromLatLong(channel) {
  switch (channel) {
    case LATITUDE:
      return "y";
    case LATITUDE2:
      return "y2";
    case LONGITUDE:
      return "x";
    case LONGITUDE2:
      return "x2";
  }
}
function isGeoPositionChannel(c3) {
  return hasOwnProperty(GEO_POSIITON_CHANNEL_INDEX, c3);
}
var GEOPOSITION_CHANNELS = keys3(GEO_POSIITON_CHANNEL_INDEX);
var UNIT_CHANNEL_INDEX = {
  ...POSITION_CHANNEL_INDEX,
  ...POLAR_POSITION_CHANNEL_INDEX,
  ...GEO_POSIITON_CHANNEL_INDEX,
  xOffset: 1,
  yOffset: 1,
  // color
  color: 1,
  fill: 1,
  stroke: 1,
  // time
  time: 1,
  // other non-position with scale
  opacity: 1,
  fillOpacity: 1,
  strokeOpacity: 1,
  strokeWidth: 1,
  strokeDash: 1,
  size: 1,
  angle: 1,
  shape: 1,
  // channels without scales
  order: 1,
  text: 1,
  detail: 1,
  key: 1,
  tooltip: 1,
  href: 1,
  url: 1,
  description: 1
};
function isColorChannel(channel) {
  return channel === COLOR || channel === FILL || channel === STROKE;
}
var FACET_CHANNEL_INDEX = {
  row: 1,
  column: 1,
  facet: 1
};
var FACET_CHANNELS = keys3(FACET_CHANNEL_INDEX);
var CHANNEL_INDEX = {
  ...UNIT_CHANNEL_INDEX,
  ...FACET_CHANNEL_INDEX
};
var CHANNELS = keys3(CHANNEL_INDEX);
var { order: _o, detail: _d, tooltip: _tt1, ...SINGLE_DEF_CHANNEL_INDEX } = CHANNEL_INDEX;
var { row: _r, column: _c, facet: _f, ...SINGLE_DEF_UNIT_CHANNEL_INDEX } = SINGLE_DEF_CHANNEL_INDEX;
function isSingleDefUnitChannel(str) {
  return hasOwnProperty(SINGLE_DEF_UNIT_CHANNEL_INDEX, str);
}
function isChannel(str) {
  return hasOwnProperty(CHANNEL_INDEX, str);
}
var SECONDARY_RANGE_CHANNEL = [X23, Y23, LATITUDE2, LONGITUDE2, THETA2, RADIUS2];
function isSecondaryRangeChannel(c3) {
  const main2 = getMainRangeChannel(c3);
  return main2 !== c3;
}
function getMainRangeChannel(channel) {
  switch (channel) {
    case X23:
      return X3;
    case Y23:
      return Y3;
    case LATITUDE2:
      return LATITUDE;
    case LONGITUDE2:
      return LONGITUDE;
    case THETA2:
      return THETA;
    case RADIUS2:
      return RADIUS;
  }
  return channel;
}
function getVgPositionChannel(channel) {
  if (isPolarPositionChannel(channel)) {
    switch (channel) {
      case THETA:
        return "startAngle";
      case THETA2:
        return "endAngle";
      case RADIUS:
        return "outerRadius";
      case RADIUS2:
        return "innerRadius";
    }
  }
  return channel;
}
function getSecondaryRangeChannel(channel) {
  switch (channel) {
    case X3:
      return X23;
    case Y3:
      return Y23;
    case LATITUDE:
      return LATITUDE2;
    case LONGITUDE:
      return LONGITUDE2;
    case THETA:
      return THETA2;
    case RADIUS:
      return RADIUS2;
  }
  return void 0;
}
function getSizeChannel(channel) {
  switch (channel) {
    case X3:
    case X23:
      return "width";
    case Y3:
    case Y23:
      return "height";
  }
  return void 0;
}
function getOffsetChannel(channel) {
  switch (channel) {
    case X3:
      return "xOffset";
    case Y3:
      return "yOffset";
    case X23:
      return "x2Offset";
    case Y23:
      return "y2Offset";
    case THETA:
      return "thetaOffset";
    case RADIUS:
      return "radiusOffset";
    case THETA2:
      return "theta2Offset";
    case RADIUS2:
      return "radius2Offset";
  }
  return void 0;
}
function getOffsetScaleChannel(channel) {
  switch (channel) {
    case X3:
      return "xOffset";
    case Y3:
      return "yOffset";
  }
  return void 0;
}
function getMainChannelFromOffsetChannel(channel) {
  switch (channel) {
    case "xOffset":
      return "x";
    case "yOffset":
      return "y";
  }
}
var UNIT_CHANNELS = keys3(UNIT_CHANNEL_INDEX);
var {
  x: _x,
  y: _y,
  // x2 and y2 share the same scale as x and y
  x2: _x2,
  y2: _y2,
  //
  xOffset: _xo,
  yOffset: _yo,
  latitude: _latitude,
  longitude: _longitude,
  latitude2: _latitude2,
  longitude2: _longitude2,
  theta: _theta,
  theta2: _theta2,
  radius: _radius,
  radius2: _radius2,
  // The rest of unit channels then have scale
  ...NONPOSITION_CHANNEL_INDEX
} = UNIT_CHANNEL_INDEX;
var NONPOSITION_CHANNELS = keys3(NONPOSITION_CHANNEL_INDEX);
var POSITION_SCALE_CHANNEL_INDEX = {
  x: 1,
  y: 1
};
var POSITION_SCALE_CHANNELS = keys3(POSITION_SCALE_CHANNEL_INDEX);
function isXorY(channel) {
  return hasOwnProperty(POSITION_SCALE_CHANNEL_INDEX, channel);
}
var POLAR_POSITION_SCALE_CHANNEL_INDEX = {
  theta: 1,
  radius: 1
};
var POLAR_POSITION_SCALE_CHANNELS = keys3(POLAR_POSITION_SCALE_CHANNEL_INDEX);
function getPositionScaleChannel(sizeType) {
  return sizeType === "width" ? X3 : Y3;
}
var OFFSET_SCALE_CHANNEL_INDEX = { xOffset: 1, yOffset: 1 };
function isXorYOffset(channel) {
  return hasOwnProperty(OFFSET_SCALE_CHANNEL_INDEX, channel);
}
var TIME_SCALE_CHANNEL_INDEX = {
  time: 1
};
function isTime(channel) {
  return channel in TIME_SCALE_CHANNEL_INDEX;
}
var {
  // x2 and y2 share the same scale as x and y
  // text and tooltip have format instead of scale,
  // href has neither format, nor scale
  text: _t,
  tooltip: _tt,
  href: _hr,
  url: _u,
  description: _al,
  // detail and order have no scale
  detail: _dd,
  key: _k,
  order: _oo,
  ...NONPOSITION_SCALE_CHANNEL_INDEX
} = NONPOSITION_CHANNEL_INDEX;
var NONPOSITION_SCALE_CHANNELS = keys3(NONPOSITION_SCALE_CHANNEL_INDEX);
function isNonPositionScaleChannel(channel) {
  return hasOwnProperty(NONPOSITION_CHANNEL_INDEX, channel);
}
function supportLegend(channel) {
  switch (channel) {
    case COLOR:
    case FILL:
    case STROKE:
    case SIZE2:
    case SHAPE:
    case OPACITY:
    case STROKEWIDTH:
    case STROKEDASH:
      return true;
    case FILLOPACITY:
    case STROKEOPACITY:
    case ANGLE:
    case TIME:
      return false;
  }
}
var SCALE_CHANNEL_INDEX = {
  ...POSITION_SCALE_CHANNEL_INDEX,
  ...POLAR_POSITION_SCALE_CHANNEL_INDEX,
  ...OFFSET_SCALE_CHANNEL_INDEX,
  ...NONPOSITION_SCALE_CHANNEL_INDEX
};
var SCALE_CHANNELS = keys3(SCALE_CHANNEL_INDEX);
function isScaleChannel(channel) {
  return hasOwnProperty(SCALE_CHANNEL_INDEX, channel);
}
function supportMark(channel, mark) {
  return getSupportedMark(channel)[mark];
}
var ALL_MARKS = {
  // all marks
  arc: "always",
  area: "always",
  bar: "always",
  circle: "always",
  geoshape: "always",
  image: "always",
  line: "always",
  rule: "always",
  point: "always",
  rect: "always",
  square: "always",
  trail: "always",
  text: "always",
  tick: "always"
};
var { geoshape: _g, ...ALL_MARKS_EXCEPT_GEOSHAPE } = ALL_MARKS;
function getSupportedMark(channel) {
  switch (channel) {
    case COLOR:
    case FILL:
    case STROKE:
    // falls through
    case DESCRIPTION:
    case DETAIL:
    case KEY:
    case TOOLTIP:
    case HREF:
    case ORDER:
    // TODO: revise (order might not support rect, which is not stackable?)
    case OPACITY:
    case FILLOPACITY:
    case STROKEOPACITY:
    case STROKEWIDTH:
    // falls through
    case FACET:
    case ROW:
    // falls through
    case COLUMN:
      return ALL_MARKS;
    case X3:
    case Y3:
    case XOFFSET:
    case YOFFSET:
    case LATITUDE:
    case LONGITUDE:
    case TIME:
      return ALL_MARKS_EXCEPT_GEOSHAPE;
    case X23:
    case Y23:
    case LATITUDE2:
    case LONGITUDE2:
      return {
        area: "always",
        bar: "always",
        image: "always",
        rect: "always",
        rule: "always",
        circle: "binned",
        point: "binned",
        square: "binned",
        tick: "binned",
        line: "binned",
        trail: "binned"
      };
    case SIZE2:
      return {
        point: "always",
        tick: "always",
        rule: "always",
        circle: "always",
        square: "always",
        bar: "always",
        text: "always",
        line: "always",
        trail: "always"
      };
    case STROKEDASH:
      return {
        line: "always",
        point: "always",
        tick: "always",
        rule: "always",
        circle: "always",
        square: "always",
        bar: "always",
        geoshape: "always"
      };
    case SHAPE:
      return { point: "always", geoshape: "always" };
    case TEXT$1:
      return { text: "always" };
    case ANGLE:
      return { point: "always", square: "always", text: "always" };
    case URL:
      return { image: "always" };
    case THETA:
      return { text: "always", arc: "always" };
    case RADIUS:
      return { text: "always", arc: "always" };
    case THETA2:
    case RADIUS2:
      return { arc: "always" };
  }
}
function rangeType(channel) {
  switch (channel) {
    case X3:
    case Y3:
    case THETA:
    case RADIUS:
    case XOFFSET:
    case YOFFSET:
    case SIZE2:
    case ANGLE:
    case STROKEWIDTH:
    case OPACITY:
    case FILLOPACITY:
    case STROKEOPACITY:
    case TIME:
    // X2 and Y2 use X and Y scales, so they similarly have continuous range. [falls through]
    case X23:
    case Y23:
    case THETA2:
    case RADIUS2:
      return void 0;
    case FACET:
    case ROW:
    case COLUMN:
    case SHAPE:
    case STROKEDASH:
    // TEXT, TOOLTIP, URL, and HREF have no scale but have discrete output [falls through]
    case TEXT$1:
    case TOOLTIP:
    case HREF:
    case URL:
    case DESCRIPTION:
      return "discrete";
    // Color can be either continuous or discrete, depending on scale type.
    case COLOR:
    case FILL:
    case STROKE:
      return "flexible";
    // No scale, no range type.
    case LATITUDE:
    case LONGITUDE:
    case LATITUDE2:
    case LONGITUDE2:
    case DETAIL:
    case KEY:
    case ORDER:
      return void 0;
  }
}
var AGGREGATE_OP_INDEX = {
  argmax: 1,
  argmin: 1,
  average: 1,
  count: 1,
  distinct: 1,
  exponential: 1,
  exponentialb: 1,
  product: 1,
  max: 1,
  mean: 1,
  median: 1,
  min: 1,
  missing: 1,
  q1: 1,
  q3: 1,
  ci0: 1,
  ci1: 1,
  stderr: 1,
  stdev: 1,
  stdevp: 1,
  sum: 1,
  valid: 1,
  values: 1,
  variance: 1,
  variancep: 1
};
var MULTIDOMAIN_SORT_OP_INDEX = {
  count: 1,
  min: 1,
  max: 1
};
function isArgminDef(a3) {
  return hasProperty(a3, "argmin");
}
function isArgmaxDef(a3) {
  return hasProperty(a3, "argmax");
}
function isAggregateOp(a3) {
  return isString(a3) && hasOwnProperty(AGGREGATE_OP_INDEX, a3);
}
var COUNTING_OPS = /* @__PURE__ */ new Set([
  "count",
  "valid",
  "missing",
  "distinct"
]);
function isCountingAggregateOp(aggregate) {
  return isString(aggregate) && COUNTING_OPS.has(aggregate);
}
function isMinMaxOp(aggregate) {
  return isString(aggregate) && contains2(["min", "max"], aggregate);
}
var SUM_OPS = /* @__PURE__ */ new Set([
  "count",
  "sum",
  "distinct",
  "valid",
  "missing"
]);
var SHARED_DOMAIN_OPS = /* @__PURE__ */ new Set([
  "mean",
  "average",
  "median",
  "q1",
  "q3",
  "min",
  "max"
]);
function binToString(bin3) {
  if (isBoolean(bin3)) {
    bin3 = normalizeBin(bin3, void 0);
  }
  return `bin${keys3(bin3).map((p) => isParameterExtent(bin3[p]) ? varName(`_${p}_${entries$1(bin3[p])}`) : varName(`_${p}_${bin3[p]}`)).join("")}`;
}
function isBinning(bin3) {
  return bin3 === true || isBinParams(bin3) && !bin3.binned;
}
function isBinned(bin3) {
  return bin3 === "binned" || isBinParams(bin3) && bin3.binned === true;
}
function isBinParams(bin3) {
  return isObject(bin3);
}
function isParameterExtent(extent2) {
  return hasProperty(extent2, "param");
}
function autoMaxBins(channel) {
  switch (channel) {
    case ROW:
    case COLUMN:
    case SIZE2:
    case COLOR:
    case FILL:
    case STROKE:
    case STROKEWIDTH:
    case OPACITY:
    case FILLOPACITY:
    case STROKEOPACITY:
    // Facets and Size shouldn't have too many bins
    // We choose 6 like shape to simplify the rule [falls through]
    case SHAPE:
      return 6;
    // Vega's "shape" has 6 distinct values
    case STROKEDASH:
      return 4;
    // We only provide 5 different stroke dash values (but 4 is more effective)
    default:
      return 10;
  }
}
function isExprRef(o) {
  return hasProperty(o, "expr");
}
function replaceExprRef(index4, { level } = { level: 0 }) {
  const props = keys3(index4 || {});
  const newIndex = {};
  for (const prop of props) {
    newIndex[prop] = level === 0 ? signalRefOrValue(index4[prop]) : replaceExprRef(index4[prop], { level: level - 1 });
  }
  return newIndex;
}
function extractTitleConfig(titleConfig) {
  const {
    // These are non-mark title config that need to be hardcoded
    anchor,
    frame: frame2,
    offset: offset6,
    orient: orient2,
    angle: angle2,
    limit,
    // color needs to be redirect to fill
    color: color6,
    // subtitle properties
    subtitleColor,
    subtitleFont,
    subtitleFontSize,
    subtitleFontStyle,
    subtitleFontWeight,
    subtitleLineHeight,
    subtitlePadding,
    // The rest are mark config.
    ...rest
  } = titleConfig;
  const titleMarkConfig = {
    ...rest,
    ...color6 ? { fill: color6 } : {}
  };
  const nonMarkTitleProperties = {
    ...anchor ? { anchor } : {},
    ...frame2 ? { frame: frame2 } : {},
    ...offset6 ? { offset: offset6 } : {},
    ...orient2 ? { orient: orient2 } : {},
    ...angle2 !== void 0 ? { angle: angle2 } : {},
    ...limit !== void 0 ? { limit } : {}
  };
  const subtitle = {
    ...subtitleColor ? { subtitleColor } : {},
    ...subtitleFont ? { subtitleFont } : {},
    ...subtitleFontSize ? { subtitleFontSize } : {},
    ...subtitleFontStyle ? { subtitleFontStyle } : {},
    ...subtitleFontWeight ? { subtitleFontWeight } : {},
    ...subtitleLineHeight ? { subtitleLineHeight } : {},
    ...subtitlePadding ? { subtitlePadding } : {}
  };
  const subtitleMarkConfig = pick3(titleConfig, ["align", "baseline", "dx", "dy", "limit"]);
  return { titleMarkConfig, subtitleMarkConfig, nonMarkTitleProperties, subtitle };
}
function isText(v2) {
  return isString(v2) || isArray(v2) && isString(v2[0]);
}
function isSignalRef(o) {
  return hasProperty(o, "signal");
}
function isVgRangeStep(range7) {
  return hasProperty(range7, "step");
}
function isDataRefUnionedDomain(domain4) {
  if (!isArray(domain4)) {
    return hasProperty(domain4, "fields") && !hasProperty(domain4, "data");
  }
  return false;
}
function isFieldRefUnionDomain(domain4) {
  if (!isArray(domain4)) {
    return hasProperty(domain4, "fields") && hasProperty(domain4, "data");
  }
  return false;
}
function isDataRefDomain(domain4) {
  if (!isArray(domain4)) {
    return hasProperty(domain4, "field") && hasProperty(domain4, "data");
  }
  return false;
}
var VG_MARK_CONFIG_INDEX = {
  aria: 1,
  description: 1,
  ariaRole: 1,
  ariaRoleDescription: 1,
  blend: 1,
  opacity: 1,
  fill: 1,
  fillOpacity: 1,
  stroke: 1,
  strokeCap: 1,
  strokeWidth: 1,
  strokeOpacity: 1,
  strokeDash: 1,
  strokeDashOffset: 1,
  strokeJoin: 1,
  strokeOffset: 1,
  strokeMiterLimit: 1,
  startAngle: 1,
  endAngle: 1,
  padAngle: 1,
  innerRadius: 1,
  outerRadius: 1,
  size: 1,
  shape: 1,
  interpolate: 1,
  tension: 1,
  orient: 1,
  align: 1,
  baseline: 1,
  text: 1,
  dir: 1,
  dx: 1,
  dy: 1,
  ellipsis: 1,
  limit: 1,
  radius: 1,
  theta: 1,
  angle: 1,
  font: 1,
  fontSize: 1,
  fontWeight: 1,
  fontStyle: 1,
  lineBreak: 1,
  lineHeight: 1,
  cursor: 1,
  href: 1,
  tooltip: 1,
  cornerRadius: 1,
  cornerRadiusTopLeft: 1,
  cornerRadiusTopRight: 1,
  cornerRadiusBottomLeft: 1,
  cornerRadiusBottomRight: 1,
  aspect: 1,
  width: 1,
  height: 1,
  url: 1,
  smooth: 1
  // commented below are vg channel that do not have mark config.
  // x: 1,
  // y: 1,
  // x2: 1,
  // y2: 1,
  // xc'|'yc'
  // clip: 1,
  // path: 1,
  // url: 1,
};
var VG_MARK_CONFIGS = keys3(VG_MARK_CONFIG_INDEX);
var VG_MARK_INDEX = {
  arc: 1,
  area: 1,
  group: 1,
  image: 1,
  line: 1,
  path: 1,
  rect: 1,
  rule: 1,
  shape: 1,
  symbol: 1,
  text: 1,
  trail: 1
};
var VG_CORNERRADIUS_CHANNELS = [
  "cornerRadius",
  "cornerRadiusTopLeft",
  "cornerRadiusTopRight",
  "cornerRadiusBottomLeft",
  "cornerRadiusBottomRight"
];
var BIN_RANGE_DELIMITER = " \u2013 ";
function signalOrValueRefWithCondition(val) {
  const condition = isArray(val.condition) ? val.condition.map(conditionalSignalRefOrValue) : conditionalSignalRefOrValue(val.condition);
  return {
    ...signalRefOrValue(val),
    condition
  };
}
function signalRefOrValue(value4) {
  if (isExprRef(value4)) {
    const { expr: expr2, ...rest } = value4;
    return { signal: expr2, ...rest };
  }
  return value4;
}
function conditionalSignalRefOrValue(value4) {
  if (isExprRef(value4)) {
    const { expr: expr2, ...rest } = value4;
    return { signal: expr2, ...rest };
  }
  return value4;
}
function signalOrValueRef(value4) {
  if (isExprRef(value4)) {
    const { expr: expr2, ...rest } = value4;
    return { signal: expr2, ...rest };
  }
  if (isSignalRef(value4)) {
    return value4;
  }
  return value4 !== void 0 ? { value: value4 } : void 0;
}
function exprFromSignalRefOrValue(ref2) {
  if (isSignalRef(ref2)) {
    return ref2.signal;
  }
  return stringValue(ref2);
}
function exprFromValueRefOrSignalRef(ref2) {
  if (isSignalRef(ref2)) {
    return ref2.signal;
  }
  return stringValue(ref2.value);
}
function signalOrStringValue(v2) {
  if (isSignalRef(v2)) {
    return v2.signal;
  }
  return v2 == null ? null : stringValue(v2);
}
function applyMarkConfig(e, model, propsList) {
  for (const property2 of propsList) {
    const value4 = getMarkConfig(property2, model.markDef, model.config);
    if (value4 !== void 0) {
      e[property2] = signalOrValueRef(value4);
    }
  }
  return e;
}
function getStyles(mark) {
  return [].concat(mark.type, mark.style ?? []);
}
function getMarkPropOrConfig(channel, mark, config, opt = {}) {
  const { vgChannel, ignoreVgConfig } = opt;
  if (vgChannel && hasProperty(mark, vgChannel)) {
    return mark[vgChannel];
  } else if (mark[channel] !== void 0) {
    return mark[channel];
  } else if (ignoreVgConfig && (!vgChannel || vgChannel === channel)) {
    return void 0;
  }
  return getMarkConfig(channel, mark, config, opt);
}
function getMarkConfig(channel, mark, config, { vgChannel } = {}) {
  const cfg = getMarkStyleConfig(channel, mark, config.style);
  return getFirstDefined(
    // style config has highest precedence
    vgChannel ? cfg : void 0,
    cfg,
    // then mark-specific config
    vgChannel ? config[mark.type][vgChannel] : void 0,
    config[mark.type][channel],
    // Need to cast because MarkDef doesn't perfectly match with AnyMarkConfig, but if the type isn't available, we'll get nothing here, which is fine
    // If there is vgChannel, skip vl channel.
    // For example, vl size for text is vg fontSize, but config.mark.size is only for point size.
    vgChannel ? config.mark[vgChannel] : config.mark[channel]
  );
}
function getMarkStyleConfig(prop, mark, styleConfigIndex) {
  return getStyleConfig(prop, getStyles(mark), styleConfigIndex);
}
function getStyleConfig(p, styles, styleConfigIndex) {
  styles = array_default(styles);
  let value4;
  for (const style3 of styles) {
    const styleConfig = styleConfigIndex[style3];
    if (hasProperty(styleConfig, p)) {
      value4 = styleConfig[p];
    }
  }
  return value4;
}
function sortParams(orderDef, fieldRefOption) {
  return array_default(orderDef).reduce((s, orderChannelDef) => {
    s.field.push(vgField(orderChannelDef, fieldRefOption));
    s.order.push(orderChannelDef.sort ?? "ascending");
    return s;
  }, { field: [], order: [] });
}
function mergeTitleFieldDefs(f1, f2) {
  const merged = [...f1];
  f2.forEach((fdToMerge) => {
    for (const fieldDef1 of merged) {
      if (deepEqual(fieldDef1, fdToMerge)) {
        return;
      }
    }
    merged.push(fdToMerge);
  });
  return merged;
}
function mergeTitle(title1, title2) {
  if (deepEqual(title1, title2) || !title2) {
    return title1;
  } else if (!title1) {
    return title2;
  } else {
    return [...array_default(title1), ...array_default(title2)].join(", ");
  }
}
function mergeTitleComponent(v1, v2) {
  const v1Val = v1.value;
  const v2Val = v2.value;
  if (v1Val == null || v2Val === null) {
    return {
      explicit: v1.explicit,
      value: null
    };
  } else if ((isText(v1Val) || isSignalRef(v1Val)) && (isText(v2Val) || isSignalRef(v2Val))) {
    return {
      explicit: v1.explicit,
      value: mergeTitle(v1Val, v2Val)
    };
  } else if (isText(v1Val) || isSignalRef(v1Val)) {
    return {
      explicit: v1.explicit,
      value: v1Val
    };
  } else if (isText(v2Val) || isSignalRef(v2Val)) {
    return {
      explicit: v1.explicit,
      value: v2Val
    };
  } else if (!isText(v1Val) && !isSignalRef(v1Val) && !isText(v2Val) && !isSignalRef(v2Val)) {
    return {
      explicit: v1.explicit,
      value: mergeTitleFieldDefs(v1Val, v2Val)
    };
  }
  throw new Error("It should never reach here");
}
function invalidSpec(spec) {
  return `Invalid specification ${stringify(spec)}. Make sure the specification includes at least one of the following properties: "mark", "layer", "facet", "hconcat", "vconcat", "concat", or "repeat".`;
}
var FIT_NON_SINGLE = 'Autosize "fit" only works for single views and layered views.';
function containerSizeNonSingle(name) {
  const uName = name == "width" ? "Width" : "Height";
  return `${uName} "container" only works for single views and layered views.`;
}
function containerSizeNotCompatibleWithAutosize(name) {
  const uName = name == "width" ? "Width" : "Height";
  const fitDirection = name == "width" ? "x" : "y";
  return `${uName} "container" only works well with autosize "fit" or "fit-${fitDirection}".`;
}
function droppingFit(channel) {
  return channel ? `Dropping "fit-${channel}" because spec has discrete ${getSizeChannel(channel)}.` : `Dropping "fit" because spec has discrete size.`;
}
function unknownField(channel) {
  return `Unknown field for ${channel}. Cannot calculate view size.`;
}
function cannotProjectOnChannelWithoutField(channel) {
  return `Cannot project a selection on encoding channel "${channel}", which has no field.`;
}
function cannotProjectAggregate(channel, aggregate) {
  return `Cannot project a selection on encoding channel "${channel}" as it uses an aggregate function ("${aggregate}").`;
}
function nearestNotSupportForContinuous(mark) {
  return `The "nearest" transform is not supported for ${mark} marks.`;
}
function selectionNotSupported(mark) {
  return `Selection not supported for ${mark} yet.`;
}
function selectionNotFound(name) {
  return `Cannot find a selection named "${name}".`;
}
var SCALE_BINDINGS_CONTINUOUS = "Scale bindings are currently only supported for scales with unbinned, continuous domains.";
var SEQUENTIAL_SCALE_DEPRECATED = "Sequntial scales are deprecated. The available quantitative scale type values are linear, log, pow, sqrt, symlog, time and utc";
var LEGEND_BINDINGS_MUST_HAVE_PROJECTION = "Legend bindings are only supported for selections over an individual field or encoding channel.";
function cannotLookupVariableParameter(name) {
  return `Lookups can only be performed on selection parameters. "${name}" is a variable parameter.`;
}
function noSameUnitLookup(name) {
  return `Cannot define and lookup the "${name}" selection in the same view. Try moving the lookup into a second, layered view?`;
}
var NEEDS_SAME_SELECTION = "The same selection must be used to override scale domains in a layered view.";
var INTERVAL_INITIALIZED_WITH_POS = 'Interval selections should be initialized using "x", "y", "longitude", or "latitude" keys.';
function noSuchRepeatedValue(field3) {
  return `Unknown repeated value "${field3}".`;
}
function columnsNotSupportByRowCol(type4) {
  return `The "columns" property cannot be used when "${type4}" has nested row/column.`;
}
var MULTIPLE_TIMER_ANIMATION_SELECTION = "Multiple timer selections in one unit spec are not supported. Ignoring all but the first.";
var MULTI_VIEW_ANIMATION_UNSUPPORTED = "Animation involving facet, layer, or concat is currently unsupported.";
function selectionAsScaleDomainWithoutField(field3) {
  return `A "field" or "encoding" must be specified when using a selection as a scale domain. Using "field": ${stringValue(field3)}.`;
}
function selectionAsScaleDomainWrongEncodings(encodings, encoding, extent2, field3) {
  return `${!encodings.length ? "No " : "Multiple "}matching ${stringValue(encoding)} encoding found for selection ${stringValue(extent2.param)}. Using "field": ${stringValue(field3)}.`;
}
var CONCAT_CANNOT_SHARE_AXIS = "Axes cannot be shared in concatenated or repeated views yet (https://github.com/vega/vega-lite/issues/2415).";
function unrecognizedParse(p) {
  return `Unrecognized parse "${p}".`;
}
function differentParse(field3, local, ancestor) {
  return `An ancestor parsed field "${field3}" as ${ancestor} but a child wants to parse the field as ${local}.`;
}
var ADD_SAME_CHILD_TWICE = "Attempt to add the same child twice.";
function invalidTransformIgnored(transform4) {
  return `Ignoring an invalid transform: ${stringify(transform4)}.`;
}
var NO_FIELDS_NEEDS_AS = 'If "from.fields" is not specified, "as" has to be a string that specifies the key to be used for the data from the secondary source.';
function customFormatTypeNotAllowed(channel) {
  return `Config.customFormatTypes is not true, thus custom format type and format for channel ${channel} are dropped.`;
}
function projectionOverridden(opt) {
  const { parentProjection, projection: projection3 } = opt;
  return `Layer's shared projection ${stringify(parentProjection)} is overridden by a child projection ${stringify(projection3)}.`;
}
var REPLACE_ANGLE_WITH_THETA = "Arc marks uses theta channel rather than angle, replacing angle with theta.";
function offsetNestedInsideContinuousPositionScaleDropped(mainChannel) {
  return `${mainChannel}Offset dropped because ${mainChannel} is continuous`;
}
function primitiveChannelDef(channel, type4, value4) {
  return `Channel ${channel} is a ${type4}. Converted to {value: ${stringify(value4)}}.`;
}
function invalidFieldType(type4) {
  return `Invalid field type "${type4}".`;
}
function invalidFieldTypeForCountAggregate(type4, aggregate) {
  return `Invalid field type "${type4}" for aggregate: "${aggregate}", using "quantitative" instead.`;
}
function invalidAggregate(aggregate) {
  return `Invalid aggregation operator "${aggregate}".`;
}
function droppingColor(type4, opt) {
  const { fill: fill3, stroke: stroke3 } = opt;
  return `Dropping color ${type4} as the plot also has ${fill3 && stroke3 ? "fill and stroke" : fill3 ? "fill" : "stroke"}.`;
}
function relativeBandSizeNotSupported(sizeChannel) {
  return `Position range does not support relative band size for ${sizeChannel}.`;
}
function emptyFieldDef(fieldDef, channel) {
  return `Dropping ${stringify(fieldDef)} from channel "${channel}" since it does not contain any data field, datum, value, or signal.`;
}
var LINE_WITH_VARYING_SIZE = "Line marks cannot encode size with a non-groupby field. You may want to use trail marks instead.";
function incompatibleChannel(channel, markOrFacet, when) {
  return `${channel} dropped as it is incompatible with "${markOrFacet}"${""}.`;
}
function invalidEncodingChannel(channel) {
  return `${channel}-encoding is dropped as ${channel} is not a valid encoding channel.`;
}
function channelShouldBeDiscrete(channel) {
  return `${channel} encoding should be discrete (ordinal / nominal / binned).`;
}
function channelShouldBeDiscreteOrDiscretizing(channel) {
  return `${channel} encoding should be discrete (ordinal / nominal / binned) or use a discretizing scale (e.g. threshold).`;
}
function facetChannelDropped(channels) {
  return `Facet encoding dropped as ${channels.join(" and ")} ${channels.length > 1 ? "are" : "is"} also specified.`;
}
function discreteChannelCannotEncode(channel, type4) {
  return `Using discrete channel "${channel}" to encode "${type4}" field can be misleading as it does not encode ${type4 === "ordinal" ? "order" : "magnitude"}.`;
}
function rangeMarkAlignmentCannotBeExpression(align2) {
  return `The ${align2} for range marks cannot be an expression`;
}
function lineWithRange(hasX2, hasY2) {
  const channels = hasX2 && hasY2 ? "x2 and y2" : hasX2 ? "x2" : "y2";
  return `Line mark is for continuous lines and thus cannot be used with ${channels}. We will use the rule mark (line segments) instead.`;
}
function orientOverridden(original, actual) {
  return `Specified orient "${original}" overridden with "${actual}".`;
}
function cannotUseScalePropertyWithNonColor(prop) {
  return `Cannot use the scale property "${prop}" with non-color channel.`;
}
function cannotUseRelativeBandSizeWithNonBandScale(scaleType2) {
  return `Cannot use the relative band size with ${scaleType2} scale.`;
}
function unaggregateDomainHasNoEffectForRawField(fieldDef) {
  return `Using unaggregated domain with raw field has no effect (${stringify(fieldDef)}).`;
}
function unaggregateDomainWithNonSharedDomainOp(aggregate) {
  return `Unaggregated domain not applicable for "${aggregate}" since it produces values outside the origin domain of the source data.`;
}
function unaggregatedDomainWithLogScale(fieldDef) {
  return `Unaggregated domain is currently unsupported for log scale (${stringify(fieldDef)}).`;
}
function cannotApplySizeToNonOrientedMark(mark) {
  return `Cannot apply size to non-oriented mark "${mark}".`;
}
function scaleTypeNotWorkWithChannel(channel, scaleType2, defaultScaleType) {
  return `Channel "${channel}" does not work with "${scaleType2}" scale. We are using "${defaultScaleType}" scale instead.`;
}
function scaleTypeNotWorkWithFieldDef(scaleType2, defaultScaleType) {
  return `FieldDef does not work with "${scaleType2}" scale. We are using "${defaultScaleType}" scale instead.`;
}
function scalePropertyNotWorkWithScaleType(scaleType2, propName, channel) {
  return `${channel}-scale's "${propName}" is dropped as it does not work with ${scaleType2} scale.`;
}
function stepDropped(channel) {
  return `The step for "${channel}" is dropped because the ${channel === "width" ? "x" : "y"} is continuous.`;
}
function mergeConflictingProperty(property2, propertyOf, v1, v2) {
  return `Conflicting ${propertyOf.toString()} property "${property2.toString()}" (${stringify(v1)} and ${stringify(v2)}). Using ${stringify(v1)}.`;
}
function mergeConflictingDomainProperty(property2, propertyOf, v1, v2) {
  return `Conflicting ${propertyOf.toString()} property "${property2.toString()}" (${stringify(v1)} and ${stringify(v2)}). Using the union of the two domains.`;
}
function independentScaleMeansIndependentGuide(channel) {
  return `Setting the scale to be independent for "${channel}" means we also have to set the guide (axis or legend) to be independent.`;
}
function domainSortDropped(sort4) {
  return `Dropping sort property ${stringify(sort4)} as unioned domains only support boolean or op "count", "min", and "max".`;
}
var MORE_THAN_ONE_SORT = "Domains that should be unioned has conflicting sort properties. Sort will be set to true.";
var FACETED_INDEPENDENT_DIFFERENT_SOURCES = "Detected faceted independent scales that union domain of multiple fields from different data sources. We will use the first field. The result view size may be incorrect.";
var FACETED_INDEPENDENT_SAME_FIELDS_DIFFERENT_SOURCES = "Detected faceted independent scales that union domain of the same fields from different source. We will assume that this is the same field from a different fork of the same data source. However, if this is not the case, the result view size may be incorrect.";
var FACETED_INDEPENDENT_SAME_SOURCE = "Detected faceted independent scales that union domain of multiple fields from the same data source. We will use the first field. The result view size may be incorrect.";
function legendValuesUnioned(channelA, channelB) {
  return `Unioning discrete legend values from ${channelA} and ${channelB}.`;
}
function cannotStackRangedMark(channel) {
  return `Cannot stack "${channel}" if there is already "${channel}2".`;
}
function stackNonLinearScale(scaleType2) {
  return `Stack is applied to a non-linear scale (${scaleType2}).`;
}
function stackNonSummativeAggregate(aggregate) {
  return `Stacking is applied even though the aggregate function is non-summative ("${aggregate}").`;
}
function invalidTimeUnit(unitName2, value4) {
  return `Invalid ${unitName2}: ${stringify(value4)}.`;
}
function droppedDay(d) {
  return `Dropping day from datetime ${stringify(d)} as day cannot be combined with other units.`;
}
function errorBarCenterAndExtentAreNotNeeded(center, extent2) {
  return `${extent2 ? "extent " : ""}${extent2 && center ? "and " : ""}${center ? "center " : ""}${extent2 && center ? "are " : "is "}not needed when data are aggregated.`;
}
function errorBarCenterIsUsedWithWrongExtent(center, extent2, mark) {
  return `${center} is not usually used with ${extent2} for ${mark}.`;
}
function errorBarContinuousAxisHasCustomizedAggregate(aggregate, compositeMark) {
  return `Continuous axis should not have customized aggregation function ${aggregate}; ${compositeMark} already agregates the axis.`;
}
function errorBand1DNotSupport(property2) {
  return `1D error band does not support ${property2}.`;
}
function channelRequiredForBinned(channel) {
  return `Channel ${channel} is required for "binned" bin.`;
}
function channelShouldNotBeUsedForBinned(channel) {
  return `Channel ${channel} should not be used with "binned" bin.`;
}
function domainRequiredForThresholdScale(channel) {
  return `Domain for ${channel} is required for threshold scale.`;
}
var main = logger_default(Warn);
var current = main;
function set6(newLogger) {
  current = newLogger;
  return current;
}
function reset2() {
  current = main;
  return current;
}
function error2(...args) {
  current.error(...args);
}
function warn3(...args) {
  current.warn(...args);
}
function debug3(...args) {
  current.debug(...args);
}
function isDateTime(o) {
  if (o && isObject(o)) {
    for (const part of TIMEUNIT_PARTS) {
      if (hasProperty(o, part)) {
        return true;
      }
    }
  }
  return false;
}
var MONTHS = [
  "january",
  "february",
  "march",
  "april",
  "may",
  "june",
  "july",
  "august",
  "september",
  "october",
  "november",
  "december"
];
var SHORT_MONTHS = MONTHS.map((m3) => m3.substr(0, 3));
var DAYS = ["sunday", "monday", "tuesday", "wednesday", "thursday", "friday", "saturday"];
var SHORT_DAYS = DAYS.map((d) => d.substr(0, 3));
function normalizeQuarter(q) {
  if (isNumeric(q)) {
    q = +q;
  }
  if (isNumber(q)) {
    if (q > 4) {
      warn3(invalidTimeUnit("quarter", q));
    }
    return q - 1;
  } else {
    throw new Error(invalidTimeUnit("quarter", q));
  }
}
function normalizeMonth(m3) {
  if (isNumeric(m3)) {
    m3 = +m3;
  }
  if (isNumber(m3)) {
    return m3 - 1;
  } else {
    const lowerM = m3.toLowerCase();
    const monthIndex = MONTHS.indexOf(lowerM);
    if (monthIndex !== -1) {
      return monthIndex;
    }
    const shortM = lowerM.substr(0, 3);
    const shortMonthIndex = SHORT_MONTHS.indexOf(shortM);
    if (shortMonthIndex !== -1) {
      return shortMonthIndex;
    }
    throw new Error(invalidTimeUnit("month", m3));
  }
}
function normalizeDay(d) {
  if (isNumeric(d)) {
    d = +d;
  }
  if (isNumber(d)) {
    return d % 7;
  } else {
    const lowerD = d.toLowerCase();
    const dayIndex = DAYS.indexOf(lowerD);
    if (dayIndex !== -1) {
      return dayIndex;
    }
    const shortD = lowerD.substr(0, 3);
    const shortDayIndex = SHORT_DAYS.indexOf(shortD);
    if (shortDayIndex !== -1) {
      return shortDayIndex;
    }
    throw new Error(invalidTimeUnit("day", d));
  }
}
function dateTimeParts(d, normalize4) {
  const parts = [];
  if (normalize4 && d.day !== void 0) {
    if (keys3(d).length > 1) {
      warn3(droppedDay(d));
      d = duplicate(d);
      delete d.day;
    }
  }
  if (d.year !== void 0) {
    parts.push(d.year);
  } else {
    parts.push(2012);
  }
  if (d.month !== void 0) {
    const month = normalize4 ? normalizeMonth(d.month) : d.month;
    parts.push(month);
  } else if (d.quarter !== void 0) {
    const quarter2 = normalize4 ? normalizeQuarter(d.quarter) : d.quarter;
    parts.push(isNumber(quarter2) ? quarter2 * 3 : `${quarter2}*3`);
  } else {
    parts.push(0);
  }
  if (d.date !== void 0) {
    parts.push(d.date);
  } else if (d.day !== void 0) {
    const day = normalize4 ? normalizeDay(d.day) : d.day;
    parts.push(isNumber(day) ? day + 1 : `${day}+1`);
  } else {
    parts.push(1);
  }
  for (const timeUnit of ["hours", "minutes", "seconds", "milliseconds"]) {
    const unit2 = d[timeUnit];
    parts.push(typeof unit2 === "undefined" ? 0 : unit2);
  }
  return parts;
}
function dateTimeToExpr(d) {
  const parts = dateTimeParts(d, true);
  const string = parts.join(", ");
  if (d.utc) {
    return `utc(${string})`;
  } else {
    return `datetime(${string})`;
  }
}
function dateTimeExprToExpr(d) {
  const parts = dateTimeParts(d, false);
  const string = parts.join(", ");
  if (d.utc) {
    return `utc(${string})`;
  } else {
    return `datetime(${string})`;
  }
}
function dateTimeToTimestamp(d) {
  const parts = dateTimeParts(d, true);
  if (d.utc) {
    return +new Date(Date.UTC(...parts));
  } else {
    return +new Date(...parts);
  }
}
var LOCAL_SINGLE_TIMEUNIT_INDEX = {
  year: 1,
  quarter: 1,
  month: 1,
  week: 1,
  day: 1,
  dayofyear: 1,
  date: 1,
  hours: 1,
  minutes: 1,
  seconds: 1,
  milliseconds: 1
};
var TIMEUNIT_PARTS = keys3(LOCAL_SINGLE_TIMEUNIT_INDEX);
function isLocalSingleTimeUnit(timeUnit) {
  return hasOwnProperty(LOCAL_SINGLE_TIMEUNIT_INDEX, timeUnit);
}
function isBinnedTimeUnit(timeUnit) {
  if (isObject(timeUnit)) {
    return timeUnit.binned;
  }
  return isBinnedTimeUnitString(timeUnit);
}
function isBinnedTimeUnitString(timeUnit) {
  return timeUnit?.startsWith("binned");
}
function isUTCTimeUnit(t) {
  return t.startsWith("utc");
}
function getLocalTimeUnitFromUTCTimeUnit(t) {
  return t.substring(3);
}
var VEGALITE_TIMEFORMAT = {
  "year-month": "%b %Y ",
  "year-month-date": "%b %d, %Y "
};
function getTimeUnitParts(timeUnit) {
  return TIMEUNIT_PARTS.filter((part) => containsTimeUnit(timeUnit, part));
}
function getSmallestTimeUnitPart(timeUnit) {
  const parts = getTimeUnitParts(timeUnit);
  return parts[parts.length - 1];
}
function containsTimeUnit(fullTimeUnit, timeUnit) {
  const index4 = fullTimeUnit.indexOf(timeUnit);
  if (index4 < 0) {
    return false;
  }
  if (index4 > 0 && timeUnit === "seconds" && fullTimeUnit.charAt(index4 - 1) === "i") {
    return false;
  }
  if (fullTimeUnit.length > index4 + 3 && timeUnit === "day" && fullTimeUnit.charAt(index4 + 3) === "o") {
    return false;
  }
  if (index4 > 0 && timeUnit === "year" && fullTimeUnit.charAt(index4 - 1) === "f") {
    return false;
  }
  return true;
}
function fieldExpr(fullTimeUnit, field3, { end } = { end: false }) {
  const fieldRef2 = accessPathWithDatum(field3);
  const utc = isUTCTimeUnit(fullTimeUnit) ? "utc" : "";
  function func(timeUnit) {
    if (timeUnit === "quarter") {
      return `(${utc}quarter(${fieldRef2})-1)`;
    } else {
      return `${utc}${timeUnit}(${fieldRef2})`;
    }
  }
  let lastTimeUnit;
  const dateExpr = {};
  for (const part of TIMEUNIT_PARTS) {
    if (containsTimeUnit(fullTimeUnit, part)) {
      dateExpr[part] = func(part);
      lastTimeUnit = part;
    }
  }
  if (end) {
    dateExpr[lastTimeUnit] += "+1";
  }
  return dateTimeExprToExpr(dateExpr);
}
function timeUnitSpecifierExpression(timeUnit) {
  if (!timeUnit) {
    return void 0;
  }
  const timeUnitParts = getTimeUnitParts(timeUnit);
  return `timeUnitSpecifier(${stringify(timeUnitParts)}, ${stringify(VEGALITE_TIMEFORMAT)})`;
}
function formatExpression(timeUnit, field3, isUTCScale) {
  if (!timeUnit) {
    return void 0;
  }
  const expr2 = timeUnitSpecifierExpression(timeUnit);
  const utc = isUTCScale || isUTCTimeUnit(timeUnit);
  return `${utc ? "utc" : "time"}Format(${field3}, ${expr2})`;
}
function normalizeTimeUnit(timeUnit) {
  if (!timeUnit) {
    return void 0;
  }
  let params2;
  if (isString(timeUnit)) {
    if (isBinnedTimeUnitString(timeUnit)) {
      params2 = {
        unit: timeUnit.substring(6),
        binned: true
      };
    } else {
      params2 = {
        unit: timeUnit
      };
    }
  } else if (isObject(timeUnit)) {
    params2 = {
      ...timeUnit,
      ...timeUnit.unit ? { unit: timeUnit.unit } : {}
    };
  }
  if (isUTCTimeUnit(params2.unit)) {
    params2.utc = true;
    params2.unit = getLocalTimeUnitFromUTCTimeUnit(params2.unit);
  }
  return params2;
}
function timeUnitToString(tu) {
  const { utc, ...rest } = normalizeTimeUnit(tu);
  if (rest.unit) {
    return (utc ? "utc" : "") + keys3(rest).map((p) => varName(`${p === "unit" ? "" : `_${p}_`}${rest[p]}`)).join("");
  } else {
    return `${utc ? "utc" : ""}timeunit${keys3(rest).map((p) => varName(`_${p}_${rest[p]}`)).join("")}`;
  }
}
function durationExpr(timeUnit, wrap3 = (x6) => x6) {
  const normalizedTimeUnit = normalizeTimeUnit(timeUnit);
  const smallestUnitPart = getSmallestTimeUnitPart(normalizedTimeUnit.unit);
  if (smallestUnitPart && smallestUnitPart !== "day") {
    const startDate = {
      year: 2001,
      // pick a non-leap year
      month: 1,
      date: 1,
      hours: 0,
      minutes: 0,
      seconds: 0,
      milliseconds: 0
    };
    const { step, part } = getDateTimePartAndStep(smallestUnitPart, normalizedTimeUnit.step);
    const endDate = {
      ...startDate,
      [part]: +startDate[part] + step
    };
    return `${wrap3(dateTimeToExpr(endDate))} - ${wrap3(dateTimeToExpr(startDate))}`;
  }
  return void 0;
}
var DATE_PARTS = {
  year: 1,
  month: 1,
  date: 1,
  hours: 1,
  minutes: 1,
  seconds: 1,
  milliseconds: 1
};
function isDatePart(timeUnit) {
  return hasOwnProperty(DATE_PARTS, timeUnit);
}
function getDateTimePartAndStep(timeUnit, step = 1) {
  if (isDatePart(timeUnit)) {
    return { part: timeUnit, step };
  }
  switch (timeUnit) {
    case "day":
    case "dayofyear":
      return { part: "date", step };
    case "quarter":
      return { part: "month", step: step * 3 };
    case "week":
      return { part: "date", step: step * 7 };
  }
}
function isSelectionPredicate(predicate) {
  return hasProperty(predicate, "param");
}
function isFieldEqualPredicate(predicate) {
  return !!predicate?.field && predicate.equal !== void 0;
}
function isFieldLTPredicate(predicate) {
  return !!predicate?.field && predicate.lt !== void 0;
}
function isFieldLTEPredicate(predicate) {
  return !!predicate?.field && predicate.lte !== void 0;
}
function isFieldGTPredicate(predicate) {
  return !!predicate?.field && predicate.gt !== void 0;
}
function isFieldGTEPredicate(predicate) {
  return !!predicate?.field && predicate.gte !== void 0;
}
function isFieldRangePredicate(predicate) {
  if (predicate?.field) {
    if (isArray(predicate.range) && predicate.range.length === 2) {
      return true;
    } else if (isSignalRef(predicate.range)) {
      return true;
    }
  }
  return false;
}
function isFieldOneOfPredicate(predicate) {
  return !!predicate?.field && (isArray(predicate.oneOf) || isArray(predicate.in));
}
function isFieldValidPredicate(predicate) {
  return !!predicate?.field && predicate.valid !== void 0;
}
function isFieldPredicate(predicate) {
  return isFieldOneOfPredicate(predicate) || isFieldEqualPredicate(predicate) || isFieldRangePredicate(predicate) || isFieldLTPredicate(predicate) || isFieldGTPredicate(predicate) || isFieldLTEPredicate(predicate) || isFieldGTEPredicate(predicate);
}
function predicateValueExpr(v2, timeUnit) {
  return valueExpr(v2, { timeUnit, wrapTime: true });
}
function predicateValuesExpr(vals2, timeUnit) {
  return vals2.map((v2) => predicateValueExpr(v2, timeUnit));
}
function fieldFilterExpression(predicate, useInRange = true) {
  const { field: field3 } = predicate;
  const normalizedTimeUnit = normalizeTimeUnit(predicate.timeUnit);
  const { unit: unit2, binned } = normalizedTimeUnit || {};
  const rawFieldExpr = vgField(predicate, { expr: "datum" });
  const fieldExpr$1 = unit2 ? (
    // For timeUnit, cast into integer with time() so we can use ===, inrange, indexOf to compare values directly.
    // TODO: We calculate timeUnit on the fly here. Consider if we would like to consolidate this with timeUnit pipeline
    // TODO: support utc
    `time(${!binned ? fieldExpr(unit2, field3) : rawFieldExpr})`
  ) : rawFieldExpr;
  if (isFieldEqualPredicate(predicate)) {
    return `${fieldExpr$1}===${predicateValueExpr(predicate.equal, unit2)}`;
  } else if (isFieldLTPredicate(predicate)) {
    const upper = predicate.lt;
    return `${fieldExpr$1}<${predicateValueExpr(upper, unit2)}`;
  } else if (isFieldGTPredicate(predicate)) {
    const lower = predicate.gt;
    return `${fieldExpr$1}>${predicateValueExpr(lower, unit2)}`;
  } else if (isFieldLTEPredicate(predicate)) {
    const upper = predicate.lte;
    return `${fieldExpr$1}<=${predicateValueExpr(upper, unit2)}`;
  } else if (isFieldGTEPredicate(predicate)) {
    const lower = predicate.gte;
    return `${fieldExpr$1}>=${predicateValueExpr(lower, unit2)}`;
  } else if (isFieldOneOfPredicate(predicate)) {
    return `indexof([${predicateValuesExpr(predicate.oneOf, unit2).join(",")}], ${fieldExpr$1}) !== -1`;
  } else if (isFieldValidPredicate(predicate)) {
    return fieldValidPredicate(fieldExpr$1, predicate.valid);
  } else if (isFieldRangePredicate(predicate)) {
    const { range: range7 } = replaceExprRef(predicate);
    const lower = isSignalRef(range7) ? { signal: `${range7.signal}[0]` } : range7[0];
    const upper = isSignalRef(range7) ? { signal: `${range7.signal}[1]` } : range7[1];
    if (lower !== null && upper !== null && useInRange) {
      return `inrange(${fieldExpr$1}, [${predicateValueExpr(lower, unit2)}, ${predicateValueExpr(upper, unit2)}])`;
    }
    const exprs = [];
    if (lower !== null) {
      exprs.push(`${fieldExpr$1} >= ${predicateValueExpr(lower, unit2)}`);
    }
    if (upper !== null) {
      exprs.push(`${fieldExpr$1} <= ${predicateValueExpr(upper, unit2)}`);
    }
    return exprs.length > 0 ? exprs.join(" && ") : "true";
  }
  throw new Error(`Invalid field predicate: ${stringify(predicate)}`);
}
function fieldValidPredicate(fieldExpr2, valid = true) {
  if (valid) {
    return `isValid(${fieldExpr2}) && isFinite(+${fieldExpr2})`;
  } else {
    return `!isValid(${fieldExpr2}) || !isFinite(+${fieldExpr2})`;
  }
}
function normalizePredicate$1(f) {
  if (isFieldPredicate(f) && f.timeUnit) {
    return {
      ...f,
      timeUnit: normalizeTimeUnit(f.timeUnit)
    };
  }
  return f;
}
var Type = {
  quantitative: "quantitative",
  ordinal: "ordinal",
  temporal: "temporal",
  nominal: "nominal",
  geojson: "geojson"
};
function isContinuous2(type4) {
  return type4 === "quantitative" || type4 === "temporal";
}
function isDiscrete$1(type4) {
  return type4 === "ordinal" || type4 === "nominal";
}
var QUANTITATIVE = Type.quantitative;
var ORDINAL = Type.ordinal;
var TEMPORAL = Type.temporal;
var NOMINAL = Type.nominal;
var GEOJSON = Type.geojson;
function getFullName(type4) {
  if (type4) {
    type4 = type4.toLowerCase();
    switch (type4) {
      case "q":
      case QUANTITATIVE:
        return "quantitative";
      case "t":
      case TEMPORAL:
        return "temporal";
      case "o":
      case ORDINAL:
        return "ordinal";
      case "n":
      case NOMINAL:
        return "nominal";
      case GEOJSON:
        return "geojson";
    }
  }
  return void 0;
}
var ScaleType = {
  // Continuous - Quantitative
  LINEAR: "linear",
  LOG: "log",
  POW: "pow",
  SQRT: "sqrt",
  // Continuous - Time
  TIME: "time",
  UTC: "utc",
  POINT: "point",
  BAND: "band"
};
var SCALE_CATEGORY_INDEX = {
  linear: "numeric",
  log: "numeric",
  pow: "numeric",
  sqrt: "numeric",
  symlog: "numeric",
  identity: "numeric",
  sequential: "numeric",
  time: "time",
  utc: "time",
  ordinal: "ordinal",
  "bin-ordinal": "bin-ordinal",
  // TODO: should bin-ordinal support merging with other
  point: "ordinal-position",
  band: "ordinal-position",
  quantile: "discretizing",
  quantize: "discretizing",
  threshold: "discretizing"
};
function scaleCompatible(scaleType1, scaleType2) {
  const scaleCategory1 = SCALE_CATEGORY_INDEX[scaleType1];
  const scaleCategory2 = SCALE_CATEGORY_INDEX[scaleType2];
  return scaleCategory1 === scaleCategory2 || scaleCategory1 === "ordinal-position" && scaleCategory2 === "time" || scaleCategory2 === "ordinal-position" && scaleCategory1 === "time";
}
var SCALE_PRECEDENCE_INDEX = {
  // numeric
  linear: 0,
  log: 1,
  pow: 1,
  sqrt: 1,
  symlog: 1,
  identity: 1,
  sequential: 1,
  // time
  time: 0,
  utc: 0,
  // ordinal-position -- these have higher precedence than continuous scales as they support more types of data
  point: 10,
  band: 11,
  // band has higher precedence as it is better for interaction
  // non grouped types
  ordinal: 0,
  "bin-ordinal": 0,
  quantile: 0,
  quantize: 0,
  threshold: 0
};
function scaleTypePrecedence(scaleType2) {
  return SCALE_PRECEDENCE_INDEX[scaleType2];
}
var QUANTITATIVE_SCALES = /* @__PURE__ */ new Set([
  "linear",
  "log",
  "pow",
  "sqrt",
  "symlog"
]);
var CONTINUOUS_TO_CONTINUOUS_SCALES = /* @__PURE__ */ new Set([
  ...QUANTITATIVE_SCALES,
  "time",
  "utc"
]);
function isQuantitative(type4) {
  return QUANTITATIVE_SCALES.has(type4);
}
var CONTINUOUS_TO_DISCRETE_SCALES = /* @__PURE__ */ new Set([
  "quantile",
  "quantize",
  "threshold"
]);
var CONTINUOUS_DOMAIN_SCALES = /* @__PURE__ */ new Set([
  ...CONTINUOUS_TO_CONTINUOUS_SCALES,
  ...CONTINUOUS_TO_DISCRETE_SCALES,
  "sequential",
  "identity"
]);
var DISCRETE_DOMAIN_SCALES = /* @__PURE__ */ new Set([
  "ordinal",
  "bin-ordinal",
  "point",
  "band"
]);
function hasDiscreteDomain(type4) {
  return DISCRETE_DOMAIN_SCALES.has(type4);
}
function hasContinuousDomain(type4) {
  return CONTINUOUS_DOMAIN_SCALES.has(type4);
}
function isContinuousToContinuous(type4) {
  return CONTINUOUS_TO_CONTINUOUS_SCALES.has(type4);
}
function isContinuousToDiscrete(type4) {
  return CONTINUOUS_TO_DISCRETE_SCALES.has(type4);
}
var defaultScaleConfig = {
  pointPadding: 0.5,
  barBandPaddingInner: 0.1,
  rectBandPaddingInner: 0,
  tickBandPaddingInner: 0.25,
  bandWithNestedOffsetPaddingInner: 0.2,
  bandWithNestedOffsetPaddingOuter: 0.2,
  minBandSize: 2,
  minFontSize: 8,
  maxFontSize: 40,
  minOpacity: 0.3,
  maxOpacity: 0.8,
  // FIXME: revise if these *can* become ratios of width/height step
  minSize: 4,
  // Point size is area. For square point, 9 = 3 pixel ^ 2, not too small!
  minStrokeWidth: 1,
  maxStrokeWidth: 4,
  quantileCount: 4,
  quantizeCount: 4,
  zero: true,
  framesPerSecond: 2,
  animationDuration: 5
};
function isExtendedScheme(scheme3) {
  return !isString(scheme3) && hasProperty(scheme3, "name");
}
function isParameterDomain(domain4) {
  return hasProperty(domain4, "param");
}
function isDomainUnionWith(domain4) {
  return hasProperty(domain4, "unionWith");
}
function isFieldRange(range7) {
  return isObject(range7) && "field" in range7;
}
var SCALE_PROPERTY_INDEX = {
  type: 1,
  domain: 1,
  domainMax: 1,
  domainMin: 1,
  domainMid: 1,
  domainRaw: 1,
  align: 1,
  range: 1,
  rangeMax: 1,
  rangeMin: 1,
  scheme: 1,
  bins: 1,
  // Other properties
  reverse: 1,
  round: 1,
  // quantitative / time
  clamp: 1,
  nice: 1,
  // quantitative
  base: 1,
  exponent: 1,
  constant: 1,
  interpolate: 1,
  zero: 1,
  // zero depends on domain
  // band/point
  padding: 1,
  paddingInner: 1,
  paddingOuter: 1
};
var { type: type3, domain: domain$1, range: range6, rangeMax, rangeMin, scheme: scheme2, ...NON_TYPE_DOMAIN_RANGE_VEGA_SCALE_PROPERTY_INDEX } = SCALE_PROPERTY_INDEX;
var NON_TYPE_DOMAIN_RANGE_VEGA_SCALE_PROPERTIES = keys3(NON_TYPE_DOMAIN_RANGE_VEGA_SCALE_PROPERTY_INDEX);
function scaleTypeSupportProperty(scaleType2, propName) {
  switch (propName) {
    case "type":
    case "domain":
    case "reverse":
    case "range":
      return true;
    case "scheme":
    case "interpolate":
      return !["point", "band", "identity"].includes(scaleType2);
    case "bins":
      return !["point", "band", "identity", "ordinal"].includes(scaleType2);
    case "round":
      return isContinuousToContinuous(scaleType2) || scaleType2 === "band" || scaleType2 === "point";
    case "padding":
    case "rangeMin":
    case "rangeMax":
      return isContinuousToContinuous(scaleType2) || ["point", "band"].includes(scaleType2);
    case "paddingOuter":
    case "align":
      return ["point", "band"].includes(scaleType2);
    case "paddingInner":
      return scaleType2 === "band";
    case "domainMax":
    case "domainMid":
    case "domainMin":
    case "domainRaw":
    case "clamp":
      return isContinuousToContinuous(scaleType2);
    case "nice":
      return isContinuousToContinuous(scaleType2) || scaleType2 === "quantize" || scaleType2 === "threshold";
    case "exponent":
      return scaleType2 === "pow";
    case "base":
      return scaleType2 === "log";
    case "constant":
      return scaleType2 === "symlog";
    case "zero":
      return hasContinuousDomain(scaleType2) && !contains2([
        "log",
        // log scale cannot have zero value
        "time",
        "utc",
        // zero is not meaningful for time
        "threshold",
        // threshold requires custom domain so zero does not matter
        "quantile"
        // quantile depends on distribution so zero does not matter
      ], scaleType2);
  }
}
function channelScalePropertyIncompatability(channel, propName) {
  switch (propName) {
    case "interpolate":
    case "scheme":
    case "domainMid":
      if (!isColorChannel(channel)) {
        return cannotUseScalePropertyWithNonColor(propName);
      }
      return void 0;
    case "align":
    case "type":
    case "bins":
    case "domain":
    case "domainMax":
    case "domainMin":
    case "domainRaw":
    case "range":
    case "base":
    case "exponent":
    case "constant":
    case "nice":
    case "padding":
    case "paddingInner":
    case "paddingOuter":
    case "rangeMax":
    case "rangeMin":
    case "reverse":
    case "round":
    case "clamp":
    case "zero":
      return void 0;
  }
}
function scaleTypeSupportDataType(specifiedType, fieldDefType) {
  if (contains2([ORDINAL, NOMINAL], fieldDefType)) {
    return specifiedType === void 0 || hasDiscreteDomain(specifiedType);
  } else if (fieldDefType === TEMPORAL) {
    return contains2([ScaleType.TIME, ScaleType.UTC, void 0], specifiedType);
  } else if (fieldDefType === QUANTITATIVE) {
    return isQuantitative(specifiedType) || isContinuousToDiscrete(specifiedType) || specifiedType === void 0;
  }
  return true;
}
function channelSupportScaleType(channel, scaleType2, hasNestedOffsetScale = false) {
  if (!isScaleChannel(channel)) {
    return false;
  }
  switch (channel) {
    case X3:
    case Y3:
    case XOFFSET:
    case YOFFSET:
    case THETA:
    case RADIUS:
      if (isContinuousToContinuous(scaleType2)) {
        return true;
      } else if (scaleType2 === "band") {
        return true;
      } else if (scaleType2 === "point") {
        return !hasNestedOffsetScale;
      }
      return false;
    case TIME:
      return contains2(["linear", "band"], scaleType2);
    case SIZE2:
    // TODO: size and opacity can support ordinal with more modification
    case STROKEWIDTH:
    case OPACITY:
    case FILLOPACITY:
    case STROKEOPACITY:
    case ANGLE:
      return isContinuousToContinuous(scaleType2) || isContinuousToDiscrete(scaleType2) || contains2(["band", "point", "ordinal"], scaleType2);
    case COLOR:
    case FILL:
    case STROKE:
      return scaleType2 !== "band";
    // band does not make sense with color
    case STROKEDASH:
    case SHAPE:
      return scaleType2 === "ordinal" || isContinuousToDiscrete(scaleType2);
  }
}
function isScaleInvalidDataIncludeAsValue(invalidDataMode) {
  return isObject(invalidDataMode) && "value" in invalidDataMode;
}
var Mark3 = {
  arc: "arc",
  area: "area",
  bar: "bar",
  image: "image",
  line: "line",
  point: "point",
  rect: "rect",
  rule: "rule",
  text: "text",
  tick: "tick",
  trail: "trail",
  circle: "circle",
  square: "square",
  geoshape: "geoshape"
};
var ARC = Mark3.arc;
var AREA = Mark3.area;
var BAR = Mark3.bar;
var IMAGE = Mark3.image;
var LINE = Mark3.line;
var POINT = Mark3.point;
var RECT = Mark3.rect;
var RULE = Mark3.rule;
var TEXT = Mark3.text;
var TICK = Mark3.tick;
var TRAIL = Mark3.trail;
var CIRCLE = Mark3.circle;
var SQUARE = Mark3.square;
var GEOSHAPE = Mark3.geoshape;
function isPathMark(m3) {
  return ["line", "area", "trail"].includes(m3);
}
function isRectBasedMark(m3) {
  return [
    "rect",
    "bar",
    "image",
    "arc",
    "tick"
    /* arc is rect/interval in polar coordinate */
  ].includes(m3);
}
var PRIMITIVE_MARKS = new Set(keys3(Mark3));
function isMarkDef(mark) {
  return hasProperty(mark, "type");
}
var STROKE_CONFIG = [
  "stroke",
  "strokeWidth",
  "strokeDash",
  "strokeDashOffset",
  "strokeOpacity",
  "strokeJoin",
  "strokeMiterLimit"
];
var FILL_CONFIG = ["fill", "fillOpacity"];
var FILL_STROKE_CONFIG = [...STROKE_CONFIG, ...FILL_CONFIG];
var VL_ONLY_MARK_CONFIG_INDEX = {
  color: 1,
  filled: 1,
  invalid: 1,
  order: 1,
  radius2: 1,
  theta2: 1,
  timeUnitBandSize: 1,
  timeUnitBandPosition: 1
};
var VL_ONLY_MARK_CONFIG_PROPERTIES = keys3(VL_ONLY_MARK_CONFIG_INDEX);
var VL_ONLY_RECT_CONFIG = [
  "binSpacing",
  "continuousBandSize",
  "discreteBandSize",
  "minBandSize"
];
var VL_ONLY_MARK_SPECIFIC_CONFIG_PROPERTY_INDEX = {
  area: ["line", "point"],
  bar: VL_ONLY_RECT_CONFIG,
  rect: VL_ONLY_RECT_CONFIG,
  line: ["point"],
  tick: ["bandSize", "thickness", ...VL_ONLY_RECT_CONFIG]
};
var defaultMarkConfig = {
  color: "#4c78a8",
  invalid: "break-paths-show-path-domains",
  timeUnitBandSize: 1
};
var MARK_CONFIG_INDEX = {
  mark: 1,
  arc: 1,
  area: 1,
  bar: 1,
  circle: 1,
  image: 1,
  line: 1,
  point: 1,
  rect: 1,
  rule: 1,
  square: 1,
  text: 1,
  tick: 1,
  trail: 1,
  geoshape: 1
};
var MARK_CONFIGS = keys3(MARK_CONFIG_INDEX);
function isRelativeBandSize(o) {
  return hasProperty(o, "band");
}
var BAR_CORNER_RADIUS_INDEX = {
  horizontal: ["cornerRadiusTopRight", "cornerRadiusBottomRight"],
  vertical: ["cornerRadiusTopLeft", "cornerRadiusTopRight"]
};
var DEFAULT_RECT_BAND_SIZE = 5;
var defaultRectConfig = {
  binSpacing: 0,
  continuousBandSize: DEFAULT_RECT_BAND_SIZE,
  minBandSize: 0.25,
  timeUnitBandPosition: 0.5
};
var defaultBarConfig = {
  ...defaultRectConfig,
  binSpacing: 1
};
var defaultTickConfig = {
  ...defaultRectConfig,
  thickness: 1
};
function getMarkType(m3) {
  return isMarkDef(m3) ? m3.type : m3;
}
function normalizeInvalidDataMode(mode, { isPath }) {
  if (mode === void 0 || mode === "break-paths-show-path-domains") {
    return isPath ? "break-paths-show-domains" : "filter";
  } else if (mode === null) {
    return "show";
  }
  return mode;
}
function getScaleInvalidDataMode({ markDef, config, scaleChannel, scaleType: scaleType2, isCountAggregate }) {
  if (!scaleType2 || !hasContinuousDomain(scaleType2) || isCountAggregate) {
    return "always-valid";
  }
  const invalidMode = normalizeInvalidDataMode(getMarkPropOrConfig("invalid", markDef, config), {
    isPath: isPathMark(markDef.type)
  });
  const scaleOutputForInvalid = config.scale?.invalid?.[scaleChannel];
  if (scaleOutputForInvalid !== void 0) {
    return "show";
  }
  return invalidMode;
}
function shouldBreakPath(mode) {
  return mode === "break-paths-filter-domains" || mode === "break-paths-show-domains";
}
function scaledZeroOrMinOrMax({ scaleName, scale: scale9, mode }) {
  const domain4 = `domain('${scaleName}')`;
  if (!scale9 || !scaleName) {
    return void 0;
  }
  const min4 = `${domain4}[0]`;
  const max4 = `peek(${domain4})`;
  const domainHasZero = scale9.domainHasZero();
  if (domainHasZero === "definitely") {
    return {
      scale: scaleName,
      value: 0
    };
  } else if (domainHasZero === "maybe") {
    const nonZeroValue = mode === "zeroOrMin" ? min4 : max4;
    return { signal: `scale('${scaleName}', inrange(0, ${domain4}) ? 0 : ${nonZeroValue})` };
  } else {
    return { signal: `scale('${scaleName}', ${mode === "zeroOrMin" ? min4 : max4})` };
  }
}
function getConditionalValueRefForIncludingInvalidValue({ scaleChannel, channelDef, scale: scale9, scaleName, markDef, config }) {
  const scaleType2 = scale9?.get("type");
  const fieldDef = getFieldDef(channelDef);
  const isCountAggregate = isCountingAggregateOp(fieldDef?.aggregate);
  const invalidDataMode = getScaleInvalidDataMode({
    scaleChannel,
    markDef,
    config,
    scaleType: scaleType2,
    isCountAggregate
  });
  if (fieldDef && invalidDataMode === "show") {
    const includeAs = config.scale.invalid?.[scaleChannel] ?? "zero-or-min";
    return {
      test: fieldValidPredicate(vgField(fieldDef, { expr: "datum" }), false),
      ...refForInvalidValues(includeAs, scale9, scaleName)
    };
  }
  return void 0;
}
function refForInvalidValues(includeAs, scale9, scaleName) {
  if (isScaleInvalidDataIncludeAsValue(includeAs)) {
    const { value: value4 } = includeAs;
    return isSignalRef(value4) ? { signal: value4.signal } : { value: value4 };
  }
  return scaledZeroOrMinOrMax({
    scale: scale9,
    scaleName,
    mode: "zeroOrMin"
  });
}
function midPointRefWithPositionInvalidTest(params2) {
  const { channel, channelDef, markDef, scale: scale9, scaleName, config } = params2;
  const scaleChannel = getMainRangeChannel(channel);
  const mainRef = midPoint(params2);
  const valueRefForIncludingInvalid = getConditionalValueRefForIncludingInvalidValue({
    scaleChannel,
    channelDef,
    scale: scale9,
    scaleName,
    markDef,
    config
  });
  return valueRefForIncludingInvalid !== void 0 ? [valueRefForIncludingInvalid, mainRef] : mainRef;
}
function datumDefToExpr(datumDef) {
  const { datum: datum3 } = datumDef;
  if (isDateTime(datum3)) {
    return dateTimeToExpr(datum3);
  }
  return `${stringify(datum3)}`;
}
function valueRefForFieldOrDatumDef(fieldDef, scaleName, opt, encode3) {
  const ref2 = {};
  if (scaleName) {
    ref2.scale = scaleName;
  }
  if (isDatumDef(fieldDef)) {
    const { datum: datum3 } = fieldDef;
    if (isDateTime(datum3)) {
      ref2.signal = dateTimeToExpr(datum3);
    } else if (isSignalRef(datum3)) {
      ref2.signal = datum3.signal;
    } else if (isExprRef(datum3)) {
      ref2.signal = datum3.expr;
    } else {
      ref2.value = datum3;
    }
  } else {
    ref2.field = vgField(fieldDef, opt);
  }
  if (encode3) {
    const { offset: offset6, band: band2 } = encode3;
    if (offset6) {
      ref2.offset = offset6;
    }
    if (band2) {
      ref2.band = band2;
    }
  }
  return ref2;
}
function interpolatedSignalRef({ scaleName, fieldOrDatumDef, fieldOrDatumDef2, offset: offset6, startSuffix, endSuffix = "end", bandPosition = 0.5 }) {
  const expr2 = !isSignalRef(bandPosition) && 0 < bandPosition && bandPosition < 1 ? "datum" : void 0;
  const start = vgField(fieldOrDatumDef, { expr: expr2, suffix: startSuffix });
  const end = fieldOrDatumDef2 !== void 0 ? vgField(fieldOrDatumDef2, { expr: expr2 }) : vgField(fieldOrDatumDef, { suffix: endSuffix, expr: expr2 });
  const ref2 = {};
  if (bandPosition === 0 || bandPosition === 1) {
    ref2.scale = scaleName;
    const field3 = bandPosition === 0 ? start : end;
    ref2.field = field3;
  } else {
    const datum3 = isSignalRef(bandPosition) ? `(1-${bandPosition.signal}) * ${start} + ${bandPosition.signal} * ${end}` : `${1 - bandPosition} * ${start} + ${bandPosition} * ${end}`;
    ref2.signal = `scale("${scaleName}", ${datum3})`;
  }
  if (offset6) {
    ref2.offset = offset6;
  }
  return ref2;
}
function binSizeExpr({ scaleName, fieldDef }) {
  const start = vgField(fieldDef, { expr: "datum" });
  const end = vgField(fieldDef, { expr: "datum", suffix: "end" });
  return `abs(scale("${scaleName}", ${end}) - scale("${scaleName}", ${start}))`;
}
function midPoint({ channel, channelDef, channel2Def, markDef, config, scaleName, scale: scale9, stack: stack2, offset: offset6, defaultRef, bandPosition }) {
  if (channelDef) {
    if (isFieldOrDatumDef(channelDef)) {
      const scaleType2 = scale9?.get("type");
      if (isTypedFieldDef(channelDef)) {
        bandPosition ??= getBandPosition({
          fieldDef: channelDef,
          fieldDef2: channel2Def,
          markDef,
          config
        });
        const { bin: bin3, timeUnit, type: type4 } = channelDef;
        if (isBinning(bin3) || bandPosition && timeUnit && type4 === TEMPORAL) {
          if (stack2?.impute) {
            return valueRefForFieldOrDatumDef(channelDef, scaleName, { binSuffix: "mid" }, { offset: offset6 });
          }
          if (bandPosition && !hasDiscreteDomain(scaleType2)) {
            return interpolatedSignalRef({ scaleName, fieldOrDatumDef: channelDef, bandPosition, offset: offset6 });
          }
          return valueRefForFieldOrDatumDef(channelDef, scaleName, binRequiresRange(channelDef, channel) ? { binSuffix: "range" } : {}, {
            offset: offset6
          });
        } else if (isBinned(bin3)) {
          if (isFieldDef(channel2Def)) {
            return interpolatedSignalRef({
              scaleName,
              fieldOrDatumDef: channelDef,
              fieldOrDatumDef2: channel2Def,
              bandPosition,
              offset: offset6
            });
          } else {
            const channel2 = channel === X3 ? X23 : Y23;
            warn3(channelRequiredForBinned(channel2));
          }
        }
      }
      return valueRefForFieldOrDatumDef(
        channelDef,
        scaleName,
        hasDiscreteDomain(scaleType2) ? { binSuffix: "range" } : {},
        // no need for bin suffix if there is no scale
        {
          offset: offset6,
          // For band, to get mid point, need to offset by half of the band
          band: scaleType2 === "band" ? bandPosition ?? channelDef.bandPosition ?? 0.5 : void 0
        }
      );
    } else if (isValueDef(channelDef)) {
      const value4 = channelDef.value;
      const offsetMixins = offset6 ? { offset: offset6 } : {};
      return { ...widthHeightValueOrSignalRef(channel, value4), ...offsetMixins };
    }
  }
  if (isFunction(defaultRef)) {
    defaultRef = defaultRef();
  }
  if (defaultRef) {
    return {
      ...defaultRef,
      // only include offset when it is non-zero (zero = no offset)
      ...offset6 ? { offset: offset6 } : {}
    };
  }
  return defaultRef;
}
function widthHeightValueOrSignalRef(channel, value4) {
  if (contains2(["x", "x2"], channel) && value4 === "width") {
    return { field: { group: "width" } };
  } else if (contains2(["y", "y2"], channel) && value4 === "height") {
    return { field: { group: "height" } };
  }
  return signalOrValueRef(value4);
}
function isCustomFormatType(formatType) {
  return formatType && formatType !== "number" && formatType !== "time";
}
function customFormatExpr(formatType, field3, format6) {
  return `${formatType}(${field3}${format6 ? `, ${stringify(format6)}` : ""})`;
}
function formatSignalRef({ fieldOrDatumDef, format: format6, formatType, expr: expr2, normalizeStack, config }) {
  if (isCustomFormatType(formatType)) {
    return formatCustomType({
      fieldOrDatumDef,
      format: format6,
      formatType,
      expr: expr2,
      config
    });
  }
  const field3 = fieldToFormat(fieldOrDatumDef, expr2, normalizeStack);
  const type4 = channelDefType(fieldOrDatumDef);
  if (format6 === void 0 && formatType === void 0 && config.customFormatTypes) {
    if (type4 === "quantitative") {
      if (normalizeStack && config.normalizedNumberFormatType)
        return formatCustomType({
          fieldOrDatumDef,
          format: config.normalizedNumberFormat,
          formatType: config.normalizedNumberFormatType,
          expr: expr2,
          config
        });
      if (config.numberFormatType) {
        return formatCustomType({
          fieldOrDatumDef,
          format: config.numberFormat,
          formatType: config.numberFormatType,
          expr: expr2,
          config
        });
      }
    }
    if (type4 === "temporal" && config.timeFormatType && isFieldDef(fieldOrDatumDef) && fieldOrDatumDef.timeUnit === void 0) {
      return formatCustomType({
        fieldOrDatumDef,
        format: config.timeFormat,
        formatType: config.timeFormatType,
        expr: expr2,
        config
      });
    }
  }
  function getTimeDef(def3) {
    if (!isFieldDef(def3)) {
      return {
        unit: void 0,
        utc: void 0
      };
    }
    return normalizeTimeUnit(def3.timeUnit) || {};
  }
  if (isFieldOrDatumDefForTimeFormat(fieldOrDatumDef)) {
    const { unit: timeUnit, utc: isUTCUnit } = getTimeDef(fieldOrDatumDef);
    const signal = timeFormatExpression({
      field: field3,
      timeUnit,
      format: format6,
      formatType: config.timeFormatType,
      rawTimeFormat: config.timeFormat,
      isUTCScale: isUTCUnit || isScaleFieldDef(fieldOrDatumDef) && fieldOrDatumDef.scale?.type === ScaleType.UTC
    });
    return signal ? { signal } : void 0;
  }
  format6 = numberFormat({ type: type4, specifiedFormat: format6, config, normalizeStack });
  if (isFieldDef(fieldOrDatumDef) && isBinning(fieldOrDatumDef.bin)) {
    const endField = vgField(fieldOrDatumDef, { expr: expr2, binSuffix: "end" });
    return {
      signal: binFormatExpression(field3, endField, format6, formatType, config)
    };
  } else if (format6 || channelDefType(fieldOrDatumDef) === "quantitative") {
    return {
      signal: `${formatExpr(field3, format6)}`
    };
  } else {
    return { signal: `isValid(${field3}) ? ${field3} : ""+${field3}` };
  }
}
function fieldToFormat(fieldOrDatumDef, expr2, normalizeStack) {
  if (isFieldDef(fieldOrDatumDef)) {
    if (normalizeStack) {
      return `${vgField(fieldOrDatumDef, { expr: expr2, suffix: "end" })}-${vgField(fieldOrDatumDef, {
        expr: expr2,
        suffix: "start"
      })}`;
    } else {
      return vgField(fieldOrDatumDef, { expr: expr2 });
    }
  } else {
    return datumDefToExpr(fieldOrDatumDef);
  }
}
function formatCustomType({ fieldOrDatumDef, format: format6, formatType, expr: expr2, normalizeStack, config, field: field3 }) {
  field3 ??= fieldToFormat(fieldOrDatumDef, expr2, normalizeStack);
  if (field3 !== "datum.value" && // For axis/legend, we can't correctly know the end of the bin from `datum`
  isFieldDef(fieldOrDatumDef) && isBinning(fieldOrDatumDef.bin)) {
    const endField = vgField(fieldOrDatumDef, { expr: expr2, binSuffix: "end" });
    return {
      signal: binFormatExpression(field3, endField, format6, formatType, config)
    };
  }
  return { signal: customFormatExpr(formatType, field3, format6) };
}
function guideFormat(fieldOrDatumDef, type4, format6, formatType, config, omitTimeFormatConfig) {
  if (isString(formatType) && isCustomFormatType(formatType)) {
    return void 0;
  } else if (format6 === void 0 && formatType === void 0 && config.customFormatTypes) {
    if (channelDefType(fieldOrDatumDef) === "quantitative") {
      if (config.normalizedNumberFormatType && isPositionFieldOrDatumDef(fieldOrDatumDef) && fieldOrDatumDef.stack === "normalize") {
        return void 0;
      }
      if (config.numberFormatType) {
        return void 0;
      }
    }
  }
  if (isPositionFieldOrDatumDef(fieldOrDatumDef) && fieldOrDatumDef.stack === "normalize" && config.normalizedNumberFormat) {
    return numberFormat({
      type: "quantitative",
      config,
      normalizeStack: true
    });
  }
  if (isFieldOrDatumDefForTimeFormat(fieldOrDatumDef)) {
    const timeUnit = isFieldDef(fieldOrDatumDef) ? normalizeTimeUnit(fieldOrDatumDef.timeUnit)?.unit : void 0;
    if (timeUnit === void 0 && config.customFormatTypes && config.timeFormatType) {
      return void 0;
    }
    return timeFormat4({ specifiedFormat: format6, timeUnit, config, omitTimeFormatConfig });
  }
  return numberFormat({ type: type4, specifiedFormat: format6, config });
}
function guideFormatType(formatType, fieldOrDatumDef, scaleType2) {
  if (formatType && (isSignalRef(formatType) || formatType === "number" || formatType === "time")) {
    return formatType;
  }
  if (isFieldOrDatumDefForTimeFormat(fieldOrDatumDef) && scaleType2 !== "time" && scaleType2 !== "utc") {
    return isFieldDef(fieldOrDatumDef) && normalizeTimeUnit(fieldOrDatumDef?.timeUnit)?.utc ? "utc" : "time";
  }
  return void 0;
}
function numberFormat({ type: type4, specifiedFormat, config, normalizeStack }) {
  if (isString(specifiedFormat)) {
    return specifiedFormat;
  }
  if (type4 === QUANTITATIVE) {
    return normalizeStack ? config.normalizedNumberFormat : config.numberFormat;
  }
  return void 0;
}
function timeFormat4({ specifiedFormat, timeUnit, config, omitTimeFormatConfig }) {
  if (specifiedFormat) {
    return specifiedFormat;
  }
  if (timeUnit) {
    return {
      signal: timeUnitSpecifierExpression(timeUnit)
    };
  }
  return omitTimeFormatConfig ? void 0 : config.timeFormat;
}
function formatExpr(field3, format6) {
  return `format(${field3}, "${format6 || ""}")`;
}
function binNumberFormatExpr(field3, format6, formatType, config) {
  if (isCustomFormatType(formatType)) {
    return customFormatExpr(formatType, field3, format6);
  }
  return formatExpr(field3, (isString(format6) ? format6 : void 0) ?? config.numberFormat);
}
function binFormatExpression(startField, endField, format6, formatType, config) {
  if (format6 === void 0 && formatType === void 0 && config.customFormatTypes && config.numberFormatType) {
    return binFormatExpression(startField, endField, config.numberFormat, config.numberFormatType, config);
  }
  const start = binNumberFormatExpr(startField, format6, formatType, config);
  const end = binNumberFormatExpr(endField, format6, formatType, config);
  return `${fieldValidPredicate(startField, false)} ? "null" : ${start} + "${BIN_RANGE_DELIMITER}" + ${end}`;
}
function timeFormatExpression({ field: field3, timeUnit, format: format6, formatType, rawTimeFormat, isUTCScale }) {
  if (!timeUnit || format6) {
    if (!timeUnit && formatType) {
      return `${formatType}(${field3}, ${stringify(format6)})`;
    }
    format6 = isString(format6) ? format6 : rawTimeFormat;
    return `${isUTCScale ? "utc" : "time"}Format(${field3}, ${stringify(format6)})`;
  } else {
    return formatExpression(timeUnit, field3, isUTCScale);
  }
}
var DEFAULT_SORT_OP = "min";
var SORT_BY_CHANNEL_INDEX = {
  x: 1,
  y: 1,
  color: 1,
  fill: 1,
  stroke: 1,
  strokeWidth: 1,
  size: 1,
  shape: 1,
  fillOpacity: 1,
  strokeOpacity: 1,
  opacity: 1,
  text: 1
};
function isSortByChannel(c3) {
  return hasOwnProperty(SORT_BY_CHANNEL_INDEX, c3);
}
function isSortByEncoding(sort4) {
  return hasProperty(sort4, "encoding");
}
function isSortField(sort4) {
  return sort4 && (sort4.op === "count" || hasProperty(sort4, "field"));
}
function isSortArray(sort4) {
  return sort4 && isArray(sort4);
}
function isFacetMapping(f) {
  return hasProperty(f, "row") || hasProperty(f, "column");
}
function isFacetFieldDef(channelDef) {
  return hasProperty(channelDef, "header");
}
function isFacetSpec(spec) {
  return hasProperty(spec, "facet");
}
function isConditionalParameter(c3) {
  return hasProperty(c3, "param");
}
function isRepeatRef(field3) {
  return !isString(field3) && hasProperty(field3, "repeat");
}
function toFieldDefBase(fieldDef) {
  const { field: field3, timeUnit, bin: bin3, aggregate } = fieldDef;
  return {
    ...timeUnit ? { timeUnit } : {},
    ...bin3 ? { bin: bin3 } : {},
    ...aggregate ? { aggregate } : {},
    field: field3
  };
}
function isSortableFieldDef(fieldDef) {
  return hasProperty(fieldDef, "sort");
}
function getBandPosition({ fieldDef, fieldDef2, markDef: mark, config }) {
  if (isFieldOrDatumDef(fieldDef) && fieldDef.bandPosition !== void 0) {
    return fieldDef.bandPosition;
  }
  if (isFieldDef(fieldDef)) {
    const { timeUnit, bin: bin3 } = fieldDef;
    if (timeUnit && !fieldDef2) {
      return getMarkConfig("timeUnitBandPosition", mark, config);
    } else if (isBinning(bin3)) {
      return 0.5;
    }
  }
  return void 0;
}
function getBandSize({ channel, fieldDef, fieldDef2, markDef: mark, config, scaleType: scaleType2, useVlSizeChannel }) {
  const sizeChannel = getSizeChannel(channel);
  const size = getMarkPropOrConfig(useVlSizeChannel ? "size" : sizeChannel, mark, config, {
    vgChannel: sizeChannel
  });
  if (size !== void 0) {
    return size;
  }
  if (isFieldDef(fieldDef)) {
    const { timeUnit, bin: bin3 } = fieldDef;
    if (timeUnit && !fieldDef2) {
      return { band: getMarkConfig("timeUnitBandSize", mark, config) };
    } else if (isBinning(bin3) && !hasDiscreteDomain(scaleType2)) {
      return { band: 1 };
    }
  }
  if (isRectBasedMark(mark.type)) {
    if (scaleType2) {
      if (hasDiscreteDomain(scaleType2)) {
        return config[mark.type]?.discreteBandSize || { band: 1 };
      } else {
        return config[mark.type]?.continuousBandSize;
      }
    }
    return config[mark.type]?.discreteBandSize;
  }
  return void 0;
}
function hasBandEnd(fieldDef, fieldDef2, markDef, config) {
  if (isBinning(fieldDef.bin) || fieldDef.timeUnit && isTypedFieldDef(fieldDef) && fieldDef.type === "temporal") {
    return getBandPosition({ fieldDef, fieldDef2, markDef, config }) !== void 0;
  }
  return false;
}
function isOrderOnlyDef(orderDef) {
  return hasProperty(orderDef, "sort") && !hasProperty(orderDef, "field");
}
function isConditionalDef(channelDef) {
  return hasProperty(channelDef, "condition");
}
function hasConditionalFieldDef(channelDef) {
  const condition = channelDef?.["condition"];
  return !!condition && !isArray(condition) && isFieldDef(condition);
}
function hasConditionalFieldOrDatumDef(channelDef) {
  const condition = channelDef?.["condition"];
  return !!condition && !isArray(condition) && isFieldOrDatumDef(condition);
}
function hasConditionalValueDef(channelDef) {
  const condition = channelDef?.["condition"];
  return !!condition && (isArray(condition) || isValueDef(condition));
}
function isFieldDef(channelDef) {
  return hasProperty(channelDef, "field") || channelDef?.aggregate === "count";
}
function channelDefType(channelDef) {
  return channelDef?.["type"];
}
function isDatumDef(channelDef) {
  return hasProperty(channelDef, "datum");
}
function isContinuousFieldOrDatumDef(cd2) {
  return isTypedFieldDef(cd2) && !isDiscrete2(cd2) || isNumericDataDef(cd2);
}
function isUnbinnedQuantitativeFieldOrDatumDef(cd2) {
  return isTypedFieldDef(cd2) && cd2.type === "quantitative" && !cd2.bin || isNumericDataDef(cd2);
}
function isNumericDataDef(cd2) {
  return isDatumDef(cd2) && isNumber(cd2.datum);
}
function isFieldOrDatumDef(channelDef) {
  return isFieldDef(channelDef) || isDatumDef(channelDef);
}
function isTypedFieldDef(channelDef) {
  return channelDef && (hasProperty(channelDef, "field") || channelDef["aggregate"] === "count") && hasProperty(channelDef, "type");
}
function isValueDef(channelDef) {
  return hasProperty(channelDef, "value");
}
function isScaleFieldDef(channelDef) {
  return hasProperty(channelDef, "scale") || hasProperty(channelDef, "sort");
}
function isPositionFieldOrDatumDef(channelDef) {
  return hasProperty(channelDef, "axis") || hasProperty(channelDef, "stack") || hasProperty(channelDef, "impute");
}
function isMarkPropFieldOrDatumDef(channelDef) {
  return hasProperty(channelDef, "legend");
}
function isStringFieldOrDatumDef(channelDef) {
  return hasProperty(channelDef, "format") || hasProperty(channelDef, "formatType");
}
function toStringFieldDef(fieldDef) {
  return omit(fieldDef, ["legend", "axis", "header", "scale"]);
}
function isOpFieldDef(fieldDef) {
  return hasProperty(fieldDef, "op");
}
function vgField(fieldDef, opt = {}) {
  let field3 = fieldDef.field;
  const prefix = opt.prefix;
  let suffix = opt.suffix;
  let argAccessor = "";
  if (isCount(fieldDef)) {
    field3 = internalField("count");
  } else {
    let fn;
    if (!opt.nofn) {
      if (isOpFieldDef(fieldDef)) {
        fn = fieldDef.op;
      } else {
        const { bin: bin3, aggregate, timeUnit } = fieldDef;
        if (isBinning(bin3)) {
          fn = binToString(bin3);
          suffix = (opt.binSuffix ?? "") + (opt.suffix ?? "");
        } else if (aggregate) {
          if (isArgmaxDef(aggregate)) {
            argAccessor = `["${field3}"]`;
            field3 = `argmax_${aggregate.argmax}`;
          } else if (isArgminDef(aggregate)) {
            argAccessor = `["${field3}"]`;
            field3 = `argmin_${aggregate.argmin}`;
          } else {
            fn = String(aggregate);
          }
        } else if (timeUnit && !isBinnedTimeUnit(timeUnit)) {
          fn = timeUnitToString(timeUnit);
          suffix = (!["range", "mid"].includes(opt.binSuffix) && opt.binSuffix || "") + (opt.suffix ?? "");
        }
      }
    }
    if (fn) {
      field3 = field3 ? `${fn}_${field3}` : fn;
    }
  }
  if (suffix) {
    field3 = `${field3}_${suffix}`;
  }
  if (prefix) {
    field3 = `${prefix}_${field3}`;
  }
  if (opt.forAs) {
    return removePathFromField(field3);
  } else if (opt.expr) {
    return flatAccessWithDatum(field3, opt.expr) + argAccessor;
  } else {
    return replacePathInField(field3) + argAccessor;
  }
}
function isDiscrete2(def3) {
  switch (def3.type) {
    case "nominal":
    case "ordinal":
    case "geojson":
      return true;
    case "quantitative":
      return isFieldDef(def3) && !!def3.bin;
    case "temporal":
      return false;
  }
  throw new Error(invalidFieldType(def3.type));
}
function isDiscretizing2(def3) {
  return isScaleFieldDef(def3) && isContinuousToDiscrete(def3.scale?.type);
}
function isCount(fieldDef) {
  return fieldDef.aggregate === "count";
}
function verbalTitleFormatter(fieldDef, config) {
  const { field: field3, bin: bin3, timeUnit, aggregate } = fieldDef;
  if (aggregate === "count") {
    return config.countTitle;
  } else if (isBinning(bin3)) {
    return `${field3} (binned)`;
  } else if (timeUnit && !isBinnedTimeUnit(timeUnit)) {
    const unit2 = normalizeTimeUnit(timeUnit)?.unit;
    if (unit2) {
      return `${field3} (${getTimeUnitParts(unit2).join("-")})`;
    }
  } else if (aggregate) {
    if (isArgmaxDef(aggregate)) {
      return `${field3} for max ${aggregate.argmax}`;
    } else if (isArgminDef(aggregate)) {
      return `${field3} for min ${aggregate.argmin}`;
    } else {
      return `${titleCase(aggregate)} of ${field3}`;
    }
  }
  return field3;
}
function functionalTitleFormatter(fieldDef) {
  const { aggregate, bin: bin3, timeUnit, field: field3 } = fieldDef;
  if (isArgmaxDef(aggregate)) {
    return `${field3} for argmax(${aggregate.argmax})`;
  } else if (isArgminDef(aggregate)) {
    return `${field3} for argmin(${aggregate.argmin})`;
  }
  const timeUnitParams = timeUnit && !isBinnedTimeUnit(timeUnit) ? normalizeTimeUnit(timeUnit) : void 0;
  const fn = aggregate || timeUnitParams?.unit || timeUnitParams?.maxbins && "timeunit" || isBinning(bin3) && "bin";
  return fn ? `${fn.toUpperCase()}(${field3})` : field3;
}
var defaultTitleFormatter = (fieldDef, config) => {
  switch (config.fieldTitle) {
    case "plain":
      return fieldDef.field;
    case "functional":
      return functionalTitleFormatter(fieldDef);
    default:
      return verbalTitleFormatter(fieldDef, config);
  }
};
var titleFormatter = defaultTitleFormatter;
function setTitleFormatter(formatter) {
  titleFormatter = formatter;
}
function resetTitleFormatter() {
  setTitleFormatter(defaultTitleFormatter);
}
function title(fieldOrDatumDef, config, { allowDisabling, includeDefault = true }) {
  const guideTitle = getGuide(fieldOrDatumDef)?.title;
  if (!isFieldDef(fieldOrDatumDef)) {
    return guideTitle ?? fieldOrDatumDef.title;
  }
  const fieldDef = fieldOrDatumDef;
  const def3 = includeDefault ? defaultTitle(fieldDef, config) : void 0;
  if (allowDisabling) {
    return getFirstDefined(guideTitle, fieldDef.title, def3);
  } else {
    return guideTitle ?? fieldDef.title ?? def3;
  }
}
function getGuide(fieldDef) {
  if (isPositionFieldOrDatumDef(fieldDef) && fieldDef.axis) {
    return fieldDef.axis;
  } else if (isMarkPropFieldOrDatumDef(fieldDef) && fieldDef.legend) {
    return fieldDef.legend;
  } else if (isFacetFieldDef(fieldDef) && fieldDef.header) {
    return fieldDef.header;
  }
  return void 0;
}
function defaultTitle(fieldDef, config) {
  return titleFormatter(fieldDef, config);
}
function getFormatMixins(fieldDef) {
  if (isStringFieldOrDatumDef(fieldDef)) {
    const { format: format6, formatType } = fieldDef;
    return { format: format6, formatType };
  } else {
    const guide = getGuide(fieldDef) ?? {};
    const { format: format6, formatType } = guide;
    return { format: format6, formatType };
  }
}
function defaultType$2(fieldDef, channel) {
  switch (channel) {
    case "latitude":
    case "longitude":
      return "quantitative";
    case "row":
    case "column":
    case "facet":
    case "shape":
    case "strokeDash":
      return "nominal";
    case "order":
      return "ordinal";
  }
  if (isSortableFieldDef(fieldDef) && isArray(fieldDef.sort)) {
    return "ordinal";
  }
  const { aggregate, bin: bin3, timeUnit } = fieldDef;
  if (timeUnit) {
    return "temporal";
  }
  if (bin3 || aggregate && !isArgmaxDef(aggregate) && !isArgminDef(aggregate)) {
    return "quantitative";
  }
  if (isScaleFieldDef(fieldDef) && fieldDef.scale?.type) {
    switch (SCALE_CATEGORY_INDEX[fieldDef.scale.type]) {
      case "numeric":
      case "discretizing":
        return "quantitative";
      case "time":
        return "temporal";
    }
  }
  return "nominal";
}
function getFieldDef(channelDef) {
  if (isFieldDef(channelDef)) {
    return channelDef;
  } else if (hasConditionalFieldDef(channelDef)) {
    return channelDef.condition;
  }
  return void 0;
}
function getFieldOrDatumDef(channelDef) {
  if (isFieldOrDatumDef(channelDef)) {
    return channelDef;
  } else if (hasConditionalFieldOrDatumDef(channelDef)) {
    return channelDef.condition;
  }
  return void 0;
}
function initChannelDef(channelDef, channel, config, opt = {}) {
  if (isPrimitive(channelDef)) {
    const primitiveType = isString(channelDef) ? "string" : isNumber(channelDef) ? "number" : "boolean";
    warn3(primitiveChannelDef(channel, primitiveType, channelDef));
    return { value: channelDef };
  }
  if (isFieldOrDatumDef(channelDef)) {
    return initFieldOrDatumDef(channelDef, channel, config, opt);
  } else if (hasConditionalFieldOrDatumDef(channelDef)) {
    return {
      ...channelDef,
      // Need to cast as normalizeFieldDef normally return FieldDef, but here we know that it is definitely Condition<FieldDef>
      condition: initFieldOrDatumDef(channelDef.condition, channel, config, opt)
    };
  }
  return channelDef;
}
function initFieldOrDatumDef(fd, channel, config, opt) {
  if (isStringFieldOrDatumDef(fd)) {
    const { format: format6, formatType, ...rest } = fd;
    if (isCustomFormatType(formatType) && !config.customFormatTypes) {
      warn3(customFormatTypeNotAllowed(channel));
      return initFieldOrDatumDef(rest, channel, config, opt);
    }
  } else {
    const guideType = isPositionFieldOrDatumDef(fd) ? "axis" : isMarkPropFieldOrDatumDef(fd) ? "legend" : isFacetFieldDef(fd) ? "header" : null;
    if (guideType && fd[guideType]) {
      const { format: format6, formatType, ...newGuide } = fd[guideType];
      if (isCustomFormatType(formatType) && !config.customFormatTypes) {
        warn3(customFormatTypeNotAllowed(channel));
        return initFieldOrDatumDef({ ...fd, [guideType]: newGuide }, channel, config, opt);
      }
    }
  }
  if (isFieldDef(fd)) {
    return initFieldDef(fd, channel, opt);
  }
  return initDatumDef(fd);
}
function initDatumDef(datumDef) {
  let type4 = datumDef["type"];
  if (type4) {
    return datumDef;
  }
  const { datum: datum3 } = datumDef;
  type4 = isNumber(datum3) ? "quantitative" : isString(datum3) ? "nominal" : isDateTime(datum3) ? "temporal" : void 0;
  return { ...datumDef, type: type4 };
}
function initFieldDef(fd, channel, { compositeMark = false } = {}) {
  const { aggregate, timeUnit, bin: bin3, field: field3 } = fd;
  const fieldDef = { ...fd };
  if (!compositeMark && aggregate && !isAggregateOp(aggregate) && !isArgmaxDef(aggregate) && !isArgminDef(aggregate)) {
    warn3(invalidAggregate(aggregate));
    delete fieldDef.aggregate;
  }
  if (timeUnit) {
    fieldDef.timeUnit = normalizeTimeUnit(timeUnit);
  }
  if (field3) {
    fieldDef.field = `${field3}`;
  }
  if (isBinning(bin3)) {
    fieldDef.bin = normalizeBin(bin3, channel);
  }
  if (isBinned(bin3) && !isXorY(channel)) {
    warn3(channelShouldNotBeUsedForBinned(channel));
  }
  if (isTypedFieldDef(fieldDef)) {
    const { type: type4 } = fieldDef;
    const fullType = getFullName(type4);
    if (type4 !== fullType) {
      fieldDef.type = fullType;
    }
    if (type4 !== "quantitative") {
      if (isCountingAggregateOp(aggregate)) {
        warn3(invalidFieldTypeForCountAggregate(type4, aggregate));
        fieldDef.type = "quantitative";
      }
    }
  } else if (!isSecondaryRangeChannel(channel)) {
    const newType = defaultType$2(fieldDef, channel);
    fieldDef["type"] = newType;
  }
  if (isTypedFieldDef(fieldDef)) {
    const { compatible, warning } = channelCompatibility(fieldDef, channel) || {};
    if (compatible === false) {
      warn3(warning);
    }
  }
  if (isSortableFieldDef(fieldDef) && isString(fieldDef.sort)) {
    const { sort: sort4 } = fieldDef;
    if (isSortByChannel(sort4)) {
      return {
        ...fieldDef,
        sort: { encoding: sort4 }
      };
    }
    const sub = sort4.substring(1);
    if (sort4.charAt(0) === "-" && isSortByChannel(sub)) {
      return {
        ...fieldDef,
        sort: { encoding: sub, order: "descending" }
      };
    }
  }
  if (isFacetFieldDef(fieldDef)) {
    const { header } = fieldDef;
    if (header) {
      const { orient: orient2, ...rest } = header;
      if (orient2) {
        return {
          ...fieldDef,
          header: {
            ...rest,
            labelOrient: header.labelOrient || orient2,
            titleOrient: header.titleOrient || orient2
          }
        };
      }
    }
  }
  return fieldDef;
}
function normalizeBin(bin3, channel) {
  if (isBoolean(bin3)) {
    return { maxbins: autoMaxBins(channel) };
  } else if (bin3 === "binned") {
    return {
      binned: true
    };
  } else if (!bin3.maxbins && !bin3.step) {
    return { ...bin3, maxbins: autoMaxBins(channel) };
  } else {
    return bin3;
  }
}
var COMPATIBLE = { compatible: true };
function channelCompatibility(fieldDef, channel) {
  const type4 = fieldDef.type;
  if (type4 === "geojson" && channel !== "shape") {
    return {
      compatible: false,
      warning: `Channel ${channel} should not be used with a geojson data.`
    };
  }
  switch (channel) {
    case ROW:
    case COLUMN:
    case FACET:
      if (!isDiscrete2(fieldDef)) {
        return {
          compatible: false,
          warning: channelShouldBeDiscrete(channel)
        };
      }
      return COMPATIBLE;
    case X3:
    case Y3:
    case XOFFSET:
    case YOFFSET:
    case COLOR:
    case FILL:
    case STROKE:
    case TEXT$1:
    case DETAIL:
    case KEY:
    case TOOLTIP:
    case HREF:
    case URL:
    case ANGLE:
    case THETA:
    case RADIUS:
    case DESCRIPTION:
      return COMPATIBLE;
    case LONGITUDE:
    case LONGITUDE2:
    case LATITUDE:
    case LATITUDE2:
      if (type4 !== QUANTITATIVE) {
        return {
          compatible: false,
          warning: `Channel ${channel} should be used with a quantitative field only, not ${fieldDef.type} field.`
        };
      }
      return COMPATIBLE;
    case OPACITY:
    case FILLOPACITY:
    case STROKEOPACITY:
    case STROKEWIDTH:
    case SIZE2:
    case THETA2:
    case RADIUS2:
    case X23:
    case Y23:
    case TIME:
      if (type4 === "nominal" && !fieldDef["sort"]) {
        return {
          compatible: false,
          warning: `Channel ${channel} should not be used with an unsorted discrete field.`
        };
      }
      return COMPATIBLE;
    case SHAPE:
    case STROKEDASH:
      if (!isDiscrete2(fieldDef) && !isDiscretizing2(fieldDef)) {
        return {
          compatible: false,
          warning: channelShouldBeDiscreteOrDiscretizing(channel)
        };
      }
      return COMPATIBLE;
    case ORDER:
      if (fieldDef.type === "nominal" && !("sort" in fieldDef)) {
        return {
          compatible: false,
          warning: `Channel order is inappropriate for nominal field, which has no inherent order.`
        };
      }
      return COMPATIBLE;
  }
}
function isFieldOrDatumDefForTimeFormat(fieldOrDatumDef) {
  const { formatType } = getFormatMixins(fieldOrDatumDef);
  return formatType === "time" || !formatType && isTemporalFieldDef(fieldOrDatumDef);
}
function isTemporalFieldDef(def3) {
  return def3 && (def3["type"] === "temporal" || isFieldDef(def3) && !!def3.timeUnit);
}
function valueExpr(v2, { timeUnit, type: type4, wrapTime, undefinedIfExprNotRequired }) {
  const unit2 = timeUnit && normalizeTimeUnit(timeUnit)?.unit;
  let isTime2 = unit2 || type4 === "temporal";
  let expr2;
  if (isExprRef(v2)) {
    expr2 = v2.expr;
  } else if (isSignalRef(v2)) {
    expr2 = v2.signal;
  } else if (isDateTime(v2)) {
    isTime2 = true;
    expr2 = dateTimeToExpr(v2);
  } else if (isString(v2) || isNumber(v2)) {
    if (isTime2) {
      expr2 = `datetime(${stringify(v2)})`;
      if (isLocalSingleTimeUnit(unit2)) {
        if (isNumber(v2) && v2 < 1e4 || isString(v2) && isNaN(Date.parse(v2))) {
          expr2 = dateTimeToExpr({ [unit2]: v2 });
        }
      }
    }
  }
  if (expr2) {
    return wrapTime && isTime2 ? `time(${expr2})` : expr2;
  }
  return undefinedIfExprNotRequired ? void 0 : stringify(v2);
}
function valueArray(fieldOrDatumDef, values4) {
  const { type: type4 } = fieldOrDatumDef;
  return values4.map((v2) => {
    const timeUnit = isFieldDef(fieldOrDatumDef) && !isBinnedTimeUnit(fieldOrDatumDef.timeUnit) ? fieldOrDatumDef.timeUnit : void 0;
    const expr2 = valueExpr(v2, {
      timeUnit,
      type: type4,
      undefinedIfExprNotRequired: true
    });
    if (expr2 !== void 0) {
      return { signal: expr2 };
    }
    return v2;
  });
}
function binRequiresRange(fieldDef, channel) {
  if (!isBinning(fieldDef.bin)) {
    console.warn("Only call this method for binned field defs.");
    return false;
  }
  return isScaleChannel(channel) && ["ordinal", "nominal"].includes(fieldDef.type);
}
var CONDITIONAL_AXIS_PROP_INDEX = {
  labelAlign: {
    part: "labels",
    vgProp: "align"
  },
  labelBaseline: {
    part: "labels",
    vgProp: "baseline"
  },
  labelColor: {
    part: "labels",
    vgProp: "fill"
  },
  labelFont: {
    part: "labels",
    vgProp: "font"
  },
  labelFontSize: {
    part: "labels",
    vgProp: "fontSize"
  },
  labelFontStyle: {
    part: "labels",
    vgProp: "fontStyle"
  },
  labelFontWeight: {
    part: "labels",
    vgProp: "fontWeight"
  },
  labelOpacity: {
    part: "labels",
    vgProp: "opacity"
  },
  labelOffset: null,
  labelPadding: null,
  // There is no fixed vgProp for tickSize, need to use signal.
  gridColor: {
    part: "grid",
    vgProp: "stroke"
  },
  gridDash: {
    part: "grid",
    vgProp: "strokeDash"
  },
  gridDashOffset: {
    part: "grid",
    vgProp: "strokeDashOffset"
  },
  gridOpacity: {
    part: "grid",
    vgProp: "opacity"
  },
  gridWidth: {
    part: "grid",
    vgProp: "strokeWidth"
  },
  tickColor: {
    part: "ticks",
    vgProp: "stroke"
  },
  tickDash: {
    part: "ticks",
    vgProp: "strokeDash"
  },
  tickDashOffset: {
    part: "ticks",
    vgProp: "strokeDashOffset"
  },
  tickOpacity: {
    part: "ticks",
    vgProp: "opacity"
  },
  tickSize: null,
  // There is no fixed vgProp for tickSize, need to use signal.
  tickWidth: {
    part: "ticks",
    vgProp: "strokeWidth"
  }
};
function isConditionalAxisValue(v2) {
  return v2?.condition;
}
var AXIS_PARTS = ["domain", "grid", "labels", "ticks", "title"];
var AXIS_PROPERTY_TYPE = {
  grid: "grid",
  gridCap: "grid",
  gridColor: "grid",
  gridDash: "grid",
  gridDashOffset: "grid",
  gridOpacity: "grid",
  gridScale: "grid",
  gridWidth: "grid",
  orient: "main",
  bandPosition: "both",
  // Need to be applied to grid axis too, so the grid will align with ticks.
  aria: "main",
  description: "main",
  domain: "main",
  domainCap: "main",
  domainColor: "main",
  domainDash: "main",
  domainDashOffset: "main",
  domainOpacity: "main",
  domainWidth: "main",
  format: "main",
  formatType: "main",
  labelAlign: "main",
  labelAngle: "main",
  labelBaseline: "main",
  labelBound: "main",
  labelColor: "main",
  labelFlush: "main",
  labelFlushOffset: "main",
  labelFont: "main",
  labelFontSize: "main",
  labelFontStyle: "main",
  labelFontWeight: "main",
  labelLimit: "main",
  labelLineHeight: "main",
  labelOffset: "main",
  labelOpacity: "main",
  labelOverlap: "main",
  labelPadding: "main",
  labels: "main",
  labelSeparation: "main",
  maxExtent: "main",
  minExtent: "main",
  offset: "both",
  position: "main",
  tickCap: "main",
  tickColor: "main",
  tickDash: "main",
  tickDashOffset: "main",
  tickMinStep: "both",
  tickOffset: "both",
  // Need to be applied to grid axis too, so the grid will align with ticks.
  tickOpacity: "main",
  tickRound: "both",
  // Apply rounding to grid and ticks so they are aligned.
  ticks: "main",
  tickSize: "main",
  tickWidth: "both",
  title: "main",
  titleAlign: "main",
  titleAnchor: "main",
  titleAngle: "main",
  titleBaseline: "main",
  titleColor: "main",
  titleFont: "main",
  titleFontSize: "main",
  titleFontStyle: "main",
  titleFontWeight: "main",
  titleLimit: "main",
  titleLineHeight: "main",
  titleOpacity: "main",
  titlePadding: "main",
  titleX: "main",
  titleY: "main",
  encode: "both",
  // we hide this in Vega-Lite
  scale: "both",
  tickBand: "both",
  tickCount: "both",
  tickExtra: "both",
  translate: "both",
  values: "both",
  zindex: "both"
  // this is actually set afterward, so it doesn't matter
};
var COMMON_AXIS_PROPERTIES_INDEX = {
  orient: 1,
  // other things can depend on orient
  aria: 1,
  bandPosition: 1,
  description: 1,
  domain: 1,
  domainCap: 1,
  domainColor: 1,
  domainDash: 1,
  domainDashOffset: 1,
  domainOpacity: 1,
  domainWidth: 1,
  format: 1,
  formatType: 1,
  grid: 1,
  gridCap: 1,
  gridColor: 1,
  gridDash: 1,
  gridDashOffset: 1,
  gridOpacity: 1,
  gridWidth: 1,
  labelAlign: 1,
  labelAngle: 1,
  labelBaseline: 1,
  labelBound: 1,
  labelColor: 1,
  labelFlush: 1,
  labelFlushOffset: 1,
  labelFont: 1,
  labelFontSize: 1,
  labelFontStyle: 1,
  labelFontWeight: 1,
  labelLimit: 1,
  labelLineHeight: 1,
  labelOffset: 1,
  labelOpacity: 1,
  labelOverlap: 1,
  labelPadding: 1,
  labels: 1,
  labelSeparation: 1,
  maxExtent: 1,
  minExtent: 1,
  offset: 1,
  position: 1,
  tickBand: 1,
  tickCap: 1,
  tickColor: 1,
  tickCount: 1,
  tickDash: 1,
  tickDashOffset: 1,
  tickExtra: 1,
  tickMinStep: 1,
  tickOffset: 1,
  tickOpacity: 1,
  tickRound: 1,
  ticks: 1,
  tickSize: 1,
  tickWidth: 1,
  title: 1,
  titleAlign: 1,
  titleAnchor: 1,
  titleAngle: 1,
  titleBaseline: 1,
  titleColor: 1,
  titleFont: 1,
  titleFontSize: 1,
  titleFontStyle: 1,
  titleFontWeight: 1,
  titleLimit: 1,
  titleLineHeight: 1,
  titleOpacity: 1,
  titlePadding: 1,
  titleX: 1,
  titleY: 1,
  translate: 1,
  values: 1,
  zindex: 1
};
var AXIS_PROPERTIES_INDEX = {
  ...COMMON_AXIS_PROPERTIES_INDEX,
  style: 1,
  labelExpr: 1,
  encoding: 1
};
function isAxisProperty(prop) {
  return hasOwnProperty(AXIS_PROPERTIES_INDEX, prop);
}
var AXIS_CONFIGS_INDEX = {
  axis: 1,
  axisBand: 1,
  axisBottom: 1,
  axisDiscrete: 1,
  axisLeft: 1,
  axisPoint: 1,
  axisQuantitative: 1,
  axisRight: 1,
  axisTemporal: 1,
  axisTop: 1,
  axisX: 1,
  axisXBand: 1,
  axisXDiscrete: 1,
  axisXPoint: 1,
  axisXQuantitative: 1,
  axisXTemporal: 1,
  axisY: 1,
  axisYBand: 1,
  axisYDiscrete: 1,
  axisYPoint: 1,
  axisYQuantitative: 1,
  axisYTemporal: 1
};
var AXIS_CONFIGS = keys3(AXIS_CONFIGS_INDEX);
function isUnitSpec(spec) {
  return hasProperty(spec, "mark");
}
var CompositeMarkNormalizer = class {
  name;
  run;
  constructor(name, run2) {
    this.name = name;
    this.run = run2;
  }
  hasMatchingType(spec) {
    if (isUnitSpec(spec)) {
      return getMarkType(spec.mark) === this.name;
    }
    return false;
  }
};
function channelHasField(encoding, channel) {
  const channelDef = encoding?.[channel];
  if (channelDef) {
    if (isArray(channelDef)) {
      return some(channelDef, (fieldDef) => !!fieldDef.field);
    } else {
      return isFieldDef(channelDef) || hasConditionalFieldDef(channelDef);
    }
  }
  return false;
}
function channelHasFieldOrDatum(encoding, channel) {
  const channelDef = encoding?.[channel];
  if (channelDef) {
    if (isArray(channelDef)) {
      return some(channelDef, (fieldDef) => !!fieldDef.field);
    } else {
      return isFieldDef(channelDef) || isDatumDef(channelDef) || hasConditionalFieldOrDatumDef(channelDef);
    }
  }
  return false;
}
function channelHasNestedOffsetScale(encoding, channel) {
  if (isXorY(channel)) {
    const fieldDef = encoding[channel];
    if ((isFieldDef(fieldDef) || isDatumDef(fieldDef)) && (isDiscrete$1(fieldDef.type) || isFieldDef(fieldDef) && fieldDef.timeUnit)) {
      const offsetChannel = getOffsetScaleChannel(channel);
      return channelHasFieldOrDatum(encoding, offsetChannel);
    }
  }
  return false;
}
function isAggregate$1(encoding) {
  return some(CHANNELS, (channel) => {
    if (channelHasField(encoding, channel)) {
      const channelDef = encoding[channel];
      if (isArray(channelDef)) {
        return some(channelDef, (fieldDef) => !!fieldDef.aggregate);
      } else {
        const fieldDef = getFieldDef(channelDef);
        return fieldDef && !!fieldDef.aggregate;
      }
    }
    return false;
  });
}
function extractTransformsFromEncoding(oldEncoding, config) {
  const groupby = [];
  const bins2 = [];
  const timeUnits3 = [];
  const aggregate = [];
  const encoding = {};
  forEach(oldEncoding, (channelDef, channel) => {
    if (isFieldDef(channelDef)) {
      const { field: field3, aggregate: aggOp, bin: bin3, timeUnit, ...remaining } = channelDef;
      if (aggOp || timeUnit || bin3) {
        const guide = getGuide(channelDef);
        const isTitleDefined = guide?.title;
        let newField = vgField(channelDef, { forAs: true });
        const newFieldDef = {
          // Only add title if it doesn't exist
          ...isTitleDefined ? [] : { title: title(channelDef, config, { allowDisabling: true }) },
          ...remaining,
          // Always overwrite field
          field: newField
        };
        if (aggOp) {
          let op;
          if (isArgmaxDef(aggOp)) {
            op = "argmax";
            newField = vgField({ op: "argmax", field: aggOp.argmax }, { forAs: true });
            newFieldDef.field = `${newField}.${field3}`;
          } else if (isArgminDef(aggOp)) {
            op = "argmin";
            newField = vgField({ op: "argmin", field: aggOp.argmin }, { forAs: true });
            newFieldDef.field = `${newField}.${field3}`;
          } else if (aggOp !== "boxplot" && aggOp !== "errorbar" && aggOp !== "errorband") {
            op = aggOp;
          }
          if (op) {
            const aggregateEntry = {
              op,
              as: newField
            };
            if (field3) {
              aggregateEntry.field = field3;
            }
            aggregate.push(aggregateEntry);
          }
        } else {
          groupby.push(newField);
          if (isTypedFieldDef(channelDef) && isBinning(bin3)) {
            bins2.push({ bin: bin3, field: field3, as: newField });
            groupby.push(vgField(channelDef, { binSuffix: "end" }));
            if (binRequiresRange(channelDef, channel)) {
              groupby.push(vgField(channelDef, { binSuffix: "range" }));
            }
            if (isXorY(channel)) {
              const secondaryChannel = {
                field: `${newField}_end`
              };
              encoding[`${channel}2`] = secondaryChannel;
            }
            newFieldDef.bin = "binned";
            if (!isSecondaryRangeChannel(channel)) {
              newFieldDef["type"] = QUANTITATIVE;
            }
          } else if (timeUnit && !isBinnedTimeUnit(timeUnit)) {
            timeUnits3.push({
              timeUnit,
              field: field3,
              as: newField
            });
            const formatType = isTypedFieldDef(channelDef) && channelDef.type !== TEMPORAL && "time";
            if (formatType) {
              if (channel === TEXT$1 || channel === TOOLTIP) {
                newFieldDef["formatType"] = formatType;
              } else if (isNonPositionScaleChannel(channel)) {
                newFieldDef["legend"] = {
                  formatType,
                  ...newFieldDef["legend"]
                };
              } else if (isXorY(channel)) {
                newFieldDef["axis"] = {
                  formatType,
                  ...newFieldDef["axis"]
                };
              }
            }
          }
        }
        encoding[channel] = newFieldDef;
      } else {
        groupby.push(field3);
        encoding[channel] = oldEncoding[channel];
      }
    } else {
      encoding[channel] = oldEncoding[channel];
    }
  });
  return {
    bins: bins2,
    timeUnits: timeUnits3,
    aggregate,
    groupby,
    encoding
  };
}
function markChannelCompatible(encoding, channel, mark) {
  const markSupported = supportMark(channel, mark);
  if (!markSupported) {
    return false;
  } else if (markSupported === "binned") {
    const primaryFieldDef = encoding[channel === X23 ? X3 : Y3];
    if (isFieldDef(primaryFieldDef) && isFieldDef(encoding[channel]) && isBinned(primaryFieldDef.bin)) {
      return true;
    } else {
      return false;
    }
  }
  return true;
}
function initEncoding(encoding, mark, filled, config) {
  const normalizedEncoding = {};
  for (const key2 of keys3(encoding)) {
    if (!isChannel(key2)) {
      warn3(invalidEncodingChannel(key2));
    }
  }
  for (let channel of UNIT_CHANNELS) {
    if (!encoding[channel]) {
      continue;
    }
    const channelDef = encoding[channel];
    if (isXorYOffset(channel)) {
      const mainChannel = getMainChannelFromOffsetChannel(channel);
      const positionDef = normalizedEncoding[mainChannel];
      if (isFieldDef(positionDef)) {
        if (isContinuous2(positionDef.type)) {
          if (isFieldDef(channelDef) && !positionDef.timeUnit) {
            warn3(offsetNestedInsideContinuousPositionScaleDropped(mainChannel));
            continue;
          }
        }
      }
    }
    if (channel === "angle" && mark === "arc" && !encoding.theta) {
      warn3(REPLACE_ANGLE_WITH_THETA);
      channel = THETA;
    }
    if (!markChannelCompatible(encoding, channel, mark)) {
      warn3(incompatibleChannel(channel, mark));
      continue;
    }
    if (channel === SIZE2 && mark === "line") {
      const fieldDef = getFieldDef(encoding[channel]);
      if (fieldDef?.aggregate) {
        warn3(LINE_WITH_VARYING_SIZE);
        continue;
      }
    }
    if (channel === COLOR && (filled ? "fill" in encoding : "stroke" in encoding)) {
      warn3(droppingColor("encoding", { fill: "fill" in encoding, stroke: "stroke" in encoding }));
      continue;
    }
    if (channel === DETAIL || channel === ORDER && !isArray(channelDef) && !isValueDef(channelDef) || channel === TOOLTIP && isArray(channelDef)) {
      if (channelDef) {
        if (channel === ORDER) {
          const def3 = encoding[channel];
          if (isOrderOnlyDef(def3)) {
            normalizedEncoding[channel] = def3;
            continue;
          }
        }
        normalizedEncoding[channel] = array_default(channelDef).reduce((defs, fieldDef) => {
          if (!isFieldDef(fieldDef)) {
            warn3(emptyFieldDef(fieldDef, channel));
          } else {
            defs.push(initFieldDef(fieldDef, channel));
          }
          return defs;
        }, []);
      }
    } else {
      if (channel === TOOLTIP && channelDef === null) {
        normalizedEncoding[channel] = null;
      } else if (!isFieldDef(channelDef) && !isDatumDef(channelDef) && !isValueDef(channelDef) && !isConditionalDef(channelDef) && !isSignalRef(channelDef)) {
        warn3(emptyFieldDef(channelDef, channel));
        continue;
      }
      normalizedEncoding[channel] = initChannelDef(channelDef, channel, config);
    }
  }
  return normalizedEncoding;
}
function normalizeEncoding(encoding, config) {
  const normalizedEncoding = {};
  for (const channel of keys3(encoding)) {
    const newChannelDef = initChannelDef(encoding[channel], channel, config, { compositeMark: true });
    normalizedEncoding[channel] = newChannelDef;
  }
  return normalizedEncoding;
}
function fieldDefs(encoding) {
  const arr = [];
  for (const channel of keys3(encoding)) {
    if (channelHasField(encoding, channel)) {
      const channelDef = encoding[channel];
      const channelDefArray = array_default(channelDef);
      for (const def3 of channelDefArray) {
        if (isFieldDef(def3)) {
          arr.push(def3);
        } else if (hasConditionalFieldDef(def3)) {
          arr.push(def3.condition);
        }
      }
    }
  }
  return arr;
}
function forEach(mapping, f, thisArg) {
  if (!mapping) {
    return;
  }
  for (const channel of keys3(mapping)) {
    const el = mapping[channel];
    if (isArray(el)) {
      for (const channelDef of el) {
        f.call(thisArg, channelDef, channel);
      }
    } else {
      f.call(thisArg, el, channel);
    }
  }
}
function reduce(mapping, f, init2, thisArg) {
  if (!mapping) {
    return init2;
  }
  return keys3(mapping).reduce((r, channel) => {
    const map3 = mapping[channel];
    if (isArray(map3)) {
      return map3.reduce((r1, channelDef) => {
        return f.call(thisArg, r1, channelDef, channel);
      }, r);
    } else {
      return f.call(thisArg, r, map3, channel);
    }
  }, init2);
}
function pathGroupingFields(mark, encoding) {
  return keys3(encoding).reduce((details, channel) => {
    switch (channel) {
      // x, y, x2, y2, lat, long, lat1, long2, order, tooltip, href, aria label, cursor should not cause lines to group
      case X3:
      case Y3:
      case HREF:
      case DESCRIPTION:
      case URL:
      case X23:
      case Y23:
        return details;
      case XOFFSET:
      case YOFFSET: {
        if (mark === "line" || mark === "area" || mark === "trail") {
          const offsetDef = encoding[channel];
          if (isFieldDef(offsetDef)) {
            const mainChannel = channel === XOFFSET ? X3 : Y3;
            const mainDef = encoding[mainChannel];
            if (isFieldDef(mainDef) && !mainDef.aggregate && !offsetDef.aggregate) {
              const mainField = vgField(mainDef, {});
              const offsetField = vgField(offsetDef, {});
              if (mainField && offsetField && mainField !== offsetField) {
                details.push(mainField);
              }
            }
          }
        }
        return details;
      }
      case THETA:
      case THETA2:
      case RADIUS:
      case RADIUS2:
      case TIME:
      // falls through
      case LATITUDE:
      case LONGITUDE:
      case LATITUDE2:
      case LONGITUDE2:
      // TODO: case 'cursor':
      // text, shape, shouldn't be a part of line/trail/area [falls through]
      case TEXT$1:
      case SHAPE:
      case ANGLE:
      // falls through
      // tooltip fields should not be added to group by [falls through]
      case TOOLTIP:
        return details;
      case ORDER:
        if (mark === "line" || mark === "trail") {
          return details;
        }
      // but order should group area for stacking (falls through)
      case DETAIL:
      case KEY: {
        const channelDef = encoding[channel];
        if (isArray(channelDef) || isFieldDef(channelDef)) {
          for (const fieldDef of array_default(channelDef)) {
            if (!fieldDef.aggregate) {
              details.push(vgField(fieldDef, {}));
            }
          }
        }
        return details;
      }
      case SIZE2:
        if (mark === "trail") {
          return details;
        }
      // For line, size should group lines.
      // falls through
      case COLOR:
      case FILL:
      case STROKE:
      case OPACITY:
      case FILLOPACITY:
      case STROKEOPACITY:
      case STROKEDASH:
      case STROKEWIDTH: {
        const fieldDef = getFieldDef(encoding[channel]);
        if (fieldDef && !fieldDef.aggregate) {
          details.push(vgField(fieldDef, {}));
        }
        return details;
      }
    }
  }, []);
}
function filterTooltipWithAggregatedField(oldEncoding) {
  const { tooltip: tooltip2, ...filteredEncoding } = oldEncoding;
  if (!tooltip2) {
    return { filteredEncoding };
  }
  let customTooltipWithAggregatedField;
  let customTooltipWithoutAggregatedField;
  if (isArray(tooltip2)) {
    for (const t of tooltip2) {
      if (t.aggregate) {
        if (!customTooltipWithAggregatedField) {
          customTooltipWithAggregatedField = [];
        }
        customTooltipWithAggregatedField.push(t);
      } else {
        if (!customTooltipWithoutAggregatedField) {
          customTooltipWithoutAggregatedField = [];
        }
        customTooltipWithoutAggregatedField.push(t);
      }
    }
    if (customTooltipWithAggregatedField) {
      filteredEncoding.tooltip = customTooltipWithAggregatedField;
    }
  } else {
    if (tooltip2.aggregate) {
      filteredEncoding.tooltip = tooltip2;
    } else {
      customTooltipWithoutAggregatedField = tooltip2;
    }
  }
  if (isArray(customTooltipWithoutAggregatedField) && customTooltipWithoutAggregatedField.length === 1) {
    customTooltipWithoutAggregatedField = customTooltipWithoutAggregatedField[0];
  }
  return { customTooltipWithoutAggregatedField, filteredEncoding };
}
function getCompositeMarkTooltip(tooltipSummary, continuousAxisChannelDef, encodingWithoutContinuousAxis, withFieldName = true) {
  if ("tooltip" in encodingWithoutContinuousAxis) {
    return { tooltip: encodingWithoutContinuousAxis.tooltip };
  }
  const fiveSummaryTooltip = tooltipSummary.map(({ fieldPrefix, titlePrefix }) => {
    const mainTitle = withFieldName ? ` of ${getTitle(continuousAxisChannelDef)}` : "";
    return {
      field: fieldPrefix + continuousAxisChannelDef.field,
      type: continuousAxisChannelDef.type,
      title: isSignalRef(titlePrefix) ? { signal: `${titlePrefix}"${escape(mainTitle)}"` } : titlePrefix + mainTitle
    };
  });
  const tooltipFieldDefs = fieldDefs(encodingWithoutContinuousAxis).map(toStringFieldDef);
  return {
    tooltip: [
      ...fiveSummaryTooltip,
      // need to cast because TextFieldDef supports fewer types of bin
      ...unique(tooltipFieldDefs, hash)
    ]
  };
}
function getTitle(continuousAxisChannelDef) {
  const { title: title2, field: field3 } = continuousAxisChannelDef;
  return getFirstDefined(title2, field3);
}
function makeCompositeAggregatePartFactory(compositeMarkDef, continuousAxis, continuousAxisChannelDef, sharedEncoding, compositeMarkConfig) {
  const { scale: scale9, axis } = continuousAxisChannelDef;
  return ({ partName, mark, positionPrefix, endPositionPrefix = void 0, extraEncoding = {} }) => {
    const title2 = getTitle(continuousAxisChannelDef);
    return partLayerMixins(compositeMarkDef, partName, compositeMarkConfig, {
      mark,
      // TODO better remove this method and just have mark as a parameter of the method
      encoding: {
        [continuousAxis]: {
          field: `${positionPrefix}_${continuousAxisChannelDef.field}`,
          type: continuousAxisChannelDef.type,
          ...title2 !== void 0 ? { title: title2 } : {},
          ...scale9 !== void 0 ? { scale: scale9 } : {},
          ...axis !== void 0 ? { axis } : {}
        },
        ...isString(endPositionPrefix) ? {
          [`${continuousAxis}2`]: {
            field: `${endPositionPrefix}_${continuousAxisChannelDef.field}`
          }
        } : {},
        ...sharedEncoding,
        ...extraEncoding
      }
    });
  };
}
function partLayerMixins(markDef, part, compositeMarkConfig, partBaseSpec) {
  const { clip: clip4, color: color6, opacity: opacity2 } = markDef;
  const mark = markDef.type;
  if (markDef[part] || markDef[part] === void 0 && compositeMarkConfig[part]) {
    return [
      {
        ...partBaseSpec,
        mark: {
          ...compositeMarkConfig[part],
          ...clip4 ? { clip: clip4 } : {},
          ...color6 ? { color: color6 } : {},
          ...opacity2 ? { opacity: opacity2 } : {},
          ...isMarkDef(partBaseSpec.mark) ? partBaseSpec.mark : { type: partBaseSpec.mark },
          style: `${mark}-${String(part)}`,
          ...isBoolean(markDef[part]) ? {} : markDef[part]
        }
      }
    ];
  }
  return [];
}
function compositeMarkContinuousAxis(spec, orient2, compositeMark) {
  const { encoding } = spec;
  const continuousAxis = orient2 === "vertical" ? "y" : "x";
  const continuousAxisChannelDef = encoding[continuousAxis];
  const continuousAxisChannelDef2 = encoding[`${continuousAxis}2`];
  const continuousAxisChannelDefError = encoding[`${continuousAxis}Error`];
  const continuousAxisChannelDefError2 = encoding[`${continuousAxis}Error2`];
  return {
    continuousAxisChannelDef: filterAggregateFromChannelDef(continuousAxisChannelDef, compositeMark),
    continuousAxisChannelDef2: filterAggregateFromChannelDef(continuousAxisChannelDef2, compositeMark),
    continuousAxisChannelDefError: filterAggregateFromChannelDef(continuousAxisChannelDefError, compositeMark),
    continuousAxisChannelDefError2: filterAggregateFromChannelDef(continuousAxisChannelDefError2, compositeMark),
    continuousAxis
  };
}
function filterAggregateFromChannelDef(continuousAxisChannelDef, compositeMark) {
  if (continuousAxisChannelDef?.aggregate) {
    const { aggregate, ...continuousAxisWithoutAggregate } = continuousAxisChannelDef;
    if (aggregate !== compositeMark) {
      warn3(errorBarContinuousAxisHasCustomizedAggregate(aggregate, compositeMark));
    }
    return continuousAxisWithoutAggregate;
  } else {
    return continuousAxisChannelDef;
  }
}
function compositeMarkOrient(spec, compositeMark) {
  const { mark, encoding } = spec;
  const { x: x6, y: y6 } = encoding;
  if (isMarkDef(mark) && mark.orient) {
    return mark.orient;
  }
  if (isContinuousFieldOrDatumDef(x6)) {
    if (isContinuousFieldOrDatumDef(y6)) {
      const xAggregate = isFieldDef(x6) && x6.aggregate;
      const yAggregate = isFieldDef(y6) && y6.aggregate;
      if (!xAggregate && yAggregate === compositeMark) {
        return "vertical";
      } else if (!yAggregate && xAggregate === compositeMark) {
        return "horizontal";
      } else if (xAggregate === compositeMark && yAggregate === compositeMark) {
        throw new Error("Both x and y cannot have aggregate");
      } else {
        if (isFieldOrDatumDefForTimeFormat(y6) && !isFieldOrDatumDefForTimeFormat(x6)) {
          return "horizontal";
        }
        return "vertical";
      }
    }
    return "horizontal";
  } else if (isContinuousFieldOrDatumDef(y6)) {
    return "vertical";
  } else {
    throw new Error(`Need a valid continuous axis for ${compositeMark}s`);
  }
}
var BOXPLOT = "boxplot";
var BOXPLOT_PARTS = ["box", "median", "outliers", "rule", "ticks"];
var boxPlotNormalizer = new CompositeMarkNormalizer(BOXPLOT, normalizeBoxPlot);
function getBoxPlotType(extent2) {
  if (isNumber(extent2)) {
    return "tukey";
  }
  return extent2;
}
function normalizeBoxPlot(spec, { config }) {
  spec = {
    ...spec,
    encoding: normalizeEncoding(spec.encoding, config)
  };
  const { mark, encoding: _encoding, params: params2, projection: _p, ...outerSpec } = spec;
  const markDef = isMarkDef(mark) ? mark : { type: mark };
  if (params2) {
    warn3(selectionNotSupported("boxplot"));
  }
  const extent2 = markDef.extent ?? config.boxplot.extent;
  const sizeValue = getMarkPropOrConfig(
    "size",
    markDef,
    // TODO: https://github.com/vega/vega-lite/issues/6245
    config
  );
  const invalid = markDef.invalid;
  const boxPlotType = getBoxPlotType(extent2);
  const { bins: bins2, timeUnits: timeUnits3, transform: transform4, continuousAxisChannelDef, continuousAxis, groupby, aggregate, encodingWithoutContinuousAxis, ticksOrient, boxOrient, customTooltipWithoutAggregatedField } = boxParams(spec, extent2, config);
  const aliasedFieldName = removePathFromField(continuousAxisChannelDef.field);
  const { color: color6, size, ...encodingWithoutSizeColorAndContinuousAxis } = encodingWithoutContinuousAxis;
  const makeBoxPlotPart = (sharedEncoding) => {
    return makeCompositeAggregatePartFactory(markDef, continuousAxis, continuousAxisChannelDef, sharedEncoding, config.boxplot);
  };
  const makeBoxPlotExtent = makeBoxPlotPart(encodingWithoutSizeColorAndContinuousAxis);
  const makeBoxPlotBox = makeBoxPlotPart(encodingWithoutContinuousAxis);
  const defaultBoxColor = (isObject(config.boxplot.box) ? config.boxplot.box.color : config.mark.color) || "#4c78a8";
  const makeBoxPlotMidTick = makeBoxPlotPart({
    ...encodingWithoutSizeColorAndContinuousAxis,
    ...size ? { size } : {},
    color: {
      condition: {
        test: `${accessWithDatumToUnescapedPath(`lower_box_${continuousAxisChannelDef.field}`)} >= ${accessWithDatumToUnescapedPath(`upper_box_${continuousAxisChannelDef.field}`)}`,
        ...color6 || { value: defaultBoxColor }
      }
    }
  });
  const fiveSummaryTooltipEncoding = getCompositeMarkTooltip([
    { fieldPrefix: boxPlotType === "min-max" ? "upper_whisker_" : "max_", titlePrefix: "Max" },
    { fieldPrefix: "upper_box_", titlePrefix: "Q3" },
    { fieldPrefix: "mid_box_", titlePrefix: "Median" },
    { fieldPrefix: "lower_box_", titlePrefix: "Q1" },
    { fieldPrefix: boxPlotType === "min-max" ? "lower_whisker_" : "min_", titlePrefix: "Min" }
  ], continuousAxisChannelDef, encodingWithoutContinuousAxis);
  const endTick = { type: "tick", color: "black", opacity: 1, orient: ticksOrient, invalid, aria: false };
  const whiskerTooltipEncoding = boxPlotType === "min-max" ? fiveSummaryTooltipEncoding : (
    // for tukey / k-IQR, just show upper/lower-whisker
    getCompositeMarkTooltip([
      { fieldPrefix: "upper_whisker_", titlePrefix: "Upper Whisker" },
      { fieldPrefix: "lower_whisker_", titlePrefix: "Lower Whisker" }
    ], continuousAxisChannelDef, encodingWithoutContinuousAxis)
  );
  const whiskerLayers = [
    ...makeBoxPlotExtent({
      partName: "rule",
      mark: { type: "rule", invalid, aria: false },
      positionPrefix: "lower_whisker",
      endPositionPrefix: "lower_box",
      extraEncoding: whiskerTooltipEncoding
    }),
    ...makeBoxPlotExtent({
      partName: "rule",
      mark: { type: "rule", invalid, aria: false },
      positionPrefix: "upper_box",
      endPositionPrefix: "upper_whisker",
      extraEncoding: whiskerTooltipEncoding
    }),
    ...makeBoxPlotExtent({
      partName: "ticks",
      mark: endTick,
      positionPrefix: "lower_whisker",
      extraEncoding: whiskerTooltipEncoding
    }),
    ...makeBoxPlotExtent({
      partName: "ticks",
      mark: endTick,
      positionPrefix: "upper_whisker",
      extraEncoding: whiskerTooltipEncoding
    })
  ];
  const boxLayers = [
    ...boxPlotType !== "tukey" ? whiskerLayers : [],
    ...makeBoxPlotBox({
      partName: "box",
      mark: {
        type: "bar",
        ...sizeValue ? { size: sizeValue } : {},
        orient: boxOrient,
        invalid,
        ariaRoleDescription: "box"
      },
      positionPrefix: "lower_box",
      endPositionPrefix: "upper_box",
      extraEncoding: fiveSummaryTooltipEncoding
    }),
    ...makeBoxPlotMidTick({
      partName: "median",
      mark: {
        type: "tick",
        invalid,
        ...isObject(config.boxplot.median) && config.boxplot.median.color ? { color: config.boxplot.median.color } : {},
        ...sizeValue ? { size: sizeValue } : {},
        orient: ticksOrient,
        aria: false
      },
      positionPrefix: "mid_box",
      extraEncoding: fiveSummaryTooltipEncoding
    })
  ];
  if (boxPlotType === "min-max") {
    return {
      ...outerSpec,
      transform: (outerSpec.transform ?? []).concat(transform4),
      layer: boxLayers
    };
  }
  const lowerBoxExpr = accessWithDatumToUnescapedPath(`lower_box_${continuousAxisChannelDef.field}`);
  const upperBoxExpr = accessWithDatumToUnescapedPath(`upper_box_${continuousAxisChannelDef.field}`);
  const iqrExpr = `(${upperBoxExpr} - ${lowerBoxExpr})`;
  const lowerWhiskerExpr = `${lowerBoxExpr} - ${extent2} * ${iqrExpr}`;
  const upperWhiskerExpr = `${upperBoxExpr} + ${extent2} * ${iqrExpr}`;
  const fieldExpr2 = accessWithDatumToUnescapedPath(continuousAxisChannelDef.field);
  const joinaggregateTransform = {
    joinaggregate: boxParamsQuartiles(continuousAxisChannelDef.field),
    groupby
  };
  const filteredWhiskerSpec = {
    transform: [
      {
        filter: `(${lowerWhiskerExpr} <= ${fieldExpr2}) && (${fieldExpr2} <= ${upperWhiskerExpr})`
      },
      {
        aggregate: [
          {
            op: "min",
            field: continuousAxisChannelDef.field,
            as: `lower_whisker_${aliasedFieldName}`
          },
          {
            op: "max",
            field: continuousAxisChannelDef.field,
            as: `upper_whisker_${aliasedFieldName}`
          },
          // preserve lower_box / upper_box
          {
            op: "min",
            field: `lower_box_${continuousAxisChannelDef.field}`,
            as: `lower_box_${aliasedFieldName}`
          },
          {
            op: "max",
            field: `upper_box_${continuousAxisChannelDef.field}`,
            as: `upper_box_${aliasedFieldName}`
          },
          ...aggregate
        ],
        groupby
      }
    ],
    layer: whiskerLayers
  };
  const { tooltip: tooltip2, ...encodingWithoutSizeColorContinuousAxisAndTooltip } = encodingWithoutSizeColorAndContinuousAxis;
  const { scale: scale9, axis } = continuousAxisChannelDef;
  const title2 = getTitle(continuousAxisChannelDef);
  const outlierLayersMixins = partLayerMixins(markDef, "outliers", config.boxplot, {
    transform: [{ filter: `(${fieldExpr2} < ${lowerWhiskerExpr}) || (${fieldExpr2} > ${upperWhiskerExpr})` }],
    mark: "point",
    encoding: {
      [continuousAxis]: {
        field: continuousAxisChannelDef.field,
        type: continuousAxisChannelDef.type,
        ...title2 !== void 0 ? { title: title2 } : {},
        ...scale9 !== void 0 ? { scale: scale9 } : {},
        ...axis !== void 0 ? { axis } : {}
      },
      ...encodingWithoutSizeColorContinuousAxisAndTooltip,
      ...color6 ? { color: color6 } : {},
      ...customTooltipWithoutAggregatedField ? { tooltip: customTooltipWithoutAggregatedField } : {}
    }
  })[0];
  let filteredLayersMixins;
  const filteredLayersMixinsTransforms = [...bins2, ...timeUnits3, joinaggregateTransform];
  if (outlierLayersMixins) {
    filteredLayersMixins = {
      transform: filteredLayersMixinsTransforms,
      layer: [outlierLayersMixins, filteredWhiskerSpec]
    };
  } else {
    filteredLayersMixins = filteredWhiskerSpec;
    filteredLayersMixins.transform.unshift(...filteredLayersMixinsTransforms);
  }
  return {
    ...outerSpec,
    layer: [
      filteredLayersMixins,
      {
        // boxplot
        transform: transform4,
        layer: boxLayers
      }
    ]
  };
}
function boxParamsQuartiles(continousAxisField) {
  const aliasedFieldName = removePathFromField(continousAxisField);
  return [
    {
      op: "q1",
      field: continousAxisField,
      as: `lower_box_${aliasedFieldName}`
    },
    {
      op: "q3",
      field: continousAxisField,
      as: `upper_box_${aliasedFieldName}`
    }
  ];
}
function boxParams(spec, extent2, config) {
  const orient2 = compositeMarkOrient(spec, BOXPLOT);
  const { continuousAxisChannelDef, continuousAxis } = compositeMarkContinuousAxis(spec, orient2, BOXPLOT);
  const continuousFieldName = continuousAxisChannelDef.field;
  const aliasedFieldName = removePathFromField(continuousFieldName);
  const boxPlotType = getBoxPlotType(extent2);
  const boxplotSpecificAggregate = [
    ...boxParamsQuartiles(continuousFieldName),
    {
      op: "median",
      field: continuousFieldName,
      as: `mid_box_${aliasedFieldName}`
    },
    {
      op: "min",
      field: continuousFieldName,
      as: (boxPlotType === "min-max" ? "lower_whisker_" : "min_") + aliasedFieldName
    },
    {
      op: "max",
      field: continuousFieldName,
      as: (boxPlotType === "min-max" ? "upper_whisker_" : "max_") + aliasedFieldName
    }
  ];
  const postAggregateCalculates = boxPlotType === "min-max" || boxPlotType === "tukey" ? [] : [
    // This is for the  original k-IQR, which we do not expose
    {
      calculate: `${accessWithDatumToUnescapedPath(`upper_box_${aliasedFieldName}`)} - ${accessWithDatumToUnescapedPath(`lower_box_${aliasedFieldName}`)}`,
      as: `iqr_${aliasedFieldName}`
    },
    {
      calculate: `min(${accessWithDatumToUnescapedPath(`upper_box_${aliasedFieldName}`)} + ${accessWithDatumToUnescapedPath(`iqr_${aliasedFieldName}`)} * ${extent2}, ${accessWithDatumToUnescapedPath(`max_${aliasedFieldName}`)})`,
      as: `upper_whisker_${aliasedFieldName}`
    },
    {
      calculate: `max(${accessWithDatumToUnescapedPath(`lower_box_${aliasedFieldName}`)} - ${accessWithDatumToUnescapedPath(`iqr_${aliasedFieldName}`)} * ${extent2}, ${accessWithDatumToUnescapedPath(`min_${aliasedFieldName}`)})`,
      as: `lower_whisker_${aliasedFieldName}`
    }
  ];
  const { [continuousAxis]: oldContinuousAxisChannelDef, ...oldEncodingWithoutContinuousAxis } = spec.encoding;
  const { customTooltipWithoutAggregatedField, filteredEncoding } = filterTooltipWithAggregatedField(oldEncodingWithoutContinuousAxis);
  const { bins: bins2, timeUnits: timeUnits3, aggregate, groupby, encoding: encodingWithoutContinuousAxis } = extractTransformsFromEncoding(filteredEncoding, config);
  const ticksOrient = orient2 === "vertical" ? "horizontal" : "vertical";
  const boxOrient = orient2;
  const transform4 = [
    ...bins2,
    ...timeUnits3,
    {
      aggregate: [...aggregate, ...boxplotSpecificAggregate],
      groupby
    },
    ...postAggregateCalculates
  ];
  return {
    bins: bins2,
    timeUnits: timeUnits3,
    transform: transform4,
    groupby,
    aggregate,
    continuousAxisChannelDef,
    continuousAxis,
    encodingWithoutContinuousAxis,
    ticksOrient,
    boxOrient,
    customTooltipWithoutAggregatedField
  };
}
var ERRORBAR = "errorbar";
var ERRORBAR_PARTS = ["ticks", "rule"];
var errorBarNormalizer = new CompositeMarkNormalizer(ERRORBAR, normalizeErrorBar);
function normalizeErrorBar(spec, { config }) {
  spec = {
    ...spec,
    encoding: normalizeEncoding(spec.encoding, config)
  };
  const { transform: transform4, continuousAxisChannelDef, continuousAxis, encodingWithoutContinuousAxis, ticksOrient, markDef, outerSpec, tooltipEncoding } = errorBarParams(spec, ERRORBAR, config);
  delete encodingWithoutContinuousAxis.size;
  const makeErrorBarPart = makeCompositeAggregatePartFactory(markDef, continuousAxis, continuousAxisChannelDef, encodingWithoutContinuousAxis, config.errorbar);
  const thickness = markDef.thickness;
  const size = markDef.size;
  const tick2 = {
    type: "tick",
    orient: ticksOrient,
    aria: false,
    ...thickness !== void 0 ? { thickness } : {},
    ...size !== void 0 ? { size } : {}
  };
  const layer = [
    ...makeErrorBarPart({
      partName: "ticks",
      mark: tick2,
      positionPrefix: "lower",
      extraEncoding: tooltipEncoding
    }),
    ...makeErrorBarPart({
      partName: "ticks",
      mark: tick2,
      positionPrefix: "upper",
      extraEncoding: tooltipEncoding
    }),
    ...makeErrorBarPart({
      partName: "rule",
      mark: {
        type: "rule",
        ariaRoleDescription: "errorbar",
        ...thickness !== void 0 ? { size: thickness } : {}
      },
      positionPrefix: "lower",
      endPositionPrefix: "upper",
      extraEncoding: tooltipEncoding
    })
  ];
  return {
    ...outerSpec,
    transform: transform4,
    ...layer.length > 1 ? { layer } : { ...layer[0] }
  };
}
function errorBarOrientAndInputType(spec, compositeMark) {
  const { encoding } = spec;
  if (errorBarIsInputTypeRaw(encoding)) {
    return {
      orient: compositeMarkOrient(spec, compositeMark),
      inputType: "raw"
    };
  }
  const isTypeAggregatedUpperLower = errorBarIsInputTypeAggregatedUpperLower(encoding);
  const isTypeAggregatedError = errorBarIsInputTypeAggregatedError(encoding);
  const x6 = encoding.x;
  const y6 = encoding.y;
  if (isTypeAggregatedUpperLower) {
    if (isTypeAggregatedError) {
      throw new Error(`${compositeMark} cannot be both type aggregated-upper-lower and aggregated-error`);
    }
    const x22 = encoding.x2;
    const y22 = encoding.y2;
    if (isFieldOrDatumDef(x22) && isFieldOrDatumDef(y22)) {
      throw new Error(`${compositeMark} cannot have both x2 and y2`);
    } else if (isFieldOrDatumDef(x22)) {
      if (isContinuousFieldOrDatumDef(x6)) {
        return { orient: "horizontal", inputType: "aggregated-upper-lower" };
      } else {
        throw new Error(`Both x and x2 have to be quantitative in ${compositeMark}`);
      }
    } else if (isFieldOrDatumDef(y22)) {
      if (isContinuousFieldOrDatumDef(y6)) {
        return { orient: "vertical", inputType: "aggregated-upper-lower" };
      } else {
        throw new Error(`Both y and y2 have to be quantitative in ${compositeMark}`);
      }
    }
    throw new Error("No ranged axis");
  } else {
    const xError = encoding.xError;
    const xError2 = encoding.xError2;
    const yError = encoding.yError;
    const yError2 = encoding.yError2;
    if (isFieldOrDatumDef(xError2) && !isFieldOrDatumDef(xError)) {
      throw new Error(`${compositeMark} cannot have xError2 without xError`);
    }
    if (isFieldOrDatumDef(yError2) && !isFieldOrDatumDef(yError)) {
      throw new Error(`${compositeMark} cannot have yError2 without yError`);
    }
    if (isFieldOrDatumDef(xError) && isFieldOrDatumDef(yError)) {
      throw new Error(`${compositeMark} cannot have both xError and yError with both are quantiative`);
    } else if (isFieldOrDatumDef(xError)) {
      if (isContinuousFieldOrDatumDef(x6)) {
        return { orient: "horizontal", inputType: "aggregated-error" };
      } else {
        throw new Error("All x, xError, and xError2 (if exist) have to be quantitative");
      }
    } else if (isFieldOrDatumDef(yError)) {
      if (isContinuousFieldOrDatumDef(y6)) {
        return { orient: "vertical", inputType: "aggregated-error" };
      } else {
        throw new Error("All y, yError, and yError2 (if exist) have to be quantitative");
      }
    }
    throw new Error("No ranged axis");
  }
}
function errorBarIsInputTypeRaw(encoding) {
  return (isFieldOrDatumDef(encoding.x) || isFieldOrDatumDef(encoding.y)) && !isFieldOrDatumDef(encoding.x2) && !isFieldOrDatumDef(encoding.y2) && !isFieldOrDatumDef(encoding.xError) && !isFieldOrDatumDef(encoding.xError2) && !isFieldOrDatumDef(encoding.yError) && !isFieldOrDatumDef(encoding.yError2);
}
function errorBarIsInputTypeAggregatedUpperLower(encoding) {
  return isFieldOrDatumDef(encoding.x2) || isFieldOrDatumDef(encoding.y2);
}
function errorBarIsInputTypeAggregatedError(encoding) {
  return isFieldOrDatumDef(encoding.xError) || isFieldOrDatumDef(encoding.xError2) || isFieldOrDatumDef(encoding.yError) || isFieldOrDatumDef(encoding.yError2);
}
function errorBarParams(spec, compositeMark, config) {
  const { mark, encoding, params: params2, projection: _p, ...outerSpec } = spec;
  const markDef = isMarkDef(mark) ? mark : { type: mark };
  if (params2) {
    warn3(selectionNotSupported(compositeMark));
  }
  const { orient: orient2, inputType } = errorBarOrientAndInputType(spec, compositeMark);
  const { continuousAxisChannelDef, continuousAxisChannelDef2, continuousAxisChannelDefError, continuousAxisChannelDefError2, continuousAxis } = compositeMarkContinuousAxis(spec, orient2, compositeMark);
  const { errorBarSpecificAggregate, postAggregateCalculates, tooltipSummary, tooltipTitleWithFieldName } = errorBarAggregationAndCalculation(markDef, continuousAxisChannelDef, continuousAxisChannelDef2, continuousAxisChannelDefError, continuousAxisChannelDefError2, inputType, compositeMark, config);
  const { [continuousAxis]: oldContinuousAxisChannelDef, [continuousAxis === "x" ? "x2" : "y2"]: oldContinuousAxisChannelDef2, [continuousAxis === "x" ? "xError" : "yError"]: oldContinuousAxisChannelDefError, [continuousAxis === "x" ? "xError2" : "yError2"]: oldContinuousAxisChannelDefError2, ...oldEncodingWithoutContinuousAxis } = encoding;
  const { bins: bins2, timeUnits: timeUnits3, aggregate: oldAggregate, groupby: oldGroupBy, encoding: encodingWithoutContinuousAxis } = extractTransformsFromEncoding(oldEncodingWithoutContinuousAxis, config);
  const aggregate = [...oldAggregate, ...errorBarSpecificAggregate];
  const groupby = inputType !== "raw" ? [] : oldGroupBy;
  const tooltipEncoding = getCompositeMarkTooltip(tooltipSummary, continuousAxisChannelDef, encodingWithoutContinuousAxis, tooltipTitleWithFieldName);
  return {
    transform: [
      ...outerSpec.transform ?? [],
      ...bins2,
      ...timeUnits3,
      ...aggregate.length === 0 ? [] : [{ aggregate, groupby }],
      ...postAggregateCalculates
    ],
    groupby,
    continuousAxisChannelDef,
    continuousAxis,
    encodingWithoutContinuousAxis,
    ticksOrient: orient2 === "vertical" ? "horizontal" : "vertical",
    markDef,
    outerSpec,
    tooltipEncoding
  };
}
function errorBarAggregationAndCalculation(markDef, continuousAxisChannelDef, continuousAxisChannelDef2, continuousAxisChannelDefError, continuousAxisChannelDefError2, inputType, compositeMark, config) {
  let errorBarSpecificAggregate = [];
  let postAggregateCalculates = [];
  const continuousFieldName = continuousAxisChannelDef.field;
  let tooltipSummary;
  let tooltipTitleWithFieldName = false;
  if (inputType === "raw") {
    const center = markDef.center ? markDef.center : markDef.extent ? markDef.extent === "iqr" ? "median" : "mean" : config.errorbar.center;
    const extent2 = markDef.extent ? markDef.extent : center === "mean" ? "stderr" : "iqr";
    if (center === "median" !== (extent2 === "iqr")) {
      warn3(errorBarCenterIsUsedWithWrongExtent(center, extent2, compositeMark));
    }
    if (extent2 === "stderr" || extent2 === "stdev") {
      errorBarSpecificAggregate = [
        { op: extent2, field: continuousFieldName, as: `extent_${continuousFieldName}` },
        { op: center, field: continuousFieldName, as: `center_${continuousFieldName}` }
      ];
      postAggregateCalculates = [
        {
          calculate: `${accessWithDatumToUnescapedPath(`center_${continuousFieldName}`)} + ${accessWithDatumToUnescapedPath(`extent_${continuousFieldName}`)}`,
          as: `upper_${continuousFieldName}`
        },
        {
          calculate: `${accessWithDatumToUnescapedPath(`center_${continuousFieldName}`)} - ${accessWithDatumToUnescapedPath(`extent_${continuousFieldName}`)}`,
          as: `lower_${continuousFieldName}`
        }
      ];
      tooltipSummary = [
        { fieldPrefix: "center_", titlePrefix: titleCase(center) },
        { fieldPrefix: "upper_", titlePrefix: getTitlePrefix(center, extent2, "+") },
        { fieldPrefix: "lower_", titlePrefix: getTitlePrefix(center, extent2, "-") }
      ];
      tooltipTitleWithFieldName = true;
    } else {
      let centerOp;
      let lowerExtentOp;
      let upperExtentOp;
      if (extent2 === "ci") {
        centerOp = "mean";
        lowerExtentOp = "ci0";
        upperExtentOp = "ci1";
      } else {
        centerOp = "median";
        lowerExtentOp = "q1";
        upperExtentOp = "q3";
      }
      errorBarSpecificAggregate = [
        { op: lowerExtentOp, field: continuousFieldName, as: `lower_${continuousFieldName}` },
        { op: upperExtentOp, field: continuousFieldName, as: `upper_${continuousFieldName}` },
        { op: centerOp, field: continuousFieldName, as: `center_${continuousFieldName}` }
      ];
      tooltipSummary = [
        {
          fieldPrefix: "upper_",
          titlePrefix: title({ field: continuousFieldName, aggregate: upperExtentOp, type: "quantitative" }, config, {
            allowDisabling: false
          })
        },
        {
          fieldPrefix: "lower_",
          titlePrefix: title({ field: continuousFieldName, aggregate: lowerExtentOp, type: "quantitative" }, config, {
            allowDisabling: false
          })
        },
        {
          fieldPrefix: "center_",
          titlePrefix: title({ field: continuousFieldName, aggregate: centerOp, type: "quantitative" }, config, {
            allowDisabling: false
          })
        }
      ];
    }
  } else {
    if (markDef.center || markDef.extent) {
      warn3(errorBarCenterAndExtentAreNotNeeded(markDef.center, markDef.extent));
    }
    if (inputType === "aggregated-upper-lower") {
      tooltipSummary = [];
      postAggregateCalculates = [
        {
          calculate: accessWithDatumToUnescapedPath(continuousAxisChannelDef2.field),
          as: `upper_${continuousFieldName}`
        },
        { calculate: accessWithDatumToUnescapedPath(continuousFieldName), as: `lower_${continuousFieldName}` }
      ];
    } else if (inputType === "aggregated-error") {
      tooltipSummary = [{ fieldPrefix: "", titlePrefix: continuousFieldName }];
      postAggregateCalculates = [
        {
          calculate: `${accessWithDatumToUnescapedPath(continuousFieldName)} + ${accessWithDatumToUnescapedPath(continuousAxisChannelDefError.field)}`,
          as: `upper_${continuousFieldName}`
        }
      ];
      if (continuousAxisChannelDefError2) {
        postAggregateCalculates.push({
          calculate: `${accessWithDatumToUnescapedPath(continuousFieldName)} + ${accessWithDatumToUnescapedPath(continuousAxisChannelDefError2.field)}`,
          as: `lower_${continuousFieldName}`
        });
      } else {
        postAggregateCalculates.push({
          calculate: `${accessWithDatumToUnescapedPath(continuousFieldName)} - ${accessWithDatumToUnescapedPath(continuousAxisChannelDefError.field)}`,
          as: `lower_${continuousFieldName}`
        });
      }
    }
    for (const postAggregateCalculate of postAggregateCalculates) {
      tooltipSummary.push({
        fieldPrefix: postAggregateCalculate.as.substring(0, 6),
        titlePrefix: replaceAll(replaceAll(postAggregateCalculate.calculate, "datum['", ""), "']", "")
      });
    }
  }
  return { postAggregateCalculates, errorBarSpecificAggregate, tooltipSummary, tooltipTitleWithFieldName };
}
function getTitlePrefix(center, extent2, operation) {
  return `${titleCase(center)} ${operation} ${extent2}`;
}
var ERRORBAND = "errorband";
var ERRORBAND_PARTS = ["band", "borders"];
var errorBandNormalizer = new CompositeMarkNormalizer(ERRORBAND, normalizeErrorBand);
function normalizeErrorBand(spec, { config }) {
  spec = {
    ...spec,
    encoding: normalizeEncoding(spec.encoding, config)
  };
  const { transform: transform4, continuousAxisChannelDef, continuousAxis, encodingWithoutContinuousAxis, markDef, outerSpec, tooltipEncoding } = errorBarParams(spec, ERRORBAND, config);
  const errorBandDef = markDef;
  const makeErrorBandPart = makeCompositeAggregatePartFactory(errorBandDef, continuousAxis, continuousAxisChannelDef, encodingWithoutContinuousAxis, config.errorband);
  const is2D = spec.encoding.x !== void 0 && spec.encoding.y !== void 0;
  let bandMark = { type: is2D ? "area" : "rect" };
  let bordersMark = { type: is2D ? "line" : "rule" };
  const interpolate3 = {
    ...errorBandDef.interpolate ? { interpolate: errorBandDef.interpolate } : {},
    ...errorBandDef.tension && errorBandDef.interpolate ? { tension: errorBandDef.tension } : {}
  };
  if (is2D) {
    bandMark = {
      ...bandMark,
      ...interpolate3,
      ariaRoleDescription: "errorband"
    };
    bordersMark = {
      ...bordersMark,
      ...interpolate3,
      aria: false
    };
  } else if (errorBandDef.interpolate) {
    warn3(errorBand1DNotSupport("interpolate"));
  } else if (errorBandDef.tension) {
    warn3(errorBand1DNotSupport("tension"));
  }
  return {
    ...outerSpec,
    transform: transform4,
    layer: [
      ...makeErrorBandPart({
        partName: "band",
        mark: bandMark,
        positionPrefix: "lower",
        endPositionPrefix: "upper",
        extraEncoding: tooltipEncoding
      }),
      ...makeErrorBandPart({
        partName: "borders",
        mark: bordersMark,
        positionPrefix: "lower",
        extraEncoding: tooltipEncoding
      }),
      ...makeErrorBandPart({
        partName: "borders",
        mark: bordersMark,
        positionPrefix: "upper",
        extraEncoding: tooltipEncoding
      })
    ]
  };
}
var compositeMarkRegistry = {};
function add6(mark, run2, parts) {
  const normalizer = new CompositeMarkNormalizer(mark, run2);
  compositeMarkRegistry[mark] = { normalizer, parts };
}
function getAllCompositeMarks() {
  return keys3(compositeMarkRegistry);
}
add6(BOXPLOT, normalizeBoxPlot, BOXPLOT_PARTS);
add6(ERRORBAR, normalizeErrorBar, ERRORBAR_PARTS);
add6(ERRORBAND, normalizeErrorBand, ERRORBAND_PARTS);
var VL_ONLY_LEGEND_CONFIG = [
  "gradientHorizontalMaxLength",
  "gradientHorizontalMinLength",
  "gradientVerticalMaxLength",
  "gradientVerticalMinLength",
  "unselectedOpacity"
];
var HEADER_TITLE_PROPERTIES_MAP = {
  titleAlign: "align",
  titleAnchor: "anchor",
  titleAngle: "angle",
  titleBaseline: "baseline",
  titleColor: "color",
  titleFont: "font",
  titleFontSize: "fontSize",
  titleFontStyle: "fontStyle",
  titleFontWeight: "fontWeight",
  titleLimit: "limit",
  titleLineHeight: "lineHeight",
  titleOrient: "orient",
  titlePadding: "offset"
};
var HEADER_LABEL_PROPERTIES_MAP = {
  labelAlign: "align",
  labelAnchor: "anchor",
  labelAngle: "angle",
  labelBaseline: "baseline",
  labelColor: "color",
  labelFont: "font",
  labelFontSize: "fontSize",
  labelFontStyle: "fontStyle",
  labelFontWeight: "fontWeight",
  labelLimit: "limit",
  labelLineHeight: "lineHeight",
  labelOrient: "orient",
  labelPadding: "offset"
};
var HEADER_TITLE_PROPERTIES = keys3(HEADER_TITLE_PROPERTIES_MAP);
var HEADER_LABEL_PROPERTIES = keys3(HEADER_LABEL_PROPERTIES_MAP);
var HEADER_CONFIGS_INDEX = {
  header: 1,
  headerRow: 1,
  headerColumn: 1,
  headerFacet: 1
};
var HEADER_CONFIGS = keys3(HEADER_CONFIGS_INDEX);
var LEGEND_SCALE_CHANNELS = [
  "size",
  "shape",
  "fill",
  "stroke",
  "strokeDash",
  "strokeWidth",
  "opacity"
];
var defaultLegendConfig = {
  gradientHorizontalMaxLength: 200,
  gradientHorizontalMinLength: 100,
  gradientVerticalMaxLength: 200,
  gradientVerticalMinLength: 64,
  // This is Vega's minimum.
  unselectedOpacity: 0.35
};
var COMMON_LEGEND_PROPERTY_INDEX = {
  aria: 1,
  clipHeight: 1,
  columnPadding: 1,
  columns: 1,
  cornerRadius: 1,
  description: 1,
  direction: 1,
  fillColor: 1,
  format: 1,
  formatType: 1,
  gradientLength: 1,
  gradientOpacity: 1,
  gradientStrokeColor: 1,
  gradientStrokeWidth: 1,
  gradientThickness: 1,
  gridAlign: 1,
  labelAlign: 1,
  labelBaseline: 1,
  labelColor: 1,
  labelFont: 1,
  labelFontSize: 1,
  labelFontStyle: 1,
  labelFontWeight: 1,
  labelLimit: 1,
  labelOffset: 1,
  labelOpacity: 1,
  labelOverlap: 1,
  labelPadding: 1,
  labelSeparation: 1,
  legendX: 1,
  legendY: 1,
  offset: 1,
  orient: 1,
  padding: 1,
  rowPadding: 1,
  strokeColor: 1,
  symbolDash: 1,
  symbolDashOffset: 1,
  symbolFillColor: 1,
  symbolLimit: 1,
  symbolOffset: 1,
  symbolOpacity: 1,
  symbolSize: 1,
  symbolStrokeColor: 1,
  symbolStrokeWidth: 1,
  symbolType: 1,
  tickCount: 1,
  tickMinStep: 1,
  title: 1,
  titleAlign: 1,
  titleAnchor: 1,
  titleBaseline: 1,
  titleColor: 1,
  titleFont: 1,
  titleFontSize: 1,
  titleFontStyle: 1,
  titleFontWeight: 1,
  titleLimit: 1,
  titleLineHeight: 1,
  titleOpacity: 1,
  titleOrient: 1,
  titlePadding: 1,
  type: 1,
  values: 1,
  zindex: 1
};
var SELECTION_ID = "_vgsid_";
var defaultConfig$1 = {
  point: {
    on: "click",
    fields: [SELECTION_ID],
    toggle: "event.shiftKey",
    resolve: "global",
    clear: "dblclick"
  },
  interval: {
    on: "[pointerdown, window:pointerup] > window:pointermove!",
    encodings: ["x", "y"],
    translate: "[pointerdown, window:pointerup] > window:pointermove!",
    zoom: "wheel!",
    mark: { fill: "#333", fillOpacity: 0.125, stroke: "white" },
    resolve: "global",
    clear: "dblclick"
  }
};
function isLegendBinding(bind4) {
  return bind4 === "legend" || !!bind4?.legend;
}
function isLegendStreamBinding(bind4) {
  return isLegendBinding(bind4) && isObject(bind4);
}
function isSelectionParameter(param2) {
  return !!param2?.["select"];
}
function assembleParameterSignals(params2) {
  const signals = [];
  for (const param2 of params2 || []) {
    if (isSelectionParameter(param2))
      continue;
    const { expr: expr2, bind: bind4, ...rest } = param2;
    if (bind4 && expr2) {
      const signal = {
        ...rest,
        bind: bind4,
        init: expr2
      };
      signals.push(signal);
    } else {
      const signal = {
        ...rest,
        ...expr2 ? { update: expr2 } : {},
        ...bind4 ? { bind: bind4 } : {}
      };
      signals.push(signal);
    }
  }
  return signals;
}
function isAnyConcatSpec(spec) {
  return isVConcatSpec(spec) || isHConcatSpec(spec) || isConcatSpec(spec);
}
function isConcatSpec(spec) {
  return hasProperty(spec, "concat");
}
function isVConcatSpec(spec) {
  return hasProperty(spec, "vconcat");
}
function isHConcatSpec(spec) {
  return hasProperty(spec, "hconcat");
}
function getStepFor({ step, offsetIsDiscrete }) {
  if (offsetIsDiscrete) {
    return step.for ?? "offset";
  } else {
    return "position";
  }
}
function isStep(size) {
  return hasProperty(size, "step");
}
function isFrameMixins(o) {
  return hasProperty(o, "view") || hasProperty(o, "width") || hasProperty(o, "height");
}
var DEFAULT_SPACING = 20;
var COMPOSITION_LAYOUT_INDEX = {
  align: 1,
  bounds: 1,
  center: 1,
  columns: 1,
  spacing: 1
};
var COMPOSITION_LAYOUT_PROPERTIES = keys3(COMPOSITION_LAYOUT_INDEX);
function extractCompositionLayout(spec, specType, config) {
  const compositionConfig = config[specType];
  const layout = {};
  const { spacing: spacingConfig, columns } = compositionConfig;
  if (spacingConfig !== void 0) {
    layout.spacing = spacingConfig;
  }
  if (columns !== void 0) {
    if (isFacetSpec(spec) && !isFacetMapping(spec.facet) || isConcatSpec(spec)) {
      layout.columns = columns;
    }
  }
  if (isVConcatSpec(spec)) {
    layout.columns = 1;
  }
  for (const prop of COMPOSITION_LAYOUT_PROPERTIES) {
    if (spec[prop] !== void 0) {
      if (prop === "spacing") {
        const spacing = spec[prop];
        layout[prop] = isNumber(spacing) ? spacing : {
          row: spacing.row ?? spacingConfig,
          column: spacing.column ?? spacingConfig
        };
      } else {
        layout[prop] = spec[prop];
      }
    }
  }
  return layout;
}
function getViewConfigContinuousSize(viewConfig, channel) {
  return viewConfig[channel] ?? viewConfig[channel === "width" ? "continuousWidth" : "continuousHeight"];
}
function getViewConfigDiscreteStep(viewConfig, channel) {
  const size = getViewConfigDiscreteSize(viewConfig, channel);
  return isStep(size) ? size.step : DEFAULT_STEP;
}
function getViewConfigDiscreteSize(viewConfig, channel) {
  const size = viewConfig[channel] ?? viewConfig[channel === "width" ? "discreteWidth" : "discreteHeight"];
  return getFirstDefined(size, { step: viewConfig.step });
}
var DEFAULT_STEP = 20;
var defaultViewConfig = {
  continuousWidth: 300,
  continuousHeight: 300,
  step: DEFAULT_STEP
};
var defaultConfig = {
  background: "white",
  padding: 5,
  timeFormat: "%b %d, %Y",
  countTitle: "Count of Records",
  view: defaultViewConfig,
  mark: defaultMarkConfig,
  arc: {},
  area: {},
  bar: defaultBarConfig,
  circle: {},
  geoshape: {},
  image: {},
  line: {},
  point: {},
  rect: defaultRectConfig,
  rule: { color: "black" },
  // Need this to override default color in mark config
  square: {},
  text: { color: "black" },
  // Need this to override default color in mark config
  tick: defaultTickConfig,
  trail: {},
  boxplot: {
    size: 14,
    extent: 1.5,
    box: {},
    median: { color: "white" },
    outliers: {},
    rule: {},
    ticks: null
  },
  errorbar: {
    center: "mean",
    rule: true,
    ticks: false
  },
  errorband: {
    band: {
      opacity: 0.3
    },
    borders: false
  },
  scale: defaultScaleConfig,
  projection: {},
  legend: defaultLegendConfig,
  header: { titlePadding: 10, labelPadding: 10 },
  headerColumn: {},
  headerRow: {},
  headerFacet: {},
  selection: defaultConfig$1,
  style: {},
  title: {},
  facet: { spacing: DEFAULT_SPACING },
  concat: { spacing: DEFAULT_SPACING },
  normalizedNumberFormat: ".0%"
};
var tab10 = [
  "#4c78a8",
  "#f58518",
  "#e45756",
  "#72b7b2",
  "#54a24b",
  "#eeca3b",
  "#b279a2",
  "#ff9da6",
  "#9d755d",
  "#bab0ac"
];
var DEFAULT_FONT_SIZE = {
  text: 11,
  guideLabel: 10,
  guideTitle: 11,
  groupTitle: 13,
  groupSubtitle: 12
};
var DEFAULT_COLOR = {
  blue: tab10[0],
  orange: tab10[1],
  red: tab10[2],
  teal: tab10[3],
  green: tab10[4],
  yellow: tab10[5],
  purple: tab10[6],
  pink: tab10[7],
  brown: tab10[8],
  gray0: "#000",
  gray1: "#111",
  gray2: "#222",
  gray3: "#333",
  gray4: "#444",
  gray5: "#555",
  gray6: "#666",
  gray7: "#777",
  gray8: "#888",
  gray9: "#999",
  gray10: "#aaa",
  gray11: "#bbb",
  gray12: "#ccc",
  gray13: "#ddd",
  gray14: "#eee",
  gray15: "#fff"
};
function colorSignalConfig(color6 = {}) {
  return {
    signals: [
      {
        name: "color",
        value: isObject(color6) ? { ...DEFAULT_COLOR, ...color6 } : DEFAULT_COLOR
      }
    ],
    mark: { color: { signal: "color.blue" } },
    rule: { color: { signal: "color.gray0" } },
    text: {
      color: { signal: "color.gray0" }
    },
    style: {
      "guide-label": {
        fill: { signal: "color.gray0" }
      },
      "guide-title": {
        fill: { signal: "color.gray0" }
      },
      "group-title": {
        fill: { signal: "color.gray0" }
      },
      "group-subtitle": {
        fill: { signal: "color.gray0" }
      },
      cell: {
        stroke: { signal: "color.gray8" }
      }
    },
    axis: {
      domainColor: { signal: "color.gray13" },
      gridColor: { signal: "color.gray8" },
      tickColor: { signal: "color.gray13" }
    },
    range: {
      category: [
        { signal: "color.blue" },
        { signal: "color.orange" },
        { signal: "color.red" },
        { signal: "color.teal" },
        { signal: "color.green" },
        { signal: "color.yellow" },
        { signal: "color.purple" },
        { signal: "color.pink" },
        { signal: "color.brown" },
        { signal: "color.grey8" }
      ]
    }
  };
}
function fontSizeSignalConfig(fontSize3) {
  return {
    signals: [
      {
        name: "fontSize",
        value: isObject(fontSize3) ? { ...DEFAULT_FONT_SIZE, ...fontSize3 } : DEFAULT_FONT_SIZE
      }
    ],
    text: {
      fontSize: { signal: "fontSize.text" }
    },
    style: {
      "guide-label": {
        fontSize: { signal: "fontSize.guideLabel" }
      },
      "guide-title": {
        fontSize: { signal: "fontSize.guideTitle" }
      },
      "group-title": {
        fontSize: { signal: "fontSize.groupTitle" }
      },
      "group-subtitle": {
        fontSize: { signal: "fontSize.groupSubtitle" }
      }
    }
  };
}
function fontConfig(font3) {
  return {
    text: { font: font3 },
    style: {
      "guide-label": { font: font3 },
      "guide-title": { font: font3 },
      "group-title": { font: font3 },
      "group-subtitle": { font: font3 }
    }
  };
}
function getAxisConfigInternal(axisConfig2) {
  const props = keys3(axisConfig2 || {});
  const axisConfigInternal = {};
  for (const prop of props) {
    const val = axisConfig2[prop];
    axisConfigInternal[prop] = isConditionalAxisValue(val) ? signalOrValueRefWithCondition(val) : signalRefOrValue(val);
  }
  return axisConfigInternal;
}
function getStyleConfigInternal(styleConfig) {
  const props = keys3(styleConfig);
  const styleConfigInternal = {};
  for (const prop of props) {
    styleConfigInternal[prop] = getAxisConfigInternal(styleConfig[prop]);
  }
  return styleConfigInternal;
}
var configPropsWithExpr = [
  ...MARK_CONFIGS,
  ...AXIS_CONFIGS,
  ...HEADER_CONFIGS,
  "background",
  "padding",
  "legend",
  "lineBreak",
  "scale",
  "style",
  "title",
  "view"
];
function initConfig(specifiedConfig = {}) {
  const { color: color6, font: font3, fontSize: fontSize3, selection, ...restConfig } = specifiedConfig;
  const mergedConfig = mergeConfig({}, duplicate(defaultConfig), font3 ? fontConfig(font3) : {}, color6 ? colorSignalConfig(color6) : {}, fontSize3 ? fontSizeSignalConfig(fontSize3) : {}, restConfig || {});
  if (selection) {
    writeConfig(mergedConfig, "selection", selection, true);
  }
  const outputConfig = omit(mergedConfig, configPropsWithExpr);
  for (const prop of ["background", "lineBreak", "padding"]) {
    if (mergedConfig[prop]) {
      outputConfig[prop] = signalRefOrValue(mergedConfig[prop]);
    }
  }
  for (const markConfigType of MARK_CONFIGS) {
    if (mergedConfig[markConfigType]) {
      outputConfig[markConfigType] = replaceExprRef(mergedConfig[markConfigType]);
    }
  }
  for (const axisConfigType of AXIS_CONFIGS) {
    if (mergedConfig[axisConfigType]) {
      outputConfig[axisConfigType] = getAxisConfigInternal(mergedConfig[axisConfigType]);
    }
  }
  for (const headerConfigType of HEADER_CONFIGS) {
    if (mergedConfig[headerConfigType]) {
      outputConfig[headerConfigType] = replaceExprRef(mergedConfig[headerConfigType]);
    }
  }
  if (mergedConfig.legend) {
    outputConfig.legend = replaceExprRef(mergedConfig.legend);
  }
  if (mergedConfig.scale) {
    const { invalid, ...otherScaleConfig } = mergedConfig.scale;
    const newScaleInvalid = replaceExprRef(invalid, { level: 1 });
    outputConfig.scale = {
      ...replaceExprRef(otherScaleConfig),
      ...keys3(newScaleInvalid).length > 0 ? { invalid: newScaleInvalid } : {}
    };
  }
  if (mergedConfig.style) {
    outputConfig.style = getStyleConfigInternal(mergedConfig.style);
  }
  if (mergedConfig.title) {
    outputConfig.title = replaceExprRef(mergedConfig.title);
  }
  if (mergedConfig.view) {
    outputConfig.view = replaceExprRef(mergedConfig.view);
  }
  return outputConfig;
}
var MARK_STYLES = /* @__PURE__ */ new Set(["view", ...PRIMITIVE_MARKS]);
var VL_ONLY_CONFIG_PROPERTIES = [
  "color",
  "fontSize",
  "background",
  // We apply background to the spec directly.
  "padding",
  "facet",
  "concat",
  "numberFormat",
  "numberFormatType",
  "normalizedNumberFormat",
  "normalizedNumberFormatType",
  "timeFormat",
  "countTitle",
  "header",
  "axisQuantitative",
  "axisTemporal",
  "axisDiscrete",
  "axisPoint",
  "axisXBand",
  "axisXPoint",
  "axisXDiscrete",
  "axisXQuantitative",
  "axisXTemporal",
  "axisYBand",
  "axisYPoint",
  "axisYDiscrete",
  "axisYQuantitative",
  "axisYTemporal",
  "scale",
  "selection",
  "overlay"
];
var VL_ONLY_ALL_MARK_SPECIFIC_CONFIG_PROPERTY_INDEX = {
  view: ["continuousWidth", "continuousHeight", "discreteWidth", "discreteHeight", "step"],
  ...VL_ONLY_MARK_SPECIFIC_CONFIG_PROPERTY_INDEX
};
function stripAndRedirectConfig(config) {
  config = duplicate(config);
  for (const prop of VL_ONLY_CONFIG_PROPERTIES) {
    delete config[prop];
  }
  if (config.axis) {
    for (const prop in config.axis) {
      if (isConditionalAxisValue(config.axis[prop])) {
        delete config.axis[prop];
      }
    }
  }
  if (config.legend) {
    for (const prop of VL_ONLY_LEGEND_CONFIG) {
      delete config.legend[prop];
    }
  }
  if (config.mark) {
    for (const prop of VL_ONLY_MARK_CONFIG_PROPERTIES) {
      delete config.mark[prop];
    }
    if (config.mark.tooltip && isObject(config.mark.tooltip)) {
      delete config.mark.tooltip;
    }
  }
  if (config.params) {
    config.signals = (config.signals || []).concat(assembleParameterSignals(config.params));
    delete config.params;
  }
  for (const markType2 of MARK_STYLES) {
    for (const prop of VL_ONLY_MARK_CONFIG_PROPERTIES) {
      delete config[markType2][prop];
    }
    const vlOnlyMarkSpecificConfigs = VL_ONLY_ALL_MARK_SPECIFIC_CONFIG_PROPERTY_INDEX[markType2];
    if (vlOnlyMarkSpecificConfigs) {
      for (const prop of vlOnlyMarkSpecificConfigs) {
        delete config[markType2][prop];
      }
    }
    redirectConfigToStyleConfig(config, markType2);
  }
  for (const m3 of getAllCompositeMarks()) {
    delete config[m3];
  }
  redirectTitleConfig(config);
  for (const prop in config) {
    if (isObject(config[prop]) && isEmpty(config[prop])) {
      delete config[prop];
    }
  }
  return isEmpty(config) ? void 0 : config;
}
function redirectTitleConfig(config) {
  const { titleMarkConfig, subtitleMarkConfig, subtitle } = extractTitleConfig(config.title);
  if (!isEmpty(titleMarkConfig)) {
    config.style["group-title"] = {
      ...config.style["group-title"],
      ...titleMarkConfig
      // config.title has higher precedence than config.style.group-title in Vega
    };
  }
  if (!isEmpty(subtitleMarkConfig)) {
    config.style["group-subtitle"] = {
      ...config.style["group-subtitle"],
      ...subtitleMarkConfig
    };
  }
  if (!isEmpty(subtitle)) {
    config.title = subtitle;
  } else {
    delete config.title;
  }
}
function redirectConfigToStyleConfig(config, prop, toProp, compositeMarkPart) {
  const propConfig = config[prop];
  if (prop === "view") {
    toProp = "cell";
  }
  const style3 = {
    ...propConfig,
    ...config.style[toProp ?? prop]
  };
  if (!isEmpty(style3)) {
    config.style[toProp ?? prop] = style3;
  }
  {
    delete config[prop];
  }
}
function isLayerSpec(spec) {
  return hasProperty(spec, "layer");
}
function isRepeatSpec(spec) {
  return hasProperty(spec, "repeat");
}
function isLayerRepeatSpec(spec) {
  return !isArray(spec.repeat) && hasProperty(spec.repeat, "layer");
}
var SpecMapper = class {
  map(spec, params2) {
    if (isFacetSpec(spec)) {
      return this.mapFacet(spec, params2);
    } else if (isRepeatSpec(spec)) {
      return this.mapRepeat(spec, params2);
    } else if (isHConcatSpec(spec)) {
      return this.mapHConcat(spec, params2);
    } else if (isVConcatSpec(spec)) {
      return this.mapVConcat(spec, params2);
    } else if (isConcatSpec(spec)) {
      return this.mapConcat(spec, params2);
    } else {
      return this.mapLayerOrUnit(spec, params2);
    }
  }
  mapLayerOrUnit(spec, params2) {
    if (isLayerSpec(spec)) {
      return this.mapLayer(spec, params2);
    } else if (isUnitSpec(spec)) {
      return this.mapUnit(spec, params2);
    }
    throw new Error(invalidSpec(spec));
  }
  mapLayer(spec, params2) {
    return {
      ...spec,
      layer: spec.layer.map((subspec) => this.mapLayerOrUnit(subspec, params2))
    };
  }
  mapHConcat(spec, params2) {
    return {
      ...spec,
      hconcat: spec.hconcat.map((subspec) => this.map(subspec, params2))
    };
  }
  mapVConcat(spec, params2) {
    return {
      ...spec,
      vconcat: spec.vconcat.map((subspec) => this.map(subspec, params2))
    };
  }
  mapConcat(spec, params2) {
    const { concat, ...rest } = spec;
    return {
      ...rest,
      concat: concat.map((subspec) => this.map(subspec, params2))
    };
  }
  mapFacet(spec, params2) {
    return {
      // as any is required here since TS cannot infer that FO may only be FieldName or Field, but not RepeatRef
      ...spec,
      // TODO: remove "any" once we support all facet listed in https://github.com/vega/vega-lite/issues/2760
      spec: this.map(spec.spec, params2)
    };
  }
  mapRepeat(spec, params2) {
    return {
      ...spec,
      // as any is required here since TS cannot infer that the output type satisfies the input type
      spec: this.map(spec.spec, params2)
    };
  }
};
var STACK_OFFSET_INDEX = {
  zero: 1,
  center: 1,
  normalize: 1
};
function isStackOffset(s) {
  return hasOwnProperty(STACK_OFFSET_INDEX, s);
}
var STACKABLE_MARKS = /* @__PURE__ */ new Set([ARC, BAR, AREA, RULE, POINT, CIRCLE, SQUARE, LINE, TEXT, TICK]);
var STACK_BY_DEFAULT_MARKS = /* @__PURE__ */ new Set([BAR, AREA, ARC]);
function isUnbinnedQuantitative(channelDef) {
  return isFieldDef(channelDef) && channelDefType(channelDef) === "quantitative" && !channelDef.bin;
}
function potentialStackedChannel(encoding, x6, { orient: orient2, type: mark }) {
  const y6 = x6 === "x" ? "y" : "radius";
  const isCartesianBarOrArea = x6 === "x" && ["bar", "area"].includes(mark);
  const xDef = encoding[x6];
  const yDef = encoding[y6];
  if (isFieldDef(xDef) && isFieldDef(yDef)) {
    if (isUnbinnedQuantitative(xDef) && isUnbinnedQuantitative(yDef)) {
      if (xDef.stack) {
        return x6;
      } else if (yDef.stack) {
        return y6;
      }
      const xAggregate = isFieldDef(xDef) && !!xDef.aggregate;
      const yAggregate = isFieldDef(yDef) && !!yDef.aggregate;
      if (xAggregate !== yAggregate) {
        return xAggregate ? x6 : y6;
      }
      if (isCartesianBarOrArea) {
        if (orient2 === "vertical") {
          return y6;
        } else if (orient2 === "horizontal") {
          return x6;
        }
      }
    } else if (isUnbinnedQuantitative(xDef)) {
      return x6;
    } else if (isUnbinnedQuantitative(yDef)) {
      return y6;
    }
  } else if (isUnbinnedQuantitative(xDef)) {
    if (isCartesianBarOrArea && orient2 === "vertical") {
      return void 0;
    }
    return x6;
  } else if (isUnbinnedQuantitative(yDef)) {
    if (isCartesianBarOrArea && orient2 === "horizontal") {
      return void 0;
    }
    return y6;
  }
  return void 0;
}
function getDimensionChannel(channel) {
  switch (channel) {
    case "x":
      return "y";
    case "y":
      return "x";
    case "theta":
      return "radius";
    case "radius":
      return "theta";
  }
}
function stack(m3, encoding) {
  const markDef = isMarkDef(m3) ? m3 : { type: m3 };
  const mark = markDef.type;
  if (!STACKABLE_MARKS.has(mark)) {
    return null;
  }
  const fieldChannel = potentialStackedChannel(encoding, "x", markDef) || potentialStackedChannel(encoding, "theta", markDef);
  if (!fieldChannel) {
    return null;
  }
  const stackedFieldDef = encoding[fieldChannel];
  const stackedField = isFieldDef(stackedFieldDef) ? vgField(stackedFieldDef, {}) : void 0;
  const dimensionChannel = getDimensionChannel(fieldChannel);
  const groupbyChannels = [];
  const groupbyFields = /* @__PURE__ */ new Set();
  if (encoding[dimensionChannel]) {
    const dimensionDef = encoding[dimensionChannel];
    const dimensionField = isFieldDef(dimensionDef) ? vgField(dimensionDef, {}) : void 0;
    if (dimensionField && dimensionField !== stackedField) {
      groupbyChannels.push(dimensionChannel);
      groupbyFields.add(dimensionField);
    }
  }
  const dimensionOffsetChannel = dimensionChannel === "x" ? "xOffset" : "yOffset";
  const dimensionOffsetDef = encoding[dimensionOffsetChannel];
  const dimensionOffsetField = isFieldDef(dimensionOffsetDef) ? vgField(dimensionOffsetDef, {}) : void 0;
  if (dimensionOffsetField && dimensionOffsetField !== stackedField) {
    groupbyChannels.push(dimensionOffsetChannel);
    groupbyFields.add(dimensionOffsetField);
  }
  const stackBy = NONPOSITION_CHANNELS.reduce((sc, channel) => {
    if (channel !== "tooltip" && channelHasField(encoding, channel)) {
      const channelDef = encoding[channel];
      for (const cDef of array_default(channelDef)) {
        const fieldDef = getFieldDef(cDef);
        if (fieldDef.aggregate) {
          continue;
        }
        const f = vgField(fieldDef, {});
        if (
          // if fielddef is a repeat, just include it in the stack by
          !f || // otherwise, the field must be different from the groupBy fields.
          !groupbyFields.has(f)
        ) {
          sc.push({ channel, fieldDef });
        }
      }
    }
    return sc;
  }, []);
  let offset6;
  if (stackedFieldDef.stack !== void 0) {
    if (isBoolean(stackedFieldDef.stack)) {
      offset6 = stackedFieldDef.stack ? "zero" : null;
    } else {
      offset6 = stackedFieldDef.stack;
    }
  } else if (STACK_BY_DEFAULT_MARKS.has(mark)) {
    offset6 = "zero";
  }
  if (!offset6 || !isStackOffset(offset6)) {
    return null;
  }
  if (isAggregate$1(encoding) && stackBy.length === 0) {
    return null;
  }
  if (stackedFieldDef?.scale?.type && stackedFieldDef?.scale?.type !== ScaleType.LINEAR) {
    if (stackedFieldDef?.stack) {
      warn3(stackNonLinearScale(stackedFieldDef.scale.type));
    }
  }
  if (isFieldOrDatumDef(encoding[getSecondaryRangeChannel(fieldChannel)])) {
    if (stackedFieldDef.stack !== void 0) {
      warn3(cannotStackRangedMark(fieldChannel));
    }
    return null;
  }
  if (isFieldDef(stackedFieldDef) && stackedFieldDef.aggregate && !SUM_OPS.has(stackedFieldDef.aggregate)) {
    warn3(stackNonSummativeAggregate(stackedFieldDef.aggregate));
  }
  return {
    groupbyChannels,
    groupbyFields,
    fieldChannel,
    impute: stackedFieldDef.impute === null ? false : isPathMark(mark),
    stackBy,
    offset: offset6
  };
}
function initMarkdef(originalMarkDef, encoding, config) {
  const markDef = replaceExprRef(originalMarkDef);
  const specifiedOrient = getMarkPropOrConfig("orient", markDef, config);
  markDef.orient = orient(markDef.type, encoding, specifiedOrient);
  if (specifiedOrient !== void 0 && specifiedOrient !== markDef.orient) {
    warn3(orientOverridden(markDef.orient, specifiedOrient));
  }
  if (markDef.type === "bar" && markDef.orient) {
    const cornerRadiusEnd = getMarkPropOrConfig("cornerRadiusEnd", markDef, config);
    if (cornerRadiusEnd !== void 0) {
      const newProps = markDef.orient === "horizontal" && encoding.x2 || markDef.orient === "vertical" && encoding.y2 ? ["cornerRadius"] : BAR_CORNER_RADIUS_INDEX[markDef.orient];
      for (const newProp of newProps) {
        markDef[newProp] = cornerRadiusEnd;
      }
      if (markDef.cornerRadiusEnd !== void 0) {
        delete markDef.cornerRadiusEnd;
      }
    }
  }
  const specifiedOpacity = getMarkPropOrConfig("opacity", markDef, config);
  const specifiedFillOpacity = getMarkPropOrConfig("fillOpacity", markDef, config);
  if (specifiedOpacity === void 0 && specifiedFillOpacity === void 0) {
    markDef.opacity = opacity(markDef.type, encoding);
  }
  const specifiedCursor = getMarkPropOrConfig("cursor", markDef, config);
  if (specifiedCursor === void 0) {
    markDef.cursor = cursor2(markDef, encoding, config);
  }
  return markDef;
}
function cursor2(markDef, encoding, config) {
  if (encoding.href || markDef.href || getMarkPropOrConfig("href", markDef, config)) {
    return "pointer";
  }
  return markDef.cursor;
}
var DEFAULT_REDUCED_OPACITY = 0.7;
function opacity(mark, encoding) {
  if (contains2([POINT, TICK, CIRCLE, SQUARE], mark) && !isAggregate$1(encoding)) {
    return DEFAULT_REDUCED_OPACITY;
  }
  return void 0;
}
function defaultFilled(markDef, config, { graticule: graticule2 }) {
  if (graticule2) {
    return false;
  }
  const filledConfig = getMarkConfig("filled", markDef, config);
  const mark = markDef.type;
  return getFirstDefined(filledConfig, mark !== POINT && mark !== LINE && mark !== RULE);
}
function orient(mark, encoding, specifiedOrient) {
  switch (mark) {
    case POINT:
    case CIRCLE:
    case SQUARE:
    case RECT:
    case IMAGE:
      return void 0;
  }
  const { x: x6, y: y6, x2: x22, y2: y22 } = encoding;
  switch (mark) {
    case TEXT:
    case BAR:
      if (isFieldDef(x6) && (isBinned(x6.bin) || isFieldDef(y6) && y6.aggregate && !x6.aggregate)) {
        return "vertical";
      }
      if (isFieldDef(y6) && (isBinned(y6.bin) || isFieldDef(x6) && x6.aggregate && !y6.aggregate)) {
        return "horizontal";
      }
      if (y22 || x22) {
        if (specifiedOrient) {
          return specifiedOrient;
        }
        if (!x22) {
          if (isFieldDef(x6) && x6.type === QUANTITATIVE && !isBinning(x6.bin) || isNumericDataDef(x6)) {
            if (isFieldDef(y6) && isBinned(y6.bin)) {
              return "horizontal";
            }
          }
          return "vertical";
        }
        if (!y22) {
          if (isFieldDef(y6) && y6.type === QUANTITATIVE && !isBinning(y6.bin) || isNumericDataDef(y6)) {
            if (isFieldDef(x6) && isBinned(x6.bin)) {
              return "vertical";
            }
          }
          return "horizontal";
        }
      }
    // falls through
    case RULE:
      if (x22 && !(isFieldDef(x6) && isBinned(x6.bin)) && y22 && !(isFieldDef(y6) && isBinned(y6.bin))) {
        return void 0;
      }
    // falls through
    case AREA:
      if (y22) {
        if (isFieldDef(y6) && isBinned(y6.bin)) {
          return "horizontal";
        } else {
          return "vertical";
        }
      } else if (x22) {
        if (isFieldDef(x6) && isBinned(x6.bin)) {
          return "vertical";
        } else {
          return "horizontal";
        }
      } else if (mark === RULE) {
        if (x6 && !y6) {
          return "vertical";
        } else if (y6 && !x6) {
          return "horizontal";
        }
      }
    // falls through
    case LINE:
    case TICK: {
      const xIsMeasure = isUnbinnedQuantitativeFieldOrDatumDef(x6);
      const yIsMeasure = isUnbinnedQuantitativeFieldOrDatumDef(y6);
      if (specifiedOrient) {
        return specifiedOrient;
      } else if (xIsMeasure && !yIsMeasure) {
        return mark !== "tick" ? "horizontal" : "vertical";
      } else if (!xIsMeasure && yIsMeasure) {
        return mark !== "tick" ? "vertical" : "horizontal";
      } else if (xIsMeasure && yIsMeasure) {
        return "vertical";
      } else {
        const xIsTemporal = isTypedFieldDef(x6) && x6.type === TEMPORAL;
        const yIsTemporal = isTypedFieldDef(y6) && y6.type === TEMPORAL;
        if (xIsTemporal && !yIsTemporal) {
          return "vertical";
        } else if (!xIsTemporal && yIsTemporal) {
          return "horizontal";
        }
      }
      return void 0;
    }
  }
  return "vertical";
}
function dropLineAndPoint(markDef) {
  const { point: _point, line: _line, ...mark } = markDef;
  return keys3(mark).length > 1 ? mark : mark.type;
}
function dropLineAndPointFromConfig(config) {
  for (const mark of ["line", "area", "rule", "trail"]) {
    if (config[mark]) {
      config = {
        ...config,
        // TODO: remove as any
        [mark]: omit(config[mark], ["point", "line"])
      };
    }
  }
  return config;
}
function getPointOverlay(markDef, markConfig = {}, encoding) {
  if (markDef.point === "transparent") {
    return { opacity: 0 };
  } else if (markDef.point) {
    return isObject(markDef.point) ? markDef.point : {};
  } else if (markDef.point !== void 0) {
    return null;
  } else {
    if (markConfig.point || encoding.shape) {
      return isObject(markConfig.point) ? markConfig.point : {};
    }
    return void 0;
  }
}
function getLineOverlay(markDef, markConfig = {}) {
  if (markDef.line) {
    return markDef.line === true ? {} : markDef.line;
  } else if (markDef.line !== void 0) {
    return null;
  } else {
    if (markConfig.line) {
      return markConfig.line === true ? {} : markConfig.line;
    }
    return void 0;
  }
}
var PathOverlayNormalizer = class {
  name = "path-overlay";
  hasMatchingType(spec, config) {
    if (isUnitSpec(spec)) {
      const { mark, encoding } = spec;
      const markDef = isMarkDef(mark) ? mark : { type: mark };
      switch (markDef.type) {
        case "line":
        case "rule":
        case "trail":
          return !!getPointOverlay(markDef, config[markDef.type], encoding);
        case "area":
          return (
            // false / null are also included as we want to remove the properties
            !!getPointOverlay(markDef, config[markDef.type], encoding) || !!getLineOverlay(markDef, config[markDef.type])
          );
      }
    }
    return false;
  }
  run(spec, normParams, normalize4) {
    const { config } = normParams;
    const { params: params2, projection: projection3, mark, name, encoding: e, ...outerSpec } = spec;
    const encoding = normalizeEncoding(e, config);
    const markDef = isMarkDef(mark) ? mark : { type: mark };
    const pointOverlay = getPointOverlay(markDef, config[markDef.type], encoding);
    const lineOverlay = markDef.type === "area" && getLineOverlay(markDef, config[markDef.type]);
    const layer = [
      {
        name,
        ...params2 ? { params: params2 } : {},
        mark: dropLineAndPoint({
          ...markDef.type === "area" && getMarkPropOrConfig("opacity", markDef, config) == void 0 && getMarkPropOrConfig("fillOpacity", markDef, config) == void 0 ? { opacity: DEFAULT_REDUCED_OPACITY } : {},
          ...markDef
        }),
        // drop shape from encoding as this might be used to trigger point overlay
        encoding: omit(encoding, ["shape"])
      }
    ];
    const stackProps = stack(initMarkdef(markDef, encoding, config), encoding);
    let overlayEncoding = encoding;
    if (stackProps) {
      const { fieldChannel: stackFieldChannel, offset: offset6 } = stackProps;
      overlayEncoding = {
        ...encoding,
        [stackFieldChannel]: {
          ...encoding[stackFieldChannel],
          ...offset6 ? { stack: offset6 } : {}
        }
      };
    }
    overlayEncoding = omit(overlayEncoding, ["y2", "x2"]);
    if (lineOverlay) {
      layer.push({
        ...projection3 ? { projection: projection3 } : {},
        mark: {
          type: "line",
          ...pick3(markDef, ["clip", "interpolate", "tension", "tooltip"]),
          ...lineOverlay
        },
        encoding: overlayEncoding
      });
    }
    if (pointOverlay) {
      layer.push({
        ...projection3 ? { projection: projection3 } : {},
        mark: {
          type: "point",
          opacity: 1,
          filled: true,
          ...pick3(markDef, ["clip", "tooltip"]),
          ...pointOverlay
        },
        encoding: overlayEncoding
      });
    }
    return normalize4({
      ...outerSpec,
      layer
    }, {
      ...normParams,
      config: dropLineAndPointFromConfig(config)
    });
  }
};
function replaceRepeaterInFacet(facet, repeater) {
  if (!repeater) {
    return facet;
  }
  if (isFacetMapping(facet)) {
    return replaceRepeaterInMapping(facet, repeater);
  }
  return replaceRepeaterInFieldDef(facet, repeater);
}
function replaceRepeaterInEncoding(encoding, repeater) {
  if (!repeater) {
    return encoding;
  }
  return replaceRepeaterInMapping(encoding, repeater);
}
function replaceRepeatInProp(prop, o, repeater) {
  const val = o[prop];
  if (isRepeatRef(val)) {
    if (val.repeat in repeater) {
      return { ...o, [prop]: repeater[val.repeat] };
    } else {
      warn3(noSuchRepeatedValue(val.repeat));
      return void 0;
    }
  }
  return o;
}
function replaceRepeaterInFieldDef(fieldDef, repeater) {
  fieldDef = replaceRepeatInProp("field", fieldDef, repeater);
  if (fieldDef === void 0) {
    return void 0;
  } else if (fieldDef === null) {
    return null;
  }
  if (isSortableFieldDef(fieldDef) && isSortField(fieldDef.sort)) {
    const sort4 = replaceRepeatInProp("field", fieldDef.sort, repeater);
    fieldDef = {
      ...fieldDef,
      ...sort4 ? { sort: sort4 } : {}
    };
  }
  return fieldDef;
}
function replaceRepeaterInFieldOrDatumDef(def3, repeater) {
  if (isFieldDef(def3)) {
    return replaceRepeaterInFieldDef(def3, repeater);
  } else {
    const datumDef = replaceRepeatInProp("datum", def3, repeater);
    if (datumDef !== def3 && !datumDef.type) {
      datumDef.type = "nominal";
    }
    return datumDef;
  }
}
function replaceRepeaterInChannelDef(channelDef, repeater) {
  if (isFieldOrDatumDef(channelDef)) {
    const fd = replaceRepeaterInFieldOrDatumDef(channelDef, repeater);
    if (fd) {
      return fd;
    } else if (isConditionalDef(channelDef)) {
      return { condition: channelDef.condition };
    }
  } else {
    if (hasConditionalFieldOrDatumDef(channelDef)) {
      const fd = replaceRepeaterInFieldOrDatumDef(channelDef.condition, repeater);
      if (fd) {
        return {
          ...channelDef,
          condition: fd
        };
      } else {
        const { condition, ...channelDefWithoutCondition } = channelDef;
        return channelDefWithoutCondition;
      }
    }
    return channelDef;
  }
  return void 0;
}
function replaceRepeaterInMapping(mapping, repeater) {
  const out = {};
  for (const channel in mapping) {
    if (hasProperty(mapping, channel)) {
      const channelDef = mapping[channel];
      if (isArray(channelDef)) {
        out[channel] = channelDef.map((cd2) => replaceRepeaterInChannelDef(cd2, repeater)).filter((cd2) => cd2);
      } else {
        const cd2 = replaceRepeaterInChannelDef(channelDef, repeater);
        if (cd2 !== void 0) {
          out[channel] = cd2;
        }
      }
    }
  }
  return out;
}
var RuleForRangedLineNormalizer = class {
  name = "RuleForRangedLine";
  hasMatchingType(spec) {
    if (isUnitSpec(spec)) {
      const { encoding, mark } = spec;
      if (mark === "line" || isMarkDef(mark) && mark.type === "line") {
        for (const channel of SECONDARY_RANGE_CHANNEL) {
          const mainChannel = getMainRangeChannel(channel);
          const mainChannelDef = encoding[mainChannel];
          if (encoding[channel]) {
            if (isFieldDef(mainChannelDef) && !isBinned(mainChannelDef.bin) || isDatumDef(mainChannelDef)) {
              return true;
            }
          }
        }
      }
    }
    return false;
  }
  run(spec, params2, normalize4) {
    const { encoding, mark } = spec;
    warn3(lineWithRange(!!encoding.x2, !!encoding.y2));
    return normalize4({
      ...spec,
      mark: isObject(mark) ? { ...mark, type: "rule" } : "rule"
    }, params2);
  }
};
var CoreNormalizer = class extends SpecMapper {
  nonFacetUnitNormalizers = [
    boxPlotNormalizer,
    errorBarNormalizer,
    errorBandNormalizer,
    new PathOverlayNormalizer(),
    new RuleForRangedLineNormalizer()
  ];
  map(spec, params2) {
    if (isUnitSpec(spec)) {
      const hasRow = channelHasField(spec.encoding, ROW);
      const hasColumn = channelHasField(spec.encoding, COLUMN);
      const hasFacet = channelHasField(spec.encoding, FACET);
      if (hasRow || hasColumn || hasFacet) {
        return this.mapFacetedUnit(spec, params2);
      }
    }
    return super.map(spec, params2);
  }
  // This is for normalizing non-facet unit
  mapUnit(spec, params2) {
    const { parentEncoding, parentProjection } = params2;
    const encoding = replaceRepeaterInEncoding(spec.encoding, params2.repeater);
    const specWithReplacedEncoding = {
      ...spec,
      ...spec.name ? { name: [params2.repeaterPrefix, spec.name].filter((n) => n).join("_") } : {},
      ...encoding ? { encoding } : {}
    };
    if (parentEncoding || parentProjection) {
      return this.mapUnitWithParentEncodingOrProjection(specWithReplacedEncoding, params2);
    }
    const normalizeLayerOrUnit = this.mapLayerOrUnit.bind(this);
    for (const unitNormalizer of this.nonFacetUnitNormalizers) {
      if (unitNormalizer.hasMatchingType(specWithReplacedEncoding, params2.config)) {
        return unitNormalizer.run(specWithReplacedEncoding, params2, normalizeLayerOrUnit);
      }
    }
    return specWithReplacedEncoding;
  }
  mapRepeat(spec, params2) {
    if (isLayerRepeatSpec(spec)) {
      return this.mapLayerRepeat(spec, params2);
    } else {
      return this.mapNonLayerRepeat(spec, params2);
    }
  }
  mapLayerRepeat(spec, params2) {
    const { repeat: repeat2, spec: childSpec, ...rest } = spec;
    const { row, column, layer } = repeat2;
    const { repeater = {}, repeaterPrefix = "" } = params2;
    if (row || column) {
      return this.mapRepeat({
        ...spec,
        repeat: {
          ...row ? { row } : {},
          ...column ? { column } : {}
        },
        spec: {
          repeat: { layer },
          spec: childSpec
        }
      }, params2);
    } else {
      return {
        ...rest,
        layer: layer.map((layerValue) => {
          const childRepeater = {
            ...repeater,
            layer: layerValue
          };
          const childName = `${(childSpec.name ? `${childSpec.name}_` : "") + repeaterPrefix}child__layer_${varName(layerValue)}`;
          const child = this.mapLayerOrUnit(childSpec, { ...params2, repeater: childRepeater, repeaterPrefix: childName });
          child.name = childName;
          return child;
        })
      };
    }
  }
  mapNonLayerRepeat(spec, params2) {
    const { repeat: repeat2, spec: childSpec, data: data4, ...remainingProperties } = spec;
    if (!isArray(repeat2) && spec.columns) {
      spec = omit(spec, ["columns"]);
      warn3(columnsNotSupportByRowCol("repeat"));
    }
    const concat = [];
    const { repeater = {}, repeaterPrefix = "" } = params2;
    const row = !isArray(repeat2) && repeat2.row || [repeater ? repeater.row : null];
    const column = !isArray(repeat2) && repeat2.column || [repeater ? repeater.column : null];
    const repeatValues = isArray(repeat2) && repeat2 || [repeater ? repeater.repeat : null];
    for (const repeatValue of repeatValues) {
      for (const rowValue of row) {
        for (const columnValue of column) {
          const childRepeater = {
            repeat: repeatValue,
            row: rowValue,
            column: columnValue,
            layer: repeater.layer
          };
          const childName = `${(childSpec.name ? `${childSpec.name}_` : "") + repeaterPrefix}child__${isArray(repeat2) ? `${varName(repeatValue)}` : (repeat2.row ? `row_${varName(rowValue)}` : "") + (repeat2.column ? `column_${varName(columnValue)}` : "")}`;
          const child = this.map(childSpec, { ...params2, repeater: childRepeater, repeaterPrefix: childName });
          child.name = childName;
          concat.push(omit(child, ["data"]));
        }
      }
    }
    const columns = isArray(repeat2) ? spec.columns : repeat2.column ? repeat2.column.length : 1;
    return {
      data: childSpec.data ?? data4,
      // data from child spec should have precedence
      align: "all",
      ...remainingProperties,
      columns,
      concat
    };
  }
  mapFacet(spec, params2) {
    const { facet } = spec;
    if (isFacetMapping(facet) && spec.columns) {
      spec = omit(spec, ["columns"]);
      warn3(columnsNotSupportByRowCol("facet"));
    }
    return super.mapFacet(spec, params2);
  }
  mapUnitWithParentEncodingOrProjection(spec, params2) {
    const { encoding, projection: projection3 } = spec;
    const { parentEncoding, parentProjection, config } = params2;
    const mergedProjection = mergeProjection({ parentProjection, projection: projection3 });
    const mergedEncoding = mergeEncoding({
      parentEncoding,
      encoding: replaceRepeaterInEncoding(encoding, params2.repeater)
    });
    return this.mapUnit({
      ...spec,
      ...mergedProjection ? { projection: mergedProjection } : {},
      ...mergedEncoding ? { encoding: mergedEncoding } : {}
    }, { config });
  }
  mapFacetedUnit(spec, normParams) {
    const { row, column, facet, ...encoding } = spec.encoding;
    const { mark, width: width2, projection: projection3, height: height2, view, params: params2, encoding: _, ...outerSpec } = spec;
    const { facetMapping, layout } = this.getFacetMappingAndLayout({ row, column, facet }, normParams);
    const newEncoding = replaceRepeaterInEncoding(encoding, normParams.repeater);
    return this.mapFacet({
      ...outerSpec,
      ...layout,
      // row / column has higher precedence than facet
      facet: facetMapping,
      spec: {
        ...width2 ? { width: width2 } : {},
        ...height2 ? { height: height2 } : {},
        ...view ? { view } : {},
        ...projection3 ? { projection: projection3 } : {},
        mark,
        encoding: newEncoding,
        ...params2 ? { params: params2 } : {}
      }
    }, normParams);
  }
  getFacetMappingAndLayout(facets, params2) {
    const { row, column, facet } = facets;
    if (row || column) {
      if (facet) {
        warn3(facetChannelDropped([...row ? [ROW] : [], ...column ? [COLUMN] : []]));
      }
      const facetMapping = {};
      const layout = {};
      for (const channel of [ROW, COLUMN]) {
        const def3 = facets[channel];
        if (def3) {
          const { align: align2, center, spacing, columns, ...defWithoutLayout } = def3;
          facetMapping[channel] = defWithoutLayout;
          for (const prop of ["align", "center", "spacing"]) {
            if (def3[prop] !== void 0) {
              layout[prop] ??= {};
              layout[prop][channel] = def3[prop];
            }
          }
        }
      }
      return { facetMapping, layout };
    } else {
      const { align: align2, center, spacing, columns, ...facetMapping } = facet;
      return {
        facetMapping: replaceRepeaterInFacet(facetMapping, params2.repeater),
        layout: {
          ...align2 ? { align: align2 } : {},
          ...center ? { center } : {},
          ...spacing ? { spacing } : {},
          ...columns ? { columns } : {}
        }
      };
    }
  }
  mapLayer(spec, { parentEncoding, parentProjection, ...otherParams }) {
    const { encoding, projection: projection3, ...rest } = spec;
    const params2 = {
      ...otherParams,
      parentEncoding: mergeEncoding({ parentEncoding, encoding, layer: true }),
      parentProjection: mergeProjection({ parentProjection, projection: projection3 })
    };
    return super.mapLayer({
      ...rest,
      ...spec.name ? { name: [params2.repeaterPrefix, spec.name].filter((n) => n).join("_") } : {}
    }, params2);
  }
};
function mergeEncoding({ parentEncoding, encoding = {}, layer }) {
  let merged = {};
  if (parentEncoding) {
    const channels = /* @__PURE__ */ new Set([...keys3(parentEncoding), ...keys3(encoding)]);
    for (const channel of channels) {
      const channelDef = encoding[channel];
      const parentChannelDef = parentEncoding[channel];
      if (isFieldOrDatumDef(channelDef)) {
        const mergedChannelDef = {
          ...parentChannelDef,
          ...channelDef
        };
        merged[channel] = mergedChannelDef;
      } else if (hasConditionalFieldOrDatumDef(channelDef)) {
        merged[channel] = {
          ...channelDef,
          condition: {
            ...parentChannelDef,
            ...channelDef.condition
          }
        };
      } else if (channelDef || channelDef === null) {
        merged[channel] = channelDef;
      } else if (layer || isValueDef(parentChannelDef) || isSignalRef(parentChannelDef) || isFieldOrDatumDef(parentChannelDef) || isArray(parentChannelDef)) {
        merged[channel] = parentChannelDef;
      }
    }
  } else {
    merged = encoding;
  }
  return !merged || isEmpty(merged) ? void 0 : merged;
}
function mergeProjection(opt) {
  const { parentProjection, projection: projection3 } = opt;
  if (parentProjection && projection3) {
    warn3(projectionOverridden({ parentProjection, projection: projection3 }));
  }
  return projection3 ?? parentProjection;
}
function isFilter(t) {
  return hasProperty(t, "filter");
}
function isImputeSequence(t) {
  return hasProperty(t, "stop");
}
function isLookup(t) {
  return hasProperty(t, "lookup");
}
function isLookupData(from) {
  return hasProperty(from, "data");
}
function isLookupSelection(from) {
  return hasProperty(from, "param");
}
function isPivot(t) {
  return hasProperty(t, "pivot");
}
function isDensity(t) {
  return hasProperty(t, "density");
}
function isQuantile2(t) {
  return hasProperty(t, "quantile");
}
function isRegression(t) {
  return hasProperty(t, "regression");
}
function isLoess(t) {
  return hasProperty(t, "loess");
}
function isSample(t) {
  return hasProperty(t, "sample");
}
function isWindow(t) {
  return hasProperty(t, "window");
}
function isJoinAggregate(t) {
  return hasProperty(t, "joinaggregate");
}
function isFlatten(t) {
  return hasProperty(t, "flatten");
}
function isCalculate(t) {
  return hasProperty(t, "calculate");
}
function isBin(t) {
  return hasProperty(t, "bin");
}
function isImpute(t) {
  return hasProperty(t, "impute");
}
function isTimeUnit(t) {
  return hasProperty(t, "timeUnit");
}
function isAggregate(t) {
  return hasProperty(t, "aggregate");
}
function isStack(t) {
  return hasProperty(t, "stack");
}
function isFold(t) {
  return hasProperty(t, "fold");
}
function isExtent(t) {
  return hasProperty(t, "extent") && !hasProperty(t, "density") && !hasProperty(t, "regression");
}
function normalizeTransform(transform4) {
  return transform4.map((t) => {
    if (isFilter(t)) {
      return {
        filter: normalizeLogicalComposition(t.filter, normalizePredicate$1)
      };
    }
    return t;
  });
}
var SelectionCompatibilityNormalizer = class extends SpecMapper {
  map(spec, normParams) {
    normParams.emptySelections ??= {};
    normParams.selectionPredicates ??= {};
    spec = normalizeTransforms(spec, normParams);
    return super.map(spec, normParams);
  }
  mapLayerOrUnit(spec, normParams) {
    spec = normalizeTransforms(spec, normParams);
    if (spec.encoding) {
      const encoding = {};
      for (const [channel, enc] of entries$1(spec.encoding)) {
        encoding[channel] = normalizeChannelDef(enc, normParams);
      }
      spec = { ...spec, encoding };
    }
    return super.mapLayerOrUnit(spec, normParams);
  }
  mapUnit(spec, normParams) {
    const { selection, ...rest } = spec;
    if (selection) {
      return {
        ...rest,
        params: entries$1(selection).map(([name, selDef]) => {
          const { init: value4, bind: bind4, empty, ...select2 } = selDef;
          if (select2.type === "single") {
            select2.type = "point";
            select2.toggle = false;
          } else if (select2.type === "multi") {
            select2.type = "point";
          }
          normParams.emptySelections[name] = empty !== "none";
          for (const pred of vals(normParams.selectionPredicates[name] ?? {})) {
            pred.empty = empty !== "none";
          }
          return { name, value: value4, select: select2, bind: bind4 };
        })
      };
    }
    return spec;
  }
};
function normalizeTransforms(spec, normParams) {
  const { transform: tx, ...rest } = spec;
  if (tx) {
    const transform4 = tx.map((t) => {
      if (isFilter(t)) {
        return { filter: normalizePredicate(t, normParams) };
      } else if (isBin(t) && isBinParams(t.bin)) {
        return {
          ...t,
          bin: normalizeBinExtent(t.bin)
        };
      } else if (isLookup(t)) {
        const { selection: param2, ...from } = t.from;
        return param2 ? {
          ...t,
          from: { param: param2, ...from }
        } : t;
      }
      return t;
    });
    return { ...rest, transform: transform4 };
  }
  return spec;
}
function normalizeChannelDef(obj, normParams) {
  const enc = duplicate(obj);
  if (isFieldDef(enc) && isBinParams(enc.bin)) {
    enc.bin = normalizeBinExtent(enc.bin);
  }
  if (isScaleFieldDef(enc) && enc.scale?.domain?.selection) {
    const { selection: param2, ...domain4 } = enc.scale.domain;
    enc.scale.domain = { ...domain4, ...param2 ? { param: param2 } : {} };
  }
  if (isConditionalDef(enc)) {
    if (isArray(enc.condition)) {
      enc.condition = enc.condition.map((c3) => {
        const { selection, param: param2, test: test2, ...cond } = c3;
        return param2 ? c3 : { ...cond, test: normalizePredicate(c3, normParams) };
      });
    } else {
      const { selection, param: param2, test: test2, ...cond } = normalizeChannelDef(enc.condition, normParams);
      enc.condition = param2 ? enc.condition : {
        ...cond,
        test: normalizePredicate(enc.condition, normParams)
      };
    }
  }
  return enc;
}
function normalizeBinExtent(bin3) {
  const ext = bin3.extent;
  if (ext?.selection) {
    const { selection: param2, ...rest } = ext;
    return { ...bin3, extent: { ...rest, param: param2 } };
  }
  return bin3;
}
function normalizePredicate(op, normParams) {
  const normalizeSelectionComposition = (o) => {
    return normalizeLogicalComposition(o, (param2) => {
      const empty = normParams.emptySelections[param2] ?? true;
      const pred = { param: param2, empty };
      normParams.selectionPredicates[param2] ??= [];
      normParams.selectionPredicates[param2].push(pred);
      return pred;
    });
  };
  return op.selection ? normalizeSelectionComposition(op.selection) : normalizeLogicalComposition(op.test || op.filter, (o) => o.selection ? normalizeSelectionComposition(o.selection) : o);
}
var TopLevelSelectionsNormalizer = class extends SpecMapper {
  map(spec, normParams) {
    const selections = normParams.selections ?? [];
    if (spec.params && !isUnitSpec(spec)) {
      const params2 = [];
      for (const param2 of spec.params) {
        if (isSelectionParameter(param2)) {
          selections.push(param2);
        } else {
          params2.push(param2);
        }
      }
      spec.params = params2;
    }
    normParams.selections = selections;
    return super.map(spec, normParams);
  }
  mapUnit(spec, normParams) {
    const selections = normParams.selections;
    if (!selections || !selections.length)
      return spec;
    const path4 = (normParams.path ?? []).concat(spec.name);
    const params2 = [];
    for (const selection of selections) {
      if (!selection.views || !selection.views.length) {
        params2.push(selection);
      } else {
        for (const view of selection.views) {
          if (isString(view) && (view === spec.name || path4.includes(view)) || isArray(view) && // logic for backwards compatibility with view paths before we had unique names
          view.map((v2) => path4.indexOf(v2)).every((v2, i, arr) => v2 !== -1 && (i === 0 || v2 > arr[i - 1]))) {
            params2.push(selection);
          }
        }
      }
    }
    if (params2.length)
      spec.params = params2;
    return spec;
  }
};
for (const method2 of ["mapFacet", "mapRepeat", "mapHConcat", "mapVConcat", "mapLayer"]) {
  const proto = TopLevelSelectionsNormalizer.prototype[method2];
  TopLevelSelectionsNormalizer.prototype[method2] = function(spec, params2) {
    return proto.call(this, spec, addSpecNameToParams(spec, params2));
  };
}
function addSpecNameToParams(spec, params2) {
  return spec.name ? {
    ...params2,
    path: (params2.path ?? []).concat(spec.name)
  } : params2;
}
function normalize3(spec, config) {
  if (config === void 0) {
    config = initConfig(spec.config);
  }
  const normalizedSpec = normalizeGenericSpec(spec, config);
  const { width: width2, height: height2 } = spec;
  const autosize = normalizeAutoSize(normalizedSpec, { width: width2, height: height2, autosize: spec.autosize }, config);
  return {
    ...normalizedSpec,
    ...autosize ? { autosize } : {}
  };
}
var coreNormalizer = new CoreNormalizer();
var selectionCompatNormalizer = new SelectionCompatibilityNormalizer();
var topLevelSelectionNormalizer = new TopLevelSelectionsNormalizer();
function normalizeGenericSpec(spec, config = {}) {
  const normParams = { config };
  return topLevelSelectionNormalizer.map(coreNormalizer.map(selectionCompatNormalizer.map(spec, normParams), normParams), normParams);
}
function _normalizeAutoSize(autosize) {
  return isString(autosize) ? { type: autosize } : autosize ?? {};
}
function normalizeAutoSize(spec, sizeInfo, config) {
  let { width: width2, height: height2 } = sizeInfo;
  const isFitCompatible = isUnitSpec(spec) || isLayerSpec(spec);
  const autosizeDefault = {};
  if (!isFitCompatible) {
    if (width2 == "container") {
      warn3(containerSizeNonSingle("width"));
      width2 = void 0;
    }
    if (height2 == "container") {
      warn3(containerSizeNonSingle("height"));
      height2 = void 0;
    }
  } else {
    if (width2 == "container" && height2 == "container") {
      autosizeDefault.type = "fit";
      autosizeDefault.contains = "padding";
    } else if (width2 == "container") {
      autosizeDefault.type = "fit-x";
      autosizeDefault.contains = "padding";
    } else if (height2 == "container") {
      autosizeDefault.type = "fit-y";
      autosizeDefault.contains = "padding";
    }
  }
  const autosize = {
    type: "pad",
    ...autosizeDefault,
    ...config ? _normalizeAutoSize(config.autosize) : {},
    ..._normalizeAutoSize(spec.autosize)
  };
  if (autosize.type === "fit" && !isFitCompatible) {
    warn3(FIT_NON_SINGLE);
    autosize.type = "pad";
  }
  if (width2 == "container" && !(autosize.type == "fit" || autosize.type == "fit-x")) {
    warn3(containerSizeNotCompatibleWithAutosize("width"));
  }
  if (height2 == "container" && !(autosize.type == "fit" || autosize.type == "fit-y")) {
    warn3(containerSizeNotCompatibleWithAutosize("height"));
  }
  if (deepEqual(autosize, { type: "pad" })) {
    return void 0;
  }
  return autosize;
}
function isFitType(autoSizeType) {
  return ["fit", "fit-x", "fit-y"].includes(autoSizeType);
}
function getFitType(sizeType) {
  return sizeType ? `fit-${getPositionScaleChannel(sizeType)}` : "fit";
}
var TOP_LEVEL_PROPERTIES = [
  "background",
  "padding"
  // We do not include "autosize" here as it is supported by only unit and layer specs and thus need to be normalized
];
function extractTopLevelProperties(t, includeParams) {
  const o = {};
  for (const p of TOP_LEVEL_PROPERTIES) {
    if (t && t[p] !== void 0) {
      o[p] = signalRefOrValue(t[p]);
    }
  }
  if (includeParams) {
    o.params = t.params;
  }
  return o;
}
var Split = class _Split {
  explicit;
  implicit;
  constructor(explicit = {}, implicit2 = {}) {
    this.explicit = explicit;
    this.implicit = implicit2;
  }
  clone() {
    return new _Split(duplicate(this.explicit), duplicate(this.implicit));
  }
  combine() {
    return {
      ...this.explicit,
      // Explicit properties comes first
      ...this.implicit
    };
  }
  get(key2) {
    return getFirstDefined(this.explicit[key2], this.implicit[key2]);
  }
  getWithExplicit(key2) {
    if (this.explicit[key2] !== void 0) {
      return { explicit: true, value: this.explicit[key2] };
    } else if (this.implicit[key2] !== void 0) {
      return { explicit: false, value: this.implicit[key2] };
    }
    return { explicit: false, value: void 0 };
  }
  setWithExplicit(key2, { value: value4, explicit }) {
    if (value4 !== void 0) {
      this.set(key2, value4, explicit);
    }
  }
  set(key2, value4, explicit) {
    delete this[explicit ? "implicit" : "explicit"][key2];
    this[explicit ? "explicit" : "implicit"][key2] = value4;
    return this;
  }
  copyKeyFromSplit(key2, { explicit, implicit: implicit2 }) {
    if (explicit[key2] !== void 0) {
      this.set(key2, explicit[key2], true);
    } else if (implicit2[key2] !== void 0) {
      this.set(key2, implicit2[key2], false);
    }
  }
  copyKeyFromObject(key2, s) {
    if (s[key2] !== void 0) {
      this.set(key2, s[key2], true);
    }
  }
  /**
   * Merge split object into this split object. Properties from the other split
   * overwrite properties from this split.
   */
  copyAll(other) {
    for (const key2 of keys3(other.combine())) {
      const val = other.getWithExplicit(key2);
      this.setWithExplicit(key2, val);
    }
  }
};
function makeExplicit(value4) {
  return {
    explicit: true,
    value: value4
  };
}
function makeImplicit(value4) {
  return {
    explicit: false,
    value: value4
  };
}
function tieBreakByComparing(compare4) {
  return (v1, v2, property2, propertyOf) => {
    const diff = compare4(v1.value, v2.value);
    if (diff > 0) {
      return v1;
    } else if (diff < 0) {
      return v2;
    }
    return defaultTieBreaker(v1, v2, property2, propertyOf);
  };
}
function defaultTieBreaker(v1, v2, property2, propertyOf) {
  if (v1.explicit && v2.explicit) {
    warn3(mergeConflictingProperty(property2, propertyOf, v1.value, v2.value));
  }
  return v1;
}
function mergeValuesWithExplicit(v1, v2, property2, propertyOf, tieBreaker = defaultTieBreaker) {
  if (v1 === void 0 || v1.value === void 0) {
    return v2;
  }
  if (v1.explicit && !v2.explicit) {
    return v1;
  } else if (v2.explicit && !v1.explicit) {
    return v2;
  } else if (deepEqual(v1.value, v2.value)) {
    return v1;
  } else {
    return tieBreaker(v1, v2, property2, propertyOf);
  }
}
var AncestorParse = class extends Split {
  explicit;
  implicit;
  parseNothing;
  constructor(explicit = {}, implicit2 = {}, parseNothing = false) {
    super(explicit, implicit2);
    this.explicit = explicit;
    this.implicit = implicit2;
    this.parseNothing = parseNothing;
  }
  clone() {
    const clone = super.clone();
    clone.parseNothing = this.parseNothing;
    return clone;
  }
};
function isUrlData(data4) {
  return hasProperty(data4, "url");
}
function isInlineData(data4) {
  return hasProperty(data4, "values");
}
function isNamedData(data4) {
  return hasProperty(data4, "name") && !isUrlData(data4) && !isInlineData(data4) && !isGenerator(data4);
}
function isGenerator(data4) {
  return data4 && (isSequenceGenerator(data4) || isSphereGenerator(data4) || isGraticuleGenerator(data4));
}
function isSequenceGenerator(data4) {
  return hasProperty(data4, "sequence");
}
function isSphereGenerator(data4) {
  return hasProperty(data4, "sphere");
}
function isGraticuleGenerator(data4) {
  return hasProperty(data4, "graticule");
}
var DataSourceType;
(function(DataSourceType2) {
  DataSourceType2[DataSourceType2["Raw"] = 0] = "Raw";
  DataSourceType2[DataSourceType2["Main"] = 1] = "Main";
  DataSourceType2[DataSourceType2["Row"] = 2] = "Row";
  DataSourceType2[DataSourceType2["Column"] = 3] = "Column";
  DataSourceType2[DataSourceType2["Lookup"] = 4] = "Lookup";
  DataSourceType2[DataSourceType2["PreFilterInvalid"] = 5] = "PreFilterInvalid";
  DataSourceType2[DataSourceType2["PostFilterInvalid"] = 6] = "PostFilterInvalid";
})(DataSourceType || (DataSourceType = {}));
function getDataSourcesForHandlingInvalidValues({ invalid, isPath }) {
  const normalizedInvalid = normalizeInvalidDataMode(invalid, { isPath });
  switch (normalizedInvalid) {
    case "filter":
      return {
        marks: "exclude-invalid-values",
        scales: "exclude-invalid-values"
      };
    case "break-paths-show-domains":
      return {
        // Path-based marks use pre-filter data so we know to skip these invalid points in the path.
        // For non-path based marks, we skip by not showing them at all.
        marks: isPath ? "include-invalid-values" : "exclude-invalid-values",
        scales: "include-invalid-values"
      };
    case "break-paths-filter-domains":
      return {
        marks: isPath ? "include-invalid-values" : "exclude-invalid-values",
        // Unlike 'break-paths-show-domains', 'break-paths-filter-domains' uses post-filter data to feed scale.
        scales: "exclude-invalid-values"
      };
    case "show":
      return {
        marks: "include-invalid-values",
        scales: "include-invalid-values"
      };
  }
}
function getScaleDataSourceForHandlingInvalidValues(props) {
  const { marks, scales: scales2 } = getDataSourcesForHandlingInvalidValues(props);
  if (marks === scales2) {
    return DataSourceType.Main;
  }
  return scales2 === "include-invalid-values" ? DataSourceType.PreFilterInvalid : DataSourceType.PostFilterInvalid;
}
var DataFlowNode = class {
  debugName;
  _children = [];
  _parent = null;
  _hash;
  constructor(parent, debugName) {
    this.debugName = debugName;
    if (parent) {
      this.parent = parent;
    }
  }
  /**
   * Clone this node with a deep copy but don't clone links to children or parents.
   */
  clone() {
    throw new Error("Cannot clone node");
  }
  get parent() {
    return this._parent;
  }
  /**
   * Set the parent of the node and also add this node to the parent's children.
   */
  set parent(parent) {
    this._parent = parent;
    if (parent) {
      parent.addChild(this);
    }
  }
  get children() {
    return this._children;
  }
  numChildren() {
    return this._children.length;
  }
  addChild(child, loc) {
    if (this._children.includes(child)) {
      warn3(ADD_SAME_CHILD_TWICE);
      return;
    }
    if (loc !== void 0) {
      this._children.splice(loc, 0, child);
    } else {
      this._children.push(child);
    }
  }
  removeChild(oldChild) {
    const loc = this._children.indexOf(oldChild);
    this._children.splice(loc, 1);
    return loc;
  }
  /**
   * Remove node from the dataflow.
   */
  remove() {
    let loc = this._parent.removeChild(this);
    for (const child of this._children) {
      child._parent = this._parent;
      this._parent.addChild(child, loc++);
    }
  }
  /**
   * Insert another node as a parent of this node.
   */
  insertAsParentOf(other) {
    const parent = other.parent;
    parent.removeChild(this);
    this.parent = parent;
    other.parent = this;
  }
  swapWithParent() {
    const parent = this._parent;
    const newParent = parent.parent;
    for (const child of this._children) {
      child.parent = parent;
    }
    this._children = [];
    parent.removeChild(this);
    const loc = parent.parent.removeChild(parent);
    this._parent = newParent;
    newParent.addChild(this, loc);
    parent.parent = this;
  }
};
var OutputNode = class extends DataFlowNode {
  type;
  refCounts;
  _source;
  _name;
  clone() {
    const cloneObj = new this.constructor();
    cloneObj.debugName = `clone_${this.debugName}`;
    cloneObj._source = this._source;
    cloneObj._name = `clone_${this._name}`;
    cloneObj.type = this.type;
    cloneObj.refCounts = this.refCounts;
    cloneObj.refCounts[cloneObj._name] = 0;
    return cloneObj;
  }
  /**
   * @param source The name of the source. Will change in assemble.
   * @param type The type of the output node.
   * @param refCounts A global ref counter map.
   */
  constructor(parent, source4, type4, refCounts) {
    super(parent, source4);
    this.type = type4;
    this.refCounts = refCounts;
    this._source = this._name = source4;
    if (this.refCounts && !(this._name in this.refCounts)) {
      this.refCounts[this._name] = 0;
    }
  }
  dependentFields() {
    return /* @__PURE__ */ new Set();
  }
  producedFields() {
    return /* @__PURE__ */ new Set();
  }
  hash() {
    if (this._hash === void 0) {
      this._hash = `Output ${uniqueId()}`;
    }
    return this._hash;
  }
  /**
   * Request the datasource name and increase the ref counter.
   *
   * During the parsing phase, this will return the simple name such as 'main' or 'raw'.
   * It is crucial to request the name from an output node to mark it as a required node.
   * If nobody ever requests the name, this datasource will not be instantiated in the assemble phase.
   *
   * In the assemble phase, this will return the correct name.
   */
  getSource() {
    this.refCounts[this._name]++;
    return this._source;
  }
  isRequired() {
    return !!this.refCounts[this._name];
  }
  setSource(source4) {
    this._source = source4;
  }
};
function isTimeUnitTransformComponent(timeUnitComponent) {
  return timeUnitComponent.as !== void 0;
}
function offsetAs(field3) {
  return `${field3}_end`;
}
var TimeUnitNode = class _TimeUnitNode extends DataFlowNode {
  timeUnits;
  clone() {
    return new _TimeUnitNode(null, duplicate(this.timeUnits));
  }
  constructor(parent, timeUnits3) {
    super(parent);
    this.timeUnits = timeUnits3;
  }
  static makeFromEncoding(parent, model) {
    const formula = model.reduceFieldDef((timeUnitComponent, fieldDef, channel) => {
      const { field: field3, timeUnit } = fieldDef;
      if (timeUnit) {
        let component;
        if (isBinnedTimeUnit(timeUnit)) {
          if (isUnitModel(model)) {
            const { mark, markDef, config } = model;
            const bandPosition = getBandPosition({ fieldDef, markDef, config });
            if (isRectBasedMark(mark) || !!bandPosition) {
              component = {
                timeUnit: normalizeTimeUnit(timeUnit),
                field: field3
              };
            }
          }
        } else {
          component = {
            as: vgField(fieldDef, { forAs: true }),
            field: field3,
            timeUnit
          };
        }
        if (isUnitModel(model)) {
          const { mark, markDef, config } = model;
          const bandPosition = getBandPosition({ fieldDef, markDef, config });
          if (isRectBasedMark(mark) && isXorY(channel) && bandPosition !== 0.5) {
            component.rectBandPosition = bandPosition;
          }
        }
        if (component) {
          timeUnitComponent[hash(component)] = component;
        }
      }
      return timeUnitComponent;
    }, {});
    if (isEmpty(formula)) {
      return null;
    }
    return new _TimeUnitNode(parent, formula);
  }
  static makeFromTransform(parent, t) {
    const { timeUnit, ...other } = { ...t };
    const normalizedTimeUnit = normalizeTimeUnit(timeUnit);
    const component = {
      ...other,
      timeUnit: normalizedTimeUnit
    };
    return new _TimeUnitNode(parent, {
      [hash(component)]: component
    });
  }
  /**
   * Merge together TimeUnitNodes assigning the children of `other` to `this`
   * and removing `other`.
   */
  merge(other) {
    this.timeUnits = { ...this.timeUnits };
    for (const key2 in other.timeUnits) {
      if (!this.timeUnits[key2]) {
        this.timeUnits[key2] = other.timeUnits[key2];
      }
    }
    for (const child of other.children) {
      other.removeChild(child);
      child.parent = this;
    }
    other.remove();
  }
  /**
   * Remove time units coming from the other node.
   */
  removeFormulas(fields) {
    const newFormula = {};
    for (const [key2, timeUnitComponent] of entries$1(this.timeUnits)) {
      const fieldAs = isTimeUnitTransformComponent(timeUnitComponent) ? timeUnitComponent.as : `${timeUnitComponent.field}_end`;
      if (!fields.has(fieldAs)) {
        newFormula[key2] = timeUnitComponent;
      }
    }
    this.timeUnits = newFormula;
  }
  producedFields() {
    return new Set(vals(this.timeUnits).map((f) => {
      return isTimeUnitTransformComponent(f) ? f.as : offsetAs(f.field);
    }));
  }
  dependentFields() {
    return new Set(vals(this.timeUnits).map((f) => f.field));
  }
  hash() {
    return `TimeUnit ${hash(this.timeUnits)}`;
  }
  assemble() {
    const transforms2 = [];
    for (const f of vals(this.timeUnits)) {
      const { rectBandPosition } = f;
      const normalizedTimeUnit = normalizeTimeUnit(f.timeUnit);
      if (isTimeUnitTransformComponent(f)) {
        const { field: field3, as } = f;
        const { unit: unit2, utc, ...params2 } = normalizedTimeUnit;
        const startEnd = [as, `${as}_end`];
        transforms2.push({
          field: replacePathInField(field3),
          type: "timeunit",
          ...unit2 ? { units: getTimeUnitParts(unit2) } : {},
          ...utc ? { timezone: "utc" } : {},
          ...params2,
          as: startEnd
        });
        transforms2.push(...offsetedRectFormulas(startEnd, rectBandPosition, normalizedTimeUnit));
      } else if (f) {
        const { field: escapedField } = f;
        const field3 = unescapeSingleQuoteAndPathDot(escapedField);
        const expr2 = offsetExpr({ timeUnit: normalizedTimeUnit, field: field3 });
        const endAs = offsetAs(field3);
        transforms2.push({
          type: "formula",
          expr: expr2,
          as: endAs
        });
        transforms2.push(...offsetedRectFormulas([field3, endAs], rectBandPosition, normalizedTimeUnit));
      }
    }
    return transforms2;
  }
};
var OFFSETTED_RECT_START_SUFFIX = "offsetted_rect_start";
var OFFSETTED_RECT_END_SUFFIX = "offsetted_rect_end";
function offsetExpr({ timeUnit, field: field3, reverse: reverse4 }) {
  const { unit: unit2, utc } = timeUnit;
  const smallestUnit = getSmallestTimeUnitPart(unit2);
  const { part, step } = getDateTimePartAndStep(smallestUnit, timeUnit.step);
  const offsetFn = utc ? "utcOffset" : "timeOffset";
  const expr2 = `${offsetFn}('${part}', ${accessWithDatumToUnescapedPath(field3)}, ${reverse4 ? -step : step})`;
  return expr2;
}
function offsetedRectFormulas([startField, endField], rectBandPosition, timeUnit) {
  if (rectBandPosition !== void 0 && rectBandPosition !== 0.5) {
    const startExpr = accessWithDatumToUnescapedPath(startField);
    const endExpr = accessWithDatumToUnescapedPath(endField);
    return [
      {
        type: "formula",
        expr: interpolateExpr([
          offsetExpr({
            timeUnit,
            field: startField,
            reverse: true
          }),
          startExpr
        ], rectBandPosition + 0.5),
        as: `${startField}_${OFFSETTED_RECT_START_SUFFIX}`
      },
      {
        type: "formula",
        expr: interpolateExpr([startExpr, endExpr], rectBandPosition + 0.5),
        as: `${startField}_${OFFSETTED_RECT_END_SUFFIX}`
      }
    ];
  }
  return [];
}
function interpolateExpr([start, end], fraction) {
  return `${1 - fraction} * ${start} + ${fraction} * ${end}`;
}
var TUPLE_FIELDS = "_tuple_fields";
var SelectionProjectionComponent = class {
  hasChannel;
  hasField;
  hasSelectionId;
  timeUnit;
  items;
  constructor(...items) {
    this.items = items;
    this.hasChannel = {};
    this.hasField = {};
    this.hasSelectionId = false;
  }
};
var project2 = {
  defined: () => {
    return true;
  },
  parse: (model, selCmpt, selDef) => {
    const name = selCmpt.name;
    const proj = selCmpt.project ??= new SelectionProjectionComponent();
    const parsed = {};
    const timeUnits3 = {};
    const signals = /* @__PURE__ */ new Set();
    const signalName = (p, range7) => {
      const suffix = range7 === "visual" ? p.channel : p.field;
      let sg = varName(`${name}_${suffix}`);
      for (let counter = 1; signals.has(sg); counter++) {
        sg = varName(`${name}_${suffix}_${counter}`);
      }
      signals.add(sg);
      return { [range7]: sg };
    };
    const type4 = selCmpt.type;
    const cfg = model.config.selection[type4];
    const init2 = selDef.value !== void 0 ? array_default(selDef.value) : null;
    let { fields, encodings } = isObject(selDef.select) ? selDef.select : {};
    if (!fields && !encodings && init2) {
      for (const initVal of init2) {
        if (!isObject(initVal)) {
          continue;
        }
        for (const key2 of keys3(initVal)) {
          if (isSingleDefUnitChannel(key2)) {
            (encodings || (encodings = [])).push(key2);
          } else {
            if (type4 === "interval") {
              warn3(INTERVAL_INITIALIZED_WITH_POS);
              encodings = cfg.encodings;
            } else {
              (fields ??= []).push(key2);
            }
          }
        }
      }
    }
    if (!fields && !encodings) {
      encodings = cfg.encodings;
      if ("fields" in cfg) {
        fields = cfg.fields;
      }
    }
    for (const channel of encodings ?? []) {
      const fieldDef = model.fieldDef(channel);
      if (fieldDef) {
        let field3 = fieldDef.field;
        if (fieldDef.aggregate) {
          warn3(cannotProjectAggregate(channel, fieldDef.aggregate));
          continue;
        } else if (!field3) {
          warn3(cannotProjectOnChannelWithoutField(channel));
          continue;
        }
        if (fieldDef.timeUnit && !isBinnedTimeUnit(fieldDef.timeUnit)) {
          field3 = model.vgField(channel);
          const component = {
            timeUnit: fieldDef.timeUnit,
            as: field3,
            field: fieldDef.field
          };
          timeUnits3[hash(component)] = component;
        }
        if (!parsed[field3]) {
          const tplType = type4 === "interval" && isScaleChannel(channel) && hasContinuousDomain(model.getScaleComponent(channel).get("type")) ? "R" : fieldDef.bin ? "R-RE" : "E";
          const p = { field: field3, channel, type: tplType, index: proj.items.length };
          p.signals = { ...signalName(p, "data"), ...signalName(p, "visual") };
          proj.items.push(parsed[field3] = p);
          proj.hasField[field3] = parsed[field3];
          proj.hasSelectionId = proj.hasSelectionId || field3 === SELECTION_ID;
          if (isGeoPositionChannel(channel)) {
            p.geoChannel = channel;
            p.channel = getPositionChannelFromLatLong(channel);
            proj.hasChannel[p.channel] = parsed[field3];
          } else {
            proj.hasChannel[channel] = parsed[field3];
          }
        }
      } else {
        warn3(cannotProjectOnChannelWithoutField(channel));
      }
    }
    for (const field3 of fields ?? []) {
      if (proj.hasField[field3])
        continue;
      const p = { type: "E", field: field3, index: proj.items.length };
      p.signals = { ...signalName(p, "data") };
      proj.items.push(p);
      proj.hasField[field3] = p;
      proj.hasSelectionId = proj.hasSelectionId || field3 === SELECTION_ID;
    }
    if (init2) {
      selCmpt.init = init2.map((v2) => {
        return proj.items.map((p) => isObject(v2) ? v2[p.geoChannel || p.channel] !== void 0 ? v2[p.geoChannel || p.channel] : v2[p.field] : v2);
      });
    }
    if (!isEmpty(timeUnits3)) {
      proj.timeUnit = new TimeUnitNode(null, timeUnits3);
    }
  },
  signals: (model, selCmpt, allSignals) => {
    const name = selCmpt.name + TUPLE_FIELDS;
    const hasSignal2 = allSignals.filter((s) => s.name === name);
    return hasSignal2.length > 0 || selCmpt.project.hasSelectionId ? allSignals : allSignals.concat({
      name,
      value: selCmpt.project.items.map(assembleProjection)
    });
  }
};
var CURR = "_curr";
var ANIM_VALUE = "anim_value";
var ANIM_CLOCK = "anim_clock";
var EASED_ANIM_CLOCK = "eased_anim_clock";
var MIN_EXTENT = "min_extent";
var MAX_RANGE_EXTENT = "max_range_extent";
var LAST_TICK = "last_tick_at";
var IS_PLAYING = "is_playing";
var THROTTLE = 1 / 60 * 1e3;
var animationSignals = (selectionName, scaleName) => {
  return [
    // timer signals
    {
      name: EASED_ANIM_CLOCK,
      // update: 'easeLinear(anim_clock / max_range_extent) * max_range_extent'
      update: ANIM_CLOCK
      // TODO: replace with above once easing functions are implemented in vega-functions
    },
    // scale signals
    // TODO(jzong): uncomment commented signals below when implementing interpolation
    { name: `${selectionName}_domain`, init: `domain('${scaleName}')` },
    { name: MIN_EXTENT, init: `extent(${selectionName}_domain)[0]` },
    // {name: 'max_extent', init: `extent(${selectionName}_domain)[1]`},
    { name: MAX_RANGE_EXTENT, init: `extent(range('${scaleName}'))[1]` },
    // {name: 't_index', update: `indexof(${selectionName}_domain, anim_value)`},
    { name: ANIM_VALUE, update: `invert('${scaleName}', ${EASED_ANIM_CLOCK})` }
  ];
};
var point$1 = {
  defined: (selCmpt) => selCmpt.type === "point",
  topLevelSignals: (model, selCmpt, signals) => {
    if (isTimerSelection(selCmpt)) {
      signals = signals.concat([
        {
          name: ANIM_CLOCK,
          init: "0",
          on: [
            {
              events: { type: "timer", throttle: THROTTLE },
              update: `${IS_PLAYING} ? (${ANIM_CLOCK} + (now() - ${LAST_TICK}) > ${MAX_RANGE_EXTENT} ? 0 : ${ANIM_CLOCK} + (now() - ${LAST_TICK})) : ${ANIM_CLOCK}`
            }
          ]
        },
        {
          name: LAST_TICK,
          init: "now()",
          on: [{ events: [{ signal: ANIM_CLOCK }, { signal: IS_PLAYING }], update: "now()" }]
        },
        {
          name: IS_PLAYING,
          init: "true"
        }
      ]);
    }
    return signals;
  },
  signals: (model, selCmpt, signals) => {
    const name = selCmpt.name;
    const fieldsSg = name + TUPLE_FIELDS;
    const project3 = selCmpt.project;
    const datum3 = "(item().isVoronoi ? datum.datum : datum)";
    const brushes = vals(model.component.selection ?? {}).reduce((acc, cmpt) => {
      return cmpt.type === "interval" ? acc.concat(cmpt.name + BRUSH) : acc;
    }, []).map((b10) => `indexof(item().mark.name, '${b10}') < 0`).join(" && ");
    const test2 = `datum && item().mark.marktype !== 'group' && indexof(item().mark.role, 'legend') < 0${brushes ? ` && ${brushes}` : ""}`;
    let update3 = `unit: ${unitName(model)}, `;
    if (selCmpt.project.hasSelectionId) {
      update3 += `${SELECTION_ID}: ${datum3}[${stringValue(SELECTION_ID)}]`;
    } else if (isTimerSelection(selCmpt)) {
      update3 += `fields: ${fieldsSg}, values: [${ANIM_VALUE} ? ${ANIM_VALUE} : ${MIN_EXTENT}]`;
    } else {
      const values4 = project3.items.map((p) => {
        const fieldDef = model.fieldDef(p.channel);
        return fieldDef?.bin ? `[${datum3}[${stringValue(model.vgField(p.channel, {}))}], ${datum3}[${stringValue(model.vgField(p.channel, { binSuffix: "end" }))}]]` : `${datum3}[${stringValue(p.field)}]`;
      }).join(", ");
      update3 += `fields: ${fieldsSg}, values: [${values4}]`;
    }
    if (isTimerSelection(selCmpt)) {
      return signals.concat(animationSignals(selCmpt.name, model.scaleName(TIME)), [
        {
          name: name + TUPLE,
          on: [
            {
              events: [{ signal: EASED_ANIM_CLOCK }, { signal: ANIM_VALUE }],
              update: `{${update3}}`,
              force: true
            }
          ]
        }
      ]);
    } else {
      const events3 = selCmpt.events;
      return signals.concat([
        {
          name: name + TUPLE,
          on: events3 ? [
            {
              events: events3,
              update: `${test2} ? {${update3}} : null`,
              force: true
            }
          ] : []
        }
      ]);
    }
  }
};
function assembleProjection(proj) {
  const { signals, hasLegend, index: index4, ...rest } = proj;
  rest.field = replacePathInField(rest.field);
  return rest;
}
function assembleInit(init2, isExpr2 = true, wrap3 = identity) {
  if (isArray(init2)) {
    const assembled = init2.map((v2) => assembleInit(v2, isExpr2, wrap3));
    return isExpr2 ? `[${assembled.join(", ")}]` : assembled;
  } else if (isDateTime(init2)) {
    if (isExpr2) {
      return wrap3(dateTimeToExpr(init2));
    } else {
      return wrap3(dateTimeToTimestamp(init2));
    }
  }
  return isExpr2 ? wrap3(stringify(init2)) : init2;
}
function assembleUnitSelectionSignals(model, signals) {
  for (const selCmpt of vals(model.component.selection ?? {})) {
    const name = selCmpt.name;
    let modifyExpr = `${name}${TUPLE}, ${selCmpt.resolve === "global" ? "true" : `{unit: ${unitName(model)}}`}`;
    for (const c3 of selectionCompilers) {
      if (!c3.defined(selCmpt))
        continue;
      if (c3.signals)
        signals = c3.signals(model, selCmpt, signals);
      if (c3.modifyExpr)
        modifyExpr = c3.modifyExpr(model, selCmpt, modifyExpr);
    }
    signals.push({
      name: name + MODIFY,
      on: [
        {
          events: { signal: selCmpt.name + TUPLE },
          update: `modify(${stringValue(selCmpt.name + STORE)}, ${modifyExpr})`
        }
      ]
    });
  }
  return cleanupEmptyOnArray(signals);
}
function assembleFacetSignals(model, signals) {
  if (model.component.selection && keys3(model.component.selection).length) {
    const name = stringValue(model.getName("cell"));
    signals.unshift({
      name: "facet",
      value: {},
      on: [
        {
          events: eventSelector("pointermove", "scope"),
          update: `isTuple(facet) ? facet : group(${name}).datum`
        }
      ]
    });
  }
  return cleanupEmptyOnArray(signals);
}
function assembleTopLevelSignals(model, signals) {
  let hasSelections = false;
  for (const selCmpt of vals(model.component.selection ?? {})) {
    const name = selCmpt.name;
    const store = stringValue(name + STORE);
    const hasSg = signals.filter((s) => s.name === name);
    if (hasSg.length === 0) {
      const resolve2 = selCmpt.resolve === "global" ? "union" : selCmpt.resolve;
      const isPoint2 = selCmpt.type === "point" ? ", true, true)" : ")";
      signals.push({
        name: selCmpt.name,
        update: `${VL_SELECTION_RESOLVE}(${store}, ${stringValue(resolve2)}${isPoint2}`
      });
    }
    hasSelections = true;
    for (const c3 of selectionCompilers) {
      if (c3.defined(selCmpt) && c3.topLevelSignals) {
        signals = c3.topLevelSignals(model, selCmpt, signals);
      }
    }
  }
  if (hasSelections) {
    const hasUnit = signals.filter((s) => s.name === "unit");
    if (hasUnit.length === 0) {
      signals.unshift({
        name: "unit",
        value: {},
        on: [{ events: "pointermove", update: "isTuple(group()) ? group() : unit" }]
      });
    }
  }
  return cleanupEmptyOnArray(signals);
}
function assembleUnitSelectionData(model, data4) {
  const selectionData = [];
  const animationData = [];
  const unit2 = unitName(model, { escape: false });
  for (const selCmpt of vals(model.component.selection ?? {})) {
    const store = { name: selCmpt.name + STORE };
    if (selCmpt.project.hasSelectionId) {
      store.transform = [{ type: "collect", sort: { field: SELECTION_ID } }];
    }
    if (selCmpt.init) {
      const fields = selCmpt.project.items.map(assembleProjection);
      store.values = selCmpt.project.hasSelectionId ? selCmpt.init.map((i) => ({ unit: unit2, [SELECTION_ID]: assembleInit(i, false)[0] })) : selCmpt.init.map((i) => ({ unit: unit2, fields, values: assembleInit(i, false) }));
    }
    const contains3 = [...selectionData, ...data4].filter((d) => d.name === selCmpt.name + STORE);
    if (!contains3.length) {
      selectionData.push(store);
    }
    if (isTimerSelection(selCmpt) && data4.length) {
      const sourceName = model.lookupDataSource(model.getDataName(DataSourceType.Main));
      const sourceData = data4.find((d) => d.name === sourceName);
      const sourceDataFilter = sourceData.transform.find((t) => t.type === "filter" && t.expr.includes("vlSelectionTest"));
      if (sourceDataFilter) {
        sourceData.transform = sourceData.transform.filter((t) => t !== sourceDataFilter);
        const currentFrame = {
          name: sourceData.name + CURR,
          source: sourceData.name,
          transform: [sourceDataFilter]
          // add the selection filter to the animation dataset
        };
        animationData.push(currentFrame);
      }
    }
  }
  return selectionData.concat(data4, animationData);
}
function assembleUnitSelectionMarks(model, marks) {
  for (const selCmpt of vals(model.component.selection ?? {})) {
    for (const c3 of selectionCompilers) {
      if (c3.defined(selCmpt) && c3.marks) {
        marks = c3.marks(model, selCmpt, marks);
      }
    }
  }
  return marks;
}
function assembleLayerSelectionMarks(model, marks) {
  for (const child of model.children) {
    if (isUnitModel(child)) {
      marks = assembleUnitSelectionMarks(child, marks);
    }
  }
  return marks;
}
function assembleSelectionScaleDomain(model, extent2, scaleCmpt, domain4) {
  const parsedExtent = parseSelectionExtent(model, extent2.param, extent2);
  return {
    signal: hasContinuousDomain(scaleCmpt.get("type")) && isArray(domain4) && domain4[0] > domain4[1] ? `isValid(${parsedExtent}) && reverse(${parsedExtent})` : parsedExtent
  };
}
function cleanupEmptyOnArray(signals) {
  return signals.map((s) => {
    if (s.on && !s.on.length)
      delete s.on;
    return s;
  });
}
var scaleBindings = {
  defined: (selCmpt) => {
    return selCmpt.type === "interval" && selCmpt.resolve === "global" && selCmpt.bind && selCmpt.bind === "scales";
  },
  parse: (model, selCmpt) => {
    const bound3 = selCmpt.scales = [];
    for (const proj of selCmpt.project.items) {
      const channel = proj.channel;
      if (!isScaleChannel(channel)) {
        continue;
      }
      const scale9 = model.getScaleComponent(channel);
      const scaleType2 = scale9 ? scale9.get("type") : void 0;
      if (scaleType2 == "sequential") {
        warn3(SEQUENTIAL_SCALE_DEPRECATED);
      }
      if (!scale9 || !hasContinuousDomain(scaleType2)) {
        warn3(SCALE_BINDINGS_CONTINUOUS);
        continue;
      }
      scale9.set("selectionExtent", { param: selCmpt.name, field: proj.field }, true);
      bound3.push(proj);
    }
  },
  topLevelSignals: (model, selCmpt, signals) => {
    const bound3 = selCmpt.scales.filter((proj) => signals.filter((s) => s.name === proj.signals.data).length === 0);
    if (!model.parent || isTopLevelLayer(model) || bound3.length === 0) {
      return signals;
    }
    const namedSg = signals.find((s) => s.name === selCmpt.name);
    let update3 = namedSg.update;
    if (update3.includes(VL_SELECTION_RESOLVE)) {
      namedSg.update = `{${bound3.map((proj) => `${stringValue(replacePathInField(proj.field))}: ${proj.signals.data}`).join(", ")}}`;
    } else {
      for (const proj of bound3) {
        const mapping = `${stringValue(replacePathInField(proj.field))}: ${proj.signals.data}`;
        if (!update3.includes(mapping)) {
          update3 = `${update3.substring(0, update3.length - 1)}, ${mapping}}`;
        }
      }
      namedSg.update = update3;
    }
    return signals.concat(bound3.map((proj) => ({ name: proj.signals.data })));
  },
  signals: (model, selCmpt, signals) => {
    if (model.parent && !isTopLevelLayer(model)) {
      for (const proj of selCmpt.scales) {
        const signal = signals.find((s) => s.name === proj.signals.data);
        signal.push = "outer";
        delete signal.value;
        delete signal.update;
      }
    }
    return signals;
  }
};
function domain3(model, channel) {
  const scale9 = stringValue(model.scaleName(channel));
  return `domain(${scale9})`;
}
function isTopLevelLayer(model) {
  return model.parent && isLayerModel(model.parent) && (!model.parent.parent || isTopLevelLayer(model.parent.parent));
}
var BRUSH = "_brush";
var SCALE_TRIGGER = "_scale_trigger";
var GEO_INIT_TICK = "geo_interval_init_tick";
var INIT = "_init";
var CENTER2 = "_center";
var interval2 = {
  defined: (selCmpt) => selCmpt.type === "interval",
  parse: (model, selCmpt, selDef) => {
    if (model.hasProjection) {
      const def3 = { ...isObject(selDef.select) ? selDef.select : {} };
      def3.fields = [SELECTION_ID];
      if (!def3.encodings) {
        def3.encodings = selDef.value ? keys3(selDef.value) : [LONGITUDE, LATITUDE];
      }
      selDef.select = { type: "interval", ...def3 };
    }
    if (selCmpt.translate && !scaleBindings.defined(selCmpt)) {
      const filterExpr = `!event.item || event.item.mark.name !== ${stringValue(selCmpt.name + BRUSH)}`;
      for (const evt of selCmpt.events) {
        if (!evt.between) {
          warn3(`${evt} is not an ordered event stream for interval selections.`);
          continue;
        }
        const filters2 = array_default(evt.between[0].filter ??= []);
        if (!filters2.includes(filterExpr)) {
          filters2.push(filterExpr);
        }
      }
    }
  },
  signals: (model, selCmpt, signals) => {
    const name = selCmpt.name;
    const tupleSg = name + TUPLE;
    const channels = vals(selCmpt.project.hasChannel).filter((p) => p.channel === X3 || p.channel === Y3);
    const init2 = selCmpt.init ? selCmpt.init[0] : null;
    signals.push(...channels.reduce((arr, proj) => arr.concat(channelSignals(model, selCmpt, proj, init2?.[proj.index])), []));
    if (!model.hasProjection) {
      if (!scaleBindings.defined(selCmpt)) {
        const triggerSg = name + SCALE_TRIGGER;
        const scaleTriggers = channels.map((proj) => {
          const channel = proj.channel;
          const { data: dname, visual: vname } = proj.signals;
          const scaleName = stringValue(model.scaleName(channel));
          const scaleType2 = model.getScaleComponent(channel).get("type");
          const toNum = hasContinuousDomain(scaleType2) ? "+" : "";
          return `(!isArray(${dname}) || (${toNum}invert(${scaleName}, ${vname})[0] === ${toNum}${dname}[0] && ${toNum}invert(${scaleName}, ${vname})[1] === ${toNum}${dname}[1]))`;
        });
        if (scaleTriggers.length) {
          signals.push({
            name: triggerSg,
            value: {},
            on: [
              {
                events: channels.map((proj) => ({ scale: model.scaleName(proj.channel) })),
                update: `${scaleTriggers.join(" && ")} ? ${triggerSg} : {}`
              }
            ]
          });
        }
      }
      const dataSignals = channels.map((proj) => proj.signals.data);
      const update3 = `unit: ${unitName(model)}, fields: ${name + TUPLE_FIELDS}, values`;
      return signals.concat({
        name: tupleSg,
        ...init2 ? { init: `{${update3}: ${assembleInit(init2)}}` } : {},
        ...dataSignals.length ? {
          on: [
            {
              events: [{ signal: dataSignals.join(" || ") }],
              // Prevents double invocation, see https://github.com/vega/vega/issues/1672.
              update: `${dataSignals.join(" && ")} ? {${update3}: [${dataSignals}]} : null`
            }
          ]
        } : {}
      });
    } else {
      const projection3 = stringValue(model.projectionName());
      const centerSg = model.projectionName() + CENTER2;
      const { x: x6, y: y6 } = selCmpt.project.hasChannel;
      const xvname = x6?.signals.visual;
      const yvname = y6?.signals.visual;
      const xinit = x6 ? init2?.[x6.index] : `${centerSg}[0]`;
      const yinit = y6 ? init2?.[y6.index] : `${centerSg}[1]`;
      const sizeSg = (layout) => model.getSizeSignalRef(layout).signal;
      const bbox = `[[${xvname ? `${xvname}[0]` : "0"}, ${yvname ? `${yvname}[0]` : "0"}],[${xvname ? `${xvname}[1]` : sizeSg("width")}, ${yvname ? `${yvname}[1]` : sizeSg("height")}]]`;
      if (init2) {
        signals.unshift({
          name: name + INIT,
          init: `[scale(${projection3}, [${x6 ? xinit[0] : xinit}, ${y6 ? yinit[0] : yinit}]), scale(${projection3}, [${x6 ? xinit[1] : xinit}, ${y6 ? yinit[1] : yinit}])]`
        });
        if (!x6 || !y6) {
          const hasCenterSg = signals.find((s) => s.name === centerSg);
          if (!hasCenterSg) {
            signals.unshift({
              name: centerSg,
              update: `invert(${projection3}, [${sizeSg("width")}/2, ${sizeSg("height")}/2])`
            });
          }
        }
      }
      const intersect7 = `intersect(${bbox}, {markname: ${stringValue(model.getName("marks"))}}, unit.mark)`;
      const base = `{unit: ${unitName(model)}}`;
      const update3 = `vlSelectionTuples(${intersect7}, ${base})`;
      const visualSignals = channels.map((proj) => proj.signals.visual);
      return signals.concat({
        name: tupleSg,
        on: [
          {
            events: [
              ...visualSignals.length ? [{ signal: visualSignals.join(" || ") }] : [],
              ...init2 ? [{ signal: GEO_INIT_TICK }] : []
            ],
            update: update3
          }
        ]
      });
    }
  },
  topLevelSignals: (model, selCmpt, signals) => {
    if (isUnitModel(model) && model.hasProjection && selCmpt.init) {
      const hasTick = signals.filter((s) => s.name === GEO_INIT_TICK);
      if (!hasTick.length) {
        signals.unshift({
          name: GEO_INIT_TICK,
          value: null,
          on: [
            {
              events: "timer{1}",
              update: `${GEO_INIT_TICK} === null ? {} : ${GEO_INIT_TICK}`
            }
          ]
        });
      }
    }
    return signals;
  },
  marks: (model, selCmpt, marks) => {
    const name = selCmpt.name;
    const { x: x6, y: y6 } = selCmpt.project.hasChannel;
    const xvname = x6?.signals.visual;
    const yvname = y6?.signals.visual;
    const store = `data(${stringValue(selCmpt.name + STORE)})`;
    if (scaleBindings.defined(selCmpt) || !x6 && !y6) {
      return marks;
    }
    const update3 = {
      x: x6 !== void 0 ? { signal: `${xvname}[0]` } : { value: 0 },
      y: y6 !== void 0 ? { signal: `${yvname}[0]` } : { value: 0 },
      x2: x6 !== void 0 ? { signal: `${xvname}[1]` } : { field: { group: "width" } },
      y2: y6 !== void 0 ? { signal: `${yvname}[1]` } : { field: { group: "height" } }
    };
    if (selCmpt.resolve === "global") {
      for (const key2 of keys3(update3)) {
        update3[key2] = [
          {
            test: `${store}.length && ${store}[0].unit === ${unitName(model)}`,
            ...update3[key2]
          },
          { value: 0 }
        ];
      }
    }
    const { fill: fill3, fillOpacity, cursor: cursor3, ...stroke3 } = selCmpt.mark;
    const vgStroke = keys3(stroke3).reduce((def3, k) => {
      def3[k] = [
        {
          test: [
            x6 !== void 0 && `${xvname}[0] !== ${xvname}[1]`,
            y6 !== void 0 && `${yvname}[0] !== ${yvname}[1]`
          ].filter((t) => t).join(" && "),
          value: stroke3[k]
        },
        { value: null }
      ];
      return def3;
    }, {});
    const vgCursor = cursor3 ?? (selCmpt.translate ? "move" : null);
    return [
      {
        name: `${name + BRUSH}_bg`,
        type: "rect",
        clip: true,
        encode: {
          enter: {
            fill: { value: fill3 },
            fillOpacity: { value: fillOpacity }
          },
          update: update3
        }
      },
      ...marks,
      {
        name: name + BRUSH,
        type: "rect",
        clip: true,
        encode: {
          enter: {
            ...vgCursor ? { cursor: { value: vgCursor } } : {},
            fill: { value: "transparent" }
          },
          update: { ...update3, ...vgStroke }
        }
      }
    ];
  }
};
function channelSignals(model, selCmpt, proj, init2) {
  const scaledInterval = !model.hasProjection;
  const channel = proj.channel;
  const vname = proj.signals.visual;
  const scaleName = stringValue(scaledInterval ? model.scaleName(channel) : model.projectionName());
  const scaled = (str) => `scale(${scaleName}, ${str})`;
  const size = model.getSizeSignalRef(channel === X3 ? "width" : "height").signal;
  const coord = `${channel}(unit)`;
  const von = selCmpt.events.reduce((def3, evt) => {
    return [
      ...def3,
      { events: evt.between[0], update: `[${coord}, ${coord}]` },
      // Brush Start
      { events: evt, update: `[${vname}[0], clamp(${coord}, 0, ${size})]` }
      // Brush End
    ];
  }, []);
  if (scaledInterval) {
    const dname = proj.signals.data;
    const hasScales = scaleBindings.defined(selCmpt);
    const scale9 = model.getScaleComponent(channel);
    const scaleType2 = scale9 ? scale9.get("type") : void 0;
    const vinit = init2 ? { init: assembleInit(init2, true, scaled) } : { value: [] };
    von.push({
      events: { signal: selCmpt.name + SCALE_TRIGGER },
      update: hasContinuousDomain(scaleType2) ? `[${scaled(`${dname}[0]`)}, ${scaled(`${dname}[1]`)}]` : `[0, 0]`
    });
    return hasScales ? [{ name: dname, on: [] }] : [
      { name: vname, ...vinit, on: von },
      {
        name: dname,
        ...init2 ? { init: assembleInit(init2) } : {},
        // Cannot be `value` as `init` may require datetime exprs.
        on: [
          {
            events: { signal: vname },
            update: `${vname}[0] === ${vname}[1] ? null : invert(${scaleName}, ${vname})`
          }
        ]
      }
    ];
  } else {
    const initIdx = channel === X3 ? 0 : 1;
    const initSg = selCmpt.name + INIT;
    const vinit = init2 ? { init: `[${initSg}[0][${initIdx}], ${initSg}[1][${initIdx}]]` } : { value: [] };
    return [{ name: vname, ...vinit, on: von }];
  }
}
function wrapCondition({ model, channelDef, vgChannel, invalidValueRef, mainRefFn }) {
  const condition = isConditionalDef(channelDef) && channelDef.condition;
  let valueRefs = [];
  if (condition) {
    const conditions = array_default(condition);
    valueRefs = conditions.map((c3) => {
      const conditionValueRef = mainRefFn(c3);
      if (isConditionalParameter(c3)) {
        const { param: param2, empty } = c3;
        const test2 = parseSelectionPredicate(model, { param: param2, empty });
        return { test: test2, ...conditionValueRef };
      } else {
        const test2 = expression2(model, c3.test);
        return { test: test2, ...conditionValueRef };
      }
    });
  }
  if (invalidValueRef !== void 0) {
    valueRefs.push(invalidValueRef);
  }
  const mainValueRef = mainRefFn(channelDef);
  if (mainValueRef !== void 0) {
    valueRefs.push(mainValueRef);
  }
  if (valueRefs.length > 1 || valueRefs.length === 1 && Boolean(valueRefs[0].test)) {
    return { [vgChannel]: valueRefs };
  } else if (valueRefs.length === 1) {
    return { [vgChannel]: valueRefs[0] };
  }
  return {};
}
function text$1(model, channel = "text") {
  const channelDef = model.encoding[channel];
  return wrapCondition({
    model,
    channelDef,
    vgChannel: channel,
    mainRefFn: (cDef) => textRef(cDef, model.config),
    invalidValueRef: void 0
    // text encoding doesn't have continuous scales and thus can't have invalid values
  });
}
function textRef(channelDef, config, expr2 = "datum") {
  if (channelDef) {
    if (isValueDef(channelDef)) {
      return signalOrValueRef(channelDef.value);
    }
    if (isFieldOrDatumDef(channelDef)) {
      const { format: format6, formatType } = getFormatMixins(channelDef);
      return formatSignalRef({ fieldOrDatumDef: channelDef, format: format6, formatType, expr: expr2, config });
    }
  }
  return void 0;
}
function tooltip(model, opt = {}) {
  const { encoding, markDef, config, stack: stack2 } = model;
  const channelDef = encoding.tooltip;
  if (isArray(channelDef)) {
    return { tooltip: tooltipRefForEncoding({ tooltip: channelDef }, stack2, config, opt) };
  } else {
    const datum3 = opt.reactiveGeom ? "datum.datum" : "datum";
    const mainRefFn = (cDef) => {
      const tooltipRefFromChannelDef = addLineBreaksToTooltip(cDef, config, datum3);
      if (tooltipRefFromChannelDef) {
        return tooltipRefFromChannelDef;
      }
      if (cDef === null) {
        return void 0;
      }
      let markTooltip = getMarkPropOrConfig("tooltip", markDef, config);
      if (markTooltip === true) {
        markTooltip = { content: "encoding" };
      }
      if (isString(markTooltip)) {
        return { value: markTooltip };
      } else if (isObject(markTooltip)) {
        if (isSignalRef(markTooltip)) {
          return markTooltip;
        } else if (markTooltip.content === "encoding") {
          return tooltipRefForEncoding(encoding, stack2, config, opt);
        } else {
          return { signal: datum3 };
        }
      }
      return void 0;
    };
    return wrapCondition({
      model,
      channelDef,
      vgChannel: "tooltip",
      mainRefFn,
      invalidValueRef: void 0
      // tooltip encoding doesn't have continuous scales and thus can't have invalid values
    });
  }
}
function tooltipData(encoding, stack2, config, { reactiveGeom } = {}) {
  const formatConfig = { ...config, ...config.tooltipFormat };
  const toSkip = /* @__PURE__ */ new Set();
  const expr2 = reactiveGeom ? "datum.datum" : "datum";
  const tuples = [];
  function add7(fDef, channel) {
    const mainChannel = getMainRangeChannel(channel);
    const fieldDef = isTypedFieldDef(fDef) ? fDef : {
      ...fDef,
      type: encoding[mainChannel].type
      // for secondary field def, copy type from main channel
    };
    const title2 = fieldDef.title || defaultTitle(fieldDef, formatConfig);
    const key2 = array_default(title2).join(", ").replaceAll(/"/g, '\\"');
    let value4;
    if (isXorY(channel)) {
      const channel2 = channel === "x" ? "x2" : "y2";
      const fieldDef2 = getFieldDef(encoding[channel2]);
      if (isBinned(fieldDef.bin) && fieldDef2) {
        const startField = vgField(fieldDef, { expr: expr2 });
        const endField = vgField(fieldDef2, { expr: expr2 });
        const { format: format6, formatType } = getFormatMixins(fieldDef);
        value4 = binFormatExpression(startField, endField, format6, formatType, formatConfig);
        toSkip.add(channel2);
      }
    }
    if ((isXorY(channel) || channel === THETA || channel === RADIUS) && stack2 && stack2.fieldChannel === channel && stack2.offset === "normalize") {
      const { format: format6, formatType } = getFormatMixins(fieldDef);
      value4 = formatSignalRef({
        fieldOrDatumDef: fieldDef,
        format: format6,
        formatType,
        expr: expr2,
        config: formatConfig,
        normalizeStack: true
      }).signal;
    }
    value4 ??= addLineBreaksToTooltip(fieldDef, formatConfig, expr2).signal;
    tuples.push({ channel, key: key2, value: value4 });
  }
  forEach(encoding, (channelDef, channel) => {
    if (isFieldDef(channelDef)) {
      add7(channelDef, channel);
    } else if (hasConditionalFieldDef(channelDef)) {
      add7(channelDef.condition, channel);
    }
  });
  const out = {};
  for (const { channel, key: key2, value: value4 } of tuples) {
    if (!toSkip.has(channel) && !out[key2]) {
      out[key2] = value4;
    }
  }
  return out;
}
function tooltipRefForEncoding(encoding, stack2, config, { reactiveGeom } = {}) {
  const data4 = tooltipData(encoding, stack2, config, { reactiveGeom });
  const keyValues = entries$1(data4).map(([key2, value4]) => `"${key2}": ${value4}`);
  return keyValues.length > 0 ? { signal: `{${keyValues.join(", ")}}` } : void 0;
}
function addLineBreaksToTooltip(channelDef, config, expr2 = "datum") {
  if (isFieldDef(channelDef) && isDiscrete$1(channelDef.type) && !channelDef.timeUnit && !getFormatMixins(channelDef).format && !getFormatMixins(channelDef).formatType) {
    const fieldString = `${expr2}["${channelDef.field}"]`;
    return {
      signal: `isValid(${fieldString}) ? isArray(${fieldString}) ? join(${fieldString}, '\\n') : ${fieldString} : ""+${fieldString}`
    };
  }
  return textRef(channelDef, config, expr2);
}
function aria(model) {
  const { markDef, config } = model;
  const enableAria = getMarkPropOrConfig("aria", markDef, config);
  if (enableAria === false) {
    return {};
  }
  return {
    ...enableAria ? { aria: enableAria } : {},
    ...ariaRoleDescription(model),
    ...description(model)
  };
}
function ariaRoleDescription(model) {
  const { mark, markDef, config } = model;
  if (config.aria === false) {
    return {};
  }
  const ariaRoleDesc = getMarkPropOrConfig("ariaRoleDescription", markDef, config);
  if (ariaRoleDesc != null) {
    return { ariaRoleDescription: { value: ariaRoleDesc } };
  }
  return hasOwnProperty(VG_MARK_INDEX, mark) ? {} : { ariaRoleDescription: { value: mark } };
}
function description(model) {
  const { encoding, markDef, config, stack: stack2 } = model;
  const channelDef = encoding.description;
  if (channelDef) {
    return wrapCondition({
      model,
      channelDef,
      vgChannel: "description",
      mainRefFn: (cDef) => textRef(cDef, model.config),
      invalidValueRef: void 0
      // aria encoding doesn't have continuous scales and thus can't have invalid values
    });
  }
  const descriptionValue = getMarkPropOrConfig("description", markDef, config);
  if (descriptionValue != null) {
    return {
      description: signalOrValueRef(descriptionValue)
    };
  }
  if (config.aria === false) {
    return {};
  }
  const data4 = tooltipData(encoding, stack2, config);
  if (isEmpty(data4)) {
    return void 0;
  }
  return {
    description: {
      signal: entries$1(data4).filter(([key2]) => !key2.startsWith("_")).map(([key2, value4]) => [key2, value4.replaceAll("\\n", " ")]).map(([key2, value4], index4) => `"${index4 > 0 ? "; " : ""}${key2}: " + (${value4})`).join(" + ")
    }
  };
}
function nonPosition(channel, model, opt = {}) {
  const { markDef, encoding, config } = model;
  const { vgChannel } = opt;
  let { defaultRef, defaultValue } = opt;
  const channelDef = encoding[channel];
  if (defaultRef === void 0) {
    defaultValue ??= getMarkPropOrConfig(channel, markDef, config, {
      vgChannel,
      // If there is no conditonal def, we ignore vgConfig so the output spec is concise.
      // However, if there is a conditional def, we must include vgConfig so the default is respected.
      ignoreVgConfig: !isConditionalDef(channelDef)
    });
    if (defaultValue !== void 0) {
      defaultRef = signalOrValueRef(defaultValue);
    }
  }
  const commonProps = {
    markDef,
    config,
    scaleName: model.scaleName(channel),
    scale: model.getScaleComponent(channel)
  };
  const invalidValueRef = getConditionalValueRefForIncludingInvalidValue({
    ...commonProps,
    scaleChannel: channel,
    channelDef
  });
  const mainRefFn = (cDef) => {
    return midPoint({
      ...commonProps,
      channel,
      channelDef: cDef,
      stack: null,
      // No need to provide stack for non-position as it does not affect mid point
      defaultRef
    });
  };
  return wrapCondition({
    model,
    channelDef,
    vgChannel: vgChannel ?? channel,
    invalidValueRef,
    mainRefFn
  });
}
function color5(model, opt = { filled: void 0 }) {
  const { markDef, encoding, config } = model;
  const { type: markType2 } = markDef;
  const filled = opt.filled ?? getMarkPropOrConfig("filled", markDef, config);
  const transparentIfNeeded = contains2(["bar", "point", "circle", "square", "geoshape"], markType2) ? "transparent" : void 0;
  const defaultFill = getMarkPropOrConfig(filled === true ? "color" : void 0, markDef, config, { vgChannel: "fill" }) ?? // need to add this manually as getMarkConfig normally drops config.mark[channel] if vgChannel is specified
  config.mark[filled === true && "color"] ?? // If there is no fill, always fill symbols, bar, geoshape
  // with transparent fills https://github.com/vega/vega-lite/issues/1316
  transparentIfNeeded;
  const defaultStroke = getMarkPropOrConfig(filled === false ? "color" : void 0, markDef, config, { vgChannel: "stroke" }) ?? // need to add this manually as getMarkConfig normally drops config.mark[channel] if vgChannel is specified
  config.mark[filled === false && "color"];
  const colorVgChannel = filled ? "fill" : "stroke";
  const fillStrokeMarkDefAndConfig = {
    ...defaultFill ? { fill: signalOrValueRef(defaultFill) } : {},
    ...defaultStroke ? { stroke: signalOrValueRef(defaultStroke) } : {}
  };
  if (markDef.color && (filled ? markDef.fill : markDef.stroke)) {
    warn3(droppingColor("property", { fill: "fill" in markDef, stroke: "stroke" in markDef }));
  }
  return {
    ...fillStrokeMarkDefAndConfig,
    ...nonPosition("color", model, {
      vgChannel: colorVgChannel,
      defaultValue: filled ? defaultFill : defaultStroke
    }),
    ...nonPosition("fill", model, {
      // if there is encoding.fill, include default fill just in case we have conditional-only fill encoding
      defaultValue: encoding.fill ? defaultFill : void 0
    }),
    ...nonPosition("stroke", model, {
      // if there is encoding.stroke, include default fill just in case we have conditional-only stroke encoding
      defaultValue: encoding.stroke ? defaultStroke : void 0
    })
  };
}
function zindex(model) {
  const { encoding, mark } = model;
  const order = encoding.order;
  if (!isPathMark(mark) && isValueDef(order)) {
    return wrapCondition({
      model,
      channelDef: order,
      vgChannel: "zindex",
      mainRefFn: (cd2) => signalOrValueRef(cd2.value),
      invalidValueRef: void 0
      // zindex encoding doesn't have continuous scales and thus can't have invalid values
    });
  }
  return {};
}
function positionOffset({ channel: baseChannel, markDef, encoding = {}, model, bandPosition }) {
  const channel = `${baseChannel}Offset`;
  const defaultValue = markDef[channel];
  const channelDef = encoding[channel];
  if ((channel === "xOffset" || channel === "yOffset") && channelDef) {
    const ref2 = midPoint({
      channel,
      channelDef,
      markDef,
      config: model?.config,
      scaleName: model.scaleName(channel),
      scale: model.getScaleComponent(channel),
      stack: null,
      defaultRef: signalOrValueRef(defaultValue),
      bandPosition
    });
    return { offsetType: "encoding", offset: ref2 };
  }
  const markDefOffsetValue = markDef[channel];
  if (markDefOffsetValue) {
    return { offsetType: "visual", offset: markDefOffsetValue };
  }
  return {};
}
function pointPosition(channel, model, { defaultPos, vgChannel }) {
  const { encoding, markDef, config, stack: stack2 } = model;
  const channelDef = encoding[channel];
  const channel2Def = encoding[getSecondaryRangeChannel(channel)];
  const scaleName = model.scaleName(channel);
  const scale9 = model.getScaleComponent(channel);
  const { offset: offset6, offsetType } = positionOffset({
    channel,
    markDef,
    encoding,
    model,
    bandPosition: 0.5
  });
  const defaultRef = pointPositionDefaultRef({
    model,
    defaultPos,
    channel,
    scaleName,
    scale: scale9
  });
  const valueRef = !channelDef && isXorY(channel) && (encoding.latitude || encoding.longitude) ? (
    // use geopoint output if there are lat/long and there is no point position overriding lat/long.
    { field: model.getName(channel) }
  ) : positionRef({
    channel,
    channelDef,
    channel2Def,
    markDef,
    config,
    scaleName,
    scale: scale9,
    stack: stack2,
    offset: offset6,
    defaultRef,
    bandPosition: offsetType === "encoding" ? 0 : void 0
  });
  return valueRef ? { [vgChannel || channel]: valueRef } : void 0;
}
function positionRef(params2) {
  const { channel, channelDef, scaleName, stack: stack2, offset: offset6, markDef } = params2;
  if (isFieldOrDatumDef(channelDef) && stack2 && channel === stack2.fieldChannel) {
    if (isFieldDef(channelDef)) {
      let bandPosition = channelDef.bandPosition;
      if (bandPosition === void 0 && markDef.type === "text" && (channel === "radius" || channel === "theta")) {
        bandPosition = 0.5;
      }
      if (bandPosition !== void 0) {
        return interpolatedSignalRef({
          scaleName,
          fieldOrDatumDef: channelDef,
          // positionRef always have type
          startSuffix: "start",
          bandPosition,
          offset: offset6
        });
      }
    }
    return valueRefForFieldOrDatumDef(channelDef, scaleName, { suffix: "end" }, { offset: offset6 });
  }
  return midPointRefWithPositionInvalidTest(params2);
}
function pointPositionDefaultRef({ model, defaultPos, channel, scaleName, scale: scale9 }) {
  const { markDef, config } = model;
  return () => {
    const mainChannel = getMainRangeChannel(channel);
    const vgChannel = getVgPositionChannel(channel);
    const definedValueOrConfig = getMarkPropOrConfig(channel, markDef, config, { vgChannel });
    if (definedValueOrConfig !== void 0) {
      return widthHeightValueOrSignalRef(channel, definedValueOrConfig);
    }
    switch (defaultPos) {
      case "zeroOrMin":
        return zeroOrMinOrMaxPosition({ scaleName, scale: scale9, mode: "zeroOrMin", mainChannel, config });
      case "zeroOrMax":
        return zeroOrMinOrMaxPosition({
          scaleName,
          scale: scale9,
          mode: { zeroOrMax: { widthSignal: model.width.signal, heightSignal: model.height.signal } },
          mainChannel,
          config
        });
      case "mid": {
        const sizeRef = model[getSizeChannel(channel)];
        return { ...sizeRef, mult: 0.5 };
      }
    }
    return void 0;
  };
}
function zeroOrMinOrMaxPosition({ mainChannel, config, ...otherProps }) {
  const scaledValueRef = scaledZeroOrMinOrMax(otherProps);
  const { mode } = otherProps;
  if (scaledValueRef) {
    return scaledValueRef;
  }
  switch (mainChannel) {
    case "radius": {
      if (mode === "zeroOrMin") {
        return { value: 0 };
      }
      const { widthSignal, heightSignal } = mode.zeroOrMax;
      return {
        signal: `min(${widthSignal},${heightSignal})/2`
      };
    }
    case "theta":
      return mode === "zeroOrMin" ? { value: 0 } : { signal: "2*PI" };
    case "x":
      return mode === "zeroOrMin" ? { value: 0 } : { field: { group: "width" } };
    case "y":
      return mode === "zeroOrMin" ? { field: { group: "height" } } : { value: 0 };
  }
}
var ALIGNED_X_CHANNEL = {
  left: "x",
  center: "xc",
  right: "x2"
};
var BASELINED_Y_CHANNEL = {
  top: "y",
  middle: "yc",
  bottom: "y2"
};
function vgAlignedPositionChannel(channel, markDef, config, defaultAlign = "middle") {
  if (channel === "radius" || channel === "theta") {
    return getVgPositionChannel(channel);
  }
  const alignChannel = channel === "x" ? "align" : "baseline";
  const align2 = getMarkPropOrConfig(alignChannel, markDef, config);
  let alignExcludingSignal;
  if (isSignalRef(align2)) {
    warn3(rangeMarkAlignmentCannotBeExpression(alignChannel));
    alignExcludingSignal = void 0;
  } else {
    alignExcludingSignal = align2;
  }
  if (channel === "x") {
    return ALIGNED_X_CHANNEL[alignExcludingSignal || (defaultAlign === "top" ? "left" : "center")];
  } else {
    return BASELINED_Y_CHANNEL[alignExcludingSignal || defaultAlign];
  }
}
function pointOrRangePosition(channel, model, { defaultPos, defaultPos2, range: range7 }) {
  if (range7) {
    return rangePosition(channel, model, { defaultPos, defaultPos2 });
  }
  return pointPosition(channel, model, { defaultPos });
}
function rangePosition(channel, model, { defaultPos, defaultPos2 }) {
  const { markDef, config } = model;
  const channel2 = getSecondaryRangeChannel(channel);
  const sizeChannel = getSizeChannel(channel);
  const pos2Mixins = pointPosition2OrSize(model, defaultPos2, channel2);
  const vgChannel = pos2Mixins[sizeChannel] ? (
    // If there is width/height, we need to position the marks based on the alignment.
    vgAlignedPositionChannel(channel, markDef, config)
  ) : (
    // Otherwise, make sure to apply to the right Vg Channel (for arc mark)
    getVgPositionChannel(channel)
  );
  return {
    ...pointPosition(channel, model, { defaultPos, vgChannel }),
    ...pos2Mixins
  };
}
function pointPosition2OrSize(model, defaultPos, channel) {
  const { encoding, mark, markDef, stack: stack2, config } = model;
  const baseChannel = getMainRangeChannel(channel);
  const sizeChannel = getSizeChannel(channel);
  const vgChannel = getVgPositionChannel(channel);
  const channelDef = encoding[baseChannel];
  const scaleName = model.scaleName(baseChannel);
  const scale9 = model.getScaleComponent(baseChannel);
  const { offset: offset6 } = channel in encoding || channel in markDef ? positionOffset({ channel, markDef, encoding, model }) : positionOffset({ channel: baseChannel, markDef, encoding, model });
  if (!channelDef && (channel === "x2" || channel === "y2") && (encoding.latitude || encoding.longitude)) {
    const vgSizeChannel = getSizeChannel(channel);
    const size = model.markDef[vgSizeChannel];
    if (size != null) {
      return {
        [vgSizeChannel]: { value: size }
      };
    } else {
      return {
        [vgChannel]: { field: model.getName(channel) }
      };
    }
  }
  const valueRef = position2Ref({
    channel,
    channelDef,
    channel2Def: encoding[channel],
    markDef,
    config,
    scaleName,
    scale: scale9,
    stack: stack2,
    offset: offset6,
    defaultRef: void 0
  });
  if (valueRef !== void 0) {
    return { [vgChannel]: valueRef };
  }
  return position2orSize(channel, markDef) || position2orSize(channel, {
    [channel]: getMarkStyleConfig(channel, markDef, config.style),
    [sizeChannel]: getMarkStyleConfig(sizeChannel, markDef, config.style)
  }) || position2orSize(channel, config[mark]) || position2orSize(channel, config.mark) || {
    [vgChannel]: pointPositionDefaultRef({
      model,
      defaultPos,
      channel,
      scaleName,
      scale: scale9
    })()
  };
}
function position2Ref({ channel, channelDef, channel2Def, markDef, config, scaleName, scale: scale9, stack: stack2, offset: offset6, defaultRef }) {
  if (isFieldOrDatumDef(channelDef) && stack2 && // If fieldChannel is X and channel is X2 (or Y and Y2)
  channel.charAt(0) === stack2.fieldChannel.charAt(0)) {
    return valueRefForFieldOrDatumDef(channelDef, scaleName, { suffix: "start" }, { offset: offset6 });
  }
  return midPointRefWithPositionInvalidTest({
    channel,
    channelDef: channel2Def,
    scaleName,
    scale: scale9,
    stack: stack2,
    markDef,
    config,
    offset: offset6,
    defaultRef
  });
}
function position2orSize(channel, markDef) {
  const sizeChannel = getSizeChannel(channel);
  const vgChannel = getVgPositionChannel(channel);
  if (markDef[vgChannel] !== void 0) {
    return { [vgChannel]: widthHeightValueOrSignalRef(channel, markDef[vgChannel]) };
  } else if (markDef[channel] !== void 0) {
    return { [vgChannel]: widthHeightValueOrSignalRef(channel, markDef[channel]) };
  } else if (markDef[sizeChannel]) {
    const dimensionSize = markDef[sizeChannel];
    if (isRelativeBandSize(dimensionSize)) {
      warn3(relativeBandSizeNotSupported(sizeChannel));
    } else {
      return { [sizeChannel]: widthHeightValueOrSignalRef(channel, dimensionSize) };
    }
  }
  return void 0;
}
function rectPosition(model, channel) {
  const { config, encoding, markDef } = model;
  const mark = markDef.type;
  const channel2 = getSecondaryRangeChannel(channel);
  const sizeChannel = getSizeChannel(channel);
  const channelDef = encoding[channel];
  const channelDef2 = encoding[channel2];
  const scale9 = model.getScaleComponent(channel);
  const scaleType2 = scale9 ? scale9.get("type") : void 0;
  const orient2 = markDef.orient;
  const hasSizeDef = encoding[sizeChannel] ?? encoding.size ?? getMarkPropOrConfig("size", markDef, config, { vgChannel: sizeChannel });
  const offsetScaleChannel = getOffsetChannel(channel);
  const isBarOrTickBand = mark === "bar" && (channel === "x" ? orient2 === "vertical" : orient2 === "horizontal") || mark === "tick" && (channel === "y" ? orient2 === "vertical" : orient2 === "horizontal");
  if (isFieldDef(channelDef) && (isBinning(channelDef.bin) || isBinned(channelDef.bin) || channelDef.timeUnit && !channelDef2) && !(hasSizeDef && !isRelativeBandSize(hasSizeDef)) && !encoding[offsetScaleChannel] && !hasDiscreteDomain(scaleType2)) {
    return rectBinPosition({
      fieldDef: channelDef,
      fieldDef2: channelDef2,
      channel,
      model
    });
  } else if ((isFieldOrDatumDef(channelDef) && hasDiscreteDomain(scaleType2) || isBarOrTickBand) && !channelDef2) {
    return positionAndSize(channelDef, channel, model);
  } else {
    return rangePosition(channel, model, { defaultPos: "zeroOrMax", defaultPos2: "zeroOrMin" });
  }
}
function defaultSizeRef(sizeChannel, scaleName, scale9, config, bandSize, hasFieldDef, mark) {
  if (isRelativeBandSize(bandSize)) {
    if (scale9) {
      const scaleType2 = scale9.get("type");
      if (scaleType2 === "band") {
        let bandWidth = `bandwidth('${scaleName}')`;
        if (bandSize.band !== 1) {
          bandWidth = `${bandSize.band} * ${bandWidth}`;
        }
        const minBandSize = getMarkConfig("minBandSize", { type: mark }, config);
        return { signal: minBandSize ? `max(${signalOrStringValue(minBandSize)}, ${bandWidth})` : bandWidth };
      } else if (bandSize.band !== 1) {
        warn3(cannotUseRelativeBandSizeWithNonBandScale(scaleType2));
      }
    } else {
      return {
        mult: bandSize.band,
        field: { group: sizeChannel }
      };
    }
  } else if (isSignalRef(bandSize)) {
    return bandSize;
  } else if (bandSize) {
    return { value: bandSize };
  }
  if (scale9) {
    const scaleRange = scale9.get("range");
    if (isVgRangeStep(scaleRange) && isNumber(scaleRange.step)) {
      return { value: scaleRange.step - 2 };
    }
  }
  if (!hasFieldDef) {
    const { bandPaddingInner, barBandPaddingInner, rectBandPaddingInner, tickBandPaddingInner } = config.scale;
    const padding3 = getFirstDefined(bandPaddingInner, mark === "tick" ? tickBandPaddingInner : mark === "bar" ? barBandPaddingInner : rectBandPaddingInner);
    if (isSignalRef(padding3)) {
      return { signal: `(1 - (${padding3.signal})) * ${sizeChannel}` };
    } else if (isNumber(padding3)) {
      return { signal: `${1 - padding3} * ${sizeChannel}` };
    }
  }
  const defaultStep = getViewConfigDiscreteStep(config.view, sizeChannel);
  return { value: defaultStep - 2 };
}
function positionAndSize(fieldDef, channel, model) {
  const { markDef, encoding, config, stack: stack2 } = model;
  const orient2 = markDef.orient;
  const scaleName = model.scaleName(channel);
  const scale9 = model.getScaleComponent(channel);
  const vgSizeChannel = getSizeChannel(channel);
  const channel2 = getSecondaryRangeChannel(channel);
  const offsetScaleChannel = getOffsetChannel(channel);
  const offsetScaleName = model.scaleName(offsetScaleChannel);
  const offsetScale = model.getScaleComponent(getOffsetScaleChannel(channel));
  const useVlSizeChannel = (
    // Always uses size channel for ticks, because tick only calls rectPosition() for the size channel
    markDef.type === "tick" || // use "size" channel for bars, if there is orient and the channel matches the right orientation
    orient2 === "horizontal" && channel === "y" || orient2 === "vertical" && channel === "x"
  );
  let sizeMixins;
  if (encoding.size || markDef.size) {
    if (useVlSizeChannel) {
      sizeMixins = nonPosition("size", model, {
        vgChannel: vgSizeChannel,
        defaultRef: signalOrValueRef(markDef.size)
      });
    } else {
      warn3(cannotApplySizeToNonOrientedMark(markDef.type));
    }
  }
  const hasSizeFromMarkOrEncoding = !!sizeMixins;
  const bandSize = getBandSize({
    channel,
    fieldDef,
    markDef,
    config,
    scaleType: (scale9 || offsetScale)?.get("type"),
    useVlSizeChannel
  });
  sizeMixins = sizeMixins || {
    [vgSizeChannel]: defaultSizeRef(vgSizeChannel, offsetScaleName || scaleName, offsetScale || scale9, config, bandSize, !!fieldDef, markDef.type)
  };
  const defaultBandAlign = (scale9 || offsetScale)?.get("type") === "band" && isRelativeBandSize(bandSize) && !hasSizeFromMarkOrEncoding ? "top" : "middle";
  const vgChannel = vgAlignedPositionChannel(channel, markDef, config, defaultBandAlign);
  const center = vgChannel === "xc" || vgChannel === "yc";
  const { offset: offset6, offsetType } = positionOffset({ channel, markDef, encoding, model, bandPosition: center ? 0.5 : 0 });
  const timeUnitBandPosition = center && offsetType !== "encoding" && isFieldDef(fieldDef) && fieldDef.timeUnit && !encoding[channel2] ? getBandPosition({ fieldDef, markDef, config }) : void 0;
  const bandPosition = timeUnitBandPosition ?? (center ? offsetType === "encoding" ? 0 : 0.5 : isSignalRef(bandSize) ? { signal: `(1-${bandSize})/2` } : isRelativeBandSize(bandSize) ? (1 - bandSize.band) / 2 : 0);
  const posRef = midPointRefWithPositionInvalidTest({
    channel,
    channelDef: fieldDef,
    markDef,
    config,
    scaleName,
    scale: scale9,
    stack: stack2,
    offset: offset6,
    defaultRef: pointPositionDefaultRef({ model, defaultPos: "mid", channel, scaleName, scale: scale9 }),
    bandPosition
  });
  if (vgSizeChannel) {
    return { [vgChannel]: posRef, ...sizeMixins };
  } else {
    const vgChannel2 = getVgPositionChannel(channel2);
    const sizeRef = sizeMixins[vgSizeChannel];
    const sizeOffset = offset6 ? { ...sizeRef, offset: offset6 } : sizeRef;
    return {
      [vgChannel]: posRef,
      // posRef might be an array that wraps position invalid test
      [vgChannel2]: isArray(posRef) ? [posRef[0], { ...posRef[1], offset: sizeOffset }] : {
        ...posRef,
        offset: sizeOffset
      }
    };
  }
}
function getBinSpacing(channel, spacing, reverse4, axisTranslate, offset6, minBandSize, bandSizeExpr) {
  if (isPolarPositionChannel(channel)) {
    return 0;
  }
  const isEnd = channel === "x" || channel === "y2";
  const spacingOffset = isEnd ? -spacing / 2 : spacing / 2;
  if (isSignalRef(reverse4) || isSignalRef(offset6) || isSignalRef(axisTranslate) || minBandSize) {
    const reverseExpr = signalOrStringValue(reverse4);
    const offsetExpr2 = signalOrStringValue(offset6);
    const axisTranslateExpr = signalOrStringValue(axisTranslate);
    const minBandSizeExpr = signalOrStringValue(minBandSize);
    const sign3 = isEnd ? "" : "-";
    const spacingAndSizeOffset = minBandSize ? `(${bandSizeExpr} < ${minBandSizeExpr} ? ${sign3}0.5 * (${minBandSizeExpr} - (${bandSizeExpr})) : ${spacingOffset})` : spacingOffset;
    const t = axisTranslateExpr ? `${axisTranslateExpr} + ` : "";
    const r = reverseExpr ? `(${reverseExpr} ? -1 : 1) * ` : "";
    const o = offsetExpr2 ? `(${offsetExpr2} + ${spacingAndSizeOffset})` : spacingAndSizeOffset;
    return {
      signal: t + r + o
    };
  } else {
    offset6 = offset6 || 0;
    return axisTranslate + (reverse4 ? -offset6 - spacingOffset : +offset6 + spacingOffset);
  }
}
function rectBinPosition({ fieldDef, fieldDef2, channel, model }) {
  const { config, markDef, encoding } = model;
  const scale9 = model.getScaleComponent(channel);
  const scaleName = model.scaleName(channel);
  const scaleType2 = scale9 ? scale9.get("type") : void 0;
  const reverse4 = scale9.get("reverse");
  const bandSize = getBandSize({ channel, fieldDef, markDef, config, scaleType: scaleType2 });
  const axis = model.component.axes[channel]?.[0];
  const axisTranslate = axis?.get("translate") ?? 0.5;
  const spacing = isXorY(channel) ? getMarkPropOrConfig("binSpacing", markDef, config) ?? 0 : 0;
  const channel2 = getSecondaryRangeChannel(channel);
  const vgChannel = getVgPositionChannel(channel);
  const vgChannel2 = getVgPositionChannel(channel2);
  const minBandSize = getMarkConfig("minBandSize", markDef, config);
  const { offset: offset6 } = positionOffset({ channel, markDef, encoding, model, bandPosition: 0 });
  const { offset: offset22 } = positionOffset({ channel: channel2, markDef, encoding, model, bandPosition: 0 });
  const bandSizeExpr = binSizeExpr({ fieldDef, scaleName });
  const binSpacingOffset = getBinSpacing(channel, spacing, reverse4, axisTranslate, offset6, minBandSize, bandSizeExpr);
  const binSpacingOffset2 = getBinSpacing(channel2, spacing, reverse4, axisTranslate, offset22 ?? offset6, minBandSize, bandSizeExpr);
  const bandPositionForBandSize = isSignalRef(bandSize) ? { signal: `(1-${bandSize.signal})/2` } : isRelativeBandSize(bandSize) ? (1 - bandSize.band) / 2 : 0.5;
  const bandPosition = getBandPosition({ fieldDef, fieldDef2, markDef, config });
  if (isBinning(fieldDef.bin) || fieldDef.timeUnit) {
    const useRectOffsetField = fieldDef.timeUnit && bandPosition !== 0.5;
    return {
      [vgChannel2]: rectBinRef({
        fieldDef,
        scaleName,
        bandPosition: bandPositionForBandSize,
        offset: binSpacingOffset2,
        useRectOffsetField
      }),
      [vgChannel]: rectBinRef({
        fieldDef,
        scaleName,
        bandPosition: isSignalRef(bandPositionForBandSize) ? { signal: `1-${bandPositionForBandSize.signal}` } : 1 - bandPositionForBandSize,
        offset: binSpacingOffset,
        useRectOffsetField
      })
    };
  } else if (isBinned(fieldDef.bin)) {
    const startRef = valueRefForFieldOrDatumDef(fieldDef, scaleName, {}, { offset: binSpacingOffset2 });
    if (isFieldDef(fieldDef2)) {
      return {
        [vgChannel2]: startRef,
        [vgChannel]: valueRefForFieldOrDatumDef(fieldDef2, scaleName, {}, { offset: binSpacingOffset })
      };
    } else if (isBinParams(fieldDef.bin) && fieldDef.bin.step) {
      return {
        [vgChannel2]: startRef,
        [vgChannel]: {
          signal: `scale("${scaleName}", ${vgField(fieldDef, { expr: "datum" })} + ${fieldDef.bin.step})`,
          offset: binSpacingOffset
        }
      };
    }
  }
  warn3(channelRequiredForBinned(channel2));
  return void 0;
}
function rectBinRef({ fieldDef, scaleName, bandPosition, offset: offset6, useRectOffsetField }) {
  return interpolatedSignalRef({
    scaleName,
    fieldOrDatumDef: fieldDef,
    bandPosition,
    offset: offset6,
    ...useRectOffsetField ? {
      startSuffix: OFFSETTED_RECT_START_SUFFIX,
      endSuffix: OFFSETTED_RECT_END_SUFFIX
    } : {}
  });
}
var ALWAYS_IGNORE = /* @__PURE__ */ new Set(["aria", "width", "height"]);
function baseEncodeEntry(model, ignore) {
  const { fill: fill3 = void 0, stroke: stroke3 = void 0 } = ignore.color === "include" ? color5(model) : {};
  return {
    ...markDefProperties(model.markDef, ignore),
    ...colorRef("fill", fill3),
    ...colorRef("stroke", stroke3),
    ...nonPosition("opacity", model),
    ...nonPosition("fillOpacity", model),
    ...nonPosition("strokeOpacity", model),
    ...nonPosition("strokeWidth", model),
    ...nonPosition("strokeDash", model),
    ...zindex(model),
    ...tooltip(model),
    ...text$1(model, "href"),
    ...aria(model)
  };
}
function colorRef(channel, valueRef) {
  return valueRef ? { [channel]: valueRef } : {};
}
function markDefProperties(mark, ignore) {
  return VG_MARK_CONFIGS.reduce((m3, prop) => {
    if (!ALWAYS_IGNORE.has(prop) && hasProperty(mark, prop) && ignore[prop] !== "ignore") {
      m3[prop] = signalOrValueRef(mark[prop]);
    }
    return m3;
  }, {});
}
function defined(model) {
  const { config, markDef } = model;
  const fieldsToBreakPath = /* @__PURE__ */ new Set();
  model.forEachFieldDef((fieldDef, channel) => {
    let scaleType2;
    if (!isScaleChannel(channel) || !(scaleType2 = model.getScaleType(channel))) {
      return;
    }
    const isCountAggregate = isCountingAggregateOp(fieldDef.aggregate);
    const invalidDataMode = getScaleInvalidDataMode({
      scaleChannel: channel,
      markDef,
      config,
      scaleType: scaleType2,
      isCountAggregate
    });
    if (shouldBreakPath(invalidDataMode)) {
      const field3 = model.vgField(channel, { expr: "datum", binSuffix: model.stack?.impute ? "mid" : void 0 });
      if (field3) {
        fieldsToBreakPath.add(field3);
      }
    }
  });
  if (fieldsToBreakPath.size > 0) {
    const signal = [...fieldsToBreakPath].map((field3) => fieldValidPredicate(field3, true)).join(" && ");
    return { defined: { signal } };
  }
  return void 0;
}
function valueIfDefined(prop, value4) {
  if (value4 !== void 0) {
    return { [prop]: signalOrValueRef(value4) };
  }
  return void 0;
}
var VORONOI = "voronoi";
var nearest = {
  defined: (selCmpt) => {
    return selCmpt.type === "point" && selCmpt.nearest;
  },
  parse: (model, selCmpt) => {
    if (selCmpt.events) {
      for (const s of selCmpt.events) {
        s.markname = model.getName(VORONOI);
      }
    }
  },
  marks: (model, selCmpt, marks) => {
    const { x: x6, y: y6 } = selCmpt.project.hasChannel;
    const markType2 = model.mark;
    if (isPathMark(markType2)) {
      warn3(nearestNotSupportForContinuous(markType2));
      return marks;
    }
    const cellDef = {
      name: model.getName(VORONOI),
      type: "path",
      interactive: true,
      aria: false,
      from: { data: model.getName("marks") },
      encode: {
        update: {
          fill: { value: "transparent" },
          strokeWidth: { value: 0.35 },
          stroke: { value: "transparent" },
          isVoronoi: { value: true },
          ...tooltip(model, { reactiveGeom: true })
        }
      },
      transform: [
        {
          type: "voronoi",
          x: { expr: x6 || !y6 ? "datum.datum.x || 0" : "0" },
          y: { expr: y6 || !x6 ? "datum.datum.y || 0" : "0" },
          size: [model.getSizeSignalRef("width"), model.getSizeSignalRef("height")]
        }
      ]
    };
    let index4 = 0;
    let exists = false;
    marks.forEach((mark, i) => {
      const name = mark.name ?? "";
      if (name === model.component.mark[0].name) {
        index4 = i;
      } else if (name.includes(VORONOI)) {
        exists = true;
      }
    });
    if (!exists) {
      marks.splice(index4 + 1, 0, cellDef);
    }
    return marks;
  }
};
var inputBindings = {
  defined: (selCmpt) => {
    return selCmpt.type === "point" && selCmpt.resolve === "global" && selCmpt.bind && selCmpt.bind !== "scales" && !isLegendBinding(selCmpt.bind);
  },
  parse: (model, selCmpt, selDef) => disableDirectManipulation(selCmpt, selDef),
  topLevelSignals: (model, selCmpt, signals) => {
    const name = selCmpt.name;
    const proj = selCmpt.project;
    const bind4 = selCmpt.bind;
    const init2 = selCmpt.init?.[0];
    const datum3 = nearest.defined(selCmpt) ? "(item().isVoronoi ? datum.datum : datum)" : "datum";
    proj.items.forEach((p, i) => {
      const sgname = varName(`${name}_${p.field}`);
      const hasSignal2 = signals.filter((s) => s.name === sgname);
      if (!hasSignal2.length) {
        signals.unshift({
          name: sgname,
          ...init2 ? { init: assembleInit(init2[i]) } : { value: null },
          on: selCmpt.events ? [
            {
              events: selCmpt.events,
              update: `datum && item().mark.marktype !== 'group' ? ${datum3}[${stringValue(p.field)}] : null`
            }
          ] : [],
          bind: bind4[p.field] ?? bind4[p.channel] ?? bind4
        });
      }
    });
    return signals;
  },
  signals: (model, selCmpt, signals) => {
    const name = selCmpt.name;
    const proj = selCmpt.project;
    const signal = signals.find((s) => s.name === name + TUPLE);
    const fields = name + TUPLE_FIELDS;
    const values4 = proj.items.map((p) => varName(`${name}_${p.field}`));
    const valid = values4.map((v2) => `${v2} !== null`).join(" && ");
    if (values4.length) {
      signal.update = `${valid} ? {fields: ${fields}, values: [${values4.join(", ")}]} : null`;
    }
    delete signal.value;
    delete signal.on;
    return signals;
  }
};
var TOGGLE = "_toggle";
var toggle = {
  defined: (selCmpt) => {
    return selCmpt.type === "point" && !isTimerSelection(selCmpt) && !!selCmpt.toggle;
  },
  signals: (model, selCmpt, signals) => {
    return signals.concat({
      name: selCmpt.name + TOGGLE,
      value: false,
      on: [{ events: selCmpt.events, update: selCmpt.toggle }]
    });
  },
  modifyExpr: (model, selCmpt) => {
    const tpl = selCmpt.name + TUPLE;
    const signal = selCmpt.name + TOGGLE;
    return `${signal} ? null : ${tpl}, ${selCmpt.resolve === "global" ? `${signal} ? null : true, ` : `${signal} ? null : {unit: ${unitName(model)}}, `}${signal} ? ${tpl} : null`;
  }
};
var clear = {
  defined: (selCmpt) => {
    return selCmpt.clear !== void 0 && selCmpt.clear !== false && !isTimerSelection(selCmpt);
  },
  parse: (model, selCmpt) => {
    if (selCmpt.clear) {
      selCmpt.clear = isString(selCmpt.clear) ? eventSelector(selCmpt.clear, "view") : selCmpt.clear;
    }
  },
  topLevelSignals: (model, selCmpt, signals) => {
    if (inputBindings.defined(selCmpt)) {
      for (const proj of selCmpt.project.items) {
        const idx = signals.findIndex((n) => n.name === varName(`${selCmpt.name}_${proj.field}`));
        if (idx !== -1) {
          signals[idx].on.push({ events: selCmpt.clear, update: "null" });
        }
      }
    }
    return signals;
  },
  signals: (model, selCmpt, signals) => {
    function addClear(idx, update3) {
      if (idx !== -1 && signals[idx].on) {
        signals[idx].on.push({ events: selCmpt.clear, update: update3 });
      }
    }
    if (selCmpt.type === "interval") {
      for (const proj of selCmpt.project.items) {
        const vIdx = signals.findIndex((n) => n.name === proj.signals.visual);
        addClear(vIdx, "[0, 0]");
        if (vIdx === -1) {
          const dIdx = signals.findIndex((n) => n.name === proj.signals.data);
          addClear(dIdx, "null");
        }
      }
    } else {
      let tIdx = signals.findIndex((n) => n.name === selCmpt.name + TUPLE);
      addClear(tIdx, "null");
      if (toggle.defined(selCmpt)) {
        tIdx = signals.findIndex((n) => n.name === selCmpt.name + TOGGLE);
        addClear(tIdx, "false");
      }
    }
    return signals;
  }
};
var legendBindings = {
  defined: (selCmpt) => {
    const spec = selCmpt.resolve === "global" && selCmpt.bind && isLegendBinding(selCmpt.bind);
    const projLen = selCmpt.project.items.length === 1 && selCmpt.project.items[0].field !== SELECTION_ID;
    if (spec && !projLen) {
      warn3(LEGEND_BINDINGS_MUST_HAVE_PROJECTION);
    }
    return spec && projLen;
  },
  parse: (model, selCmpt, selDef) => {
    const selDef_ = duplicate(selDef);
    selDef_.select = isString(selDef_.select) ? { type: selDef_.select, toggle: selCmpt.toggle } : { ...selDef_.select, toggle: selCmpt.toggle };
    disableDirectManipulation(selCmpt, selDef_);
    if (isObject(selDef.select) && (selDef.select.on || selDef.select.clear)) {
      const legendFilter = 'event.item && indexof(event.item.mark.role, "legend") < 0';
      for (const evt2 of selCmpt.events) {
        evt2.filter = array_default(evt2.filter ?? []);
        if (!evt2.filter.includes(legendFilter)) {
          evt2.filter.push(legendFilter);
        }
      }
    }
    const evt = isLegendStreamBinding(selCmpt.bind) ? selCmpt.bind.legend : "click";
    const stream2 = isString(evt) ? eventSelector(evt, "view") : array_default(evt);
    selCmpt.bind = { legend: { merge: stream2 } };
  },
  topLevelSignals: (model, selCmpt, signals) => {
    const selName = selCmpt.name;
    const stream2 = isLegendStreamBinding(selCmpt.bind) && selCmpt.bind.legend;
    const markName = (name) => (s) => {
      const ds = duplicate(s);
      ds.markname = name;
      return ds;
    };
    for (const proj of selCmpt.project.items) {
      if (!proj.hasLegend)
        continue;
      const prefix = `${varName(proj.field)}_legend`;
      const sgName = `${selName}_${prefix}`;
      const hasSignal2 = signals.filter((s) => s.name === sgName);
      if (hasSignal2.length === 0) {
        const events3 = stream2.merge.map(markName(`${prefix}_symbols`)).concat(stream2.merge.map(markName(`${prefix}_labels`))).concat(stream2.merge.map(markName(`${prefix}_entries`)));
        signals.unshift({
          name: sgName,
          ...!selCmpt.init ? { value: null } : {},
          on: [
            // Legend entries do not store values, so we need to walk the scenegraph to the symbol datum.
            {
              events: events3,
              update: "isDefined(datum.value) ? datum.value : item().items[0].items[0].datum.value",
              force: true
            },
            { events: stream2.merge, update: `!event.item || !datum ? null : ${sgName}`, force: true }
          ]
        });
      }
    }
    return signals;
  },
  signals: (model, selCmpt, signals) => {
    const name = selCmpt.name;
    const proj = selCmpt.project;
    const tuple = signals.find((s) => s.name === name + TUPLE);
    const fields = name + TUPLE_FIELDS;
    const values4 = proj.items.filter((p) => p.hasLegend).map((p) => varName(`${name}_${varName(p.field)}_legend`));
    const valid = values4.map((v2) => `${v2} !== null`).join(" && ");
    const update3 = `${valid} ? {fields: ${fields}, values: [${values4.join(", ")}]} : null`;
    if (selCmpt.events && values4.length > 0) {
      tuple.on.push({
        events: values4.map((signal) => ({ signal })),
        update: update3
      });
    } else if (values4.length > 0) {
      tuple.update = update3;
      delete tuple.value;
      delete tuple.on;
    }
    const toggle2 = signals.find((s) => s.name === name + TOGGLE);
    const events3 = isLegendStreamBinding(selCmpt.bind) && selCmpt.bind.legend;
    if (toggle2) {
      if (!selCmpt.events)
        toggle2.on[0].events = events3;
      else
        toggle2.on.push({ ...toggle2.on[0], events: events3 });
    }
    return signals;
  }
};
function parseInteractiveLegend(model, channel, legendCmpt) {
  const field3 = model.fieldDef(channel)?.field;
  for (const selCmpt of vals(model.component.selection ?? {})) {
    const proj = selCmpt.project.hasField[field3] ?? selCmpt.project.hasChannel[channel];
    if (proj && legendBindings.defined(selCmpt)) {
      const legendSelections = legendCmpt.get("selections") ?? [];
      legendSelections.push(selCmpt.name);
      legendCmpt.set("selections", legendSelections, false);
      proj.hasLegend = true;
    }
  }
}
var ANCHOR$1 = "_translate_anchor";
var DELTA$1 = "_translate_delta";
var translate4 = {
  defined: (selCmpt) => {
    return selCmpt.type === "interval" && selCmpt.translate;
  },
  signals: (model, selCmpt, signals) => {
    const name = selCmpt.name;
    const boundScales = scaleBindings.defined(selCmpt);
    const anchor = name + ANCHOR$1;
    const { x: x6, y: y6 } = selCmpt.project.hasChannel;
    let events3 = eventSelector(selCmpt.translate, "scope");
    if (!boundScales) {
      events3 = events3.map((e) => (e.between[0].markname = name + BRUSH, e));
    }
    signals.push({
      name: anchor,
      value: {},
      on: [
        {
          events: events3.map((e) => e.between[0]),
          update: `{x: x(unit), y: y(unit)${x6 !== void 0 ? `, extent_x: ${boundScales ? domain3(model, X3) : `slice(${x6.signals.visual})`}` : ""}${y6 !== void 0 ? `, extent_y: ${boundScales ? domain3(model, Y3) : `slice(${y6.signals.visual})`}` : ""}}`
        }
      ]
    }, {
      name: name + DELTA$1,
      value: {},
      on: [
        {
          events: events3,
          update: `{x: ${anchor}.x - x(unit), y: ${anchor}.y - y(unit)}`
        }
      ]
    });
    if (x6 !== void 0) {
      onDelta$1(model, selCmpt, x6, "width", signals);
    }
    if (y6 !== void 0) {
      onDelta$1(model, selCmpt, y6, "height", signals);
    }
    return signals;
  }
};
function onDelta$1(model, selCmpt, proj, size, signals) {
  const name = selCmpt.name;
  const anchor = name + ANCHOR$1;
  const delta = name + DELTA$1;
  const channel = proj.channel;
  const boundScales = scaleBindings.defined(selCmpt);
  const signal = signals.find((s) => s.name === proj.signals[boundScales ? "data" : "visual"]);
  const sizeSg = model.getSizeSignalRef(size).signal;
  const scaleCmpt = model.getScaleComponent(channel);
  const scaleType2 = scaleCmpt?.get("type");
  const reversed = scaleCmpt?.get("reverse");
  const sign3 = !boundScales ? "" : channel === X3 ? reversed ? "" : "-" : reversed ? "-" : "";
  const extent2 = `${anchor}.extent_${channel}`;
  const offset6 = `${sign3}${delta}.${channel} / ${boundScales ? `${sizeSg}` : `span(${extent2})`}`;
  const panFn = !boundScales || !scaleCmpt ? "panLinear" : scaleType2 === "log" ? "panLog" : scaleType2 === "symlog" ? "panSymlog" : scaleType2 === "pow" ? "panPow" : "panLinear";
  const arg = !boundScales ? "" : scaleType2 === "pow" ? `, ${scaleCmpt.get("exponent") ?? 1}` : scaleType2 === "symlog" ? `, ${scaleCmpt.get("constant") ?? 1}` : "";
  const update3 = `${panFn}(${extent2}, ${offset6}${arg})`;
  signal.on.push({
    events: { signal: delta },
    update: boundScales ? update3 : `clampRange(${update3}, 0, ${sizeSg})`
  });
}
var ANCHOR = "_zoom_anchor";
var DELTA = "_zoom_delta";
var zoom2 = {
  defined: (selCmpt) => {
    return selCmpt.type === "interval" && selCmpt.zoom;
  },
  signals: (model, selCmpt, signals) => {
    const name = selCmpt.name;
    const boundScales = scaleBindings.defined(selCmpt);
    const delta = name + DELTA;
    const { x: x6, y: y6 } = selCmpt.project.hasChannel;
    const sx = stringValue(model.scaleName(X3));
    const sy = stringValue(model.scaleName(Y3));
    let events3 = eventSelector(selCmpt.zoom, "scope");
    if (!boundScales) {
      events3 = events3.map((e) => (e.markname = name + BRUSH, e));
    }
    signals.push({
      name: name + ANCHOR,
      on: [
        {
          events: events3,
          update: !boundScales ? `{x: x(unit), y: y(unit)}` : `{${[sx ? `x: invert(${sx}, x(unit))` : "", sy ? `y: invert(${sy}, y(unit))` : ""].filter((expr2) => expr2).join(", ")}}`
        }
      ]
    }, {
      name: delta,
      on: [
        {
          events: events3,
          force: true,
          update: "pow(1.001, event.deltaY * pow(16, event.deltaMode))"
        }
      ]
    });
    if (x6 !== void 0) {
      onDelta(model, selCmpt, x6, "width", signals);
    }
    if (y6 !== void 0) {
      onDelta(model, selCmpt, y6, "height", signals);
    }
    return signals;
  }
};
function onDelta(model, selCmpt, proj, size, signals) {
  const name = selCmpt.name;
  const channel = proj.channel;
  const boundScales = scaleBindings.defined(selCmpt);
  const signal = signals.find((s) => s.name === proj.signals[boundScales ? "data" : "visual"]);
  const sizeSg = model.getSizeSignalRef(size).signal;
  const scaleCmpt = model.getScaleComponent(channel);
  const scaleType2 = scaleCmpt?.get("type");
  const base = boundScales ? domain3(model, channel) : signal.name;
  const delta = name + DELTA;
  const anchor = `${name}${ANCHOR}.${channel}`;
  const zoomFn = !boundScales || !scaleCmpt ? "zoomLinear" : scaleType2 === "log" ? "zoomLog" : scaleType2 === "symlog" ? "zoomSymlog" : scaleType2 === "pow" ? "zoomPow" : "zoomLinear";
  const arg = !boundScales ? "" : scaleType2 === "pow" ? `, ${scaleCmpt.get("exponent") ?? 1}` : scaleType2 === "symlog" ? `, ${scaleCmpt.get("constant") ?? 1}` : "";
  const update3 = `${zoomFn}(${base}, ${anchor}, ${delta}${arg})`;
  signal.on.push({
    events: { signal: delta },
    update: boundScales ? update3 : `clampRange(${update3}, 0, ${sizeSg})`
  });
}
var STORE = "_store";
var TUPLE = "_tuple";
var MODIFY = "_modify";
var VL_SELECTION_RESOLVE = "vlSelectionResolve";
var selectionCompilers = [
  point$1,
  interval2,
  project2,
  toggle,
  // Bindings may disable direct manipulation.
  inputBindings,
  scaleBindings,
  legendBindings,
  clear,
  translate4,
  zoom2,
  nearest
];
function getFacetModel(model) {
  let parent = model.parent;
  while (parent) {
    if (isFacetModel(parent))
      break;
    parent = parent.parent;
  }
  return parent;
}
function unitName(model, { escape: escape2 } = { escape: true }) {
  let name = escape2 ? stringValue(model.name) : model.name;
  const facetModel = getFacetModel(model);
  if (facetModel) {
    const { facet } = facetModel;
    for (const channel of FACET_CHANNELS) {
      if (facet[channel]) {
        name += ` + '__facet_${channel}_' + (facet[${stringValue(facetModel.vgField(channel))}])`;
      }
    }
  }
  return name;
}
function requiresSelectionId(model) {
  return vals(model.component.selection ?? {}).reduce((identifier, selCmpt) => {
    return identifier || selCmpt.project.hasSelectionId;
  }, false);
}
function disableDirectManipulation(selCmpt, selDef) {
  if (isString(selDef.select) || !selDef.select.on)
    delete selCmpt.events;
  if (isString(selDef.select) || !selDef.select.clear)
    delete selCmpt.clear;
  if (isString(selDef.select) || !selDef.select.toggle)
    delete selCmpt.toggle;
}
function isTimerSelection(selCmpt) {
  return selCmpt.events?.find((e) => "type" in e && e.type === "timer");
}
function getName(node) {
  const name = [];
  if (node.type === "Identifier") {
    return [node.name];
  }
  if (node.type === "Literal") {
    return [node.value];
  }
  if (node.type === "MemberExpression") {
    name.push(...getName(node.object));
    name.push(...getName(node.property));
  }
  return name;
}
function startsWithDatum(node) {
  if (node.object.type === "MemberExpression") {
    return startsWithDatum(node.object);
  }
  return node.object.name === "datum";
}
function getDependentFields(expression4) {
  const ast = parser3(expression4);
  const dependents = /* @__PURE__ */ new Set();
  ast.visit((node) => {
    if (node.type === "MemberExpression" && startsWithDatum(node)) {
      dependents.add(getName(node).slice(1).join("."));
    }
  });
  return dependents;
}
var FilterNode = class _FilterNode extends DataFlowNode {
  model;
  filter;
  expr;
  _dependentFields;
  clone() {
    return new _FilterNode(null, this.model, duplicate(this.filter));
  }
  constructor(parent, model, filter4) {
    super(parent);
    this.model = model;
    this.filter = filter4;
    this.expr = expression2(this.model, this.filter, this);
    this._dependentFields = getDependentFields(this.expr);
  }
  dependentFields() {
    return this._dependentFields;
  }
  producedFields() {
    return /* @__PURE__ */ new Set();
  }
  assemble() {
    return {
      type: "filter",
      expr: this.expr
    };
  }
  hash() {
    return `Filter ${this.expr}`;
  }
};
function parseUnitSelection(model, selDefs) {
  const selCmpts = {};
  const selectionConfig = model.config.selection;
  if (!selDefs || !selDefs.length)
    return selCmpts;
  let nTimerSelections = 0;
  for (const def3 of selDefs) {
    const name = varName(def3.name);
    const selDef = def3.select;
    const type4 = isString(selDef) ? selDef : selDef.type;
    const defaults2 = isObject(selDef) ? duplicate(selDef) : { type: type4 };
    const cfg = selectionConfig[type4];
    for (const key2 in cfg) {
      if (key2 === "fields" || key2 === "encodings") {
        continue;
      }
      if (key2 === "mark") {
        defaults2.mark = { ...cfg.mark, ...defaults2.mark };
      }
      if (defaults2[key2] === void 0 || defaults2[key2] === true) {
        defaults2[key2] = duplicate(cfg[key2] ?? defaults2[key2]);
      }
    }
    const selCmpt = selCmpts[name] = {
      ...defaults2,
      name,
      type: type4,
      init: def3.value,
      bind: def3.bind,
      events: isString(defaults2.on) ? eventSelector(defaults2.on, "scope") : array_default(duplicate(defaults2.on))
    };
    if (isTimerSelection(selCmpt)) {
      nTimerSelections++;
      if (nTimerSelections > 1) {
        delete selCmpts[name];
        continue;
      }
    }
    const def_ = duplicate(def3);
    for (const c3 of selectionCompilers) {
      if (c3.defined(selCmpt) && c3.parse) {
        c3.parse(model, selCmpt, def_);
      }
    }
  }
  if (nTimerSelections > 1) {
    warn3(MULTIPLE_TIMER_ANIMATION_SELECTION);
  }
  return selCmpts;
}
function parseSelectionPredicate(model, pred, dfnode, datum3 = "datum") {
  const name = isString(pred) ? pred : pred.param;
  const vname = varName(name);
  const store = stringValue(vname + STORE);
  let selCmpt;
  try {
    selCmpt = model.getSelectionComponent(vname, name);
  } catch {
    return `!!${vname}`;
  }
  if (selCmpt.project.timeUnit) {
    const child = dfnode ?? model.component.data.raw;
    const tunode = selCmpt.project.timeUnit.clone();
    if (child.parent) {
      tunode.insertAsParentOf(child);
    } else {
      child.parent = tunode;
    }
  }
  const fn = selCmpt.project.hasSelectionId ? "vlSelectionIdTest(" : "vlSelectionTest(";
  const resolve2 = selCmpt.resolve === "global" ? ")" : `, ${stringValue(selCmpt.resolve)})`;
  const test2 = `${fn}${store}, ${datum3}${resolve2}`;
  const length3 = `length(data(${store}))`;
  return pred.empty === false ? `${length3} && ${test2}` : `!${length3} || ${test2}`;
}
function parseSelectionExtent(model, name, extent2) {
  const vname = varName(name);
  const encoding = extent2.encoding;
  let field3 = extent2.field;
  let selCmpt;
  try {
    selCmpt = model.getSelectionComponent(vname, name);
  } catch {
    return vname;
  }
  if (!encoding && !field3) {
    field3 = selCmpt.project.items[0].field;
    if (selCmpt.project.items.length > 1) {
      warn3(selectionAsScaleDomainWithoutField(field3));
    }
  } else if (encoding && !field3) {
    const encodings = selCmpt.project.items.filter((p) => p.channel === encoding);
    if (!encodings.length || encodings.length > 1) {
      field3 = selCmpt.project.items[0].field;
      warn3(selectionAsScaleDomainWrongEncodings(encodings, encoding, extent2, field3));
    } else {
      field3 = encodings[0].field;
    }
  }
  return `${selCmpt.name}[${stringValue(replacePathInField(field3))}]`;
}
function materializeSelections(model, main2) {
  for (const [selection, selCmpt] of entries$1(model.component.selection ?? {})) {
    const lookupName = model.getName(`lookup_${selection}`);
    model.component.data.outputNodes[lookupName] = selCmpt.materialized = new OutputNode(new FilterNode(main2, model, { param: selection }), lookupName, DataSourceType.Lookup, model.component.data.outputNodeRefCounts);
  }
}
function expression2(model, filterOp, node) {
  return logicalExpr(filterOp, (predicate) => {
    if (isString(predicate)) {
      return predicate;
    } else if (isSelectionPredicate(predicate)) {
      return parseSelectionPredicate(model, predicate, node);
    } else {
      return fieldFilterExpression(predicate);
    }
  });
}
function assembleTitle(title2, config) {
  if (!title2) {
    return void 0;
  }
  if (isArray(title2) && !isText(title2)) {
    return title2.map((fieldDef) => defaultTitle(fieldDef, config)).join(", ");
  }
  return title2;
}
function setAxisEncode(axis, part, vgProp, vgRef) {
  axis.encode ??= {};
  axis.encode[part] ??= {};
  axis.encode[part].update ??= {};
  axis.encode[part].update[vgProp] = vgRef;
}
function assembleAxis(axisCmpt, kind, config, opt = { header: false }) {
  const { disable, orient: orient2, scale: scale9, labelExpr, title: title2, zindex: zindex2, ...axis } = axisCmpt.combine();
  if (disable) {
    return void 0;
  }
  for (const p in axis) {
    const prop = p;
    const propType = AXIS_PROPERTY_TYPE[prop];
    const propValue = axis[prop];
    if (propType && propType !== kind && propType !== "both") {
      delete axis[prop];
    } else if (isConditionalAxisValue(propValue)) {
      const { condition, ...valueOrSignalRef } = propValue;
      const conditions = array_default(condition);
      const propIndex = CONDITIONAL_AXIS_PROP_INDEX[prop];
      if (propIndex) {
        const { vgProp, part } = propIndex;
        const vgRef = [
          ...conditions.map((c3) => {
            const { test: test2, ...valueOrSignalCRef } = c3;
            return {
              test: expression2(null, test2),
              ...valueOrSignalCRef
            };
          }),
          valueOrSignalRef
        ];
        setAxisEncode(axis, part, vgProp, vgRef);
        delete axis[prop];
      } else if (propIndex === null) {
        const signalRef = {
          signal: conditions.map((c3) => {
            const { test: test2, ...valueOrSignalCRef } = c3;
            return `${expression2(null, test2)} ? ${exprFromValueRefOrSignalRef(valueOrSignalCRef)} : `;
          }).join("") + exprFromValueRefOrSignalRef(valueOrSignalRef)
        };
        axis[prop] = signalRef;
      }
    } else if (isSignalRef(propValue)) {
      const propIndex = CONDITIONAL_AXIS_PROP_INDEX[prop];
      if (propIndex) {
        const { vgProp, part } = propIndex;
        setAxisEncode(axis, part, vgProp, propValue);
        delete axis[prop];
      }
    }
    if (contains2(["labelAlign", "labelBaseline"], prop) && axis[prop] === null) {
      delete axis[prop];
    }
  }
  if (kind === "grid") {
    if (!axis.grid) {
      return void 0;
    }
    if (axis.encode) {
      const { grid } = axis.encode;
      axis.encode = {
        ...grid ? { grid } : {}
      };
      if (isEmpty(axis.encode)) {
        delete axis.encode;
      }
    }
    return {
      scale: scale9,
      orient: orient2,
      ...axis,
      domain: false,
      labels: false,
      aria: false,
      // always hide grid axis
      // Always set min/maxExtent to 0 to ensure that `config.axis*.minExtent` and `config.axis*.maxExtent`
      // would not affect gridAxis
      maxExtent: 0,
      minExtent: 0,
      ticks: false,
      zindex: getFirstDefined(zindex2, 0)
      // put grid behind marks by default
    };
  } else {
    if (!opt.header && axisCmpt.mainExtracted) {
      return void 0;
    }
    if (labelExpr !== void 0) {
      let expr2 = labelExpr;
      if (axis.encode?.labels?.update && isSignalRef(axis.encode.labels.update.text)) {
        expr2 = replaceAll(labelExpr, "datum.label", axis.encode.labels.update.text.signal);
      }
      setAxisEncode(axis, "labels", "text", { signal: expr2 });
    }
    if (axis.labelAlign === null) {
      delete axis.labelAlign;
    }
    if (axis.encode) {
      for (const part of AXIS_PARTS) {
        if (!axisCmpt.hasAxisPart(part)) {
          delete axis.encode[part];
        }
      }
      if (isEmpty(axis.encode)) {
        delete axis.encode;
      }
    }
    const titleString = assembleTitle(title2, config);
    return {
      scale: scale9,
      orient: orient2,
      grid: false,
      ...titleString ? { title: titleString } : {},
      ...axis,
      ...config.aria === false ? { aria: false } : {},
      zindex: getFirstDefined(zindex2, 0)
      // put axis line above marks by default
    };
  }
}
function assembleAxisSignals(model) {
  const { axes } = model.component;
  const signals = [];
  for (const channel of POSITION_SCALE_CHANNELS) {
    if (axes[channel]) {
      for (const axis of axes[channel]) {
        if (!axis.get("disable") && !axis.get("gridScale")) {
          const sizeType = channel === "x" ? "height" : "width";
          const update3 = model.getSizeSignalRef(sizeType).signal;
          if (sizeType !== update3) {
            signals.push({
              name: sizeType,
              update: update3
            });
          }
        }
      }
    }
  }
  return signals;
}
function assembleAxes(axisComponents, config) {
  const { x: x6 = [], y: y6 = [] } = axisComponents;
  return [
    ...x6.map((a3) => assembleAxis(a3, "grid", config)),
    ...y6.map((a3) => assembleAxis(a3, "grid", config)),
    ...x6.map((a3) => assembleAxis(a3, "main", config)),
    ...y6.map((a3) => assembleAxis(a3, "main", config))
  ].filter((a3) => a3);
}
function getAxisConfigFromConfigTypes(configTypes, config, channel, orient2) {
  return Object.assign.apply(null, [
    {},
    ...configTypes.map((configType) => {
      if (configType === "axisOrient") {
        const orient1 = channel === "x" ? "bottom" : "left";
        const orientConfig1 = config[channel === "x" ? "axisBottom" : "axisLeft"] || {};
        const orientConfig2 = config[channel === "x" ? "axisTop" : "axisRight"] || {};
        const props = /* @__PURE__ */ new Set([...keys3(orientConfig1), ...keys3(orientConfig2)]);
        const conditionalOrientAxisConfig = {};
        for (const prop of props.values()) {
          conditionalOrientAxisConfig[prop] = {
            // orient is surely signal in this case
            signal: `${orient2["signal"]} === "${orient1}" ? ${signalOrStringValue(orientConfig1[prop])} : ${signalOrStringValue(orientConfig2[prop])}`
          };
        }
        return conditionalOrientAxisConfig;
      }
      return config[configType];
    })
  ]);
}
function getAxisConfigs(channel, scaleType2, orient2, config) {
  const typeBasedConfigTypes = scaleType2 === "band" ? ["axisDiscrete", "axisBand"] : scaleType2 === "point" ? ["axisDiscrete", "axisPoint"] : isQuantitative(scaleType2) ? ["axisQuantitative"] : scaleType2 === "time" || scaleType2 === "utc" ? ["axisTemporal"] : [];
  const axisChannel = channel === "x" ? "axisX" : "axisY";
  const axisOrient = isSignalRef(orient2) ? "axisOrient" : `axis${titleCase(orient2)}`;
  const vlOnlyConfigTypes = [
    // technically Vega does have axisBand, but if we make another separation here,
    // it will further introduce complexity in the code
    ...typeBasedConfigTypes,
    ...typeBasedConfigTypes.map((c3) => axisChannel + c3.substr(4))
  ];
  const vgConfigTypes = ["axis", axisOrient, axisChannel];
  return {
    vlOnlyAxisConfig: getAxisConfigFromConfigTypes(vlOnlyConfigTypes, config, channel, orient2),
    vgAxisConfig: getAxisConfigFromConfigTypes(vgConfigTypes, config, channel, orient2),
    axisConfigStyle: getAxisConfigStyle([...vgConfigTypes, ...vlOnlyConfigTypes], config)
  };
}
function getAxisConfigStyle(axisConfigTypes, config) {
  const toMerge = [{}];
  for (const configType of axisConfigTypes) {
    let style3 = config[configType]?.style;
    if (style3) {
      style3 = array_default(style3);
      for (const s of style3) {
        toMerge.push(config.style[s]);
      }
    }
  }
  return Object.assign.apply(null, toMerge);
}
function getAxisConfig(property2, styleConfigIndex, style3, axisConfigs = {}) {
  const styleConfig = getStyleConfig(property2, style3, styleConfigIndex);
  if (styleConfig !== void 0) {
    return {
      configFrom: "style",
      configValue: styleConfig
    };
  }
  for (const configFrom of ["vlOnlyAxisConfig", "vgAxisConfig", "axisConfigStyle"]) {
    if (axisConfigs[configFrom]?.[property2] !== void 0) {
      return { configFrom, configValue: axisConfigs[configFrom][property2] };
    }
  }
  return {};
}
var axisRules = {
  scale: ({ model, channel }) => model.scaleName(channel),
  format: ({ format: format6 }) => format6,
  // we already calculate this in parse
  formatType: ({ formatType }) => formatType,
  // we already calculate this in parse
  grid: ({ fieldOrDatumDef, axis, scaleType: scaleType2 }) => axis.grid ?? defaultGrid(scaleType2, fieldOrDatumDef),
  gridScale: ({ model, channel }) => gridScale(model, channel),
  labelAlign: ({ axis, labelAngle, orient: orient2, channel }) => axis.labelAlign || defaultLabelAlign(labelAngle, orient2, channel),
  labelAngle: ({ labelAngle }) => labelAngle,
  // we already calculate this in parse
  labelBaseline: ({ axis, labelAngle, orient: orient2, channel }) => axis.labelBaseline || defaultLabelBaseline(labelAngle, orient2, channel),
  labelFlush: ({ axis, fieldOrDatumDef, channel }) => axis.labelFlush ?? defaultLabelFlush(fieldOrDatumDef.type, channel),
  labelOverlap: ({ axis, fieldOrDatumDef, scaleType: scaleType2 }) => axis.labelOverlap ?? defaultLabelOverlap$1(fieldOrDatumDef.type, scaleType2, isFieldDef(fieldOrDatumDef) && !!fieldOrDatumDef.timeUnit, isFieldDef(fieldOrDatumDef) ? fieldOrDatumDef.sort : void 0),
  // we already calculate orient in parse
  orient: ({ orient: orient2 }) => orient2,
  // Need to cast until Vega supports signal
  tickCount: ({ channel, model, axis, fieldOrDatumDef, scaleType: scaleType2 }) => {
    const sizeType = channel === "x" ? "width" : channel === "y" ? "height" : void 0;
    const size = sizeType ? model.getSizeSignalRef(sizeType) : void 0;
    return axis.tickCount ?? defaultTickCount({ fieldOrDatumDef, scaleType: scaleType2, size, values: axis.values });
  },
  tickMinStep: ({ axis, format: format6, fieldOrDatumDef }) => axis.tickMinStep ?? defaultTickMinStep({ format: format6, fieldOrDatumDef }),
  title: ({ axis, model, channel }) => {
    if (axis.title !== void 0) {
      return axis.title;
    }
    const fieldDefTitle = getFieldDefTitle(model, channel);
    if (fieldDefTitle !== void 0) {
      return fieldDefTitle;
    }
    const fieldDef = model.typedFieldDef(channel);
    const channel2 = channel === "x" ? "x2" : "y2";
    const fieldDef2 = model.fieldDef(channel2);
    return mergeTitleFieldDefs(fieldDef ? [toFieldDefBase(fieldDef)] : [], isFieldDef(fieldDef2) ? [toFieldDefBase(fieldDef2)] : []);
  },
  values: ({ axis, fieldOrDatumDef }) => values$1(axis, fieldOrDatumDef),
  zindex: ({ axis, fieldOrDatumDef, mark }) => axis.zindex ?? defaultZindex(mark, fieldOrDatumDef)
};
function defaultGrid(scaleType2, fieldDef) {
  return !hasDiscreteDomain(scaleType2) && isFieldDef(fieldDef) && !isBinning(fieldDef?.bin) && !isBinned(fieldDef?.bin);
}
function gridScale(model, channel) {
  const gridChannel = channel === "x" ? "y" : "x";
  if (model.getScaleComponent(gridChannel)) {
    return model.scaleName(gridChannel);
  }
  return void 0;
}
function getLabelAngle(fieldOrDatumDef, axis, channel, styleConfig, axisConfigs) {
  const labelAngle = axis?.labelAngle;
  if (labelAngle !== void 0) {
    return isSignalRef(labelAngle) ? labelAngle : normalizeAngle(labelAngle);
  } else {
    const { configValue: angle2 } = getAxisConfig("labelAngle", styleConfig, axis?.style, axisConfigs);
    if (angle2 !== void 0) {
      return normalizeAngle(angle2);
    } else {
      if (channel === X3 && contains2([NOMINAL, ORDINAL], fieldOrDatumDef.type) && !(isFieldDef(fieldOrDatumDef) && fieldOrDatumDef.timeUnit)) {
        return 270;
      }
      return void 0;
    }
  }
}
function normalizeAngleExpr(angle2) {
  return `(((${angle2.signal} % 360) + 360) % 360)`;
}
function defaultLabelBaseline(angle2, orient2, channel, alwaysIncludeMiddle) {
  if (angle2 !== void 0) {
    if (channel === "x") {
      if (isSignalRef(angle2)) {
        const a3 = normalizeAngleExpr(angle2);
        const orientIsTop = isSignalRef(orient2) ? `(${orient2.signal} === "top")` : orient2 === "top";
        return {
          signal: `(45 < ${a3} && ${a3} < 135) || (225 < ${a3} && ${a3} < 315) ? "middle" :(${a3} <= 45 || 315 <= ${a3}) === ${orientIsTop} ? "bottom" : "top"`
        };
      }
      if (45 < angle2 && angle2 < 135 || 225 < angle2 && angle2 < 315) {
        return "middle";
      }
      if (isSignalRef(orient2)) {
        const op = angle2 <= 45 || 315 <= angle2 ? "===" : "!==";
        return { signal: `${orient2.signal} ${op} "top" ? "bottom" : "top"` };
      }
      return (angle2 <= 45 || 315 <= angle2) === (orient2 === "top") ? "bottom" : "top";
    } else {
      if (isSignalRef(angle2)) {
        const a3 = normalizeAngleExpr(angle2);
        const orientIsLeft = isSignalRef(orient2) ? `(${orient2.signal} === "left")` : orient2 === "left";
        const middle = alwaysIncludeMiddle ? '"middle"' : "null";
        return {
          signal: `${a3} <= 45 || 315 <= ${a3} || (135 <= ${a3} && ${a3} <= 225) ? ${middle} : (45 <= ${a3} && ${a3} <= 135) === ${orientIsLeft} ? "top" : "bottom"`
        };
      }
      if (angle2 <= 45 || 315 <= angle2 || 135 <= angle2 && angle2 <= 225) {
        return alwaysIncludeMiddle ? "middle" : null;
      }
      if (isSignalRef(orient2)) {
        const op = 45 <= angle2 && angle2 <= 135 ? "===" : "!==";
        return { signal: `${orient2.signal} ${op} "left" ? "top" : "bottom"` };
      }
      return (45 <= angle2 && angle2 <= 135) === (orient2 === "left") ? "top" : "bottom";
    }
  }
  return void 0;
}
function defaultLabelAlign(angle2, orient2, channel) {
  if (angle2 === void 0) {
    return void 0;
  }
  const isX2 = channel === "x";
  const startAngle = isX2 ? 0 : 90;
  const mainOrient = isX2 ? "bottom" : "left";
  if (isSignalRef(angle2)) {
    const a3 = normalizeAngleExpr(angle2);
    const orientIsMain = isSignalRef(orient2) ? `(${orient2.signal} === "${mainOrient}")` : orient2 === mainOrient;
    return {
      signal: `(${startAngle ? `(${a3} + 90)` : a3} % 180 === 0) ? ${isX2 ? null : '"center"'} :(${startAngle} < ${a3} && ${a3} < ${180 + startAngle}) === ${orientIsMain} ? "left" : "right"`
    };
  }
  if ((angle2 + startAngle) % 180 === 0) {
    return isX2 ? null : "center";
  }
  if (isSignalRef(orient2)) {
    const op = startAngle < angle2 && angle2 < 180 + startAngle ? "===" : "!==";
    const orientIsMain = `${orient2.signal} ${op} "${mainOrient}"`;
    return {
      signal: `${orientIsMain} ? "left" : "right"`
    };
  }
  if ((startAngle < angle2 && angle2 < 180 + startAngle) === (orient2 === mainOrient)) {
    return "left";
  }
  return "right";
}
function defaultLabelFlush(type4, channel) {
  if (channel === "x" && contains2(["quantitative", "temporal"], type4)) {
    return true;
  }
  return void 0;
}
function defaultLabelOverlap$1(type4, scaleType2, hasTimeUnit, sort4) {
  if (hasTimeUnit && !isObject(sort4) || type4 !== "nominal" && type4 !== "ordinal") {
    if (scaleType2 === "log" || scaleType2 === "symlog") {
      return "greedy";
    }
    return true;
  }
  return void 0;
}
function defaultOrient(channel) {
  return channel === "x" ? "bottom" : "left";
}
function defaultTickCount({ fieldOrDatumDef, scaleType: scaleType2, size, values: vals2 }) {
  if (!vals2 && !hasDiscreteDomain(scaleType2) && scaleType2 !== "log") {
    if (isFieldDef(fieldOrDatumDef)) {
      if (isBinning(fieldOrDatumDef.bin)) {
        return { signal: `ceil(${size.signal}/10)` };
      }
      if (fieldOrDatumDef.timeUnit && contains2(["month", "hours", "day", "quarter"], normalizeTimeUnit(fieldOrDatumDef.timeUnit)?.unit)) {
        return void 0;
      }
    }
    return { signal: `ceil(${size.signal}/40)` };
  }
  return void 0;
}
function defaultTickMinStep({ format: format6, fieldOrDatumDef }) {
  if (format6 === "d") {
    return 1;
  }
  if (isFieldDef(fieldOrDatumDef)) {
    const { timeUnit } = fieldOrDatumDef;
    if (timeUnit) {
      const signal = durationExpr(timeUnit);
      if (signal) {
        return { signal };
      }
    }
  }
  return void 0;
}
function getFieldDefTitle(model, channel) {
  const channel2 = channel === "x" ? "x2" : "y2";
  const fieldDef = model.fieldDef(channel);
  const fieldDef2 = model.fieldDef(channel2);
  const title1 = fieldDef ? fieldDef.title : void 0;
  const title2 = fieldDef2 ? fieldDef2.title : void 0;
  if (title1 && title2) {
    return mergeTitle(title1, title2);
  } else if (title1) {
    return title1;
  } else if (title2) {
    return title2;
  } else if (title1 !== void 0) {
    return title1;
  } else if (title2 !== void 0) {
    return title2;
  }
  return void 0;
}
function values$1(axis, fieldOrDatumDef) {
  const vals2 = axis.values;
  if (isArray(vals2)) {
    return valueArray(fieldOrDatumDef, vals2);
  } else if (isSignalRef(vals2)) {
    return vals2;
  }
  return void 0;
}
function defaultZindex(mark, fieldDef) {
  if (mark === "rect" && isDiscrete2(fieldDef)) {
    return 1;
  }
  return 0;
}
var CalculateNode = class _CalculateNode extends DataFlowNode {
  transform;
  _dependentFields;
  clone() {
    return new _CalculateNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
    this._dependentFields = getDependentFields(this.transform.calculate);
  }
  static parseAllForSortIndex(parent, model) {
    model.forEachFieldDef((fieldDef, channel) => {
      if (!isScaleFieldDef(fieldDef)) {
        return;
      }
      if (isSortArray(fieldDef.sort)) {
        const { field: field3, timeUnit } = fieldDef;
        const sort4 = fieldDef.sort;
        const calculate = sort4.map((sortValue, i) => {
          return `${fieldFilterExpression({ field: field3, timeUnit, equal: sortValue })} ? ${i} : `;
        }).join("") + sort4.length;
        parent = new _CalculateNode(parent, {
          calculate,
          as: sortArrayIndexField(fieldDef, channel, { forAs: true })
        });
      }
    });
    return parent;
  }
  producedFields() {
    return /* @__PURE__ */ new Set([this.transform.as]);
  }
  dependentFields() {
    return this._dependentFields;
  }
  assemble() {
    return {
      type: "formula",
      expr: this.transform.calculate,
      as: this.transform.as
    };
  }
  hash() {
    return `Calculate ${hash(this.transform)}`;
  }
};
function sortArrayIndexField(fieldDef, channel, opt) {
  return vgField(fieldDef, { prefix: channel, suffix: "sort_index", ...opt });
}
function getHeaderChannel(channel, orient2) {
  if (contains2(["top", "bottom"], orient2)) {
    return "column";
  } else if (contains2(["left", "right"], orient2)) {
    return "row";
  }
  return channel === "row" ? "row" : "column";
}
function getHeaderProperty(prop, header, config, channel) {
  const headerSpecificConfig = channel === "row" ? config.headerRow : channel === "column" ? config.headerColumn : config.headerFacet;
  return getFirstDefined((header || {})[prop], headerSpecificConfig[prop], config.header[prop]);
}
function getHeaderProperties(properties, header, config, channel) {
  const props = {};
  for (const prop of properties) {
    const value4 = getHeaderProperty(prop, header || {}, config, channel);
    if (value4 !== void 0) {
      props[prop] = value4;
    }
  }
  return props;
}
var HEADER_CHANNELS = ["row", "column"];
var HEADER_TYPES = ["header", "footer"];
function assembleTitleGroup(model, channel) {
  const title2 = model.component.layoutHeaders[channel].title;
  const config = model.config ? model.config : void 0;
  const facetFieldDef = model.component.layoutHeaders[channel].facetFieldDef ? model.component.layoutHeaders[channel].facetFieldDef : void 0;
  const { titleAnchor, titleAngle: ta, titleOrient } = getHeaderProperties(["titleAnchor", "titleAngle", "titleOrient"], facetFieldDef.header, config, channel);
  const headerChannel = getHeaderChannel(channel, titleOrient);
  const titleAngle = normalizeAngle(ta);
  return {
    name: `${channel}-title`,
    type: "group",
    role: `${headerChannel}-title`,
    title: {
      text: title2,
      ...channel === "row" ? { orient: "left" } : {},
      style: "guide-title",
      ...defaultHeaderGuideBaseline(titleAngle, headerChannel),
      ...defaultHeaderGuideAlign(headerChannel, titleAngle, titleAnchor),
      ...assembleHeaderProperties(config, facetFieldDef, channel, HEADER_TITLE_PROPERTIES, HEADER_TITLE_PROPERTIES_MAP)
    }
  };
}
function defaultHeaderGuideAlign(headerChannel, angle2, anchor = "middle") {
  switch (anchor) {
    case "start":
      return { align: "left" };
    case "end":
      return { align: "right" };
  }
  const align2 = defaultLabelAlign(angle2, headerChannel === "row" ? "left" : "top", headerChannel === "row" ? "y" : "x");
  return align2 ? { align: align2 } : {};
}
function defaultHeaderGuideBaseline(angle2, channel) {
  const baseline3 = defaultLabelBaseline(angle2, channel === "row" ? "left" : "top", channel === "row" ? "y" : "x", true);
  return baseline3 ? { baseline: baseline3 } : {};
}
function assembleHeaderGroups(model, channel) {
  const layoutHeader = model.component.layoutHeaders[channel];
  const groups = [];
  for (const headerType of HEADER_TYPES) {
    if (layoutHeader[headerType]) {
      for (const headerComponent of layoutHeader[headerType]) {
        const group3 = assembleHeaderGroup(model, channel, headerType, layoutHeader, headerComponent);
        if (group3 != null) {
          groups.push(group3);
        }
      }
    }
  }
  return groups;
}
function getSort$1(facetFieldDef, channel) {
  const { sort: sort4 } = facetFieldDef;
  if (isSortField(sort4)) {
    return {
      field: vgField(sort4, { expr: "datum" }),
      order: sort4.order ?? "ascending"
    };
  } else if (isArray(sort4)) {
    return {
      field: sortArrayIndexField(facetFieldDef, channel, { expr: "datum" }),
      order: "ascending"
    };
  } else {
    return {
      field: vgField(facetFieldDef, { expr: "datum" }),
      order: sort4 ?? "ascending"
    };
  }
}
function assembleLabelTitle(facetFieldDef, channel, config) {
  const { format: format6, formatType, labelAngle, labelAnchor, labelOrient, labelExpr } = getHeaderProperties(["format", "formatType", "labelAngle", "labelAnchor", "labelOrient", "labelExpr"], facetFieldDef.header, config, channel);
  const titleTextExpr = formatSignalRef({
    fieldOrDatumDef: facetFieldDef,
    format: format6,
    formatType,
    expr: "parent",
    config
  }).signal;
  const headerChannel = getHeaderChannel(channel, labelOrient);
  return {
    text: {
      signal: labelExpr ? replaceAll(replaceAll(labelExpr, "datum.label", titleTextExpr), "datum.value", vgField(facetFieldDef, { expr: "parent" })) : titleTextExpr
    },
    ...channel === "row" ? { orient: "left" } : {},
    style: "guide-label",
    frame: "group",
    ...defaultHeaderGuideBaseline(labelAngle, headerChannel),
    ...defaultHeaderGuideAlign(headerChannel, labelAngle, labelAnchor),
    ...assembleHeaderProperties(config, facetFieldDef, channel, HEADER_LABEL_PROPERTIES, HEADER_LABEL_PROPERTIES_MAP)
  };
}
function assembleHeaderGroup(model, channel, headerType, layoutHeader, headerComponent) {
  if (headerComponent) {
    let title2 = null;
    const { facetFieldDef } = layoutHeader;
    const config = model.config ? model.config : void 0;
    if (facetFieldDef && headerComponent.labels) {
      const { labelOrient } = getHeaderProperties(["labelOrient"], facetFieldDef.header, config, channel);
      if (channel === "row" && !contains2(["top", "bottom"], labelOrient) || channel === "column" && !contains2(["left", "right"], labelOrient)) {
        title2 = assembleLabelTitle(facetFieldDef, channel, config);
      }
    }
    const isFacetWithoutRowCol = isFacetModel(model) && !isFacetMapping(model.facet);
    const axes = headerComponent.axes;
    const hasAxes = axes?.length > 0;
    if (title2 || hasAxes) {
      const sizeChannel = channel === "row" ? "height" : "width";
      return {
        name: model.getName(`${channel}_${headerType}`),
        type: "group",
        role: `${channel}-${headerType}`,
        ...layoutHeader.facetFieldDef ? {
          from: { data: model.getName(`${channel}_domain`) },
          sort: getSort$1(facetFieldDef, channel)
        } : {},
        ...hasAxes && isFacetWithoutRowCol ? {
          from: { data: model.getName(`facet_domain_${channel}`) }
        } : {},
        ...title2 ? { title: title2 } : {},
        ...headerComponent.sizeSignal ? {
          encode: {
            update: {
              [sizeChannel]: headerComponent.sizeSignal
            }
          }
        } : {},
        ...hasAxes ? { axes } : {}
      };
    }
  }
  return null;
}
var LAYOUT_TITLE_BAND = {
  column: {
    start: 0,
    end: 1
  },
  row: {
    start: 1,
    end: 0
  }
};
function getLayoutTitleBand(titleAnchor, headerChannel) {
  return LAYOUT_TITLE_BAND[headerChannel][titleAnchor];
}
function assembleLayoutTitleBand(headerComponentIndex, config) {
  const titleBand = {};
  for (const channel of FACET_CHANNELS) {
    const headerComponent = headerComponentIndex[channel];
    if (headerComponent?.facetFieldDef) {
      const { titleAnchor, titleOrient } = getHeaderProperties(["titleAnchor", "titleOrient"], headerComponent.facetFieldDef.header, config, channel);
      const headerChannel = getHeaderChannel(channel, titleOrient);
      const band2 = getLayoutTitleBand(titleAnchor, headerChannel);
      if (band2 !== void 0) {
        titleBand[headerChannel] = band2;
      }
    }
  }
  return isEmpty(titleBand) ? void 0 : titleBand;
}
function assembleHeaderProperties(config, facetFieldDef, channel, properties, propertiesMap) {
  const props = {};
  for (const prop of properties) {
    if (!propertiesMap[prop]) {
      continue;
    }
    const value4 = getHeaderProperty(prop, facetFieldDef?.header, config, channel);
    if (value4 !== void 0) {
      props[propertiesMap[prop]] = value4;
    }
  }
  return props;
}
function assembleLayoutSignals(model) {
  return [
    ...sizeSignals(model, "width"),
    ...sizeSignals(model, "height"),
    ...sizeSignals(model, "childWidth"),
    ...sizeSignals(model, "childHeight")
  ];
}
function sizeSignals(model, sizeType) {
  const channel = sizeType === "width" ? "x" : "y";
  const size = model.component.layoutSize.get(sizeType);
  if (size == null || size === "merged") {
    return [];
  }
  const name = model.getSizeSignalRef(sizeType).signal;
  if (size === "step") {
    const scaleComponent = model.getScaleComponent(channel);
    if (scaleComponent) {
      const type4 = scaleComponent.get("type");
      const range7 = scaleComponent.get("range");
      if (hasDiscreteDomain(type4) && isVgRangeStep(range7)) {
        const scaleName = model.scaleName(channel);
        if (isFacetModel(model.parent)) {
          const parentResolve = model.parent.component.resolve;
          if (parentResolve.scale[channel] === "independent") {
            return [stepSignal(scaleName, range7)];
          }
        }
        return [
          stepSignal(scaleName, range7),
          {
            name,
            update: sizeExpr(scaleName, scaleComponent, `domain('${scaleName}').length`)
          }
        ];
      }
    }
    throw new Error("layout size is step although width/height is not step.");
  } else if (size == "container") {
    const isWidth = name.endsWith("width");
    const expr2 = isWidth ? "containerSize()[0]" : "containerSize()[1]";
    const defaultValue = getViewConfigContinuousSize(model.config.view, isWidth ? "width" : "height");
    const safeExpr = `isFinite(${expr2}) ? ${expr2} : ${defaultValue}`;
    return [{ name, init: safeExpr, on: [{ update: safeExpr, events: "window:resize" }] }];
  } else {
    return [
      {
        name,
        value: size
      }
    ];
  }
}
function stepSignal(scaleName, range7) {
  const name = `${scaleName}_step`;
  if (isSignalRef(range7.step)) {
    return { name, update: range7.step.signal };
  } else {
    return { name, value: range7.step };
  }
}
function sizeExpr(scaleName, scaleComponent, cardinality) {
  const type4 = scaleComponent.get("type");
  const padding3 = scaleComponent.get("padding");
  const paddingOuter2 = getFirstDefined(scaleComponent.get("paddingOuter"), padding3);
  let paddingInner2 = scaleComponent.get("paddingInner");
  paddingInner2 = type4 === "band" ? (
    // only band has real paddingInner
    paddingInner2 !== void 0 ? paddingInner2 : padding3
  ) : (
    // For point, as calculated in https://github.com/vega/vega-scale/blob/master/src/band.js#L128,
    // it's equivalent to have paddingInner = 1 since there is only n-1 steps between n points.
    1
  );
  return `bandspace(${cardinality}, ${signalOrStringValue(paddingInner2)}, ${signalOrStringValue(paddingOuter2)}) * ${scaleName}_step`;
}
function getSizeTypeFromLayoutSizeType(layoutSizeType) {
  return layoutSizeType === "childWidth" ? "width" : layoutSizeType === "childHeight" ? "height" : layoutSizeType;
}
function guideEncodeEntry(encoding, model) {
  return keys3(encoding).reduce((encode3, channel) => {
    return {
      ...encode3,
      ...wrapCondition({
        model,
        channelDef: encoding[channel],
        vgChannel: channel,
        mainRefFn: (def3) => signalOrValueRef(def3.value),
        invalidValueRef: void 0
        // guide encoding won't show invalid values for the scale
      })
    };
  }, {});
}
function defaultScaleResolve(channel, model) {
  if (isFacetModel(model)) {
    return channel === "theta" ? "independent" : "shared";
  } else if (isLayerModel(model)) {
    return "shared";
  } else if (isConcatModel(model)) {
    return isXorY(channel) || channel === "theta" || channel === "radius" ? "independent" : "shared";
  }
  throw new Error("invalid model type for resolve");
}
function parseGuideResolve(resolve2, channel) {
  const channelScaleResolve = resolve2.scale[channel];
  const guide = isXorY(channel) ? "axis" : "legend";
  if (channelScaleResolve === "independent") {
    if (resolve2[guide][channel] === "shared") {
      warn3(independentScaleMeansIndependentGuide(channel));
    }
    return "independent";
  }
  return resolve2[guide][channel] || "shared";
}
var LEGEND_COMPONENT_PROPERTY_INDEX = {
  ...COMMON_LEGEND_PROPERTY_INDEX,
  disable: 1,
  labelExpr: 1,
  selections: 1,
  // channel scales
  opacity: 1,
  shape: 1,
  stroke: 1,
  fill: 1,
  size: 1,
  strokeWidth: 1,
  strokeDash: 1,
  // encode
  encode: 1
};
var LEGEND_COMPONENT_PROPERTIES = keys3(LEGEND_COMPONENT_PROPERTY_INDEX);
var LegendComponent = class extends Split {
};
var legendEncodeRules = {
  symbols: symbols4,
  gradient: gradient4,
  labels: labels$1,
  entries
};
function symbols4(symbolsSpec, { fieldOrDatumDef, model, channel, legendCmpt, legendType: legendType2 }) {
  if (legendType2 !== "symbol") {
    return void 0;
  }
  const { markDef, encoding, config, mark } = model;
  const filled = markDef.filled && mark !== "trail";
  let out = {
    ...applyMarkConfig({}, model, FILL_STROKE_CONFIG),
    ...color5(model, { filled })
  };
  const symbolOpacity = legendCmpt.get("symbolOpacity") ?? config.legend.symbolOpacity;
  const symbolFillColor = legendCmpt.get("symbolFillColor") ?? config.legend.symbolFillColor;
  const symbolStrokeColor = legendCmpt.get("symbolStrokeColor") ?? config.legend.symbolStrokeColor;
  const opacity2 = symbolOpacity === void 0 ? getMaxValue(encoding.opacity) ?? markDef.opacity : void 0;
  if (out.fill) {
    if (channel === "fill" || filled && channel === COLOR) {
      delete out.fill;
    } else if (hasProperty(out.fill, "field")) {
      if (symbolFillColor) {
        delete out.fill;
      } else {
        out.fill = signalOrValueRef(config.legend.symbolBaseFillColor ?? "black");
        out.fillOpacity = signalOrValueRef(opacity2 ?? 1);
      }
    } else if (isArray(out.fill)) {
      const fill3 = getFirstConditionValue(encoding.fill ?? encoding.color) ?? markDef.fill ?? (filled && markDef.color);
      if (fill3) {
        out.fill = signalOrValueRef(fill3);
      }
    }
  }
  if (out.stroke) {
    if (channel === "stroke" || !filled && channel === COLOR) {
      delete out.stroke;
    } else if (hasProperty(out.stroke, "field") || symbolStrokeColor) {
      delete out.stroke;
    } else if (isArray(out.stroke)) {
      const stroke3 = getFirstDefined(getFirstConditionValue(encoding.stroke || encoding.color), markDef.stroke, filled ? markDef.color : void 0);
      if (stroke3) {
        out.stroke = { value: stroke3 };
      }
    }
  }
  if (channel !== OPACITY) {
    const condition = isFieldDef(fieldOrDatumDef) && selectedCondition(model, legendCmpt, fieldOrDatumDef);
    if (condition) {
      out.opacity = [
        { test: condition, ...signalOrValueRef(opacity2 ?? 1) },
        signalOrValueRef(config.legend.unselectedOpacity)
      ];
    } else if (opacity2) {
      out.opacity = signalOrValueRef(opacity2);
    }
  }
  out = { ...out, ...symbolsSpec };
  return isEmpty(out) ? void 0 : out;
}
function gradient4(gradientSpec, { model, legendType: legendType2, legendCmpt }) {
  if (legendType2 !== "gradient") {
    return void 0;
  }
  const { config, markDef, encoding } = model;
  let out = {};
  const gradientOpacity = legendCmpt.get("gradientOpacity") ?? config.legend.gradientOpacity;
  const opacity2 = gradientOpacity === void 0 ? getMaxValue(encoding.opacity) || markDef.opacity : void 0;
  if (opacity2) {
    out.opacity = signalOrValueRef(opacity2);
  }
  out = { ...out, ...gradientSpec };
  return isEmpty(out) ? void 0 : out;
}
function labels$1(specifiedlabelsSpec, { fieldOrDatumDef, model, channel, legendCmpt }) {
  const legend = model.legend(channel) || {};
  const config = model.config;
  const condition = isFieldDef(fieldOrDatumDef) ? selectedCondition(model, legendCmpt, fieldOrDatumDef) : void 0;
  const opacity2 = condition ? [{ test: condition, value: 1 }, { value: config.legend.unselectedOpacity }] : void 0;
  const { format: format6, formatType } = legend;
  let text4 = void 0;
  if (isCustomFormatType(formatType)) {
    text4 = formatCustomType({
      fieldOrDatumDef,
      field: "datum.value",
      format: format6,
      formatType,
      config
    });
  } else if (format6 === void 0 && formatType === void 0 && config.customFormatTypes) {
    if (fieldOrDatumDef.type === "quantitative" && config.numberFormatType) {
      text4 = formatCustomType({
        fieldOrDatumDef,
        field: "datum.value",
        format: config.numberFormat,
        formatType: config.numberFormatType,
        config
      });
    } else if (fieldOrDatumDef.type === "temporal" && config.timeFormatType && isFieldDef(fieldOrDatumDef) && fieldOrDatumDef.timeUnit === void 0) {
      text4 = formatCustomType({
        fieldOrDatumDef,
        field: "datum.value",
        format: config.timeFormat,
        formatType: config.timeFormatType,
        config
      });
    }
  }
  const labelsSpec = {
    ...opacity2 ? { opacity: opacity2 } : {},
    ...text4 ? { text: text4 } : {},
    ...specifiedlabelsSpec
  };
  return isEmpty(labelsSpec) ? void 0 : labelsSpec;
}
function entries(entriesSpec, { legendCmpt }) {
  const selections = legendCmpt.get("selections");
  return selections?.length ? { ...entriesSpec, fill: { value: "transparent" } } : entriesSpec;
}
function getMaxValue(channelDef) {
  return getConditionValue(channelDef, (v2, conditionalDef) => Math.max(v2, conditionalDef.value));
}
function getFirstConditionValue(channelDef) {
  return getConditionValue(channelDef, (v2, conditionalDef) => {
    return getFirstDefined(v2, conditionalDef.value);
  });
}
function getConditionValue(channelDef, reducer) {
  if (hasConditionalValueDef(channelDef)) {
    return array_default(channelDef.condition).reduce(reducer, channelDef.value);
  } else if (isValueDef(channelDef)) {
    return channelDef.value;
  }
  return void 0;
}
function selectedCondition(model, legendCmpt, fieldDef) {
  const selections = legendCmpt.get("selections");
  if (!selections?.length)
    return void 0;
  const field3 = stringValue(fieldDef.field);
  return selections.map((name) => {
    const store = stringValue(varName(name) + STORE);
    return `(!length(data(${store})) || (${name}[${field3}] && indexof(${name}[${field3}], datum.value) >= 0))`;
  }).join(" || ");
}
var legendRules = {
  direction: ({ direction }) => direction,
  format: ({ fieldOrDatumDef, legend, config }) => {
    const { format: format6, formatType } = legend;
    return guideFormat(fieldOrDatumDef, fieldOrDatumDef.type, format6, formatType, config, false);
  },
  formatType: ({ legend, fieldOrDatumDef, scaleType: scaleType2 }) => {
    const { formatType } = legend;
    return guideFormatType(formatType, fieldOrDatumDef, scaleType2);
  },
  gradientLength: (params2) => {
    const { legend, legendConfig } = params2;
    return legend.gradientLength ?? legendConfig.gradientLength ?? defaultGradientLength(params2);
  },
  labelOverlap: ({ legend, legendConfig, scaleType: scaleType2 }) => legend.labelOverlap ?? legendConfig.labelOverlap ?? defaultLabelOverlap(scaleType2),
  symbolType: ({ legend, markDef, channel, encoding }) => legend.symbolType ?? defaultSymbolType(markDef.type, channel, encoding.shape, markDef.shape),
  title: ({ fieldOrDatumDef, config }) => title(fieldOrDatumDef, config, { allowDisabling: true }),
  type: ({ legendType: legendType2, scaleType: scaleType2, channel }) => {
    if (isColorChannel(channel) && isContinuousToContinuous(scaleType2)) {
      if (legendType2 === "gradient") {
        return void 0;
      }
    } else if (legendType2 === "symbol") {
      return void 0;
    }
    return legendType2;
  },
  // depended by other property, let's define upfront
  values: ({ fieldOrDatumDef, legend }) => values3(legend, fieldOrDatumDef)
};
function values3(legend, fieldOrDatumDef) {
  const vals2 = legend.values;
  if (isArray(vals2)) {
    return valueArray(fieldOrDatumDef, vals2);
  } else if (isSignalRef(vals2)) {
    return vals2;
  }
  return void 0;
}
function defaultSymbolType(mark, channel, shapeChannelDef, markShape) {
  if (channel !== "shape") {
    const shape3 = getFirstConditionValue(shapeChannelDef) ?? markShape;
    if (shape3) {
      return shape3;
    }
  }
  switch (mark) {
    case "bar":
    case "rect":
    case "image":
    case "square":
      return "square";
    case "line":
    case "trail":
    case "rule":
      return "stroke";
    case "arc":
    case "point":
    case "circle":
    case "tick":
    case "geoshape":
    case "area":
    case "text":
      return "circle";
  }
}
function getLegendType(params2) {
  const { legend } = params2;
  return getFirstDefined(legend.type, defaultType$1(params2));
}
function defaultType$1({ channel, timeUnit, scaleType: scaleType2 }) {
  if (isColorChannel(channel)) {
    if (contains2(["quarter", "month", "day"], timeUnit)) {
      return "symbol";
    }
    if (isContinuousToContinuous(scaleType2)) {
      return "gradient";
    }
  }
  return "symbol";
}
function getDirection({ legendConfig, legendType: legendType2, orient: orient2, legend }) {
  return legend.direction ?? legendConfig[legendType2 ? "gradientDirection" : "symbolDirection"] ?? defaultDirection(orient2, legendType2);
}
function defaultDirection(orient2, legendType2) {
  switch (orient2) {
    case "top":
    case "bottom":
      return "horizontal";
    case "left":
    case "right":
    case "none":
    case void 0:
      return void 0;
    // vertical is Vega's default
    default:
      return legendType2 === "gradient" ? "horizontal" : void 0;
  }
}
function defaultGradientLength({ legendConfig, model, direction, orient: orient2, scaleType: scaleType2 }) {
  const { gradientHorizontalMaxLength, gradientHorizontalMinLength, gradientVerticalMaxLength, gradientVerticalMinLength } = legendConfig;
  if (isContinuousToContinuous(scaleType2)) {
    if (direction === "horizontal") {
      if (orient2 === "top" || orient2 === "bottom") {
        return gradientLengthSignal(model, "width", gradientHorizontalMinLength, gradientHorizontalMaxLength);
      } else {
        return gradientHorizontalMinLength;
      }
    } else {
      return gradientLengthSignal(model, "height", gradientVerticalMinLength, gradientVerticalMaxLength);
    }
  }
  return void 0;
}
function gradientLengthSignal(model, sizeType, min4, max4) {
  const sizeSignal = model.getSizeSignalRef(sizeType).signal;
  return { signal: `clamp(${sizeSignal}, ${min4}, ${max4})` };
}
function defaultLabelOverlap(scaleType2) {
  if (contains2(["quantile", "threshold", "log", "symlog"], scaleType2)) {
    return "greedy";
  }
  return void 0;
}
function parseLegend2(model) {
  const legendComponent = isUnitModel(model) ? parseUnitLegend(model) : parseNonUnitLegend(model);
  model.component.legends = legendComponent;
  return legendComponent;
}
function parseUnitLegend(model) {
  const { encoding } = model;
  const legendComponent = {};
  for (const channel of [COLOR, ...LEGEND_SCALE_CHANNELS]) {
    const def3 = getFieldOrDatumDef(encoding[channel]);
    if (!def3 || !model.getScaleComponent(channel)) {
      continue;
    }
    if (channel === SHAPE && isFieldDef(def3) && def3.type === GEOJSON) {
      continue;
    }
    legendComponent[channel] = parseLegendForChannel(model, channel);
  }
  return legendComponent;
}
function getLegendDefWithScale(model, channel) {
  const scale9 = model.scaleName(channel);
  if (model.mark === "trail") {
    if (channel === "color") {
      return { stroke: scale9 };
    } else if (channel === "size") {
      return { strokeWidth: scale9 };
    }
  }
  if (channel === "color") {
    return model.markDef.filled ? { fill: scale9 } : { stroke: scale9 };
  }
  return { [channel]: scale9 };
}
function isExplicit$1(value4, property2, legend, fieldDef) {
  switch (property2) {
    case "disable":
      return legend !== void 0;
    // if axis is specified or null/false, then its enable/disable state is explicit
    case "values":
      return !!legend?.values;
    case "title":
      if (property2 === "title" && value4 === fieldDef?.title) {
        return true;
      }
  }
  return value4 === (legend || {})[property2];
}
function parseLegendForChannel(model, channel) {
  let legend = model.legend(channel);
  const { markDef, encoding, config } = model;
  const legendConfig = config.legend;
  const legendCmpt = new LegendComponent({}, getLegendDefWithScale(model, channel));
  parseInteractiveLegend(model, channel, legendCmpt);
  const disable = legend !== void 0 ? !legend : legendConfig.disable;
  legendCmpt.set("disable", disable, legend !== void 0);
  if (disable) {
    return legendCmpt;
  }
  legend = legend || {};
  const scaleType2 = model.getScaleComponent(channel).get("type");
  const fieldOrDatumDef = getFieldOrDatumDef(encoding[channel]);
  const timeUnit = isFieldDef(fieldOrDatumDef) ? normalizeTimeUnit(fieldOrDatumDef.timeUnit)?.unit : void 0;
  const orient2 = legend.orient || config.legend.orient || "right";
  const legendType2 = getLegendType({ legend, channel, timeUnit, scaleType: scaleType2 });
  const direction = getDirection({ legend, legendType: legendType2, orient: orient2, legendConfig });
  const ruleParams = {
    legend,
    channel,
    model,
    markDef,
    encoding,
    fieldOrDatumDef,
    legendConfig,
    config,
    scaleType: scaleType2,
    orient: orient2,
    legendType: legendType2,
    direction
  };
  for (const property2 of LEGEND_COMPONENT_PROPERTIES) {
    if (legendType2 === "gradient" && property2.startsWith("symbol") || legendType2 === "symbol" && property2.startsWith("gradient")) {
      continue;
    }
    const value4 = property2 in legendRules ? legendRules[property2](ruleParams) : legend[property2];
    if (value4 !== void 0) {
      const explicit = isExplicit$1(value4, property2, legend, model.fieldDef(channel));
      if (explicit || config.legend[property2] === void 0) {
        legendCmpt.set(property2, value4, explicit);
      }
    }
  }
  const legendEncoding = legend?.encoding ?? {};
  const selections = legendCmpt.get("selections");
  const legendEncode = {};
  const legendEncodeParams = { fieldOrDatumDef, model, channel, legendCmpt, legendType: legendType2 };
  for (const part of ["labels", "legend", "title", "symbols", "gradient", "entries"]) {
    const legendEncodingPart = guideEncodeEntry(legendEncoding[part] ?? {}, model);
    const value4 = part in legendEncodeRules ? legendEncodeRules[part](legendEncodingPart, legendEncodeParams) : legendEncodingPart;
    if (value4 !== void 0 && !isEmpty(value4)) {
      legendEncode[part] = {
        ...selections?.length && isFieldDef(fieldOrDatumDef) ? { name: `${varName(fieldOrDatumDef.field)}_legend_${part}` } : {},
        ...selections?.length ? { interactive: true } : {},
        update: selections?.length ? { ...value4, cursor: { value: "pointer" } } : value4
      };
    }
  }
  if (!isEmpty(legendEncode)) {
    legendCmpt.set("encode", legendEncode, !!legend?.encoding);
  }
  return legendCmpt;
}
function parseNonUnitLegend(model) {
  const { legends, resolve: resolve2 } = model.component;
  for (const child of model.children) {
    parseLegend2(child);
    for (const channel of keys3(child.component.legends)) {
      resolve2.legend[channel] = parseGuideResolve(model.component.resolve, channel);
      if (resolve2.legend[channel] === "shared") {
        legends[channel] = mergeLegendComponent(legends[channel], child.component.legends[channel]);
        if (!legends[channel]) {
          resolve2.legend[channel] = "independent";
          delete legends[channel];
        }
      }
    }
  }
  for (const channel of keys3(legends)) {
    for (const child of model.children) {
      if (!child.component.legends[channel]) {
        continue;
      }
      if (resolve2.legend[channel] === "shared") {
        delete child.component.legends[channel];
      }
    }
  }
  return legends;
}
function mergeLegendComponent(mergedLegend, childLegend) {
  if (!mergedLegend) {
    return childLegend.clone();
  }
  const mergedOrient = mergedLegend.getWithExplicit("orient");
  const childOrient = childLegend.getWithExplicit("orient");
  if (mergedOrient.explicit && childOrient.explicit && mergedOrient.value !== childOrient.value) {
    return void 0;
  }
  let typeMerged = false;
  for (const prop of LEGEND_COMPONENT_PROPERTIES) {
    const mergedValueWithExplicit = mergeValuesWithExplicit(
      mergedLegend.getWithExplicit(prop),
      childLegend.getWithExplicit(prop),
      prop,
      "legend",
      // Tie breaker function
      (v1, v2) => {
        switch (prop) {
          case "symbolType":
            return mergeSymbolType(v1, v2);
          case "title":
            return mergeTitleComponent(v1, v2);
          case "type":
            typeMerged = true;
            return makeImplicit("symbol");
        }
        return defaultTieBreaker(v1, v2, prop, "legend");
      }
    );
    mergedLegend.setWithExplicit(prop, mergedValueWithExplicit);
  }
  if (typeMerged) {
    if (mergedLegend.implicit?.encode?.gradient) {
      deleteNestedProperty(mergedLegend.implicit, ["encode", "gradient"]);
    }
    if (mergedLegend.explicit?.encode?.gradient) {
      deleteNestedProperty(mergedLegend.explicit, ["encode", "gradient"]);
    }
  }
  return mergedLegend;
}
function mergeSymbolType(st1, st2) {
  if (st2.value === "circle") {
    return st2;
  }
  return st1;
}
function rangeFormula(model, fieldDef, channel, config) {
  if (binRequiresRange(fieldDef, channel)) {
    const guide = isUnitModel(model) ? model.axis(channel) ?? model.legend(channel) ?? {} : {};
    const startField = vgField(fieldDef, { expr: "datum" });
    const endField = vgField(fieldDef, { expr: "datum", binSuffix: "end" });
    return {
      formulaAs: vgField(fieldDef, { binSuffix: "range", forAs: true }),
      formula: binFormatExpression(startField, endField, guide.format, guide.formatType, config)
    };
  }
  return {};
}
function binKey(bin3, field3) {
  return `${binToString(bin3)}_${field3}`;
}
function getSignalsFromModel(model, key2) {
  return {
    signal: model.getName(`${key2}_bins`),
    extentSignal: model.getName(`${key2}_extent`)
  };
}
function getBinSignalName(model, field3, bin3) {
  const normalizedBin = normalizeBin(bin3, void 0) ?? {};
  const key2 = binKey(normalizedBin, field3);
  return model.getName(`${key2}_bins`);
}
function isBinTransform(t) {
  return "as" in t;
}
function createBinComponent(t, bin3, model) {
  let as;
  let span;
  if (isBinTransform(t)) {
    as = isString(t.as) ? [t.as, `${t.as}_end`] : [t.as[0], t.as[1]];
  } else {
    as = [vgField(t, { forAs: true }), vgField(t, { binSuffix: "end", forAs: true })];
  }
  const normalizedBin = { ...normalizeBin(bin3, void 0) };
  const key2 = binKey(normalizedBin, t.field);
  const { signal, extentSignal } = getSignalsFromModel(model, key2);
  if (isParameterExtent(normalizedBin.extent)) {
    const ext = normalizedBin.extent;
    span = parseSelectionExtent(model, ext.param, ext);
    delete normalizedBin.extent;
  }
  const binComponent = {
    bin: normalizedBin,
    field: t.field,
    as: [as],
    ...signal ? { signal } : {},
    ...extentSignal ? { extentSignal } : {},
    ...span ? { span } : {}
  };
  return { key: key2, binComponent };
}
var BinNode = class _BinNode extends DataFlowNode {
  bins;
  clone() {
    return new _BinNode(null, duplicate(this.bins));
  }
  constructor(parent, bins2) {
    super(parent);
    this.bins = bins2;
  }
  static makeFromEncoding(parent, model) {
    const bins2 = model.reduceFieldDef((binComponentIndex, fieldDef, channel) => {
      if (isTypedFieldDef(fieldDef) && isBinning(fieldDef.bin)) {
        const { key: key2, binComponent } = createBinComponent(fieldDef, fieldDef.bin, model);
        binComponentIndex[key2] = {
          ...binComponent,
          ...binComponentIndex[key2],
          ...rangeFormula(model, fieldDef, channel, model.config)
        };
      }
      return binComponentIndex;
    }, {});
    if (isEmpty(bins2)) {
      return null;
    }
    return new _BinNode(parent, bins2);
  }
  /**
   * Creates a bin node from BinTransform.
   * The optional parameter should provide
   */
  static makeFromTransform(parent, t, model) {
    const { key: key2, binComponent } = createBinComponent(t, t.bin, model);
    return new _BinNode(parent, {
      [key2]: binComponent
    });
  }
  /**
   * Merge bin nodes. This method either integrates the bin config from the other node
   * or if this node already has a bin config, renames the corresponding signal in the model.
   */
  merge(other, renameSignal) {
    for (const key2 of keys3(other.bins)) {
      if (key2 in this.bins) {
        renameSignal(other.bins[key2].signal, this.bins[key2].signal);
        this.bins[key2].as = unique([...this.bins[key2].as, ...other.bins[key2].as], hash);
      } else {
        this.bins[key2] = other.bins[key2];
      }
    }
    for (const child of other.children) {
      other.removeChild(child);
      child.parent = this;
    }
    other.remove();
  }
  producedFields() {
    return new Set(vals(this.bins).map((c3) => c3.as).flat(2));
  }
  dependentFields() {
    return new Set(vals(this.bins).map((c3) => c3.field));
  }
  hash() {
    return `Bin ${hash(this.bins)}`;
  }
  assemble() {
    return vals(this.bins).flatMap((bin3) => {
      const transform4 = [];
      const [binAs, ...remainingAs] = bin3.as;
      const { extent: extent2, ...params2 } = bin3.bin;
      const binTrans = {
        type: "bin",
        field: replacePathInField(bin3.field),
        as: binAs,
        signal: bin3.signal,
        ...!isParameterExtent(extent2) ? { extent: extent2 } : { extent: null },
        ...bin3.span ? { span: { signal: `span(${bin3.span})` } } : {},
        ...params2
      };
      if (!extent2 && bin3.extentSignal) {
        transform4.push({
          type: "extent",
          field: replacePathInField(bin3.field),
          signal: bin3.extentSignal
        });
        binTrans.extent = { signal: bin3.extentSignal };
      }
      transform4.push(binTrans);
      for (const as of remainingAs) {
        for (let i = 0; i < 2; i++) {
          transform4.push({
            type: "formula",
            expr: vgField({ field: binAs[i] }, { expr: "datum" }),
            as: as[i]
          });
        }
      }
      if (bin3.formula) {
        transform4.push({
          type: "formula",
          expr: bin3.formula,
          as: bin3.formulaAs
        });
      }
      return transform4;
    });
  }
};
function addDimension(dims, channel, fieldDef, model) {
  const channelDef2 = isUnitModel(model) ? model.encoding[getSecondaryRangeChannel(channel)] : void 0;
  if (isTypedFieldDef(fieldDef) && isUnitModel(model) && hasBandEnd(fieldDef, channelDef2, model.markDef, model.config)) {
    dims.add(vgField(fieldDef, {}));
    dims.add(vgField(fieldDef, { suffix: "end" }));
    const { mark, markDef, config } = model;
    const bandPosition = getBandPosition({ fieldDef, markDef, config });
    if (isRectBasedMark(mark) && bandPosition !== 0.5 && isXorY(channel)) {
      dims.add(vgField(fieldDef, { suffix: OFFSETTED_RECT_START_SUFFIX }));
      dims.add(vgField(fieldDef, { suffix: OFFSETTED_RECT_END_SUFFIX }));
    }
    if (fieldDef.bin && binRequiresRange(fieldDef, channel)) {
      dims.add(vgField(fieldDef, { binSuffix: "range" }));
    }
  } else if (isGeoPositionChannel(channel)) {
    const posChannel = getPositionChannelFromLatLong(channel);
    dims.add(model.getName(posChannel));
  } else {
    dims.add(vgField(fieldDef));
  }
  if (isScaleFieldDef(fieldDef) && isFieldRange(fieldDef.scale?.range)) {
    dims.add(fieldDef.scale.range.field);
  }
  return dims;
}
function mergeMeasures(parentMeasures, childMeasures) {
  for (const field3 of keys3(childMeasures)) {
    const ops2 = childMeasures[field3];
    for (const op of keys3(ops2)) {
      if (field3 in parentMeasures) {
        parentMeasures[field3][op] = /* @__PURE__ */ new Set([...parentMeasures[field3][op] ?? [], ...ops2[op]]);
      } else {
        parentMeasures[field3] = { [op]: ops2[op] };
      }
    }
  }
}
var AggregateNode = class _AggregateNode extends DataFlowNode {
  dimensions;
  measures;
  clone() {
    return new _AggregateNode(null, new Set(this.dimensions), duplicate(this.measures));
  }
  /**
   * @param dimensions string set for dimensions
   * @param measures dictionary mapping field name => dict of aggregation functions and names to use
   */
  constructor(parent, dimensions, measures) {
    super(parent);
    this.dimensions = dimensions;
    this.measures = measures;
  }
  get groupBy() {
    return this.dimensions;
  }
  static makeFromEncoding(parent, model) {
    let isAggregate2 = false;
    model.forEachFieldDef((fd) => {
      if (fd.aggregate) {
        isAggregate2 = true;
      }
    });
    const meas = {};
    const dims = /* @__PURE__ */ new Set();
    if (!isAggregate2) {
      return null;
    }
    model.forEachFieldDef((fieldDef, channel) => {
      const { aggregate, field: field3 } = fieldDef;
      if (aggregate) {
        if (aggregate === "count") {
          meas["*"] ??= {};
          meas["*"]["count"] = /* @__PURE__ */ new Set([vgField(fieldDef, { forAs: true })]);
        } else {
          if (isArgminDef(aggregate) || isArgmaxDef(aggregate)) {
            const op = isArgminDef(aggregate) ? "argmin" : "argmax";
            const argField = aggregate[op];
            meas[argField] ??= {};
            meas[argField][op] = /* @__PURE__ */ new Set([vgField({ op, field: argField }, { forAs: true })]);
          } else {
            meas[field3] ??= {};
            meas[field3][aggregate] = /* @__PURE__ */ new Set([vgField(fieldDef, { forAs: true })]);
          }
          if (isScaleChannel(channel) && model.scaleDomain(channel) === "unaggregated") {
            meas[field3] ??= {};
            meas[field3]["min"] = /* @__PURE__ */ new Set([vgField({ field: field3, aggregate: "min" }, { forAs: true })]);
            meas[field3]["max"] = /* @__PURE__ */ new Set([vgField({ field: field3, aggregate: "max" }, { forAs: true })]);
          }
        }
      } else {
        addDimension(dims, channel, fieldDef, model);
      }
    });
    if (dims.size + keys3(meas).length === 0) {
      return null;
    }
    return new _AggregateNode(parent, dims, meas);
  }
  static makeFromTransform(parent, t) {
    const dims = /* @__PURE__ */ new Set();
    const meas = {};
    for (const s of t.aggregate) {
      const { op, field: field3, as } = s;
      if (op) {
        if (op === "count") {
          meas["*"] ??= {};
          meas["*"]["count"] = /* @__PURE__ */ new Set([as ? as : vgField(s, { forAs: true })]);
        } else {
          meas[field3] ??= {};
          meas[field3][op] ??= /* @__PURE__ */ new Set();
          meas[field3][op].add(as ? as : vgField(s, { forAs: true }));
        }
      }
    }
    for (const s of t.groupby ?? []) {
      dims.add(s);
    }
    if (dims.size + keys3(meas).length === 0) {
      return null;
    }
    return new _AggregateNode(parent, dims, meas);
  }
  merge(other) {
    if (setEqual(this.dimensions, other.dimensions)) {
      mergeMeasures(this.measures, other.measures);
      return true;
    }
    debug3("different dimensions, cannot merge");
    return false;
  }
  addDimensions(fields) {
    fields.forEach(this.dimensions.add, this.dimensions);
  }
  dependentFields() {
    return /* @__PURE__ */ new Set([...this.dimensions, ...keys3(this.measures)]);
  }
  producedFields() {
    const out = /* @__PURE__ */ new Set();
    for (const field3 of keys3(this.measures)) {
      for (const op of keys3(this.measures[field3])) {
        const m3 = this.measures[field3][op];
        if (m3.size === 0) {
          out.add(`${op}_${field3}`);
        } else {
          m3.forEach(out.add, out);
        }
      }
    }
    return out;
  }
  hash() {
    return `Aggregate ${hash({ dimensions: this.dimensions, measures: this.measures })}`;
  }
  assemble() {
    const ops2 = [];
    const fields = [];
    const as = [];
    for (const field3 of keys3(this.measures)) {
      for (const op of keys3(this.measures[field3])) {
        for (const alias of this.measures[field3][op]) {
          as.push(alias);
          ops2.push(op);
          fields.push(field3 === "*" ? null : replacePathInField(field3));
        }
      }
    }
    const result = {
      type: "aggregate",
      groupby: [...this.dimensions].map(replacePathInField),
      ops: ops2,
      fields,
      as
    };
    return result;
  }
};
var FacetNode = class extends DataFlowNode {
  model;
  name;
  data;
  column;
  row;
  facet;
  childModel;
  /**
   * @param model The facet model.
   * @param name The name that this facet source will have.
   * @param data The source data for this facet data.
   */
  constructor(parent, model, name, data4) {
    super(parent);
    this.model = model;
    this.name = name;
    this.data = data4;
    for (const channel of FACET_CHANNELS) {
      const fieldDef = model.facet[channel];
      if (fieldDef) {
        const { bin: bin3, sort: sort4 } = fieldDef;
        this[channel] = {
          name: model.getName(`${channel}_domain`),
          fields: [vgField(fieldDef), ...isBinning(bin3) ? [vgField(fieldDef, { binSuffix: "end" })] : []],
          ...isSortField(sort4) ? { sortField: sort4 } : isArray(sort4) ? { sortIndexField: sortArrayIndexField(fieldDef, channel) } : {}
        };
      }
    }
    this.childModel = model.child;
  }
  hash() {
    let out = `Facet`;
    for (const channel of FACET_CHANNELS) {
      if (this[channel]) {
        out += ` ${channel.charAt(0)}:${hash(this[channel])}`;
      }
    }
    return out;
  }
  get fields() {
    const f = [];
    for (const channel of FACET_CHANNELS) {
      if (this[channel]?.fields) {
        f.push(...this[channel].fields);
      }
    }
    return f;
  }
  dependentFields() {
    const depFields = new Set(this.fields);
    for (const channel of FACET_CHANNELS) {
      if (this[channel]) {
        if (this[channel].sortField) {
          depFields.add(this[channel].sortField.field);
        }
        if (this[channel].sortIndexField) {
          depFields.add(this[channel].sortIndexField);
        }
      }
    }
    return depFields;
  }
  producedFields() {
    return /* @__PURE__ */ new Set();
  }
  /**
   * The name to reference this source is its name.
   */
  getSource() {
    return this.name;
  }
  getChildIndependentFieldsWithStep() {
    const childIndependentFieldsWithStep = {};
    for (const channel of POSITION_SCALE_CHANNELS) {
      const childScaleComponent = this.childModel.component.scales[channel];
      if (childScaleComponent && !childScaleComponent.merged) {
        const type4 = childScaleComponent.get("type");
        const range7 = childScaleComponent.get("range");
        if (hasDiscreteDomain(type4) && isVgRangeStep(range7)) {
          const domain4 = assembleDomain(this.childModel, channel);
          const field3 = getFieldFromDomain(domain4);
          if (field3) {
            childIndependentFieldsWithStep[channel] = field3;
          } else {
            warn3(unknownField(channel));
          }
        }
      }
    }
    return childIndependentFieldsWithStep;
  }
  assembleRowColumnHeaderData(channel, crossedDataName, childIndependentFieldsWithStep) {
    const childChannel = { row: "y", column: "x", facet: void 0 }[channel];
    const fields = [];
    const ops2 = [];
    const as = [];
    if (childChannel && childIndependentFieldsWithStep && childIndependentFieldsWithStep[childChannel]) {
      if (crossedDataName) {
        fields.push(`distinct_${childIndependentFieldsWithStep[childChannel]}`);
        ops2.push("max");
      } else {
        fields.push(childIndependentFieldsWithStep[childChannel]);
        ops2.push("distinct");
      }
      as.push(`distinct_${childIndependentFieldsWithStep[childChannel]}`);
    }
    const { sortField, sortIndexField } = this[channel];
    if (sortField) {
      const { op = DEFAULT_SORT_OP, field: field3 } = sortField;
      fields.push(field3);
      ops2.push(op);
      as.push(vgField(sortField, { forAs: true }));
    } else if (sortIndexField) {
      fields.push(sortIndexField);
      ops2.push("max");
      as.push(sortIndexField);
    }
    return {
      name: this[channel].name,
      // Use data from the crossed one if it exist
      source: crossedDataName ?? this.data,
      transform: [
        {
          type: "aggregate",
          groupby: this[channel].fields,
          ...fields.length ? {
            fields,
            ops: ops2,
            as
          } : {}
        }
      ]
    };
  }
  assembleFacetHeaderData(childIndependentFieldsWithStep) {
    const { columns } = this.model.layout;
    const { layoutHeaders: layoutHeaders2 } = this.model.component;
    const data4 = [];
    const hasSharedAxis = {};
    for (const headerChannel of HEADER_CHANNELS) {
      for (const headerType of HEADER_TYPES) {
        const headers = layoutHeaders2[headerChannel]?.[headerType] ?? [];
        for (const header of headers) {
          if (header.axes?.length > 0) {
            hasSharedAxis[headerChannel] = true;
            break;
          }
        }
      }
      if (hasSharedAxis[headerChannel]) {
        const cardinality = `length(data("${this.facet.name}"))`;
        const stop2 = headerChannel === "row" ? columns ? { signal: `ceil(${cardinality} / ${columns})` } : 1 : columns ? { signal: `min(${cardinality}, ${columns})` } : { signal: cardinality };
        data4.push({
          name: `${this.facet.name}_${headerChannel}`,
          transform: [
            {
              type: "sequence",
              start: 0,
              stop: stop2
            }
          ]
        });
      }
    }
    const { row, column } = hasSharedAxis;
    if (row || column) {
      data4.unshift(this.assembleRowColumnHeaderData("facet", null, childIndependentFieldsWithStep));
    }
    return data4;
  }
  assemble() {
    const data4 = [];
    let crossedDataName = null;
    const childIndependentFieldsWithStep = this.getChildIndependentFieldsWithStep();
    const { column, row, facet } = this;
    if (column && row && (childIndependentFieldsWithStep.x || childIndependentFieldsWithStep.y)) {
      crossedDataName = `cross_${this.column.name}_${this.row.name}`;
      const fields = [].concat(childIndependentFieldsWithStep.x ?? [], childIndependentFieldsWithStep.y ?? []);
      const ops2 = fields.map(() => "distinct");
      data4.push({
        name: crossedDataName,
        source: this.data,
        transform: [
          {
            type: "aggregate",
            groupby: this.fields,
            fields,
            ops: ops2
          }
        ]
      });
    }
    for (const channel of [COLUMN, ROW]) {
      if (this[channel]) {
        data4.push(this.assembleRowColumnHeaderData(channel, crossedDataName, childIndependentFieldsWithStep));
      }
    }
    if (facet) {
      const facetData = this.assembleFacetHeaderData(childIndependentFieldsWithStep);
      if (facetData) {
        data4.push(...facetData);
      }
    }
    return data4;
  }
};
function unquote(pattern) {
  if (pattern.startsWith("'") && pattern.endsWith("'") || pattern.startsWith('"') && pattern.endsWith('"')) {
    return pattern.slice(1, -1);
  }
  return pattern;
}
function parseExpression3(field3, parse8) {
  const f = accessPathWithDatum(field3);
  if (parse8 === "number") {
    return `toNumber(${f})`;
  } else if (parse8 === "boolean") {
    return `toBoolean(${f})`;
  } else if (parse8 === "string") {
    return `toString(${f})`;
  } else if (parse8 === "date") {
    return `toDate(${f})`;
  } else if (parse8 === "flatten") {
    return f;
  } else if (parse8.startsWith("date:")) {
    const specifier = unquote(parse8.slice(5, parse8.length));
    return `timeParse(${f},'${specifier}')`;
  } else if (parse8.startsWith("utc:")) {
    const specifier = unquote(parse8.slice(4, parse8.length));
    return `utcParse(${f},'${specifier}')`;
  } else {
    warn3(unrecognizedParse(parse8));
    return null;
  }
}
function getImplicitFromFilterTransform(transform4) {
  const implicit2 = {};
  forEachLeaf(transform4.filter, (filter4) => {
    if (isFieldPredicate(filter4)) {
      let val = null;
      if (isFieldEqualPredicate(filter4)) {
        val = signalRefOrValue(filter4.equal);
      } else if (isFieldLTEPredicate(filter4)) {
        val = signalRefOrValue(filter4.lte);
      } else if (isFieldLTPredicate(filter4)) {
        val = signalRefOrValue(filter4.lt);
      } else if (isFieldGTPredicate(filter4)) {
        val = signalRefOrValue(filter4.gt);
      } else if (isFieldGTEPredicate(filter4)) {
        val = signalRefOrValue(filter4.gte);
      } else if (isFieldRangePredicate(filter4)) {
        val = filter4.range[0];
      } else if (isFieldOneOfPredicate(filter4)) {
        val = (filter4.oneOf ?? filter4.in)[0];
      }
      if (val) {
        if (isDateTime(val)) {
          implicit2[filter4.field] = "date";
        } else if (isNumber(val)) {
          implicit2[filter4.field] = "number";
        } else if (isString(val)) {
          implicit2[filter4.field] = "string";
        }
      }
      if (filter4.timeUnit) {
        implicit2[filter4.field] = "date";
      }
    }
  });
  return implicit2;
}
function getImplicitFromEncoding(model) {
  const implicit2 = {};
  function add7(fieldDef) {
    if (isFieldOrDatumDefForTimeFormat(fieldDef)) {
      implicit2[fieldDef.field] = "date";
    } else if (fieldDef.type === "quantitative" && isMinMaxOp(fieldDef.aggregate)) {
      implicit2[fieldDef.field] = "number";
    } else if (accessPathDepth(fieldDef.field) > 1) {
      if (!(fieldDef.field in implicit2)) {
        implicit2[fieldDef.field] = "flatten";
      }
    } else if (isScaleFieldDef(fieldDef) && isSortField(fieldDef.sort) && accessPathDepth(fieldDef.sort.field) > 1) {
      if (!(fieldDef.sort.field in implicit2)) {
        implicit2[fieldDef.sort.field] = "flatten";
      }
    }
  }
  if (isUnitModel(model) || isFacetModel(model)) {
    model.forEachFieldDef((fieldDef, channel) => {
      if (isTypedFieldDef(fieldDef)) {
        add7(fieldDef);
      } else {
        const mainChannel = getMainRangeChannel(channel);
        const mainFieldDef = model.fieldDef(mainChannel);
        add7({
          ...fieldDef,
          type: mainFieldDef.type
        });
      }
    });
  }
  if (isUnitModel(model)) {
    const { mark, markDef, encoding } = model;
    if (isPathMark(mark) && // No need to sort by dimension if we have a connected scatterplot (order channel is present)
    !model.encoding.order) {
      const dimensionChannel = markDef.orient === "horizontal" ? "y" : "x";
      const dimensionChannelDef = encoding[dimensionChannel];
      if (isFieldDef(dimensionChannelDef) && dimensionChannelDef.type === "quantitative" && !(dimensionChannelDef.field in implicit2)) {
        implicit2[dimensionChannelDef.field] = "number";
      }
    }
  }
  return implicit2;
}
function getImplicitFromSelection(model) {
  const implicit2 = {};
  if (isUnitModel(model) && model.component.selection) {
    for (const name of keys3(model.component.selection)) {
      const selCmpt = model.component.selection[name];
      for (const proj of selCmpt.project.items) {
        if (!proj.channel && accessPathDepth(proj.field) > 1) {
          implicit2[proj.field] = "flatten";
        }
      }
    }
  }
  return implicit2;
}
var ParseNode = class _ParseNode extends DataFlowNode {
  _parse;
  clone() {
    return new _ParseNode(null, duplicate(this._parse));
  }
  constructor(parent, parse8) {
    super(parent);
    this._parse = parse8;
  }
  hash() {
    return `Parse ${hash(this._parse)}`;
  }
  /**
   * Creates a parse node from a data.format.parse and updates ancestorParse.
   */
  static makeExplicit(parent, model, ancestorParse) {
    let explicit = {};
    const data4 = model.data;
    if (!isGenerator(data4) && data4?.format?.parse) {
      explicit = data4.format.parse;
    }
    return this.makeWithAncestors(parent, explicit, {}, ancestorParse);
  }
  /**
   * Creates a parse node from "explicit" parse and "implicit" parse and updates ancestorParse.
   */
  static makeWithAncestors(parent, explicit, implicit2, ancestorParse) {
    for (const field3 of keys3(implicit2)) {
      const parsedAs = ancestorParse.getWithExplicit(field3);
      if (parsedAs.value !== void 0) {
        if (parsedAs.explicit || parsedAs.value === implicit2[field3] || parsedAs.value === "derived" || implicit2[field3] === "flatten") {
          delete implicit2[field3];
        } else {
          warn3(differentParse(field3, implicit2[field3], parsedAs.value));
        }
      }
    }
    for (const field3 of keys3(explicit)) {
      const parsedAs = ancestorParse.get(field3);
      if (parsedAs !== void 0) {
        if (parsedAs === explicit[field3]) {
          delete explicit[field3];
        } else {
          warn3(differentParse(field3, explicit[field3], parsedAs));
        }
      }
    }
    const parse8 = new Split(explicit, implicit2);
    ancestorParse.copyAll(parse8);
    const p = {};
    for (const key2 of keys3(parse8.combine())) {
      const val = parse8.get(key2);
      if (val !== null) {
        p[key2] = val;
      }
    }
    if (keys3(p).length === 0 || ancestorParse.parseNothing) {
      return null;
    }
    return new _ParseNode(parent, p);
  }
  get parse() {
    return this._parse;
  }
  merge(other) {
    this._parse = { ...this._parse, ...other.parse };
    other.remove();
  }
  /**
   * Assemble an object for Vega's format.parse property.
   */
  assembleFormatParse() {
    const formatParse = {};
    for (const field3 of keys3(this._parse)) {
      const p = this._parse[field3];
      if (accessPathDepth(field3) === 1) {
        formatParse[field3] = p;
      }
    }
    return formatParse;
  }
  // format parse depends and produces all fields in its parse
  producedFields() {
    return new Set(keys3(this._parse));
  }
  dependentFields() {
    return new Set(keys3(this._parse));
  }
  assembleTransforms(onlyNested = false) {
    return keys3(this._parse).filter((field3) => onlyNested ? accessPathDepth(field3) > 1 : true).map((field3) => {
      const expr2 = parseExpression3(field3, this._parse[field3]);
      if (!expr2) {
        return null;
      }
      const formula = {
        type: "formula",
        expr: expr2,
        as: removePathFromField(field3)
        // Vega output is always flattened
      };
      return formula;
    }).filter((t) => t !== null);
  }
};
var IdentifierNode = class _IdentifierNode extends DataFlowNode {
  clone() {
    return new _IdentifierNode(null);
  }
  constructor(parent) {
    super(parent);
  }
  dependentFields() {
    return /* @__PURE__ */ new Set();
  }
  producedFields() {
    return /* @__PURE__ */ new Set([SELECTION_ID]);
  }
  hash() {
    return "Identifier";
  }
  assemble() {
    return { type: "identifier", as: SELECTION_ID };
  }
};
var GraticuleNode = class _GraticuleNode extends DataFlowNode {
  params;
  clone() {
    return new _GraticuleNode(null, this.params);
  }
  constructor(parent, params2) {
    super(parent);
    this.params = params2;
  }
  dependentFields() {
    return /* @__PURE__ */ new Set();
  }
  producedFields() {
    return void 0;
  }
  hash() {
    return `Graticule ${hash(this.params)}`;
  }
  assemble() {
    return {
      type: "graticule",
      ...this.params === true ? {} : this.params
    };
  }
};
var SequenceNode = class _SequenceNode extends DataFlowNode {
  params;
  clone() {
    return new _SequenceNode(null, this.params);
  }
  constructor(parent, params2) {
    super(parent);
    this.params = params2;
  }
  dependentFields() {
    return /* @__PURE__ */ new Set();
  }
  producedFields() {
    return /* @__PURE__ */ new Set([this.params.as ?? "data"]);
  }
  hash() {
    return `Hash ${hash(this.params)}`;
  }
  assemble() {
    return {
      type: "sequence",
      ...this.params
    };
  }
};
var SourceNode = class extends DataFlowNode {
  _data;
  _name;
  _generator;
  constructor(data4) {
    super(null);
    data4 ??= { name: "source" };
    let format6;
    if (!isGenerator(data4)) {
      format6 = data4.format ? { ...omit(data4.format, ["parse"]) } : {};
    }
    if (isInlineData(data4)) {
      this._data = { values: data4.values };
    } else if (isUrlData(data4)) {
      this._data = { url: data4.url };
      if (!format6.type) {
        let defaultExtension = /(?:\.([^.]+))?$/.exec(data4.url)[1];
        if (!contains2(["json", "csv", "tsv", "dsv", "topojson"], defaultExtension)) {
          defaultExtension = "json";
        }
        format6.type = defaultExtension;
      }
    } else if (isSphereGenerator(data4)) {
      this._data = { values: [{ type: "Sphere" }] };
    } else if (isNamedData(data4) || isGenerator(data4)) {
      this._data = {};
    }
    this._generator = isGenerator(data4);
    if (data4.name) {
      this._name = data4.name;
    }
    if (format6 && !isEmpty(format6)) {
      this._data.format = format6;
    }
  }
  dependentFields() {
    return /* @__PURE__ */ new Set();
  }
  producedFields() {
    return void 0;
  }
  get data() {
    return this._data;
  }
  hasName() {
    return !!this._name;
  }
  get isGenerator() {
    return this._generator;
  }
  get dataName() {
    return this._name;
  }
  set dataName(name) {
    this._name = name;
  }
  set parent(parent) {
    throw new Error("Source nodes have to be roots.");
  }
  remove() {
    throw new Error("Source nodes are roots and cannot be removed.");
  }
  hash() {
    throw new Error("Cannot hash sources");
  }
  assemble() {
    return {
      name: this._name,
      ...this._data,
      transform: []
    };
  }
};
function isDataSourceNode(node) {
  return node instanceof SourceNode || node instanceof GraticuleNode || node instanceof SequenceNode;
}
var Optimizer = class {
  #modified;
  constructor() {
    this.#modified = false;
  }
  // Once true, #modified is never set to false
  setModified() {
    this.#modified = true;
  }
  get modifiedFlag() {
    return this.#modified;
  }
};
var BottomUpOptimizer = class extends Optimizer {
  /**
   * Compute a map of node depths that we can use to determine a topological sort order.
   */
  getNodeDepths(node, depth, depths) {
    depths.set(node, depth);
    for (const child of node.children) {
      this.getNodeDepths(child, depth + 1, depths);
    }
    return depths;
  }
  /**
   * Run the optimizer on all nodes starting from the leaves.
   */
  optimize(node) {
    const depths = this.getNodeDepths(node, 0, /* @__PURE__ */ new Map());
    const topologicalSort = [...depths.entries()].sort((a3, b10) => b10[1] - a3[1]);
    for (const tuple of topologicalSort) {
      this.run(tuple[0]);
    }
    return this.modifiedFlag;
  }
};
var TopDownOptimizer = class extends Optimizer {
  /**
   * Run the optimizer depth first on all nodes starting from the roots.
   */
  optimize(node) {
    this.run(node);
    for (const child of node.children) {
      this.optimize(child);
    }
    return this.modifiedFlag;
  }
};
var MergeIdenticalNodes = class extends TopDownOptimizer {
  mergeNodes(parent, nodes) {
    const mergedNode = nodes.shift();
    for (const node of nodes) {
      parent.removeChild(node);
      node.parent = mergedNode;
      node.remove();
    }
  }
  run(node) {
    const hashes = node.children.map((x6) => x6.hash());
    const buckets = {};
    for (let i = 0; i < hashes.length; i++) {
      if (buckets[hashes[i]] === void 0) {
        buckets[hashes[i]] = [node.children[i]];
      } else {
        buckets[hashes[i]].push(node.children[i]);
      }
    }
    for (const k of keys3(buckets)) {
      if (buckets[k].length > 1) {
        this.setModified();
        this.mergeNodes(node, buckets[k]);
      }
    }
  }
};
var RemoveUnnecessaryIdentifierNodes = class extends TopDownOptimizer {
  requiresSelectionId;
  constructor(model) {
    super();
    this.requiresSelectionId = model && requiresSelectionId(model);
  }
  run(node) {
    if (node instanceof IdentifierNode) {
      if (!(this.requiresSelectionId && (isDataSourceNode(node.parent) || node.parent instanceof AggregateNode || node.parent instanceof ParseNode))) {
        this.setModified();
        node.remove();
      }
    }
  }
};
var RemoveDuplicateTimeUnits = class extends Optimizer {
  optimize(node) {
    this.run(node, /* @__PURE__ */ new Set());
    return this.modifiedFlag;
  }
  run(node, timeUnitFields) {
    let producedFields = /* @__PURE__ */ new Set();
    if (node instanceof TimeUnitNode) {
      producedFields = node.producedFields();
      if (hasIntersection(producedFields, timeUnitFields)) {
        this.setModified();
        node.removeFormulas(timeUnitFields);
        if (node.producedFields.length === 0) {
          node.remove();
        }
      }
    }
    for (const child of node.children) {
      this.run(child, /* @__PURE__ */ new Set([...timeUnitFields, ...producedFields]));
    }
  }
};
var RemoveUnnecessaryOutputNodes = class extends TopDownOptimizer {
  constructor() {
    super();
  }
  run(node) {
    if (node instanceof OutputNode && !node.isRequired()) {
      this.setModified();
      node.remove();
    }
  }
};
var MoveParseUp = class extends BottomUpOptimizer {
  run(node) {
    if (isDataSourceNode(node)) {
      return;
    }
    if (node.numChildren() > 1) {
      return;
    }
    for (const child of node.children) {
      if (child instanceof ParseNode) {
        if (node instanceof ParseNode) {
          this.setModified();
          node.merge(child);
        } else {
          if (fieldIntersection(node.producedFields(), child.dependentFields())) {
            continue;
          }
          this.setModified();
          child.swapWithParent();
        }
      }
    }
    return;
  }
};
var MergeParse = class extends BottomUpOptimizer {
  run(node) {
    const originalChildren = [...node.children];
    const parseChildren = node.children.filter((child) => child instanceof ParseNode);
    if (node.numChildren() > 1 && parseChildren.length >= 1) {
      const commonParse = {};
      const conflictingParse = /* @__PURE__ */ new Set();
      for (const parseNode of parseChildren) {
        const parse8 = parseNode.parse;
        for (const k of keys3(parse8)) {
          if (!(k in commonParse)) {
            commonParse[k] = parse8[k];
          } else if (commonParse[k] !== parse8[k]) {
            conflictingParse.add(k);
          }
        }
      }
      for (const field3 of conflictingParse) {
        delete commonParse[field3];
      }
      if (!isEmpty(commonParse)) {
        this.setModified();
        const mergedParseNode = new ParseNode(node, commonParse);
        for (const childNode of originalChildren) {
          if (childNode instanceof ParseNode) {
            for (const key2 of keys3(commonParse)) {
              delete childNode.parse[key2];
            }
          }
          node.removeChild(childNode);
          childNode.parent = mergedParseNode;
          if (childNode instanceof ParseNode && keys3(childNode.parse).length === 0) {
            childNode.remove();
          }
        }
      }
    }
  }
};
var RemoveUnusedSubtrees = class extends BottomUpOptimizer {
  run(node) {
    if (node instanceof OutputNode || node.numChildren() > 0 || node instanceof FacetNode) ;
    else if (node instanceof SourceNode) ;
    else {
      this.setModified();
      node.remove();
    }
  }
};
var MergeTimeUnits = class extends BottomUpOptimizer {
  run(node) {
    const timeUnitChildren = node.children.filter((x6) => x6 instanceof TimeUnitNode);
    const combination = timeUnitChildren.pop();
    for (const timeUnit of timeUnitChildren) {
      this.setModified();
      combination.merge(timeUnit);
    }
  }
};
var MergeAggregates = class extends BottomUpOptimizer {
  run(node) {
    const aggChildren = node.children.filter((child) => child instanceof AggregateNode);
    const groupedAggregates = {};
    for (const agg of aggChildren) {
      const groupBys = hash(agg.groupBy);
      if (!(groupBys in groupedAggregates)) {
        groupedAggregates[groupBys] = [];
      }
      groupedAggregates[groupBys].push(agg);
    }
    for (const group3 of keys3(groupedAggregates)) {
      const mergeableAggs = groupedAggregates[group3];
      if (mergeableAggs.length > 1) {
        const mergedAggs = mergeableAggs.pop();
        for (const agg of mergeableAggs) {
          if (mergedAggs.merge(agg)) {
            node.removeChild(agg);
            agg.parent = mergedAggs;
            agg.remove();
            this.setModified();
          }
        }
      }
    }
  }
};
var MergeBins = class extends BottomUpOptimizer {
  model;
  constructor(model) {
    super();
    this.model = model;
  }
  run(node) {
    const moveBinsUp = !(isDataSourceNode(node) || node instanceof FilterNode || node instanceof ParseNode || node instanceof IdentifierNode);
    const promotableBins = [];
    const remainingBins = [];
    for (const child of node.children) {
      if (child instanceof BinNode) {
        if (moveBinsUp && !fieldIntersection(node.producedFields(), child.dependentFields())) {
          promotableBins.push(child);
        } else {
          remainingBins.push(child);
        }
      }
    }
    if (promotableBins.length > 0) {
      const promotedBin = promotableBins.pop();
      for (const bin3 of promotableBins) {
        promotedBin.merge(bin3, this.model.renameSignal.bind(this.model));
      }
      this.setModified();
      if (node instanceof BinNode) {
        node.merge(promotedBin, this.model.renameSignal.bind(this.model));
      } else {
        promotedBin.swapWithParent();
      }
    }
    if (remainingBins.length > 1) {
      const remainingBin = remainingBins.pop();
      for (const bin3 of remainingBins) {
        remainingBin.merge(bin3, this.model.renameSignal.bind(this.model));
      }
      this.setModified();
    }
  }
};
var MergeOutputs = class extends BottomUpOptimizer {
  run(node) {
    const children4 = [...node.children];
    const hasOutputChild = some(children4, (child) => child instanceof OutputNode);
    if (!hasOutputChild || node.numChildren() <= 1) {
      return;
    }
    const otherChildren = [];
    let mainOutput;
    for (const child of children4) {
      if (child instanceof OutputNode) {
        let lastOutput = child;
        while (lastOutput.numChildren() === 1) {
          const [theChild] = lastOutput.children;
          if (theChild instanceof OutputNode) {
            lastOutput = theChild;
          } else {
            break;
          }
        }
        otherChildren.push(...lastOutput.children);
        if (mainOutput) {
          node.removeChild(child);
          child.parent = mainOutput.parent;
          mainOutput.parent.removeChild(mainOutput);
          mainOutput.parent = lastOutput;
          this.setModified();
        } else {
          mainOutput = lastOutput;
        }
      } else {
        otherChildren.push(child);
      }
    }
    if (otherChildren.length) {
      this.setModified();
      for (const child of otherChildren) {
        child.parent.removeChild(child);
        child.parent = mainOutput;
      }
    }
  }
};
var JoinAggregateTransformNode = class _JoinAggregateTransformNode extends DataFlowNode {
  transform;
  clone() {
    return new _JoinAggregateTransformNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
  }
  addDimensions(fields) {
    this.transform.groupby = unique(this.transform.groupby.concat(fields), (d) => d);
  }
  dependentFields() {
    const out = /* @__PURE__ */ new Set();
    if (this.transform.groupby) {
      this.transform.groupby.forEach(out.add, out);
    }
    this.transform.joinaggregate.map((w3) => w3.field).filter((f) => f !== void 0).forEach(out.add, out);
    return out;
  }
  producedFields() {
    return new Set(this.transform.joinaggregate.map(this.getDefaultName));
  }
  getDefaultName(joinAggregateFieldDef) {
    return joinAggregateFieldDef.as ?? vgField(joinAggregateFieldDef);
  }
  hash() {
    return `JoinAggregateTransform ${hash(this.transform)}`;
  }
  assemble() {
    const fields = [];
    const ops2 = [];
    const as = [];
    for (const joinaggregate of this.transform.joinaggregate) {
      ops2.push(joinaggregate.op);
      as.push(this.getDefaultName(joinaggregate));
      fields.push(joinaggregate.field === void 0 ? null : joinaggregate.field);
    }
    const groupby = this.transform.groupby;
    return {
      type: "joinaggregate",
      as,
      ops: ops2,
      fields,
      ...groupby !== void 0 ? { groupby } : {}
    };
  }
};
var FilterInvalidNode = class _FilterInvalidNode extends DataFlowNode {
  filter;
  clone() {
    return new _FilterInvalidNode(null, { ...this.filter });
  }
  constructor(parent, filter4) {
    super(parent);
    this.filter = filter4;
  }
  static make(parent, model, dataSourcesForHandlingInvalidValues) {
    const { config, markDef } = model;
    const { marks, scales: scales2 } = dataSourcesForHandlingInvalidValues;
    if (marks === "include-invalid-values" && scales2 === "include-invalid-values") {
      return null;
    }
    const filter4 = model.reduceFieldDef((aggregator, fieldDef, channel) => {
      const scaleComponent = isScaleChannel(channel) && model.getScaleComponent(channel);
      if (scaleComponent) {
        const scaleType2 = scaleComponent.get("type");
        const { aggregate } = fieldDef;
        const invalidDataMode = getScaleInvalidDataMode({
          scaleChannel: channel,
          markDef,
          config,
          scaleType: scaleType2,
          isCountAggregate: isCountingAggregateOp(aggregate)
        });
        if (invalidDataMode !== "show" && invalidDataMode !== "always-valid") {
          aggregator[fieldDef.field] = fieldDef;
        }
      }
      return aggregator;
    }, {});
    if (!keys3(filter4).length) {
      return null;
    }
    return new _FilterInvalidNode(parent, filter4);
  }
  dependentFields() {
    return new Set(keys3(this.filter));
  }
  producedFields() {
    return /* @__PURE__ */ new Set();
  }
  hash() {
    return `FilterInvalid ${hash(this.filter)}`;
  }
  /**
   * Create the VgTransforms for each of the filtered fields.
   */
  assemble() {
    const filters2 = keys3(this.filter).reduce((vegaFilters, field3) => {
      const fieldDef = this.filter[field3];
      const ref2 = vgField(fieldDef, { expr: "datum" });
      if (fieldDef !== null) {
        if (fieldDef.type === "temporal") {
          vegaFilters.push(`(isDate(${ref2}) || (${isValidFiniteNumberExpr(ref2)}))`);
        } else if (fieldDef.type === "quantitative") {
          vegaFilters.push(isValidFiniteNumberExpr(ref2));
        } else ;
      }
      return vegaFilters;
    }, []);
    return filters2.length > 0 ? {
      type: "filter",
      expr: filters2.join(" && ")
    } : null;
  }
};
function isValidFiniteNumberExpr(ref2) {
  return `isValid(${ref2}) && isFinite(+${ref2})`;
}
function getStackByFields(model) {
  return model.stack.stackBy.reduce((fields, by) => {
    const fieldDef = by.fieldDef;
    const _field = vgField(fieldDef);
    if (_field) {
      fields.push(_field);
    }
    return fields;
  }, []);
}
function isValidAsArray(as) {
  return isArray(as) && as.every((s) => isString(s)) && as.length > 1;
}
var StackNode = class _StackNode extends DataFlowNode {
  _stack;
  clone() {
    return new _StackNode(null, duplicate(this._stack));
  }
  constructor(parent, stack2) {
    super(parent);
    this._stack = stack2;
  }
  static makeFromTransform(parent, stackTransform) {
    const { stack: stack2, groupby, as, offset: offset6 = "zero" } = stackTransform;
    const sortFields = [];
    const sortOrder = [];
    if (stackTransform.sort !== void 0) {
      for (const sortField of stackTransform.sort) {
        sortFields.push(sortField.field);
        sortOrder.push(getFirstDefined(sortField.order, "ascending"));
      }
    }
    const sort4 = {
      field: sortFields,
      order: sortOrder
    };
    let normalizedAs;
    if (isValidAsArray(as)) {
      normalizedAs = as;
    } else if (isString(as)) {
      normalizedAs = [as, `${as}_end`];
    } else {
      normalizedAs = [`${stackTransform.stack}_start`, `${stackTransform.stack}_end`];
    }
    return new _StackNode(parent, {
      dimensionFieldDefs: [],
      stackField: stack2,
      groupby,
      offset: offset6,
      sort: sort4,
      facetby: [],
      as: normalizedAs
    });
  }
  static makeFromEncoding(parent, model) {
    const stackProperties = model.stack;
    const { encoding } = model;
    if (!stackProperties) {
      return null;
    }
    const { groupbyChannels, fieldChannel, offset: offset6, impute } = stackProperties;
    const dimensionFieldDefs = groupbyChannels.map((groupbyChannel) => {
      const cDef = encoding[groupbyChannel];
      return getFieldDef(cDef);
    }).filter((def3) => !!def3);
    const stackby = getStackByFields(model);
    const orderDef = model.encoding.order;
    let sort4;
    if (isArray(orderDef) || isFieldDef(orderDef)) {
      sort4 = sortParams(orderDef);
    } else {
      const sortOrder = isOrderOnlyDef(orderDef) ? orderDef.sort : fieldChannel === "y" ? "descending" : "ascending";
      sort4 = stackby.reduce((s, field3) => {
        if (!s.field.includes(field3)) {
          s.field.push(field3);
          s.order.push(sortOrder);
        }
        return s;
      }, { field: [], order: [] });
    }
    return new _StackNode(parent, {
      dimensionFieldDefs,
      stackField: model.vgField(fieldChannel),
      facetby: [],
      stackby,
      sort: sort4,
      offset: offset6,
      impute,
      as: [
        model.vgField(fieldChannel, { suffix: "start", forAs: true }),
        model.vgField(fieldChannel, { suffix: "end", forAs: true })
      ]
    });
  }
  get stack() {
    return this._stack;
  }
  addDimensions(fields) {
    this._stack.facetby.push(...fields);
  }
  dependentFields() {
    const out = /* @__PURE__ */ new Set();
    out.add(this._stack.stackField);
    this.getGroupbyFields().forEach(out.add, out);
    this._stack.facetby.forEach(out.add, out);
    this._stack.sort.field.forEach(out.add, out);
    return out;
  }
  producedFields() {
    return new Set(this._stack.as);
  }
  hash() {
    return `Stack ${hash(this._stack)}`;
  }
  getGroupbyFields() {
    const { dimensionFieldDefs, impute, groupby } = this._stack;
    if (dimensionFieldDefs.length > 0) {
      return dimensionFieldDefs.map((dimensionFieldDef) => {
        if (dimensionFieldDef.bin) {
          if (impute) {
            return [vgField(dimensionFieldDef, { binSuffix: "mid" })];
          }
          return [
            // For binned group by field without impute, we need both bin (start) and bin_end
            vgField(dimensionFieldDef, {}),
            vgField(dimensionFieldDef, { binSuffix: "end" })
          ];
        }
        return [vgField(dimensionFieldDef)];
      }).flat();
    }
    return groupby ?? [];
  }
  assemble() {
    const transform4 = [];
    const { facetby, dimensionFieldDefs, stackField: field3, stackby, sort: sort4, offset: offset6, impute, as } = this._stack;
    if (impute) {
      for (const dimensionFieldDef of dimensionFieldDefs) {
        const { bandPosition = 0.5, bin: bin3 } = dimensionFieldDef;
        if (bin3) {
          const binStart = vgField(dimensionFieldDef, { expr: "datum" });
          const binEnd = vgField(dimensionFieldDef, { expr: "datum", binSuffix: "end" });
          transform4.push({
            type: "formula",
            expr: `${isValidFiniteNumberExpr(binStart)} ? ${bandPosition}*${binStart}+${1 - bandPosition}*${binEnd} : ${binStart}`,
            as: vgField(dimensionFieldDef, { binSuffix: "mid", forAs: true })
          });
        }
        transform4.push({
          type: "impute",
          field: field3,
          groupby: [...stackby, ...facetby],
          key: vgField(dimensionFieldDef, { binSuffix: "mid" }),
          method: "value",
          value: 0
        });
      }
    }
    transform4.push({
      type: "stack",
      groupby: [...this.getGroupbyFields(), ...facetby],
      field: field3,
      sort: sort4,
      as,
      offset: offset6
    });
    return transform4;
  }
};
var WindowTransformNode = class _WindowTransformNode extends DataFlowNode {
  transform;
  clone() {
    return new _WindowTransformNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
  }
  addDimensions(fields) {
    this.transform.groupby = unique(this.transform.groupby.concat(fields), (d) => d);
  }
  dependentFields() {
    const out = /* @__PURE__ */ new Set();
    (this.transform.groupby ?? []).forEach(out.add, out);
    (this.transform.sort ?? []).forEach((m3) => out.add(m3.field));
    this.transform.window.map((w3) => w3.field).filter((f) => f !== void 0).forEach(out.add, out);
    return out;
  }
  producedFields() {
    return new Set(this.transform.window.map(this.getDefaultName));
  }
  getDefaultName(windowFieldDef) {
    return windowFieldDef.as ?? vgField(windowFieldDef);
  }
  hash() {
    return `WindowTransform ${hash(this.transform)}`;
  }
  assemble() {
    const fields = [];
    const ops2 = [];
    const as = [];
    const params2 = [];
    for (const window2 of this.transform.window) {
      ops2.push(window2.op);
      as.push(this.getDefaultName(window2));
      params2.push(window2.param === void 0 ? null : window2.param);
      fields.push(window2.field === void 0 ? null : window2.field);
    }
    const frame2 = this.transform.frame;
    const groupby = this.transform.groupby;
    if (frame2 && frame2[0] === null && frame2[1] === null && ops2.every((o) => isAggregateOp(o))) {
      return {
        type: "joinaggregate",
        as,
        ops: ops2,
        fields,
        ...groupby !== void 0 ? { groupby } : {}
      };
    }
    const sortFields = [];
    const sortOrder = [];
    if (this.transform.sort !== void 0) {
      for (const sortField of this.transform.sort) {
        sortFields.push(sortField.field);
        sortOrder.push(sortField.order ?? "ascending");
      }
    }
    const sort4 = {
      field: sortFields,
      order: sortOrder
    };
    const ignorePeers = this.transform.ignorePeers;
    return {
      type: "window",
      params: params2,
      as,
      ops: ops2,
      fields,
      sort: sort4,
      ...ignorePeers !== void 0 ? { ignorePeers } : {},
      ...groupby !== void 0 ? { groupby } : {},
      ...frame2 !== void 0 ? { frame: frame2 } : {}
    };
  }
};
function cloneSubtree(facet) {
  function clone(node) {
    if (!(node instanceof FacetNode)) {
      const copy5 = node.clone();
      if (copy5 instanceof OutputNode) {
        const newName = FACET_SCALE_PREFIX + copy5.getSource();
        copy5.setSource(newName);
        facet.model.component.data.outputNodes[newName] = copy5;
      } else if (copy5 instanceof AggregateNode || copy5 instanceof StackNode || copy5 instanceof WindowTransformNode || copy5 instanceof JoinAggregateTransformNode) {
        copy5.addDimensions(facet.fields);
      }
      for (const n of node.children.flatMap(clone)) {
        n.parent = copy5;
      }
      return [copy5];
    }
    return node.children.flatMap(clone);
  }
  return clone;
}
function moveFacetDown(node) {
  if (node instanceof FacetNode) {
    if (node.numChildren() === 1 && !(node.children[0] instanceof OutputNode)) {
      const child = node.children[0];
      if (child instanceof AggregateNode || child instanceof StackNode || child instanceof WindowTransformNode || child instanceof JoinAggregateTransformNode) {
        child.addDimensions(node.fields);
      }
      child.swapWithParent();
      moveFacetDown(node);
    } else {
      const facetMain = node.model.component.data.main;
      moveMainDownToFacet(facetMain);
      const cloner = cloneSubtree(node);
      const copy5 = node.children.map(cloner).flat();
      for (const c3 of copy5) {
        c3.parent = facetMain;
      }
    }
  } else {
    node.children.map(moveFacetDown);
  }
}
function moveMainDownToFacet(node) {
  if (node instanceof OutputNode && node.type === DataSourceType.Main) {
    if (node.numChildren() === 1) {
      const child = node.children[0];
      if (!(child instanceof FacetNode)) {
        child.swapWithParent();
        moveMainDownToFacet(node);
      }
    }
  }
}
var FACET_SCALE_PREFIX = "scale_";
var MAX_OPTIMIZATION_RUNS = 5;
function checkLinks(nodes) {
  for (const node of nodes) {
    for (const child of node.children) {
      if (child.parent !== node) {
        return false;
      }
    }
    if (!checkLinks(node.children)) {
      return false;
    }
  }
  return true;
}
function runOptimizer(optimizer, nodes) {
  let modified2 = false;
  for (const node of nodes) {
    modified2 = optimizer.optimize(node) || modified2;
  }
  return modified2;
}
function optimizationDataflowHelper(dataComponent, model, firstPass) {
  let roots = dataComponent.sources;
  let modified2 = false;
  modified2 = runOptimizer(new RemoveUnnecessaryOutputNodes(), roots) || modified2;
  modified2 = runOptimizer(new RemoveUnnecessaryIdentifierNodes(model), roots) || modified2;
  roots = roots.filter((r) => r.numChildren() > 0);
  modified2 = runOptimizer(new RemoveUnusedSubtrees(), roots) || modified2;
  roots = roots.filter((r) => r.numChildren() > 0);
  if (!firstPass) {
    modified2 = runOptimizer(new MoveParseUp(), roots) || modified2;
    modified2 = runOptimizer(new MergeBins(model), roots) || modified2;
    modified2 = runOptimizer(new RemoveDuplicateTimeUnits(), roots) || modified2;
    modified2 = runOptimizer(new MergeParse(), roots) || modified2;
    modified2 = runOptimizer(new MergeAggregates(), roots) || modified2;
    modified2 = runOptimizer(new MergeTimeUnits(), roots) || modified2;
    modified2 = runOptimizer(new MergeIdenticalNodes(), roots) || modified2;
    modified2 = runOptimizer(new MergeOutputs(), roots) || modified2;
  }
  dataComponent.sources = roots;
  return modified2;
}
function optimizeDataflow(data4, model) {
  checkLinks(data4.sources);
  let firstPassCounter = 0;
  let secondPassCounter = 0;
  for (let i = 0; i < MAX_OPTIMIZATION_RUNS; i++) {
    if (!optimizationDataflowHelper(data4, model, true)) {
      break;
    }
    firstPassCounter++;
  }
  data4.sources.map(moveFacetDown);
  for (let i = 0; i < MAX_OPTIMIZATION_RUNS; i++) {
    if (!optimizationDataflowHelper(data4, model, false)) {
      break;
    }
    secondPassCounter++;
  }
  checkLinks(data4.sources);
  if (Math.max(firstPassCounter, secondPassCounter) === MAX_OPTIMIZATION_RUNS) {
    warn3(`Maximum optimization runs(${MAX_OPTIMIZATION_RUNS}) reached.`);
  }
}
var SignalRefWrapper = class _SignalRefWrapper {
  constructor(exprGenerator) {
    Object.defineProperty(this, "signal", {
      enumerable: true,
      get: exprGenerator
    });
  }
  signal;
  // for ts
  static fromName(rename, signalName) {
    return new _SignalRefWrapper(() => rename(signalName));
  }
};
function parseScaleDomain2(model) {
  if (isUnitModel(model)) {
    parseUnitScaleDomain(model);
  } else {
    parseNonUnitScaleDomain(model);
  }
}
function parseUnitScaleDomain(model) {
  const localScaleComponents = model.component.scales;
  for (const channel of keys3(localScaleComponents)) {
    const domains = parseDomainForChannel(model, channel);
    const localScaleCmpt = localScaleComponents[channel];
    localScaleCmpt.setWithExplicit("domains", domains);
    parseSelectionDomain(model, channel);
    if (model.component.data.isFaceted) {
      let facetParent = model;
      while (!isFacetModel(facetParent) && facetParent.parent) {
        facetParent = facetParent.parent;
      }
      const resolve2 = facetParent.component.resolve.scale[channel];
      if (resolve2 === "shared") {
        for (const domain4 of domains.value) {
          if (isDataRefDomain(domain4)) {
            domain4.data = FACET_SCALE_PREFIX + domain4.data.replace(FACET_SCALE_PREFIX, "");
          }
        }
      }
    }
  }
}
function parseNonUnitScaleDomain(model) {
  for (const child of model.children) {
    parseScaleDomain2(child);
  }
  const localScaleComponents = model.component.scales;
  for (const channel of keys3(localScaleComponents)) {
    let domains;
    let selectionExtent = null;
    for (const child of model.children) {
      const childComponent = child.component.scales[channel];
      if (childComponent) {
        if (domains === void 0) {
          domains = childComponent.getWithExplicit("domains");
        } else {
          domains = mergeValuesWithExplicit(domains, childComponent.getWithExplicit("domains"), "domains", "scale", domainsTieBreaker);
        }
        const se = childComponent.get("selectionExtent");
        if (selectionExtent && se && selectionExtent.param !== se.param) {
          warn3(NEEDS_SAME_SELECTION);
        }
        selectionExtent = se;
      }
    }
    localScaleComponents[channel].setWithExplicit("domains", domains);
    if (selectionExtent) {
      localScaleComponents[channel].set("selectionExtent", selectionExtent, true);
    }
  }
}
function normalizeUnaggregatedDomain(domain4, fieldDef, scaleType2, scaleConfig) {
  if (domain4 === "unaggregated") {
    const { valid, reason } = canUseUnaggregatedDomain(fieldDef, scaleType2);
    if (!valid) {
      warn3(reason);
      return void 0;
    }
  } else if (domain4 === void 0 && scaleConfig.useUnaggregatedDomain) {
    const { valid } = canUseUnaggregatedDomain(fieldDef, scaleType2);
    if (valid) {
      return "unaggregated";
    }
  }
  return domain4;
}
function parseDomainForChannel(model, channel) {
  const scaleType2 = model.getScaleComponent(channel).get("type");
  const { encoding } = model;
  const domain4 = normalizeUnaggregatedDomain(model.scaleDomain(channel), model.typedFieldDef(channel), scaleType2, model.config.scale);
  if (domain4 !== model.scaleDomain(channel)) {
    model.specifiedScales[channel] = {
      ...model.specifiedScales[channel],
      domain: domain4
    };
  }
  if (channel === "x" && getFieldOrDatumDef(encoding.x2)) {
    if (getFieldOrDatumDef(encoding.x)) {
      return mergeValuesWithExplicit(parseSingleChannelDomain(scaleType2, domain4, model, "x"), parseSingleChannelDomain(scaleType2, domain4, model, "x2"), "domain", "scale", domainsTieBreaker);
    } else {
      return parseSingleChannelDomain(scaleType2, domain4, model, "x2");
    }
  } else if (channel === "y" && getFieldOrDatumDef(encoding.y2)) {
    if (getFieldOrDatumDef(encoding.y)) {
      return mergeValuesWithExplicit(parseSingleChannelDomain(scaleType2, domain4, model, "y"), parseSingleChannelDomain(scaleType2, domain4, model, "y2"), "domain", "scale", domainsTieBreaker);
    } else {
      return parseSingleChannelDomain(scaleType2, domain4, model, "y2");
    }
  }
  return parseSingleChannelDomain(scaleType2, domain4, model, channel);
}
function mapDomainToDataSignal(domain4, type4, timeUnit) {
  return domain4.map((v2) => {
    const data4 = valueExpr(v2, { timeUnit, type: type4 });
    return { signal: `{data: ${data4}}` };
  });
}
function convertDomainIfItIsDateTime(domain4, type4, timeUnit) {
  const normalizedTimeUnit = normalizeTimeUnit(timeUnit)?.unit;
  if (type4 === "temporal" || normalizedTimeUnit) {
    return mapDomainToDataSignal(domain4, type4, normalizedTimeUnit);
  }
  return [domain4];
}
function parseSingleChannelDomain(scaleType2, domain4, model, channel) {
  const { encoding, markDef, mark, config, stack: stack2 } = model;
  const fieldOrDatumDef = getFieldOrDatumDef(encoding[channel]);
  const { type: type4 } = fieldOrDatumDef;
  const timeUnit = fieldOrDatumDef["timeUnit"];
  const dataSourceTypeForScaleDomain = getScaleDataSourceForHandlingInvalidValues({
    invalid: getMarkConfig("invalid", markDef, config),
    isPath: isPathMark(mark)
  });
  if (isDomainUnionWith(domain4)) {
    const defaultDomain = parseSingleChannelDomain(scaleType2, void 0, model, channel);
    const unionWith = convertDomainIfItIsDateTime(domain4.unionWith, type4, timeUnit);
    return makeExplicit([...unionWith, ...defaultDomain.value]);
  } else if (isSignalRef(domain4)) {
    return makeExplicit([domain4]);
  } else if (domain4 && domain4 !== "unaggregated" && !isParameterDomain(domain4)) {
    return makeExplicit(convertDomainIfItIsDateTime(domain4, type4, timeUnit));
  }
  if (stack2 && channel === stack2.fieldChannel) {
    if (stack2.offset === "normalize") {
      return makeImplicit([[0, 1]]);
    }
    const data4 = model.requestDataName(dataSourceTypeForScaleDomain);
    return makeImplicit([
      {
        data: data4,
        field: model.vgField(channel, { suffix: "start" })
      },
      {
        data: data4,
        field: model.vgField(channel, { suffix: "end" })
      }
    ]);
  }
  const sort4 = isScaleChannel(channel) && isFieldDef(fieldOrDatumDef) ? domainSort(model, channel, scaleType2) : void 0;
  if (isDatumDef(fieldOrDatumDef)) {
    const d = convertDomainIfItIsDateTime([fieldOrDatumDef.datum], type4, timeUnit);
    return makeImplicit(d);
  }
  const fieldDef = fieldOrDatumDef;
  if (domain4 === "unaggregated") {
    const { field: field3 } = fieldOrDatumDef;
    return makeImplicit([
      {
        data: model.requestDataName(dataSourceTypeForScaleDomain),
        field: vgField({ field: field3, aggregate: "min" })
      },
      {
        data: model.requestDataName(dataSourceTypeForScaleDomain),
        field: vgField({ field: field3, aggregate: "max" })
      }
    ]);
  } else if (isBinning(fieldDef.bin)) {
    if (hasDiscreteDomain(scaleType2)) {
      if (scaleType2 === "bin-ordinal") {
        return makeImplicit([]);
      }
      return makeImplicit([
        {
          // If sort by aggregation of a specified sort field, we need to use RAW table,
          // so we can aggregate values for the scale independently from the main aggregation.
          data: isBoolean3(sort4) ? model.requestDataName(dataSourceTypeForScaleDomain) : model.requestDataName(DataSourceType.Raw),
          // Use range if we added it and the scale does not support computing a range as a signal.
          field: model.vgField(channel, binRequiresRange(fieldDef, channel) ? { binSuffix: "range" } : {}),
          // we have to use a sort object if sort = true to make the sort correct by bin start
          sort: sort4 === true || !isObject(sort4) ? {
            field: model.vgField(channel, {}),
            op: "min"
          } : sort4
        }
      ]);
    } else {
      const { bin: bin3 } = fieldDef;
      if (isBinning(bin3)) {
        const binSignal = getBinSignalName(model, fieldDef.field, bin3);
        return makeImplicit([
          new SignalRefWrapper(() => {
            const signal = model.getSignalName(binSignal);
            return `[${signal}.start, ${signal}.stop]`;
          })
        ]);
      } else {
        return makeImplicit([
          {
            data: model.requestDataName(dataSourceTypeForScaleDomain),
            field: model.vgField(channel, {})
          }
        ]);
      }
    }
  } else if (fieldDef.timeUnit && contains2(["time", "utc"], scaleType2)) {
    const fieldDef2 = encoding[getSecondaryRangeChannel(channel)];
    if (hasBandEnd(fieldDef, fieldDef2, markDef, config)) {
      const data4 = model.requestDataName(dataSourceTypeForScaleDomain);
      const bandPosition = getBandPosition({ fieldDef, fieldDef2, markDef, config });
      const isRectWithOffset = isRectBasedMark(mark) && bandPosition !== 0.5 && isXorY(channel);
      return makeImplicit([
        {
          data: data4,
          field: model.vgField(channel, isRectWithOffset ? { suffix: OFFSETTED_RECT_START_SUFFIX } : {})
        },
        {
          data: data4,
          field: model.vgField(channel, { suffix: isRectWithOffset ? OFFSETTED_RECT_END_SUFFIX : "end" })
        }
      ]);
    }
  }
  if (sort4) {
    return makeImplicit([
      {
        // If sort by aggregation of a specified sort field, we need to use RAW table,
        // so we can aggregate values for the scale independently from the main aggregation.
        data: isBoolean3(sort4) ? model.requestDataName(dataSourceTypeForScaleDomain) : model.requestDataName(DataSourceType.Raw),
        field: model.vgField(channel),
        sort: sort4
      }
    ]);
  } else {
    return makeImplicit([
      {
        data: model.requestDataName(dataSourceTypeForScaleDomain),
        field: model.vgField(channel)
      }
    ]);
  }
}
function normalizeSortField(sort4, isStackedMeasure) {
  const { op, field: field3, order } = sort4;
  return {
    // Apply default op
    op: op ?? (isStackedMeasure ? "sum" : DEFAULT_SORT_OP),
    // flatten nested fields
    ...field3 ? { field: replacePathInField(field3) } : {},
    ...order ? { order } : {}
  };
}
function parseSelectionDomain(model, channel) {
  const scale9 = model.component.scales[channel];
  const spec = model.specifiedScales[channel].domain;
  const bin3 = model.fieldDef(channel)?.bin;
  const domain4 = isParameterDomain(spec) ? spec : void 0;
  const extent2 = isBinParams(bin3) && isParameterExtent(bin3.extent) ? bin3.extent : void 0;
  if (domain4 || extent2) {
    scale9.set("selectionExtent", domain4 ?? extent2, true);
  }
}
function domainSort(model, channel, scaleType2) {
  if (!hasDiscreteDomain(scaleType2)) {
    return void 0;
  }
  const fieldDef = model.fieldDef(channel);
  const sort4 = fieldDef.sort;
  if (isSortArray(sort4)) {
    return {
      op: "min",
      field: sortArrayIndexField(fieldDef, channel),
      order: "ascending"
    };
  }
  const { stack: stack2 } = model;
  const stackDimensions = stack2 ? /* @__PURE__ */ new Set([...stack2.groupbyFields, ...stack2.stackBy.map((s) => s.fieldDef.field)]) : void 0;
  if (isSortField(sort4)) {
    const isStackedMeasure = stack2 && !stackDimensions.has(sort4.field);
    return normalizeSortField(sort4, isStackedMeasure);
  } else if (isSortByEncoding(sort4)) {
    const { encoding, order } = sort4;
    const fieldDefToSortBy = model.fieldDef(encoding);
    const { aggregate, field: field3 } = fieldDefToSortBy;
    const isStackedMeasure = stack2 && !stackDimensions.has(field3);
    if (isArgminDef(aggregate) || isArgmaxDef(aggregate)) {
      return normalizeSortField({
        field: vgField(fieldDefToSortBy),
        order
      }, isStackedMeasure);
    } else if (isAggregateOp(aggregate) || !aggregate) {
      return normalizeSortField({
        op: aggregate,
        // can't be argmin/argmax since we don't support them in encoding field def
        field: field3,
        order
      }, isStackedMeasure);
    }
  } else if (sort4 === "descending") {
    return {
      op: "min",
      field: model.vgField(channel),
      order: "descending"
    };
  } else if (contains2([
    "ascending",
    void 0
    /* default =ascending*/
  ], sort4)) {
    return true;
  }
  return void 0;
}
function canUseUnaggregatedDomain(fieldDef, scaleType2) {
  const { aggregate, type: type4 } = fieldDef;
  if (!aggregate) {
    return {
      valid: false,
      reason: unaggregateDomainHasNoEffectForRawField(fieldDef)
    };
  }
  if (isString(aggregate) && !SHARED_DOMAIN_OPS.has(aggregate)) {
    return {
      valid: false,
      reason: unaggregateDomainWithNonSharedDomainOp(aggregate)
    };
  }
  if (type4 === "quantitative") {
    if (scaleType2 === "log") {
      return {
        valid: false,
        reason: unaggregatedDomainWithLogScale(fieldDef)
      };
    }
  }
  return { valid: true };
}
function domainsTieBreaker(v1, v2, property2, propertyOf) {
  if (v1.explicit && v2.explicit) {
    warn3(mergeConflictingDomainProperty(property2, propertyOf, v1.value, v2.value));
  }
  return { explicit: v1.explicit, value: [...v1.value, ...v2.value] };
}
function mergeDomains(domains) {
  const uniqueDomains = unique(domains.map((domain4) => {
    if (isDataRefDomain(domain4)) {
      const { sort: _s, ...domainWithoutSort } = domain4;
      return domainWithoutSort;
    }
    return domain4;
  }), hash);
  const sorts = unique(domains.map((d) => {
    if (isDataRefDomain(d)) {
      const s = d.sort;
      if (s !== void 0 && !isBoolean3(s)) {
        if ("op" in s && s.op === "count") {
          delete s.field;
        }
        if (s.order === "ascending") {
          delete s.order;
        }
      }
      return s;
    }
    return void 0;
  }).filter((s) => s !== void 0), hash);
  if (uniqueDomains.length === 0) {
    return void 0;
  } else if (uniqueDomains.length === 1) {
    const domain4 = domains[0];
    if (isDataRefDomain(domain4) && sorts.length > 0) {
      let sort5 = sorts[0];
      if (sorts.length > 1) {
        warn3(MORE_THAN_ONE_SORT);
        const filteredSorts = sorts.filter((s) => isObject(s) && "op" in s && s.op !== "min");
        if (sorts.every((s) => isObject(s) && "op" in s) && filteredSorts.length === 1) {
          sort5 = filteredSorts[0];
        } else {
          sort5 = true;
        }
      } else {
        if (isObject(sort5) && "field" in sort5) {
          const sortField = sort5.field;
          if (domain4.field === sortField) {
            sort5 = sort5.order ? { order: sort5.order } : true;
          }
        }
      }
      return {
        ...domain4,
        sort: sort5
      };
    }
    return domain4;
  }
  const unionDomainSorts = unique(sorts.map((s) => {
    if (isBoolean3(s) || !("op" in s) || isString(s.op) && hasOwnProperty(MULTIDOMAIN_SORT_OP_INDEX, s.op)) {
      return s;
    }
    warn3(domainSortDropped(s));
    return true;
  }), hash);
  let sort4;
  if (unionDomainSorts.length === 1) {
    sort4 = unionDomainSorts[0];
  } else if (unionDomainSorts.length > 1) {
    warn3(MORE_THAN_ONE_SORT);
    sort4 = true;
  }
  const allData = unique(domains.map((d) => {
    if (isDataRefDomain(d)) {
      return d.data;
    }
    return null;
  }), (x6) => x6);
  if (allData.length === 1 && allData[0] !== null) {
    const domain4 = {
      data: allData[0],
      fields: uniqueDomains.map((d) => d.field),
      ...sort4 ? { sort: sort4 } : {}
    };
    return domain4;
  }
  return { fields: uniqueDomains, ...sort4 ? { sort: sort4 } : {} };
}
function getFieldFromDomain(domain4) {
  if (isDataRefDomain(domain4) && isString(domain4.field)) {
    return domain4.field;
  } else if (isDataRefUnionedDomain(domain4)) {
    let field3;
    for (const nonUnionDomain of domain4.fields) {
      if (isDataRefDomain(nonUnionDomain) && isString(nonUnionDomain.field)) {
        if (!field3) {
          field3 = nonUnionDomain.field;
        } else if (field3 !== nonUnionDomain.field) {
          warn3(FACETED_INDEPENDENT_DIFFERENT_SOURCES);
          return field3;
        }
      }
    }
    warn3(FACETED_INDEPENDENT_SAME_FIELDS_DIFFERENT_SOURCES);
    return field3;
  } else if (isFieldRefUnionDomain(domain4)) {
    warn3(FACETED_INDEPENDENT_SAME_SOURCE);
    const field3 = domain4.fields[0];
    return isString(field3) ? field3 : void 0;
  }
  return void 0;
}
function assembleDomain(model, channel) {
  const scaleComponent = model.component.scales[channel];
  const domains = scaleComponent.get("domains").map((domain4) => {
    if (isDataRefDomain(domain4)) {
      domain4.data = model.lookupDataSource(domain4.data);
    }
    return domain4;
  });
  return mergeDomains(domains);
}
function setLegendEncode(legend, part, vgProp, vgRef) {
  legend.encode ??= {};
  legend.encode[part] ??= {};
  legend.encode[part].update ??= {};
  legend.encode[part].update[vgProp] = vgRef;
}
function getFieldKeyForChannel(model, channel) {
  if (isUnitModel(model)) {
    const fd = model.fieldDef(channel);
    if (fd?.field) {
      return fd.field;
    }
  }
  const childFields = (model.children ?? []).map((child) => getFieldKeyForChannel(child, channel)).filter((f) => !!f);
  if (childFields.length > 0) {
    const unique$1 = unique(childFields, hash);
    if (unique$1.length === 1) {
      return unique$1[0];
    }
    return void 0;
  }
  return void 0;
}
function legendsAreMergeCompatible(model, channelA, channelB) {
  if (channelA === channelB)
    return true;
  const typeA = model.getScaleType(channelA);
  const typeB = model.getScaleType(channelB);
  if (!typeA || !typeB)
    return false;
  const aIsDiscrete = hasDiscreteDomain(typeA);
  const bIsDiscrete = hasDiscreteDomain(typeB);
  return aIsDiscrete === bIsDiscrete;
}
function getLegendGroupKey(fieldKey2, channel) {
  return fieldKey2 ? `field:${fieldKey2}` : `channel:${String(channel)}`;
}
function extractDiscreteValuesFromDomain(domain4) {
  if (isArray(domain4)) {
    const primitives = domain4.filter(isPrimitive);
    return primitives.length > 0 ? primitives : null;
  }
  if (isDataRefUnionedDomain(domain4)) {
    const values4 = [];
    values4.push(...domain4.fields.flatMap((f) => isArray(f) ? f.filter(isPrimitive) : []));
    if (values4.length > 0) {
      return unique(values4, hash);
    }
  }
  return null;
}
function getDiscreteValuesForChannel(model, channel) {
  try {
    const domain4 = assembleDomain(model, channel);
    return extractDiscreteValuesFromDomain(domain4);
  } catch {
    return null;
  }
}
function unionDiscreteValuesForChannels(model, channelA, channelB) {
  const vA = getDiscreteValuesForChannel(model, channelA);
  const vB = getDiscreteValuesForChannel(model, channelB);
  return vA && vB ? unique([...vA, ...vB], hash) : null;
}
function setImplicitLegendValues(cmpt, values4, warnMessage) {
  if (values4 && values4.length > 0) {
    const valuesProp = cmpt.getWithExplicit("values");
    if (!valuesProp?.explicit) {
      if (warnMessage) {
        warn3(warnMessage);
      }
      cmpt.set("values", values4, false);
    }
  }
}
function domainsExplicitAndEqual(model, channelA, channelB) {
  const scA = model.getScaleComponent(channelA);
  const scB = model.getScaleComponent(channelB);
  if (!scA || !scB)
    return false;
  const dA = scA.getWithExplicit("domains");
  const dB = scB.getWithExplicit("domains");
  if (!(dA?.explicit && dB?.explicit))
    return false;
  const assembledA = assembleDomain(model, channelA);
  const assembledB = assembleDomain(model, channelB);
  return hash(assembledA) === hash(assembledB);
}
function assembleLegends(model) {
  const legendComponentIndex = model.component.legends;
  const legendsByGroup = {};
  for (const channel of keys3(legendComponentIndex)) {
    const fieldKey2 = getFieldKeyForChannel(model, channel);
    const groupKey = getLegendGroupKey(fieldKey2, channel);
    if (!legendsByGroup[groupKey]) {
      legendsByGroup[groupKey] = [{ channel, cmpt: legendComponentIndex[channel].clone() }];
      continue;
    }
    let mergedIntoExisting = false;
    for (const existing of legendsByGroup[groupKey]) {
      if (!legendsAreMergeCompatible(model, existing.channel, channel)) {
        continue;
      }
      const merged = mergeLegendComponent(existing.cmpt, legendComponentIndex[channel]);
      if (merged) {
        const typeA = model.getScaleType(existing.channel);
        const typeB = model.getScaleType(channel);
        if (typeA && typeB && hasDiscreteDomain(typeA) && hasDiscreteDomain(typeB)) {
          if (domainsExplicitAndEqual(model, existing.channel, channel)) {
            setImplicitLegendValues(existing.cmpt, getDiscreteValuesForChannel(model, existing.channel));
          } else {
            setImplicitLegendValues(
              existing.cmpt,
              unionDiscreteValuesForChannels(model, existing.channel, channel),
              // Warn when unioning discrete legend values so that users are aware
              legendValuesUnioned(existing.channel, channel)
            );
          }
        }
        mergedIntoExisting = true;
        break;
      }
    }
    if (!mergedIntoExisting) {
      legendsByGroup[groupKey].push({ channel, cmpt: legendComponentIndex[channel].clone() });
    }
  }
  const legends = vals(legendsByGroup).flat().map((entry2) => assembleLegend(entry2.cmpt, model.config)).filter((l) => l !== void 0);
  return legends;
}
function assembleLegend(legendCmpt, config) {
  const { disable, labelExpr, selections, ...legend } = legendCmpt.combine();
  if (disable) {
    return void 0;
  }
  if (config.aria === false && legend.aria == void 0) {
    legend.aria = false;
  }
  if (legend.encode?.symbols) {
    const out = legend.encode.symbols.update;
    if (out.fill && out.fill["value"] !== "transparent" && !out.stroke && !legend.stroke) {
      out.stroke = { value: "transparent" };
    }
    for (const property2 of LEGEND_SCALE_CHANNELS) {
      if (legend[property2]) {
        delete out[property2];
      }
    }
  }
  if (!legend.title) {
    delete legend.title;
  }
  if (labelExpr !== void 0) {
    let expr2 = labelExpr;
    if (legend.encode?.labels?.update && isSignalRef(legend.encode.labels.update.text)) {
      expr2 = replaceAll(labelExpr, "datum.label", legend.encode.labels.update.text.signal);
    }
    setLegendEncode(legend, "labels", "text", { signal: expr2 });
  }
  return legend;
}
function assembleProjections(model) {
  if (isLayerModel(model) || isConcatModel(model)) {
    return assembleProjectionsForModelAndChildren(model);
  } else {
    return assembleProjectionForModel(model);
  }
}
function assembleProjectionsForModelAndChildren(model) {
  return model.children.reduce((projections2, child) => {
    return projections2.concat(child.assembleProjections());
  }, assembleProjectionForModel(model));
}
function assembleProjectionForModel(model) {
  const component = model.component.projection;
  if (!component || component.merged) {
    return [];
  }
  const projection3 = component.combine();
  const { name } = projection3;
  if (!component.data) {
    return [
      {
        name,
        // translate to center by default
        translate: { signal: "[width / 2, height / 2]" },
        // parameters, overwrite default translate if specified
        ...projection3
      }
    ];
  } else {
    const size = {
      signal: `[${component.size.map((ref2) => ref2.signal).join(", ")}]`
    };
    const fits = component.data.reduce((sources, data4) => {
      const source4 = isSignalRef(data4) ? data4.signal : `data('${model.lookupDataSource(data4)}')`;
      if (!contains2(sources, source4)) {
        sources.push(source4);
      }
      return sources;
    }, []);
    if (fits.length <= 0) {
      throw new Error("Projection's fit didn't find any data sources");
    }
    return [
      {
        name,
        size,
        fit: {
          signal: fits.length > 1 ? `[${fits.join(", ")}]` : fits[0]
        },
        ...projection3
      }
    ];
  }
}
var PROJECTION_PROPERTIES = [
  "type",
  "clipAngle",
  "clipExtent",
  "center",
  "rotate",
  "precision",
  "reflectX",
  "reflectY",
  "coefficient",
  "distance",
  "fraction",
  "lobes",
  "parallel",
  "radius",
  "ratio",
  "spacing",
  "tilt"
];
var ProjectionComponent = class extends Split {
  specifiedProjection;
  size;
  data;
  merged = false;
  constructor(name, specifiedProjection, size, data4) {
    super(
      { ...specifiedProjection },
      // all explicit properties of projection
      { name }
    );
    this.specifiedProjection = specifiedProjection;
    this.size = size;
    this.data = data4;
  }
  /**
   * Whether the projection parameters should fit provided data.
   */
  get isFit() {
    return !!this.data;
  }
};
function parseProjection2(model) {
  model.component.projection = isUnitModel(model) ? parseUnitProjection(model) : parseNonUnitProjections(model);
}
function parseUnitProjection(model) {
  if (model.hasProjection) {
    const proj = replaceExprRef(model.specifiedProjection);
    const fit3 = !(proj && (proj.scale != null || proj.translate != null));
    const size = fit3 ? [model.getSizeSignalRef("width"), model.getSizeSignalRef("height")] : void 0;
    const data4 = fit3 ? gatherFitData(model) : void 0;
    const projComp = new ProjectionComponent(model.projectionName(true), {
      ...replaceExprRef(model.config.projection),
      ...proj
    }, size, data4);
    if (!projComp.get("type")) {
      projComp.set("type", "equalEarth", false);
    }
    return projComp;
  }
  return void 0;
}
function gatherFitData(model) {
  const data4 = [];
  const { encoding } = model;
  for (const posssiblePair of [
    [LONGITUDE, LATITUDE],
    [LONGITUDE2, LATITUDE2]
  ]) {
    if (getFieldOrDatumDef(encoding[posssiblePair[0]]) || getFieldOrDatumDef(encoding[posssiblePair[1]])) {
      data4.push({
        signal: model.getName(`geojson_${data4.length}`)
      });
    }
  }
  if (model.channelHasField(SHAPE) && model.typedFieldDef(SHAPE).type === GEOJSON) {
    data4.push({
      signal: model.getName(`geojson_${data4.length}`)
    });
  }
  if (data4.length === 0) {
    data4.push(model.requestDataName(DataSourceType.Main));
  }
  return data4;
}
function mergeIfNoConflict(first, second2) {
  const allPropertiesShared = every(PROJECTION_PROPERTIES, (prop) => {
    if (!hasOwnProperty(first.explicit, prop) && !hasOwnProperty(second2.explicit, prop)) {
      return true;
    }
    if (hasOwnProperty(first.explicit, prop) && hasOwnProperty(second2.explicit, prop) && // some properties might be signals or objects and require hashing for comparison
    deepEqual(first.get(prop), second2.get(prop))) {
      return true;
    }
    return false;
  });
  const size = deepEqual(first.size, second2.size);
  if (size) {
    if (allPropertiesShared) {
      return first;
    } else if (deepEqual(first.explicit, {})) {
      return second2;
    } else if (deepEqual(second2.explicit, {})) {
      return first;
    }
  }
  return null;
}
function parseNonUnitProjections(model) {
  if (model.children.length === 0) {
    return void 0;
  }
  let nonUnitProjection;
  for (const child of model.children) {
    parseProjection2(child);
  }
  const mergable = every(model.children, (child) => {
    const projection3 = child.component.projection;
    if (!projection3) {
      return true;
    } else if (!nonUnitProjection) {
      nonUnitProjection = projection3;
      return true;
    } else {
      const merge6 = mergeIfNoConflict(nonUnitProjection, projection3);
      if (merge6) {
        nonUnitProjection = merge6;
      }
      return !!merge6;
    }
  });
  if (nonUnitProjection && mergable) {
    const name = model.projectionName(true);
    const modelProjection = new ProjectionComponent(name, nonUnitProjection.specifiedProjection, nonUnitProjection.size, duplicate(nonUnitProjection.data));
    for (const child of model.children) {
      const projection3 = child.component.projection;
      if (projection3) {
        if (projection3.isFit) {
          modelProjection.data.push(...child.component.projection.data);
        }
        child.renameProjection(projection3.get("name"), name);
        projection3.merged = true;
      }
    }
    return modelProjection;
  }
  return void 0;
}
function assembleScales(model) {
  if (isLayerModel(model) || isConcatModel(model)) {
    return model.children.reduce((scales2, child) => {
      return scales2.concat(assembleScales(child));
    }, assembleScalesForModel(model));
  } else {
    return assembleScalesForModel(model);
  }
}
function assembleScalesForModel(model) {
  return keys3(model.component.scales).reduce((scales2, channel) => {
    const scaleComponent = model.component.scales[channel];
    if (scaleComponent.merged) {
      return scales2;
    }
    const scale9 = scaleComponent.combine();
    const { name, type: type4, selectionExtent, domains: _d2, range: _r2, reverse: reverse4, ...otherScaleProps } = scale9;
    const range7 = assembleScaleRange(scale9.range, name, channel, model);
    const domain4 = assembleDomain(model, channel);
    const domainRaw = selectionExtent ? assembleSelectionScaleDomain(model, selectionExtent, scaleComponent, domain4) : null;
    scales2.push({
      name,
      type: type4,
      ...domain4 ? { domain: domain4 } : {},
      ...domainRaw ? { domainRaw } : {},
      range: range7,
      ...reverse4 !== void 0 ? { reverse: reverse4 } : {},
      ...otherScaleProps
    });
    return scales2;
  }, []);
}
function assembleScaleRange(scaleRange, scaleName, channel, model) {
  if (isXorY(channel)) {
    if (isVgRangeStep(scaleRange)) {
      return {
        step: { signal: `${scaleName}_step` }
      };
    }
  } else if (isObject(scaleRange) && isDataRefDomain(scaleRange)) {
    return {
      ...scaleRange,
      data: model.lookupDataSource(scaleRange.data)
    };
  }
  return scaleRange;
}
var ScaleComponent = class extends Split {
  merged = false;
  constructor(name, typeWithExplicit) {
    super(
      {},
      // no initial explicit property
      { name }
    );
    this.setWithExplicit("type", typeWithExplicit);
  }
  /**
   * Whether the scale definitely includes or not include zero in the domain
   */
  domainHasZero() {
    const scaleType2 = this.get("type");
    if (contains2([ScaleType.LOG, ScaleType.TIME, ScaleType.UTC], scaleType2)) {
      return "definitely-not";
    }
    const scaleZero = this.get("zero");
    if (scaleZero === true || // If zero is undefined, linear/sqrt/pow scales have zero by default.
    scaleZero === void 0 && contains2([ScaleType.LINEAR, ScaleType.SQRT, ScaleType.POW], scaleType2)) {
      return "definitely";
    }
    const domains = this.get("domains");
    if (domains.length > 0) {
      let hasExplicitDomainWithZero = false;
      let hasExplicitDomainWithoutZero = false;
      let hasDomainBasedOnField = false;
      for (const d of domains) {
        if (isArray(d)) {
          const first = d[0];
          const last = d[d.length - 1];
          if (isNumber(first) && isNumber(last)) {
            if (first <= 0 && last >= 0) {
              hasExplicitDomainWithZero = true;
              continue;
            } else {
              hasExplicitDomainWithoutZero = true;
              continue;
            }
          }
        }
        hasDomainBasedOnField = true;
      }
      if (hasExplicitDomainWithZero) {
        return "definitely";
      } else if (hasExplicitDomainWithoutZero && !hasDomainBasedOnField) {
        return "definitely-not";
      }
    }
    return "maybe";
  }
};
var RANGE_PROPERTIES = ["range", "scheme"];
function parseUnitScaleRange(model) {
  const localScaleComponents = model.component.scales;
  for (const channel of SCALE_CHANNELS) {
    const localScaleCmpt = localScaleComponents[channel];
    if (!localScaleCmpt) {
      continue;
    }
    const rangeWithExplicit = parseRangeForChannel(channel, model);
    localScaleCmpt.setWithExplicit("range", rangeWithExplicit);
  }
}
function getBinStepSignal(model, channel) {
  const fieldDef = model.fieldDef(channel);
  if (fieldDef?.bin) {
    const { bin: bin3, field: field3 } = fieldDef;
    const sizeType = getSizeChannel(channel);
    const sizeSignal = model.getName(sizeType);
    if (isObject(bin3) && bin3.binned && bin3.step !== void 0) {
      return new SignalRefWrapper(() => {
        const scaleName = model.scaleName(channel);
        const binCount = `(domain("${scaleName}")[1] - domain("${scaleName}")[0]) / ${bin3.step}`;
        return `${model.getSignalName(sizeSignal)} / (${binCount})`;
      });
    } else if (isBinning(bin3)) {
      const binSignal = getBinSignalName(model, field3, bin3);
      return new SignalRefWrapper(() => {
        const updatedName = model.getSignalName(binSignal);
        const binCount = `(${updatedName}.stop - ${updatedName}.start) / ${updatedName}.step`;
        return `${model.getSignalName(sizeSignal)} / (${binCount})`;
      });
    }
  }
  return void 0;
}
function parseRangeForChannel(channel, model) {
  const specifiedScale = model.specifiedScales[channel];
  const { size } = model;
  const mergedScaleCmpt = model.getScaleComponent(channel);
  const scaleType2 = mergedScaleCmpt.get("type");
  for (const property2 of RANGE_PROPERTIES) {
    if (specifiedScale[property2] !== void 0) {
      const supportedByScaleType = scaleTypeSupportProperty(scaleType2, property2);
      const channelIncompatability = channelScalePropertyIncompatability(channel, property2);
      if (!supportedByScaleType) {
        warn3(scalePropertyNotWorkWithScaleType(scaleType2, property2, channel));
      } else if (channelIncompatability) {
        warn3(channelIncompatability);
      } else {
        switch (property2) {
          case "range": {
            const range7 = specifiedScale.range;
            if (isArray(range7)) {
              if (isXorY(channel)) {
                return makeExplicit(range7.map((v2) => {
                  if (v2 === "width" || v2 === "height") {
                    const sizeSignal = model.getName(v2);
                    const getSignalName = model.getSignalName.bind(model);
                    return SignalRefWrapper.fromName(getSignalName, sizeSignal);
                  }
                  return v2;
                }));
              }
            } else if (isObject(range7)) {
              return makeExplicit({
                data: model.requestDataName(DataSourceType.Main),
                field: range7.field,
                sort: { op: "min", field: model.vgField(channel) }
              });
            }
            return makeExplicit(range7);
          }
          case "scheme":
            return makeExplicit(parseScheme(specifiedScale[property2]));
        }
      }
    }
  }
  const sizeChannel = channel === X3 || channel === "xOffset" ? "width" : "height";
  const sizeValue = size[sizeChannel];
  if (isStep(sizeValue)) {
    if (isXorY(channel)) {
      if (hasDiscreteDomain(scaleType2)) {
        const step = getPositionStep(sizeValue, model, channel);
        if (step) {
          return makeExplicit({ step });
        }
      } else {
        warn3(stepDropped(sizeChannel));
      }
    } else if (isXorYOffset(channel)) {
      const positionChannel = channel === XOFFSET ? "x" : "y";
      const positionScaleCmpt = model.getScaleComponent(positionChannel);
      const positionScaleType = positionScaleCmpt.get("type");
      if (positionScaleType === "band") {
        const step = getOffsetStep(sizeValue, scaleType2);
        if (step) {
          return makeExplicit(step);
        }
      }
    }
  }
  const { rangeMin: rangeMin2, rangeMax: rangeMax2 } = specifiedScale;
  const d = defaultRange(channel, model);
  if ((rangeMin2 !== void 0 || rangeMax2 !== void 0) && // it's ok to check just rangeMin's compatibility since rangeMin/rangeMax are the same
  scaleTypeSupportProperty(scaleType2, "rangeMin") && isArray(d) && d.length === 2) {
    return makeExplicit([rangeMin2 ?? d[0], rangeMax2 ?? d[1]]);
  }
  return makeImplicit(d);
}
function parseScheme(scheme3) {
  if (isExtendedScheme(scheme3)) {
    return {
      scheme: scheme3.name,
      ...omit(scheme3, ["name"])
    };
  }
  return { scheme: scheme3 };
}
function fullWidthOrHeightRange(channel, model, scaleType2, { center } = {}) {
  const sizeType = getSizeChannel(channel);
  const sizeSignal = model.getName(sizeType);
  const getSignalName = model.getSignalName.bind(model);
  if (channel === Y3 && hasContinuousDomain(scaleType2)) {
    return center ? [
      SignalRefWrapper.fromName((name) => `${getSignalName(name)}/2`, sizeSignal),
      SignalRefWrapper.fromName((name) => `-${getSignalName(name)}/2`, sizeSignal)
    ] : [SignalRefWrapper.fromName(getSignalName, sizeSignal), 0];
  } else {
    return center ? [
      SignalRefWrapper.fromName((name) => `-${getSignalName(name)}/2`, sizeSignal),
      SignalRefWrapper.fromName((name) => `${getSignalName(name)}/2`, sizeSignal)
    ] : [0, SignalRefWrapper.fromName(getSignalName, sizeSignal)];
  }
}
function defaultRange(channel, model) {
  const { size, config, mark, encoding } = model;
  const { type: type4 } = getFieldOrDatumDef(encoding[channel]);
  const mergedScaleCmpt = model.getScaleComponent(channel);
  const scaleType2 = mergedScaleCmpt.get("type");
  const { domain: domain4, domainMid } = model.specifiedScales[channel];
  switch (channel) {
    case X3:
    case Y3: {
      if (contains2(["point", "band"], scaleType2)) {
        const positionSize = getDiscretePositionSize(channel, size, config.view);
        if (isStep(positionSize)) {
          const step = getPositionStep(positionSize, model, channel);
          return { step };
        }
      }
      return fullWidthOrHeightRange(channel, model, scaleType2);
    }
    case XOFFSET:
    case YOFFSET:
      return getOffsetRange(channel, model, scaleType2);
    case SIZE2: {
      const rangeMin2 = sizeRangeMin(mark, config);
      const rangeMax2 = sizeRangeMax(mark, size, model, config);
      if (isContinuousToDiscrete(scaleType2)) {
        return interpolateRange2(rangeMin2, rangeMax2, defaultContinuousToDiscreteCount(scaleType2, config, domain4, channel));
      } else {
        return [rangeMin2, rangeMax2];
      }
    }
    case THETA:
      return [0, Math.PI * 2];
    case ANGLE:
      return [0, 360];
    case RADIUS: {
      return [
        0,
        new SignalRefWrapper(() => {
          const w3 = model.getSignalName(isFacetModel(model.parent) ? "child_width" : "width");
          const h3 = model.getSignalName(isFacetModel(model.parent) ? "child_height" : "height");
          return `min(${w3},${h3})/2`;
        })
      ];
    }
    case TIME: {
      return { step: 1e3 / config.scale.framesPerSecond };
    }
    case STROKEWIDTH:
      return [config.scale.minStrokeWidth, config.scale.maxStrokeWidth];
    case STROKEDASH:
      return [
        // TODO: add this to Vega's config.range?
        [1, 0],
        [4, 2],
        [2, 1],
        [1, 1],
        [1, 2, 4, 2]
      ];
    case SHAPE:
      return "symbol";
    case COLOR:
    case FILL:
    case STROKE:
      if (scaleType2 === "ordinal") {
        return type4 === "nominal" ? "category" : "ordinal";
      } else {
        if (domainMid !== void 0) {
          return "diverging";
        } else {
          return mark === "rect" || mark === "geoshape" ? "heatmap" : "ramp";
        }
      }
    case OPACITY:
    case FILLOPACITY:
    case STROKEOPACITY:
      return [config.scale.minOpacity, config.scale.maxOpacity];
  }
}
function getPositionStep(step, model, channel) {
  const { encoding } = model;
  const mergedScaleCmpt = model.getScaleComponent(channel);
  const offsetChannel = getOffsetScaleChannel(channel);
  const offsetDef = encoding[offsetChannel];
  const stepFor = getStepFor({ step, offsetIsDiscrete: isFieldOrDatumDef(offsetDef) && isDiscrete$1(offsetDef.type) });
  if (stepFor === "offset" && channelHasFieldOrDatum(encoding, offsetChannel)) {
    const offsetScaleCmpt = model.getScaleComponent(offsetChannel);
    const offsetScaleName = model.scaleName(offsetChannel);
    let stepCount = `domain('${offsetScaleName}').length`;
    if (offsetScaleCmpt.get("type") === "band") {
      const offsetPaddingInner = offsetScaleCmpt.get("paddingInner") ?? offsetScaleCmpt.get("padding") ?? 0;
      const offsetPaddingOuter = offsetScaleCmpt.get("paddingOuter") ?? offsetScaleCmpt.get("padding") ?? 0;
      stepCount = `bandspace(${stepCount}, ${offsetPaddingInner}, ${offsetPaddingOuter})`;
    }
    const paddingInner2 = mergedScaleCmpt.get("paddingInner") ?? mergedScaleCmpt.get("padding");
    return {
      signal: `${step.step} * ${stepCount} / (1-${exprFromSignalRefOrValue(paddingInner2)})`
    };
  } else {
    return step.step;
  }
}
function getOffsetStep(step, offsetScaleType) {
  const stepFor = getStepFor({ step, offsetIsDiscrete: hasDiscreteDomain(offsetScaleType) });
  if (stepFor === "offset") {
    return { step: step.step };
  }
  return void 0;
}
function getOffsetRange(channel, model, offsetScaleType) {
  const positionChannel = channel === XOFFSET ? "x" : "y";
  const positionScaleCmpt = model.getScaleComponent(positionChannel);
  if (!positionScaleCmpt) {
    return fullWidthOrHeightRange(positionChannel, model, offsetScaleType, { center: true });
  }
  const positionScaleType = positionScaleCmpt.get("type");
  const positionScaleName = model.scaleName(positionChannel);
  const { markDef, config } = model;
  if (positionScaleType === "band") {
    const size = getDiscretePositionSize(positionChannel, model.size, model.config.view);
    if (isStep(size)) {
      const step = getOffsetStep(size, offsetScaleType);
      if (step) {
        return step;
      }
    }
    return [0, { signal: `bandwidth('${positionScaleName}')` }];
  } else {
    const positionDef = model.encoding[positionChannel];
    if (isFieldDef(positionDef) && positionDef.timeUnit) {
      const duration = durationExpr(positionDef.timeUnit, (expr2) => `scale('${positionScaleName}', ${expr2})`);
      const padding3 = model.config.scale.bandWithNestedOffsetPaddingInner;
      const bandPositionOffset = getBandPosition({
        fieldDef: positionDef,
        markDef,
        config
      }) - 0.5;
      const bandPositionOffsetExpr = bandPositionOffset !== 0 ? ` + ${bandPositionOffset}` : "";
      if (padding3) {
        const startRatio = isSignalRef(padding3) ? `${padding3.signal}/2${bandPositionOffsetExpr}` : `${padding3 / 2 + bandPositionOffset}`;
        const endRatio = isSignalRef(padding3) ? `(1 - ${padding3.signal}/2)${bandPositionOffsetExpr}` : `${1 - padding3 / 2 + bandPositionOffset}`;
        return [{ signal: `${startRatio} * (${duration})` }, { signal: `${endRatio} * (${duration})` }];
      }
      return [0, { signal: duration }];
    }
    return never(`Cannot use ${channel} scale if ${positionChannel} scale is not discrete.`);
  }
}
function getDiscretePositionSize(channel, size, viewConfig) {
  const sizeChannel = channel === X3 ? "width" : "height";
  const sizeValue = size[sizeChannel];
  if (sizeValue !== void 0) {
    return sizeValue;
  }
  return getViewConfigDiscreteSize(viewConfig, sizeChannel);
}
function defaultContinuousToDiscreteCount(scaleType2, config, domain4, channel) {
  switch (scaleType2) {
    case "quantile":
      return config.scale.quantileCount;
    case "quantize":
      return config.scale.quantizeCount;
    case "threshold":
      if (domain4 !== void 0 && isArray(domain4)) {
        return domain4.length + 1;
      } else {
        warn3(domainRequiredForThresholdScale(channel));
        return 3;
      }
  }
}
function interpolateRange2(rangeMin2, rangeMax2, cardinality) {
  const f = () => {
    const rMax = signalOrStringValue(rangeMax2);
    const rMin = signalOrStringValue(rangeMin2);
    const step = `(${rMax} - ${rMin}) / (${cardinality} - 1)`;
    return `sequence(${rMin}, ${rMax} + ${step}, ${step})`;
  };
  if (isSignalRef(rangeMax2)) {
    return new SignalRefWrapper(f);
  } else {
    return { signal: f() };
  }
}
function sizeRangeMin(mark, config) {
  switch (mark) {
    case "bar":
    case "tick":
      return config.scale.minBandSize;
    case "line":
    case "trail":
    case "rule":
      return config.scale.minStrokeWidth;
    case "text":
      return config.scale.minFontSize;
    case "point":
    case "square":
    case "circle":
      return config.scale.minSize;
  }
  throw new Error(incompatibleChannel("size", mark));
}
var MAX_SIZE_RANGE_STEP_RATIO = 0.95;
function sizeRangeMax(mark, size, model, config) {
  const xyStepSignals = {
    x: getBinStepSignal(model, "x"),
    y: getBinStepSignal(model, "y")
  };
  switch (mark) {
    case "bar":
    case "tick": {
      if (config.scale.maxBandSize !== void 0) {
        return config.scale.maxBandSize;
      }
      const min4 = minXYStep(size, xyStepSignals, config.view);
      if (isNumber(min4)) {
        return min4 - 1;
      } else {
        return new SignalRefWrapper(() => `${min4.signal} - 1`);
      }
    }
    case "line":
    case "trail":
    case "rule":
      return config.scale.maxStrokeWidth;
    case "text":
      return config.scale.maxFontSize;
    case "point":
    case "square":
    case "circle": {
      if (config.scale.maxSize) {
        return config.scale.maxSize;
      }
      const pointStep = minXYStep(size, xyStepSignals, config.view);
      if (isNumber(pointStep)) {
        return Math.pow(MAX_SIZE_RANGE_STEP_RATIO * pointStep, 2);
      } else {
        return new SignalRefWrapper(() => `pow(${MAX_SIZE_RANGE_STEP_RATIO} * ${pointStep.signal}, 2)`);
      }
    }
  }
  throw new Error(incompatibleChannel("size", mark));
}
function minXYStep(size, xyStepSignals, viewConfig) {
  const widthStep = isStep(size.width) ? size.width.step : getViewConfigDiscreteStep(viewConfig, "width");
  const heightStep = isStep(size.height) ? size.height.step : getViewConfigDiscreteStep(viewConfig, "height");
  if (xyStepSignals.x || xyStepSignals.y) {
    return new SignalRefWrapper(() => {
      const exprs = [
        xyStepSignals.x ? xyStepSignals.x.signal : widthStep,
        xyStepSignals.y ? xyStepSignals.y.signal : heightStep
      ];
      return `min(${exprs.join(", ")})`;
    });
  }
  return Math.min(widthStep, heightStep);
}
function parseScaleProperty(model, property2) {
  if (isUnitModel(model)) {
    parseUnitScaleProperty(model, property2);
  } else {
    parseNonUnitScaleProperty(model, property2);
  }
}
function parseUnitScaleProperty(model, property2) {
  const localScaleComponents = model.component.scales;
  const { config, encoding, markDef, specifiedScales } = model;
  for (const channel of keys3(localScaleComponents)) {
    const specifiedScale = specifiedScales[channel];
    const localScaleCmpt = localScaleComponents[channel];
    const mergedScaleCmpt = model.getScaleComponent(channel);
    const fieldOrDatumDef = getFieldOrDatumDef(encoding[channel]);
    const specifiedValue = specifiedScale[property2];
    const scaleType2 = mergedScaleCmpt.get("type");
    const scalePadding = mergedScaleCmpt.get("padding");
    const scalePaddingInner = mergedScaleCmpt.get("paddingInner");
    const supportedByScaleType = scaleTypeSupportProperty(scaleType2, property2);
    const channelIncompatability = channelScalePropertyIncompatability(channel, property2);
    if (specifiedValue !== void 0) {
      if (!supportedByScaleType) {
        warn3(scalePropertyNotWorkWithScaleType(scaleType2, property2, channel));
      } else if (channelIncompatability) {
        warn3(channelIncompatability);
      }
    }
    if (supportedByScaleType && channelIncompatability === void 0) {
      if (specifiedValue !== void 0) {
        const timeUnit = fieldOrDatumDef.timeUnit;
        const type4 = fieldOrDatumDef.type;
        switch (property2) {
          // domainMax/Min to signal if the value is a datetime object
          case "domainMax":
          case "domainMin":
            if (isDateTime(specifiedScale[property2]) || type4 === "temporal" || timeUnit) {
              localScaleCmpt.set(property2, { signal: valueExpr(specifiedScale[property2], { type: type4, timeUnit }) }, true);
            } else {
              localScaleCmpt.set(property2, specifiedScale[property2], true);
            }
            break;
          default:
            localScaleCmpt.copyKeyFromObject(property2, specifiedScale);
        }
      } else {
        const value4 = hasProperty(scaleRules, property2) ? scaleRules[property2]({
          model,
          channel,
          fieldOrDatumDef,
          scaleType: scaleType2,
          scalePadding,
          scalePaddingInner,
          domain: specifiedScale.domain,
          domainMin: specifiedScale.domainMin,
          domainMax: specifiedScale.domainMax,
          markDef,
          config,
          hasNestedOffsetScale: channelHasNestedOffsetScale(encoding, channel),
          hasSecondaryRangeChannel: !!encoding[getSecondaryRangeChannel(channel)]
        }) : config.scale[property2];
        if (value4 !== void 0) {
          localScaleCmpt.set(property2, value4, false);
        }
      }
    }
  }
}
var scaleRules = {
  bins: ({ model, fieldOrDatumDef }) => isFieldDef(fieldOrDatumDef) ? bins(model, fieldOrDatumDef) : void 0,
  interpolate: ({ channel, fieldOrDatumDef }) => interpolate2(channel, fieldOrDatumDef.type),
  nice: ({ scaleType: scaleType2, channel, domain: domain4, domainMin, domainMax, fieldOrDatumDef }) => nice2(scaleType2, channel, domain4, domainMin, domainMax, fieldOrDatumDef),
  padding: ({ channel, scaleType: scaleType2, fieldOrDatumDef, markDef, config }) => padding2(channel, scaleType2, config.scale, fieldOrDatumDef, markDef, config.bar),
  paddingInner: ({ scalePadding, channel, markDef, scaleType: scaleType2, config, hasNestedOffsetScale }) => paddingInner(scalePadding, channel, markDef.type, scaleType2, config.scale, hasNestedOffsetScale),
  paddingOuter: ({ scalePadding, channel, scaleType: scaleType2, scalePaddingInner, config, hasNestedOffsetScale }) => paddingOuter(scalePadding, channel, scaleType2, scalePaddingInner, config.scale, hasNestedOffsetScale),
  reverse: ({ fieldOrDatumDef, scaleType: scaleType2, channel, config }) => {
    const sort4 = isFieldDef(fieldOrDatumDef) ? fieldOrDatumDef.sort : void 0;
    return reverse3(scaleType2, sort4, channel, config.scale);
  },
  zero: ({ channel, fieldOrDatumDef, domain: domain4, markDef, scaleType: scaleType2, config, hasSecondaryRangeChannel }) => zero5(channel, fieldOrDatumDef, domain4, markDef, scaleType2, config.scale, hasSecondaryRangeChannel)
};
function parseScaleRange2(model) {
  if (isUnitModel(model)) {
    parseUnitScaleRange(model);
  } else {
    parseNonUnitScaleProperty(model, "range");
  }
}
function parseNonUnitScaleProperty(model, property2) {
  const localScaleComponents = model.component.scales;
  for (const child of model.children) {
    if (property2 === "range") {
      parseScaleRange2(child);
    } else {
      parseScaleProperty(child, property2);
    }
  }
  for (const channel of keys3(localScaleComponents)) {
    let valueWithExplicit;
    for (const child of model.children) {
      const childComponent = child.component.scales[channel];
      if (childComponent) {
        const childValueWithExplicit = childComponent.getWithExplicit(property2);
        valueWithExplicit = mergeValuesWithExplicit(valueWithExplicit, childValueWithExplicit, property2, "scale", tieBreakByComparing((v1, v2) => {
          switch (property2) {
            case "range":
              if (v1.step && v2.step) {
                return v1.step - v2.step;
              }
              return 0;
          }
          return 0;
        }));
      }
    }
    localScaleComponents[channel].setWithExplicit(property2, valueWithExplicit);
  }
}
function bins(model, fieldDef) {
  const bin3 = fieldDef.bin;
  if (isBinning(bin3)) {
    const binSignal = getBinSignalName(model, fieldDef.field, bin3);
    return new SignalRefWrapper(() => {
      return model.getSignalName(binSignal);
    });
  } else if (isBinned(bin3) && isBinParams(bin3) && bin3.step !== void 0) {
    return {
      step: bin3.step
    };
  }
  return void 0;
}
function interpolate2(channel, type4) {
  if (contains2([COLOR, FILL, STROKE], channel) && type4 !== "nominal") {
    return "hcl";
  }
  return void 0;
}
function nice2(scaleType2, channel, specifiedDomain, domainMin, domainMax, fieldOrDatumDef) {
  if (getFieldDef(fieldOrDatumDef)?.bin || isArray(specifiedDomain) || domainMax != null || domainMin != null || contains2([ScaleType.TIME, ScaleType.UTC], scaleType2)) {
    return void 0;
  }
  return isXorY(channel) ? true : void 0;
}
function padding2(channel, scaleType2, scaleConfig, fieldOrDatumDef, markDef, barConfig) {
  if (isXorY(channel)) {
    if (isContinuousToContinuous(scaleType2)) {
      if (scaleConfig.continuousPadding !== void 0) {
        return scaleConfig.continuousPadding;
      }
      const { type: type4, orient: orient2 } = markDef;
      if (type4 === "bar" && !(isFieldDef(fieldOrDatumDef) && (fieldOrDatumDef.bin || fieldOrDatumDef.timeUnit))) {
        if (orient2 === "vertical" && channel === "x" || orient2 === "horizontal" && channel === "y") {
          return barConfig.continuousBandSize;
        }
      }
    }
    if (scaleType2 === ScaleType.POINT) {
      return scaleConfig.pointPadding;
    }
  }
  return void 0;
}
function paddingInner(paddingValue, channel, mark, scaleType2, scaleConfig, hasNestedOffsetScale = false) {
  if (paddingValue !== void 0) {
    return void 0;
  }
  if (isXorY(channel)) {
    const { bandPaddingInner, barBandPaddingInner, rectBandPaddingInner, tickBandPaddingInner, bandWithNestedOffsetPaddingInner } = scaleConfig;
    if (hasNestedOffsetScale) {
      return bandWithNestedOffsetPaddingInner;
    }
    return getFirstDefined(bandPaddingInner, mark === "bar" ? barBandPaddingInner : mark === "tick" ? tickBandPaddingInner : rectBandPaddingInner);
  } else if (isXorYOffset(channel)) {
    if (scaleType2 === ScaleType.BAND) {
      return scaleConfig.offsetBandPaddingInner;
    }
  }
  return void 0;
}
function paddingOuter(paddingValue, channel, scaleType2, paddingInnerValue, scaleConfig, hasNestedOffsetScale = false) {
  if (paddingValue !== void 0) {
    return void 0;
  }
  if (isXorY(channel)) {
    const { bandPaddingOuter, bandWithNestedOffsetPaddingOuter } = scaleConfig;
    if (hasNestedOffsetScale) {
      return bandWithNestedOffsetPaddingOuter;
    }
    if (scaleType2 === ScaleType.BAND) {
      return getFirstDefined(
        bandPaddingOuter,
        /* By default, paddingOuter is paddingInner / 2. The reason is that
          size (width/height) = step * (cardinality - paddingInner + 2 * paddingOuter).
          and we want the width/height to be integer by default.
          Note that step (by default) and cardinality are integers.) */
        isSignalRef(paddingInnerValue) ? { signal: `${paddingInnerValue.signal}/2` } : paddingInnerValue / 2
      );
    }
  } else if (isXorYOffset(channel)) {
    if (scaleType2 === ScaleType.POINT) {
      return 0.5;
    } else if (scaleType2 === ScaleType.BAND) {
      return scaleConfig.offsetBandPaddingOuter;
    }
  }
  return void 0;
}
function reverse3(scaleType2, sort4, channel, scaleConfig) {
  if (channel === "x" && scaleConfig.xReverse !== void 0) {
    if (hasContinuousDomain(scaleType2) && sort4 === "descending") {
      if (isSignalRef(scaleConfig.xReverse)) {
        return { signal: `!${scaleConfig.xReverse.signal}` };
      } else {
        return !scaleConfig.xReverse;
      }
    }
    return scaleConfig.xReverse;
  }
  if (hasContinuousDomain(scaleType2) && sort4 === "descending") {
    return true;
  }
  return void 0;
}
function zero5(channel, fieldDef, specifiedDomain, markDef, scaleType2, scaleConfig, hasSecondaryRangeChannel) {
  const hasCustomDomain = !!specifiedDomain && specifiedDomain !== "unaggregated";
  if (hasCustomDomain) {
    if (hasContinuousDomain(scaleType2)) {
      if (isArray(specifiedDomain)) {
        const first = specifiedDomain[0];
        const last = specifiedDomain[specifiedDomain.length - 1];
        if (isNumber(first) && first <= 0 && isNumber(last) && last >= 0) {
          return true;
        }
      }
      return false;
    }
  }
  if (channel === "size" && fieldDef.type === "quantitative" && !isContinuousToDiscrete(scaleType2)) {
    return true;
  }
  if (!(isFieldDef(fieldDef) && fieldDef.bin) && contains2([...POSITION_SCALE_CHANNELS, ...POLAR_POSITION_SCALE_CHANNELS], channel)) {
    const { orient: orient2, type: type4 } = markDef;
    if (contains2(["bar", "area", "line", "trail"], type4)) {
      if (orient2 === "horizontal" && channel === "y" || orient2 === "vertical" && channel === "x") {
        return false;
      }
    }
    if (contains2(["bar", "area"], type4) && !hasSecondaryRangeChannel) {
      return true;
    }
    return scaleConfig?.zero;
  }
  return false;
}
function scaleType(specifiedScale, channel, fieldDef, mark, hasNestedOffsetScale = false) {
  const defaultScaleType = defaultType(channel, fieldDef, mark, hasNestedOffsetScale);
  const { type: type4 } = specifiedScale;
  if (!isScaleChannel(channel)) {
    return null;
  }
  if (type4 !== void 0) {
    if (!channelSupportScaleType(channel, type4)) {
      warn3(scaleTypeNotWorkWithChannel(channel, type4, defaultScaleType));
      return defaultScaleType;
    }
    if (isFieldDef(fieldDef) && !scaleTypeSupportDataType(type4, fieldDef.type)) {
      warn3(scaleTypeNotWorkWithFieldDef(type4, defaultScaleType));
      return defaultScaleType;
    }
    return type4;
  }
  return defaultScaleType;
}
function defaultType(channel, fieldDef, mark, hasNestedOffsetScale) {
  switch (fieldDef.type) {
    case "nominal":
    case "ordinal": {
      if (isColorChannel(channel) || rangeType(channel) === "discrete") {
        if (channel === "shape" && fieldDef.type === "ordinal") {
          warn3(discreteChannelCannotEncode(channel, "ordinal"));
        }
        return "ordinal";
      }
      if (isTime(channel)) {
        return "band";
      }
      if (isXorY(channel) || isXorYOffset(channel)) {
        if (contains2(["rect", "bar", "image", "rule", "tick"], mark.type)) {
          return "band";
        }
        if (hasNestedOffsetScale) {
          return "band";
        }
      } else if (mark.type === "arc" && channel in POLAR_POSITION_SCALE_CHANNEL_INDEX) {
        return "band";
      }
      const dimensionSize = mark[getSizeChannel(channel)];
      if (isRelativeBandSize(dimensionSize)) {
        return "band";
      }
      if (isPositionFieldOrDatumDef(fieldDef) && fieldDef.axis?.tickBand) {
        return "band";
      }
      return "point";
    }
    case "temporal":
      if (isColorChannel(channel)) {
        return "time";
      } else if (rangeType(channel) === "discrete") {
        warn3(discreteChannelCannotEncode(channel, "temporal"));
        return "ordinal";
      } else if (isFieldDef(fieldDef) && fieldDef.timeUnit && normalizeTimeUnit(fieldDef.timeUnit).utc) {
        return "utc";
      } else if (isTime(channel)) {
        return "band";
      }
      return "time";
    case "quantitative":
      if (isColorChannel(channel)) {
        if (isFieldDef(fieldDef) && isBinning(fieldDef.bin)) {
          return "bin-ordinal";
        }
        return "linear";
      } else if (rangeType(channel) === "discrete") {
        warn3(discreteChannelCannotEncode(channel, "quantitative"));
        return "ordinal";
      } else if (isTime(channel)) {
        return "band";
      }
      return "linear";
    case "geojson":
      return void 0;
  }
  throw new Error(invalidFieldType(fieldDef.type));
}
function parseScales(model, { ignoreRange } = {}) {
  parseScaleCore(model);
  parseScaleDomain2(model);
  for (const prop of NON_TYPE_DOMAIN_RANGE_VEGA_SCALE_PROPERTIES) {
    parseScaleProperty(model, prop);
  }
  if (!ignoreRange) {
    parseScaleRange2(model);
  }
}
function parseScaleCore(model) {
  if (isUnitModel(model)) {
    model.component.scales = parseUnitScaleCore(model);
  } else {
    model.component.scales = parseNonUnitScaleCore(model);
  }
}
function parseUnitScaleCore(model) {
  const { encoding, mark, markDef } = model;
  const scaleComponents = {};
  for (const channel of SCALE_CHANNELS) {
    const fieldOrDatumDef = getFieldOrDatumDef(encoding[channel]);
    if (fieldOrDatumDef && mark === GEOSHAPE && channel === SHAPE && fieldOrDatumDef.type === GEOJSON) {
      continue;
    }
    let specifiedScale = fieldOrDatumDef && fieldOrDatumDef.scale;
    if (fieldOrDatumDef && specifiedScale !== null && specifiedScale !== false) {
      specifiedScale ??= {};
      const hasNestedOffsetScale = channelHasNestedOffsetScale(encoding, channel);
      const sType = scaleType(specifiedScale, channel, fieldOrDatumDef, markDef, hasNestedOffsetScale);
      scaleComponents[channel] = new ScaleComponent(model.scaleName(`${channel}`, true), {
        value: sType,
        explicit: specifiedScale.type === sType
      });
    }
  }
  return scaleComponents;
}
var scaleTypeTieBreaker = tieBreakByComparing((st1, st2) => scaleTypePrecedence(st1) - scaleTypePrecedence(st2));
function parseNonUnitScaleCore(model) {
  const scaleComponents = model.component.scales = {};
  const scaleTypeWithExplicitIndex = {};
  const resolve2 = model.component.resolve;
  for (const child of model.children) {
    parseScaleCore(child);
    for (const channel of keys3(child.component.scales)) {
      resolve2.scale[channel] ??= defaultScaleResolve(channel, model);
      if (resolve2.scale[channel] === "shared") {
        const explicitScaleType = scaleTypeWithExplicitIndex[channel];
        const childScaleType = child.component.scales[channel].getWithExplicit("type");
        if (explicitScaleType) {
          if (scaleCompatible(explicitScaleType.value, childScaleType.value)) {
            scaleTypeWithExplicitIndex[channel] = mergeValuesWithExplicit(explicitScaleType, childScaleType, "type", "scale", scaleTypeTieBreaker);
          } else {
            resolve2.scale[channel] = "independent";
            delete scaleTypeWithExplicitIndex[channel];
          }
        } else {
          scaleTypeWithExplicitIndex[channel] = childScaleType;
        }
      }
    }
  }
  for (const channel of keys3(scaleTypeWithExplicitIndex)) {
    const name = model.scaleName(channel, true);
    const typeWithExplicit = scaleTypeWithExplicitIndex[channel];
    scaleComponents[channel] = new ScaleComponent(name, typeWithExplicit);
    for (const child of model.children) {
      const childScale = child.component.scales[channel];
      if (childScale) {
        child.renameScale(childScale.get("name"), name);
        childScale.merged = true;
      }
    }
  }
  return scaleComponents;
}
var NameMap = class {
  nameMap;
  constructor() {
    this.nameMap = {};
  }
  rename(oldName, newName) {
    this.nameMap[oldName] = newName;
  }
  has(name) {
    return this.nameMap[name] !== void 0;
  }
  get(name) {
    while (this.nameMap[name] && name !== this.nameMap[name]) {
      name = this.nameMap[name];
    }
    return name;
  }
};
function isUnitModel(model) {
  return model?.type === "unit";
}
function isFacetModel(model) {
  return model?.type === "facet";
}
function isConcatModel(model) {
  return model?.type === "concat";
}
function isLayerModel(model) {
  return model?.type === "layer";
}
var Model = class {
  type;
  parent;
  config;
  name;
  size;
  title;
  description;
  data;
  transforms;
  layout;
  /** Name map for scales, which can be renamed by a model's parent. */
  scaleNameMap;
  /** Name map for projections, which can be renamed by a model's parent. */
  projectionNameMap;
  /** Name map for signals, which can be renamed by a model's parent. */
  signalNameMap;
  component;
  view;
  constructor(spec, type4, parent, parentGivenName, config, resolve2, view) {
    this.type = type4;
    this.parent = parent;
    this.config = config;
    this.parent = parent;
    this.config = config;
    this.view = replaceExprRef(view);
    this.name = spec.name ?? parentGivenName;
    this.title = isText(spec.title) ? { text: spec.title } : spec.title ? replaceExprRef(spec.title) : void 0;
    this.scaleNameMap = parent ? parent.scaleNameMap : new NameMap();
    this.projectionNameMap = parent ? parent.projectionNameMap : new NameMap();
    this.signalNameMap = parent ? parent.signalNameMap : new NameMap();
    this.data = spec.data;
    this.description = spec.description;
    this.transforms = normalizeTransform(spec.transform ?? []);
    this.layout = type4 === "layer" || type4 === "unit" ? {} : extractCompositionLayout(spec, type4, config);
    this.component = {
      data: {
        sources: parent ? parent.component.data.sources : [],
        outputNodes: parent ? parent.component.data.outputNodes : {},
        outputNodeRefCounts: parent ? parent.component.data.outputNodeRefCounts : {},
        // data is faceted if the spec is a facet spec or the parent has faceted data and data is undefined
        isFaceted: isFacetSpec(spec) || parent?.component.data.isFaceted && spec.data === void 0
      },
      layoutSize: new Split(),
      layoutHeaders: { row: {}, column: {}, facet: {} },
      mark: null,
      resolve: {
        scale: {},
        axis: {},
        legend: {},
        ...resolve2 ? duplicate(resolve2) : {}
      },
      selection: null,
      scales: null,
      projection: null,
      axes: {},
      legends: {}
    };
  }
  get width() {
    return this.getSizeSignalRef("width");
  }
  get height() {
    return this.getSizeSignalRef("height");
  }
  parse() {
    this.parseScale();
    this.parseLayoutSize();
    this.renameTopLevelLayoutSizeSignal();
    this.parseSelections();
    this.parseProjection();
    this.parseData();
    this.parseAxesAndHeaders();
    this.parseLegends();
    this.parseMarkGroup();
  }
  parseScale() {
    parseScales(this);
  }
  parseProjection() {
    parseProjection2(this);
  }
  /**
   * Rename top-level spec's size to be just width / height, ignoring model name.
   * This essentially merges the top-level spec's width/height signals with the width/height signals
   * to help us reduce redundant signals declaration.
   */
  renameTopLevelLayoutSizeSignal() {
    if (this.getName("width") !== "width") {
      this.renameSignal(this.getName("width"), "width");
    }
    if (this.getName("height") !== "height") {
      this.renameSignal(this.getName("height"), "height");
    }
  }
  parseLegends() {
    parseLegend2(this);
  }
  assembleEncodeFromView(view) {
    const { style: _, ...baseView } = view;
    const e = {};
    for (const property2 of keys3(baseView)) {
      const value4 = baseView[property2];
      if (value4 !== void 0) {
        e[property2] = signalOrValueRef(value4);
      }
    }
    return e;
  }
  assembleGroupEncodeEntry(isTopLevel) {
    let encodeEntry2 = {};
    if (this.view) {
      encodeEntry2 = this.assembleEncodeFromView(this.view);
    }
    if (!isTopLevel) {
      if (this.description) {
        encodeEntry2["description"] = signalOrValueRef(this.description);
      }
      if (this.type === "unit" || this.type === "layer") {
        return {
          width: this.getSizeSignalRef("width"),
          height: this.getSizeSignalRef("height"),
          ...encodeEntry2
        };
      }
    }
    return isEmpty(encodeEntry2) ? void 0 : encodeEntry2;
  }
  assembleLayout() {
    if (!this.layout) {
      return void 0;
    }
    const { spacing, ...layout } = this.layout;
    const { component, config } = this;
    const titleBand = assembleLayoutTitleBand(component.layoutHeaders, config);
    return {
      padding: spacing,
      ...this.assembleDefaultLayout(),
      ...layout,
      ...titleBand ? { titleBand } : {}
    };
  }
  assembleDefaultLayout() {
    return {};
  }
  assembleHeaderMarks() {
    const { layoutHeaders: layoutHeaders2 } = this.component;
    let headerMarks = [];
    for (const channel of FACET_CHANNELS) {
      if (layoutHeaders2[channel].title) {
        headerMarks.push(assembleTitleGroup(this, channel));
      }
    }
    for (const channel of HEADER_CHANNELS) {
      headerMarks = headerMarks.concat(assembleHeaderGroups(this, channel));
    }
    return headerMarks;
  }
  assembleAxes() {
    return assembleAxes(this.component.axes, this.config);
  }
  assembleLegends() {
    return assembleLegends(this);
  }
  assembleProjections() {
    return assembleProjections(this);
  }
  assembleTitle() {
    const { encoding, ...titleNoEncoding } = this.title ?? {};
    const title2 = {
      ...extractTitleConfig(this.config.title).nonMarkTitleProperties,
      ...titleNoEncoding,
      ...encoding ? { encode: { update: encoding } } : {}
    };
    if (title2.text) {
      if (contains2(["unit", "layer"], this.type)) {
        if (contains2(["middle", void 0], title2.anchor)) {
          title2.frame ??= "group";
        }
      } else {
        title2.anchor ??= "start";
      }
      return isEmpty(title2) ? void 0 : title2;
    }
    return void 0;
  }
  /**
   * Assemble the mark group for this model. We accept optional `signals` so that we can include concat top-level signals with the top-level model's local signals.
   */
  assembleGroup(signals = []) {
    const group3 = {};
    signals = signals.concat(this.assembleSignals());
    if (signals.length > 0) {
      group3.signals = signals;
    }
    const layout = this.assembleLayout();
    if (layout) {
      group3.layout = layout;
    }
    group3.marks = [].concat(this.assembleHeaderMarks(), this.assembleMarks());
    const scales2 = !this.parent || isFacetModel(this.parent) ? assembleScales(this) : [];
    if (scales2.length > 0) {
      group3.scales = scales2;
    }
    const axes = this.assembleAxes();
    if (axes.length > 0) {
      group3.axes = axes;
    }
    const legends = this.assembleLegends();
    if (legends.length > 0) {
      group3.legends = legends;
    }
    return group3;
  }
  getName(text4) {
    return varName((this.name ? `${this.name}_` : "") + text4);
  }
  getDataName(type4) {
    return this.getName(DataSourceType[type4].toLowerCase());
  }
  /**
   * Request a data source name for the given data source type and mark that data source as required.
   * This method should be called in parse, so that all used data source can be correctly instantiated in assembleData().
   * You can lookup the correct dataset name in assemble with `lookupDataSource`.
   */
  requestDataName(name) {
    const fullName = this.getDataName(name);
    const refCounts = this.component.data.outputNodeRefCounts;
    refCounts[fullName] = (refCounts[fullName] || 0) + 1;
    return fullName;
  }
  getSizeSignalRef(layoutSizeType) {
    if (isFacetModel(this.parent)) {
      const sizeType = getSizeTypeFromLayoutSizeType(layoutSizeType);
      const channel = getPositionScaleChannel(sizeType);
      const scaleComponent = this.component.scales[channel];
      if (scaleComponent && !scaleComponent.merged) {
        const type4 = scaleComponent.get("type");
        const range7 = scaleComponent.get("range");
        if (hasDiscreteDomain(type4) && isVgRangeStep(range7)) {
          const scaleName = scaleComponent.get("name");
          const domain4 = assembleDomain(this, channel);
          const field3 = getFieldFromDomain(domain4);
          if (field3) {
            const fieldRef2 = vgField({ aggregate: "distinct", field: field3 }, { expr: "datum" });
            return {
              signal: sizeExpr(scaleName, scaleComponent, fieldRef2)
            };
          } else {
            warn3(unknownField(channel));
            return null;
          }
        }
      }
    }
    return {
      signal: this.signalNameMap.get(this.getName(layoutSizeType))
    };
  }
  /**
   * Lookup the name of the datasource for an output node. You probably want to call this in assemble.
   */
  lookupDataSource(name) {
    const node = this.component.data.outputNodes[name];
    if (!node) {
      return name;
    }
    return node.getSource();
  }
  getSignalName(oldSignalName) {
    return this.signalNameMap.get(oldSignalName);
  }
  renameSignal(oldName, newName) {
    this.signalNameMap.rename(oldName, newName);
  }
  renameScale(oldName, newName) {
    this.scaleNameMap.rename(oldName, newName);
  }
  renameProjection(oldName, newName) {
    this.projectionNameMap.rename(oldName, newName);
  }
  /**
   * @return scale name for a given channel after the scale has been parsed and named.
   */
  scaleName(originalScaleName, parse8) {
    if (parse8) {
      return this.getName(originalScaleName);
    }
    if (
      // If there is a scale for the channel, there should be a local scale component for it
      isChannel(originalScaleName) && isScaleChannel(originalScaleName) && this.component.scales[originalScaleName] || // in the scale name map (the scale get merged by its parent)
      this.scaleNameMap.has(this.getName(originalScaleName))
    ) {
      return this.scaleNameMap.get(this.getName(originalScaleName));
    }
    return void 0;
  }
  /**
   * @return projection name after the projection has been parsed and named.
   */
  projectionName(parse8) {
    if (parse8) {
      return this.getName("projection");
    }
    if (this.component.projection && !this.component.projection.merged || this.projectionNameMap.has(this.getName("projection"))) {
      return this.projectionNameMap.get(this.getName("projection"));
    }
    return void 0;
  }
  /**
   * Traverse a model's hierarchy to get the scale component for a particular channel.
   */
  getScaleComponent(channel) {
    if (!this.component.scales) {
      throw new Error("getScaleComponent cannot be called before parseScale(). Make sure you have called parseScale or use parseUnitModelWithScale().");
    }
    const localScaleComponent = this.component.scales[channel];
    if (localScaleComponent && !localScaleComponent.merged) {
      return localScaleComponent;
    }
    return this.parent ? this.parent.getScaleComponent(channel) : void 0;
  }
  getScaleType(channel) {
    const scaleComponent = this.getScaleComponent(channel);
    return scaleComponent ? scaleComponent.get("type") : void 0;
  }
  /**
   * Traverse a model's hierarchy to get a particular selection component.
   */
  getSelectionComponent(variableName, origName) {
    let sel = this.component.selection[variableName];
    if (!sel && this.parent) {
      sel = this.parent.getSelectionComponent(variableName, origName);
    }
    if (!sel) {
      throw new Error(selectionNotFound(origName));
    }
    return sel;
  }
  /**
   * Returns true if the model has a signalRef for an axis orient.
   */
  hasAxisOrientSignalRef() {
    return this.component.axes.x?.some((a3) => a3.hasOrientSignalRef()) || this.component.axes.y?.some((a3) => a3.hasOrientSignalRef());
  }
};
var ModelWithField = class extends Model {
  /** Get "field" reference for Vega */
  vgField(channel, opt = {}) {
    const fieldDef = this.fieldDef(channel);
    if (!fieldDef) {
      return void 0;
    }
    return vgField(fieldDef, opt);
  }
  reduceFieldDef(f, init2) {
    return reduce(this.getMapping(), (acc, cd2, c3) => {
      const fieldDef = getFieldDef(cd2);
      if (fieldDef) {
        return f(acc, fieldDef, c3);
      }
      return acc;
    }, init2);
  }
  forEachFieldDef(f, t) {
    forEach(this.getMapping(), (cd2, c3) => {
      const fieldDef = getFieldDef(cd2);
      if (fieldDef) {
        f(fieldDef, c3);
      }
    }, t);
  }
};
var DensityTransformNode = class _DensityTransformNode extends DataFlowNode {
  transform;
  clone() {
    return new _DensityTransformNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
    this.transform = duplicate(transform4);
    const specifiedAs = this.transform.as ?? [void 0, void 0];
    this.transform.as = [specifiedAs[0] ?? "value", specifiedAs[1] ?? "density"];
    const resolve2 = this.transform.resolve ?? "shared";
    this.transform.resolve = resolve2;
  }
  dependentFields() {
    return /* @__PURE__ */ new Set([this.transform.density, ...this.transform.groupby ?? []]);
  }
  producedFields() {
    return new Set(this.transform.as);
  }
  hash() {
    return `DensityTransform ${hash(this.transform)}`;
  }
  assemble() {
    const { density, ...rest } = this.transform;
    const result = {
      type: "kde",
      field: density,
      ...rest
    };
    result.resolve = this.transform.resolve;
    return result;
  }
};
var ExtentTransformNode = class _ExtentTransformNode extends DataFlowNode {
  transform;
  clone() {
    return new _ExtentTransformNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
    this.transform = duplicate(transform4);
  }
  dependentFields() {
    return /* @__PURE__ */ new Set([this.transform.extent]);
  }
  producedFields() {
    return /* @__PURE__ */ new Set([]);
  }
  hash() {
    return `ExtentTransform ${hash(this.transform)}`;
  }
  assemble() {
    const { extent: extent2, param: param2 } = this.transform;
    const result = {
      type: "extent",
      field: extent2,
      signal: param2
    };
    return result;
  }
};
var FlattenTransformNode = class _FlattenTransformNode extends DataFlowNode {
  transform;
  clone() {
    return new _FlattenTransformNode(this.parent, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
    this.transform = duplicate(transform4);
    const { flatten: flatten2, as = [] } = this.transform;
    this.transform.as = flatten2.map((f, i) => as[i] ?? f);
  }
  dependentFields() {
    return new Set(this.transform.flatten);
  }
  producedFields() {
    return new Set(this.transform.as);
  }
  hash() {
    return `FlattenTransform ${hash(this.transform)}`;
  }
  assemble() {
    const { flatten: fields, as } = this.transform;
    const result = {
      type: "flatten",
      fields,
      as
    };
    return result;
  }
};
var FoldTransformNode = class _FoldTransformNode extends DataFlowNode {
  transform;
  clone() {
    return new _FoldTransformNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
    this.transform = duplicate(transform4);
    const specifiedAs = this.transform.as ?? [void 0, void 0];
    this.transform.as = [specifiedAs[0] ?? "key", specifiedAs[1] ?? "value"];
  }
  dependentFields() {
    return new Set(this.transform.fold);
  }
  producedFields() {
    return new Set(this.transform.as);
  }
  hash() {
    return `FoldTransform ${hash(this.transform)}`;
  }
  assemble() {
    const { fold, as } = this.transform;
    const result = {
      type: "fold",
      fields: fold,
      as
    };
    return result;
  }
};
var GeoJSONNode = class _GeoJSONNode extends DataFlowNode {
  fields;
  geojson;
  signal;
  clone() {
    return new _GeoJSONNode(null, duplicate(this.fields), this.geojson, this.signal);
  }
  static parseAll(parent, model) {
    if (model.component.projection && !model.component.projection.isFit) {
      return parent;
    }
    let geoJsonCounter = 0;
    for (const coordinates of [
      [LONGITUDE, LATITUDE],
      [LONGITUDE2, LATITUDE2]
    ]) {
      const pair = coordinates.map((channel) => {
        const def3 = getFieldOrDatumDef(model.encoding[channel]);
        return isFieldDef(def3) ? def3.field : isDatumDef(def3) ? { expr: `${def3.datum}` } : isValueDef(def3) ? { expr: `${def3["value"]}` } : void 0;
      });
      if (pair[0] || pair[1]) {
        parent = new _GeoJSONNode(parent, pair, null, model.getName(`geojson_${geoJsonCounter++}`));
      }
    }
    if (model.channelHasField(SHAPE)) {
      const fieldDef = model.typedFieldDef(SHAPE);
      if (fieldDef.type === GEOJSON) {
        parent = new _GeoJSONNode(parent, null, fieldDef.field, model.getName(`geojson_${geoJsonCounter}`));
      }
    }
    return parent;
  }
  constructor(parent, fields, geojson, signal) {
    super(parent);
    this.fields = fields;
    this.geojson = geojson;
    this.signal = signal;
  }
  dependentFields() {
    const fields = (this.fields ?? []).filter(isString);
    return /* @__PURE__ */ new Set([...this.geojson ? [this.geojson] : [], ...fields]);
  }
  producedFields() {
    return /* @__PURE__ */ new Set();
  }
  hash() {
    return `GeoJSON ${this.geojson} ${this.signal} ${hash(this.fields)}`;
  }
  assemble() {
    return [
      ...this.geojson ? [
        {
          type: "filter",
          expr: `isValid(datum["${this.geojson}"])`
        }
      ] : [],
      {
        type: "geojson",
        ...this.fields ? { fields: this.fields } : {},
        ...this.geojson ? { geojson: this.geojson } : {},
        signal: this.signal
      }
    ];
  }
};
var GeoPointNode = class _GeoPointNode extends DataFlowNode {
  projection;
  fields;
  as;
  clone() {
    return new _GeoPointNode(null, this.projection, duplicate(this.fields), duplicate(this.as));
  }
  constructor(parent, projection3, fields, as) {
    super(parent);
    this.projection = projection3;
    this.fields = fields;
    this.as = as;
  }
  static parseAll(parent, model) {
    if (!model.projectionName()) {
      return parent;
    }
    for (const coordinates of [
      [LONGITUDE, LATITUDE],
      [LONGITUDE2, LATITUDE2]
    ]) {
      const pair = coordinates.map((channel) => {
        const def3 = getFieldOrDatumDef(model.encoding[channel]);
        return isFieldDef(def3) ? def3.field : isDatumDef(def3) ? { expr: `${def3.datum}` } : isValueDef(def3) ? { expr: `${def3["value"]}` } : void 0;
      });
      const suffix = coordinates[0] === LONGITUDE2 ? "2" : "";
      if (pair[0] || pair[1]) {
        parent = new _GeoPointNode(parent, model.projectionName(), pair, [
          model.getName(`x${suffix}`),
          model.getName(`y${suffix}`)
        ]);
      }
    }
    return parent;
  }
  dependentFields() {
    return new Set(this.fields.filter(isString));
  }
  producedFields() {
    return new Set(this.as);
  }
  hash() {
    return `Geopoint ${this.projection} ${hash(this.fields)} ${hash(this.as)}`;
  }
  assemble() {
    return {
      type: "geopoint",
      projection: this.projection,
      fields: this.fields,
      as: this.as
    };
  }
};
var ImputeNode = class _ImputeNode extends DataFlowNode {
  transform;
  clone() {
    return new _ImputeNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
  }
  dependentFields() {
    return /* @__PURE__ */ new Set([this.transform.impute, this.transform.key, ...this.transform.groupby ?? []]);
  }
  producedFields() {
    return /* @__PURE__ */ new Set([this.transform.impute]);
  }
  processSequence(keyvals) {
    const { start = 0, stop: stop2, step } = keyvals;
    const result = [start, stop2, ...step ? [step] : []].join(",");
    return { signal: `sequence(${result})` };
  }
  static makeFromTransform(parent, imputeTransform) {
    return new _ImputeNode(parent, imputeTransform);
  }
  static makeFromEncoding(parent, model) {
    const encoding = model.encoding;
    const xDef = encoding.x;
    const yDef = encoding.y;
    if (isFieldDef(xDef) && isFieldDef(yDef)) {
      const imputedChannel = xDef.impute ? xDef : yDef.impute ? yDef : void 0;
      if (imputedChannel === void 0) {
        return void 0;
      }
      const keyChannel = xDef.impute ? yDef : yDef.impute ? xDef : void 0;
      const { method: method2, value: value4, frame: frame2, keyvals } = imputedChannel.impute;
      const groupbyFields = pathGroupingFields(model.mark, encoding);
      return new _ImputeNode(parent, {
        impute: imputedChannel.field,
        key: keyChannel.field,
        ...method2 ? { method: method2 } : {},
        ...value4 !== void 0 ? { value: value4 } : {},
        ...frame2 ? { frame: frame2 } : {},
        ...keyvals !== void 0 ? { keyvals } : {},
        ...groupbyFields.length ? { groupby: groupbyFields } : {}
      });
    }
    return null;
  }
  hash() {
    return `Impute ${hash(this.transform)}`;
  }
  assemble() {
    const { impute, key: key2, keyvals, method: method2, groupby, value: value4, frame: frame2 = [null, null] } = this.transform;
    const imputeTransform = {
      type: "impute",
      field: impute,
      key: key2,
      ...keyvals ? { keyvals: isImputeSequence(keyvals) ? this.processSequence(keyvals) : keyvals } : {},
      method: "value",
      ...groupby ? { groupby } : {},
      value: !method2 || method2 === "value" ? value4 : null
    };
    if (method2 && method2 !== "value") {
      const deriveNewField = {
        type: "window",
        as: [`imputed_${impute}_value`],
        ops: [method2],
        fields: [impute],
        frame: frame2,
        ignorePeers: false,
        ...groupby ? { groupby } : {}
      };
      const replaceOriginal = {
        type: "formula",
        expr: `datum.${impute} === null ? datum.imputed_${impute}_value : datum.${impute}`,
        as: impute
      };
      return [imputeTransform, deriveNewField, replaceOriginal];
    } else {
      return [imputeTransform];
    }
  }
};
var LoessTransformNode = class _LoessTransformNode extends DataFlowNode {
  transform;
  clone() {
    return new _LoessTransformNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
    this.transform = duplicate(transform4);
    const specifiedAs = this.transform.as ?? [void 0, void 0];
    this.transform.as = [specifiedAs[0] ?? transform4.on, specifiedAs[1] ?? transform4.loess];
  }
  dependentFields() {
    return /* @__PURE__ */ new Set([this.transform.loess, this.transform.on, ...this.transform.groupby ?? []]);
  }
  producedFields() {
    return new Set(this.transform.as);
  }
  hash() {
    return `LoessTransform ${hash(this.transform)}`;
  }
  assemble() {
    const { loess: loess2, on: on2, ...rest } = this.transform;
    const result = {
      type: "loess",
      x: on2,
      y: loess2,
      ...rest
    };
    return result;
  }
};
var LookupNode = class _LookupNode extends DataFlowNode {
  transform;
  secondary;
  clone() {
    return new _LookupNode(null, duplicate(this.transform), this.secondary);
  }
  constructor(parent, transform4, secondary) {
    super(parent);
    this.transform = transform4;
    this.secondary = secondary;
  }
  static make(parent, model, transform4, counter) {
    const sources = model.component.data.sources;
    const { from } = transform4;
    let fromOutputNode = null;
    if (isLookupData(from)) {
      let fromSource = findSource(from.data, sources);
      if (!fromSource) {
        fromSource = new SourceNode(from.data);
        sources.push(fromSource);
      }
      const fromOutputName = model.getName(`lookup_${counter}`);
      fromOutputNode = new OutputNode(fromSource, fromOutputName, DataSourceType.Lookup, model.component.data.outputNodeRefCounts);
      model.component.data.outputNodes[fromOutputName] = fromOutputNode;
    } else if (isLookupSelection(from)) {
      const selName = from.param;
      transform4 = { as: selName, ...transform4 };
      let selCmpt;
      try {
        selCmpt = model.getSelectionComponent(varName(selName), selName);
      } catch {
        throw new Error(cannotLookupVariableParameter(selName));
      }
      fromOutputNode = selCmpt.materialized;
      if (!fromOutputNode) {
        throw new Error(noSameUnitLookup(selName));
      }
    }
    return new _LookupNode(parent, transform4, fromOutputNode.getSource());
  }
  dependentFields() {
    return /* @__PURE__ */ new Set([this.transform.lookup]);
  }
  producedFields() {
    return new Set(this.transform.as ? array_default(this.transform.as) : this.transform.from.fields);
  }
  hash() {
    return `Lookup ${hash({ transform: this.transform, secondary: this.secondary })}`;
  }
  assemble() {
    let foreign;
    if (this.transform.from.fields) {
      foreign = {
        values: this.transform.from.fields,
        ...this.transform.as ? { as: array_default(this.transform.as) } : {}
      };
    } else {
      let asName = this.transform.as;
      if (!isString(asName)) {
        warn3(NO_FIELDS_NEEDS_AS);
        asName = "_lookup";
      }
      foreign = {
        as: [asName]
      };
    }
    return {
      type: "lookup",
      from: this.secondary,
      key: this.transform.from.key,
      fields: [this.transform.lookup],
      ...foreign,
      ...this.transform.default ? { default: this.transform.default } : {}
    };
  }
};
var QuantileTransformNode = class _QuantileTransformNode extends DataFlowNode {
  transform;
  clone() {
    return new _QuantileTransformNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
    this.transform = duplicate(transform4);
    const specifiedAs = this.transform.as ?? [void 0, void 0];
    this.transform.as = [specifiedAs[0] ?? "prob", specifiedAs[1] ?? "value"];
  }
  dependentFields() {
    return /* @__PURE__ */ new Set([this.transform.quantile, ...this.transform.groupby ?? []]);
  }
  producedFields() {
    return new Set(this.transform.as);
  }
  hash() {
    return `QuantileTransform ${hash(this.transform)}`;
  }
  assemble() {
    const { quantile: quantile3, ...rest } = this.transform;
    const result = {
      type: "quantile",
      field: quantile3,
      ...rest
    };
    return result;
  }
};
var RegressionTransformNode = class _RegressionTransformNode extends DataFlowNode {
  transform;
  clone() {
    return new _RegressionTransformNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
    this.transform = duplicate(transform4);
    const specifiedAs = this.transform.as ?? [void 0, void 0];
    this.transform.as = [specifiedAs[0] ?? transform4.on, specifiedAs[1] ?? transform4.regression];
  }
  dependentFields() {
    return /* @__PURE__ */ new Set([this.transform.regression, this.transform.on, ...this.transform.groupby ?? []]);
  }
  producedFields() {
    return new Set(this.transform.as);
  }
  hash() {
    return `RegressionTransform ${hash(this.transform)}`;
  }
  assemble() {
    const { regression, on: on2, ...rest } = this.transform;
    const result = {
      type: "regression",
      x: on2,
      y: regression,
      ...rest
    };
    return result;
  }
};
var PivotTransformNode = class _PivotTransformNode extends DataFlowNode {
  transform;
  clone() {
    return new _PivotTransformNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
  }
  addDimensions(fields) {
    this.transform.groupby = unique((this.transform.groupby ?? []).concat(fields), (d) => d);
  }
  producedFields() {
    return void 0;
  }
  dependentFields() {
    return /* @__PURE__ */ new Set([this.transform.pivot, this.transform.value, ...this.transform.groupby ?? []]);
  }
  hash() {
    return `PivotTransform ${hash(this.transform)}`;
  }
  assemble() {
    const { pivot, value: value4, groupby, limit, op } = this.transform;
    return {
      type: "pivot",
      field: pivot,
      value: value4,
      ...limit !== void 0 ? { limit } : {},
      ...op !== void 0 ? { op } : {},
      ...groupby !== void 0 ? { groupby } : {}
    };
  }
};
var SampleTransformNode = class _SampleTransformNode extends DataFlowNode {
  transform;
  clone() {
    return new _SampleTransformNode(null, duplicate(this.transform));
  }
  constructor(parent, transform4) {
    super(parent);
    this.transform = transform4;
  }
  dependentFields() {
    return /* @__PURE__ */ new Set();
  }
  producedFields() {
    return /* @__PURE__ */ new Set();
  }
  hash() {
    return `SampleTransform ${hash(this.transform)}`;
  }
  assemble() {
    return {
      type: "sample",
      size: this.transform.sample
    };
  }
};
function makeWalkTree(data4) {
  let datasetIndex = 0;
  function walkTree(node, dataSource) {
    if (node instanceof SourceNode) {
      if (!node.isGenerator && !isUrlData(node.data)) {
        data4.push(dataSource);
        const newData = {
          name: null,
          source: dataSource.name,
          transform: []
        };
        dataSource = newData;
      }
    }
    if (node instanceof ParseNode) {
      if (node.parent instanceof SourceNode && !dataSource.source) {
        dataSource.format = {
          ...dataSource.format,
          parse: node.assembleFormatParse()
        };
        dataSource.transform.push(...node.assembleTransforms(true));
      } else {
        dataSource.transform.push(...node.assembleTransforms());
      }
    }
    if (node instanceof FacetNode) {
      if (!dataSource.name) {
        dataSource.name = `data_${datasetIndex++}`;
      }
      if (!dataSource.source || dataSource.transform.length > 0) {
        data4.push(dataSource);
        node.data = dataSource.name;
      } else {
        node.data = dataSource.source;
      }
      data4.push(...node.assemble());
      return;
    }
    if (node instanceof GraticuleNode || node instanceof SequenceNode || node instanceof FilterInvalidNode || node instanceof FilterNode || node instanceof CalculateNode || node instanceof GeoPointNode || node instanceof AggregateNode || node instanceof LookupNode || node instanceof WindowTransformNode || node instanceof JoinAggregateTransformNode || node instanceof FoldTransformNode || node instanceof FlattenTransformNode || node instanceof DensityTransformNode || node instanceof LoessTransformNode || node instanceof QuantileTransformNode || node instanceof RegressionTransformNode || node instanceof IdentifierNode || node instanceof SampleTransformNode || node instanceof PivotTransformNode || node instanceof ExtentTransformNode) {
      dataSource.transform.push(node.assemble());
    }
    if (node instanceof BinNode || node instanceof TimeUnitNode || node instanceof ImputeNode || node instanceof StackNode || node instanceof GeoJSONNode) {
      dataSource.transform.push(...node.assemble());
    }
    if (node instanceof OutputNode) {
      if (dataSource.source && dataSource.transform.length === 0) {
        node.setSource(dataSource.source);
      } else if (node.parent instanceof OutputNode) {
        node.setSource(dataSource.name);
      } else {
        if (!dataSource.name) {
          dataSource.name = `data_${datasetIndex++}`;
        }
        node.setSource(dataSource.name);
        if (node.numChildren() === 1) {
          data4.push(dataSource);
          const newData = {
            name: null,
            source: dataSource.name,
            transform: []
          };
          dataSource = newData;
        }
      }
    }
    switch (node.numChildren()) {
      case 0:
        if (node instanceof OutputNode && (!dataSource.source || dataSource.transform.length > 0)) {
          data4.push(dataSource);
        }
        break;
      case 1:
        walkTree(node.children[0], dataSource);
        break;
      default: {
        if (!dataSource.name) {
          dataSource.name = `data_${datasetIndex++}`;
        }
        let source4 = dataSource.name;
        if (!dataSource.source || dataSource.transform.length > 0) {
          data4.push(dataSource);
        } else {
          source4 = dataSource.source;
        }
        for (const child of node.children) {
          const newData = {
            name: null,
            source: source4,
            transform: []
          };
          walkTree(child, newData);
        }
        break;
      }
    }
  }
  return walkTree;
}
function assembleFacetData(root) {
  const data4 = [];
  const walkTree = makeWalkTree(data4);
  for (const child of root.children) {
    walkTree(child, {
      source: root.name,
      name: null,
      transform: []
    });
  }
  return data4;
}
function assembleRootData(dataComponent, datasets) {
  const data4 = [];
  const walkTree = makeWalkTree(data4);
  let sourceIndex = 0;
  for (const root of dataComponent.sources) {
    if (!root.hasName()) {
      root.dataName = `source_${sourceIndex++}`;
    }
    const newData = root.assemble();
    walkTree(root, newData);
  }
  for (const d of data4) {
    if (d.transform.length === 0) {
      delete d.transform;
    }
  }
  let whereTo = 0;
  for (const [i, d] of data4.entries()) {
    if ((d.transform ?? []).length === 0 && !d.source) {
      data4.splice(whereTo++, 0, data4.splice(i, 1)[0]);
    }
  }
  for (const d of data4) {
    for (const t of d.transform ?? []) {
      if (t.type === "lookup") {
        t.from = dataComponent.outputNodes[t.from].getSource();
      }
    }
  }
  for (const d of data4) {
    if (d.name in datasets) {
      d.values = datasets[d.name];
    }
  }
  return data4;
}
function getHeaderType(orient2) {
  if (orient2 === "top" || orient2 === "left" || isSignalRef(orient2)) {
    return "header";
  }
  return "footer";
}
function parseFacetHeaders(model) {
  for (const channel of FACET_CHANNELS) {
    parseFacetHeader(model, channel);
  }
  mergeChildAxis(model, "x");
  mergeChildAxis(model, "y");
}
function parseFacetHeader(model, channel) {
  const { facet, config, child, component } = model;
  if (model.channelHasField(channel)) {
    const fieldDef = facet[channel];
    const titleConfig = getHeaderProperty("title", null, config, channel);
    let title$1 = title(fieldDef, config, {
      allowDisabling: true,
      includeDefault: titleConfig === void 0 || !!titleConfig
    });
    if (child.component.layoutHeaders[channel].title) {
      title$1 = isArray(title$1) ? title$1.join(", ") : title$1;
      title$1 += ` / ${child.component.layoutHeaders[channel].title}`;
      child.component.layoutHeaders[channel].title = null;
    }
    const labelOrient = getHeaderProperty("labelOrient", fieldDef.header, config, channel);
    const labels2 = fieldDef.header !== null ? getFirstDefined(fieldDef.header?.labels, config.header.labels, true) : false;
    const headerType = contains2(["bottom", "right"], labelOrient) ? "footer" : "header";
    component.layoutHeaders[channel] = {
      title: fieldDef.header !== null ? title$1 : null,
      facetFieldDef: fieldDef,
      [headerType]: channel === "facet" ? [] : [makeHeaderComponent(model, channel, labels2)]
    };
  }
}
function makeHeaderComponent(model, channel, labels2) {
  const sizeType = channel === "row" ? "height" : "width";
  return {
    labels: labels2,
    sizeSignal: model.child.component.layoutSize.get(sizeType) ? model.child.getSizeSignalRef(sizeType) : void 0,
    axes: []
  };
}
function mergeChildAxis(model, channel) {
  const { child } = model;
  if (child.component.axes[channel]) {
    const { layoutHeaders: layoutHeaders2, resolve: resolve2 } = model.component;
    resolve2.axis[channel] = parseGuideResolve(resolve2, channel);
    if (resolve2.axis[channel] === "shared") {
      const headerChannel = channel === "x" ? "column" : "row";
      const layoutHeader = layoutHeaders2[headerChannel];
      for (const axisComponent of child.component.axes[channel]) {
        const headerType = getHeaderType(axisComponent.get("orient"));
        layoutHeader[headerType] ??= [makeHeaderComponent(model, headerChannel, false)];
        const mainAxis = assembleAxis(axisComponent, "main", model.config, { header: true });
        if (mainAxis) {
          layoutHeader[headerType][0].axes.push(mainAxis);
        }
        axisComponent.mainExtracted = true;
      }
    }
  }
}
function parseLayerLayoutSize(model) {
  parseChildrenLayoutSize(model);
  parseNonUnitLayoutSizeForChannel(model, "width");
  parseNonUnitLayoutSizeForChannel(model, "height");
}
function parseConcatLayoutSize(model) {
  parseChildrenLayoutSize(model);
  const widthType = model.layout.columns === 1 ? "width" : "childWidth";
  const heightType = model.layout.columns === void 0 ? "height" : "childHeight";
  parseNonUnitLayoutSizeForChannel(model, widthType);
  parseNonUnitLayoutSizeForChannel(model, heightType);
}
function parseChildrenLayoutSize(model) {
  for (const child of model.children) {
    child.parseLayoutSize();
  }
}
function parseNonUnitLayoutSizeForChannel(model, layoutSizeType) {
  const sizeType = getSizeTypeFromLayoutSizeType(layoutSizeType);
  const channel = getPositionScaleChannel(sizeType);
  const resolve2 = model.component.resolve;
  const layoutSizeCmpt = model.component.layoutSize;
  let mergedSize;
  for (const child of model.children) {
    const childSize = child.component.layoutSize.getWithExplicit(sizeType);
    const scaleResolve = resolve2.scale[channel] ?? defaultScaleResolve(channel, model);
    if (scaleResolve === "independent" && childSize.value === "step") {
      mergedSize = void 0;
      break;
    }
    if (mergedSize) {
      if (scaleResolve === "independent" && mergedSize.value !== childSize.value) {
        mergedSize = void 0;
        break;
      }
      mergedSize = mergeValuesWithExplicit(mergedSize, childSize, sizeType, "");
    } else {
      mergedSize = childSize;
    }
  }
  if (mergedSize) {
    for (const child of model.children) {
      model.renameSignal(child.getName(sizeType), model.getName(layoutSizeType));
      child.component.layoutSize.set(sizeType, "merged", false);
    }
    layoutSizeCmpt.setWithExplicit(layoutSizeType, mergedSize);
  } else {
    layoutSizeCmpt.setWithExplicit(layoutSizeType, {
      explicit: false,
      value: void 0
    });
  }
}
function parseUnitLayoutSize(model) {
  const { size, component } = model;
  for (const channel of POSITION_SCALE_CHANNELS) {
    const sizeType = getSizeChannel(channel);
    if (size[sizeType] != void 0 && size[sizeType] != null) {
      const specifiedSize = size[sizeType];
      component.layoutSize.set(sizeType, isStep(specifiedSize) ? "step" : specifiedSize, true);
    } else {
      const defaultSize = defaultUnitSize(model, sizeType);
      component.layoutSize.set(sizeType, defaultSize, false);
    }
  }
}
function defaultUnitSize(model, sizeType) {
  const channel = sizeType === "width" ? "x" : "y";
  const config = model.config;
  const scaleComponent = model.getScaleComponent(channel);
  if (scaleComponent) {
    const scaleType2 = scaleComponent.get("type");
    const range7 = scaleComponent.get("range");
    if (hasDiscreteDomain(scaleType2)) {
      const size = getViewConfigDiscreteSize(config.view, sizeType);
      if (isVgRangeStep(range7) || isStep(size)) {
        return "step";
      } else {
        return size;
      }
    } else {
      return getViewConfigContinuousSize(config.view, sizeType);
    }
  } else if (model.hasProjection || model.mark === "arc") {
    return getViewConfigContinuousSize(config.view, sizeType);
  } else {
    const size = getViewConfigDiscreteSize(config.view, sizeType);
    return isStep(size) ? size.step : size;
  }
}
function facetSortFieldName(fieldDef, sort4, opt) {
  return vgField(sort4, { suffix: `by_${vgField(fieldDef)}`, ...opt });
}
var FacetModel = class _FacetModel extends ModelWithField {
  facet;
  child;
  children;
  constructor(spec, parent, parentGivenName, config) {
    super(spec, "facet", parent, parentGivenName, config, spec.resolve);
    this.child = buildModel(spec.spec, this, this.getName("child"), void 0, config);
    this.children = [this.child];
    this.facet = this.initFacet(spec.facet);
  }
  initFacet(facet) {
    if (!isFacetMapping(facet)) {
      return { facet: this.initFacetFieldDef(facet, "facet") };
    }
    const channels = keys3(facet);
    const normalizedFacet = {};
    for (const channel of channels) {
      if (![ROW, COLUMN].includes(channel)) {
        warn3(incompatibleChannel(channel, "facet"));
        break;
      }
      const fieldDef = facet[channel];
      if (fieldDef.field === void 0) {
        warn3(emptyFieldDef(fieldDef, channel));
        break;
      }
      normalizedFacet[channel] = this.initFacetFieldDef(fieldDef, channel);
    }
    return normalizedFacet;
  }
  initFacetFieldDef(fieldDef, channel) {
    const facetFieldDef = initFieldDef(fieldDef, channel);
    if (facetFieldDef.header) {
      facetFieldDef.header = replaceExprRef(facetFieldDef.header);
    } else if (facetFieldDef.header === null) {
      facetFieldDef.header = null;
    }
    return facetFieldDef;
  }
  channelHasField(channel) {
    return hasProperty(this.facet, channel);
  }
  fieldDef(channel) {
    return this.facet[channel];
  }
  parseData() {
    this.component.data = parseData2(this);
    this.child.parseData();
  }
  parseLayoutSize() {
    parseChildrenLayoutSize(this);
  }
  parseSelections() {
    this.child.parseSelections();
    this.component.selection = this.child.component.selection;
    if (vals(this.component.selection).some((selCmpt) => isTimerSelection(selCmpt))) {
      error2(MULTI_VIEW_ANIMATION_UNSUPPORTED);
    }
  }
  parseMarkGroup() {
    this.child.parseMarkGroup();
  }
  parseAxesAndHeaders() {
    this.child.parseAxesAndHeaders();
    parseFacetHeaders(this);
  }
  assembleSelectionTopLevelSignals(signals) {
    return this.child.assembleSelectionTopLevelSignals(signals);
  }
  assembleSignals() {
    this.child.assembleSignals();
    return [];
  }
  assembleSelectionData(data4) {
    return this.child.assembleSelectionData(data4);
  }
  getHeaderLayoutMixins() {
    const layoutMixins = {};
    for (const channel of FACET_CHANNELS) {
      for (const headerType of HEADER_TYPES) {
        const layoutHeaderComponent = this.component.layoutHeaders[channel];
        const headerComponent = layoutHeaderComponent[headerType];
        const { facetFieldDef } = layoutHeaderComponent;
        if (facetFieldDef) {
          const titleOrient = getHeaderProperty("titleOrient", facetFieldDef.header, this.config, channel);
          if (["right", "bottom"].includes(titleOrient)) {
            const headerChannel = getHeaderChannel(channel, titleOrient);
            layoutMixins.titleAnchor ??= {};
            layoutMixins.titleAnchor[headerChannel] = "end";
          }
        }
        if (headerComponent?.[0]) {
          const sizeType = channel === "row" ? "height" : "width";
          const bandType = headerType === "header" ? "headerBand" : "footerBand";
          if (channel !== "facet" && !this.child.component.layoutSize.get(sizeType)) {
            layoutMixins[bandType] ??= {};
            layoutMixins[bandType][channel] = 0.5;
          }
          if (layoutHeaderComponent.title) {
            layoutMixins.offset ??= {};
            layoutMixins.offset[channel === "row" ? "rowTitle" : "columnTitle"] = 10;
          }
        }
      }
    }
    return layoutMixins;
  }
  assembleDefaultLayout() {
    const { column, row } = this.facet;
    const columns = column ? this.columnDistinctSignal() : row ? 1 : void 0;
    let align2 = "all";
    if (!row && this.component.resolve.scale.x === "independent") {
      align2 = "none";
    } else if (!column && this.component.resolve.scale.y === "independent") {
      align2 = "none";
    }
    return {
      ...this.getHeaderLayoutMixins(),
      ...columns ? { columns } : {},
      bounds: "full",
      align: align2
    };
  }
  assembleLayoutSignals() {
    return this.child.assembleLayoutSignals();
  }
  columnDistinctSignal() {
    if (this.parent && this.parent instanceof _FacetModel) {
      return void 0;
    } else {
      const facetLayoutDataName = this.getName("column_domain");
      return { signal: `length(data('${facetLayoutDataName}'))` };
    }
  }
  assembleGroupStyle() {
    return void 0;
  }
  assembleGroup(signals) {
    if (this.parent && this.parent instanceof _FacetModel) {
      return {
        ...this.channelHasField("column") ? {
          encode: {
            update: {
              // TODO(https://github.com/vega/vega-lite/issues/2759):
              // Correct the signal for facet of concat of facet_column
              columns: { field: vgField(this.facet.column, { prefix: "distinct" }) }
            }
          }
        } : {},
        ...super.assembleGroup(signals)
      };
    }
    return super.assembleGroup(signals);
  }
  /**
   * Aggregate cardinality for calculating size
   */
  getCardinalityAggregateForChild() {
    const fields = [];
    const ops2 = [];
    const as = [];
    if (this.child instanceof _FacetModel) {
      if (this.child.channelHasField("column")) {
        const field3 = vgField(this.child.facet.column);
        fields.push(field3);
        ops2.push("distinct");
        as.push(`distinct_${field3}`);
      }
    } else {
      for (const channel of POSITION_SCALE_CHANNELS) {
        const childScaleComponent = this.child.component.scales[channel];
        if (childScaleComponent && !childScaleComponent.merged) {
          const type4 = childScaleComponent.get("type");
          const range7 = childScaleComponent.get("range");
          if (hasDiscreteDomain(type4) && isVgRangeStep(range7)) {
            const domain4 = assembleDomain(this.child, channel);
            const field3 = getFieldFromDomain(domain4);
            if (field3) {
              fields.push(field3);
              ops2.push("distinct");
              as.push(`distinct_${field3}`);
            } else {
              warn3(unknownField(channel));
            }
          }
        }
      }
    }
    return { fields, ops: ops2, as };
  }
  assembleFacet() {
    const { name, data: data4 } = this.component.data.facetRoot;
    const { row, column } = this.facet;
    const { fields, ops: ops2, as } = this.getCardinalityAggregateForChild();
    const groupby = [];
    for (const channel of FACET_CHANNELS) {
      const fieldDef = this.facet[channel];
      if (fieldDef) {
        groupby.push(vgField(fieldDef));
        const { bin: bin3, sort: sort4 } = fieldDef;
        if (isBinning(bin3)) {
          groupby.push(vgField(fieldDef, { binSuffix: "end" }));
        }
        if (isSortField(sort4)) {
          const { field: field3, op = DEFAULT_SORT_OP } = sort4;
          const outputName = facetSortFieldName(fieldDef, sort4);
          if (row && column) {
            fields.push(outputName);
            ops2.push("max");
            as.push(outputName);
          } else {
            fields.push(field3);
            ops2.push(op);
            as.push(outputName);
          }
        } else if (isArray(sort4)) {
          const outputName = sortArrayIndexField(fieldDef, channel);
          fields.push(outputName);
          ops2.push("max");
          as.push(outputName);
        }
      }
    }
    const cross2 = !!row && !!column;
    return {
      name,
      data: data4,
      groupby,
      ...cross2 || fields.length > 0 ? {
        aggregate: {
          ...cross2 ? { cross: cross2 } : {},
          ...fields.length ? { fields, ops: ops2, as } : {}
        }
      } : {}
    };
  }
  facetSortFields(channel) {
    const { facet } = this;
    const fieldDef = facet[channel];
    if (fieldDef) {
      if (isSortField(fieldDef.sort)) {
        return [facetSortFieldName(fieldDef, fieldDef.sort, { expr: "datum" })];
      } else if (isArray(fieldDef.sort)) {
        return [sortArrayIndexField(fieldDef, channel, { expr: "datum" })];
      }
      return [vgField(fieldDef, { expr: "datum" })];
    }
    return [];
  }
  facetSortOrder(channel) {
    const { facet } = this;
    const fieldDef = facet[channel];
    if (fieldDef) {
      const { sort: sort4 } = fieldDef;
      const order = (isSortField(sort4) ? sort4.order : !isArray(sort4) && sort4) || "ascending";
      return [order];
    }
    return [];
  }
  assembleLabelTitle() {
    const { facet, config } = this;
    if (facet.facet) {
      return assembleLabelTitle(facet.facet, "facet", config);
    }
    const ORTHOGONAL_ORIENT = {
      row: ["top", "bottom"],
      column: ["left", "right"]
    };
    for (const channel of HEADER_CHANNELS) {
      if (facet[channel]) {
        const labelOrient = getHeaderProperty("labelOrient", facet[channel]?.header, config, channel);
        if (ORTHOGONAL_ORIENT[channel].includes(labelOrient)) {
          return assembleLabelTitle(facet[channel], channel, config);
        }
      }
    }
    return void 0;
  }
  assembleMarks() {
    const { child } = this;
    const facetRoot = this.component.data.facetRoot;
    const data4 = assembleFacetData(facetRoot);
    const encodeEntry2 = child.assembleGroupEncodeEntry(false);
    const title2 = this.assembleLabelTitle() || child.assembleTitle();
    const style3 = child.assembleGroupStyle();
    const markGroup = {
      name: this.getName("cell"),
      type: "group",
      ...title2 ? { title: title2 } : {},
      ...style3 ? { style: style3 } : {},
      from: {
        facet: this.assembleFacet()
      },
      // TODO: move this to after data
      sort: {
        field: FACET_CHANNELS.map((c3) => this.facetSortFields(c3)).flat(),
        order: FACET_CHANNELS.map((c3) => this.facetSortOrder(c3)).flat()
      },
      ...data4.length > 0 ? { data: data4 } : {},
      ...encodeEntry2 ? { encode: { update: encodeEntry2 } } : {},
      ...child.assembleGroup(assembleFacetSignals(this, []))
    };
    return [markGroup];
  }
  getMapping() {
    return this.facet;
  }
};
function makeJoinAggregateFromFacet(parent, facet) {
  const { row, column } = facet;
  if (row && column) {
    let newParent = null;
    for (const fieldDef of [row, column]) {
      if (isSortField(fieldDef.sort)) {
        const { field: field3, op = DEFAULT_SORT_OP } = fieldDef.sort;
        parent = newParent = new JoinAggregateTransformNode(parent, {
          joinaggregate: [
            {
              op,
              field: field3,
              as: facetSortFieldName(fieldDef, fieldDef.sort, { forAs: true })
            }
          ],
          groupby: [vgField(fieldDef)]
        });
      }
    }
    return newParent;
  }
  return null;
}
function findSource(data4, sources) {
  for (const other of sources) {
    const otherData = other.data;
    if (data4.name && other.hasName() && data4.name !== other.dataName) {
      continue;
    }
    const formatMesh = data4.format?.mesh;
    const otherFeature = otherData.format?.feature;
    if (formatMesh && otherFeature) {
      continue;
    }
    const formatFeature = data4.format?.feature;
    if ((formatFeature || otherFeature) && formatFeature !== otherFeature) {
      continue;
    }
    const otherMesh = otherData.format?.mesh;
    if ((formatMesh || otherMesh) && formatMesh !== otherMesh) {
      continue;
    }
    if (isInlineData(data4) && isInlineData(otherData)) {
      if (deepEqual(data4.values, otherData.values)) {
        return other;
      }
    } else if (isUrlData(data4) && isUrlData(otherData)) {
      if (data4.url === otherData.url) {
        return other;
      }
    } else if (isNamedData(data4)) {
      if (data4.name === other.dataName) {
        return other;
      }
    }
  }
  return null;
}
function parseRoot(model, sources) {
  if (model.data || !model.parent) {
    if (model.data === null) {
      const source4 = new SourceNode({ values: [] });
      sources.push(source4);
      return source4;
    }
    const existingSource = findSource(model.data, sources);
    if (existingSource) {
      if (!isGenerator(model.data)) {
        existingSource.data.format = mergeDeep({}, model.data.format, existingSource.data.format);
      }
      if (!existingSource.hasName() && model.data.name) {
        existingSource.dataName = model.data.name;
      }
      return existingSource;
    } else {
      const source4 = new SourceNode(model.data);
      sources.push(source4);
      return source4;
    }
  } else {
    return model.parent.component.data.facetRoot ? model.parent.component.data.facetRoot : model.parent.component.data.main;
  }
}
function parseTransformArray(head, model, ancestorParse) {
  let lookupCounter = 0;
  for (const t of model.transforms) {
    let derivedType = void 0;
    let transformNode;
    if (isCalculate(t)) {
      transformNode = head = new CalculateNode(head, t);
      derivedType = "derived";
    } else if (isFilter(t)) {
      const implicit2 = getImplicitFromFilterTransform(t);
      transformNode = head = ParseNode.makeWithAncestors(head, {}, implicit2, ancestorParse) ?? head;
      head = new FilterNode(head, model, t.filter);
    } else if (isBin(t)) {
      transformNode = head = BinNode.makeFromTransform(head, t, model);
      derivedType = "number";
    } else if (isTimeUnit(t)) {
      derivedType = "date";
      const parsedAs = ancestorParse.getWithExplicit(t.field);
      if (parsedAs.value === void 0) {
        head = new ParseNode(head, { [t.field]: derivedType });
        ancestorParse.set(t.field, derivedType, false);
      }
      transformNode = head = TimeUnitNode.makeFromTransform(head, t);
    } else if (isAggregate(t)) {
      transformNode = head = AggregateNode.makeFromTransform(head, t);
      derivedType = "number";
      if (requiresSelectionId(model)) {
        head = new IdentifierNode(head);
      }
    } else if (isLookup(t)) {
      transformNode = head = LookupNode.make(head, model, t, lookupCounter++);
      derivedType = "derived";
    } else if (isWindow(t)) {
      transformNode = head = new WindowTransformNode(head, t);
      derivedType = "number";
    } else if (isJoinAggregate(t)) {
      transformNode = head = new JoinAggregateTransformNode(head, t);
      derivedType = "number";
    } else if (isStack(t)) {
      transformNode = head = StackNode.makeFromTransform(head, t);
      derivedType = "derived";
    } else if (isFold(t)) {
      transformNode = head = new FoldTransformNode(head, t);
      derivedType = "derived";
    } else if (isExtent(t)) {
      transformNode = head = new ExtentTransformNode(head, t);
      derivedType = "derived";
    } else if (isFlatten(t)) {
      transformNode = head = new FlattenTransformNode(head, t);
      derivedType = "derived";
    } else if (isPivot(t)) {
      transformNode = head = new PivotTransformNode(head, t);
      derivedType = "derived";
    } else if (isSample(t)) {
      head = new SampleTransformNode(head, t);
    } else if (isImpute(t)) {
      transformNode = head = ImputeNode.makeFromTransform(head, t);
      derivedType = "derived";
    } else if (isDensity(t)) {
      transformNode = head = new DensityTransformNode(head, t);
      derivedType = "derived";
    } else if (isQuantile2(t)) {
      transformNode = head = new QuantileTransformNode(head, t);
      derivedType = "derived";
    } else if (isRegression(t)) {
      transformNode = head = new RegressionTransformNode(head, t);
      derivedType = "derived";
    } else if (isLoess(t)) {
      transformNode = head = new LoessTransformNode(head, t);
      derivedType = "derived";
    } else {
      warn3(invalidTransformIgnored(t));
      continue;
    }
    if (transformNode && derivedType !== void 0) {
      for (const field3 of transformNode.producedFields() ?? []) {
        ancestorParse.set(field3, derivedType, false);
      }
    }
  }
  return head;
}
function parseData2(model) {
  let head = parseRoot(model, model.component.data.sources);
  const { outputNodes, outputNodeRefCounts } = model.component.data;
  const data4 = model.data;
  const newData = data4 && (isGenerator(data4) || isUrlData(data4) || isInlineData(data4));
  const ancestorParse = !newData && model.parent ? model.parent.component.data.ancestorParse.clone() : new AncestorParse();
  if (isGenerator(data4)) {
    if (isSequenceGenerator(data4)) {
      head = new SequenceNode(head, data4.sequence);
    } else if (isGraticuleGenerator(data4)) {
      head = new GraticuleNode(head, data4.graticule);
    }
    ancestorParse.parseNothing = true;
  } else if (data4?.format?.parse === null) {
    ancestorParse.parseNothing = true;
  }
  head = ParseNode.makeExplicit(head, model, ancestorParse) ?? head;
  head = new IdentifierNode(head);
  const parentIsLayer = model.parent && isLayerModel(model.parent);
  if (isUnitModel(model) || isFacetModel(model)) {
    if (parentIsLayer) {
      head = BinNode.makeFromEncoding(head, model) ?? head;
    }
  }
  if (model.transforms.length > 0) {
    head = parseTransformArray(head, model, ancestorParse);
  }
  const implicitSelection = getImplicitFromSelection(model);
  const implicitEncoding = getImplicitFromEncoding(model);
  head = ParseNode.makeWithAncestors(head, {}, { ...implicitSelection, ...implicitEncoding }, ancestorParse) ?? head;
  if (isUnitModel(model)) {
    head = GeoJSONNode.parseAll(head, model);
    head = GeoPointNode.parseAll(head, model);
  }
  if (isUnitModel(model) || isFacetModel(model)) {
    if (!parentIsLayer) {
      head = BinNode.makeFromEncoding(head, model) ?? head;
    }
    head = TimeUnitNode.makeFromEncoding(head, model) ?? head;
    head = CalculateNode.parseAllForSortIndex(head, model);
  }
  const raw = head = makeOutputNode(DataSourceType.Raw, model, head);
  if (isUnitModel(model)) {
    const agg = AggregateNode.makeFromEncoding(head, model);
    if (agg) {
      head = agg;
      if (requiresSelectionId(model)) {
        head = new IdentifierNode(head);
      }
    }
    head = ImputeNode.makeFromEncoding(head, model) ?? head;
    head = StackNode.makeFromEncoding(head, model) ?? head;
  }
  let preFilterInvalid;
  let dataSourcesForHandlingInvalidValues;
  if (isUnitModel(model)) {
    const { markDef, mark, config } = model;
    const invalid = getMarkPropOrConfig("invalid", markDef, config);
    const { marks, scales: scales2 } = dataSourcesForHandlingInvalidValues = getDataSourcesForHandlingInvalidValues({
      invalid,
      isPath: isPathMark(mark)
    });
    if (marks !== scales2 && scales2 === "include-invalid-values") {
      preFilterInvalid = head = makeOutputNode(DataSourceType.PreFilterInvalid, model, head);
    }
    if (marks === "exclude-invalid-values") {
      head = FilterInvalidNode.make(head, model, dataSourcesForHandlingInvalidValues) ?? head;
    }
  }
  const main2 = head = makeOutputNode(DataSourceType.Main, model, head);
  let postFilterInvalid;
  if (isUnitModel(model) && dataSourcesForHandlingInvalidValues) {
    const { marks, scales: scales2 } = dataSourcesForHandlingInvalidValues;
    if (marks === "include-invalid-values" && scales2 === "exclude-invalid-values") {
      head = FilterInvalidNode.make(head, model, dataSourcesForHandlingInvalidValues) ?? head;
      postFilterInvalid = head = makeOutputNode(DataSourceType.PostFilterInvalid, model, head);
    }
  }
  if (isUnitModel(model)) {
    materializeSelections(model, main2);
  }
  let facetRoot = null;
  if (isFacetModel(model)) {
    const facetName = model.getName("facet");
    head = makeJoinAggregateFromFacet(head, model.facet) ?? head;
    facetRoot = new FacetNode(head, model, facetName, main2.getSource());
    outputNodes[facetName] = facetRoot;
  }
  return {
    ...model.component.data,
    outputNodes,
    outputNodeRefCounts,
    raw,
    main: main2,
    facetRoot,
    ancestorParse,
    preFilterInvalid,
    postFilterInvalid
  };
}
function makeOutputNode(dataSourceType, model, head) {
  const { outputNodes, outputNodeRefCounts } = model.component.data;
  const name = model.getDataName(dataSourceType);
  const node = new OutputNode(head, name, dataSourceType, outputNodeRefCounts);
  outputNodes[name] = node;
  return node;
}
var ConcatModel = class extends Model {
  children;
  constructor(spec, parent, parentGivenName, config) {
    super(spec, "concat", parent, parentGivenName, config, spec.resolve);
    if (spec.resolve?.axis?.x === "shared" || spec.resolve?.axis?.y === "shared") {
      warn3(CONCAT_CANNOT_SHARE_AXIS);
    }
    this.children = this.getChildren(spec).map((child, i) => {
      return buildModel(child, this, this.getName(`concat_${i}`), void 0, config);
    });
  }
  parseData() {
    this.component.data = parseData2(this);
    for (const child of this.children) {
      child.parseData();
    }
  }
  parseSelections() {
    this.component.selection = {};
    for (const child of this.children) {
      child.parseSelections();
      for (const key2 of keys3(child.component.selection)) {
        this.component.selection[key2] = child.component.selection[key2];
      }
    }
    if (vals(this.component.selection).some((selCmpt) => isTimerSelection(selCmpt))) {
      error2(MULTI_VIEW_ANIMATION_UNSUPPORTED);
    }
  }
  parseMarkGroup() {
    for (const child of this.children) {
      child.parseMarkGroup();
    }
  }
  parseAxesAndHeaders() {
    for (const child of this.children) {
      child.parseAxesAndHeaders();
    }
  }
  getChildren(spec) {
    if (isVConcatSpec(spec)) {
      return spec.vconcat;
    } else if (isHConcatSpec(spec)) {
      return spec.hconcat;
    }
    return spec.concat;
  }
  parseLayoutSize() {
    parseConcatLayoutSize(this);
  }
  parseAxisGroup() {
    return null;
  }
  assembleSelectionTopLevelSignals(signals) {
    return this.children.reduce((sg, child) => child.assembleSelectionTopLevelSignals(sg), signals);
  }
  assembleSignals() {
    this.children.forEach((child) => child.assembleSignals());
    return [];
  }
  assembleLayoutSignals() {
    const layoutSignals = assembleLayoutSignals(this);
    for (const child of this.children) {
      layoutSignals.push(...child.assembleLayoutSignals());
    }
    return layoutSignals;
  }
  assembleSelectionData(data4) {
    return this.children.reduce((db, child) => child.assembleSelectionData(db), data4);
  }
  assembleMarks() {
    return this.children.map((child) => {
      const title2 = child.assembleTitle();
      const style3 = child.assembleGroupStyle();
      const encodeEntry2 = child.assembleGroupEncodeEntry(false);
      return {
        type: "group",
        name: child.getName("group"),
        ...title2 ? { title: title2 } : {},
        ...style3 ? { style: style3 } : {},
        ...encodeEntry2 ? { encode: { update: encodeEntry2 } } : {},
        ...child.assembleGroup()
      };
    });
  }
  assembleGroupStyle() {
    return void 0;
  }
  assembleDefaultLayout() {
    const columns = this.layout.columns;
    return {
      ...columns != null ? { columns } : {},
      bounds: "full",
      // Use align each so it can work with multiple plots with different size
      align: "each"
    };
  }
};
function isFalseOrNull(v2) {
  return v2 === false || v2 === null;
}
var AXIS_COMPONENT_PROPERTIES_INDEX = {
  disable: 1,
  gridScale: 1,
  scale: 1,
  ...COMMON_AXIS_PROPERTIES_INDEX,
  labelExpr: 1,
  encode: 1
};
var AXIS_COMPONENT_PROPERTIES = keys3(AXIS_COMPONENT_PROPERTIES_INDEX);
var AxisComponent = class _AxisComponent extends Split {
  explicit;
  implicit;
  mainExtracted;
  constructor(explicit = {}, implicit2 = {}, mainExtracted = false) {
    super();
    this.explicit = explicit;
    this.implicit = implicit2;
    this.mainExtracted = mainExtracted;
  }
  clone() {
    return new _AxisComponent(duplicate(this.explicit), duplicate(this.implicit), this.mainExtracted);
  }
  hasAxisPart(part) {
    if (part === "axis") {
      return true;
    }
    if (part === "grid" || part === "title") {
      return !!this.get(part);
    }
    return !isFalseOrNull(this.get(part));
  }
  hasOrientSignalRef() {
    return isSignalRef(this.explicit.orient);
  }
};
function labels(model, channel, specifiedLabelsSpec) {
  const { encoding, config } = model;
  const fieldOrDatumDef = getFieldOrDatumDef(encoding[channel]) ?? getFieldOrDatumDef(encoding[getSecondaryRangeChannel(channel)]);
  const axis = model.axis(channel) || {};
  const { format: format6, formatType } = axis;
  if (isCustomFormatType(formatType)) {
    return {
      text: formatCustomType({
        fieldOrDatumDef,
        field: "datum.value",
        format: format6,
        formatType,
        config
      }),
      ...specifiedLabelsSpec
    };
  } else if (format6 === void 0 && formatType === void 0 && config.customFormatTypes) {
    if (channelDefType(fieldOrDatumDef) === "quantitative") {
      if (isPositionFieldOrDatumDef(fieldOrDatumDef) && fieldOrDatumDef.stack === "normalize" && config.normalizedNumberFormatType) {
        return {
          text: formatCustomType({
            fieldOrDatumDef,
            field: "datum.value",
            format: config.normalizedNumberFormat,
            formatType: config.normalizedNumberFormatType,
            config
          }),
          ...specifiedLabelsSpec
        };
      } else if (config.numberFormatType) {
        return {
          text: formatCustomType({
            fieldOrDatumDef,
            field: "datum.value",
            format: config.numberFormat,
            formatType: config.numberFormatType,
            config
          }),
          ...specifiedLabelsSpec
        };
      }
    }
    if (channelDefType(fieldOrDatumDef) === "temporal" && config.timeFormatType && isFieldDef(fieldOrDatumDef) && !fieldOrDatumDef.timeUnit) {
      return {
        text: formatCustomType({
          fieldOrDatumDef,
          field: "datum.value",
          format: config.timeFormat,
          formatType: config.timeFormatType,
          config
        }),
        ...specifiedLabelsSpec
      };
    }
  }
  return specifiedLabelsSpec;
}
function parseUnitAxes(model) {
  return POSITION_SCALE_CHANNELS.reduce((axis, channel) => {
    if (model.component.scales[channel]) {
      axis[channel] = [parseAxis2(channel, model)];
    }
    return axis;
  }, {});
}
var OPPOSITE_ORIENT = {
  bottom: "top",
  top: "bottom",
  left: "right",
  right: "left"
};
function parseLayerAxes(model) {
  const { axes, resolve: resolve2 } = model.component;
  const axisCount = { top: 0, bottom: 0, right: 0, left: 0 };
  for (const child of model.children) {
    child.parseAxesAndHeaders();
    for (const channel of keys3(child.component.axes)) {
      resolve2.axis[channel] = parseGuideResolve(model.component.resolve, channel);
      if (resolve2.axis[channel] === "shared") {
        axes[channel] = mergeAxisComponents(axes[channel], child.component.axes[channel]);
        if (!axes[channel]) {
          resolve2.axis[channel] = "independent";
          delete axes[channel];
        }
      }
    }
  }
  for (const channel of POSITION_SCALE_CHANNELS) {
    for (const child of model.children) {
      if (!child.component.axes[channel]) {
        continue;
      }
      if (resolve2.axis[channel] === "independent") {
        axes[channel] = (axes[channel] ?? []).concat(child.component.axes[channel]);
        for (const axisComponent of child.component.axes[channel]) {
          const { value: orient2, explicit } = axisComponent.getWithExplicit("orient");
          if (isSignalRef(orient2)) {
            continue;
          }
          if (axisCount[orient2] > 0 && !explicit) {
            const oppositeOrient = OPPOSITE_ORIENT[orient2];
            if (axisCount[orient2] > axisCount[oppositeOrient]) {
              axisComponent.set("orient", oppositeOrient, false);
            }
          }
          axisCount[orient2]++;
        }
      }
      delete child.component.axes[channel];
    }
    if (resolve2.axis[channel] === "independent" && axes[channel] && axes[channel].length > 1) {
      for (const [index4, axisCmpt] of (axes[channel] || []).entries()) {
        if (index4 > 0 && !!axisCmpt.get("grid") && !axisCmpt.explicit.grid) {
          axisCmpt.implicit.grid = false;
        }
      }
    }
  }
}
function mergeAxisComponents(mergedAxisCmpts, childAxisCmpts) {
  if (mergedAxisCmpts) {
    if (mergedAxisCmpts.length !== childAxisCmpts.length) {
      return void 0;
    }
    const length3 = mergedAxisCmpts.length;
    for (let i = 0; i < length3; i++) {
      const merged = mergedAxisCmpts[i];
      const child = childAxisCmpts[i];
      if (!!merged !== !!child) {
        return void 0;
      } else if (merged && child) {
        const mergedOrient = merged.getWithExplicit("orient");
        const childOrient = child.getWithExplicit("orient");
        if (mergedOrient.explicit && childOrient.explicit && mergedOrient.value !== childOrient.value) {
          return void 0;
        } else {
          mergedAxisCmpts[i] = mergeAxisComponent(merged, child);
        }
      }
    }
  } else {
    return childAxisCmpts.map((axisComponent) => axisComponent.clone());
  }
  return mergedAxisCmpts;
}
function mergeAxisComponent(merged, child) {
  for (const prop of AXIS_COMPONENT_PROPERTIES) {
    const mergedValueWithExplicit = mergeValuesWithExplicit(
      merged.getWithExplicit(prop),
      child.getWithExplicit(prop),
      prop,
      "axis",
      // Tie breaker function
      (v1, v2) => {
        switch (prop) {
          case "title":
            return mergeTitleComponent(v1, v2);
          case "gridScale":
            return {
              explicit: v1.explicit,
              // keep the old explicit
              value: getFirstDefined(v1.value, v2.value)
            };
        }
        return defaultTieBreaker(v1, v2, prop, "axis");
      }
    );
    merged.setWithExplicit(prop, mergedValueWithExplicit);
  }
  return merged;
}
function isExplicit(value4, property2, axis, model, channel) {
  if (property2 === "disable") {
    return axis !== void 0;
  }
  axis = axis || {};
  switch (property2) {
    case "titleAngle":
    case "labelAngle":
      return value4 === (isSignalRef(axis.labelAngle) ? axis.labelAngle : normalizeAngle(axis.labelAngle));
    case "values":
      return !!axis.values;
    // specified axis.values is already respected, but may get transformed.
    case "encode":
      return !!axis.encoding || !!axis.labelAngle;
    case "title":
      if (value4 === getFieldDefTitle(model, channel)) {
        return true;
      }
  }
  return value4 === axis[property2];
}
var propsToAlwaysIncludeConfig = /* @__PURE__ */ new Set([
  "grid",
  // Grid is an exception because we need to set grid = true to generate another grid axis
  "translate",
  // translate has dependent logic for bar's bin position and it's 0.5 by default in Vega. If a config overrides this value, we need to know.
  // the rest are not axis configs in Vega, but are in VL, so we need to set too.
  "format",
  "formatType",
  "orient",
  "labelExpr",
  "tickCount",
  "position",
  "tickMinStep"
]);
function parseAxis2(channel, model) {
  let axis = model.axis(channel);
  const axisComponent = new AxisComponent();
  const fieldOrDatumDef = getFieldOrDatumDef(model.encoding[channel]);
  const { mark, config } = model;
  const orient2 = axis?.orient || config[channel === "x" ? "axisX" : "axisY"]?.orient || config.axis?.orient || defaultOrient(channel);
  const scaleType2 = model.getScaleComponent(channel).get("type");
  const axisConfigs = getAxisConfigs(channel, scaleType2, orient2, model.config);
  const disable = axis !== void 0 ? !axis : getAxisConfig("disable", config.style, axis?.style, axisConfigs).configValue;
  axisComponent.set("disable", disable, axis !== void 0);
  if (disable) {
    return axisComponent;
  }
  axis = axis || {};
  const labelAngle = getLabelAngle(fieldOrDatumDef, axis, channel, config.style, axisConfigs);
  const formatType = guideFormatType(axis.formatType, fieldOrDatumDef, scaleType2);
  const format6 = guideFormat(fieldOrDatumDef, fieldOrDatumDef.type, axis.format, axis.formatType, config, true);
  const ruleParams = {
    fieldOrDatumDef,
    axis,
    channel,
    model,
    scaleType: scaleType2,
    orient: orient2,
    labelAngle,
    format: format6,
    formatType,
    mark,
    config
  };
  for (const property2 of AXIS_COMPONENT_PROPERTIES) {
    const value4 = property2 in axisRules ? axisRules[property2](ruleParams) : isAxisProperty(property2) ? axis[property2] : void 0;
    const hasValue = value4 !== void 0;
    const explicit = isExplicit(value4, property2, axis, model, channel);
    if (hasValue && explicit) {
      axisComponent.set(property2, value4, explicit);
    } else {
      const { configValue = void 0, configFrom = void 0 } = isAxisProperty(property2) && property2 !== "values" ? getAxisConfig(property2, config.style, axis.style, axisConfigs) : {};
      const hasConfigValue = configValue !== void 0;
      if (hasValue && !hasConfigValue) {
        axisComponent.set(property2, value4, explicit);
      } else if (
        // Cases need implicit values
        // 1. Axis config that aren't available in Vega
        !(configFrom === "vgAxisConfig") || // 2. Certain properties are always included (see `propsToAlwaysIncludeConfig`'s declaration for more details)
        propsToAlwaysIncludeConfig.has(property2) && hasConfigValue || // 3. Conditional axis values and signals
        isConditionalAxisValue(configValue) || isSignalRef(configValue)
      ) {
        axisComponent.set(property2, configValue, false);
      }
    }
  }
  const axisEncoding = axis.encoding ?? {};
  const axisEncode = AXIS_PARTS.reduce((e, part) => {
    if (!axisComponent.hasAxisPart(part)) {
      return e;
    }
    const axisEncodingPart = guideEncodeEntry(axisEncoding[part] ?? {}, model);
    const value4 = part === "labels" ? labels(model, channel, axisEncodingPart) : axisEncodingPart;
    if (value4 !== void 0 && !isEmpty(value4)) {
      e[part] = { update: value4 };
    }
    return e;
  }, {});
  if (!isEmpty(axisEncode)) {
    axisComponent.set("encode", axisEncode, !!axis.encoding || axis.labelAngle !== void 0);
  }
  return axisComponent;
}
function initLayoutSize({ encoding, size }) {
  for (const channel of POSITION_SCALE_CHANNELS) {
    const sizeType = getSizeChannel(channel);
    if (isStep(size[sizeType])) {
      if (isContinuousFieldOrDatumDef(encoding[channel])) {
        delete size[sizeType];
        warn3(stepDropped(sizeType));
      }
    }
  }
  return size;
}
var arc4 = {
  vgMark: "arc",
  encodeEntry: (model) => {
    return {
      ...baseEncodeEntry(model, {
        align: "ignore",
        baseline: "ignore",
        color: "include",
        size: "ignore",
        orient: "ignore",
        theta: "ignore"
      }),
      ...pointPosition("x", model, { defaultPos: "mid" }),
      ...pointPosition("y", model, { defaultPos: "mid" }),
      // arcs are rectangles in polar coordinates
      ...rectPosition(model, "radius"),
      ...rectPosition(model, "theta")
    };
  }
};
var area4 = {
  vgMark: "area",
  encodeEntry: (model) => {
    return {
      ...baseEncodeEntry(model, {
        align: "ignore",
        baseline: "ignore",
        color: "include",
        orient: "include",
        size: "ignore",
        theta: "ignore"
      }),
      ...pointOrRangePosition("x", model, {
        defaultPos: "zeroOrMin",
        defaultPos2: "zeroOrMin",
        range: model.markDef.orient === "horizontal"
      }),
      ...pointOrRangePosition("y", model, {
        defaultPos: "zeroOrMin",
        defaultPos2: "zeroOrMin",
        range: model.markDef.orient === "vertical"
      }),
      ...defined(model)
    };
  }
};
var bar = {
  vgMark: "rect",
  encodeEntry: (model) => {
    return {
      ...baseEncodeEntry(model, {
        align: "ignore",
        baseline: "ignore",
        color: "include",
        orient: "ignore",
        size: "ignore",
        theta: "ignore"
      }),
      ...rectPosition(model, "x"),
      ...rectPosition(model, "y")
    };
  }
};
var geoshape = {
  vgMark: "shape",
  encodeEntry: (model) => {
    return {
      ...baseEncodeEntry(model, {
        align: "ignore",
        baseline: "ignore",
        color: "include",
        size: "ignore",
        orient: "ignore",
        theta: "ignore"
      })
    };
  },
  postEncodingTransform: (model) => {
    const { encoding } = model;
    const shapeDef = encoding.shape;
    const transform4 = {
      type: "geoshape",
      projection: model.projectionName(),
      // as: 'shape',
      ...shapeDef && isFieldDef(shapeDef) && shapeDef.type === GEOJSON ? { field: vgField(shapeDef, { expr: "datum" }) } : {}
    };
    return [transform4];
  }
};
var image3 = {
  vgMark: "image",
  encodeEntry: (model) => {
    return {
      ...baseEncodeEntry(model, {
        align: "ignore",
        baseline: "ignore",
        color: "ignore",
        orient: "ignore",
        size: "ignore",
        theta: "ignore"
      }),
      ...rectPosition(model, "x"),
      ...rectPosition(model, "y"),
      ...text$1(model, "url")
    };
  }
};
var line4 = {
  vgMark: "line",
  encodeEntry: (model) => {
    return {
      ...baseEncodeEntry(model, {
        align: "ignore",
        baseline: "ignore",
        color: "include",
        size: "ignore",
        orient: "ignore",
        theta: "ignore"
      }),
      ...pointPosition("x", model, { defaultPos: "mid" }),
      ...pointPosition("y", model, { defaultPos: "mid" }),
      ...nonPosition("size", model, {
        vgChannel: "strokeWidth"
        // VL's line size is strokeWidth
      }),
      ...defined(model)
    };
  }
};
var trail3 = {
  vgMark: "trail",
  encodeEntry: (model) => {
    return {
      ...baseEncodeEntry(model, {
        align: "ignore",
        baseline: "ignore",
        color: "include",
        size: "include",
        orient: "ignore",
        theta: "ignore"
      }),
      ...pointPosition("x", model, { defaultPos: "mid" }),
      ...pointPosition("y", model, { defaultPos: "mid" }),
      ...nonPosition("size", model),
      ...defined(model)
    };
  }
};
function encodeEntry(model, fixedShape) {
  const { config } = model;
  return {
    ...baseEncodeEntry(model, {
      align: "ignore",
      baseline: "ignore",
      color: "include",
      size: "include",
      orient: "ignore",
      theta: "ignore"
    }),
    ...pointPosition("x", model, { defaultPos: "mid" }),
    ...pointPosition("y", model, { defaultPos: "mid" }),
    ...nonPosition("size", model),
    ...nonPosition("angle", model),
    ...shapeMixins(model, config, fixedShape)
  };
}
function shapeMixins(model, config, fixedShape) {
  if (fixedShape) {
    return { shape: { value: fixedShape } };
  }
  return nonPosition("shape", model);
}
var point8 = {
  vgMark: "symbol",
  encodeEntry: (model) => {
    return encodeEntry(model);
  }
};
var circle = {
  vgMark: "symbol",
  encodeEntry: (model) => {
    return encodeEntry(model, "circle");
  }
};
var square = {
  vgMark: "symbol",
  encodeEntry: (model) => {
    return encodeEntry(model, "square");
  }
};
var rect3 = {
  vgMark: "rect",
  encodeEntry: (model) => {
    return {
      ...baseEncodeEntry(model, {
        align: "ignore",
        baseline: "ignore",
        color: "include",
        orient: "ignore",
        size: "ignore",
        theta: "ignore"
      }),
      ...rectPosition(model, "x"),
      ...rectPosition(model, "y")
    };
  }
};
var rule4 = {
  vgMark: "rule",
  encodeEntry: (model) => {
    const { markDef } = model;
    const orient2 = markDef.orient;
    if (!model.encoding.x && !model.encoding.y && !model.encoding.latitude && !model.encoding.longitude) {
      return {};
    }
    return {
      ...baseEncodeEntry(model, {
        align: "ignore",
        baseline: "ignore",
        color: "include",
        orient: "ignore",
        size: "ignore",
        theta: "ignore"
      }),
      ...pointOrRangePosition("x", model, {
        defaultPos: orient2 === "horizontal" ? "zeroOrMax" : "mid",
        defaultPos2: "zeroOrMin",
        range: orient2 !== "vertical"
        // include x2 for horizontal or line segment rule
      }),
      ...pointOrRangePosition("y", model, {
        defaultPos: orient2 === "vertical" ? "zeroOrMax" : "mid",
        defaultPos2: "zeroOrMin",
        range: orient2 !== "horizontal"
        // include y2 for vertical or line segment rule
      }),
      ...nonPosition("size", model, {
        vgChannel: "strokeWidth"
        // VL's rule size is strokeWidth
      })
    };
  }
};
var text3 = {
  vgMark: "text",
  encodeEntry: (model) => {
    const { config, encoding } = model;
    return {
      ...baseEncodeEntry(model, {
        align: "include",
        baseline: "include",
        color: "include",
        size: "ignore",
        orient: "ignore",
        theta: "include"
      }),
      ...pointPosition("x", model, { defaultPos: "mid" }),
      ...pointPosition("y", model, { defaultPos: "mid" }),
      ...text$1(model),
      ...nonPosition("size", model, {
        vgChannel: "fontSize"
        // VL's text size is fontSize
      }),
      ...nonPosition("angle", model),
      ...valueIfDefined("align", align(model.markDef, encoding, config)),
      ...valueIfDefined("baseline", baseline2(model.markDef, encoding, config)),
      ...pointPosition("radius", model, { defaultPos: null }),
      ...pointPosition("theta", model, { defaultPos: null })
    };
  }
};
function align(markDef, encoding, config) {
  const a3 = getMarkPropOrConfig("align", markDef, config);
  if (a3 === void 0) {
    return "center";
  }
  return void 0;
}
function baseline2(markDef, encoding, config) {
  const b10 = getMarkPropOrConfig("baseline", markDef, config);
  if (b10 === void 0) {
    return "middle";
  }
  return void 0;
}
var tick = {
  vgMark: "rect",
  encodeEntry: (model) => {
    const { config, markDef } = model;
    const orient2 = markDef.orient;
    const vgSizeAxisChannel = orient2 === "horizontal" ? "x" : "y";
    const vgThicknessAxisChannel = orient2 === "horizontal" ? "y" : "x";
    const vgThicknessChannel = orient2 === "horizontal" ? "height" : "width";
    return {
      ...baseEncodeEntry(model, {
        align: "ignore",
        baseline: "ignore",
        color: "include",
        orient: "ignore",
        size: "ignore",
        theta: "ignore"
      }),
      ...rectPosition(model, vgSizeAxisChannel),
      ...pointPosition(vgThicknessAxisChannel, model, {
        defaultPos: "mid",
        vgChannel: vgThicknessAxisChannel === "y" ? "yc" : "xc"
      }),
      [vgThicknessChannel]: signalOrValueRef(getMarkPropOrConfig("thickness", markDef, config))
    };
  }
};
var markCompiler = {
  arc: arc4,
  area: area4,
  bar,
  circle,
  geoshape,
  image: image3,
  line: line4,
  point: point8,
  rect: rect3,
  rule: rule4,
  square,
  text: text3,
  tick,
  trail: trail3
};
function parseMarkGroups(model) {
  if (contains2([LINE, AREA, TRAIL], model.mark)) {
    const details = pathGroupingFields(model.mark, model.encoding);
    if (details.length > 0) {
      return getPathGroups(model, details);
    }
  } else if (model.mark === BAR) {
    const hasCornerRadius3 = VG_CORNERRADIUS_CHANNELS.some((prop) => getMarkPropOrConfig(prop, model.markDef, model.config));
    if (model.stack && !model.fieldDef("size") && hasCornerRadius3) {
      return getGroupsForStackedBarWithCornerRadius(model);
    }
  }
  return getMarkGroup(model);
}
var FACETED_PATH_PREFIX = "faceted_path_";
function getPathGroups(model, details) {
  return [
    {
      name: model.getName("pathgroup"),
      type: "group",
      from: {
        facet: {
          name: FACETED_PATH_PREFIX + model.requestDataName(DataSourceType.Main),
          data: model.requestDataName(DataSourceType.Main),
          groupby: details
        }
      },
      encode: {
        update: {
          width: { field: { group: "width" } },
          height: { field: { group: "height" } }
        }
      },
      // With subfacet for line/area group, need to use faceted data from above.
      marks: getMarkGroup(model, { fromPrefix: FACETED_PATH_PREFIX })
    }
  ];
}
var STACK_GROUP_PREFIX = "stack_group_";
function getGroupsForStackedBarWithCornerRadius(model) {
  const [mark] = getMarkGroup(model, { fromPrefix: STACK_GROUP_PREFIX });
  const fieldScale = model.scaleName(model.stack.fieldChannel);
  const stackField = (opt = {}) => model.vgField(model.stack.fieldChannel, opt);
  const stackFieldGroup = (func, expr2) => {
    const vgFieldMinMax = [
      stackField({ prefix: "min", suffix: "start", expr: expr2 }),
      stackField({ prefix: "max", suffix: "start", expr: expr2 }),
      stackField({ prefix: "min", suffix: "end", expr: expr2 }),
      stackField({ prefix: "max", suffix: "end", expr: expr2 })
    ];
    return `${func}(${vgFieldMinMax.map((field3) => `scale('${fieldScale}',${field3})`).join(",")})`;
  };
  let groupUpdate;
  let innerGroupUpdate;
  if (model.stack.fieldChannel === "x") {
    groupUpdate = {
      ...pick3(mark.encode.update, ["y", "yc", "y2", "height", ...VG_CORNERRADIUS_CHANNELS]),
      x: { signal: stackFieldGroup("min", "datum") },
      x2: { signal: stackFieldGroup("max", "datum") },
      clip: { value: true }
    };
    innerGroupUpdate = {
      x: { field: { group: "x" }, mult: -1 },
      height: { field: { group: "height" } }
    };
    mark.encode.update = {
      ...omit(mark.encode.update, ["y", "yc", "y2"]),
      height: { field: { group: "height" } }
    };
  } else {
    groupUpdate = {
      ...pick3(mark.encode.update, ["x", "xc", "x2", "width"]),
      y: { signal: stackFieldGroup("min", "datum") },
      y2: { signal: stackFieldGroup("max", "datum") },
      clip: { value: true }
    };
    innerGroupUpdate = {
      y: { field: { group: "y" }, mult: -1 },
      width: { field: { group: "width" } }
    };
    mark.encode.update = {
      ...omit(mark.encode.update, ["x", "xc", "x2"]),
      width: { field: { group: "width" } }
    };
  }
  for (const key2 of VG_CORNERRADIUS_CHANNELS) {
    const configValue = getMarkConfig(key2, model.markDef, model.config);
    if (mark.encode.update[key2]) {
      groupUpdate[key2] = mark.encode.update[key2];
      delete mark.encode.update[key2];
    } else if (configValue) {
      groupUpdate[key2] = signalOrValueRef(configValue);
    }
    if (configValue) {
      mark.encode.update[key2] = { value: 0 };
    }
  }
  const groupby = [];
  if (model.stack.groupbyChannels?.length > 0) {
    for (const groupbyChannel of model.stack.groupbyChannels) {
      const groupByField = model.fieldDef(groupbyChannel);
      const field3 = vgField(groupByField);
      if (field3) {
        groupby.push(field3);
      }
      if (groupByField?.bin || groupByField?.timeUnit) {
        groupby.push(vgField(groupByField, { binSuffix: "end" }));
      }
    }
  }
  const strokeProperties = [
    "stroke",
    "strokeWidth",
    "strokeJoin",
    "strokeCap",
    "strokeDash",
    "strokeDashOffset",
    "strokeMiterLimit",
    "strokeOpacity"
  ];
  groupUpdate = strokeProperties.reduce((encode3, prop) => {
    if (mark.encode.update[prop]) {
      return { ...encode3, [prop]: mark.encode.update[prop] };
    } else {
      const configValue = getMarkConfig(prop, model.markDef, model.config);
      if (configValue !== void 0) {
        return { ...encode3, [prop]: signalOrValueRef(configValue) };
      } else {
        return encode3;
      }
    }
  }, groupUpdate);
  if (groupUpdate.stroke) {
    groupUpdate.strokeForeground = { value: true };
    groupUpdate.strokeOffset = { value: 0 };
  }
  return [
    {
      type: "group",
      from: {
        facet: {
          data: model.requestDataName(DataSourceType.Main),
          name: STACK_GROUP_PREFIX + model.requestDataName(DataSourceType.Main),
          groupby,
          aggregate: {
            fields: [
              stackField({ suffix: "start" }),
              stackField({ suffix: "start" }),
              stackField({ suffix: "end" }),
              stackField({ suffix: "end" })
            ],
            ops: ["min", "max", "min", "max"]
          }
        }
      },
      encode: {
        update: groupUpdate
      },
      marks: [
        {
          type: "group",
          encode: { update: innerGroupUpdate },
          marks: [mark]
        }
      ]
    }
  ];
}
function getSort(model) {
  const { encoding, stack: stack2, mark, markDef, config } = model;
  const order = encoding.order;
  if (!isArray(order) && isValueDef(order) && isNullOrFalse(order.value) || !order && isNullOrFalse(getMarkPropOrConfig("order", markDef, config))) {
    return void 0;
  } else if ((isArray(order) || isFieldDef(order)) && !stack2) {
    return sortParams(order, { expr: "datum" });
  } else if (isPathMark(mark)) {
    const dimensionChannel = markDef.orient === "horizontal" ? "y" : "x";
    const dimensionChannelDef = encoding[dimensionChannel];
    if (isFieldDef(dimensionChannelDef)) {
      return { field: dimensionChannel };
    }
  }
  return void 0;
}
function getMarkGroup(model, opt = { fromPrefix: "" }) {
  const { mark, markDef, encoding, config } = model;
  const clip4 = getFirstDefined(markDef.clip, scaleClip(model), projectionClip(model));
  const style3 = getStyles(markDef);
  const key2 = encoding.key;
  const sort4 = getSort(model);
  const interactive2 = interactiveFlag(model);
  if (interactive2 && Object.values(model.component.selection).some((s) => s.type === "point" && !s.bind && // if on is a pointerover (hover) the pointer makes less sense since the mark is not clickable.
  s.on !== "pointerover")) {
    model.markDef.cursor ??= "pointer";
  }
  const aria2 = getMarkPropOrConfig("aria", markDef, config);
  const postEncodingTransform = markCompiler[mark].postEncodingTransform ? markCompiler[mark].postEncodingTransform(model) : null;
  return [
    {
      name: model.getName("marks"),
      type: markCompiler[mark].vgMark,
      ...clip4 ? { clip: clip4 } : {},
      ...style3 ? { style: style3 } : {},
      ...key2 ? { key: key2.field } : {},
      ...sort4 ? { sort: sort4 } : {},
      ...interactive2 ? interactive2 : {},
      ...aria2 === false ? { aria: aria2 } : {},
      from: { data: opt.fromPrefix + model.requestDataName(DataSourceType.Main) },
      encode: {
        update: markCompiler[mark].encodeEntry(model)
      },
      ...postEncodingTransform ? {
        transform: postEncodingTransform
      } : {}
    }
  ];
}
function scaleClip(model) {
  const xScale = model.getScaleComponent("x");
  const yScale = model.getScaleComponent("y");
  return xScale?.get("selectionExtent") || yScale?.get("selectionExtent") ? true : void 0;
}
function projectionClip(model) {
  const projection3 = model.component.projection;
  return projection3 && !projection3.isFit ? true : void 0;
}
function interactiveFlag(model) {
  if (!model.component.selection)
    return null;
  const unitCount = keys3(model.component.selection).length;
  let parentCount = unitCount;
  let parent = model.parent;
  while (parent && parentCount === 0) {
    parentCount = keys3(parent.component.selection).length;
    parent = parent.parent;
  }
  return parentCount ? {
    interactive: unitCount > 0 || model.mark === "geoshape" || !!model.encoding.tooltip || !!model.markDef.tooltip
  } : null;
}
var UnitModel = class extends ModelWithField {
  markDef;
  encoding;
  specifiedScales = {};
  stack;
  specifiedAxes = {};
  specifiedLegends = {};
  specifiedProjection = {};
  selection = [];
  children = [];
  constructor(spec, parent, parentGivenName, parentGivenSize = {}, config) {
    super(spec, "unit", parent, parentGivenName, config, void 0, isFrameMixins(spec) ? spec.view : void 0);
    const markDef = isMarkDef(spec.mark) ? { ...spec.mark } : { type: spec.mark };
    const mark = markDef.type;
    if (markDef.filled === void 0) {
      markDef.filled = defaultFilled(markDef, config, {
        graticule: spec.data && isGraticuleGenerator(spec.data)
      });
    }
    const encoding = this.encoding = initEncoding(spec.encoding || {}, mark, markDef.filled, config);
    this.markDef = initMarkdef(markDef, encoding, config);
    this.size = initLayoutSize({
      encoding,
      size: isFrameMixins(spec) ? {
        ...parentGivenSize,
        ...spec.width !== void 0 ? { width: spec.width } : {},
        ...spec.height !== void 0 ? { height: spec.height } : {}
      } : parentGivenSize
    });
    this.stack = stack(this.markDef, encoding);
    this.specifiedScales = this.initScales(mark, encoding);
    this.specifiedAxes = this.initAxes(encoding);
    this.specifiedLegends = this.initLegends(encoding);
    this.specifiedProjection = spec.projection;
    this.selection = (spec.params ?? []).filter((p) => isSelectionParameter(p));
    this.alignStackOrderWithColorDomain();
  }
  get hasProjection() {
    const { encoding } = this;
    const isGeoShapeMark = this.mark === GEOSHAPE;
    const hasGeoPosition = encoding && GEOPOSITION_CHANNELS.some((channel) => isFieldOrDatumDef(encoding[channel]));
    return isGeoShapeMark || hasGeoPosition;
  }
  /**
   * Return specified Vega-Lite scale domain for a particular channel
   * @param channel
   */
  scaleDomain(channel) {
    const scale9 = this.specifiedScales[channel];
    return scale9 ? scale9.domain : void 0;
  }
  axis(channel) {
    return this.specifiedAxes[channel];
  }
  legend(channel) {
    return this.specifiedLegends[channel];
  }
  initScales(mark, encoding) {
    return SCALE_CHANNELS.reduce((scales2, channel) => {
      const fieldOrDatumDef = getFieldOrDatumDef(encoding[channel]);
      if (fieldOrDatumDef) {
        scales2[channel] = this.initScale(fieldOrDatumDef.scale ?? {});
      }
      return scales2;
    }, {});
  }
  initScale(scale9) {
    const { domain: domain4, range: range7 } = scale9;
    const scaleInternal = replaceExprRef(scale9);
    if (isArray(domain4)) {
      scaleInternal.domain = domain4.map(signalRefOrValue);
    }
    if (isArray(range7)) {
      scaleInternal.range = range7.map(signalRefOrValue);
    }
    return scaleInternal;
  }
  initAxes(encoding) {
    return POSITION_SCALE_CHANNELS.reduce((_axis, channel) => {
      const channelDef = encoding[channel];
      if (isFieldOrDatumDef(channelDef) || channel === X3 && isFieldOrDatumDef(encoding.x2) || channel === Y3 && isFieldOrDatumDef(encoding.y2)) {
        const axisSpec = isFieldOrDatumDef(channelDef) ? channelDef.axis : void 0;
        _axis[channel] = axisSpec ? this.initAxis({ ...axisSpec }) : axisSpec;
      }
      return _axis;
    }, {});
  }
  initAxis(axis) {
    const props = keys3(axis);
    const axisInternal = {};
    for (const prop of props) {
      const val = axis[prop];
      axisInternal[prop] = isConditionalAxisValue(val) ? signalOrValueRefWithCondition(val) : signalRefOrValue(val);
    }
    return axisInternal;
  }
  initLegends(encoding) {
    return NONPOSITION_SCALE_CHANNELS.reduce((_legend, channel) => {
      const fieldOrDatumDef = getFieldOrDatumDef(encoding[channel]);
      if (fieldOrDatumDef && supportLegend(channel)) {
        const legend = fieldOrDatumDef.legend;
        _legend[channel] = legend ? replaceExprRef(legend) : legend;
      }
      return _legend;
    }, {});
  }
  /**
   * If this unit lacks order encoding but does contain a color domain
   * add transform and encoding that aligns the stack order with the color domain.
   */
  alignStackOrderWithColorDomain() {
    const { color: color6, fill: fill3, order, xOffset, yOffset } = this.encoding;
    const colorField = fill3 || color6;
    const colorEncoding = isFieldDef(colorField) ? colorField : void 0;
    const field3 = colorEncoding?.field;
    const scale9 = colorEncoding?.scale;
    const colorEncodingType = colorEncoding?.type;
    const domain4 = scale9?.domain;
    const offset6 = xOffset || yOffset;
    const offsetEncoding = isFieldDef(offset6) ? offset6 : void 0;
    const orderFieldName = `_${field3}_sort_index`;
    if (!order && Array.isArray(domain4) && typeof field3 === "string" && colorEncodingType === "nominal") {
      if (offsetEncoding && !offsetEncoding.sort) {
        offsetEncoding.sort = domain4;
      } else {
        if (!this.stack) {
          return;
        }
        const orderExpression = `indexof(${stringValue(domain4)}, datum['${field3}'])`;
        const sort4 = this.markDef?.orient === "horizontal" ? "ascending" : "descending";
        this.transforms.push({ calculate: orderExpression, as: orderFieldName });
        this.encoding.order = { field: orderFieldName, type: "quantitative", sort: sort4 };
      }
    }
  }
  parseData() {
    this.component.data = parseData2(this);
  }
  parseLayoutSize() {
    parseUnitLayoutSize(this);
  }
  parseSelections() {
    this.component.selection = parseUnitSelection(this, this.selection);
  }
  parseMarkGroup() {
    this.component.mark = parseMarkGroups(this);
  }
  parseAxesAndHeaders() {
    this.component.axes = parseUnitAxes(this);
  }
  assembleSelectionTopLevelSignals(signals) {
    return assembleTopLevelSignals(this, signals);
  }
  assembleSignals() {
    return [...assembleAxisSignals(this), ...assembleUnitSelectionSignals(this, [])];
  }
  assembleSelectionData(data4) {
    return assembleUnitSelectionData(this, data4);
  }
  assembleLayout() {
    return null;
  }
  assembleLayoutSignals() {
    return assembleLayoutSignals(this);
  }
  /**
   * Corrects the data references in marks after assemble.
   */
  correctDataNames = (mark) => {
    if (mark.from?.data) {
      mark.from.data = this.lookupDataSource(mark.from.data);
      if ("time" in this.encoding) {
        mark.from.data = mark.from.data + CURR;
      }
    }
    if (mark.from?.facet?.data) {
      mark.from.facet.data = this.lookupDataSource(mark.from.facet.data);
    }
    return mark;
  };
  assembleMarks() {
    let marks = this.component.mark ?? [];
    if (!this.parent || !isLayerModel(this.parent)) {
      marks = assembleUnitSelectionMarks(this, marks);
    }
    return marks.map(this.correctDataNames);
  }
  assembleGroupStyle() {
    const { style: style3 } = this.view || {};
    if (style3 !== void 0) {
      return style3;
    }
    if (this.encoding.x || this.encoding.y) {
      return "cell";
    } else {
      return "view";
    }
  }
  getMapping() {
    return this.encoding;
  }
  get mark() {
    return this.markDef.type;
  }
  channelHasField(channel) {
    return channelHasField(this.encoding, channel);
  }
  fieldDef(channel) {
    const channelDef = this.encoding[channel];
    return getFieldDef(channelDef);
  }
  typedFieldDef(channel) {
    const fieldDef = this.fieldDef(channel);
    if (isTypedFieldDef(fieldDef)) {
      return fieldDef;
    }
    return null;
  }
};
var LayerModel = class _LayerModel extends Model {
  // HACK: This should be (LayerModel | UnitModel)[], but setting the correct type leads to weird error.
  // So I'm just putting generic Model for now
  children;
  constructor(spec, parent, parentGivenName, parentGivenSize, config) {
    super(spec, "layer", parent, parentGivenName, config, spec.resolve, spec.view);
    const layoutSize = {
      ...parentGivenSize,
      ...spec.width ? { width: spec.width } : {},
      ...spec.height ? { height: spec.height } : {}
    };
    this.children = spec.layer.map((layer, i) => {
      if (isLayerSpec(layer)) {
        return new _LayerModel(layer, this, this.getName(`layer_${i}`), layoutSize, config);
      } else if (isUnitSpec(layer)) {
        return new UnitModel(layer, this, this.getName(`layer_${i}`), layoutSize, config);
      }
      throw new Error(invalidSpec(layer));
    });
  }
  parseData() {
    this.component.data = parseData2(this);
    for (const child of this.children) {
      child.parseData();
    }
  }
  parseLayoutSize() {
    parseLayerLayoutSize(this);
  }
  parseSelections() {
    this.component.selection = {};
    for (const child of this.children) {
      child.parseSelections();
      for (const key2 of keys3(child.component.selection)) {
        this.component.selection[key2] = child.component.selection[key2];
      }
    }
    if (vals(this.component.selection).some((selCmpt) => isTimerSelection(selCmpt))) {
      error2(MULTI_VIEW_ANIMATION_UNSUPPORTED);
    }
  }
  parseMarkGroup() {
    for (const child of this.children) {
      child.parseMarkGroup();
    }
  }
  parseAxesAndHeaders() {
    parseLayerAxes(this);
  }
  assembleSelectionTopLevelSignals(signals) {
    return this.children.reduce((sg, child) => child.assembleSelectionTopLevelSignals(sg), signals);
  }
  // TODO: Support same named selections across children.
  assembleSignals() {
    return this.children.reduce((signals, child) => {
      return signals.concat(child.assembleSignals());
    }, assembleAxisSignals(this));
  }
  assembleLayoutSignals() {
    return this.children.reduce((signals, child) => {
      return signals.concat(child.assembleLayoutSignals());
    }, assembleLayoutSignals(this));
  }
  assembleSelectionData(data4) {
    return this.children.reduce((db, child) => child.assembleSelectionData(db), data4);
  }
  assembleGroupStyle() {
    const uniqueStyles = /* @__PURE__ */ new Set();
    for (const child of this.children) {
      for (const style3 of array_default(child.assembleGroupStyle())) {
        uniqueStyles.add(style3);
      }
    }
    const styles = Array.from(uniqueStyles);
    return styles.length > 1 ? styles : styles.length === 1 ? styles[0] : void 0;
  }
  assembleTitle() {
    let title2 = super.assembleTitle();
    if (title2) {
      return title2;
    }
    for (const child of this.children) {
      title2 = child.assembleTitle();
      if (title2) {
        return title2;
      }
    }
    return void 0;
  }
  assembleLayout() {
    return null;
  }
  assembleMarks() {
    return assembleLayerSelectionMarks(this, this.children.flatMap((child) => {
      return child.assembleMarks();
    }));
  }
  assembleLegends() {
    return this.children.reduce((legends, child) => {
      return legends.concat(child.assembleLegends());
    }, assembleLegends(this));
  }
};
function buildModel(spec, parent, parentGivenName, unitSize, config) {
  if (isFacetSpec(spec)) {
    return new FacetModel(spec, parent, parentGivenName, config);
  } else if (isLayerSpec(spec)) {
    return new LayerModel(spec, parent, parentGivenName, unitSize, config);
  } else if (isUnitSpec(spec)) {
    return new UnitModel(spec, parent, parentGivenName, unitSize, config);
  } else if (isAnyConcatSpec(spec)) {
    return new ConcatModel(spec, parent, parentGivenName, config);
  }
  throw new Error(invalidSpec(spec));
}
function compile(inputSpec, opt = {}) {
  if (opt.logger) {
    set6(opt.logger);
  }
  if (opt.fieldTitle) {
    setTitleFormatter(opt.fieldTitle);
  }
  try {
    const config = initConfig(mergeConfig(opt.config, inputSpec.config));
    const spec = normalize3(inputSpec, config);
    const model = buildModel(spec, null, "", void 0, config);
    model.parse();
    optimizeDataflow(model.component.data, model);
    const vgSpec = assembleTopLevelModel(model, getTopLevelProperties(inputSpec, spec.autosize, config, model), inputSpec.datasets, inputSpec.usermeta);
    return {
      spec: vgSpec,
      normalized: spec
    };
  } finally {
    if (opt.logger) {
      reset2();
    }
    if (opt.fieldTitle) {
      resetTitleFormatter();
    }
  }
}
function getTopLevelProperties(inputSpec, autosize, config, model) {
  const width2 = model.component.layoutSize.get("width");
  const height2 = model.component.layoutSize.get("height");
  if (autosize === void 0) {
    autosize = { type: "pad" };
    if (model.hasAxisOrientSignalRef()) {
      autosize.resize = true;
    }
  } else if (isString(autosize)) {
    autosize = { type: autosize };
  }
  if (width2 && height2 && isFitType(autosize.type)) {
    if (width2 === "step" && height2 === "step") {
      warn3(droppingFit());
      autosize.type = "pad";
    } else if (width2 === "step" || height2 === "step") {
      const sizeType = width2 === "step" ? "width" : "height";
      warn3(droppingFit(getPositionScaleChannel(sizeType)));
      const inverseSizeType = sizeType === "width" ? "height" : "width";
      autosize.type = getFitType(inverseSizeType);
    }
  }
  return {
    ...keys3(autosize).length === 1 && autosize.type ? autosize.type === "pad" ? {} : { autosize: autosize.type } : { autosize },
    ...extractTopLevelProperties(config, false),
    ...extractTopLevelProperties(inputSpec, true)
  };
}
function assembleTopLevelModel(model, topLevelProperties, datasets = {}, usermeta) {
  const vgConfig = model.config ? stripAndRedirectConfig(model.config) : void 0;
  const rootData = assembleRootData(model.component.data, datasets);
  const data4 = model.assembleSelectionData(rootData);
  const projections2 = model.assembleProjections();
  const title2 = model.assembleTitle();
  const style3 = model.assembleGroupStyle();
  const encodeEntry2 = model.assembleGroupEncodeEntry(true);
  let layoutSignals = model.assembleLayoutSignals();
  layoutSignals = layoutSignals.filter((signal) => {
    if ((signal.name === "width" || signal.name === "height") && signal.value !== void 0) {
      topLevelProperties[signal.name] = +signal.value;
      return false;
    }
    return true;
  });
  const { params: params2, ...otherTopLevelProps } = topLevelProperties;
  return {
    $schema: "https://vega.github.io/schema/vega/v6.json",
    ...model.description ? { description: model.description } : {},
    ...otherTopLevelProps,
    ...title2 ? { title: title2 } : {},
    ...style3 ? { style: style3 } : {},
    ...encodeEntry2 ? { encode: { update: encodeEntry2 } } : {},
    data: data4,
    ...projections2.length > 0 ? { projections: projections2 } : {},
    ...model.assembleGroup([
      ...layoutSignals,
      ...model.assembleSelectionTopLevelSignals([]),
      ...assembleParameterSignals(params2)
    ]),
    ...vgConfig ? { config: vgConfig } : {},
    ...usermeta ? { usermeta } : {}
  };
}
var version = pkg.version;

// ../deepseek-harness/node_modules/.pnpm/vega-interpreter@2.3.1/node_modules/vega-interpreter/build/vega-interpreter.js
function adjustSpatial2(item, encode3, swap3) {
  let t;
  if (encode3.x2) {
    if (encode3.x) {
      if (swap3 && item.x > item.x2) {
        t = item.x;
        item.x = item.x2;
        item.x2 = t;
      }
      item.width = item.x2 - item.x;
    } else {
      item.x = item.x2 - (item.width || 0);
    }
  }
  if (encode3.xc) {
    item.x = item.xc - (item.width || 0) / 2;
  }
  if (encode3.y2) {
    if (encode3.y) {
      if (swap3 && item.y > item.y2) {
        t = item.y;
        item.y = item.y2;
        item.y2 = t;
      }
      item.height = item.y2 - item.y;
    } else {
      item.y = item.y2 - (item.height || 0);
    }
  }
  if (encode3.yc) {
    item.y = item.yc - (item.height || 0) / 2;
  }
}
var Constants2 = {
  NaN: NaN,
  E: Math.E,
  LN2: Math.LN2,
  LN10: Math.LN10,
  LOG2E: Math.LOG2E,
  LOG10E: Math.LOG10E,
  PI: Math.PI,
  SQRT1_2: Math.SQRT1_2,
  SQRT2: Math.SQRT2,
  MIN_VALUE: Number.MIN_VALUE,
  MAX_VALUE: Number.MAX_VALUE
};
var Ops = {
  "*": (a3, b10) => a3 * b10,
  "+": (a3, b10) => a3 + b10,
  "-": (a3, b10) => a3 - b10,
  "/": (a3, b10) => a3 / b10,
  "%": (a3, b10) => a3 % b10,
  ">": (a3, b10) => a3 > b10,
  "<": (a3, b10) => a3 < b10,
  "<=": (a3, b10) => a3 <= b10,
  ">=": (a3, b10) => a3 >= b10,
  "==": (a3, b10) => a3 == b10,
  "!=": (a3, b10) => a3 != b10,
  "===": (a3, b10) => a3 === b10,
  "!==": (a3, b10) => a3 !== b10,
  "&": (a3, b10) => a3 & b10,
  "|": (a3, b10) => a3 | b10,
  "^": (a3, b10) => a3 ^ b10,
  "<<": (a3, b10) => a3 << b10,
  ">>": (a3, b10) => a3 >> b10,
  ">>>": (a3, b10) => a3 >>> b10
};
var Unary = {
  "+": (a3) => +a3,
  "-": (a3) => -a3,
  "~": (a3) => ~a3,
  "!": (a3) => !a3
};
var slice5 = Array.prototype.slice;
var apply2 = (m3, args, cast) => {
  const obj = cast ? cast(args[0]) : args[0];
  return obj[m3].apply(obj, slice5.call(args, 1));
};
var datetime = (yearOrTimestring, m3 = 0, d = 1, H = 0, M2 = 0, S = 0, ms = 0) => isString(yearOrTimestring) ? new Date(yearOrTimestring) : new Date(yearOrTimestring, m3, d, H, M2, S, ms);
var Functions2 = {
  // math functions
  isNaN: Number.isNaN,
  isFinite: Number.isFinite,
  abs: Math.abs,
  acos: Math.acos,
  asin: Math.asin,
  atan: Math.atan,
  atan2: Math.atan2,
  ceil: Math.ceil,
  cos: Math.cos,
  exp: Math.exp,
  floor: Math.floor,
  log: Math.log,
  max: Math.max,
  min: Math.min,
  pow: Math.pow,
  random: Math.random,
  round: Math.round,
  sin: Math.sin,
  sqrt: Math.sqrt,
  tan: Math.tan,
  clamp: (a3, b10, c3) => Math.max(b10, Math.min(c3, a3)),
  // date functions
  now: Date.now,
  utc: Date.UTC,
  datetime,
  date: (d) => new Date(d).getDate(),
  day: (d) => new Date(d).getDay(),
  year: (d) => new Date(d).getFullYear(),
  month: (d) => new Date(d).getMonth(),
  hours: (d) => new Date(d).getHours(),
  minutes: (d) => new Date(d).getMinutes(),
  seconds: (d) => new Date(d).getSeconds(),
  milliseconds: (d) => new Date(d).getMilliseconds(),
  time: (d) => new Date(d).getTime(),
  timezoneoffset: (d) => new Date(d).getTimezoneOffset(),
  utcdate: (d) => new Date(d).getUTCDate(),
  utcday: (d) => new Date(d).getUTCDay(),
  utcyear: (d) => new Date(d).getUTCFullYear(),
  utcmonth: (d) => new Date(d).getUTCMonth(),
  utchours: (d) => new Date(d).getUTCHours(),
  utcminutes: (d) => new Date(d).getUTCMinutes(),
  utcseconds: (d) => new Date(d).getUTCSeconds(),
  utcmilliseconds: (d) => new Date(d).getUTCMilliseconds(),
  // sequence functions
  length: (x6) => x6.length,
  join: function() {
    return apply2("join", arguments);
  },
  indexof: function() {
    return apply2("indexOf", arguments);
  },
  lastindexof: function() {
    return apply2("lastIndexOf", arguments);
  },
  slice: function() {
    return apply2("slice", arguments);
  },
  reverse: (x6) => x6.slice().reverse(),
  sort: (x6) => x6.slice().sort(ascending),
  // string functions
  parseFloat,
  parseInt,
  upper: (x6) => String(x6).toUpperCase(),
  lower: (x6) => String(x6).toLowerCase(),
  substring: function() {
    return apply2("substring", arguments, String);
  },
  split: function() {
    return apply2("split", arguments, String);
  },
  replace: function() {
    return apply2("replace", arguments, String);
  },
  trim: (x6) => String(x6).trim(),
  // Base64 encode/decode
  // Convert binary string to base64-encoded ascii
  btoa: (x6) => btoa(x6),
  // Convert base64-encoded ascii to binary string
  atob: (x6) => atob(x6),
  // URI encoding
  encodeURIComponent: (x6) => encodeURIComponent(x6),
  // regexp functions
  regexp: RegExp,
  test: (r, t) => RegExp(r).test(t)
};
var EventFunctions = ["view", "item", "group", "xy", "x", "y"];
var DisallowedMethods = /* @__PURE__ */ new Set([Function, eval, setTimeout, setInterval]);
if (typeof setImmediate === "function") DisallowedMethods.add(setImmediate);
var Visitors = {
  Literal: ($, n) => n.value,
  Identifier: ($, n) => {
    const id2 = n.name;
    return $.memberDepth > 0 ? id2 : id2 === "datum" ? $.datum : id2 === "event" ? $.event : id2 === "item" ? $.item : Constants2[id2] || $.params["$" + id2];
  },
  MemberExpression: ($, n) => {
    const d = !n.computed, o = $(n.object);
    if (d) $.memberDepth += 1;
    const p = $(n.property);
    if (d) $.memberDepth -= 1;
    if (DisallowedMethods.has(o[p])) {
      console.error(`Prevented interpretation of member "${p}" which could lead to insecure code execution`);
      return;
    }
    return o[p];
  },
  CallExpression: ($, n) => {
    const args = n.arguments;
    let name = n.callee.name;
    if (name.startsWith("_")) {
      name = name.slice(1);
    }
    return name === "if" ? $(args[0]) ? $(args[1]) : $(args[2]) : ($.fn[name] || Functions2[name]).apply($.fn, args.map($));
  },
  ArrayExpression: ($, n) => n.elements.map($),
  BinaryExpression: ($, n) => Ops[n.operator]($(n.left), $(n.right)),
  UnaryExpression: ($, n) => Unary[n.operator]($(n.argument)),
  ConditionalExpression: ($, n) => $(n.test) ? $(n.consequent) : $(n.alternate),
  LogicalExpression: ($, n) => n.operator === "&&" ? $(n.left) && $(n.right) : $(n.left) || $(n.right),
  ObjectExpression: ($, n) => n.properties.reduce((o, p) => {
    $.memberDepth += 1;
    const k = $(p.key);
    $.memberDepth -= 1;
    const v2 = $(p.value);
    if (DisallowedObjectProperties.has(k)) {
      console.error(`Prevented interpretation of property "${k}" which could lead to insecure code execution`);
    } else if (DisallowedMethods.has(v2)) {
      console.error(`Prevented interpretation of method "${k}" which could lead to insecure code execution`);
    } else {
      o[k] = v2;
    }
    return o;
  }, {})
};
function interpret(ast, fn, params2, datum3, event2, item) {
  const $ = (n) => Visitors[n.type]($, n);
  $.memberDepth = 0;
  $.fn = Object.create(fn);
  $.params = params2;
  $.datum = datum3;
  $.event = event2;
  $.item = item;
  EventFunctions.forEach((f) => $.fn[f] = (...args) => event2.vega[f](...args));
  return $(ast);
}
var expression3 = {
  /**
   * Parse an expression used to update an operator value.
   */
  operator(ctx, expr2) {
    const ast = expr2.ast, fn = ctx.functions;
    return (_) => interpret(ast, fn, _);
  },
  /**
   * Parse an expression provided as an operator parameter value.
   */
  parameter(ctx, expr2) {
    const ast = expr2.ast, fn = ctx.functions;
    return (datum3, _) => interpret(ast, fn, _, datum3);
  },
  /**
   * Parse an expression applied to an event stream.
   */
  event(ctx, expr2) {
    const ast = expr2.ast, fn = ctx.functions;
    return (event2) => interpret(ast, fn, void 0, void 0, event2);
  },
  /**
   * Parse an expression used to handle an event-driven operator update.
   */
  handler(ctx, expr2) {
    const ast = expr2.ast, fn = ctx.functions;
    return (_, event2) => {
      const datum3 = event2.item && event2.item.datum;
      return interpret(ast, fn, _, datum3, event2);
    };
  },
  /**
   * Parse an expression that performs visual encoding.
   */
  encode(ctx, encode3) {
    const {
      marktype,
      channels
    } = encode3, fn = ctx.functions, swap3 = marktype === "group" || marktype === "image" || marktype === "rect";
    return (item, _) => {
      const datum3 = item.datum;
      let m3 = 0, v2;
      for (const name in channels) {
        v2 = interpret(channels[name].ast, fn, _, datum3, void 0, item);
        if (item[name] !== v2) {
          item[name] = v2;
          m3 = 1;
        }
      }
      if (marktype !== "rule") {
        adjustSpatial2(item, channels, swap3);
      }
      return m3;
    };
  }
};

// src/limits.ts
var LIMITS = Object.freeze({
  sourceBytes: 64 * 1024,
  mermaidLines: 500,
  mermaidStatements: 500,
  mermaidEdges: 500,
  jsonDepth: 32,
  jsonNodes: 2e4,
  dataRows: 5e3,
  dataCells: 5e4,
  stringBytes: 8 * 1024,
  views: 32,
  transforms: 32,
  estimatedMarks: 2e4,
  dimension: 1e3,
  area: 1e6,
  compiledBytes: 1024 * 1024,
  compiledMarks: 256,
  compiledData: 128,
  compiledSignals: 256,
  compiledTransforms: 1024,
  svgBytes: 2 * 1024 * 1024,
  svgElements: 2e4,
  svgAttributes: 64,
  queuePending: 4,
  workerTimeoutMs: 2e3
});
var encoder2 = new TextEncoder();
function utf8Bytes(value4) {
  return encoder2.encode(value4).byteLength;
}
function assertByteLimit(value4, limit, subject) {
  if (utf8Bytes(value4) > limit) throw new Error(`${subject} exceeds ${limit} UTF-8 bytes`);
}

// src/vega-policy.ts
var OBJECT = Object.prototype;
var VIEW_KEYS = /* @__PURE__ */ new Set(["layer", "concat", "hconcat", "vconcat"]);
var SOURCE_FORBIDDEN_KEYS = /* @__PURE__ */ new Set([
  "url",
  "href",
  "src",
  "image",
  "on",
  "bind",
  "selection",
  "select",
  "params",
  "usermeta",
  "autosize",
  "datasets",
  "__proto__",
  "prototype",
  "constructor"
]);
var ALLOWED_MARKS = /* @__PURE__ */ new Set(["arc", "area", "bar", "line", "point", "rect", "rule", "text", "tick", "trail"]);
var EXPRESSION_KEYS = /* @__PURE__ */ new Set(["expr", "expression", "calculate", "test"]);
var ALLOWED_TRANSFORMS = /* @__PURE__ */ new Set([
  "aggregate",
  "bin",
  "joinaggregate",
  "sample",
  "stack",
  "timeunit",
  "window"
]);
function validateVegaLiteSpec(input) {
  if (!isRecord(input)) throw new Error("Vega-Lite spec must be a plain object");
  const source4 = JSON.stringify(input);
  assertByteLimit(source4, LIMITS.sourceBytes, "Vega-Lite spec");
  const state = { nodes: 0, views: 0, transforms: 0, rows: 0, cells: 0 };
  visitSource(input, 0, state, void 0);
  if (state.views > LIMITS.views) throw new Error(`Vega-Lite spec exceeds ${LIMITS.views} views`);
  if (state.transforms > LIMITS.transforms) throw new Error(`Vega-Lite spec exceeds ${LIMITS.transforms} transforms`);
  const estimatedMarks = Math.max(1, state.views) * Math.max(1, state.rows);
  if (estimatedMarks > LIMITS.estimatedMarks) {
    throw new Error(`Vega-Lite spec exceeds ${LIMITS.estimatedMarks} estimated marks`);
  }
  return JSON.parse(source4);
}
function validateCompiledVega(input) {
  if (!isRecord(input)) throw new Error("Compiled Vega must be a plain object");
  const source4 = JSON.stringify(input);
  assertByteLimit(source4, LIMITS.compiledBytes, "Compiled Vega");
  assertArrayLimit(input.marks, LIMITS.compiledMarks, "marks");
  assertArrayLimit(input.data, LIMITS.compiledData, "data");
  assertArrayLimit(input.signals, LIMITS.compiledSignals, "signals");
  let marks = 0;
  let data4 = 0;
  let signals = 0;
  let transforms2 = 0;
  walkJson(input, 0, (key2, value4) => {
    if (key2 === "url" || key2 === "href") throw new Error(`Compiled Vega ${key2} is not allowed`);
    if (key2 === "on" || key2 === "bind") throw new Error(`Compiled Vega interaction ${key2} is not allowed`);
    if (key2 === "marks" && Array.isArray(value4)) marks += value4.length;
    if (key2 === "data" && Array.isArray(value4)) data4 += value4.length;
    if (key2 === "signals" && Array.isArray(value4)) signals += value4.length;
    if (key2 === "transform" && Array.isArray(value4)) transforms2 += value4.length;
  });
  if (marks > LIMITS.compiledMarks) throw new Error(`Compiled Vega marks exceeds ${LIMITS.compiledMarks}`);
  if (data4 > LIMITS.compiledData) throw new Error(`Compiled Vega data exceeds ${LIMITS.compiledData}`);
  if (signals > LIMITS.compiledSignals) throw new Error(`Compiled Vega signals exceeds ${LIMITS.compiledSignals}`);
  if (transforms2 > LIMITS.compiledTransforms) {
    throw new Error(`Compiled Vega exceeds ${LIMITS.compiledTransforms} transforms`);
  }
}
function visitSource(value4, depth, state, parentKey) {
  if (depth > LIMITS.jsonDepth) throw new Error(`Vega-Lite spec exceeds depth ${LIMITS.jsonDepth}`);
  state.nodes += 1;
  if (state.nodes > LIMITS.jsonNodes) throw new Error(`Vega-Lite spec exceeds ${LIMITS.jsonNodes} JSON nodes`);
  if (typeof value4 === "string") {
    if (utf8Bytes(value4) > LIMITS.stringBytes) throw new Error(`Vega-Lite string exceeds ${LIMITS.stringBytes} UTF-8 bytes`);
    if (/[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:^|[^\uD800-\uDBFF])[\uDC00-\uDFFF]/u.test(value4)) {
      throw new Error("Vega-Lite strings must contain paired UTF-16 surrogates");
    }
    if (/(?:https?:|file:|data:|blob:|\/\/)/i.test(value4)) throw new Error("Vega-Lite external URLs are not allowed");
    if (parentKey === "mark" && !ALLOWED_MARKS.has(value4.toLowerCase())) throw new Error(`Vega-Lite mark ${value4} is not allowed`);
    return;
  }
  if (typeof value4 === "number" && !Number.isFinite(value4)) throw new Error("Vega-Lite numbers must be finite");
  if (value4 === null || typeof value4 === "number" || typeof value4 === "boolean") return;
  if (Array.isArray(value4)) {
    if (parentKey === "values") {
      state.rows += value4.length;
      if (state.rows > LIMITS.dataRows) throw new Error(`Vega-Lite data exceeds ${LIMITS.dataRows} rows`);
      for (const row of value4) {
        if (!isRecord(row)) throw new Error("Vega-Lite rows must be plain objects");
        state.cells += Object.keys(row).length;
        if (state.cells > LIMITS.dataCells) throw new Error(`Vega-Lite data exceeds ${LIMITS.dataCells} cells`);
        for (const cell2 of Object.values(row)) {
          if (!isScalar(cell2)) throw new Error("Vega-Lite cells must be scalar JSON values");
        }
      }
    }
    for (const item of value4) visitSource(item, depth + 1, state, parentKey);
    return;
  }
  if (!isRecord(value4)) throw new Error("Vega-Lite spec contains a non-JSON value");
  validateDimensions(value4);
  if (parentKey === "data" && Object.keys(value4).some((key2) => key2 !== "values")) {
    throw new Error("Vega-Lite data must contain only inline values");
  }
  if ("mark" in value4 || VIEW_KEYS.has(parentKey ?? "")) state.views += 1;
  if (isRecord(value4.mark)) {
    if (typeof value4.mark.type !== "string" || !ALLOWED_MARKS.has(value4.mark.type.toLowerCase())) {
      throw new Error("Vega-Lite mark type is not allowed");
    }
  }
  for (const [key2, child] of Object.entries(value4)) {
    if (utf8Bytes(key2) > LIMITS.stringBytes) throw new Error(`Vega-Lite key exceeds ${LIMITS.stringBytes} UTF-8 bytes`);
    const lowered = key2.toLowerCase();
    if (SOURCE_FORBIDDEN_KEYS.has(lowered) || EXPRESSION_KEYS.has(lowered)) {
      throw new Error(`Vega-Lite property ${key2} is not allowed`);
    }
    if (key2 === "filter" && typeof child === "string") throw new Error("Vega-Lite expression filters are not allowed");
    if (key2 === "transform") {
      if (!Array.isArray(child)) throw new Error("Vega-Lite transform must be an array");
      state.transforms += child.length;
      for (const transform4 of child) validateTransform(transform4);
    }
    visitSource(child, depth + 1, state, key2);
  }
}
function validateTransform(value4) {
  if (!isRecord(value4)) throw new Error("Vega-Lite transform must be a plain object");
  const kinds = Object.keys(value4).filter((key2) => ALLOWED_TRANSFORMS.has(key2));
  if (kinds.length !== 1) throw new Error("Vega-Lite transform type is not allowed");
  if ("from" in value4 && isRecord(value4.from) && "data" in value4.from) {
    throw new Error("Vega-Lite lookup from named or remote data is not allowed");
  }
}
function validateDimensions(spec) {
  const width2 = readDimension(spec.width, "width");
  const height2 = readDimension(spec.height, "height");
  if (width2 !== void 0 && height2 !== void 0 && width2 * height2 > LIMITS.area) {
    throw new Error(`Vega-Lite dimensions exceed area ${LIMITS.area}`);
  }
}
function readDimension(value4, name) {
  if (value4 === void 0) return void 0;
  if (typeof value4 !== "number" || !Number.isFinite(value4) || value4 <= 0 || value4 > LIMITS.dimension) {
    throw new Error(`Vega-Lite ${name} must be between 0 and ${LIMITS.dimension}`);
  }
  return value4;
}
function assertArrayLimit(value4, limit, subject) {
  if (value4 !== void 0 && (!Array.isArray(value4) || value4.length > limit)) {
    throw new Error(`Compiled Vega ${subject} exceeds ${limit}`);
  }
}
function walkJson(value4, depth, visit3) {
  if (depth > LIMITS.jsonDepth) throw new Error(`Compiled Vega exceeds depth ${LIMITS.jsonDepth}`);
  if (Array.isArray(value4)) {
    for (const item of value4) walkJson(item, depth + 1, visit3);
    return;
  }
  if (!isRecord(value4)) return;
  for (const [key2, child] of Object.entries(value4)) {
    visit3(key2, child);
    walkJson(child, depth + 1, visit3);
  }
}
function isScalar(value4) {
  return value4 === null || typeof value4 === "boolean" || typeof value4 === "string" || typeof value4 === "number" && Number.isFinite(value4);
}
function isRecord(value4) {
  if (value4 === null || typeof value4 !== "object" || Array.isArray(value4)) return false;
  const prototype2 = Object.getPrototypeOf(value4);
  return prototype2 === OBJECT || prototype2 === null;
}

// src/worker-protocol.ts
function isVegaWorkerRequest(value4) {
  if (!isRecord2(value4) || value4.kind !== "render" || typeof value4.id !== "string" || !isRecord2(value4.spec)) return false;
  return isPalette(value4.palette);
}
function isPalette(value4) {
  return isRecord2(value4) && Object.keys(value4).length === 8 && ["foreground", "muted", "accent", "border", "background", "surface", "line", "fontFamily"].every((key2) => typeof value4[key2] === "string");
}
function isRecord2(value4) {
  return value4 !== null && typeof value4 === "object" && !Array.isArray(value4);
}

// src/vega-lite.worker.ts
var scope = self;
var deny = () => Promise.reject(new Error("External loading is disabled"));
var denyLoader = {
  load: deny,
  sanitize: deny,
  http: deny,
  file: deny
};
var handled = false;
scope.addEventListener("message", (event2) => {
  if (handled) return;
  handled = true;
  void render(event2.data).then((response) => {
    scope.postMessage(response);
    scope.close();
  });
});
async function render(data4) {
  const id2 = isVegaWorkerRequest(data4) ? data4.id : "invalid";
  if (!isVegaWorkerRequest(data4)) return { kind: "failure", id: id2, error: "Invalid Vega worker request" };
  let view;
  try {
    const spec = validateVegaLiteSpec(data4.spec);
    const compiled = compile(spec, {
      config: {
        background: data4.palette.background,
        axis: {
          domainColor: data4.palette.border,
          gridColor: data4.palette.border,
          labelColor: data4.palette.muted,
          titleColor: data4.palette.foreground
        },
        legend: { labelColor: data4.palette.muted, titleColor: data4.palette.foreground },
        mark: { color: data4.palette.accent },
        title: { color: data4.palette.foreground },
        view: { stroke: data4.palette.border }
      }
    }).spec;
    validateCompiledVega(compiled);
    const runtime2 = parse7(compiled, {}, { ast: true });
    view = new View(runtime2, {
      expr: expression3,
      loader: denyLoader,
      renderer: "none"
    });
    await view.runAsync();
    const svg = await view.toSVG();
    return { kind: "success", id: id2, svg };
  } catch (error3) {
    return { kind: "failure", id: id2, error: error3 instanceof Error ? error3.message : "Vega-Lite rendering failed" };
  } finally {
    if (view !== void 0) view.finalize();
  }
}
